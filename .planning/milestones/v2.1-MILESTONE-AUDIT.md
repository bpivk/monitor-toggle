---
milestone: v2.1
audited: 2026-08-17T00:00:00Z
status: tech_debt
scores:
  requirements: 14/14
  phases: 5/5
  integration: 14/14
  flows: 5/5
gaps: {}
tech_debt:
  - phase: 20-custom-toggle-switch-control
    items:
      - "Focus-ring margin math has no dedicated regression test (found post-rig code review, fixed inline: the confirm-dialog lease race, CR-level; these 3 remain unfixed, Warning-level)"
      - "Keyboard-autorepeat could re-fire the toggle action under held Space/Enter"
      - "Two settings-loads run without a guard against a concurrent Save"
  - phase: 21-accent-color-reading-live-update
    items:
      - "Registry byte-order masking math (WR: accent color ARGB unpack) has no dedicated unit test — confirmed correct on rig, but unprotected against regression"
      - "AccentColorChanged diff uses Color equality instead of .ToArgb() comparison — functionally equivalent today, fragile if Color's equality semantics change"
  - phase: 22-settingsform-layout-pass
    items:
      - "WR-01/WR-02 (from 22-REVIEW.md) left unfixed as out-of-scope for a layout/resize-only phase — not re-verified in this audit, carried forward as-is"
  - phase: 23-manual-light-dark-override
    items:
      - "WR-01: SettingsForm_Load reads AppSettings.ThemeOverride without the same Enum.IsDefined validation OverridableThemeProvider.ReadPersistedOverride() applies — a corrupted persisted value leaves all three theme radios unchecked"
      - "WR-02: OnThemeRadioCheckedChanged calls _previewThemeOverride with no try/catch, the one theme touchpoint that breaks the otherwise-universal theming-must-never-crash convention"
      - "WR-03: SettingsForm's constructor doesn't call ApplyEffectiveColorMode the way MonitorConfirmDialog's does — currently masked only by MainForm's always-on live sync"
  - phase: cross-milestone
    items:
      - "MAIN-01/MAIN-02/SETTINGS-01/SETTINGS-02 are layout-only requirements verified structurally by the integration checker (code-level), not via live rendering in this audit pass — a light manual visual spot-check on the rig is a reasonable follow-up, not a blocker (all four already carry a rig-verified PASS from their own phase's VERIFICATION.md)"
---

# v2.1 Milestone Audit — Modern UI Redesign & Theme Backlog

**Audited:** 2026-08-17
**Status:** tech_debt (no blockers — all requirements satisfied, all phases verified, all integration wired; 10 tracked Warning-level findings across 4 phases, all deliberately deferred as out-of-scope-for-their-phase or pre-existing-and-rig-confirmed-correct)

## Requirements Coverage (3-Source Cross-Reference)

All 14 requirements cross-referenced against REQUIREMENTS.md traceability, each phase's VERIFICATION.md requirements table, and each SUMMARY.md's `requirements-completed` frontmatter.

| Requirement | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|---|---|---|---|---|---|
| TILE-01 | 19 | passed | listed (19-02, 19-05) | `[x]` (fixed this audit — was `[ ]`) | **satisfied** |
| TILE-02 | 19 | passed | listed (19-03, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| TILE-03 | 19 | passed | listed (19-03, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| TILE-04 | 19 | passed | listed (19-03, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| TILE-05 | 19 | passed | listed (19-05) | `[x]` (fixed this audit) | **satisfied** |
| TILE-06 | 19 | passed | listed (19-02, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| TILE-07 | 19 | passed | listed (19-04, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| MAIN-01 | 19 | passed | listed (19-02, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| MAIN-02 | 19 | passed | listed (19-02, 19-05) | `[x]` (fixed this audit) | **satisfied** |
| THEME-08 | 20 | passed | listed (20-01, 20-02, 20-03) | `[x]` | **satisfied** |
| THEME-07 | 21 | passed | listed (21-01, 21-02, 21-03) | `[x]` | **satisfied** |
| SETTINGS-01 | 22 | passed | listed (22-01, 22-02, 22-05) | `[x]` | **satisfied** |
| SETTINGS-02 | 22 | passed | listed (22-01, 22-02, 22-05) | `[x]` | **satisfied** |
| THEME-09 | 23 | passed | listed (23-01, 23-02, 23-03) | `[x]` | **satisfied** |

**Orphan detection:** None — every requirement in the traceability table is claimed by a plan and confirmed by its phase's VERIFICATION.md.

**Pre-existing bookkeeping gap found and fixed by this audit:** TILE-01..07 and MAIN-01/02 were validated by Phase 19 (verified `passed` on 2026-08-10, all 9 requirements confirmed in `19-05-SUMMARY.md`'s final requirement-mapping table) but their REQUIREMENTS.md checkboxes were never ticked — a `phase.complete` traceability-writer quirk, not a real gap. Fixed in commit `1a5129a` as part of this audit.

## Phase Verification Summary

| Phase | Status | Score | Rig-Verified | Notes |
|---|---|---|---|---|
| 19: Monitor-Tile Dashboard & MonitorPanelForm Retirement | passed | 9/9 requirements | Yes | Zero live code references to `MonitorPanelForm` remain (grep confirms only comment mentions) |
| 20: Custom Toggle-Switch Control | passed | THEME-08 | Yes, 2 fix rounds | Post-rig code review found and fixed a real concurrency race (confirm-dialog nested message loop wasn't holding the exclusive-access lease); 3 lower-severity findings tracked as tech debt |
| 21: Accent-Color Reading & Live Update | passed | THEME-07 | Yes, all 3 criteria | Registry byte-order question resolved (no swap needed); 2 non-blocking findings tracked as tech debt |
| 22: SettingsForm Layout Pass | passed | SETTINGS-01/02 | Yes, 2 gap-closure rounds + 1 post-rig code-review fix round | First rig pass failed outright (missing controls, dead resize); root-caused and fixed; second rig pass passed 17/17; post-rig review then found and fixed 2 further Critical bugs (working-area clamp chrome/monitor math), user-confirmed on real hardware as a follow-up UAT item |
| 23: Manual Light/Dark Override | passed | THEME-09 | Yes, 15/15 checks on first pass | Collapsed 3 independent `IsDark` copies into one `OverridableThemeProvider` resolver; the one open technical question (in-place repaint of an already-open Settings window) passed cleanly, no residual gap |

## Cross-Phase Integration

Spawned `gsd-integration-checker` against all 5 phases. Result: **14/14 requirements WIRED, 0 orphaned, 0 missing, 5/5 E2E flows complete, 0 blockers, 0 warnings.**

Key wiring confirmed:
- Phase 19's `MonitorTile` and Phase 20's `ToggleSwitch` both receive **live** accent-color updates from Phase 21's `IThemeProvider.AccentColor`/`AccentColorChanged` (not Phase 20's original placeholder color), routed through `MainForm.ApplyDashboardTheming()`'s two-call-site rule.
- Phase 23's radio group landed exactly in Phase 22's reserved `pnlThemeReserved` slot with zero reflow of anything else; Phase 22's layout invariants (no `Location=`, no `SizeType.Absolute`) still hold after Phase 23's edit.
- Phase 23's `OverridableThemeProvider` composition-root wiring in `Program.cs` reaches every theme consumer across the milestone — `MainForm`, `SettingsForm`, `MonitorConfirmDialog` — with no form reading a raw un-overridden `IThemeProvider.CurrentTheme` directly.
- Core toggle logic (monitor/audio/process control) shows no regression — this milestone was explicitly scoped as pure UI/UX and stayed that way.

Full integration checker output is not persisted as a separate file; findings are folded into this report per the standard audit flow.

## Nyquist Compliance

Skipped — `workflow.nyquist_validation` is disabled for this project.

## Tech Debt (10 items, 4 phases — see YAML frontmatter for the structured list)

All items above are non-blocking Warning-level findings from post-implementation code review, deliberately left unfixed at the time (either explicitly out-of-scope for that phase's stated boundary, or a hardening/defensive-coding gap around behavior that's already confirmed correct on real hardware). None contradict a stated `must_haves` truth in their originating plan. Recommended as backlog candidates for a future cleanup phase, not a blocker to closing v2.1.

## Verdict

**v2.1 is ready to complete.** All 14 requirements satisfied and rig-verified, all cross-phase integration wired, all 5 phases individually verified `passed`. The only action this audit took was a bookkeeping fix (9 stale checkboxes) — no functional gaps found.
