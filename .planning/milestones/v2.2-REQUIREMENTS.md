# Requirements Archive: v2.2 Auto-Update, Single-Instance Guard & Smaller Footprint

**Archived:** 2026-08-29
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Rig Toggle

**Defined:** 2026-08-18
**Core Value:** A single reliable action that disables the primary monitor (not just powers it off) and switches audio output — so games that mishandle secondary-monitor launches reliably open on the rig monitor — and just as reliably applies Normal mode's own explicitly configured monitor/audio state on toggle-back.

## v2.2 Requirements

Requirements for the "Auto-Update, Single-Instance Guard & Smaller Footprint" milestone. Each maps to roadmap phases.

### Auto-Update

- [x] **UPDATE-01**: Running exe carries a build-time version identifier reflecting the actual released git tag (infra prerequisite — `release.yml`/`.csproj` currently stamp no version anywhere)
- [x] **UPDATE-02**: On launch, app checks GitHub Releases for a version newer than the running build
- [x] **UPDATE-03**: When a newer version is found, user is prompted to confirm before anything is downloaded or applied
- [x] **UPDATE-04**: Confirming downloads the new release exe, applies it in place, and relaunches on the new version
- [x] **UPDATE-05**: A failed update apply (interrupted download, disk full, locked file) leaves the original exe intact and runnable — never stranding the app in a non-launchable state
- [x] **UPDATE-06**: A manual "Check for Updates" action lets the user trigger the same check on demand, independent of the automatic on-launch check
- [x] **UPDATE-07**: The update-apply relaunch always succeeds despite the new single-instance guard being active (the guard must not block the app's own internal relaunch)

### Single-Instance Guard

- [x] **INSTANCE-01**: Launching the app while an instance is already running does not start a second instance
- [x] **INSTANCE-02**: The already-running instance is brought to focus (shown/restored if minimized or tray-hidden) when a duplicate launch is attempted

### Performance

- [x] **PERF-03**: Self-contained exe is measurably smaller than the v2.1 baseline via additional safe MSBuild-level levers only (no IL trimming, no Native AOT, no `PublishReadyToRun`)

## Future Requirements

Deferred to future release. Tracked but not in current roadmap.

### Auto-Update

- **UPDATE-08**: WM_COPYDATA/named-pipe IPC between instances (richer than bare focus-and-exit, e.g. passing along CLI args from the second launch attempt to the first instance) — explicitly TRIG-02/TRIG-03 territory, permanently out of scope per v1.1 close; not reopened here

### Distribution

- **DIST-01**: Reconsidering Velopack or another packaging/installer framework — only relevant if a future milestone deliberately drops the standalone-.exe distribution constraint

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| UI modernization second pass (rounded corners, borderless chrome, motion/animation, Fluent iconography) | Explicitly raised and then explicitly dropped by the user for this milestone — v2.1's redesign stands as-is |
| Silent background auto-update (no confirm prompt) | User explicitly chose "notify then confirm" — a tool that controls display/audio routing on a sim-racing rig shouldn't auto-apply an update mid-session without a chance to notice/skip a bad release |
| Manual-check-only auto-update (no automatic on-launch check) | User explicitly chose automatic on-launch checking as the primary behavior; manual check (UPDATE-06) is an addition, not a replacement |
| Silent-exit or message-then-exit on duplicate launch | User explicitly chose "focus existing window" |
| Delta/differential update downloads | Meaningless overhead for a single-user tool checking its own GitHub repo |
| IL trimming (`PublishTrimmed=true`) or Native AOT (`PublishAot=true`) for exe size | Established project guidance: this codebase's COM interop (`IPolicyConfig`) and P/Invoke marshalling (`WindowsDisplayAPI`/CCD) code paths are exactly what static reachability analysis misidentifies as unreachable and strips; Native AOT relies on the same underlying analysis |
| `PublishReadyToRun` for exe size | Confirmed to increase self-contained exe size (adds native-compiled code images) — a startup-speed lever, not a size lever; wrong direction for this milestone's goal |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| UPDATE-01 | Phase 26 | Complete |
| UPDATE-02 | Phase 26 | Complete |
| UPDATE-03 | Phase 26 | Complete |
| UPDATE-04 | Phase 26 | Complete |
| UPDATE-05 | Phase 26 | Complete |
| UPDATE-06 | Phase 26 | Complete |
| UPDATE-07 | Phase 25 | Complete |
| INSTANCE-01 | Phase 25 | Complete |
| INSTANCE-02 | Phase 25 | Complete |
| PERF-03 | Phase 24 | Complete |

**Coverage:**

- v2.2 requirements: 10 total
- Mapped to phases: 10
- Unmapped: 0 ✓

---
*Requirements defined: 2026-08-18*
*Last updated: 2026-08-18 after v2.2 roadmap creation (Phases 24-26)*
