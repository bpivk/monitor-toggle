---
phase: 01-monitor-disable-feasibility-spike
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spike/RUN-INSTRUCTIONS.md
  - spike/RESULTS-TEMPLATE.md
  - spike/FALLBACK.md
autonomous: true
requirements: []   # Intentionally empty — throwaway spike, zero mapped REQ-IDs (per ROADMAP Phase 1 + planning context)
user_setup:
  - service: dotnet-sdk
    why: "RUN-INSTRUCTIONS.md is the user's copy-pasteable guide to install the SDK, build, and run the spike on the rig PC (D-02: only VS Code confirmed installed)."
    env_vars: []
    dashboard_config:
      - task: "Follow spike/RUN-INSTRUCTIONS.md on the rig PC to install .NET SDK 10.0.x, build, and run the tool, then fill in spike/RESULTS-TEMPLATE.md"
        location: "Rig PC — ordinary terminal for the tool; a SEPARATE elevated terminal only if the fallback in FALLBACK.md is needed"

must_haves:
  truths:
    - "The user has a self-contained, copy-pasteable guide covering .NET SDK install, project restore/build, and running each spike mode, plus exactly what output means PASS vs FAIL."
    - "The user has a fill-in-the-blanks results template that captures winver, the before/after display list from BOTH enumeration sources, the delayed re-check, and elevation observations — enough to record the ROADMAP go/no-go decision."
    - "The admin pnputil fallback is documented as a clearly separate, manually-invoked escalation path that runs in its own elevated terminal and is never triggered by the primary non-elevated tool."
    - "D-02: RUN-INSTRUCTIONS.md includes explicit .NET SDK installation/setup steps (not just build/run), because the user has only VS Code confirmed on the rig PC."
  artifacts:
    - path: "spike/RUN-INSTRUCTIONS.md"
      provides: "SDK install + build + run-per-mode + how-to-read-output guide"
      contains: "dotnet new console"
    - path: "spike/RESULTS-TEMPLATE.md"
      provides: "Fill-in go/no-go results capture template"
      contains: "go/no-go"
    - path: "spike/FALLBACK.md"
      provides: "Separate, manually-invoked elevated pnputil escalation path"
      contains: "pnputil /disable-device"
  key_links:
    - from: "spike/RUN-INSTRUCTIONS.md"
      to: "spike/MonitorDetachSpike (Program.cs modes)"
      via: "documents --list / --disable / --verify invocations"
      pattern: "--disable"
    - from: "spike/RESULTS-TEMPLATE.md"
      to: "ROADMAP Phase 1 Success Criteria #3"
      via: "records the go/no-go mechanism decision"
      pattern: "go/no-go"
---

<objective>
Produce the three user-facing documents that turn the spike tool (plan 01-01) into something the user can actually run on their Windows rig and report back from:
1. `spike/RUN-INSTRUCTIONS.md` — copy-pasteable install/build/run guide.
2. `spike/RESULTS-TEMPLATE.md` — a fill-in-the-blanks report the user completes after running, capturing everything needed to make the ROADMAP go/no-go decision.
3. `spike/FALLBACK.md` — the admin-only `pnputil` escalation path, documented as a clearly SEPARATE, manually-invoked mechanism (never auto-run by the primary tool, per D-08).

Purpose: The Linux sandbox cannot run Windows code, so this phase's real answer comes from the USER running the tool. These docs make that round-trip self-contained and unambiguous — the user only has VS Code confirmed installed (D-02) and needs SDK-install steps, not just build/run commands.

Output: three markdown files under `spike/`.

IMPORTANT — execution boundary: The executor writes markdown only. Do not run `dotnet` or any Windows command. Acceptance criteria are content assertions checkable by reading the files. Phase completion is gated on the USER running the tool per RUN-INSTRUCTIONS.md and reporting the filled-in RESULTS-TEMPLATE.md back in a follow-up interaction — that step is NOT a task in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monitor-disable-feasibility-spike/01-CONTEXT.md
@.planning/phases/01-monitor-disable-feasibility-spike/01-RESEARCH.md
@.planning/phases/01-monitor-disable-feasibility-spike/01-01-PLAN.md

<interfaces>
<!-- The spike tool's CLI contract (defined in plan 01-01 Program.cs). RUN-INSTRUCTIONS + RESULTS-TEMPLATE must match these exactly. -->
- `dotnet run -- --list`               // enumerate active display paths; user reads the index of the DisplayPort/primary monitor
- `dotnet run -- --disable <index>`    // detach that index; prints PASS/FAIL twice (immediate + after ~20s), then prompts Enter to restore
- `dotnet run -- --verify`             // print current active-path count + Screen.AllScreens.Length at any time
<!-- Scaffold commands (from 01-RESEARCH.md "Standard Stack" installation block) -->
- dotnet new console -n MonitorDetachSpike -f net10.0-windows
- dotnet add package WindowsDisplayAPI --version 1.3.0.13
<!-- SDK install (from 01-RESEARCH.md "Environment Availability") -->
- winget install --id Microsoft.DotNet.SDK.10 -e   (or manual: dotnet.microsoft.com/download/dotnet/10.0)
- dotnet --list-sdks   (verify)
<!-- Fallback (from 01-RESEARCH.md "Code Examples" pnputil block) — ELEVATED terminal only -->
- Get-PnpDevice -Class Monitor | Format-Table FriendlyName, InstanceId, Status
- pnputil /disable-device "<InstanceId>"   ;   pnputil /enable-device "<InstanceId>"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RUN-INSTRUCTIONS.md (install + build + run + read-output guide)</name>
  <files>spike/RUN-INSTRUCTIONS.md</files>
  <read_first>
    - .planning/phases/01-monitor-disable-feasibility-spike/01-RESEARCH.md (sections "Standard Stack" installation block, "Environment Availability", "Recommended Project Structure")
    - .planning/phases/01-monitor-disable-feasibility-spike/01-01-PLAN.md (the exact files produced and the three CLI modes)
    - .planning/phases/01-monitor-disable-feasibility-spike/01-CONTEXT.md (D-02 user has only VS Code; D-08 non-elevated)
  </read_first>
  <action>
    Create `spike/RUN-INSTRUCTIONS.md`. Structure it as ordered, copy-pasteable steps a user can follow on the rig PC from an ORDINARY (non-elevated) terminal:
    Step 0 — Confirm/install the .NET SDK: `dotnet --list-sdks`; if 10.0.x is absent, `winget install --id Microsoft.DotNet.SDK.10 -e` (or manual download from dotnet.microsoft.com/download/dotnet/10.0), then re-run `dotnet --list-sdks` to confirm a 10.0.x line appears. State that VS Code alone is NOT enough — the SDK is a separate install (D-02).
    Step 1 — Get the two source files onto the rig PC: note that `spike/MonitorDetachSpike/MonitorDetachSpike.csproj` and `spike/MonitorDetachSpike/Program.cs` come from this repo; instruct the user to place them in a folder named `MonitorDetachSpike`. Include the alternative scaffold-from-scratch commands `dotnet new console -n MonitorDetachSpike -f net10.0-windows` then `dotnet add package WindowsDisplayAPI --version 1.3.0.13` (then paste the provided Program.cs over the generated one and ensure the csproj has `<UseWindowsForms>true</UseWindowsForms>`), for the case where copying files is inconvenient.
    Step 2 — Restore & build: `dotnet build` from inside the `MonitorDetachSpike` folder; note first build restores the NuGet package (needs internet).
    Step 3 — Run each mode, in order, with a one-line explanation of each: `dotnet run -- --list` (read the printed list; note the index whose row shows the DisplayPort output technology and IsGDIPrimary=True — that is the monitor to disable, per D-04/D-05); `dotnet run -- --disable <index>` (substitute the index from --list); `dotnet run -- --verify` (check state at any time).
    Step 4 — How to read the output (PASS vs FAIL): PASS = the tool prints PASS on BOTH the immediate check and the ~20-second delayed re-check, meaning the monitor is gone from WindowsDisplayAPI's re-query AND from `Screen.AllScreens` count. FAIL = either check still sees the monitor, OR the monitor merely goes black/blank while the counts do not drop (the exact Pitfall-A failure mode to watch for). Tell the user to also glance at Windows Display Settings and whether the taskbar/desktop reflowed. Note that `--disable` restores automatically after they press Enter, and that if it does NOT restore they can re-run and use `--verify`, or reboot (topology changes are not persisted by the tool). TROUBLESHOOTING: if the IMMEDIATE check PASSes but the ~20-second DELAYED re-check FAILs (the monitor reappears — Pitfall C / DisplayPort hotplug re-detection), instruct the user to try closing or stopping the AMD Software (Adrenalin) background service and re-run `dotnet run -- --disable <index>`; if that changes the result it points to an AMD-software interference layer rather than the driver itself (see 01-RESEARCH Open Question 2).
    Step 5 — What to capture and report back: point the user to `spike/RESULTS-TEMPLATE.md`, and remind them to also run `winver` and record the Windows build (Open Question 3). State explicitly: do NOT run this tool "as administrator" — it must stay non-elevated so the elevation conclusion is valid (D-08); the admin fallback is a separate document (`spike/FALLBACK.md`) to use only if the primary approach FAILs.
    Keep every command in fenced code blocks so it is directly copy-pasteable. Do not embed C# source in this file.
  </action>
  <acceptance_criteria>
    - `spike/RUN-INSTRUCTIONS.md` exists.
    - Contains `dotnet --list-sdks` and a winget or manual SDK install command (`winget install` and/or `dotnet.microsoft.com/download`).
    - Contains `dotnet new console` and `dotnet add package WindowsDisplayAPI --version 1.3.0.13`.
    - Contains `dotnet build`.
    - Contains all three run invocations: `--list`, `--disable`, `--verify`.
    - Contains both the words `PASS` and `FAIL` in a section explaining how to read output.
    - Contains `winver`.
    - Contains an explicit instruction NOT to run the tool elevated / as administrator, and a pointer to `RESULTS-TEMPLATE.md` and `FALLBACK.md`.
    - Contains a troubleshooting note naming the AMD Software / Adrenalin background service as a diagnostic step for the delayed-recheck (Pitfall C) failure mode.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/bpivk/moza && grep -q 'dotnet --list-sdks' spike/RUN-INSTRUCTIONS.md && grep -qiE 'winget install|dotnet.microsoft.com/download' spike/RUN-INSTRUCTIONS.md && grep -q 'dotnet new console' spike/RUN-INSTRUCTIONS.md && grep -q 'WindowsDisplayAPI --version 1.3.0.13' spike/RUN-INSTRUCTIONS.md && grep -q 'dotnet build' spike/RUN-INSTRUCTIONS.md && grep -q -- '--list' spike/RUN-INSTRUCTIONS.md && grep -q -- '--disable' spike/RUN-INSTRUCTIONS.md && grep -q -- '--verify' spike/RUN-INSTRUCTIONS.md && grep -q 'PASS' spike/RUN-INSTRUCTIONS.md && grep -q 'FAIL' spike/RUN-INSTRUCTIONS.md && grep -q 'winver' spike/RUN-INSTRUCTIONS.md && grep -qi 'administrator' spike/RUN-INSTRUCTIONS.md && grep -q 'RESULTS-TEMPLATE.md' spike/RUN-INSTRUCTIONS.md && grep -q 'FALLBACK.md' spike/RUN-INSTRUCTIONS.md && grep -qi 'adrenalin' spike/RUN-INSTRUCTIONS.md && echo RUNINSTR_OK</automated>
  </verify>
  <done>RUN-INSTRUCTIONS.md is a self-contained, copy-pasteable install→build→run→interpret guide that keeps the tool non-elevated and points to the results template and fallback doc.</done>
</task>

<task type="auto">
  <name>Task 2: Write RESULTS-TEMPLATE.md (fill-in-the-blanks go/no-go report)</name>
  <files>spike/RESULTS-TEMPLATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 "Success Criteria" — especially #1 removed-from-enumeration, #3 documented go/no-go mechanism, #4 elevation confirmed)
    - .planning/phases/01-monitor-disable-feasibility-spike/01-RESEARCH.md (sections "Common Pitfalls" A/C, "Open Questions", "Assumptions Log" A1-A4)
    - .planning/phases/01-monitor-disable-feasibility-spike/01-01-PLAN.md (what output the tool prints)
  </read_first>
  <action>
    Create `spike/RESULTS-TEMPLATE.md` as a fill-in-the-blanks report the user completes after running the spike. Include labeled blank fields (use `___` or checkbox `- [ ]` markers) for: Environment — `winver` build string, GPU/driver (AMD Radeon model + Adrenalin version), the target monitor's connection (DisplayPort per D-05) and its FriendlyName/DevicePath from `--list`. Before-disable state — count and list of monitors from BOTH sources (WindowsDisplayAPI `--list` count AND `Screen.AllScreens.Length`, reported by `--verify`). Disable result — the tool's IMMEDIATE PASS/FAIL line (verbatim), the ~20-second DELAYED re-check PASS/FAIL line (verbatim; call out Pitfall C — did the monitor reappear after the delay?), whether the monitor merely went black vs actually disappeared from enumeration (Pitfall A), and whether Windows reflowed the taskbar/desktop. Restore result — did pressing Enter restore the original monitor count/position/primary. Elevation observation — did any UAC prompt appear when running the tool from an ordinary terminal (expected: NO, per Assumption A4 / D-08); confirm the tool was NOT run as administrator. Then a decision section with three explicit checkboxes mapping to ROADMAP Success Criterion #3: `- [ ] GO — SetDisplayConfig topology-path-removal (primary, non-elevated) works reliably`, `- [ ] GO (with fallback) — primary approach failed; pnputil /disable-device admin fallback (see FALLBACK.md) works — NOTE: this makes Phase 4 elevation-isolation mandatory (Assumption A1)`, `- [ ] NO-GO — neither mechanism removes the monitor from enumeration on this hardware`, plus a free-text "Chosen mechanism for Phase 4" line and a "Notes / anomalies" block. Include the literal phrase `go/no-go` in a heading. Add a short header sentence stating this filled-in file is what the user returns to record the phase's decision.
  </action>
  <acceptance_criteria>
    - `spike/RESULTS-TEMPLATE.md` exists.
    - Contains the literal string `go/no-go`.
    - Contains fields referencing both enumeration sources: `Screen.AllScreens` and `--list` (or WindowsDisplayAPI).
    - Contains `winver` and a driver/Adrenalin field and `DisplayPort`.
    - Contains references to both the immediate and the delayed/20-second re-check.
    - Contains all three decision options: `GO`, `NO-GO`, and a fallback option mentioning `pnputil`.
    - Contains the plain `- [ ] GO —` checkbox line as a distinct option (not merely as a substring of `NO-GO`).
    - Contains an elevation/UAC observation field (`UAC` or `administrator`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/bpivk/moza && grep -q 'go/no-go' spike/RESULTS-TEMPLATE.md && grep -q 'Screen.AllScreens' spike/RESULTS-TEMPLATE.md && grep -qE -- '--list|WindowsDisplayAPI' spike/RESULTS-TEMPLATE.md && grep -q 'winver' spike/RESULTS-TEMPLATE.md && grep -qi 'adrenalin' spike/RESULTS-TEMPLATE.md && grep -q 'DisplayPort' spike/RESULTS-TEMPLATE.md && grep -qiE '20[ -]?second|delayed' spike/RESULTS-TEMPLATE.md && grep -q 'NO-GO' spike/RESULTS-TEMPLATE.md && grep -qE '\- \[ \] GO —' spike/RESULTS-TEMPLATE.md && grep -q 'pnputil' spike/RESULTS-TEMPLATE.md && grep -qiE 'UAC|administrator' spike/RESULTS-TEMPLATE.md && echo RESULTS_OK</automated>
  </verify>
  <done>RESULTS-TEMPLATE.md captures environment, before/after dual-source enumeration, immediate+delayed checks, restore, elevation observation, and a three-way go/no-go decision tied to ROADMAP Success Criterion #3.</done>
</task>

<task type="auto">
  <name>Task 3: Write FALLBACK.md (separate, manually-invoked admin escalation path)</name>
  <files>spike/FALLBACK.md</files>
  <read_first>
    - .planning/phases/01-monitor-disable-feasibility-spike/01-RESEARCH.md (sections "Code Examples" pnputil block, "Supporting" table fallback row, "State of the Art" devcon→pnputil, Pitfall D)
    - .planning/phases/01-monitor-disable-feasibility-spike/01-CONTEXT.md (D-08 elevation isolation)
  </read_first>
  <action>
    Create `spike/FALLBACK.md`. Open with a bold statement that this is a SEPARATE, MANUALLY-INVOKED escalation path to try ONLY if the primary non-elevated tool prints FAIL (monitor blanks but stays in enumeration), and that it must be run from a SEPARATELY-OPENED elevated (admin) PowerShell/cmd window — never by running the spike `.exe` as administrator, because elevating the spike itself would invalidate the elevation conclusion and could hide a UIPI problem that affects the real app later (Pitfall D / D-08 / the Moza Companion window-focus feature in Phase 3). Then give the steps: (1) in an ordinary or elevated terminal, `Get-PnpDevice -Class Monitor | Format-Table FriendlyName, InstanceId, Status` to find the target monitor's InstanceId; (2) in an ELEVATED terminal, `pnputil /disable-device "<InstanceId>"`; (3) re-verify from the ordinary (non-elevated) spike terminal with `dotnet run -- --verify` to confirm the count dropped in BOTH sources; (4) re-enable with `pnputil /enable-device "<InstanceId>"` (also elevated). Add a note that `devcon.exe` is deprecated by Microsoft and must NOT be hunted down from third-party mirrors — `pnputil` ships with Windows and is the recommended replacement. Add a note that if this fallback is what works, that outcome makes Phase 4's elevated-helper-process isolation mandatory rather than optional (Assumption A1), and the user should record that under "GO (with fallback)" in RESULTS-TEMPLATE.md. Keep commands in fenced code blocks; no C# in this file.
  </action>
  <acceptance_criteria>
    - `spike/FALLBACK.md` exists.
    - Contains `pnputil /disable-device` and `pnputil /enable-device`.
    - Contains `Get-PnpDevice -Class Monitor`.
    - States the fallback runs in a separate elevated terminal and that the spike `.exe` must NOT itself be run as administrator (contains `administrator` and `elevated`).
    - Contains `dotnet run -- --verify` for re-checking after the manual disable.
    - Mentions `devcon` is deprecated / not to be used.
    - References the "GO (with fallback)" outcome or Phase 4 elevation isolation.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/bpivk/moza && grep -q 'pnputil /disable-device' spike/FALLBACK.md && grep -q 'pnputil /enable-device' spike/FALLBACK.md && grep -q 'Get-PnpDevice -Class Monitor' spike/FALLBACK.md && grep -qi 'administrator' spike/FALLBACK.md && grep -qi 'elevated' spike/FALLBACK.md && grep -q 'dotnet run -- --verify' spike/FALLBACK.md && grep -qi 'devcon' spike/FALLBACK.md && grep -qiE 'fallback|Phase 4' spike/FALLBACK.md && echo FALLBACK_OK</automated>
  </verify>
  <done>FALLBACK.md documents the admin pnputil path as a distinct, manually-invoked escalation run in its own elevated terminal, never auto-triggered by the primary tool, with re-verification and Phase-4 implications noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented admin command → OS PnP subsystem | FALLBACK.md instructs a human to run an elevated `pnputil /disable-device` against a device node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | documented fallback path | mitigate | FALLBACK.md explicitly scopes elevation to a separate, manually-opened admin terminal and forbids running the spike `.exe` elevated (D-08 / Pitfall D); the primary tool stays asInvoker. No elevation is baked into any shipped artifact. |
| T-02-02 | Tampering (supply chain) | deprecated `devcon.exe` | mitigate | FALLBACK.md directs users to built-in `pnputil` and explicitly warns against downloading `devcon.exe` from third-party mirrors. |
</threat_model>

<verification>
All verification for THIS plan is markdown content assertion only (see each task's `<verify>`) — the executor writes docs and does not run Windows commands.

Phase-level functional verification (does the monitor actually leave enumeration on the rig hardware?) is performed by the USER following RUN-INSTRUCTIONS.md and recorded in RESULTS-TEMPLATE.md. Phase 1's go/no-go decision (ROADMAP Success Criteria #1-#4) is gated on that user-reported result in a follow-up interaction, not on this plan completing.
</verification>

<success_criteria>
- All three docs exist and pass their content assertions.
- A user with only VS Code installed can, from RUN-INSTRUCTIONS.md alone, install the SDK, build, run all three modes, and interpret PASS/FAIL.
- RESULTS-TEMPLATE.md captures enough (dual-source before/after, delayed re-check, elevation, winver) to record a defensible go/no-go decision mapped to ROADMAP Success Criterion #3.
- FALLBACK.md keeps the admin path strictly separate and manually invoked (D-08).
</success_criteria>

<output>
Create `.planning/phases/01-monitor-disable-feasibility-spike/01-02-SUMMARY.md` when done.
</output>
