---
phase: 13-tray-app-icon-redesign
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.App/RigToggle.App.csproj
autonomous: true
requirements: [ICON-03, ICON-04]

must_haves:
  truths:
    - "The tray NotifyIcon requests the DPI-correct frame at load time instead of always getting the smallest (16px) frame"
    - "The compiled RigToggle.App.exe carries app.ico as its native Win32 icon (taskbar/Explorer/Alt-Tab identity) (D-06 exe icon reuses normal-mode motif)"
    - "normal.ico and rig.ico keep their existing LogicalName embedded-resource loading path unchanged"
  artifacts:
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "DPI-correct tray icon construction via SystemInformation.SmallIconSize"
      contains: "SystemInformation.SmallIconSize"
    - path: "src/RigToggle.App/RigToggle.App.csproj"
      provides: "<ApplicationIcon> wiring for app.ico"
      contains: "ApplicationIcon"
  key_links:
    - from: "src/RigToggle.App/MainForm.cs (LoadTrayIconsIfNeeded)"
      to: "System.Drawing.Icon(stream, SystemInformation.SmallIconSize)"
      via: "sized Icon constructor at both normal.ico and rig.ico load sites"
      pattern: "SystemInformation\\.SmallIconSize"
    - from: "src/RigToggle.App/RigToggle.App.csproj"
      to: "Resources\\app.ico"
      via: "<ApplicationIcon> MSBuild property"
      pattern: "<ApplicationIcon>"
---

<objective>
Complete ICON-03 and ICON-04 on the runtime/build side: fix the two size-less `new Icon(...)` calls in `MainForm.LoadTrayIconsIfNeeded()` so the tray icon actually uses the DPI-correct frame (not the always-smallest 16px frame), and add the `<ApplicationIcon>` MSBuild property so `app.ico` becomes the compiled `.exe`'s native taskbar/Explorer icon.

Purpose: The multi-frame `.ico` files from Plan 13-01 accomplish NOTHING for DPI sharpness at the tray until the loading code requests a size — this is the critical 13-RESEARCH.md Pitfall 1 fix, which directly contradicts (and supersedes) 13-UI-SPEC.md's Scope Notes claim that "no code changes needed in MainForm.cs's loading path". Trust RESEARCH.md here. The `<ApplicationIcon>` property is the separate, native-Win32-resource mechanism that satisfies ICON-04's exe/taskbar requirement.
Output: Two small edits (`MainForm.cs`, `RigToggle.App.csproj`) that consume the Plan 13-01 assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tray-app-icon-redesign/13-RESEARCH.md
@.planning/phases/13-tray-app-icon-redesign/13-UI-SPEC.md
@.planning/phases/13-tray-app-icon-redesign/13-PATTERNS.md

<interfaces>
<!-- Current state of the two files being edited (from 13-PATTERNS.md, verified against the repo). -->
- MainForm.LoadTrayIconsIfNeeded() (MainForm.cs ~lines 231-249): loads normal.ico/rig.ico
  via GetManifestResourceStream, then `new System.Drawing.Icon(stream ?? throw ...)` with NO
  size argument at both sites. The IN-01 null-check-with-descriptive-exception convention must
  be preserved exactly; only the constructor gains a second argument.
- RigToggle.App.csproj: top <PropertyGroup> has OutputType/TargetFramework/UseWindowsForms/
  RuntimeIdentifier/ImplicitUsings/Nullable and NO <ApplicationIcon>. The EmbeddedResource
  <ItemGroup> for normal.ico/rig.ico (LogicalName) must stay unchanged.
- Required app.ico produced by Plan 13-01 at src/RigToggle.App/Resources/app.ico.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tray-icon DPI frame selection in MainForm.cs (Pitfall 1)</name>
  <files>src/RigToggle.App/MainForm.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (the full LoadTrayIconsIfNeeded method, ~lines 225-249, plus the field declarations for _normalIcon/_rigIcon)
    - 13-RESEARCH.md Pitfall 1 (lines 373-379) — the verified size-less-constructor-returns-smallest-frame defect and the SystemInformation.SmallIconSize fix
    - 13-PATTERNS.md MainForm.cs section (lines 143-183) — exact before/after
  </read_first>
  <action>
    In `LoadTrayIconsIfNeeded()`, change both `new System.Drawing.Icon(stream ?? throw ...)` calls to pass a second argument `SystemInformation.SmallIconSize`, i.e. `new System.Drawing.Icon(normalStream ?? throw new InvalidOperationException("Embedded resource 'normal.ico' not found."), SystemInformation.SmallIconSize)` and the equivalent for `rigStream`/`'rig.ico'`. `SystemInformation.SmallIconSize` reflects the OS's current small-icon metric which scales with DPI (100%→16, 125%→20, 150%→24, 200%→32 — exactly the four frames Plan 13-01 embeds), so `NotifyIcon` receives the correctly-sized frame instead of the always-smallest one.
    Preserve everything else in the method verbatim: the `_normalIcon is not null && _rigIcon is not null` early-return caching guard (08-RESEARCH.md Pitfall 3 GDI-handle-leak avoidance), the `using` streams, and the IN-01 explicit null-check-with-descriptive-exception convention (do NOT switch to the null-forgiving `!` operator). Only the constructor call gains the `SystemInformation.SmallIconSize` argument.
    Do NOT reference `app.ico` anywhere in this file — that icon is embedded as a native Win32 resource via `<ApplicationIcon>` (Task 2) and is read by the OS shell, never by app code (13-RESEARCH.md Anti-Patterns, line 357).
  </action>
  <verify>
    <automated>grep -c "SystemInformation.SmallIconSize" src/RigToggle.App/MainForm.cs; grep -c "app.ico" src/RigToggle.App/MainForm.cs; dotnet build src/RigToggle.App/RigToggle.App.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SystemInformation.SmallIconSize" src/RigToggle.App/MainForm.cs` prints `2` (both tray icon load sites fixed)
    - `grep -c "app.ico" src/RigToggle.App/MainForm.cs` prints `0` (no runtime reference to the exe icon)
    - MainForm.cs still contains both `InvalidOperationException("Embedded resource 'normal.ico' not found.")` and `...'rig.ico'...` messages (IN-01 convention preserved)
    - MainForm.cs still contains the `if (_normalIcon is not null && _rigIcon is not null)` early-return caching guard
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0
  </acceptance_criteria>
  <done>Both tray Icon constructors request SystemInformation.SmallIconSize; the caching guard and IN-01 null-check are intact; the project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire app.ico as the exe/taskbar icon via <ApplicationIcon></name>
  <files>src/RigToggle.App/RigToggle.App.csproj</files>
  <read_first>
    - src/RigToggle.App/RigToggle.App.csproj (full file — confirm no existing <ApplicationIcon> and preserve the EmbeddedResource block)
    - 13-RESEARCH.md Pattern 4 (lines 332-353) — <ApplicationIcon> semantics and why no separate ItemGroup is needed
    - 13-PATTERNS.md RigToggle.App.csproj section (lines 121-139)
    - Confirm src/RigToggle.App/Resources/app.ico exists (produced by Plan 13-01)
  </read_first>
  <action>
    Add a single line `<ApplicationIcon>Resources\app.ico</ApplicationIcon>` inside the existing top `<PropertyGroup>` of `RigToggle.App.csproj` (alongside OutputType/TargetFramework/etc.). No separate `<ItemGroup>`/`Content`/`None` entry is needed — the property alone passes app.ico to the Win32 resource compiler (`/win32icon`), embedding it as an `RT_GROUP_ICON`/`RT_ICON` native resource that Explorer, Alt-Tab, and the taskbar resolve per-DPI on their own.
    Leave the existing `EmbeddedResource`/`LogicalName` `<ItemGroup>` for `normal.ico` and `rig.ico` completely unchanged — app.ico uses a structurally different (native-resource) embedding mechanism and must NOT be added as an EmbeddedResource. Leave the `asInvoker`/no-elevation-manifest comment and all other properties untouched.
  </action>
  <verify>
    <automated>grep -c "<ApplicationIcon>Resources\\\\app.ico</ApplicationIcon>" src/RigToggle.App/RigToggle.App.csproj; grep -c "LogicalName>normal.ico" src/RigToggle.App/RigToggle.App.csproj; dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - RigToggle.App.csproj contains exactly one `<ApplicationIcon>Resources\app.ico</ApplicationIcon>` element inside the top PropertyGroup
    - The existing `<EmbeddedResource Include="Resources\normal.ico">` with `<LogicalName>normal.ico</LogicalName>` and the rig.ico equivalent are still present and unchanged
    - app.ico is NOT added as an `<EmbeddedResource>` (grep confirms no `LogicalName>app.ico` and no `EmbeddedResource Include="Resources\app.ico"`)
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release` exits 0 (the Win32 resource compiler accepted app.ico — a malformed app.ico would fail here, an early/safe failure point)
  </acceptance_criteria>
  <done>app.ico is embedded as the executable's native Win32 icon via <ApplicationIcon>; the tray icons' embedding path is untouched; Release build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in .ico assets → shipped .exe | app.ico is compiled into the binary as a native resource; normal.ico/rig.ico are embedded managed resources loaded at runtime. No external input crosses either boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of Service (self-inflicted) | Missing/malformed app.ico at compile time | mitigate | depends_on 13-01 guarantees app.ico exists and round-trip-verified before this plan runs; a malformed app.ico fails the Win32 resource compiler at `dotnet build` (Task 2 verify) — an earlier/safer failure than a runtime exception |
| T-13-05 | Tampering / availability | The DPI fix regresses tray icon load or the GDI-handle-leak-avoidance caching | mitigate | Task 1 preserves the `_normalIcon is not null && _rigIcon is not null` caching guard and the IN-01 null-check verbatim; only the constructor argument changes; `dotnet build` gate confirms compilation |
| T-13-06 | Spoofing / correctness | app.ico accidentally loaded via the managed EmbeddedResource path (wrong mechanism) | mitigate | Task 1 asserts `grep -c app.ico MainForm.cs` == 0; Task 2 asserts app.ico is not an EmbeddedResource |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package-manager installs in this phase (13-RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `grep -c "SystemInformation.SmallIconSize" src/RigToggle.App/MainForm.cs` == 2.
- RigToggle.App.csproj contains `<ApplicationIcon>Resources\app.ico</ApplicationIcon>` and the unchanged normal.ico/rig.ico EmbeddedResource block.
- `dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release` exits 0.
- No runtime code references app.ico.
</verification>

<success_criteria>
- The tray NotifyIcon loads the DPI-correct frame (ICON-03 runtime half complete).
- The compiled exe carries app.ico as its native taskbar/Explorer icon (ICON-04 wiring half complete).
- The existing tray embedded-resource loading path and IN-01/caching conventions are preserved.
</success_criteria>

<output>
Create `.planning/phases/13-tray-app-icon-redesign/13-02-SUMMARY.md` when done.
</output>
