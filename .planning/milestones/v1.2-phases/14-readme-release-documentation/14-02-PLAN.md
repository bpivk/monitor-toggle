---
phase: 14-readme-release-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DOCS-02, DOCS-03]
must_haves:
  truths:
    - "D-03: The GitHub repo bpivk/monitor-toggle is publicly viewable (badges render for outside visitors)"
    - "D-04: v1.0 and v1.1 exist as published GitHub Releases with notes carried from their annotated tags (notes only, no binary)"
  artifacts: []
  key_links:
    - from: "gh release create v1.0/v1.1"
      to: "existing annotated git tags v1.0/v1.1"
      via: "--notes-from-tag --verify-tag"
      pattern: "gh release create v1.[01] --notes-from-tag --verify-tag"
    - from: "gh repo edit --visibility public"
      to: "shields.io license + release badges"
      via: "public repo makes GitHub API readable to shields.io/outside viewers"
      pattern: "isPrivate.*false"
---

<objective>
Perform the two one-time GitHub state changes that make the README's badges and download flow real: flip the repo to public, and backfill v1.0/v1.1 as GitHub Releases from their existing annotated tags.

Purpose: Per CONTEXT.md D-03 (repo currently private — breaks badge rendering for outside viewers, contradicts a "GitHub-ready README") and D-04 (no GitHub Release has ever been published despite v1.0/v1.1 tags existing; backfill notes-only, no binaries — originals not retained). These are locked decisions, executed as real `gh` CLI commands, not documented-for-later. Supports DOCS-03 (license + latest-release badges need a public repo and ≥1 release) and DOCS-02 (a real release is the download target).
Output: bpivk/monitor-toggle is public; v1.0 and v1.1 appear in the Releases tab. No repo files changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-readme-release-documentation/14-CONTEXT.md
@.planning/phases/14-readme-release-documentation/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flip secret scan (Information-disclosure guard)</name>
  <files></files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-CONTEXT.md (D-03 — deliberate public flip; the user has accepted exposing repo source/history)
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Runtime State Inventory — no secrets tier; this is a guard confirming that)
  </read_first>
  <action>Before making the repo public, scan the working tree for accidentally-committed secrets/credentials so the visibility flip does not leak anything. Grep the tracked files (respect .gitignore; do not scan bin/obj/publish output) for high-signal secret patterns: API keys, tokens, `password=`, `secret=`, `-----BEGIN * PRIVATE KEY-----`, AWS-style `AKIA` prefixes, and `.pfx`/`.snk` signing material. This project is a local WinForms utility with settings stored at runtime in `%APPDATA%\RigToggle\settings.json` (outside the repo), so a clean result is expected. If any real secret is found, STOP and surface it rather than proceeding to the public flip.</action>
  <verify>
    <automated>git grep -nIE "BEGIN [A-Z ]*PRIVATE KEY|AKIA[0-9A-Z]{16}|ghp_[0-9A-Za-z]{30,}|xox[baprs]-[0-9A-Za-z-]{10,}|(password|passwd|secret|api[_-]?key|access[_-]?token)[\"' ]*[:=][\"' ]*[^\"'\s]{8,}" -- . ':(exclude).planning/**' && echo "REVIEW: potential secrets above — do not flip until confirmed benign" || echo "PASS: no obvious committed secrets"</automated>
  </verify>
  <acceptance_criteria>
    - The secret-pattern git grep produces no true-positive credential (test fixtures / doc examples like the MIT text or placeholder env names are acceptable and must be visually confirmed benign)
    - If a real secret were present, the plan stops before Task 2 rather than flipping visibility
  </acceptance_criteria>
  <done>Working tree confirmed free of committed secrets; safe to make the repo public.</done>
</task>

<task type="auto">
  <name>Task 2: Flip repo bpivk/monitor-toggle to public (D-03)</name>
  <files></files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-CONTEXT.md (D-03)
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Code Examples — gh repo visibility flip line 274-278; Pitfall 3 — the required --accept-visibility-change-consequences flag)
  </read_first>
  <action>Run the verified command `gh repo edit bpivk/monitor-toggle --visibility public --accept-visibility-change-consequences`. Both flags are required together — per RESEARCH Pitfall 3, `gh repo edit --visibility public` alone fails demanding the consequences flag; do not treat that as a transient error and retry without it. This is a one-time setting flip using the already-authenticated `gh` CLI (auth confirmed present with repo scope). Do not open the web UI.</action>
  <verify>
    <automated>gh repo view bpivk/monitor-toggle --json isPrivate --jq '.isPrivate' | grep -qx false && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view bpivk/monitor-toggle --json isPrivate` returns `isPrivate: false`
    - Command completed with both `--visibility public` and `--accept-visibility-change-consequences` flags
  </acceptance_criteria>
  <done>The repo is publicly viewable; shields.io and outside visitors can read repo/release API state for badge rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill v1.0 and v1.1 GitHub Releases from annotated tags (D-04)</name>
  <files></files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-CONTEXT.md (D-04 — backfill notes-only, no binaries; v1.2 deferred to milestone close)
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Code Examples — gh release backfill lines 280-285; both tags confirmed annotated with substantive messages)
  </read_first>
  <action>Publish GitHub Releases for the two existing annotated tags, notes-only, no binary attached (D-04: original build artifacts were not retained). Run `gh release create v1.0 --notes-from-tag --verify-tag --repo bpivk/monitor-toggle` then `gh release create v1.1 --notes-from-tag --verify-tag --repo bpivk/monitor-toggle`. `--notes-from-tag` reuses each annotated tag's existing multi-line message verbatim (both confirmed annotated via git cat-file); `--verify-tag` aborts if a tag isn't on the remote. Do NOT pass any asset/file argument — these two releases are intentionally notes-only. Do NOT create a v1.2 release in this phase — v1.2 is deferred to milestone close per D-04 and RESEARCH Open Question #1. If a release for a tag already exists, that is acceptable (idempotent intent) — confirm it exists rather than failing the plan.</action>
  <verify>
    <automated>gh release list --repo bpivk/monitor-toggle | grep -q "v1.0" && gh release list --repo bpivk/monitor-toggle | grep -q "v1.1" && ! gh release list --repo bpivk/monitor-toggle | grep -q "v1.2" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh release list --repo bpivk/monitor-toggle` lists both `v1.0` and `v1.1`
    - Neither release has a binary asset attached (`gh release view v1.0 --json assets --jq '.assets | length'` returns `0`; same for v1.1)
    - No `v1.2` release exists yet (deferred to milestone close)
  </acceptance_criteria>
  <done>v1.0 and v1.1 appear in the Releases tab with tag-message notes; the latest-release badge and DOCS-02 download narrative have a real target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private repo → public repo | Flipping visibility exposes all source + git history to the internet |
| Local operator → GitHub API | Authenticated `gh` CLI mutates repo settings and creates releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | Repo visibility flip (D-03) | mitigate | Task 1 secret-scan gate runs before the flip; a found real secret stops the plan. User has knowingly accepted exposing source/history (D-03 locked decision). |
| T-14-04 | Repudiation / Tampering | Backfilled release notes | accept | `--notes-from-tag` reuses immutable annotated-tag messages verbatim; no hand-edited notes to drift or misattribute. Low value, personal project. |
| T-14-05 | Elevation of Privilege | `gh` CLI token scope | accept | One-time operator-run commands using an already-authenticated token with repo scope; no new credentials created or stored. |
</threat_model>

<verification>
- `gh repo view ... --json isPrivate` → false
- `gh release list` shows v1.0 and v1.1, not v1.2
- v1.0/v1.1 releases have zero attached assets
- Secret scan produced no true-positive credentials before the flip
</verification>

<success_criteria>
- Repo bpivk/monitor-toggle is public
- v1.0 and v1.1 published as notes-only GitHub Releases
- No v1.2 release created (deferred), no binaries attached to backfilled releases
</success_criteria>

<output>
Create `.planning/phases/14-readme-release-documentation/14-02-SUMMARY.md` when done
</output>
