---
phase: 12-theme-infrastructure-live-theme-following
plan: 06
type: execute
wave: 2
gap_closure: true
depends_on: ["12-05"]
files_modified: []
autonomous: false
requirements: [THEME-03, THEME-04, THEME-05]

must_haves:
  truths:
    - "A Release self-contained single-file exe is produced from the 12-05 fixes for rig testing"
    - "On the Windows 11 rig, the title bar renders dark in dark mode on MainForm and SettingsForm (the check that FAILED in 12-04)"
    - "On the rig, all buttons render dark-themed in dark mode on all three forms — at rest AND on hover/pressed (the check that FAILED in 12-04)"
    - "On the rig, both SettingsForm audio-device combos render dark in dark mode (the check that FAILED in 12-04)"
    - "The three fixed surfaces also update live when the Windows theme is flipped while the app runs"
  artifacts:
    - path: "src/RigToggle.App/bin/Release/net10.0-windows/win-x64/publish/RigToggle.App.exe"
      provides: "Self-contained single-file exe embodying the 12-05 gap fixes"
      contains: ""
  key_links:
    - from: "12-05 code fixes"
      to: "rig human-verify checklist (3 previously-failing checks)"
      via: "build + publish then run on Windows 11"
      pattern: ""
---

<objective>
Re-run the rig verification that failed in 12-04, focused strictly on the three previously-failing checks now addressed by plan 12-05: dark title bar (THEME-03), dark buttons (THEME-05), and dark audio ComboBoxes (THEME-04). This project's precedent (Phases 8, 9, 11, and 12-04 itself) is that WinForms rendered appearance and live theme-flip behavior can only be confirmed on real Windows 11 hardware — the automated goal-backward verifier compares code artifacts against must_haves, not rendered pixels, so it structurally cannot catch a "the code sets the attribute but the surface still renders light" regression.

Purpose: Confirm the 12-05 fixes actually close THEME-03/04/05 on the rig, so the phase can finally be marked complete in REQUIREMENTS.md.
Output: A pass/fail rig result. On pass → phase complete. On fail → a fresh gap-closure pass with the new observed detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-theme-infrastructure-live-theme-following/12-04-SUMMARY.md
@.planning/phases/12-theme-infrastructure-live-theme-following/12-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and publish the app for rig testing</name>
  <files>src/RigToggle.App/bin/Release/net10.0-windows/win-x64/publish/RigToggle.App.exe</files>
  <read_first>
    - .planning/phases/12-theme-infrastructure-live-theme-following/12-04-SUMMARY.md (Task 1 — the exact build + publish commands and expected artifact that were used for the previous rig session)
    - CLAUDE.md (Standalone .exe Packaging: self-contained, PublishSingleFile, PublishTrimmed left false)
  </read_first>
  <action>
    Run `dotnet build RigToggle.sln -c Release` (must be 0 errors) then `dotnet publish src/RigToggle.App -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true` to produce the current self-contained single-file exe from the 12-05 fixes. Do NOT add `-p:PublishTrimmed=true` (CLAUDE.md: trimming strips the COM/P-Invoke marshalling paths this app depends on). Confirm the publish output path `src/RigToggle.App/bin/Release/net10.0-windows/win-x64/publish/RigToggle.App.exe` exists and report its size. This build/publish runs fine on this environment; the exe is copied to the Windows 11 rig by the user for Task 2.
  </action>
  <verify>
    <automated>dotnet build RigToggle.sln -c Release &amp;&amp; test -f src/RigToggle.App/bin/Release/net10.0-windows/win-x64/publish/RigToggle.App.exe || dotnet publish src/RigToggle.App -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build RigToggle.sln -c Release` completes with 0 errors.
    - `dotnet publish ... -r win-x64 --self-contained true -p:PublishSingleFile=true` completes and `RigToggle.App.exe` exists at the publish path above.
    - No `-p:PublishTrimmed=true` was used.
  </acceptance_criteria>
  <done>A current Release self-contained single-file exe embodying the 12-05 fixes exists at the publish path, ready for the rig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 12-05 addressed the three rig failures from 12-04: (1) DwmTitleBar now explicitly sets DWMWA_USE_IMMERSIVE_DARK_MODE so the title bar can go dark; (2) all 7 buttons switched from FlatStyle.System to explicit-color FlatStyle.Flat theming via ThemeApplier.ThemeButton — including explicit dark hover/pressed colors (BorderSize=0 + MouseOverBackColor/MouseDownBackColor) that work around dotnet/winforms#13897 so the interactive states also stay dark; (3) the two audio-device ComboBoxes are now themed via ThemeApplier.ThemeComboBox. All three re-apply on live theme flip.
  </what-built>
  <how-to-verify>
    Copy `RigToggle.App.exe` from the publish path to the Windows 11 rig and run it. With Windows set to DARK mode, verify the three previously-failing checks:
    1. TITLE BAR (THEME-03): MainForm's title bar is DARK, not white. Open Settings (button) — SettingsForm's title bar is also DARK. Open the monitor-confirm dialog if reachable — its title bar is DARK too.
    2. BUTTONS (THEME-05): On MainForm the Toggle + Settings buttons render with a dark fill and light text (not white/light). On SettingsForm the Browse…, Save Settings, and Discard Changes buttons are dark-themed. On the confirm dialog, Continue + Cancel are dark-themed. Then HOVER over and CLICK-AND-HOLD each button — the highlight (hover) and pressed shading must ALSO stay dark-themed, not flash a light/white color (this is the exact dotnet/winforms#13897 failure mode the 12-05 fix targets; a light hover/pressed flash means the workaround did not take).
    3. AUDIO COMBOS (THEME-04): In Settings, the Normal and Rig audio-device dropdowns show a dark background with light text in their closed (selected-value) state, not stock white.
    4. LIVE FLIP: With the app open, flip Windows Settings → Personalization → Colors from Dark to Light and back. Confirm the title bar, buttons (including hover/pressed), and audio combos all recolor without an app restart, on whichever forms are open.
    Also do a quick sanity pass that nothing the rig previously confirmed regressed: the monitor grid, hotkey box states, Panel layout, Mica/rounded corners, and light-mode appearance all still look correct.
  </how-to-verify>
  <resume-signal>Type "approved" if all three previously-failing checks now pass (title bar, buttons at rest AND on hover/pressed, audio combos, live-flip); otherwise describe exactly what still renders light/wrong and on which control/form/theme/interaction-state so a follow-up gap pass can target it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host → Windows 11 rig | The verified appearance only exists on the rig; the build host cannot render WinForms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-VERIFY | Repudiation | Rig appearance claims | mitigate | Human rig checkpoint is the authoritative record of the three fixed surfaces; a false "pass" is prevented by an explicit per-check, per-form, per-interaction-state checklist (including button hover/pressed) rather than a single yes/no. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; verification-only plan (no code changes). |
</threat_model>

<verification>
- Task 1 build + publish succeed and the exe exists.
- Task 2 rig checklist confirms title bar, buttons (rest + hover + pressed), and audio combos are dark in dark mode on all three forms, and all three recolor on a live theme flip.
- On any failure, capture the exact control/form/theme/interaction-state so a follow-up `--gaps` pass can target it precisely.
</verification>

<success_criteria>
- The three previously-failing rig checks (THEME-03 title bar, THEME-05 buttons incl. hover/pressed, THEME-04 audio combos) now pass on Windows 11.
- Live theme flip updates all three surfaces without restart.
- No regression in the surfaces 12-04 already confirmed working.
- On pass, THEME-03/04/05 can be marked satisfied and Phase 12 completed.
</success_criteria>

<output>
Create `.planning/phases/12-theme-infrastructure-live-theme-following/12-06-SUMMARY.md` when done, recording the rig result. On pass, note that THEME-03/04/05 are confirmed and Phase 12 is complete. On fail, record the exact observed detail for a follow-up gap pass.
</output>
</content>
