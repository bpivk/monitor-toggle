---
phase: 06-multi-monitor-data-model-controller-generalization
plan: 05
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/RigToggle.App/MonitorConfirmDialog.Designer.cs
  - src/RigToggle.App/MonitorConfirmDialog.cs
  - src/RigToggle.App/MainForm.cs
autonomous: true
requirements: [DISPLAY-07]
must_haves:
  truths:
    - "The pre-disable confirmation dialog names every monitor being disabled AND every monitor being enabled, comma-separated, no truncation (D-06)"
    - "MainForm resolves both sets' friendly names via GetAllMonitors() so enable-set (inactive) monitors still resolve"
    - "The dialog message adapts to disable-only, enable-only, and both cases"
  artifacts:
    - path: "src/RigToggle.App/MonitorConfirmDialog.cs"
      provides: "multi-name confirmation message construction from two name lists"
      contains: "FormatNames"
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "confirmation call site resolving both sets via GetAllMonitors and passing two name lists"
      contains: "GetAllMonitors"
  key_links:
    - from: "MainForm.BtnToggle_Click"
      to: "IMonitorController.GetAllMonitors"
      via: "resolve disable + enable friendly names (enable-set is inactive at confirm-time)"
      pattern: "GetAllMonitors"
    - from: "MainForm"
      to: "MonitorConfirmDialog(disableNames, enableNames)"
      via: "two-list constructor"
      pattern: "new MonitorConfirmDialog\\("
---

<objective>
Generalize the pre-disable confirmation dialog (DISPLAY-07/D-06): `MonitorConfirmDialog` must name every monitor being disabled AND every monitor being enabled in a full, comma-separated, non-truncated list; and `MainForm` must resolve both sets' friendly names via `GetAllMonitors()` (an enable-set monitor is inactive at confirm-time, so `GetActiveMonitors()` would fail to resolve it and silently degrade the wording).

Purpose: The user's informed-consent moment must accurately describe the full topology change, not just "the configured monitor."
Output: Updated `MonitorConfirmDialog.Designer.cs` (layout + title), `MonitorConfirmDialog.cs` (two-list message), and `MainForm.cs` (name resolution + two-list construction + minor copy fix for D-07).

ENVIRONMENT: `RigToggle.App` is `net10.0-windows` (no non-Windows build). After Plan 02 the App project compiles only once BOTH this plan and Plan 04 land. Verify via the source assertions in each `<acceptance_criteria>`; full App build + dialog behavior are validated at the Plan 06 rig checkpoint. System-default WinForms styling only (no custom colors/fonts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-CONTEXT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md

<interfaces>
IMonitorController (from Plan 02): GetAllMonitors() -> IReadOnlyList<MonitorInfo>.
AppSettings (from Plan 01): List<string>? MonitorsToDisable, List<string>? MonitorsToEnable.

Current MonitorConfirmDialog (src/RigToggle.App/MonitorConfirmDialog.cs):
  ctor(string monitorFriendlyName) -> lblMessage.Text = $"This will disable \"{...}\" (primary). Continue?"
  public bool DontAskAgain => chkDontAskAgain.Checked;   // KEEP

Current MainForm confirmation call site (src/RigToggle.App/MainForm.cs lines 84-102):
  settings = _settingsStore.Load();
  if (!settings.SkipMonitorConfirmation) {
     monitor = _monitorController.GetActiveMonitors().FirstOrDefault(m => m.DevicePath == settings.MonitorDevicePath);
     name = monitor?.FriendlyName ?? "the configured monitor";
     using var confirmDialog = new MonitorConfirmDialog(name);
     ... ShowDialog / DontAskAgain / Save ...
  }
  IsSettingsConfigured() guard message (lines 71-77) references "monitor, both audio devices, and the companion app".

Current MonitorConfirmDialog.Designer.cs coordinates:
  lblMessage (12,12) size (360,48); chkDontAskAgain (12,64) size (200,24);
  btnContinue (184,100) size (90,32); btnCancel (282,100) size (90,32);
  ClientSize (384,144); Text = "Disable Monitor?".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MonitorConfirmDialog multi-name message + layout/title (D-06)</name>
  <files>src/RigToggle.App/MonitorConfirmDialog.cs, src/RigToggle.App/MonitorConfirmDialog.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/MonitorConfirmDialog.cs (current single-name ctor)
    - src/RigToggle.App/MonitorConfirmDialog.Designer.cs (current coordinates + title)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md (Confirmation Dialog Spec: message construction, exact layout coordinates table, title copy)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-CONTEXT.md (D-06 exact example wording)
  </read_first>
  <action>
    Change the `MonitorConfirmDialog` constructor signature to `MonitorConfirmDialog(IReadOnlyList<string> disableNames, IReadOnlyList<string> enableNames)`. Build the message per 06-UI-SPEC.md exactly: a private static `FormatNames(IReadOnlyList<string> names)` returning `string.Join(", ", names.Select(n => $"\"{n}\""))`; a `clauses` list adding `$"disable {FormatNames(disableNames)}"` only if `disableNames.Count > 0` and `$"enable {FormatNames(enableNames)}"` only if `enableNames.Count > 0`; then `lblMessage.Text = $"This will {string.Join(" and ", clauses)}. Continue?"`. Keep `public bool DontAskAgain => chkDontAskAgain.Checked;` and the `AcceptButton`/`CancelButton` wiring unchanged. Preserve the "pure display data, no Core interface injected" doc-comment convention (generalize its wording to both sets). Do NOT truncate or add "and N more" logic (D-06).
    In `MonitorConfirmDialog.Designer.cs`, apply the UI-SPEC coordinates: `lblMessage` size (360,72) (was 48); `chkDontAskAgain` -> (12,88); `btnContinue` -> (184,124); `btnCancel` -> (282,124); `ClientSize` -> (384,168); change `this.Text` from "Disable Monitor?" to "Confirm Monitor Changes?". All other properties unchanged.
  </action>
  <verify>
    <automated>grep -q "IReadOnlyList<string> disableNames, IReadOnlyList<string> enableNames" src/RigToggle.App/MonitorConfirmDialog.cs && grep -q "FormatNames" src/RigToggle.App/MonitorConfirmDialog.cs && grep -q "Confirm Monitor Changes?" src/RigToggle.App/MonitorConfirmDialog.Designer.cs && grep -q "new System.Drawing.Size(384, 168)" src/RigToggle.App/MonitorConfirmDialog.Designer.cs</automated>
  </verify>
  <acceptance_criteria>
    - Constructor takes two `IReadOnlyList<string>` name lists; old single-string ctor is gone.
    - Message uses `FormatNames` + `string.Join(" and ", clauses)` and only includes a clause for a non-empty set (adapts to disable-only / enable-only / both).
    - No truncation / "and N more" logic anywhere in the file.
    - Designer: `lblMessage` height 72, `ClientSize` (384,168), title "Confirm Monitor Changes?"; `btnContinue`/`btnCancel` y=124.
    - `DontAskAgain` property preserved.
  </acceptance_criteria>
  <done>The dialog builds a full, non-truncated multi-name message for both sets and its layout/title fit the longer sentence.</done>
</task>

<task type="auto">
  <name>Task 2: MainForm resolves both sets via GetAllMonitors + two-list construction + D-07 copy fix</name>
  <files>src/RigToggle.App/MainForm.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (BtnToggle_Click confirmation block 84-102, IsSettingsConfigured guard message 71-77)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md (MainForm section: GetActiveMonitors -> GetAllMonitors switch, both-sets resolution)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md (Confirmation Dialog Spec: resolve via GetAllMonitors)
  </read_first>
  <action>
    In the `!settings.SkipMonitorConfirmation` block: replace the single `GetActiveMonitors().FirstOrDefault(...)` lookup with a `GetAllMonitors()` call captured once into a local list, then resolve `disableNames` = for each device path in `settings.MonitorsToDisable ?? new()`, the matching `MonitorInfo.FriendlyName` (fallback to the raw device path, or a generic placeholder if truly unresolvable — reuse the existing `?? "the configured monitor"` defensive spirit); resolve `enableNames` the same way from `settings.MonitorsToEnable ?? new()`. It is CRITICAL to use `GetAllMonitors()` (not `GetActiveMonitors()`) because an enable-set monitor is inactive at confirm-time and could never resolve otherwise (DISPLAY-07 would silently degrade). Construct `new MonitorConfirmDialog(disableNames, enableNames)`. Keep the `ShowDialog`/`DontAskAgain`/`Save` flow unchanged. Wrap the `GetAllMonitors()` call defensively (if it throws, fall back to using the raw device paths as names) so a monitor-enumeration hiccup never blocks the confirmation.
    Also soften the `IsSettingsConfigured()` guard message (lines 71-77) from "...configuring Settings (monitor, both audio devices, and the companion app)..." to "...configuring Settings (at least one monitor to disable or enable, both audio devices, and the companion app)..." to match D-07 (an enable-only config is valid).
  </action>
  <verify>
    <automated>grep -q "GetAllMonitors" src/RigToggle.App/MainForm.cs && grep -q "new MonitorConfirmDialog(" src/RigToggle.App/MainForm.cs && grep -q "at least one monitor to disable or enable" src/RigToggle.App/MainForm.cs && ! grep -q "MonitorConfirmDialog(name)" src/RigToggle.App/MainForm.cs</automated>
  </verify>
  <acceptance_criteria>
    - Confirmation resolves names via `GetAllMonitors()` (not `GetActiveMonitors()` in the confirmation block): `grep -c "GetAllMonitors" src/RigToggle.App/MainForm.cs` >= 1.
    - `new MonitorConfirmDialog(` is called with two name-list arguments (disable + enable), not a single string.
    - Names are resolved for BOTH `settings.MonitorsToDisable` and `settings.MonitorsToEnable`, keyed by `DevicePath`, with a defensive fallback when unresolvable and when `GetAllMonitors()` throws.
    - The `IsSettingsConfigured` guard message contains "at least one monitor to disable or enable".
  </acceptance_criteria>
  <done>MainForm resolves and passes both disable-set and enable-set friendly names (via GetAllMonitors) to the two-list dialog, and its guard copy reflects D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings sets → confirmation display | Device paths resolved to friendly names for a display-only consent dialog (no mutation here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure / Misleading UI | Confirmation silently omits enable-set (or a name) it cannot resolve, understating the change the user consents to | mitigate | Resolve via `GetAllMonitors()` (sees inactive enable-set monitors); fall back to the raw device path rather than dropping the entry, so every affected monitor is always represented (DISPLAY-07 "names every monitor"). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; dialog is a built-in WinForms Form. |
</threat_model>

<verification>
- Source assertions in each task's `<acceptance_criteria>` (portable).
- On the Windows rig (with Plan 04 landed): full `RigToggle.App` builds; dialog wording for disable-only/enable-only/both verified in the Plan 06 rig checkpoint.
</verification>

<success_criteria>
- DISPLAY-07: the confirmation names every disabled AND every enabled monitor, no truncation, resolved even for inactive enable-set monitors.
- D-07: MainForm's configuration guard copy no longer implies a single required monitor.
</success_criteria>

<output>
Create `.planning/phases/06-multi-monitor-data-model-controller-generalization/06-05-SUMMARY.md` when done.
</output>
