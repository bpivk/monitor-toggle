---
phase: 06-multi-monitor-data-model-controller-generalization
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05"]
files_modified: []
autonomous: false
requirements: [DISPLAY-04, DISPLAY-05]
must_haves:
  truths:
    - "On the real rig, a disabled monitor survives sleep/wake or reboot and re-enables at a sane resolution"
    - "A combined disable-set + enable-set topology applies in one Monitor step leaving exactly one GDI primary and no position overlap"
    - "The full RigToggle.App self-contained build compiles and runs on Windows after Plans 03/04/05"
  artifacts: []
  key_links: []
---

<objective>
Execute the mandatory Phase 6 rig-validation completion gate (ROADMAP.md / 06-CONTEXT.md), mirroring v1.0 Phase 1's spike-first discipline. This phase is NOT "done" when code compiles and unit tests pass — the two CCD scenarios below are unvalidated by documentation alone (06-RESEARCH.md confidence MEDIUM on combined-topology sequencing; Assumptions A1/A2) and require hands-on rig hardware as a go/no-go gate.

Purpose: Empirically confirm the two load-bearing CCD behaviors the automated tests structurally cannot exercise (no live display hardware in CI), before Phase 6 is marked complete.
Output: A human go/no-go decision recorded in this plan's SUMMARY.

This plan is a checkpoint only — it modifies no files. It depends on Plans 03 (Windows adapter), 04 (Settings grid), and 05 (confirmation dialog) all being complete so the full `RigToggle.App` compiles and runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-CONTEXT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Build and run the app on the Windows rig, then validate the two CCD gate scenarios</name>
  <what-built>
    Plans 01-05 delivered: plural monitor-set data model + silent v1.0 migration (01), the generalized `IMonitorController` triad + orchestration ordering (02), the real CCD `GetAllMonitors`/`ActivateMonitors`/`DeactivateMonitors` + N-generalized `Restore` verify (03), the multi-select Settings grid with DISPLAY-06 validation (04), and the multi-monitor confirmation dialog (05).
  </what-built>
  <how-to-verify>
    Preconditions (run on the Windows rig, not the Linux planning box):
    1. Build everything green: `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` (cross-platform tests) AND `dotnet build src/RigToggle.App/RigToggle.App.csproj` AND `dotnet test src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj` (overlap-helper tests). All must pass/compile.
    2. Publish/run the app (self-contained per CLAUDE.md packaging), open Settings, and confirm the new grid lists every monitor (including any currently OS-disabled rig monitor, shown with the "(currently OS-disabled)" suffix).

    Migration check (DISPLAY-08 — quick, do first):
    3. Place a genuine v1.0-era `settings.json` (only `MonitorDevicePath`/`MonitorFriendlyName` + audio + app path, no plural fields) at `%LocalAppData%\RigToggle\settings.json`, open Settings, confirm that monitor is already checked in the Disable column with no prompt/banner.

    GATE SCENARIO (a) — long-idle/reboot re-enable:
    4. Configure a disable-set of one monitor; Switch to Rig Mode (confirm the monitor detaches at the OS level, not just powers off).
    5. Sleep/wake OR reboot the rig PC.
    6. Switch back to Normal Mode; confirm the previously-disabled monitor is still enumerable and comes back at a sane resolution (its prior resolution, not a fallback like 1024x768).

    GATE SCENARIO (b) — combined disable+enable topology in one operation:
    7. Configure a disable-set AND an enable-set (the enable-set containing a monitor normally kept OS-disabled), using the real configured sets.
    8. Switch to Rig Mode; confirm in one Monitor step: the enable-set monitor becomes active, the disable-set monitor detaches, EXACTLY ONE monitor is the GDI primary, and there is NO position/bounding-box overlap between active monitors (Windows Display Settings shows a clean arrangement).
    9. Switch back to Normal Mode; confirm the disable-set monitor is restored to its exact prior position/primary state AND every enable-set monitor is returned to OS-disabled (D-02).

    Record PASS/FAIL for each of scenario (a) and (b) as an explicit go/no-go. If either fails, this is a no-go gate — capture the failure detail (which assertion, any thrown `InvalidOperationException` message) for a gap-closure plan rather than marking the phase complete.
  </how-to-verify>
  <resume-signal>Type "approved: (a) PASS, (b) PASS" (or describe which scenario failed and the observed behavior / error message).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Validation-only checkpoint; introduces no code and no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Denial of Service | A combined-topology bug leaves the rig with zero/overlapping active displays after a real toggle | mitigate | This checkpoint IS the mitigation — a hands-on go/no-go gate that catches exactly this before the phase ships; the adapter's verify-and-throw + overlap check (Plan 03) surfaces it as a reported failure rather than a silent bad state. |
</threat_model>

<verification>
- Both gate scenarios (a) and (b) return an explicit PASS, plus the DISPLAY-08 migration spot-check.
- All three build/test commands succeed on the rig.
</verification>

<success_criteria>
- Scenario (a) PASS: disabled monitor survives sleep/wake or reboot and re-enables at a sane resolution.
- Scenario (b) PASS: combined disable+enable topology yields exactly one GDI primary, no overlap, and correct D-02 teardown on toggle-back.
- If either fails: recorded as a no-go with failure detail for gap closure (phase NOT marked complete).
</success_criteria>

<output>
Create `.planning/phases/06-multi-monitor-data-model-controller-generalization/06-06-SUMMARY.md` recording the go/no-go decision and per-scenario results.
</output>
