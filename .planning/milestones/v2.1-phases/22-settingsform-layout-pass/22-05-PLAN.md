---
phase: 22-settingsform-layout-pass
plan: 05
type: execute
wave: 2
depends_on: ["22-04"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [SETTINGS-01, SETTINGS-02]

must_haves:
  truths:
    - "On real Windows hardware, both the Normal and the Rig mode column render their monitor DataGridView and their audio device ComboBox — not just caption and explain text (closes rig Check 1, the SETTINGS-01 gap)"
    - "On real Windows hardware, dragging the Settings window's right or bottom edge actually resizes the window, with no flicker-then-snap-back (closes rig Check 3, the D-06 regression)"
    - "Each mode's monitor grid and audio picker read as one visual group, and the shared section below reads as one coherent group, with consistent spacing (SETTINGS-02)"
    - "Growing the window taller makes the two monitor grids taller rather than leaving a dead empty band, and growing it wider widens both columns evenly (rig Check 3d — proves the Percent rows kept in Plan 04 do their job)"
    - "Shrinking the window stops at the content floor rather than clipping or overlapping controls (rig Check 3c — proves the new MinimumSize works)"
    - "The 14 checks that were never reached in the failed session (2, 4-17) are each individually exercised and reported, including the full grid/overlap/button-text/resize set repeated at 125% and 150% display scale"
    - "No behaviour regressed while fixing the layout: drag-drop still works on the whole app-path box, validation icons still render un-clipped, live theme flip still re-themes every control, and tab order still reads as deliberate"
  artifacts:
    - path: ".planning/phases/22-settingsform-layout-pass/22-05-SUMMARY.md"
      provides: "The published binary path, a per-check PASS/FAIL table for all 17 checks in the order run, and an explicit per-criterion verdict for SETTINGS-01 and SETTINGS-02 naming the check numbers that evidence each"
      contains: "SETTINGS-01"
  key_links:
    - from: ".planning/phases/22-settingsform-layout-pass/22-05-SUMMARY.md"
      to: ".planning/phases/22-settingsform-layout-pass/22-VERIFICATION.md"
      via: "each of the two recorded gaps answered by the specific rig check that closes it"
      pattern: "Check 1|Check 3"
    - from: ".planning/phases/22-settingsform-layout-pass/22-05-SUMMARY.md"
      to: ".planning/ROADMAP.md"
      via: "explicit per-criterion verdict replacing the BLOCKED annotation on the Phase 22 line"
      pattern: "SETTINGS-0[12]"
---

<objective>
Re-run Phase 22's rig-hardware verification against the Plan 04 fix, starting from the two checks that failed (Check 1 — grid and audio picker missing; Check 3 — window will not resize) and then completing the 14 checks the failed session never reached.

Purpose: Phase 22 cannot close on source evidence. Both of its ROADMAP success criteria are currently recorded FAIL from a real-hardware session, and the two defects live in WinForms' own layout resolution — a class of failure that static analysis structurally cannot detect and this headless Linux environment structurally cannot render. Plan 04's fix is well-reasoned but explicitly unproven; one of its two components (the `MinimumSize` floor) exists specifically because the exact failure mechanism could not be pinned down. Only the user's own eyes on the rig can settle it.
Output: `22-05-SUMMARY.md` carrying the published binary path, the 17-check result table, and a per-criterion verdict for SETTINGS-01 and SETTINGS-02. No source changes.
</objective>

<hard_constraints>
1. **This plan makes NO source changes.** If a check fails, record it in full detail and stop. Remediation is another gap-closure plan, not an edit smuggled into a verification plan. `git status --porcelain src/` being empty is an acceptance criterion.
2. **The checkpoint is blocking and must not be auto-approved.** `workflow.auto_advance` does not apply. Every item in it is a property that cannot be observed in this environment, and this is the second attempt at exactly this verification — an inferred pass here would repeat the failure mode that produced `22-VERIFICATION.md`.
3. **Checks 1 and 3 run first, before anything else.** They are the two recorded gaps. If either still fails, note it and continue through the remaining checks anyway this time — the failed session stopped at Check 3 and left 14 checks unexercised, which is why a second full round trip is now needed. Gathering the rest of the data even under a partial failure avoids a third.
4. **Do not mark the phase done without the user's reported per-check result.** An inferred, assumed, or "should work" verdict does not satisfy D-03's accepted tradeoff, and this phase has already demonstrated once that a SUMMARY's `requirements-completed` claim can be flatly wrong.
5. **Do not weaken or drop a check to make the set pass.** All 17 are the same checks `22-03-PLAN.md` specified; the only change is their order.
</hard_constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-settingsform-layout-pass/22-CONTEXT.md
@.planning/phases/22-settingsform-layout-pass/22-UI-SPEC.md
@.planning/phases/22-settingsform-layout-pass/22-VERIFICATION.md
@.planning/phases/22-settingsform-layout-pass/22-03-SUMMARY.md
@.planning/phases/22-settingsform-layout-pass/22-04-PLAN.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
**Publish command for the rig binary** (same as the failed session, per `22-03-PLAN.md`):

`dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`

The produced single-file executable's exact path must be recorded in the SUMMARY. Windows display scale changes require **relaunching the app**, not just reopening the Settings window — the process reads its DPI at start (`AutoScaleMode.Font`, no per-monitor DPI manifest).

**What changed since the binary the user last tested** (Plan 04, two files, five property changes plus one new method):
- `pnlMonitor` and `pnlMonitorNormal` — the two bordered mode boxes — now measure their own content instead of reporting a stale default `Panel` size to their layout cell. This is the suspected direct cause of Check 1's collapse.
- `tlpModeColumns` gained a `MinimumSize` height floor of 280px at 100% scale (DPI-scaled automatically), as a backstop that makes the collapse impossible even if the diagnosis above is wrong.
- `Form.AutoSize` is now off. It was rewriting the window's bounds on every layout pass, which is what ate the edge-drag.
- The window's initial size is now computed once at load from `tlpRoot.PreferredSize`, clamped to the screen's working area, with `MinimumSize` set from the result. Still fully content-driven — no hardcoded dimension.
- Nothing else changed. No control moved, no row or column style changed, no validation, theming, save/load, or wording touched.

**Four things that look different but are intentional** (unchanged from the last session's framing — re-state them so a deliberate difference is never reported as a defect):
1. Normal is on the left, Rig on the right — the reverse of the pre-Phase-22 build.
2. The separate "Audio Devices" box is gone; each mode's picker lives in that mode's column.
3. The debug-logging checkbox caption is one line instead of two.
4. The window has neither a maximize nor a minimize button.

**Two behaviours that are new since the last session and are also intentional:**
5. The window can now be dragged *smaller* only down to its content size, then it stops. That is the fix for "shrinking must not clip," not a stuck window.
6. The window no longer resizes itself when a warning label appears. If content grows past the window, drag it larger. Check 9 is where this is judged.

**Requirement wording being verified** (`.planning/REQUIREMENTS.md`):
- SETTINGS-01 — SettingsForm has no overlapping or crowded controls at its default window size.
- SETTINGS-02 — Related controls (each mode's monitor grid, audio device pickers, app path, hotkey capture) are visually grouped and consistently spaced.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Rig-hardware re-verification of both Phase 22 success criteria after the gap-closure fix</name>
  <files>.planning/phases/22-settingsform-layout-pass/22-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/22-settingsform-layout-pass/22-VERIFICATION.md — the two `gaps:` entries; these define what Checks 1 and 3 must close
    - .planning/phases/22-settingsform-layout-pass/22-03-SUMMARY.md — Task 2's 17-check result table from the failed session, so the same checks are asked the same way and the results are directly comparable
    - .planning/phases/22-settingsform-layout-pass/22-04-PLAN.md — the `<root_cause_analysis>` section, so the recorded fallback is named correctly if Check 1 still fails
    - .planning/phases/22-settingsform-layout-pass/22-04-SUMMARY.md — what was actually changed, to describe the delta to the user accurately
    - .planning/phases/22-settingsform-layout-pass/22-UI-SPEC.md — the Layout Structure Contract and Spacing Scale, the intended result the user is judging against
    - .planning/research/PITFALLS.md — Pitfall 9, the milestone-level statement of the DPI risk these checks exist to close
  </read_first>
  <action>
    Publish the rig binary with the command in the interfaces block and record its exact output path. Present the `<what-built>` framing and all 17 checks in `<how-to-verify>` to the user verbatim, including the six "intentional, not a bug" items from the interfaces block. Make no source change of any kind.

    Wait for the user's per-check response. Do not infer, assume, or substitute a "should work" verdict for any check (hard constraint 4).

    When results come back, write `22-05-SUMMARY.md` containing: the published binary path and the Windows build used; a 17-row PASS/FAIL table in the order run, with the user's note per check; explicit per-criterion verdicts for SETTINGS-01 and SETTINGS-02 naming the check numbers that evidence each; and a direct answer to each of the two gaps recorded in `22-VERIFICATION.md`.

    If Check 1 still fails, record exactly what is visible and what is not, then name — do not implement — the fallback already recorded in `22-04-PLAN.md`'s `<root_cause_analysis>`: eliminate the intermediate wrapper `Panel`s entirely by hosting `tlpNormalColumn`/`tlpRigColumn` directly in the `tlpModeColumns` cells with `BorderStyle.FixedSingle` and `Padding=12` carried onto the tables themselves (both are `Panel` subclasses and render an identical THEME-05 flat border). If Check 3 still fails, record whether the drag is refused entirely or snaps back, since those point at different causes.

    If every check passes, also update `.planning/ROADMAP.md`: remove the `(BLOCKED — rig verification FAILED 2026-08-15, gap-closure plan required)` annotation from the Phase 22 line and tick the phase and both new plan entries.
  </action>
  <what-built>
    You tested this window on 2026-08-15 and two things were broken: the monitor grid and the audio dropdown were completely missing from both the Normal and Rig boxes (only the headings and the grey explanation text showed), and dragging the window's edge did nothing except flicker.

    Both have been diagnosed and fixed. Two separate causes:

    **The missing grid and dropdown.** The two bordered boxes (Normal Mode, Rig Mode) were not measuring their own contents — they were reporting a stale default size to the layout, roughly 100 pixels tall. That is just about enough room for the heading and the explanation line, and nothing else, which is exactly what you saw. They now measure properly. On top of that, a hard minimum height has been set on the row those two boxes live in, so even if that diagnosis is incomplete, the row physically cannot be laid out shorter than the content needs. Belt and braces, because this cannot be tested anywhere but on your machine.

    **The dead resize.** The window had `AutoSize` turned on, which makes a form recompute and rewrite its own size on every layout pass. Your drag was being overwritten the instant you made it — hence the flicker with no resize. `AutoSize` is now off at the window level. The window still sizes itself to its content when it opens (that was the point of it), but it now does so once, deliberately, at load, instead of continuously. It also now has a floor: you can drag it smaller until it reaches the size its content actually needs, then it stops rather than clipping things off.

    Nothing else changed. No control moved, no grouping changed, no validation, saving, loading, theming, or wording was touched. The only files edited were the Settings window's layout file and one new sizing method in its code-behind.

    **Six things that will look different and are intentional, not bugs:**
    1. Normal is on the left, Rig on the right (the reverse of the old build).
    2. There is no separate "Audio Devices" box any more — each mode's audio dropdown is inside that mode's own box.
    3. The debug-logging checkbox caption is one line instead of two.
    4. There is no maximize button and no minimize button.
    5. The window stops shrinking at a certain point. That is the new floor, not a stuck window.
    6. The window no longer resizes itself when a warning appears. If content grows past the window, drag it bigger. Check 9 is where you judge whether that feels right.

    **What needs your eyes:** everything. This build environment is Linux with no Windows GUI, so nothing above has been rendered even once. The last round of this went out on a confident-sounding source analysis and came back with two hard failures, so please treat every check below as genuinely open.
  </what-built>
  <how-to-verify>
    Publish and run on the rig: `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`. Work through the checks in the order given and report each as PASS or FAIL with a short note. Changing Windows display scale requires **relaunching the app**, not just reopening the window.

    **First — the two that failed last time. Do these before anything else.**

    1. **Grid and audio picker actually render (closes the SETTINGS-01 gap).** Open Settings at 100% scale. In the **Normal Mode** box on the left, confirm you can see: the "Normal Mode" heading, the explanation text, **the monitor grid with its Monitor / Off / On columns**, and **the "Normal:" audio dropdown**. Then confirm the same four things in the **Rig Mode** box on the right. Report each box separately. This is the check that failed outright last time — last time you reported "both modes only have explanations and monitor and audio are both missing."

    3. **Manual resize actually works (closes the D-06 regression).** Drag the window's right edge, then its bottom edge — first larger, then smaller. Confirm: (a) the window actually resizes and tracks the drag, with no flicker-then-snap-back; (b) there is no maximize button; (c) shrinking either reflows cleanly or stops at a floor — it must not clip or overlap anything; (d) growing taller makes the two monitor grids taller rather than leaving a dead empty band at the bottom, and growing wider widens both columns evenly.

    **If either of the two above still fails, say so and keep going anyway** — last time testing stopped here and left 14 checks unexercised, which is why this is a second full round trip.

    **Then, the rest at 100% scale**

    2. **Reserved space is invisible.** Confirm there is no unexplained empty gap at the bottom of the shared box. A named empty slot for Phase 23's future Light/Dark setting is deliberately parked there and should occupy zero visible space today. A visible gap is a FAIL.
    4. **No minimize button.** Confirm the title bar has no minimize button. This is deliberate — the dialog is modal with no taskbar entry, and minimizing that combination can leave it unreachable. If one is present, try minimizing and report whether you can get the window back at all.
    5. **Tab order.** Without touching the mouse, press Tab repeatedly from the first control and report the order focus actually visits things. It should read as deliberate — Normal column, then Rig column, then the shared section top to bottom, then Save/Discard — not scrambled. The Hotkey field is deliberately skipped by Tab (click-to-activate only); that is correct, not a bug.
    6. **Drag-drop on the whole box.** Drag a `.lnk` or `.exe` onto the **empty area** of the Target App box (not the text field) and confirm it is accepted and the path updates. Then drag one onto the text field itself and confirm the same. Report both independently — the box-wide drop target is the one at risk.
    7. **Validation feedback visible and un-clipped.** Force an error on each of these and confirm the red error icon appears next to the control and is not clipped off the edge of its box: clear or invalidate the app path (`txtAppPath`), and pick a monitor configuration the form objects to (both grids). If you have a quick way to trigger the autostart or hotkey error, check those too. Also confirm any yellow/warning text line is fully readable and not cut off.
    8. **Live theme flip.** With Settings **open**, flip Windows between Light and Dark. Confirm every control re-themes: both grids, both audio dropdowns in their new column positions, all four buttons, and the hotkey box.
    9. **Warning appearing vs. your manual resize.** Resize the window to a size you like, then trigger a warning label to appear (check 7's method works). Report what happens. Expected now: the window stays exactly where you put it and does not snap back. Also report whether the newly-appeared warning is fully readable at your chosen size or whether something got squeezed — with the window no longer auto-growing, that is the one accepted tradeoff of the resize fix and this is where it gets judged.

    **At 125% scale** (Settings > System > Display > Scale, then relaunch the app)

    10. **Grid columns.** For **both** grids separately: confirm the "Monitor" name column fills the available width without overflowing, confirm the "Off" and "On" checkbox columns are fully visible and not clipped at the grid's right edge, and confirm **no horizontal scrollbar** appears. A horizontal scrollbar means the fill-column math broke at this scale.
    11. **Overlap and crowding (SETTINGS-01 at 125%).** Re-check every section — Normal column, Rig column, shared box — for overlapping or crowded controls. Text needs more room at 125%; confirm no label is clipped or colliding with a neighbour.
    12. **Button text.** Confirm `Browse…`, `Clear`, `Save Settings`, and `Discard Changes` all show their full text with no truncation and no ellipsis.
    13. **Resize at 125%.** Repeat check 3's edge-drag test at this scale, including the shrink floor and the grow-grows-the-grids behaviour.

    **At 150% scale** (relaunch again)

    14. **Repeat checks 10, 11, and 12 at 150%.** This is the higher-risk factor — slack that survives 125% may not survive 150%.
    15. **Whole-window sanity.** On a fresh open, before any manual resize, confirm the window's overall size and proportions still look like a coherent dialog and that it fits on your screen. The size is computed from content and clamped to your screen's working area; 150% is where a content-driven size is most likely to overshoot.
    16. **Resize at 150%.** Repeat check 3's edge-drag test at this scale.

    **Judgment call**

    17. **Shared-section width.** In the shared box at the bottom, the content is left-aligned and may not stretch to the right edge of the box (the container sizes its content column to its widest item). Report whether that reads as fine/intentional or as broken dead space. If broken, say so — there is an already-researched fallback (rebuilding that one section as a table instead of a flow stack).

    Finally: **report PASS or FAIL for every numbered check**, and set your display scale back to normal when you are done.
  </how-to-verify>
  <verify>
    <automated>git status --porcelain src/ | wc -l | grep -qx 0 && echo "no source changed during verification plan"</automated>
    <human-check>All 17 checks reported individually as PASS or FAIL by the user on real Windows rig hardware at 100%, 125%, and 150% display scale.</human-check>
  </verify>
  <acceptance_criteria>
    - Checks 1 and 3 are reported first and answered explicitly, because they are the two recorded gaps in `22-VERIFICATION.md`. Check 1 is reported **per mode box** (Normal and Rig separately) with an explicit yes/no on the grid and an explicit yes/no on the audio dropdown — four answers, not one, because a one-sided failure is exactly what a combined answer would hide.
    - Check 3 is answered against all four of its sub-points (a) actually resizes, (b) no maximize button, (c) shrinking stops rather than clips, (d) growing taller grows the grids and growing wider widens both columns evenly. Sub-point (d) is what proves the `Percent` rows deliberately retained in Plan 04 do their job; sub-point (c) is what proves the new `MinimumSize` floor works.
    - All 17 checks are reported individually as PASS or FAIL with a note. A single combined "looks good" does not satisfy this.
    - Checks 10 and 14 are reported **per grid**, each with an explicit yes/no on horizontal scrollbar presence.
    - Checks 3, 13, and 16 are reported as three separate results — resize behaviour interacting with DPI scaling is a compound risk a single-scale check cannot cover.
    - Check 9 reports the observed behaviour, not a verdict: whether the window held the user's manual size, and whether the newly-visible warning was fully readable at that size. Both halves are required — the second is the accepted tradeoff of turning `Form.AutoSize` off and needs a real-hardware read.
    - Check 6 confirms **both** drop targets (empty panel area and text field) independently.
    - Check 4 confirms minimize-button absence; if one is present, the report states whether the window was recoverable after minimizing.
    - Check 17's answer is recorded verbatim; if it reads as broken, the SUMMARY names the `FlowLayoutPanel` → single-column `TableLayoutPanel` swap for `flpShared` (`22-RESEARCH.md` Assumptions Log A2) as the authorised follow-up, without implementing it.
    - Any FAIL is recorded verbatim with which control, which display scale, and what the user saw — enough detail to plan the next fix without another round trip for information. If Check 1 fails, the SUMMARY names the wrapper-`Panel`-elimination fallback from `22-04-PLAN.md`; if Check 3 fails, it records whether the drag was refused or snapped back.
    - The SUMMARY records the exact published binary path, the Windows build used, and confirms the display scale was returned to the user's normal setting.
    - The SUMMARY states an explicit verdict for SETTINGS-01 and SETTINGS-02 individually, each naming the check numbers that evidence it, and answers each of the two `gaps:` entries in `22-VERIFICATION.md` by name.
    - `git status --porcelain src/` is empty for the entire plan.
    - The phase is not marked complete with an open FAIL, and no `requirements-completed` claim is written into any SUMMARY frontmatter ahead of the user's reported result — that is the precise error `22-01-SUMMARY.md` and `22-02-SUMMARY.md` made and that `22-VERIFICATION.md` caught.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 17 checks pass, or report each failing check with the display scale it failed at and exactly what you observed.</resume-signal>
  <done>Both Phase 22 success criteria are confirmed on real rig hardware at 100%, 125%, and 150% display scale; both gaps recorded in `22-VERIFICATION.md` are answered by their specific check; PITFALLS.md Pitfall 9 is closed; and the per-item verdict is recorded in `22-05-SUMMARY.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rig verdict → phase-complete decision | This checkpoint is the only thing that can close Phase 22. An inferred or partial verdict here would repeat exactly the failure `22-VERIFICATION.md` was written to catch. |
| Human operator → live Windows display-scale setting | The pass mutates a real system setting three times and must restore it. |
| Rig session artifacts → planning record | Screenshots of the Settings dialog may incidentally capture the user's real configured app path and audio device names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-25 | Repudiation | A check claimed without being performed — especially the 125%/150% ones, which need a relaunch and are the easiest to skip | mitigate | Hard constraints 2 and 4; acceptance criteria demand per-grid, per-box, per-scale answers, so a skipped check is visibly missing its answer; the SUMMARY must record the Windows build and binary path |
| T-22-26 | Repudiation | A second premature `requirements-completed` claim written before the user's result comes back | mitigate | Explicit acceptance criterion forbidding it, naming the two SUMMARY files that already made this error once |
| T-22-27 | Tampering | A newly-found defect patched inline during the session, producing an unverified change no audit covers | mitigate | Hard constraint 1: no source change; `git status --porcelain src/` empty is an acceptance criterion; every defect routes to a new gap-closure plan |
| T-22-28 | Denial of Service | The session stopping at the first failure again, leaving the remaining checks unexercised and forcing a third round trip | mitigate | Hard constraint 3 explicitly instructs continuing past a failure this time, and the check order puts the two known-failing checks first so the rest is gathered regardless of their outcome |
| T-22-29 | Tampering | Validation feedback or the box-wide drag-drop target silently lost while fixing layout | mitigate | Checks 6 and 7 test both on real hardware with independent per-target answers |
| T-22-30 | Denial of Service | The new content-derived `MinimumSize` floor being larger than the user expects, making the window feel stuck, or the working-area clamp misbehaving at 150% | mitigate | Check 3(c) and Check 15 target these directly; interfaces item 5 pre-frames the floor as intentional so it is judged rather than reported as a bug |
| T-22-31 | Tampering | The user's display scale left at 150% after the session | mitigate | Final instruction in `<how-to-verify>` plus an acceptance criterion requiring the SUMMARY to confirm restoration |
| T-22-32 | Information Disclosure | Rig screenshots capturing the user's real app path and audio device names into the planning record | accept | Single-user personal project on the user's own machine; recorded evidence is PASS/FAIL notes rather than screenshots, and the user controls what is pasted back |
| T-22-SC | Tampering | npm/pip/cargo/NuGet installs | accept | N/A — this plan installs nothing and changes no source file; `22-RESEARCH.md`'s Package Legitimacy Audit records zero external packages for this phase |
</threat_model>

<verification>
- `git status --porcelain src/` is empty for the whole plan.
- The blocking rig checkpoint returns an individual result for all 17 checks, with Checks 1 and 3 answered first and in the sub-point detail their acceptance criteria require.
- `22-05-SUMMARY.md` exists and carries the binary path, the Windows build, the 17-row result table, the two per-criterion verdicts, and a by-name answer to each of `22-VERIFICATION.md`'s two gaps.
- If all checks pass, `.planning/ROADMAP.md`'s Phase 22 line no longer carries the `BLOCKED` annotation.
</verification>

<success_criteria>
- SETTINGS-01 and SETTINGS-02 each carry an explicit rig-evidenced verdict naming the check numbers that support it.
- Both gaps recorded in `22-VERIFICATION.md` are answered directly: the grid/audio-picker collapse by Check 1, the dead resize by Check 3.
- The 14 checks never reached in the failed session are exercised and reported, including the full DPI set at 125% and 150%.
- PITFALLS.md Pitfall 9 is answered with hardware evidence, not inference.
- No source file changed during this plan.
- If anything still fails, the record contains enough detail — which control, which scale, what was seen — to plan the next fix without a further information-gathering round trip, and the already-researched fallbacks are named rather than implemented.
</success_criteria>

<output>
Create `.planning/phases/22-settingsform-layout-pass/22-05-SUMMARY.md` when done, recording the published binary path, the Windows build used, all 17 check results in the order run, the per-criterion verdicts for SETTINGS-01 and SETTINGS-02, a by-name answer to each of `22-VERIFICATION.md`'s two gaps, confirmation that the display scale was restored, and any defect found with enough detail to plan gap closure.
</output>
