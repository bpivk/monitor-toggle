---
phase: 19-monitor-tile-dashboard-monitorpanelform-retirement
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - src/RigToggle.App/MainForm.Designer.cs
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.App/Program.cs
  - src/RigToggle.App/MonitorIdentifyOverlay.cs
  - src/RigToggle.App/MonitorPanelForm.cs
  - src/RigToggle.App/MonitorPanelForm.Designer.cs
autonomous: true
requirements: [TILE-07]

must_haves:
  truths:
    - "The standalone Monitors panel no longer exists anywhere in the solution — no source file, no type, no factory, no constructor parameter"
    - "Neither entry point survives: MainForm has no Monitors button and the tray context menu has no Monitors item"
    - "The tray context menu reads Switch to Rig/Normal Mode, then Settings, then a separator, then Exit"
    - "MainForm's constructor takes five dependencies instead of six, and the composition root constructs it accordingly"
    - "The solution builds clean and the existing test suite still passes with no regressions after the deletion"
  artifacts:
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "MainForm with all MonitorPanelForm coupling removed — no factory field, no cached instance, no open helper, no tray handler, five-argument constructor"
      contains: "public MainForm("
      min_lines: 950
    - path: "src/RigToggle.App/Program.cs"
      provides: "Composition root with the MonitorPanelFormFactory local function removed and a five-argument MainForm construction"
      contains: "new MainForm(toggleOrchestrator, settingsStore, monitorController, SettingsFormFactory, themeProvider)"
      min_lines: 190
  key_links:
    - from: "src/RigToggle.App/Program.cs"
      to: "src/RigToggle.App/MainForm.cs"
      via: "five-argument construction, no panel factory threaded through"
      pattern: "new MainForm\\(toggleOrchestrator, settingsStore, monitorController, SettingsFormFactory, themeProvider\\)"
---

<objective>
Retire `MonitorPanelForm` and both its entry points (TILE-07), now that the tile dashboard has fully absorbed its capability.

Purpose: the panel is a deletion target, not a maintenance target. Everything it did — enumerate, mutate, confirm, hotplug-refresh, Identify — now lives on `MainForm` (Plans 02 and 03). Deleting it last, rather than first, is the "prove the replacement before removing the original" sequencing 19-RESEARCH.md's build order and 19-UI-SPEC.md's Component Behavior Contract both require.
Output: two source files deleted, four modified, solution green.
</objective>

<hard_constraints>
Load-bearing rules.

1. Delete, do not deprecate. No `[Obsolete]` shim, no renamed-but-kept file, no commented-out block preserving the panel "just in case". `MonitorPanelForm.cs` and `MonitorPanelForm.Designer.cs` are removed from the working tree via `git rm`.
2. Do NOT delete `MonitorConfirmDialog.cs`, `MonitorIdentifyOverlay.cs`, or `ThemeApplier.ThemeMonitorGrid`. The first two are now called from `MainForm` (Plan 03); `ThemeMonitorGrid` is still used by `SettingsForm` for `dgvMonitors` and `dgvMonitorsNormal`.
3. Do NOT change any tile, layout, mutation, Identify, hotplug, or theming behaviour in this plan. This is a pure removal plus the mechanical constructor-arity and tray-menu consequences. If a behavioural change looks necessary to make the deletion compile, stop — that means Plan 02 or 03 left a gap that should be fixed as a gap, not absorbed here.
4. Do NOT touch `SettingsForm`, `ToggleService`, `ToggleOrchestrator`, `WindowsMonitorController`, or anything in `RigToggle.Core`/`RigToggle.Windows`. The panel's removal is confined to `RigToggle.App`.
</hard_constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-CONTEXT.md
@.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-RESEARCH.md
@.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-PATTERNS.md
@.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-UI-SPEC.md
@.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-03-SUMMARY.md

<interfaces>
Exact removal targets (verified against the current tree — no exploration required):

- `src/RigToggle.App/MainForm.cs`: field `private readonly Func<MonitorPanelForm> _monitorPanelFormFactory;` (line 35); field `private MonitorPanelForm? _monitorPanelForm;` (line 42) with its 17-03/PANEL-03 doc comment at lines 38-41; constructor parameter `Func<MonitorPanelForm> monitorPanelFormFactory,` (line 61) and its assignment (line 68); `TrayMonitorsMenuItem_Click` (line 477); `OpenMonitorPanel()` (lines 499-514) with its four-point divergence doc comment at lines 479-498; the class doc comment's Phase 17 paragraph (lines 25-27) and the `Switch mode / Settings / Monitors / Exit` wording in the Phase 8 paragraph (line 22). Note `BtnMonitors_Click` was already removed by Plan 19-02.
- `src/RigToggle.App/MainForm.Designer.cs`: `this.trayMonitorsMenuItem = new System.Windows.Forms.ToolStripMenuItem();` (line 60); the whole `// trayMonitorsMenuItem` block (lines 134-139); the `this.trayMonitorsMenuItem,` entry in the `trayContextMenu.Items.AddRange` array (line 170) and the order comment above it (lines 164-165); the field declaration `private System.Windows.Forms.ToolStripMenuItem trayMonitorsMenuItem;` (line 222).
- `src/RigToggle.App/Program.cs`: the `MonitorPanelFormFactory` local function and its 17-03/T-17-12 doc comment (lines 152-160); the `MonitorPanelFormFactory` argument in the `new MainForm(...)` call (line 162).
- `src/RigToggle.App/MonitorIdentifyOverlay.cs` line 11: doc comment reading "used by MonitorPanelForm's \"Identify\" action (Plan 02)" — stale after the deletion.
- Files to delete outright: `src/RigToggle.App/MonitorPanelForm.cs`, `src/RigToggle.App/MonitorPanelForm.Designer.cs`. There is no `MonitorPanelForm.resx`.
- Locked tray menu order after removal (19-UI-SPEC.md Copywriting Contract): `Switch to Rig/Normal Mode` → `Settings` → separator → `Exit` — the pre-Phase-17 three-item order.
- Target constructor call in the composition root: `new MainForm(toggleOrchestrator, settingsStore, monitorController, SettingsFormFactory, themeProvider)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainForm — remove the tray Monitors entry, the panel factory, and the open helper</name>
  <files>src/RigToggle.App/MainForm.Designer.cs, src/RigToggle.App/MainForm.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.Designer.cs (the file being modified — the five `trayMonitorsMenuItem` sites listed in the interfaces block, and the `trayContextMenu.Items.AddRange` array whose order comment must be corrected)
    - src/RigToggle.App/MainForm.cs (the file being modified — the two panel fields, the constructor parameter and its assignment, `TrayMonitorsMenuItem_Click`, `OpenMonitorPanel()`, and the two class-doc-comment paragraphs naming the Monitors entry)
    - .planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-UI-SPEC.md (Copywriting Contract: the locked post-removal tray menu order, and the explicit statement that both entry points are DELETED rather than renamed or repurposed)
  </read_first>
  <action>
    In `src/RigToggle.App/MainForm.Designer.cs`, remove every trace of the tray Monitors item: its instantiation line, its whole configuration block including the `Text = "Monitors"` and `Click += TrayMonitorsMenuItem_Click` lines, its entry in the `trayContextMenu.Items.AddRange` array, and its private field declaration. Update the order comment above `Items.AddRange` to read the post-removal order — Switch mode, then Settings, then separator, then Exit — and note that this reverts to the pre-Phase-17 three-item order now that the Monitors entry is retired (TILE-07). Leave `traySeparator`, `traySettingsMenuItem`, `trayToggleMenuItem`, `trayExitMenuItem`, and the 12-02/D-03 accepted-limitation comment about ToolStrip separator theming exactly as they are.

    In `src/RigToggle.App/MainForm.cs`:
    - Delete the `_monitorPanelFormFactory` field and the `_monitorPanelForm` cached-instance field together with the 17-03/PANEL-03 doc comment that explains the caching strategy — that rationale dies with the panel.
    - Remove the `Func<MonitorPanelForm> monitorPanelFormFactory,` constructor parameter and its `?? throw new ArgumentNullException` assignment line. The constructor's remaining five parameters and their order stay unchanged: `orchestrator`, `settingsStore`, `monitorController`, `settingsFormFactory`, `themeProvider`.
    - Delete `TrayMonitorsMenuItem_Click` and `OpenMonitorPanel()` in full, including `OpenMonitorPanel`'s four-point "deliberately diverges from OpenSettingsDialog" doc comment.
    - Update the class doc comment: in the Phase 8 paragraph, change the tray-menu inventory from `Switch mode / Settings / Monitors / Exit` to `Switch mode / Settings / Exit`. Replace the Phase 17 paragraph with a Phase 19 paragraph stating that the standalone `MonitorPanelForm` and both its entry points are retired (TILE-07) and that per-monitor enable/disable, hotplug refresh, the confirmation gate, and Identify now live inline on this form as the monitor-tile dashboard (TILE-01..06, MAIN-01/02).
    - Verify no other reference to the panel type remains in this file.

    Do not change any other method, comment, or behaviour.
  </action>
  <verify>
    <automated>grep -rc 'MonitorPanelForm' src/RigToggle.App/MainForm.cs src/RigToggle.App/MainForm.Designer.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MonitorPanelForm' src/RigToggle.App/MainForm.cs` outputs `0` and `grep -c 'MonitorPanelForm' src/RigToggle.App/MainForm.Designer.cs` outputs `0`.
    - `grep -c 'trayMonitorsMenuItem' src/RigToggle.App/MainForm.Designer.cs` outputs `0` and `grep -c 'TrayMonitorsMenuItem_Click' src/RigToggle.App/MainForm.cs` outputs `0`.
    - `grep -c 'OpenMonitorPanel' src/RigToggle.App/MainForm.cs` outputs `0`.
    - `grep -c 'Monitors' src/RigToggle.App/MainForm.Designer.cs` outputs `0`.
    - The tray menu is back to three items plus the separator: `awk '/trayContextMenu.Items.AddRange/,/});/' src/RigToggle.App/MainForm.Designer.cs | grep -c 'this.tray'` outputs `4`, and the same range contains `trayToggleMenuItem`, `traySettingsMenuItem`, `traySeparator`, `trayExitMenuItem` in that order.
    - The constructor is now five-argument: `grep -c 'Func<SettingsForm> settingsFormFactory,' src/RigToggle.App/MainForm.cs` outputs `1` and `grep -c 'Func<MonitorPanelForm>' src/RigToggle.App/MainForm.cs` outputs `0`.
    - The dashboard behaviour added by Plans 02 and 03 is untouched — each outputs `1` against `src/RigToggle.App/MainForm.cs`: `grep -c 'RefreshMonitorTiles()'`-family presence confirmed via `grep -c 'private void RefreshMonitorTiles()'`, `grep -c 'private void LayoutDashboard()'`, `grep -c 'private void ApplyDashboardTheming()'`, `grep -c 'private void OnTileAction(MonitorTile tile)'`, `grep -c 'private IDisposable? TryAcquireMonitorAccess()'`.
    - `grep -c 'SystemEvents.DisplaySettingsChanged += OnDisplaySettingsChanged' src/RigToggle.App/MainForm.cs` still outputs `1`.
    - `RigToggle.App` is EXPECTED not to compile after this task alone — `Program.cs` still passes six arguments. Task 2 closes that. This mirrors the documented Plan 17-02 intermediate-build-state pattern and is not a regression.
  </acceptance_criteria>
  <done>`MainForm` and its Designer hold no reference to the panel: no factory, no cached instance, no constructor parameter, no tray item, no open helper, and the tray menu is back to its three-item order. Compilation resumes at Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Delete MonitorPanelForm, unwire the composition root, and gate on a clean build</name>
  <files>src/RigToggle.App/Program.cs, src/RigToggle.App/MonitorIdentifyOverlay.cs, src/RigToggle.App/MonitorPanelForm.cs, src/RigToggle.App/MonitorPanelForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/Program.cs (the file being modified — the `MonitorPanelFormFactory` local function at lines 152-160 with its 17-03/T-17-12 doc comment, and the six-argument `new MainForm(...)` call at line 162)
    - src/RigToggle.App/MonitorIdentifyOverlay.cs (the file being modified — the stale class doc comment at line 11 naming `MonitorPanelForm` as its caller)
    - src/RigToggle.App/MonitorPanelForm.cs and src/RigToggle.App/MonitorPanelForm.Designer.cs (the deletion targets — confirm nothing in them is still referenced elsewhere before removing)
    - src/RigToggle.App/ThemeApplier.cs (confirm `ThemeMonitorGrid` has surviving callers in `SettingsForm` and must NOT be removed alongside the panel)
  </read_first>
  <action>
    In `src/RigToggle.App/Program.cs`, delete the `MonitorPanelFormFactory` local function together with its whole 17-03/T-17-12 doc comment block (the one explaining why the panel was deliberately denied `audioController`, `appController`, `autostartConfigurator`, `modeStore`, and `markerStore`), and change the `MainForm` construction to `mainForm = new MainForm(toggleOrchestrator, settingsStore, monitorController, SettingsFormFactory, themeProvider);`. Leave the `SettingsFormFactory` local function, the `MainForm mainForm = null!;` pre-declaration, and its CS0841 textual-scope explanation comment exactly as they are — that cycle is about `SettingsFormFactory`, not the panel, and still applies.

    In `src/RigToggle.App/MonitorIdentifyOverlay.cs`, update the class doc comment so it names its current caller: the overlay is used by `MainForm`'s Identify action on the monitor-tile dashboard (TILE-04), replacing the stale `MonitorPanelForm`/"Plan 02" reference. Change nothing else in that file — its positioning logic, `AutoCloseMilliseconds`, and CCD-snapshot-only rule are unchanged and still correct.

    Delete both panel source files from the working tree using `git rm src/RigToggle.App/MonitorPanelForm.cs src/RigToggle.App/MonitorPanelForm.Designer.cs`. Do not leave stubs, do not add an `[Obsolete]` shim, do not comment the code out. There is no `.resx` to remove and no `.csproj` change needed — `RigToggle.App.csproj` uses default SDK globbing with no explicit `Compile` items for these files.

    After the deletion, sweep the whole solution for any surviving reference and fix stale doc comments only (never behaviour): search `src/` for the string `MonitorPanelForm` and for `PANEL-0` and confirm the only remaining hits, if any, are in files whose comments legitimately reference historical phases. If an executable reference survives anywhere, that is a build break to fix, not a comment to tolerate.

    Finally run the full build and test gate and record the output for the summary.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true</automated>
  </verify>
  <acceptance_criteria>
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` exits 0 and reports `0 Error(s)`.
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0` with `Total: 81`.
    - Both files are gone from the working tree and staged as deletions: `ls src/RigToggle.App/MonitorPanelForm.cs` fails with "No such file or directory", `ls src/RigToggle.App/MonitorPanelForm.Designer.cs` fails the same way, and `git status --porcelain src/RigToggle.App/` shows a `D` entry for each.
    - Zero surviving executable references anywhere in the solution: `grep -rn 'MonitorPanelForm' src/ --include=*.cs --include=*.csproj | grep -vE ':\s*(//|\*|/\*)' | wc -l` outputs `0`.
    - The composition root is five-argument: `grep -c 'new MainForm(toggleOrchestrator, settingsStore, monitorController, SettingsFormFactory, themeProvider)' src/RigToggle.App/Program.cs` outputs `1` and `grep -c 'MonitorPanelFormFactory' src/RigToggle.App/Program.cs` outputs `0`.
    - Reused collaborators survived the deletion — each outputs `1`: `ls src/RigToggle.App/MonitorConfirmDialog.cs`, `ls src/RigToggle.App/MonitorIdentifyOverlay.cs` (both succeed), `grep -c 'public static void ThemeMonitorGrid(DataGridView grid, bool dark)' src/RigToggle.App/ThemeApplier.cs`.
    - `ThemeMonitorGrid` still has live callers: `grep -c 'ThemeApplier.ThemeMonitorGrid' src/RigToggle.App/SettingsForm.cs` outputs `4`.
    - The overlay's doc comment no longer names the deleted form: `grep -c 'MonitorPanelForm' src/RigToggle.App/MonitorIdentifyOverlay.cs` outputs `0`.
    - No deprecation shim was left behind: `grep -rn 'Obsolete' src/RigToggle.App/ --include=*.cs | wc -l` outputs `0`.
    - The published app surface did not gain or lose a package: `grep -c 'PackageReference' src/RigToggle.App/RigToggle.App.csproj` outputs `0`.
  </acceptance_criteria>
  <done>`MonitorPanelForm` is gone from the tree, the composition root constructs a five-argument `MainForm`, no executable reference to the panel survives anywhere, the reused dialog/overlay/grid-theming collaborators are intact, and the solution builds and tests clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composition root → `MainForm` | Dependency wiring changes arity; a mis-wired argument would inject the wrong collaborator. Mitigated by the compiler (all five parameter types are distinct) and the exact-call-string criterion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Denial of Service | Deleting a still-referenced collaborator (`MonitorConfirmDialog`, `MonitorIdentifyOverlay`, `ThemeMonitorGrid`) alongside the panel, breaking the tile dashboard or Settings | mitigate | Hard constraint 2 plus explicit survival criteria asserting the two files still exist and `ThemeMonitorGrid` still has four `SettingsForm` callers |
| T-19-14 | Repudiation | A partially-removed panel leaving dead-but-reachable code paths | mitigate | Solution-wide grep for executable `MonitorPanelForm` references must return zero, and no `[Obsolete]` shim is permitted |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages added or removed; `PackageReference` count remains zero |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` exits 0 with `0 Error(s)`.
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0, Total: 81`.
- `git status --porcelain` shows exactly two deletions and four modifications, with no untracked leftovers.
- Runtime confirmation that the tray menu has three items and that no path can still open a standalone panel is a rig observation, deferred to Plan 05.
</verification>

<success_criteria>
- `MonitorPanelForm.cs` and `MonitorPanelForm.Designer.cs` no longer exist.
- No executable reference to the panel type survives anywhere in `src/`.
- MainForm has no Monitors button and the tray menu has no Monitors item; the menu order is Switch → Settings → separator → Exit.
- `MainForm`'s constructor and the composition root both use five dependencies.
- `MonitorConfirmDialog`, `MonitorIdentifyOverlay`, and `ThemeApplier.ThemeMonitorGrid` all survive with live callers.
- Build and tests are green with no regression from the 81-test baseline.
</success_criteria>

<output>
Create `.planning/phases/19-monitor-tile-dashboard-monitorpanelform-retirement/19-04-SUMMARY.md` when done, recording the deleted files, the solution-wide reference-sweep output, and the build/test results.
</output>
