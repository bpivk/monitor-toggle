---
phase: 16-normal-mode-explicit-monitor-config-mode-store-redesign
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-03]
files_modified:
  - src/RigToggle.App/StartupRecoveryChecker.cs
  - src/RigToggle.App/Program.cs
  - src/RigToggle.App/MainForm.cs
autonomous: true
requirements: [DISPLAY-11, DISPLAY-13]

must_haves:
  truths:
    - "On first v2.0 launch mode.json is seeded once from legacy snapshot presence (Rig if state.json exists, else Normal), so existing Rig-mode users are NOT shown a spurious 'mode unknown' dialog"
    - "If the mode file exists but is corrupted, a blocking startup dialog fails loudly rather than silently defaulting (D-06), fires at startup rather than deferred to a toggle attempt (D-07), and states the mode is unknown and to verify manually; if a crash marker is present, a second blocking startup dialog (D-02) is inform-only with no inline retry action (D-03), naming the interrupted target mode and clearing the marker"
    - "Both startup dialogs run before MainForm is shown, on both the visible and --tray paths, with owner null, mode-corruption dialog first"
    - "Program.cs constructs JsonModeStore + JsonToggleInProgressStore and wires them into ToggleService/ToggleOrchestrator instead of ISnapshotStore"
    - "MainForm's mode label/tray/tooltip read the IModeStore-backed mode, showing an explicit 'Unknown' state when mode is unknown"
    - "All three toggle triggers (button, tray, hotkey) refuse to toggle when mode is unknown, using each trigger's existing chrome (MessageBox for GUI, balloon for tray/hotkey)"
  artifacts:
    - path: "src/RigToggle.App/StartupRecoveryChecker.cs"
      provides: "Two blocking-dialog startup checks (mode corruption, crash marker), extracted for testability"
      contains: "MessageBoxIcon.Warning"
    - path: "src/RigToggle.App/Program.cs"
      provides: "Store construction, one-time bootstrap seed, StartupRecoveryChecker invocation, rewired DI"
      contains: "JsonModeStore"
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "Mode-known-aware RefreshUi + three toggle-trigger unknown-mode guards"
      contains: "IsModeKnown"
  key_links:
    - from: "src/RigToggle.App/Program.cs"
      to: "src/RigToggle.Core/Persistence/JsonModeStore.cs"
      via: "constructs JsonModeStore(mode.json) + JsonToggleInProgressStore(toggle-in-progress.json)"
      pattern: "new JsonModeStore"
    - from: "src/RigToggle.App/Program.cs"
      to: "src/RigToggle.App/StartupRecoveryChecker.cs"
      via: "invokes the two blocking checks before Application.Run"
      pattern: "StartupRecoveryChecker"
    - from: "src/RigToggle.App/MainForm.cs"
      to: "src/RigToggle.Core/ToggleOrchestrator.cs"
      via: "RefreshUi + guards read _orchestrator.IsModeKnown()"
      pattern: "_orchestrator.IsModeKnown"
---

<objective>
Deliver the App-tier half of DISPLAY-11/DISPLAY-13: wire the new stores into the composition root, seed `mode.json` once from legacy snapshot presence so upgrading users carry their state forward silently, add the two blocking startup dialogs (mode-corruption D-06/D-07, crash-recovery D-02/D-03) in a new `StartupRecoveryChecker`, and update `MainForm` so the mode label and all three toggle triggers respect an unknown-mode state.

Purpose: This connects Plan 01's stores and Plan 03's rewired `ToggleService`/`ToggleOrchestrator` to the real startup path and UI. The one-time bootstrap (Pattern 1) is the single highest-value correctness detail — without it every current Rig-mode user hits a spurious dialog on first v2.0 launch. All copy is LOCKED in the UI-SPEC Copywriting Contract.

Output: New `StartupRecoveryChecker.cs`, updated `Program.cs`, updated `MainForm.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-CONTEXT.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-01-SUMMARY.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-03-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01/03 + current App shapes to modify. Copy is LOCKED in UI-SPEC. -->

Plan 01: JsonModeStore(string path) : IModeStore { Exists(); ToggleMode? TryLoad(); Save(mode); }
  JsonToggleInProgressStore(string path) : IToggleInProgressStore { ToggleInProgressMarker? TryLoad(); Save(marker); Clear(); }
Plan 03: ToggleService ctor now (ISettingsStore, IModeStore, IMonitorController, IAudioController, IAppController)
  ToggleOrchestrator ctor now takes IToggleInProgressStore too; exposes IsModeKnown()

Current Program.cs (lines 48-111): basePath = %LocalAppData%\RigToggle;
  new JsonSettingsStore("settings.json"); new JsonSnapshotStore("state.json");
  new ToggleService(settingsStore, snapshotStore, monitorController, audioController, appController);
  new ToggleOrchestrator(toggleService);
  Tray-safe timing point (lines 132-143): mainForm.InitializeTrayState(); mainForm.RegisterHotkeyAtStartup();

Current MainForm.cs: RefreshUi (lines 255-271) reads _orchestrator.IsInRigMode();
  BtnToggle_Click (273-403) uses MessageBox.Show(this,...); WR-01 guard shape at 285-297;
  TrayToggleMenuItem_Click (574-610) + HandleHotkeyToggle (621-657) use notifyIcon.ShowBalloonTip (D-08 no MessageBox).

LOCKED copy (UI-SPEC Copywriting Contract):
  Startup dialog 1 (mode corruption): owner null, caption "Rig Toggle", MessageBoxButtons.OK, MessageBoxIcon.Warning,
    text: "Rig Toggle can't tell whether you're currently in Rig Mode or Normal Mode — the saved mode file is
    missing or unreadable. Please check your monitors and audio device manually before using Toggle."
  Startup dialog 2 (crash marker): owner null, caption "Rig Toggle", OK, Warning,
    text: "Rig Toggle didn't finish its last toggle to {TargetMode} Mode cleanly — it may have crashed or been
    closed mid-toggle. No automatic retry has been attempted; please check your monitors and audio device
    manually before using Toggle again."  ({TargetMode} = "Rig"/"Normal" from the cleared marker)
  Toggle guard (GUI button): MessageBox, OK, Information,
    "Rig Toggle's current mode is unknown — fix or delete the mode file, then restart the app, before toggling."
  Toggle guard (tray/hotkey): balloon, ToolTipIcon.Warning,
    title "Rig Toggle", text "Current mode is unknown — restart the app to fix, or check Settings."
  Dialog order when both fire: mode-corruption FIRST, then crash-recovery.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StartupRecoveryChecker with the two blocking dialogs</name>
  <files>src/RigToggle.App/StartupRecoveryChecker.cs</files>
  <read_first>
    - src/RigToggle.App/Program.cs (the best-effort try/catch startup idiom + the tray-safe timing point, for structure)
    - src/RigToggle.Core/Abstractions/IModeStore.cs and IToggleInProgressStore.cs (the TryLoad/Clear contracts)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (Copywriting Contract "Startup dialog 1"/"Startup dialog 2" tables + dialog-ordering rule — use verbatim)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md (Pattern 3 startup blocking-dialog sequencing)
  </read_first>
  <action>
    Create StartupRecoveryChecker.cs in the App's namespace as a small static helper exposing `public static void Run(IModeStore modeStore, IToggleInProgressStore markerStore)`. Implement Pattern 3's exact sequence: read `ToggleMode? currentMode = modeStore.TryLoad();`. If `currentMode is null`, show Startup dialog 1 (mode-corruption) with the LOCKED copy via `MessageBox.Show(null, <dialog-1 text>, "Rig Toggle", MessageBoxButtons.OK, MessageBoxIcon.Warning)` and do NOT check the marker (the mode-unknown instruction subsumes it). Else, `var marker = markerStore.TryLoad();` and if `marker is not null`, call `markerStore.Clear();` first, then show Startup dialog 2 (crash-recovery) with the LOCKED copy, interpolating `{marker.TargetMode}` (renders "Rig"/"Normal") into the text.

    Use `owner: null` on both `MessageBox.Show` calls (MainForm may not be shown yet under --tray). These two dialogs are the deliberate blocking exception to the app's best-effort-swallow startup idiom — do NOT wrap them in a silent try/catch. Assume this helper is called before `Application.Run` (Program.cs wires it in Task 2), after the bootstrap seed. Do not seed or clear the mode file here beyond the single `TryLoad` — bootstrap seeding is Program.cs's job.
  </action>
  <verify>
    <automated>grep -q "class StartupRecoveryChecker" src/RigToggle.App/StartupRecoveryChecker.cs && grep -q "modeStore.TryLoad()" src/RigToggle.App/StartupRecoveryChecker.cs && grep -q "markerStore.Clear()" src/RigToggle.App/StartupRecoveryChecker.cs && grep -q "can't tell whether you're currently in Rig Mode or Normal Mode" src/RigToggle.App/StartupRecoveryChecker.cs && grep -q "didn't finish its last toggle to" src/RigToggle.App/StartupRecoveryChecker.cs</automated>
  </verify>
  <acceptance_criteria>
    - StartupRecoveryChecker exposes a `Run(IModeStore, IToggleInProgressStore)` entry point
    - When `TryLoad()` returns null, dialog 1 shows and the marker is NOT checked
    - When mode is known and a marker exists, the marker is Cleared then dialog 2 shows with `{TargetMode}` interpolated
    - Both dialogs use `owner: null`, caption "Rig Toggle", `MessageBoxButtons.OK`, `MessageBoxIcon.Warning`, with the exact LOCKED text
    - Neither dialog is wrapped in a silent try/catch
  </acceptance_criteria>
  <done>The two startup blocking dialogs exist in a testable helper, ordered mode-corruption-first, with the marker cleared when surfaced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new stores, one-time bootstrap, and StartupRecoveryChecker into Program.cs</name>
  <files>src/RigToggle.App/Program.cs</files>
  <read_first>
    - src/RigToggle.App/Program.cs (full file — the store-construction block, the ToggleService/ToggleOrchestrator construction, the best-effort try/catch idiom, the tray-safe timing point at lines 132-143)
    - src/RigToggle.App/StartupRecoveryChecker.cs (the Run signature from Task 1)
    - src/RigToggle.Core/ToggleService.cs and ToggleOrchestrator.cs (the new constructor signatures from Plan 03)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md (Pattern 1 bootstrap-seed anti-pattern warning, Pattern 3 ordering, Pattern 5 dependency removal)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (Program.cs section)
  </read_first>
  <action>
    Alongside the existing `new JsonSnapshotStore(Path.Combine(basePath, "state.json"))`, construct `var modeStore = new JsonModeStore(Path.Combine(basePath, "mode.json"));` and `var markerStore = new JsonToggleInProgressStore(Path.Combine(basePath, "toggle-in-progress.json"));`. Keep `snapshotStore` constructed (still needed for the one-time bootstrap read; full removal is Phase 18).

    Immediately after constructing those stores, insert the one-time bootstrap (Pattern 1) — CRITICAL, prevents a spurious dialog for every current Rig-mode user: `if (!modeStore.Exists()) { modeStore.Save(snapshotStore.Exists() ? ToggleMode.Rig : ToggleMode.Normal); }`. Do NOT unconditionally default to Normal (anti-pattern: silently flips currently-Rig users). Seed exactly once, only when `mode.json` is absent.

    After the bootstrap, call `StartupRecoveryChecker.Run(modeStore, markerStore);` — before constructing `ToggleService`/`ToggleOrchestrator` and before the tray-safe timing point (`InitializeTrayState`/`RegisterHotkeyAtStartup`), so the two blocking dialogs run on both the visible and `--tray` startup paths ahead of any toggle capability.

    Rewire DI: change `new ToggleService(settingsStore, snapshotStore, monitorController, audioController, appController)` to pass `modeStore` in place of `snapshotStore` (per Plan 03's ctor order), and change `new ToggleOrchestrator(toggleService)` to `new ToggleOrchestrator(toggleService, markerStore)` per Plan 03's extended ctor. Preserve the existing best-effort try/catch idiom for `settingsStore.Load()` and any other side-effect startup code — only the two new dialogs are deliberately non-swallowed.
  </action>
  <verify>
    <automated>grep -q "new JsonModeStore(Path.Combine(basePath, \"mode.json\"))" src/RigToggle.App/Program.cs && grep -q "new JsonToggleInProgressStore(Path.Combine(basePath, \"toggle-in-progress.json\"))" src/RigToggle.App/Program.cs && grep -q "if (!modeStore.Exists())" src/RigToggle.App/Program.cs && grep -q "snapshotStore.Exists() ? " src/RigToggle.App/Program.cs && grep -q "StartupRecoveryChecker.Run(modeStore, markerStore)" src/RigToggle.App/Program.cs && grep -q "new ToggleOrchestrator(toggleService, markerStore)" src/RigToggle.App/Program.cs && dotnet build src/RigToggle.App/RigToggle.App.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `JsonModeStore("mode.json")` and `JsonToggleInProgressStore("toggle-in-progress.json")` are constructed under basePath
    - The bootstrap seeds mode ONLY when `!modeStore.Exists()`, using `snapshotStore.Exists() ? Rig : Normal` (never an unconditional Normal default)
    - `StartupRecoveryChecker.Run(modeStore, markerStore)` is called after the bootstrap and before the tray-safe timing point / ToggleService construction
    - ToggleService is constructed with `modeStore` (not `snapshotStore`); ToggleOrchestrator is constructed with `markerStore`
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0
  </acceptance_criteria>
  <done>The composition root seeds mode once from legacy state, runs the startup checks before any toggle path is live, and injects the new stores into the rewired service/orchestrator.</done>
</task>

<task type="auto">
  <name>Task 3: Make MainForm mode-known-aware — RefreshUi Unknown branch + three trigger guards</name>
  <files>src/RigToggle.App/MainForm.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (RefreshUi lines 255-271; BtnToggle_Click 273-403 incl. the WR-01 IsSettingsConfigured guard at 285-297; TrayToggleMenuItem_Click 574-610; HandleHotkeyToggle 621-657 — note both tray/hotkey use ShowBalloonTip, never MessageBox, per D-08)
    - src/RigToggle.Core/ToggleOrchestrator.cs (the new IsModeKnown pass-through from Plan 03)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (Copywriting Contract "Toggle-trigger guard when mode is unknown" table + RefreshUi Unknown label guidance — use verbatim)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (MainForm.cs section)
  </read_first>
  <action>
    In `RefreshUi`, add an unknown-mode branch BEFORE the existing `IsInRigMode()` branch: when `!_orchestrator.IsModeKnown()`, set `lblMode.Text = "Mode: Unknown"`, and set `btnToggle.Text`/`trayToggleMenuItem.Text`/`notifyIcon.Text` to a neutral non-directional label (e.g. "Toggle" and "Rig Toggle — Mode Unknown") rather than defaulting to either Rig or Normal wording; leave the notify icon at a safe default (current icon acceptable). Keep the existing known-mode rendering path unchanged for the Rig/Normal cases. The `IsInRigMode()` branch decision must only run when the mode is known.

    Add an unknown-mode guard to `BtnToggle_Click`, placed BEFORE the `IsInRigMode()` branch decision (it needs a known mode to branch): `if (!_orchestrator.IsModeKnown()) { MessageBox.Show(this, "Rig Toggle's current mode is unknown — fix or delete the mode file, then restart the app, before toggling.", "Rig Toggle", MessageBoxButtons.OK, MessageBoxIcon.Information); return; }` — mirror the existing WR-01 guard shape exactly (LOCKED copy).

    Add an unknown-mode guard to the TOP of both `TrayToggleMenuItem_Click` and `HandleHotkeyToggle`, before their branch decision, using the balloon-tip chrome (never MessageBox — D-08): `if (!_orchestrator.IsModeKnown()) { notifyIcon.ShowBalloonTip(3000, "Rig Toggle", "Current mode is unknown — restart the app to fix, or check Settings.", ToolTipIcon.Warning); return; }` (LOCKED copy). Do not add any MessageBox to the tray/hotkey paths.
  </action>
  <verify>
    <automated>grep -q "Mode: Unknown" src/RigToggle.App/MainForm.cs && grep -q "current mode is unknown — fix or delete the mode file" src/RigToggle.App/MainForm.cs && grep -q "Current mode is unknown — restart the app to fix" src/RigToggle.App/MainForm.cs && [ "$(grep -c "_orchestrator.IsModeKnown()" src/RigToggle.App/MainForm.cs)" -ge 4 ] && dotnet build src/RigToggle.App/RigToggle.App.csproj</automated>
  </verify>
  <acceptance_criteria>
    - RefreshUi renders "Mode: Unknown" and a non-directional toggle label when `!IsModeKnown()`, and only runs the Rig/Normal branch when mode is known
    - BtnToggle_Click has an IsModeKnown guard (MessageBox, Information, LOCKED copy) before the branch decision
    - TrayToggleMenuItem_Click and HandleHotkeyToggle each have an IsModeKnown guard using ShowBalloonTip (Warning, LOCKED copy) — no MessageBox in either
    - `IsModeKnown()` is referenced at least four times (RefreshUi + three triggers)
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0
  </acceptance_criteria>
  <done>The mode label honestly shows Unknown, and none of the three toggle triggers will act on an unknown mode — each refuses with its own established chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file → composition root | mode.json / toggle-in-progress.json / state.json read at startup drive bootstrap, dialogs, and mode routing |
| startup dialogs → user | blocking MessageBoxes surface unknown/interrupted state before any toggle capability is live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Spoofing (wrong mode on upgrade) | one-time mode bootstrap in Program.cs | mitigate | Seed `mode.json` from `snapshotStore.Exists()` exactly once (Rig if state.json present, else Normal) rather than defaulting to Normal — an upgrading Rig-mode user's true state is carried forward, not silently flipped, and no spurious corruption dialog fires on a healthy install. |
| T-16-09 | Denial of Service (self) / Repudiation | unknown-mode toggle attempt | mitigate | All three triggers gate on `IsModeKnown()` before the branch decision, refusing to toggle from an unknown state (which would otherwise require guessing a direction) — the user is told to fix/restart, matching the corrupted-snapshot precedent, and the interrupted-toggle marker is surfaced-then-cleared so it cannot silently persist. |
| T-16-10 | Tampering (local file) | StartupRecoveryChecker reading corrupted mode.json | mitigate | The checker relies on JsonModeStore.TryLoad's degrade-to-null (Plan 01) — a hand-edited/truncated file becomes the well-defined "unknown" dialog path, never an unhandled startup crash. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0 (App now constructs the new ToggleService/ToggleOrchestrator ctors, restoring the full-solution build).
- `grep -q "if (!modeStore.Exists())" src/RigToggle.App/Program.cs` (bootstrap present).
- `grep -c "_orchestrator.IsModeKnown()" src/RigToggle.App/MainForm.cs` is >= 4.
- Visual/behavioral confirmation of the dialogs and Unknown label is deferred to Plan 05's rig checkpoint.
</verification>

<success_criteria>
- mode.json is seeded once from legacy snapshot presence; no spurious dialog for upgrading Rig-mode users.
- Mode-corruption and crash-recovery dialogs run before MainForm on both startup paths, ordered corruption-first, with LOCKED copy.
- Program.cs injects JsonModeStore/JsonToggleInProgressStore into the rewired ToggleService/ToggleOrchestrator.
- MainForm shows an Unknown mode and all three triggers refuse to toggle when mode is unknown.
</success_criteria>

<output>
Create `.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-04-SUMMARY.md` when done.
</output>
