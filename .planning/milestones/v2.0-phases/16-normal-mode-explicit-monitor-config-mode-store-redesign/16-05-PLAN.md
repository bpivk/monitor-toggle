---
phase: 16-normal-mode-explicit-monitor-config-mode-store-redesign
plan: 05
type: execute
wave: 4
depends_on: [16-02, 16-03, 16-04]
files_modified: []
autonomous: false
requirements: [DISPLAY-09, DISPLAY-10, DISPLAY-11, DISPLAY-13]

must_haves:
  truths:
    - "The full solution builds and every automated test passes after the phase's Core + Windows + App changes"
    - "On the real rig, a Normal-mode monitor set configured in Settings actually applies on toggle-to-Normal (not a snapshot restore)"
    - "On the real rig, the mode label is correct immediately after an app restart with no snapshot file present"
    - "On the real rig, a simulated crash mid-toggle surfaces the crash-recovery dialog on next launch and clears the marker"
    - "On the real rig, the re-flowed Settings dialog shows both monitor grids with no overlap/clipping, and the Normal grid is themed in dark mode"
  artifacts:
    - path: ".planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-05-SUMMARY.md"
      provides: "Rig verification record for the four Phase 16 success criteria plus the layout/theming check"
      contains: "DISPLAY-11"
  key_links:
    - from: "Settings Normal grid (Plan 02)"
      to: "ToggleToNormalMode explicit apply (Plan 03)"
      via: "a Normal-mode monitor set chosen in Settings is applied on toggle-to-Normal"
      pattern: "NormalMonitorsTo"
    - from: "toggle-in-progress marker (Plan 03)"
      to: "StartupRecoveryChecker dialog (Plan 04)"
      via: "a marker left by an interrupted toggle triggers the next-launch crash dialog"
      pattern: "ToggleInProgressMarker"
---

<objective>
Confirm, on the real rig hardware, that the Normal-mode explicit monitor config and mode-store redesign work end-to-end: the configured Normal set applies directly (DISPLAY-10), the mode reports correctly after restart with no snapshot file (DISPLAY-11), an interrupted toggle is detected and surfaced on next launch (DISPLAY-13), and the second Settings grid (DISPLAY-09) is laid out and themed correctly. Automated tests (Plan 03) prove the branching/timing with fakes; this plan proves the same against real Windows CCD topology changes, the real persisted mode/marker files, and the real re-flowed Settings dialog.

Purpose: This phase changes genuine runtime behavior (Normal-mode monitor apply, mode persistence, startup dialogs) and a large Designer.cs layout re-flow — both classes of change that only rig verification can fully confirm (topology mutation, dialog timing on the --tray path, and non-compiler-catchable overlap/clipping per Pitfall 4). Consistent with this project's UAT-per-phase discipline (Phases 8, 9, 11, 13, 15).

Output: A green build/test run plus a human-confirmed rig verification of all four success criteria and the layout/theming check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-CONTEXT.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-02-SUMMARY.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-03-SUMMARY.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-solution regression gate (build + all tests)</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-03-SUMMARY.md (what Core/Windows behavior landed)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-04-SUMMARY.md (what App wiring/UI landed)
  </read_first>
  <action>
    Build the whole solution and run the full test suite to catch any cross-project regression before asking for rig time. Run `dotnet build RigToggle.sln` and `dotnet test RigToggle.sln`. If any failure appears, stop and report it (do not proceed to the rig checkpoint) — the most likely misses are: a ToggleServiceTests assertion still expecting pre-mutation/snapshot behavior (Plan 03 Task 3), an App-tier reference to the removed `ISnapshotStore` ToggleService ctor, or JsonSettingsStore.cs having been edited (Pitfall 5 — its diff must be empty). Also run the stale-prose grep sweep below as a final phase-wide check.
  </action>
  <verify>
    <automated>dotnet build RigToggle.sln && dotnet test RigToggle.sln && ! grep -rn "restored exactly as it was before\|Restored automatically when switching back\|Turned off again automatically when switching back\|before switching to Rig Mode" src/ && git -C /home/bpivk/moza diff --quiet src/RigToggle.Core/Persistence/JsonSettingsStore.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build RigToggle.sln` exits 0
    - `dotnet test RigToggle.sln` exits 0 (all RigToggle.Tests and RigToggle.Windows.Tests pass)
    - No `restored exactly as it was before` / `Restored automatically when switching back` / `Turned off again automatically when switching back` / `before switching to Rig Mode` strings remain anywhere under `src/`
    - `git diff` on JsonSettingsStore.cs is empty (Pitfall 5)
  </acceptance_criteria>
  <done>The full solution compiles, every automated test passes, and no stale "always restored" prose survives — rig verification starts from a known-green state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rig verification of DISPLAY-09/10/11/13 + layout/theming</name>
  <what-built>
    Phase 16 gave Normal mode its own explicitly configured monitor set (a second "Normal Mode" grid in Settings below the Rig grid), rewrote toggle-to-Normal to apply that set directly instead of restoring a pre-toggle snapshot, moved "which mode am I in" onto an explicit persisted `mode.json` flag (no longer inferred from snapshot-file presence), and added a `toggle-in-progress.json` crash marker plus two startup blocking dialogs (mode-unknown, and toggle-interrupted). Existing Rig-mode users are seeded forward once from their old state file so they see no spurious dialog. The Settings dialog grew a second grid and every control below it moved down.
  </what-built>
  <how-to-verify>
    Run the published/debug app on the rig and verify each criterion:

    1. DISPLAY-09 (Normal grid config + layout): Open Settings. Confirm a second "Normal Mode" monitor grid appears directly below the Rig grid, with "Off (Normal)"/"On (Normal)" column headers and its own explanation label. Confirm NO controls overlap or clip anywhere down the dialog (audio, app path, hotkey, checkboxes, Save/Discard all visible and unclipped). Switch Windows to dark mode (or confirm current) and confirm the Normal grid is themed identically to the Rig grid (not reverting to light colors). Configure a distinct Normal-mode set (e.g. enable a monitor that Rig mode disables), Save.
    2. DISPLAY-10 (Normal set applies directly): Toggle to Rig, then toggle back to Normal. Expect: on toggle-to-Normal the monitors change to exactly the Normal grid's configured set (the Monitor step applies your explicit config, NOT whatever the pre-toggle snapshot was). Confirm the toggle-result checklist shows "Monitor: OK".
    3. DISPLAY-11 (mode correct after restart, no snapshot): With the app in Normal (or Rig) mode, fully close and relaunch the app. Expect: the mode label immediately reads the correct mode. Optionally delete `%LocalAppData%\RigToggle\state.json` while keeping `mode.json`, relaunch: the mode label is still correct (mode no longer depends on the snapshot file).
    4. DISPLAY-13 (crash marker detected next launch): Start a toggle and kill the process mid-toggle (Task Manager End Task during the toggle), OR manually create `%LocalAppData%\RigToggle\toggle-in-progress.json` with a valid marker. Relaunch. Expect: a blocking dialog states the last toggle to {Rig/Normal} Mode didn't finish cleanly and that no auto-retry was attempted; after OK, `toggle-in-progress.json` is gone (marker cleared).
    5. D-06/D-07 (mode corruption dialog): Hand-corrupt `%LocalAppData%\RigToggle\mode.json` (e.g. write "garbage") and relaunch. Expect: a blocking dialog stating the mode is unknown and to verify manually; the mode label reads "Mode: Unknown"; clicking Toggle (button) shows the "unknown — fix or delete the mode file, then restart" message and does NOT toggle; tray/hotkey toggle shows the balloon variant and does NOT toggle. Fix by deleting mode.json and restarting (it re-seeds).
    6. Upgrade smoke check (bootstrap): Delete `mode.json` but LEAVE a valid `state.json` in place, relaunch. Expect: NO "mode unknown" dialog — the app silently seeds mode to Rig (because state.json exists) and shows the correct label.
  </how-to-verify>
  <read_first>
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-CONTEXT.md (D-01..D-07 for expected behavior and dialog tone)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (exact dialog copy + Settings layout expectations)
  </read_first>
  <acceptance_criteria>
    - All six checks observed as described on the rig, OR any deviation recorded with specifics in 16-05-SUMMARY.md
    - Normal-mode toggle applies the explicitly configured set, not a snapshot restore (DISPLAY-10)
    - Mode label is correct after restart with no dependence on state.json (DISPLAY-11)
    - A crash marker triggers the crash-recovery dialog and is cleared afterward (DISPLAY-13)
    - The re-flowed Settings dialog shows both grids with no overlap/clipping and the Normal grid is themed in dark mode (DISPLAY-09 / Pitfall 4)
    - A missing mode.json with a present state.json seeds silently to Rig with no spurious dialog (bootstrap)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six checks pass, or describe any deviation for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → running app on rig | Human-driven verification of persisted mode/marker + explicit-monitor-apply behavior against real Windows CCD hardware |
| local files → startup path | Hand-corrupted mode.json / injected marker exercise the degrade-to-null and bootstrap paths on real disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Spoofing / Repudiation | Mode label vs. real display state after a real toggle/restart | mitigate | Rig checkpoint explicitly verifies the mode label matches the true display state after restart with and without state.json (DISPLAY-11), and that a real interrupted toggle is surfaced (DISPLAY-13) — the last line of defense against the mode flag misrepresenting reality on real hardware. |
| T-16-12 | Denial of Service (self) | Re-flowed Settings dialog overlap/clipping | mitigate | Rig checkpoint visually confirms no control overlaps/clips (Pitfall 4 — not compiler-catchable), so the dialog remains usable after the +246px downstream re-flow. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet build RigToggle.sln` and `dotnet test RigToggle.sln` both exit 0.
- Phase-wide stale-prose grep sweep returns zero matches; JsonSettingsStore.cs diff is empty.
- Human confirms all six rig checks (DISPLAY-09 layout/theme, DISPLAY-10, DISPLAY-11, DISPLAY-13, D-06/D-07, bootstrap) pass or logs deviations.
</verification>

<success_criteria>
- Full solution builds and all automated tests pass; no stale "always restored" prose remains.
- On the rig: Normal-mode toggle applies the configured set directly; mode is correct after restart without a snapshot; a crash marker is detected and cleared with a dialog; the re-flowed, themed Settings dialog shows both grids cleanly; a missing mode.json with present state.json seeds silently to Rig.
</success_criteria>

<output>
Create `.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-05-SUMMARY.md` when done.
</output>
