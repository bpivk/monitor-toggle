---
phase: 15-optional-app-audio-targets
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/RigToggle.Core/ToggleService.cs
  - src/RigToggle.Tests/ToggleServiceTests.cs
autonomous: true
requirements: [APP-04, APP-05, AUDIO-03, AUDIO-04, AUDIO-05]

must_haves:
  truths:
    - "Leaving the companion app path unset makes toggle-to-Rig skip launch/focus and toggle-to-Normal skip minimize, each as a Skipped step, with no error (APP-04)"
    - "A configured app path whose file no longer exists surfaces as a Failed App step, not Skipped (APP-05)"
    - "Leaving the Rig-mode audio device unset makes toggle-to-Rig skip audio as a Skipped step (AUDIO-03)"
    - "A configured Normal-mode audio device is applied via SetDefault on toggle-to-Normal (not snapshot Restore); unset is Skipped (AUDIO-04)"
    - "A configured audio device ID that no longer resolves surfaces as a Failed step in the relevant direction (AUDIO-05)"
    - "Every toggle result still contains all three steps (Monitor/Audio/App) regardless of what is configured (D-04)"
    - "Toggle-to-Rig is only blocked when the monitor set is empty; audio/app being unset never blocks it (D-05)"
    - "A deliberately-unset target renders as the distinct Skipped outcome, never conflated with NotAttempted (blocked-by-earlier-failure) (D-03)"
    - "A missing/removed audio device surfaces a friendly, actionable failure message ('The configured Rig/Normal-mode audio device could not be found. Open Settings and reselect it.'), matching the existing app-path message's tone (D-07)"
  artifacts:
    - path: "src/RigToggle.Core/ToggleService.cs"
      provides: "TryExecuteOptionalStep helper; optional Audio/App steps both directions; Normal-mode SetDefault audio; relaxed IsFullyConfigured; reworded stale message"
      contains: "TryExecuteOptionalStep"
    - path: "src/RigToggle.Tests/ToggleServiceTests.cs"
      provides: "Skipped and Failed test coverage per optional field, plus the inverted Normal-mode audio test"
      contains: "Skipped"
  key_links:
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "IAudioController.TryResolveDevice"
      via: "configured-but-broken audio existence check inside each Audio step body"
      pattern: "TryResolveDevice"
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "IAudioController.SetDefault"
      via: "ToggleToNormalMode Audio step now calls SetDefault(NormalAudioDeviceId), not Restore(snapshot.Audio)"
      pattern: "SetDefault\\(settings.NormalAudioDeviceId"
---

<objective>
Make the companion-app and audio targets genuinely optional inside `ToggleService`, and give the Normal-mode audio device real runtime effect (AUDIO-04). Unset targets become distinct `Skipped` steps that never block a toggle or raise an error; configured-but-broken targets (missing file / removed device) still surface as real `Failed` steps. Relax the `IsFullyConfigured` gate to the monitor-set check only, and fix the two stale "both audio devices, and the companion app" message strings.

Purpose: This is the behavioral core of Phase 15 — a validation-gate relaxation plus one genuine behavior change (Normal-mode audio moves from snapshot-`Restore` to `SetDefault`). It depends on Plan 01's `Skipped` outcome and `TryResolveDevice` contract.

Output: Rewritten optional-step logic in `ToggleService`, and the test suite that proves both the Skipped and Failed branches for every optional field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-optional-app-audio-targets/15-CONTEXT.md
@.planning/phases/15-optional-app-audio-targets/15-RESEARCH.md
@.planning/phases/15-optional-app-audio-targets/15-PATTERNS.md
@.planning/phases/15-optional-app-audio-targets/15-01-SUMMARY.md

<interfaces>
<!-- Contracts already landed by Plan 01. Use directly; no exploration needed. -->
ToggleStepOutcome: { Succeeded, Failed, NotAttempted, Skipped }
ToggleResult.Success => all steps are Succeeded or Skipped
IAudioController.TryResolveDevice(string? deviceId) : AudioDeviceInfo?   // null = not found / threw
FakeAudioController(callLog, capturedDefaultDeviceId="fake-normal-device", throwOnRestore=false, deviceExists=true)
  logs "audio.SetDefault:{id}", "audio.Restore:{multimediaDeviceId}", "audio.TryResolveDevice:{id}"

Current ToggleService.cs landmarks (verified this session):
  lines 60-68  IsFullyConfigured guard throw (stale "both audio devices, and the companion app path" string)
  lines 70-78  top-level File.Exists(CompanionAppPath) preflight throw  ← REMOVE / relocate into App step
  lines 140-146 Rig-mode Audio (SetDefault(RigAudioDeviceId!)) + App (LaunchOrFocus) steps
  lines 157-175 TryExecuteStep helper (bool return, appends ToggleStepResult)
  lines 201-205 IsFullyConfigured predicate (currently requires audio+app fields)
  lines 303-317 Normal-mode Audio step (_audioController.Restore(snapshot.Audio))  ← change to SetDefault-or-skip
  lines 319-334 Normal-mode Monitor/Audio ToggleStepResult append + monitorFailure short-circuit
  lines 342-367 Normal-mode App step (unset branch currently records NotAttempted)  ← change unset to Skipped
  Monitor try/catch lines 273-301  ← DO NOT TOUCH (Phase 16 territory)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite/add ToggleService tests for the Skipped and Failed branches (RED)</name>
  <files>src/RigToggle.Tests/ToggleServiceTests.cs</files>
  <read_first>
    - src/RigToggle.Tests/ToggleServiceTests.cs (full file — the CreateService helper lines 47-66, ConfiguredSettings lines 28-45, the existing Rig/Normal tests, and specifically `ToggleToNormalMode_RestoresAudioViaRestore_NeverSetDefault` lines 124-134, `ToggleToRigMode_Throws_WhenCompanionAppPathDoesNotExist` around line 244, and the TWO pre-existing audio-restore-throws tests that AUDIO-04 invalidates: `ToggleToNormalMode_StillMinimizesAndClears_WhenAudioRestoreThrows` lines 136-152 and `ToggleToNormalMode_ReturnsFailedAudioStep_ButStillClears_WhenAudioRestoreThrows` lines 203-220)
    - src/RigToggle.Tests/Doubles/FakeControllers.cs (FakeAudioController's new `deviceExists` knob and `audio.TryResolveDevice:` / `audio.SetDefault:` call-log strings, added by Plan 01)
    - src/RigToggle.Core/Models/AppSettings.cs (AppSettings is a class, not a record — construct new instances directly; `with { }` does not compile, per the existing test comment at line 246)
    - .planning/phases/15-optional-app-audio-targets/15-RESEARCH.md (Open Question 2 — the inverted Normal-mode audio test; Pitfall 1 — one Skipped + one Failed test per optional field)
    - .planning/phases/15-optional-app-audio-targets/15-PATTERNS.md (ToggleServiceTests.cs section)
  </read_first>
  <behavior>
    - Rig Audio unset (RigAudioDeviceId null): Audio step outcome == Skipped, App step still runs, result.Success == true, no "audio.SetDefault" in call log
    - Rig Audio configured-but-gone (RigAudioDeviceId set, FakeAudioController deviceExists=false): Audio step == Failed with a message mentioning "audio device"; App step == NotAttempted (Rig is stop-on-first-failure); result.Success == false
    - Rig App unset (CompanionAppPath null): App step == Skipped; result.Success == true; no "app.LaunchOrFocus" in call log
    - Rig App configured-but-missing (CompanionAppPath set to a nonexistent path): App step == Failed (replaces the OLD throw-based `ToggleToRigMode_Throws_WhenCompanionAppPathDoesNotExist`; result now has 3 steps, Monitor+Audio ran); result.Success == false
    - Normal Audio configured (NormalAudioDeviceId set, deviceExists=true): call log contains "audio.SetDefault:{NormalAudioDeviceId}" and does NOT contain "audio.Restore" — this is the rewrite of `ToggleToNormalMode_RestoresAudioViaRestore_NeverSetDefault`; monitor.Restore still appears (Phase 16 untouched)
    - Normal Audio unset (NormalAudioDeviceId null): Audio step == Skipped; no "audio.SetDefault", no "audio.Restore"; snapshot.Clear still runs last
    - Normal Audio configured-but-gone (deviceExists=false): Audio step == Failed; isolate-and-continue means App step (MinimizeIfRunning) still attempted, snapshot.Clear still runs last, and IsInRigMode() flips back to false. This case is the direct replacement for the deleted `ToggleToNormalMode_ReturnsFailedAudioStep_ButStillClears_WhenAudioRestoreThrows` — it MUST carry the same "still minimizes / still clears / no longer stuck in Rig mode" assertions (Assert.Contains "app.MinimizeIfRunning", Assert.Contains "snapshot.Clear", Assert.False(service.IsInRigMode())) so no isolate-and-continue coverage is lost
    - Normal App unset (CompanionAppPath null): App step == Skipped (NOT NotAttempted); result.Success == true; snapshot.Clear runs last
    - A toggle with audio+app both unset but a valid monitor set: result has exactly 3 steps, Monitor Succeeded, Audio Skipped, App Skipped, Success == true
  </behavior>
  <action>
    Extend the `CreateService` helper (lines 47-66) with a new `bool audioDeviceMissing = false` parameter, passed through to `new FakeAudioController(callLog, throwOnRestore: audioThrowsOnRestore, deviceExists: !audioDeviceMissing)`. Keep all existing parameters and defaults so current tests are unaffected.

    Rewrite `ToggleToNormalMode_RestoresAudioViaRestore_NeverSetDefault` to the inverted AUDIO-04 contract: assert `audio.SetDefault:{ConfiguredSettings.NormalAudioDeviceId}` IS in the call log and NO entry starts with `audio.Restore`, while `monitor.Restore:` still appears. Rename the test to reflect the new behavior (e.g. `ToggleToNormalMode_AppliesNormalAudioDeviceViaSetDefault_NeverRestore`).

    Rewrite `ToggleToRigMode_Throws_WhenCompanionAppPathDoesNotExist` from a throw-assertion into a Failed-App-step assertion: the toggle now returns a 3-step ToggleResult with App == Failed (Monitor and Audio already ran), result.Success == false. Rename accordingly (e.g. `ToggleToRigMode_RecordsAppFailed_WhenCompanionAppPathMissing`).

    Delete the two pre-existing tests whose "audio restore throws" premise AUDIO-04 removes (in the new Normal-mode path `_audioController.Restore` is never called, so the `audioThrowsOnRestore` knob no longer reaches the Audio step and these tests would either break or pass only vacuously):
      - `ToggleToNormalMode_ReturnsFailedAudioStep_ButStillClears_WhenAudioRestoreThrows` (lines 203-220). This test uses `CreateService(audioThrowsOnRestore: true)` and asserts `audioStep.Outcome == Failed`; after AUDIO-04 the Normal-mode Audio step resolves `NormalAudioDeviceId` via `TryResolveDevice`/`SetDefault` (which now succeeds with `deviceExists` defaulting to true), so the Audio step becomes Succeeded and this assertion breaks. Its "Failed Audio still minimizes and clears, no longer stuck in Rig mode" coverage moves to the new `deviceExists=false` "Normal Audio configured-but-gone" test above (which must carry the MinimizeIfRunning + snapshot.Clear + IsInRigMode(false) assertions).
      - `ToggleToNormalMode_StillMinimizesAndClears_WhenAudioRestoreThrows` (lines 136-152). This test would still pass but only vacuously — its `audioThrowsOnRestore: true` premise no longer reaches any code path (Restore is never called for Normal-mode audio), so it misleadingly implies Restore is still in the path. Its minimize-and-clear-survive-an-audio-failure coverage is likewise carried by the new `deviceExists=false` test.
    (If you would rather keep either test than delete it, you MUST rename it away from the `WhenAudioRestoreThrows` suffix and rewrite its comment and body to drive the failure via `CreateService(audioDeviceMissing: true)` — never `audioThrowsOnRestore` — so it stops implying Normal-mode audio still calls Restore. Deletion is preferred since the new `deviceExists=false` test already covers this behavior.)

    Add the remaining behavior cases above as new `[Fact]` tests, constructing per-test AppSettings with the specific field left null (build a fresh `AppSettings { ... }` — it is a class, mirror the existing line 248-258 construction pattern), following the established `CreateService()`/`callLog`/`Assert.Contains`/`Assert.Equal(ToggleStepOutcome.X, step.Outcome)` idiom. Use `result.Steps.Single(s => s.StepName == "Audio")` / `"App"` to target a specific step's outcome.
  </action>
  <verify>
    <automated>grep -q "audioDeviceMissing" src/RigToggle.Tests/ToggleServiceTests.cs && grep -cE "ToggleStepOutcome.Skipped" src/RigToggle.Tests/ToggleServiceTests.cs && ! grep -q "RestoresAudioViaRestore_NeverSetDefault" src/RigToggle.Tests/ToggleServiceTests.cs && ! grep -q "WhenAudioRestoreThrows" src/RigToggle.Tests/ToggleServiceTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - CreateService accepts `audioDeviceMissing` and forwards it as FakeAudioController `deviceExists: !audioDeviceMissing`
    - No test named `ToggleToNormalMode_RestoresAudioViaRestore_NeverSetDefault` remains; its replacement asserts SetDefault is called and Restore is not
    - No test asserts `ToggleToRigMode` throws for a missing companion app path; the replacement asserts a Failed App step in a 3-step result
    - Neither `ToggleToNormalMode_ReturnsFailedAudioStep_ButStillClears_WhenAudioRestoreThrows` nor `ToggleToNormalMode_StillMinimizesAndClears_WhenAudioRestoreThrows` remains (both deleted, or renamed off the `WhenAudioRestoreThrows` suffix and rewritten to drive the failure via `audioDeviceMissing: true`); `grep -q "WhenAudioRestoreThrows" src/RigToggle.Tests/ToggleServiceTests.cs` returns nonzero exit
    - The isolate-and-continue coverage they provided (a Failed Normal-mode Audio step still runs MinimizeIfRunning, still runs snapshot.Clear, and leaves IsInRigMode() == false) is preserved in the new `deviceExists=false` "Normal Audio configured-but-gone" test
    - At least one Skipped-asserting test and one Failed-asserting test exist for each of: Rig Audio, Rig App, Normal Audio, Normal App
    - `grep -c "ToggleStepOutcome.Skipped" src/RigToggle.Tests/ToggleServiceTests.cs` is >= 4
    - After Task 2, `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>The test suite encodes both the Skipped and Failed contract for every optional field and the inverted Normal-mode audio behavior; the pre-existing audio-restore-throws tests that AUDIO-04 invalidates are removed (their coverage re-homed to the device-missing test); tests fail (RED) until Task 2 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Audio/App optional in both directions and give Normal-mode audio real effect (GREEN)</name>
  <files>src/RigToggle.Core/ToggleService.cs</files>
  <read_first>
    - src/RigToggle.Core/ToggleService.cs (full file — every landmark listed in the Interfaces block)
    - src/RigToggle.Tests/ToggleServiceTests.cs (the behavior this must satisfy — from Task 1)
    - .planning/phases/15-optional-app-audio-targets/15-PATTERNS.md (ToggleService.cs section — the verbatim before/after for TryExecuteOptionalStep, the Rig-mode Audio/App replacement, the Normal-mode Audio block replacement, the IsFullyConfigured relaxation, and the stale message)
    - .planning/phases/15-optional-app-audio-targets/15-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 4; the "behavior change this implies" note — App is now the last step so Monitor/Audio run before a broken app path is discovered)
  </read_first>
  <action>
    Add a private static `TryExecuteOptionalStep(string stepName, string? configuredValue, Action<string> action, List&lt;ToggleStepResult&gt; steps)` sibling to `TryExecuteStep`. When `configuredValue` is null/empty, append a `ToggleStepResult(stepName, ToggleStepOutcome.Skipped, null)` and return true (Skipped does not block the chain — same bool contract as Succeeded). Otherwise delegate to the existing `TryExecuteStep(stepName, () => action(configuredValue), steps)`. Do NOT inline a second try/catch — reuse TryExecuteStep's body.

    Remove the top-level `File.Exists(settings.CompanionAppPath)` preflight throw (lines 70-78). Its check moves INTO the App step body (see below), because D-04 requires the result to always contain 3 steps and a preflight throw produces zero.

    In `ToggleToRigMode`, replace the Rig-mode Audio and App steps (lines 140-146) using TryExecuteOptionalStep: Audio keyed on `settings.RigAudioDeviceId` — inside the action, if `_audioController.TryResolveDevice(deviceId) is null` throw `InvalidOperationException("The configured Rig-mode audio device could not be found. Open Settings and reselect it.")` (D-07 — matches the app-path message's tone/actionability), else call `_audioController.SetDefault(deviceId)`. Preserve the existing stop-on-first-failure short-circuit exactly: if the Audio call returns false, append `ToggleStepResult("App", ToggleStepOutcome.NotAttempted, null)` and return — a Failed Audio still blocks App, but a Skipped Audio must not. App keyed on `settings.CompanionAppPath` — inside the action, if `!File.Exists(path)` throw the existing companion-app-not-found message verbatim (the one currently at lines 76-77), else call `_appController.LaunchOrFocus(path)`.

    In `ToggleToNormalMode`, change ONLY the Audio block (lines 303-317) — the Monitor try/catch (lines 273-301) is Phase 16 territory and must stay byte-for-byte. Replace `_audioController.Restore(snapshot.Audio)` with: if `string.IsNullOrEmpty(settings.NormalAudioDeviceId)` set a local `audioOutcome = Skipped`; else in a try/catch, if `TryResolveDevice(NormalAudioDeviceId) is null` throw `InvalidOperationException("The configured Normal-mode audio device could not be found. Open Settings and reselect it.")` (D-07, same friendly-message pattern as the Rig-mode case above), else `SetDefault(NormalAudioDeviceId)` and set `audioOutcome = Succeeded`; on catch trace and set `audioFailure = ex`, `audioOutcome = Failed` (keep isolate-and-continue — a monitor failure short-circuits later, but audio failure does not). Change the Audio `steps.Add` (lines 323-326) to use `audioOutcome` and `audioFailure?.Message` instead of the `audioFailure is null ? Succeeded : Failed` expression. `snapshot.Audio` becomes unread dead data — leave AudioState/CaptureState untouched (Phase 18 scope).

    In `ToggleToNormalMode`'s App block, change the unset branch (line 366) from `ToggleStepOutcome.NotAttempted` to `ToggleStepOutcome.Skipped` (per D-03/D-04 — an unset app path is deliberately-skipped, not blocked-by-failure). Keep the configured branch's try/catch minimize behavior as-is.

    Relax `IsFullyConfigured` (lines 201-205) to `settings.MonitorsToDisable?.Count > 0 || settings.MonitorsToEnable?.Count > 0` only — drop the NormalAudioDeviceId/RigAudioDeviceId/CompanionAppPath terms (D-05). Update the surrounding doc comment (lines 188-200) to drop the "mirrors the four fields SettingsForm requires" framing; it now gates on the monitor set only.

    Reword the stale `IsFullyConfigured`-guard exception string (lines 66-67) to reference only the monitor-set requirement — drop "both audio devices, and the companion app path". `grep -rn "both audio devices" src/RigToggle.Core/` must return zero matches after this task.
  </action>
  <verify>
    <automated>grep -q "TryExecuteOptionalStep" src/RigToggle.Core/ToggleService.cs && grep -q "SetDefault(settings.NormalAudioDeviceId" src/RigToggle.Core/ToggleService.cs && ! grep -q "Restore(snapshot.Audio)" src/RigToggle.Core/ToggleService.cs && ! grep -q "both audio devices" src/RigToggle.Core/ToggleService.cs && dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - ToggleService.cs defines `TryExecuteOptionalStep` which delegates to `TryExecuteStep` (no duplicated try/catch)
    - The top-level `File.Exists(settings.CompanionAppPath)` preflight throw is gone; the check now lives inside the Rig App step body
    - `ToggleToNormalMode` Audio step calls `SetDefault(settings.NormalAudioDeviceId)` and never `Restore(snapshot.Audio)`; the Monitor try/catch block is unchanged (git diff shows no change to lines around monitorFailure/`_monitorController.Restore(snapshot.Monitor)`)
    - `ToggleToNormalMode` App unset branch records `ToggleStepOutcome.Skipped`, not NotAttempted
    - `IsFullyConfigured` gates on the monitor set only
    - `grep -rn "both audio devices" src/RigToggle.Core/` returns zero matches
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` exits 0 (all Task 1 tests pass, including the removal of the two invalidated audio-restore-throws tests)
  </acceptance_criteria>
  <done>Audio and App are optional in both directions with Skipped-vs-Failed kept distinct; Normal-mode audio applies via SetDefault; the toggle gate and stale messages reflect monitor-set-only configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings → ToggleService | Nullable app path + audio device IDs loaded from settings.json drive which toggle steps run |
| ToggleService → OS (File.Exists → Process.Start; audio device set) | Configured targets are resolved and acted on at toggle time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information Disclosure / Tampering | Optional Audio/App step guards | mitigate | "Unset" and "configured-but-broken" are kept structurally distinct: null/empty → Skipped; set-but-unresolvable → Failed via TryResolveDevice/File.Exists checks inside the step body. Prevents a broken/removed target being silently downgraded to "skipped" (Pitfall 1/3). Enforced by paired Skipped+Failed tests per field. |
| T-15-03 | Denial of Service (safety-relevant step skipped) | IsFullyConfigured relaxation | accept | Only Audio/App become optional; the monitor-disable step (the safety-relevant display action) is NOT optional and still gates the toggle via the monitor-set check. No safety step can be silently disabled by leaving a field unset. |
| T-15-09 | Tampering (TOCTOU) | File.Exists → later Process.Start in App step | accept | Pre-existing accepted risk T-03-09 (ToggleService.cs comment): fail-fast UX guard, not a security control. Moving the check into the App step body does not change its strictness — the check still runs when the path is configured. Do not re-litigate. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` exits 0 with the new Skipped/Failed coverage passing.
- `grep -rn "both audio devices" src/RigToggle.Core/` returns zero matches (Pitfall 4 verification).
- `grep -q "Restore(snapshot.Audio)" src/RigToggle.Core/ToggleService.cs` returns nonzero exit (the snapshot-audio restore is gone from the Normal-mode Audio step).
- `grep -q "WhenAudioRestoreThrows" src/RigToggle.Tests/ToggleServiceTests.cs` returns nonzero exit (the two invalidated audio-restore-throws tests are gone/renamed).
- The Monitor try/catch and `_monitorController.Restore(snapshot.Monitor)` call in ToggleToNormalMode are unchanged (Phase 16 boundary respected).
</verification>

<success_criteria>
- Unset app path → toggle-to-Rig skips launch/focus and toggle-to-Normal skips minimize, each as a Skipped step, no error (APP-04).
- Configured-but-missing app path → Failed App step (APP-05).
- Unset Rig audio → Skipped on toggle-to-Rig (AUDIO-03).
- Configured Normal audio → applied via SetDefault on toggle-to-Normal; unset → Skipped (AUDIO-04).
- Configured-but-gone audio device → Failed step in that direction (AUDIO-05).
- IsFullyConfigured gates on the monitor set only; stale messages reworded.
</success_criteria>

<output>
Create `.planning/phases/15-optional-app-audio-targets/15-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
