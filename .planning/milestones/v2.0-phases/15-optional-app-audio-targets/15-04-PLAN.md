---
phase: 15-optional-app-audio-targets
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified: []
autonomous: false
requirements: [APP-04, APP-05, AUDIO-03, AUDIO-04, AUDIO-05]

must_haves:
  truths:
    - "The full solution builds and all automated tests pass after the phase's Core + App changes"
    - "On the real rig, leaving app/audio unset skips those steps cleanly with no error and no false 'did not complete' warning"
    - "On the real rig, a configured-but-broken target (moved .exe / removed audio device) surfaces as a real failure"
    - "On the real rig, a configured Normal-mode audio device actually applies on toggle-to-Normal"
  artifacts:
    - path: ".planning/phases/15-optional-app-audio-targets/15-04-SUMMARY.md"
      provides: "Rig verification record for the five Phase 15 success criteria"
      contains: "AUDIO-04"
  key_links:
    - from: "SettingsForm unset affordances (Plan 03)"
      to: "ToggleService optional steps (Plan 02)"
      via: "an unset target chosen in Settings is honored as a Skipped step at toggle time"
      pattern: "Skipped"
---

<objective>
Confirm, on the real rig hardware, that the optional-target behavior works end-to-end: unset targets skip cleanly, configured-but-broken targets fail loudly, and the Normal-mode audio device actually applies. Automated tests (Plan 02) prove the branching logic with fakes; this plan proves the same against real Windows audio endpoints, a real companion-app path, and the real toggle-result UI so the "skipped" and "failed" states render as intended on this specific Windows 11 rig.

Purpose: This phase changes both validation gating and a genuine runtime behavior (Normal-mode audio via SetDefault). The skip-vs-fail distinction and the checklist rendering are things only rig verification can fully confirm. Consistent with this project's UAT-per-phase discipline.

Output: A green build/test run plus a human-confirmed rig verification of all five success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-optional-app-audio-targets/15-CONTEXT.md
@.planning/phases/15-optional-app-audio-targets/15-02-SUMMARY.md
@.planning/phases/15-optional-app-audio-targets/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-solution regression gate (build + all tests)</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/15-optional-app-audio-targets/15-02-SUMMARY.md (what Core behavior landed)
    - .planning/phases/15-optional-app-audio-targets/15-03-SUMMARY.md (what App changes landed)
  </read_first>
  <action>
    Build the whole solution and run the full test suite to catch any cross-project regression before asking for rig time. Run `dotnet build RigToggle.sln` and `dotnet test RigToggle.sln`. If any failure appears, stop and report it (do not proceed to the rig checkpoint) — the most likely miss is Pitfall 3 (a ToggleResult with an intentionally-unset target reporting Success == false) or a downstream consumer not handling the Skipped arm; both surface as failing tests here.
  </action>
  <verify>
    <automated>dotnet build RigToggle.sln && dotnet test RigToggle.sln</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build RigToggle.sln` exits 0
    - `dotnet test RigToggle.sln` exits 0 (all RigToggle.Tests and RigToggle.Windows.Tests pass)
    - No `grep -rn "both audio devices" src/` matches remain (final phase-wide check of Pitfall 4)
  </acceptance_criteria>
  <done>The full solution compiles and every automated test passes, so rig verification starts from a known-green state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 15 made the companion-app path and both audio devices optional, gave the Normal-mode audio device real runtime effect, and added a distinct "Skipped (not configured)" toggle-result state. In Settings there is now a Clear button next to the app-path box and a "(None — don't switch audio)" entry in each audio dropdown; Save is enabled with only a valid monitor grid.
  </what-built>
  <how-to-verify>
    Run the published/debug app on the rig and verify each success criterion:

    1. APP-04 (unset app): In Settings, click Clear on the app path (box shows "No app shortcut or .exe selected", Clear becomes disabled), Save. Toggle to Rig, then back to Normal. Expect: the toggle-result checklist shows "App: Skipped (not configured)" both directions, no error dialog, no "did not fully complete" warning.
    2. AUDIO-03 (unset Rig audio): Set the Rig audio dropdown to "(None — don't switch audio)", Save, toggle to Rig. Expect: "Audio: Skipped (not configured)"; your default audio device is unchanged.
    3. AUDIO-04 (Normal audio applies): Set a specific Normal-mode audio device (different from the current default), Save, toggle to Rig then back to Normal. Expect: on toggle-to-Normal the Windows default playback device actually switches to the configured Normal device (verify in the Windows sound flyout), and "Audio: OK" shows.
    4. APP-05 (broken app path): Configure a valid .exe, Save, then move/rename that .exe on disk. Toggle to Rig. Expect: "App: FAILED (...)" with the friendly "could not be found ... Open Settings and reselect..." message — NOT skipped.
    5. AUDIO-05 (removed audio device): Configure a USB/removable audio device as the Rig (or Normal) device, Save, unplug it, then toggle in that direction. Expect: "Audio: FAILED (...)" with the "configured ... audio device could not be found. Open Settings and reselect it." message — NOT skipped.
    6. D-06 (broken still blocks Save): With a moved .exe still configured (not cleared), reopen Settings. Expect: Save is disabled and the app-path warning shows; clicking Clear then re-Saving succeeds.
  </how-to-verify>
  <read_first>
    - .planning/phases/15-optional-app-audio-targets/15-CONTEXT.md (D-01..D-07 for the expected affordances and wording tone)
  </read_first>
  <acceptance_criteria>
    - All six checks above observed as described on the rig, OR any deviation recorded with specifics in 15-04-SUMMARY.md
    - Skipped steps render as "Skipped (not configured)" and never trigger a failure warning
    - Broken targets render as FAILED with the friendly reselect message, never as Skipped
    - The Normal-mode audio device is confirmed to actually change the Windows default on toggle-to-Normal (AUDIO-04)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six checks pass, or describe any deviation for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → running app on rig | Human-driven verification of persisted-settings-to-toggle behavior against real hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Repudiation / Information Disclosure | Skipped-vs-Failed rendering on the rig | mitigate | Rig checkpoint explicitly verifies that unset renders as Skipped and broken renders as FAILED — the last line of defense against the two states being conflated in the real UI (they render via the shared formatter, verified once here for both dialog and tray paths). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet build RigToggle.sln` and `dotnet test RigToggle.sln` both exit 0.
- Human confirms all six rig checks (APP-04, APP-05, AUDIO-03, AUDIO-04, AUDIO-05, D-06) pass or logs deviations.
</verification>

<success_criteria>
- Full solution builds and all automated tests pass.
- On the rig: unset targets skip cleanly with no error; broken targets fail with the friendly reselect message; the Normal-mode audio device actually applies on toggle-to-Normal.
</success_criteria>

<output>
Create `.planning/phases/15-optional-app-audio-targets/15-04-SUMMARY.md` when done.
</output>
