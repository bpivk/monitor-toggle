---
phase: 18-cleanup-pass-exe-size-reduction
plan: 05
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - src/RigToggle.Tests/Doubles/FakeControllers.cs
  - src/RigToggle.Tests/Doubles/BlockingMonitorController.cs
  - src/RigToggle.Tests/ToggleServiceTests.cs
  - src/RigToggle.Core/Models/MonitorState.cs
autonomous: true
requirements: [CLEANUP-01, CLEANUP-02]

must_haves:
  truths:
    - "No test double declares a Restore member and no test knob exists for a Restore failure that can no longer happen"
    - "No assertion in the suite asserts the absence of a method that no longer exists anywhere in the codebase"
    - "Grepping the whole src tree for Restore-as-an-API returns nothing; only historical prose remains, and only where it explains why today's design is what it is"
    - "The full solution builds with 0 errors and the core suite passes with no test lost"
  artifacts:
    - path: "src/RigToggle.Tests/Doubles/FakeControllers.cs"
      provides: "Recording fakes for the three mutation-adapter interfaces, matching the post-cleanup contracts exactly"
    - path: ".planning/phases/18-cleanup-pass-exe-size-reduction/18-05-SUMMARY.md"
      provides: "The tree-wide zero-reference audit output closing CLEANUP-01, plus before/after test totals"
  key_links:
    - from: "src/RigToggle.Tests/Doubles/FakeControllers.cs"
      to: "IMonitorController / IAudioController"
      via: "the doubles implement exactly the post-cleanup member sets, no extras"
      pattern: "class FakeMonitorController : IMonitorController"
    - from: "src/RigToggle.Tests/ToggleServiceTests.cs"
      to: "CreateService fixture"
      via: "constructor knobs that all still drive at least one real test"
      pattern: "CreateService\\("
---

<objective>
Finish CLEANUP-01 by removing the last vestiges of the retired restore mechanism from the test doubles and test fixtures, close `15-REVIEW.md` IN-01's dead test knob, and run the tree-wide zero-reference audit that proves the subsystem is genuinely gone.

Purpose: plans 18-01 and 18-02 removed `Restore` from both contracts and both Windows adapters, deliberately leaving the doubles' now-extra `Restore` methods behind so those plans could run in parallel without file conflicts. Those methods, the `throwOnRestore` plumbing that drives one of them, and three assertions that check a nonexistent method is not called are all dead weight — and until they are gone, the codebase still reads as though a restore path exists.
Output: three cleaned test files, one corrected model doc comment, and a recorded tree-wide audit proving zero surviving references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-RESEARCH.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-PATTERNS.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-02-SUMMARY.md
@.planning/phases/15-optional-app-audio-targets/15-REVIEW.md

<environment_note>
This dev host is Linux. `dotnet` lives at `$HOME/.dotnet/dotnet` — prefix commands with `PATH="$HOME/.dotnet:$PATH"`.

`dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` and `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` both work. `src/RigToggle.Windows.Tests` builds but cannot execute here — build only, and record it as "built, not executed (dev-host limitation)".
</environment_note>

<measured_baselines>
Exact current locations, verified against the working tree at planning time. Confirm each with a fresh `grep -n` before editing, since plans 18-01 and 18-02 land before this one and line numbers in other files will have shifted (these four files are untouched by those plans, so their numbers should still hold):

- `src/RigToggle.Tests/Doubles/FakeControllers.cs` is **183 lines**. `FakeMonitorController.Restore` occupies **lines 77-80**. `FakeAudioController` declares `private readonly bool _throwOnRestore;` at **line 87**, takes `bool throwOnRestore = false,` as its third constructor parameter at **line 93**, assigns `_throwOnRestore = throwOnRestore;` at **line 98**, and declares `Restore` at **lines 120-131** (its body logs `audio.Restore:...` and conditionally throws on `_throwOnRestore`). `FakeAudioController` also has `_deviceExists`/`deviceExists`, which are **live** (they drive `TryResolveDevice`, a Phase 15 / AUDIO-05 feature) and must stay.
- `src/RigToggle.Tests/Doubles/BlockingMonitorController.cs` is **67 lines**; the `Restore` no-op stub is at **lines 63-66**. Its class doc comment states "The other five IMonitorController members return the same minimal fixture values FakeMonitorController uses" — after this plan that count becomes four.
- `src/RigToggle.Tests/ToggleServiceTests.cs` is **578 lines**. `CreateService` declares `bool audioThrowsOnRestore = false,` at **line 50** and passes it through at **line 63** as `new FakeAudioController(callLog, throwOnRestore: audioThrowsOnRestore, deviceExists: !audioDeviceMissing)`. **No test in the file passes `audioThrowsOnRestore: true`** — this is exactly `15-REVIEW.md` IN-01. Three vacuous assertions exist: **lines 142 and 143** (`audio.Restore` and `monitor.Restore`) and **line 529** (`audio.Restore`). The test at **line 130** is named `ToggleToNormalMode_AppliesNormalAudioDeviceViaSetDefault_AndAppliesExplicitMonitorSet_NeverRestore`. Only one call site passes a positional argument (`CreateService(settings, monitorMutatesBeforeThrowingOnDisable: true)` at line 219) and `settings` is the first parameter, so removing the second parameter is safe for every caller.
- `src/RigToggle.Core/Models/MonitorState.cs` line 9 reads "TargetDevicePath names which entry is the configured monitor to disable, so Disable/Restore need no second settings read" — a doc reference to a removed API.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` reported `Total: 85` before Phase 18 began; plan 18-01 removes exactly 5 tests and plan 18-04 adds exactly 1. **This plan must remove zero tests.**
</measured_baselines>

<scope_boundary>
This plan removes **methods, fields, parameters and assertions only** — it must not remove a single `[Fact]` or `[Theory]`. Every test in `ToggleServiceTests.cs` keeps its coverage; three assertions inside two of them go away because they assert the absence of something that cannot exist.

Do not touch: `FakeAppController` (already has no restore-style member and is the shape the other two converge toward), `_deviceExists`/`deviceExists` on `FakeAudioController`, `_throwOnDisable`/`_mutatesBeforeThrowingOnDisable`/`_disableWasCalled` on `FakeMonitorController` (all three drive live tests — `_disableWasCalled` is read at line 52 and set at line 67), `BlockingMonitorController`'s blocking `DeactivateMonitors` mechanism, `InMemoryStores.cs` (plan 18-01 owns it), `ToggleResultFormatterTests.cs` or `ToggleService.cs` (plan 18-04 owns those), and anything under `src/RigToggle.Windows*` (plans 18-02 and 18-03 own those).
</scope_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip the Restore members and the dead throwOnRestore knob from the test doubles</name>
  <files>src/RigToggle.Tests/Doubles/FakeControllers.cs, src/RigToggle.Tests/Doubles/BlockingMonitorController.cs, src/RigToggle.Core/Models/MonitorState.cs</files>
  <read_first>
    - src/RigToggle.Tests/Doubles/FakeControllers.cs (all 183 lines — you need the exact extent of both `Restore` methods, the `_throwOnRestore` field/parameter/assignment triple, and confirmation of which other flags are live)
    - src/RigToggle.Tests/Doubles/BlockingMonitorController.cs (all 67 lines — the class doc comment counts its members and must be corrected)
    - src/RigToggle.Core/Abstractions/IMonitorController.cs and src/RigToggle.Core/Abstractions/IAudioController.cs (post-18-01/18-02 state — these define exactly which members each double must implement; the doubles should implement that set and nothing more)
    - src/RigToggle.Core/Models/MonitorState.cs (12 lines — its doc comment references the removed API)
    - .planning/phases/15-optional-app-audio-targets/15-REVIEW.md section IN-01 (the reviewer's stated fix names both the `CreateService` parameter and the `throwOnRestore`/`_throwOnRestore` plumbing as one unit)
  </read_first>
  <action>
    In `src/RigToggle.Tests/Doubles/FakeControllers.cs`:
    - Delete `FakeMonitorController.Restore(MonitorState previousState)` (lines 77-80) in full. It was the only writer of the `monitor.Restore:` call-log label.
    - Delete `FakeAudioController.Restore(AudioState previousState)` (lines 120-131) in full, including its inner `if (_throwOnRestore)` block and the gap-closure 03-04 comment inside it. It was the only writer of the `audio.Restore:` call-log label.
    - Delete the `private readonly bool _throwOnRestore;` field (line 87), the `bool throwOnRestore = false,` constructor parameter (line 93), and the `_throwOnRestore = throwOnRestore;` assignment (line 98). After removal, `FakeAudioController`'s constructor takes exactly three parameters: `List<string> callLog`, `string? capturedDefaultDeviceId = "fake-normal-device"`, and `bool deviceExists = true`.
    - Keep `FakeAudioController`'s `_deviceExists`/`deviceExists` (they drive `TryResolveDevice`) and all of `FakeMonitorController`'s disable-failure flags including `_disableWasCalled`, which is read at line 52 and set at line 67 by the still-live mutate-then-throw path.
    - Update the file's class doc comment (lines 6-11): it currently says each call "appends a label to a shared call-log so tests can assert both call order (e.g. **snapshot Save must precede any mutation**) and the values passed through from settings". The snapshot store no longer exists, so that parenthetical example is stale — replace it with a call-order example drawn from what the fakes actually record today, such as the enable-set activation preceding the disable-set deactivation.
    - After the deletions, confirm `FakeMonitorController` and `FakeAudioController` each implement exactly the members their interface declares and no more, matching the lean shape `FakeAppController` already has.

    In `src/RigToggle.Tests/Doubles/BlockingMonitorController.cs`: delete the `public void Restore(MonitorState previousState)` no-op stub (lines 63-66) and its `// No-op — not exercised by the reentrancy test.` body comment. Change the class doc comment's phrase "The other five IMonitorController members" to "four", since the interface now declares five members total and `DeactivateMonitors` is the one this double specializes. Leave the `_enteredGuardedRegion`/`_releaseFirstCall` blocking mechanism, the 5-second timeout guard, and the 07-RESEARCH.md provenance notes entirely alone — that mechanism is the whole point of this file.

    In `src/RigToggle.Core/Models/MonitorState.cs`: the record itself stays (it is live — both controllers' `CaptureState()` return it and `ToggleService` compares instances of it). Only fix the doc comment at lines 3-11, which describes the record as existing "to restore the exact prior configuration" and says "TargetDevicePath names which entry is the configured monitor to disable, so Disable/Restore need no second settings read". Rewrite it to describe the record's actual live purpose: a full-topology capture of every active display path at capture time, used by `ToggleService`'s CR-01 unchanged-versus-mutated comparison after a failed monitor step. Do not reference `Restore` as a current API. Keep the DISPLAY-02 and Phase-4-shape provenance as historical context if you want, but phrase it as history.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Restore" src/RigToggle.Tests/Doubles/FakeControllers.cs` prints `0`
    - `grep -c "Restore" src/RigToggle.Tests/Doubles/BlockingMonitorController.cs` prints `0`
    - `grep -c "Restore" src/RigToggle.Core/Models/MonitorState.cs` prints `0`
    - `grep -c "throwOnRestore" src/RigToggle.Tests/Doubles/FakeControllers.cs` prints `0`
    - `grep -c "deviceExists\|_deviceExists" src/RigToggle.Tests/Doubles/FakeControllers.cs` prints at least `3` (the live `TryResolveDevice` knob survived: field, parameter, assignment)
    - `grep -c "_disableWasCalled" src/RigToggle.Tests/Doubles/FakeControllers.cs` prints `3` (declaration, read, write — the live mutate-then-throw path survived)
    - `grep -c "snapshot Save" src/RigToggle.Tests/Doubles/FakeControllers.cs` prints `0`
    - `grep -c "_releaseFirstCall\|_enteredGuardedRegion" src/RigToggle.Tests/Doubles/BlockingMonitorController.cs` prints at least `2` (the blocking mechanism survived)
    - `grep -c "five IMonitorController members" src/RigToggle.Tests/Doubles/BlockingMonitorController.cs` prints `0`
    - `grep -n "public sealed record MonitorState" src/RigToggle.Core/Models/MonitorState.cs` still prints one line (the record itself was not deleted)
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>Neither test double declares a `Restore` member or a `throwOnRestore` knob, all live knobs and the blocking mechanism survive, `MonitorState`'s doc comment describes its real current purpose, and the solution builds — though `ToggleServiceTests` still passes the removed parameter until Task 2, so the test project may not compile yet.</done>
</task>

<task type="auto">
  <name>Task 2: Close IN-01's dead test knob, drop the vacuous assertions, and run the tree-wide zero-reference audit</name>
  <files>src/RigToggle.Tests/ToggleServiceTests.cs, .planning/phases/18-cleanup-pass-exe-size-reduction/18-05-SUMMARY.md</files>
  <read_first>
    - src/RigToggle.Tests/ToggleServiceTests.cs (all 578 lines — you need `CreateService`'s full signature and body, every one of its ~22 call sites to confirm none passes the removed parameter, and the exact context of the three assertions being deleted)
    - src/RigToggle.Tests/Doubles/FakeControllers.cs (post-Task-1 state — `FakeAudioController`'s constructor now takes three parameters and the call site at line 63 must match)
    - .planning/phases/15-optional-app-audio-targets/15-REVIEW.md section IN-01
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md and 18-02-SUMMARY.md (what those plans already removed, so the audit output can be attributed correctly)
  </read_first>
  <action>
    In `src/RigToggle.Tests/ToggleServiceTests.cs`:

    - **IN-01:** delete the `bool audioThrowsOnRestore = false,` parameter from `CreateService` (line 50) and update the `FakeAudioController` construction on line 63 to drop the `throwOnRestore: audioThrowsOnRestore` named argument, leaving `new FakeAudioController(callLog, deviceExists: !audioDeviceMissing)`. Verify with `grep -n "CreateService("` that no caller passes a second positional argument — the only multi-argument call is `CreateService(settings, monitorMutatesBeforeThrowingOnDisable: true)` at line 219, where `settings` binds to the first parameter and the rest are named, so no call site needs editing.

    - **Vacuous assertions:** delete the two `Assert.DoesNotContain(callLog, entry => entry.StartsWith(...))` lines at 142 (`audio.Restore`) and 143 (`monitor.Restore`), and the one at 529 (`audio.Restore`). These assert that a method which no longer exists in the codebase is not called — after this phase they can never fail, so they are noise rather than regression guards. Delete only those three lines. Every other assertion in both tests stays, including the `Assert.Contains` lines around them that check `audio.SetDefault`, `monitor.ActivateMonitors` and `monitor.DeactivateMonitors` — those are the assertions that carry the real meaning, and after the deletion each affected test still asserts the positive behavior it was written for.

    - **Vestigial test name:** rename the test at line 130 from `ToggleToNormalMode_AppliesNormalAudioDeviceViaSetDefault_AndAppliesExplicitMonitorSet_NeverRestore` to the same name with the `_NeverRestore` suffix removed. Update its leading comment, which currently ends "neither subsystem calls a snapshot-based Restore any more" — reword it to state positively what the test proves: Normal-mode audio applies via `SetDefault(NormalAudioDeviceId)` and Normal-mode monitors apply their own explicit set via Activate-then-Deactivate.

    - **Historical comments:** two comments legitimately describe the retired design as history to explain why today's code is shaped as it is — the one around line 206 ("previously only Rig mode could reach a guarded DeactivateMonitors failure at all, since Restore() could never leave zero monitors active by construction") and the one around line 396 ("the old 'Restore before re-disabling the enable-set' D-02 asymmetry is retired"). These are allowed to keep the word `Restore`, but each must read unambiguously as past-tense history. Adjust the wording if either could be misread as describing current behavior; do not delete them — they encode why the current guard and ordering exist.

    Then run the tree-wide CLEANUP-01 closing audit and record every command's verbatim output in the summary:
    - `grep -rn "\.Restore(" src --include="*.cs"` — must return nothing.
    - `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot\|InMemorySnapshotStore" src --include="*.cs"` — must return nothing.
    - `grep -rn "RestoreViaReconstruction\|CopyOutputTechnology\|AssignSource\|_originalPathsCache\|throwOnRestore" src --include="*.cs"` — must return nothing.
    - `grep -rn "Restore" src --include="*.cs"` — will return a small residue of bare-word matches. Enumerate every one of them in the summary and justify each individually as either (a) the English word in an unrelated context, such as `MainForm.cs` around line 583 describing window restore-from-tray, or (b) an explicitly past-tense historical note explaining a current design decision. If any hit is neither, fix it before finishing.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "audioThrowsOnRestore\|throwOnRestore" src/RigToggle.Tests/ToggleServiceTests.cs` prints `0`
    - `grep -c "DoesNotContain(callLog, entry => entry.StartsWith(\"audio.Restore\"))" src/RigToggle.Tests/ToggleServiceTests.cs` prints `0`
    - `grep -c "monitor.Restore" src/RigToggle.Tests/ToggleServiceTests.cs` prints `0`
    - `grep -c "_NeverRestore" src/RigToggle.Tests/ToggleServiceTests.cs` prints `0`
    - `grep -c "new FakeAudioController(callLog, deviceExists: !audioDeviceMissing)" src/RigToggle.Tests/ToggleServiceTests.cs` prints `1`
    - `grep -c "\[Fact\]\|\[Theory\]" src/RigToggle.Tests/ToggleServiceTests.cs` prints `23` — unchanged from the pre-plan count; this plan removes assertions and parameters, never a test
    - `grep -c "audio.SetDefault\|monitor.ActivateMonitors\|monitor.DeactivateMonitors" src/RigToggle.Tests/ToggleServiceTests.cs` prints a number greater than 0 (the positive assertions that carry the real meaning survived)
    - `grep -rn "\.Restore(" src --include="*.cs"` returns nothing (exit status 1)
    - `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot\|InMemorySnapshotStore" src --include="*.cs"` returns nothing (exit status 1)
    - `grep -rn "RestoreViaReconstruction\|CopyOutputTechnology\|AssignSource\|_originalPathsCache\|throwOnRestore" src --include="*.cs"` returns nothing (exit status 1)
    - Every remaining hit of `grep -rn "Restore" src --include="*.cs"` is listed verbatim in the summary with a one-line justification each
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0` with a Total equal to the total this project reported at the start of this task (no test gained or lost)
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
    - `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` reports `0 Error(s)`; record "built, not executed (dev-host limitation)"
  </acceptance_criteria>
  <done>`CreateService` has no dead knob, no assertion checks for a nonexistent method, no test was lost, the tree-wide audit returns zero API-level references to the retired restore subsystem, and every surviving bare-word `Restore` is individually justified in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test doubles → production contracts | Doubles implement `IMonitorController`/`IAudioController`; drift between double and contract silently weakens what the suite actually proves |
| regression suite → shipped behavior | Removing assertions changes what the suite can catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Tampering | Removal of `Assert.DoesNotContain(... "audio.Restore" ...)` / `"monitor.Restore"` | mitigate | These assertions become unfalsifiable once no `Restore` method exists anywhere — they would pass unconditionally and give false assurance. The positive assertions in the same tests (`audio.SetDefault`, `monitor.ActivateMonitors`, `monitor.DeactivateMonitors`) are explicitly required to survive by acceptance criteria, so each affected test still proves the behavior it was written for. |
| T-18-05-02 | Denial of Service | Accidental deletion of a live test-double knob | mitigate | `_deviceExists` (drives the AUDIO-05 missing-device path) and `_disableWasCalled` (drives the mutate-then-throw CR-01 path) are named as must-survive, and acceptance criteria grep-assert their exact occurrence counts rather than relying on the suite merely compiling. |
| T-18-05-03 | Repudiation | Silent test-count regression | mitigate | Acceptance criteria pin `ToggleServiceTests.cs` at exactly 23 `[Fact]`/`[Theory]` declarations and require the project's test Total to be unchanged across this plan, so a removed test cannot slip through as "cleanup". |
| T-18-05-04 | Tampering | Reintroduction of a snapshot-restore path | mitigate | The tree-wide audit is the closing gate for CLEANUP-01: four greps must return empty, and any residual bare-word `Restore` must be individually justified in writing rather than waved through. |
| T-18-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan — deletions and comment edits only. Package changes are confined to plan 18-03. |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` → `0 Error(s)`
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` → `Failed: 0`, Total unchanged across this plan
- `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` → builds; record "not executed (dev-host limitation)"
- `grep -rn "\.Restore(" src --include="*.cs"` → no output
- `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot\|InMemorySnapshotStore" src --include="*.cs"` → no output
- `grep -rn "RestoreViaReconstruction\|CopyOutputTechnology\|AssignSource\|_originalPathsCache\|throwOnRestore" src --include="*.cs"` → no output
- `grep -c "\[Fact\]\|\[Theory\]" src/RigToggle.Tests/ToggleServiceTests.cs` → `23`
</verification>

<success_criteria>
No test double, fixture parameter or assertion references the retired restore mechanism; `15-REVIEW.md` IN-01 is closed; the tree-wide audit returns zero API-level references to `Restore`, the snapshot types, or the two removed CCD helpers, with every residual bare-word occurrence individually justified; no test was removed; and the full solution builds and passes.
</success_criteria>

<output>
Create `.planning/phases/18-cleanup-pass-exe-size-reduction/18-05-SUMMARY.md` when done, containing the verbatim output of all four audit greps, the per-hit justification table for any residual bare-word `Restore`, the before/after `[Fact]`/`[Theory]` count for `ToggleServiceTests.cs`, and the final build and test output.
</output>
