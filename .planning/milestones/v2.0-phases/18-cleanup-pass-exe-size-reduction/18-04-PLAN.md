---
phase: 18-cleanup-pass-exe-size-reduction
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.App/SettingsForm.cs
  - src/RigToggle.Core/ToggleResultFormatter.cs
  - src/RigToggle.Core/ToggleService.cs
  - src/RigToggle.Tests/ToggleResultFormatterTests.cs
autonomous: true
requirements: [CLEANUP-02]

must_haves:
  truths:
    - "PopulateAudioCombo has no unreachable empty-list branch and no 'No audio devices detected.' string"
    - "Selecting the '(None — don't switch audio)' sentinel persists a null device name rather than the sentinel's UI sentence"
    - "The checklist's Skipped line matches the capitalization convention of its sibling not-attempted line, and that rendering is covered by a test"
    - "ReconcileModeAfterMonitorFailure's two branches are observably distinct via diagnostic tracing instead of both silently doing nothing"
    - "No user-visible dialog, grid, combo box or toggle outcome changes as a result of this plan"
  artifacts:
    - path: "src/RigToggle.App/SettingsForm.cs"
      provides: "PopulateAudioCombo with a single unconditional body, and sentinel-aware audio device name persistence"
    - path: "src/RigToggle.Core/ToggleResultFormatter.cs"
      provides: "Checklist rendering with consistent lowercase status wording across the Skipped and NotAttempted arms"
    - path: "src/RigToggle.Tests/ToggleResultFormatterTests.cs"
      provides: "Regression coverage for the Skipped arm's exact rendered string"
      contains: "FormatChecklist_SkippedStep"
    - path: "src/RigToggle.Core/ToggleService.cs"
      provides: "ReconcileModeAfterMonitorFailure with per-case Trace diagnostics distinguishing no-change, partial-mutation and recapture-failure"
  key_links:
    - from: "src/RigToggle.App/SettingsForm.cs"
      to: "AppSettings.NormalAudioDeviceName / RigAudioDeviceName"
      via: "BtnSaveSettings_Click sentinel-aware assignment"
      pattern: "audioNormalItem.Id is null"
    - from: "src/RigToggle.Core/ToggleResultFormatter.cs"
      to: "src/RigToggle.Tests/ToggleResultFormatterTests.cs"
      via: "exact-string assertion on the Skipped arm"
      pattern: "skipped \\(not configured\\)"
---

<objective>
Close the four still-open, already-reviewed code-quality findings that prior phases deliberately deferred to "Phase 18 cleanup scope": an unreachable defensive branch, a UI sentence leaking into persisted settings, an inconsistent user-facing status word, and a pair of branches that both silently do nothing.

Purpose: CLEANUP-02 is a bounded pass over findings that three prior code reviews already identified, triaged and deferred — not an open-ended refactor. Each item below traces to a specific numbered finding in `15-REVIEW.md` or `16-REVIEW.md` with a reviewer-proposed fix.
Output: four small, individually-verifiable edits plus one new regression test, with no user-facing behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-RESEARCH.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-PATTERNS.md
@.planning/phases/15-optional-app-audio-targets/15-REVIEW.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-REVIEW.md

<environment_note>
This dev host is Linux. `dotnet` lives at `$HOME/.dotnet/dotnet` — prefix commands with `PATH="$HOME/.dotnet:$PATH"`.

`dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` and `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` both work. `src/RigToggle.Windows.Tests` builds but cannot execute here — build only.

`SettingsForm` is a WinForms UI file that cannot be run or visually inspected on this host. All four edits in this plan are deliberately chosen to be source-verifiable and non-visual; the rig checkpoint in plan 18-06 provides the functional confirmation.
</environment_note>

<measured_baselines>
Exact current locations, verified against the working tree at planning time:

- `src/RigToggle.App/SettingsForm.cs` is 1186 lines. `PopulateAudioCombo` is declared at **line 702**; its unreachable `if (items.Count == 0)` opens at **line 713**; the `combo.Items.Add("No audio devices detected.");` inside it is at **line 717**; the `else` follows immediately after that block's closing brace.
- `src/RigToggle.App/SettingsForm.cs` lines **1089-1092** hold the four audio assignments in `BtnSaveSettings_Click`: `NormalAudioDeviceId = audioNormalItem.Id`, `NormalAudioDeviceName = audioNormalItem.DisplayLabel`, `RigAudioDeviceId = audioRigItem.Id`, `RigAudioDeviceName = audioRigItem.DisplayLabel`.
- `src/RigToggle.Core/ToggleResultFormatter.cs` **line 32** renders `$"{step.StepName}: not attempted"` (lowercase) and **line 33** renders `$"{step.StepName}: Skipped (not configured)"` (capitalized). Line 31 renders `: OK` and line 34 renders `: FAILED (...)`.
- `src/RigToggle.Tests/ToggleResultFormatterTests.cs` has **no test at all** covering the `Skipped` arm — `grep -n "Skipped" src/RigToggle.Tests/ToggleResultFormatterTests.cs` returns nothing. The `NotAttempted` arm is covered at line 52 by an exact-string assertion.
- `src/RigToggle.Core/ToggleService.cs` is 468 lines; `ReconcileModeAfterMonitorFailure` is declared at **line 246** and ends at **line 263**. The file's established diagnostic idiom is the fully-qualified `System.Diagnostics.Trace.WriteLine($"...")`, used at lines 185, 285, 363, 417 and 439.
- `grep -rn "NormalAudioDeviceName\|RigAudioDeviceName" src --include="*.cs"` confirms these two settings fields are **write-only in production**: the only production write is `SettingsForm.cs:1090,1092` and there is no production read anywhere. This is what makes the IN-03 fix a zero-user-facing-change edit.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` currently reports `Total: 85`.
</measured_baselines>

<scope_boundary>
CLEANUP-02 for this phase is **exactly** the four findings below. Do not perform a fresh sweep for additional cruft, and do not expand any item beyond the reviewer's own proposed fix:

- `15-REVIEW.md` **IN-04** — unreachable `items.Count == 0` branch in `PopulateAudioCombo`
- `15-REVIEW.md` **IN-03** — sentinel display label persisted as the audio device *name*
- `15-REVIEW.md` **IN-02** — capitalization mismatch in `FormatChecklist`'s Skipped line
- `16-REVIEW.md` **WR-03** — `ReconcileModeAfterMonitorFailure`'s two functionally identical branches

Explicitly OUT of scope, do not touch:
- `17-REVIEW.md` **WR-02** — recorded as a deliberate non-fix and a locked Phase 17 planning decision. Changing it would override a prior design decision from outside this phase's scope.
- `16-REVIEW.md` **WR-04** (unguarded `_modeStore.Save`) — already fixed in Phase 16 (the try/catch and its Trace line exist at `ToggleService.cs:285`). Do not re-open it.
- `src/RigToggle.Tests/ToggleServiceTests.cs`, `src/RigToggle.Tests/Doubles/**` — plan 18-05 owns those files. Do not add or edit tests there, even for WR-03.
- Any `.csproj` or `.pubxml` — plan 18-03 owns those.
- Any snapshot/`Restore` code — plans 18-01 and 18-02 own those.

`15-REVIEW.md` IN-01 (the dead `audioThrowsOnRestore` test knob) is also a CLEANUP-02 item but lives in files owned by plan 18-05, which closes it there.
</scope_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the unreachable audio-combo branch and stop persisting the sentinel label as a device name</name>
  <files>src/RigToggle.App/SettingsForm.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.cs lines 685-765 (`PopulateAudioPickers` prepending the sentinel unconditionally, then the whole of `PopulateAudioCombo` including both branches, the `ShowStaleWarning` call, and the trailing `ThemeApplier.ThemeComboBox` call that must survive)
    - src/RigToggle.App/SettingsForm.cs lines 1075-1110 (the `BtnSaveSettings_Click` settings-construction block containing the four audio assignments)
    - .planning/phases/15-optional-app-audio-targets/15-REVIEW.md sections IN-03 and IN-04 (the reviewer's exact stated fix for each)
    - src/RigToggle.Core/Models/AppSettings.cs (confirm `NormalAudioDeviceName`/`RigAudioDeviceName` are declared `string?`, so assigning null is legal and needs no other change)
  </read_first>
  <action>
    **IN-04 — collapse the unreachable branch.** In `PopulateAudioCombo` (declared line 702), delete the entire `if (items.Count == 0) { ... }` block that opens at line 713, together with the 4-line leading comment above it beginning "15-03/D-02: items always contains at least the '(None...)' sentinel now". Delete the `else` keyword and its braces, promoting the else-branch body to be the method's unconditional body at the correct indentation level. The promoted body starts with `combo.Enabled = true;` and runs through the `combo.SelectedItem = items.First(i => i.Id is null);` in its inner else. The string literal `"No audio devices detected."` disappears with the deleted block and must not survive anywhere in the file.

    Everything before and after the collapsed block stays: the three leading statements (`errProvider.SetError(combo, string.Empty);`, `warningLabel.Visible = false;`, `combo.SelectedIndexChanged -= OnPickerChanged;`) and the three trailing statements (`combo.SelectedIndexChanged += OnPickerChanged;`, the 12-05/CR-03 comment, and `ThemeApplier.ThemeComboBox(combo, IsDarkTheme);`). Preserve the inner comments explaining the fresh-`List`-per-combo `CurrencyManager` guard, the `ShowStaleWarning` path, and the 15-03/D-02 explicit-sentinel-selection rationale — all three still describe live code.

    **IN-03 — stop persisting the sentinel sentence as a device name.** In `BtnSaveSettings_Click`, change line 1090 from `NormalAudioDeviceName = audioNormalItem.DisplayLabel,` to a conditional that yields `null` when the sentinel is selected and the display label otherwise, keyed on `audioNormalItem.Id is null`. Apply the identical change to line 1092 for `RigAudioDeviceName` keyed on `audioRigItem.Id is null`. Leave lines 1089 and 1091 (the two `*AudioDeviceId` assignments) exactly as they are — the sentinel's `Id` is already `null` there, which is the intended representation.

    This is a persistence-shape fix with no user-facing effect: both `*AudioDeviceName` fields are write-only in production (verified — there is no production read anywhere in `src/`), so nothing displayed to the user changes. What changes is that a diagnostics dump or hand-inspection of `settings.json` will show `null` instead of the literal UI sentence `"(None — don't switch audio)"` sitting in a field labelled "device name".

    Add a brief inline comment above the two changed assignments citing `15-REVIEW.md IN-03` and stating why: the sentinel's `DisplayLabel` is UI copy, not a device name, and persisting it misleads any future consumer of `settings.json`.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "No audio devices detected" src/RigToggle.App/SettingsForm.cs` prints `0`
    - `grep -c "items.Count == 0" src/RigToggle.App/SettingsForm.cs` prints `0`
    - `grep -n "private void PopulateAudioCombo" src/RigToggle.App/SettingsForm.cs` still prints exactly one line
    - `grep -c "ThemeApplier.ThemeComboBox(combo, IsDarkTheme)" src/RigToggle.App/SettingsForm.cs` prints at least `1` (the trailing theming call survived the collapse)
    - `grep -c "combo.SelectedIndexChanged -= OnPickerChanged" src/RigToggle.App/SettingsForm.cs` and `grep -c "combo.SelectedIndexChanged += OnPickerChanged" src/RigToggle.App/SettingsForm.cs` each print at least `1`
    - `grep -c "ShowStaleWarning(errProvider, combo, warningLabel, \"audio device\")" src/RigToggle.App/SettingsForm.cs` prints `1` (the stale-saved-device warning path survived)
    - `grep -c "audioNormalItem.Id is null" src/RigToggle.App/SettingsForm.cs` prints `1`
    - `grep -c "audioRigItem.Id is null" src/RigToggle.App/SettingsForm.cs` prints `1`
    - `grep -c "NormalAudioDeviceName = audioNormalItem.DisplayLabel," src/RigToggle.App/SettingsForm.cs` prints `0` and `grep -c "RigAudioDeviceName = audioRigItem.DisplayLabel," src/RigToggle.App/SettingsForm.cs` prints `0`
    - `grep -n "NormalAudioDeviceId = audioNormalItem.Id," src/RigToggle.App/SettingsForm.cs` and `grep -n "RigAudioDeviceId = audioRigItem.Id," src/RigToggle.App/SettingsForm.cs` each still print one line (the Id assignments are unchanged)
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>`PopulateAudioCombo` has one unconditional body with all live behavior (theming, stale warning, sentinel selection, CurrencyManager guard) intact, the dead branch and its string are gone, and selecting the audio sentinel now persists a null device name instead of UI copy.</done>
</task>

<task type="auto">
  <name>Task 2: Align the checklist Skipped wording with test coverage, and make the mode-reconcile branches observably distinct</name>
  <files>src/RigToggle.Core/ToggleResultFormatter.cs, src/RigToggle.Tests/ToggleResultFormatterTests.cs, src/RigToggle.Core/ToggleService.cs</files>
  <read_first>
    - src/RigToggle.Core/ToggleResultFormatter.cs (61 lines — read all of it; the `FormatChecklist` switch arms at lines 31-35 are the target and the doc comment above them claims the wording is "byte-identical to MainForm's current wording, relocated verbatim", which this change makes stale)
    - src/RigToggle.Tests/ToggleResultFormatterTests.cs (the existing `FormatChecklist_NotAttemptedStep_RendersNotAttempted` test at lines 45-53 is the exact shape to copy for the new Skipped test — note how it constructs a `ToggleResult` from a single `ToggleStepResult`)
    - src/RigToggle.Core/Models/ToggleResult.cs or wherever `ToggleStepResult`/`ToggleStepOutcome` are declared (confirm the constructor argument order used by the existing tests before writing the new one)
    - src/RigToggle.Core/ToggleService.cs lines 230-290 (the `ReconcileModeAfterMonitorFailure` doc comment and body, plus the neighbouring `Trace.WriteLine` at line 285 that establishes the file's diagnostic idiom)
    - .planning/phases/15-optional-app-audio-targets/15-REVIEW.md section IN-02 and .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-REVIEW.md section WR-03
  </read_first>
  <action>
    **IN-02 — make the two status words consistent.** In `src/RigToggle.Core/ToggleResultFormatter.cs`, change the `ToggleStepOutcome.Skipped` arm (line 33) so it renders `skipped (not configured)` with a lowercase leading `s` instead of `Skipped (not configured)`. This is the reviewer's primary proposed fix: the sibling `NotAttempted` arm on line 32 renders lowercase `not attempted`, and both strings appear side by side in the same MessageBox/toast checklist. Change only the leading capital letter of that one word; leave the `(not configured)` parenthetical, the `{step.StepName}: ` prefix, and the other three arms (`OK`, `FAILED (...)`, `unknown`) untouched. Update the `FormatChecklist` doc comment's claim that the wording is "byte-identical to MainForm's current wording (relocated verbatim, not reworded)" — that is no longer true for the Skipped arm; note the IN-02 alignment and cite `15-REVIEW.md IN-02`.

    **Cover it with a test.** In `src/RigToggle.Tests/ToggleResultFormatterTests.cs`, add one `[Fact]` named exactly `FormatChecklist_SkippedStep_RendersLowercaseSkipped`, modelled directly on the existing `FormatChecklist_NotAttemptedStep_RendersNotAttempted`: build a `ToggleResult` containing a single `ToggleStepResult` for step name `Audio` with outcome `ToggleStepOutcome.Skipped` and a null reason, then assert the formatted output equals exactly `Audio: skipped (not configured)`. This arm currently has zero coverage, so this test is both the regression guard for the change and net-new coverage. Add no other tests to this file.

    **WR-03 — give the branches genuinely different behavior.** In `src/RigToggle.Core/ToggleService.cs`, `ReconcileModeAfterMonitorFailure` (line 246) currently has three code paths that all silently do nothing: the `MonitorStateUnchanged` early return, the fall-through partial-mutation case, and the `catch`. Take option 1 from `16-REVIEW.md` WR-03 — keep the branch structure and the `MonitorStateUnchanged` comparison, but emit a distinct diagnostic on each path so the recapture work produces observable value and the three cases are distinguishable in `debug.log`. Use this file's existing idiom exactly: the fully-qualified `System.Diagnostics.Trace.WriteLine($"...")` form seen at lines 185, 285, 363, 417 and 439.

    Emit three distinct messages:
    - Before the early `return` in the unchanged branch: state that the monitor step failed with no observable topology change and that the mode flag is deliberately left at its prior value.
    - On the fall-through partial-mutation path (where the existing "Partial mutation: leave the mode flag at its prior value (Assumptions Log A3, 16-RESEARCH.md)" comment sits): state that the monitor step failed after a partial topology mutation and that the mode flag is deliberately left at its prior value rather than guessed.
    - Inside the `catch`: include the caught exception in the interpolation (matching how line 185 and line 285 include `{ex}`) and state that state re-capture failed so nothing could be confirmed and the mode flag is left as-is.

    Do not change the method's behavior in any other way: it must still never call `_modeStore.Save(...)`, must still swallow the exception, and must still return normally on every path. Update the method's XML doc comment to note that the three cases are now individually traced. Do not touch `MonitorStateUnchanged`, the CR-01 helper, or the `_modeStore.Save` call sites at lines 124/361/285.

    Do not add or modify any test in `src/RigToggle.Tests/ToggleServiceTests.cs` — that file belongs to plan 18-05. The WR-03 change is diagnostics-only and is verified here by source assertion plus the existing suite continuing to pass.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true --filter "FullyQualifiedName~FormatChecklist" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs` prints `0`
    - `grep -c "skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs` prints `1`
    - `grep -c ": not attempted" src/RigToggle.Core/ToggleResultFormatter.cs` prints `1` and `grep -c ": FAILED (" src/RigToggle.Core/ToggleResultFormatter.cs` prints `1` (the sibling arms are untouched)
    - `grep -n "FormatChecklist_SkippedStep_RendersLowercaseSkipped" src/RigToggle.Tests/ToggleResultFormatterTests.cs` prints exactly one line
    - `grep -c "Audio: skipped (not configured)" src/RigToggle.Tests/ToggleResultFormatterTests.cs` prints `1`
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true --filter "FullyQualifiedName~FormatChecklist_SkippedStep_RendersLowercaseSkipped"` reports `Failed: 0, Passed: 1`
    - Record the test project's Total BEFORE this task and AFTER: the AFTER total must be exactly BEFORE + 1 (this task adds exactly one test and removes none)
    - `grep -c "System.Diagnostics.Trace.WriteLine" src/RigToggle.Core/ToggleService.cs` prints `8` (the 5 pre-existing occurrences at lines 185/285/363/417/439 plus the 3 new ones in `ReconcileModeAfterMonitorFailure`)
    - `sed -n '/private void ReconcileModeAfterMonitorFailure/,/^    }/p' src/RigToggle.Core/ToggleService.cs | grep -c "System.Diagnostics.Trace.WriteLine"` prints `3`
    - `sed -n '/private void ReconcileModeAfterMonitorFailure/,/^    }/p' src/RigToggle.Core/ToggleService.cs | grep -c "_modeStore.Save"` prints `0` (the method still never writes the mode flag)
    - `sed -n '/private void ReconcileModeAfterMonitorFailure/,/^    }/p' src/RigToggle.Core/ToggleService.cs | grep -c "MonitorStateUnchanged"` prints `1` (the comparison is retained, not removed)
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0`
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>The checklist's Skipped and NotAttempted lines share one capitalization convention and the Skipped arm now has exact-string test coverage it never had; `ReconcileModeAfterMonitorFailure` traces all three of its previously-indistinguishable cases while its behavior (never writes the mode flag, always returns normally) is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → `%APPDATA%\RigToggle\settings.json` | `BtnSaveSettings_Click` writes user-selected configuration to disk |
| app → `debug.log` (Trace listener) | `ReconcileModeAfterMonitorFailure`'s new diagnostics are written to a user-readable log file |
| toggle result → user-visible checklist | `FormatChecklist` output is rendered in a MessageBox/toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Information Disclosure | New `Trace.WriteLine` diagnostics in `ReconcileModeAfterMonitorFailure` | mitigate | The three messages describe monitor-topology change detection and mode-flag policy only. The `catch` arm interpolates the caught exception exactly as the five pre-existing `Trace.WriteLine` call sites in the same file already do — no new class of data reaches the log, and no credential, device path or user path is newly emitted. |
| T-18-04-02 | Tampering | Removal of the `items.Count == 0` defensive branch | mitigate | The branch is provably unreachable from the codebase's only call site: `PopulateAudioPickers` prepends the `(None …)` sentinel unconditionally before every call, so `items` always has at least one element. Acceptance criteria assert the surviving live paths (theming, stale warning, sentinel auto-selection, CurrencyManager guard) individually rather than relying on "it still compiles". |
| T-18-04-03 | Tampering | Sentinel-aware `*AudioDeviceName` persistence | accept | The two fields are write-only in production (grep-verified: zero production reads). Writing `null` instead of a UI sentence cannot alter resolution logic, which keys exclusively off `*AudioDeviceId`. A stale `settings.json` written by a prior version still deserializes, since both fields are `string?`. |
| T-18-04-04 | Spoofing | User-facing checklist wording change | accept | Lowercasing one status word in `FormatChecklist` cannot be used to misrepresent an outcome — the arm is still uniquely bound to `ToggleStepOutcome.Skipped` and is now pinned by an exact-string test that did not previously exist. |
| T-18-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan — source edits and one added test only. Package changes are confined to plan 18-03. |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` → `0 Error(s)`
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` → `Failed: 0`, total exactly one higher than the pre-task total recorded in Task 2
- `grep -c "No audio devices detected\|items.Count == 0" src/RigToggle.App/SettingsForm.cs` → `0`
- `grep -c "Skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs` → `0`
- `sed -n '/private void ReconcileModeAfterMonitorFailure/,/^    }/p' src/RigToggle.Core/ToggleService.cs | grep -c "System.Diagnostics.Trace.WriteLine"` → `3`
- `git diff --stat` touches only the four files in `files_modified` — confirm nothing under `src/RigToggle.Tests/Doubles/`, `src/RigToggle.Tests/ToggleServiceTests.cs`, or any `.csproj`/`.pubxml` was changed
</verification>

<success_criteria>
All four in-scope review findings (15-REVIEW IN-02, IN-03, IN-04 and 16-REVIEW WR-03) are closed with the reviewer's own proposed fix, one net-new regression test covers the previously-uncovered Skipped rendering, the out-of-scope findings (17-REVIEW WR-02, 16-REVIEW WR-04) are untouched, the solution builds with zero errors, the suite passes, and no user-visible dialog, grid, combo box or toggle outcome behaves differently.
</success_criteria>

<output>
Create `.planning/phases/18-cleanup-pass-exe-size-reduction/18-04-SUMMARY.md` when done, listing each finding ID with its resolution, the before/after test totals, and confirmation that 17-REVIEW WR-02 was deliberately not touched.
</output>
