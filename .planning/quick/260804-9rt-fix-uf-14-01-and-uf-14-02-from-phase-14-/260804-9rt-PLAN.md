---
phase: quick-260804-9rt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [UF-14-01, UF-14-02]

must_haves:
  truths:
    - "build.yml declares an explicit least-privilege permissions block (contents: read)"
    - "release.yml runs restore, build, and test before publishing the release exe"
    - "A v* tag pushed against untested code fails CI before any release asset is attached"
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "Build workflow with explicit permissions block"
      contains: "permissions:"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow with build/test gate before publish"
      contains: "dotnet test --no-build -c Release"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "dotnet publish"
      via: "restore -> build -> test steps ordered before publish"
      pattern: "dotnet test.*Release[\\s\\S]*dotnet publish"
---

<objective>
Close two CI-hardening findings from the Phase 14 security audit (UF-14-01, UF-14-02).

Purpose: Apply least-privilege token scope to the build workflow, and prevent
untested code from ever being published to a public GitHub Release.
Output: Hardened `build.yml` (explicit permissions) and `release.yml` (build/test gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/build.yml
@.github/workflows/release.yml
@.planning/milestones/v1.2-phases/14-readme-release-documentation/14-SECURITY.md

<constraints>
- Additive hardening ONLY — do not change triggers, runner OS (`windows-latest`),
  action version pins (`actions/checkout@v7`, `actions/setup-dotnet@v6`,
  `softprops/action-gh-release@v3`), or the existing publish / release-asset-attach steps.
- release.yml already has `permissions: contents: write` (correct for release) — leave it untouched.
- Mirror build.yml's existing steps exactly (same commands, same order) when adding
  the gate to release.yml.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add least-privilege permissions block to build.yml (UF-14-01)</name>
  <files>.github/workflows/build.yml</files>
  <action>Insert a top-level `permissions:` block declaring `contents: read` between the `on:` block and the `jobs:` block. The build job only checks out, restores, builds, and tests — it never writes to the repo, releases, or any other scope — so least-privilege is `contents: read`. Do NOT alter the `on:` triggers, `runs-on: windows-latest`, action pins, or any `run:` step.</action>
  <verify>
    <automated>grep -A1 '^permissions:' .github/workflows/build.yml | grep -q 'contents: read' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build.yml'))"</automated>
  </verify>
  <done>build.yml contains a top-level `permissions:` block with `contents: read`, the file is valid YAML, and no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add build/test gate before publish in release.yml (UF-14-02)</name>
  <files>.github/workflows/release.yml</files>
  <action>Insert three steps AFTER the `setup-dotnet` step and BEFORE the "Publish self-contained single-file exe" step: `dotnet restore`, then `dotnet build --no-restore -c Release`, then `dotnet test --no-build -c Release`. These mirror build.yml's steps exactly (same commands, same order) so a v* tag pushed against a commit that never passed CI fails at build/test before any exe is published or attached. Do NOT change the trigger, `permissions: contents: write`, `runs-on`, action pins, or the existing publish / action-gh-release steps.</action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" && grep -Pzoq 'dotnet restore[\s\S]*dotnet build --no-restore -c Release[\s\S]*dotnet test --no-build -c Release[\s\S]*dotnet publish' .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml runs restore -> build -> test (mirroring build.yml) in order before the publish step; file is valid YAML; publish and asset-attach steps unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR contributor → build.yml GITHUB_TOKEN | PR-controlled `.csproj`/`NuGet.config` can run arbitrary restore/MSBuild code under whatever default token scope applies |
| Pushed v* tag → release.yml publish | A tag can point at any commit, including code that never passed CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| UF-14-01 | Elevation of Privilege | `.github/workflows/build.yml` | mitigate | Declare explicit top-level `permissions: contents: read` so PR-triggered restore/build/test code cannot use a broader default GITHUB_TOKEN scope (Task 1) |
| UF-14-02 | Tampering | `.github/workflows/release.yml` | mitigate | Add restore/build/test gate before publish so a v* tag against untested code fails CI before any asset is attached (Task 2) |
</threat_model>

<verification>
- `.github/workflows/build.yml` has a top-level `permissions: contents: read` block.
- `.github/workflows/release.yml` runs `dotnet restore`, `dotnet build --no-restore -c Release`, `dotnet test --no-build -c Release` (in that order) before `dotnet publish`.
- Both files remain valid YAML.
- No triggers, runner OS, action pins, or existing publish/asset-attach steps changed.
</verification>

<success_criteria>
- UF-14-01 closed: build.yml has explicit least-privilege `contents: read` permissions.
- UF-14-02 closed: release.yml gates publish behind a passing build + test.
- Diff is strictly additive — no unrelated lines modified.
</success_criteria>

<output>
Create `.planning/quick/260804-9rt-fix-uf-14-01-and-uf-14-02-from-phase-14-/260804-9rt-SUMMARY.md` when done
</output>
