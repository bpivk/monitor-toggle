---
phase: 15-optional-app-audio-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Core/Models/ToggleStepOutcome.cs
  - src/RigToggle.Core/Models/ToggleResult.cs
  - src/RigToggle.Core/ToggleResultFormatter.cs
  - src/RigToggle.Core/Abstractions/IAudioController.cs
  - src/RigToggle.Tests/Doubles/FakeControllers.cs
autonomous: true
requirements: [APP-04, AUDIO-03, AUDIO-04, AUDIO-05]

must_haves:
  truths:
    - "A toggle step deliberately left unconfigured is representable as a distinct Skipped outcome, never conflated with NotAttempted"
    - "A ToggleResult whose steps are all Succeeded/Skipped reports Success == true (no false 'did not fully complete' warning)"
    - "The shared checklist formatter renders a Skipped step as its own non-alarming line, distinct from FAILED and 'not attempted'"
    - "ToggleService (Core) can ask whether an audio device ID still resolves, without referencing any Windows type"
  artifacts:
    - path: "src/RigToggle.Core/Models/ToggleStepOutcome.cs"
      provides: "Skipped enum member plus doc comment distinguishing Skipped from NotAttempted"
      contains: "Skipped"
    - path: "src/RigToggle.Core/Models/ToggleResult.cs"
      provides: "Success predicate widened to accept Skipped"
      contains: "Skipped"
    - path: "src/RigToggle.Core/ToggleResultFormatter.cs"
      provides: "FormatChecklist switch arm for Skipped"
      contains: "Skipped (not configured)"
    - path: "src/RigToggle.Core/Abstractions/IAudioController.cs"
      provides: "TryResolveDevice existence-check contract"
      contains: "TryResolveDevice"
    - path: "src/RigToggle.Tests/Doubles/FakeControllers.cs"
      provides: "FakeAudioController.TryResolveDevice with a deviceExists knob"
      contains: "TryResolveDevice"
  key_links:
    - from: "src/RigToggle.Core/Abstractions/IAudioController.cs"
      to: "src/RigToggle.Windows/WindowsAudioController.cs"
      via: "interface member already implemented by the concrete class (zero-logic pickup)"
      pattern: "TryResolveDevice"
    - from: "src/RigToggle.Tests/Doubles/FakeControllers.cs"
      to: "src/RigToggle.Core/Abstractions/IAudioController.cs"
      via: "FakeAudioController implements the new interface member"
      pattern: "TryResolveDevice"
---

<objective>
Establish the Core contracts and the test double that the Phase 15 optional-target behavior depends on: a distinct `Skipped` toggle outcome, a `Success` predicate that treats `Skipped` as non-failing, a checklist rendering for `Skipped`, and a `TryResolveDevice` existence-check on `IAudioController` so `ToggleService` can detect a configured-but-gone audio device without touching Windows types.

Purpose: These are pure contract/model additions with no orchestration logic. Landing them first (interface-first ordering) lets Plan 02's `ToggleService` rewrite and its tests build against fixed contracts rather than discovering them. Every change here is additive and leaves the existing behavior compiling and passing.

Output: Updated `ToggleStepOutcome`, `ToggleResult`, `ToggleResultFormatter`, `IAudioController`, and `FakeAudioController`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-optional-app-audio-targets/15-CONTEXT.md
@.planning/phases/15-optional-app-audio-targets/15-RESEARCH.md
@.planning/phases/15-optional-app-audio-targets/15-PATTERNS.md

<interfaces>
<!-- Current contracts being extended. Extracted from the repo this session; use directly. -->

From src/RigToggle.Core/Models/ToggleStepOutcome.cs (current — 3 members):
  enum ToggleStepOutcome { Succeeded, Failed, NotAttempted }

From src/RigToggle.Core/Models/ToggleResult.cs (current):
  record ToggleResult(IReadOnlyList<ToggleStepResult> Steps)
    Success => Steps.All(s => s.Outcome == ToggleStepOutcome.Succeeded)

From src/RigToggle.Core/Abstractions/IAudioController.cs (current — 4 members):
  GetPlaybackDevices() : IReadOnlyList<AudioDeviceInfo>
  CaptureState() : AudioState
  SetDefault(string deviceId) : void
  Restore(AudioState previousState) : void

From src/RigToggle.Windows/WindowsAudioController.cs (already implements, do NOT modify):
  public AudioDeviceInfo? TryResolveDevice(string? deviceId)  // returns null for "not found" and "enumerator threw"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skipped outcome, widen Success, render Skipped in the formatter</name>
  <files>src/RigToggle.Core/Models/ToggleStepOutcome.cs, src/RigToggle.Core/Models/ToggleResult.cs, src/RigToggle.Core/ToggleResultFormatter.cs</files>
  <read_first>
    - src/RigToggle.Core/Models/ToggleStepOutcome.cs (current enum + doc comment)
    - src/RigToggle.Core/Models/ToggleResult.cs (current Success predicate)
    - src/RigToggle.Core/ToggleResultFormatter.cs (current FormatChecklist switch, lines 24-35)
    - .planning/phases/15-optional-app-audio-targets/15-RESEARCH.md (Pitfall 3 — the Success-predicate regression, and the enum/formatter Code Examples)
  </read_first>
  <action>
    In ToggleStepOutcome.cs add a fourth member `Skipped` after `NotAttempted`. Extend the enum's XML doc comment to state the D-03 distinction explicitly: `NotAttempted` = a step blocked because an earlier step in a stop-on-first-failure sequence (ToggleToRigMode) already failed; `Skipped` (Phase 15/D-03) = a step deliberately left unconfigured by the user (optional App/Audio targets). State that these are NOT the same state and must never render identically. Do NOT rename or reuse `NotAttempted`.

    In ToggleResult.cs widen `Success` from `Steps.All(s => s.Outcome == ToggleStepOutcome.Succeeded)` to `Steps.All(s => s.Outcome is ToggleStepOutcome.Succeeded or ToggleStepOutcome.Skipped)`. Add a comment (per Pitfall 3) warning that reverting to a strict `== Succeeded` check would make every toggle with any optional target left unset report "did not fully complete" via MainForm's warning path.

    In ToggleResultFormatter.cs add one switch arm to `FormatChecklist`, before the `_ =>` fallback: `ToggleStepOutcome.Skipped => $"{step.StepName}: Skipped (not configured)"` (D-04 wording). Do not change the existing Succeeded/Failed/NotAttempted arms. This formatter is shared by both MainForm's dialog checklist and its tray/hotkey balloon paths — one arm fixes both surfaces.
  </action>
  <verify>
    <automated>grep -q "Skipped" src/RigToggle.Core/Models/ToggleStepOutcome.cs && grep -q "or ToggleStepOutcome.Skipped" src/RigToggle.Core/Models/ToggleResult.cs && grep -q "Skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs</automated>
  </verify>
  <acceptance_criteria>
    - ToggleStepOutcome.cs declares exactly four members: Succeeded, Failed, NotAttempted, Skipped
    - ToggleStepOutcome.cs doc comment contains both the word "Skipped" and the word "NotAttempted" and describes the distinction
    - ToggleResult.cs Success predicate matches `s.Outcome is ToggleStepOutcome.Succeeded or ToggleStepOutcome.Skipped`
    - ToggleResultFormatter.cs FormatChecklist contains the arm `ToggleStepOutcome.Skipped => $"{step.StepName}: Skipped (not configured)"`
    - The existing Succeeded/Failed/NotAttempted formatter arms are unchanged
  </acceptance_criteria>
  <done>The Skipped outcome exists, Success treats it as non-failing, and the shared formatter renders it as a distinct non-alarming line.</done>
</task>

<task type="auto">
  <name>Task 2: Promote TryResolveDevice onto IAudioController and implement it in FakeAudioController</name>
  <files>src/RigToggle.Core/Abstractions/IAudioController.cs, src/RigToggle.Tests/Doubles/FakeControllers.cs</files>
  <read_first>
    - src/RigToggle.Core/Abstractions/IAudioController.cs (current 4-member interface)
    - src/RigToggle.Windows/WindowsAudioController.cs (TryResolveDevice at lines 219-236 — already public, confirm the exact signature `AudioDeviceInfo? TryResolveDevice(string? deviceId)` so the interface member matches and the class picks it up with no impl change)
    - src/RigToggle.Tests/Doubles/FakeControllers.cs (FakeAudioController lines 83-129 — copy its call-log idiom exactly)
    - .planning/phases/15-optional-app-audio-targets/15-PATTERNS.md (FakeControllers.cs section — the required TryResolveDevice fake shape and call-log string convention)
  </read_first>
  <action>
    In IAudioController.cs add one member: `AudioDeviceInfo? TryResolveDevice(string? deviceId);`. Add an XML doc comment noting it is the AUDIO-05 cheap existence check, promoted from WindowsAudioController's existing helper, and that it returns null for both "not found" and "enumerator threw". Do not change the other four members. WindowsAudioController already implements this exact signature — do NOT touch WindowsAudioController.cs; the concrete class satisfies the interface automatically.

    In FakeControllers.cs, extend FakeAudioController: add a `private readonly bool _deviceExists;` field and a `bool deviceExists = true` constructor parameter assigned to it (default true preserves every existing test's behavior). Implement `public AudioDeviceInfo? TryResolveDevice(string? deviceId)` that appends `$"audio.TryResolveDevice:{deviceId}"` to `_callLog`, returns `null` when `string.IsNullOrEmpty(deviceId) || !_deviceExists`, otherwise returns `new AudioDeviceInfo(deviceId, "Fake Speakers")`. Follow the file's existing call-log-string convention (matching `audio.SetDefault:{deviceId}`) so ToggleServiceTests can assert on it.
  </action>
  <verify>
    <automated>grep -q "AudioDeviceInfo? TryResolveDevice(string? deviceId)" src/RigToggle.Core/Abstractions/IAudioController.cs && grep -q "audio.TryResolveDevice:" src/RigToggle.Tests/Doubles/FakeControllers.cs && grep -q "deviceExists" src/RigToggle.Tests/Doubles/FakeControllers.cs</automated>
  </verify>
  <acceptance_criteria>
    - IAudioController.cs declares `AudioDeviceInfo? TryResolveDevice(string? deviceId);`
    - WindowsAudioController.cs is unchanged (git diff shows no modification to that file)
    - FakeAudioController has a `deviceExists` constructor parameter defaulting to true
    - FakeAudioController.TryResolveDevice logs `audio.TryResolveDevice:{deviceId}` and returns null when the id is empty or `_deviceExists` is false, else a non-null AudioDeviceInfo
    - `dotnet build src/RigToggle.Tests/RigToggle.Tests.csproj` compiles (FakeAudioController satisfies the widened interface)
  </acceptance_criteria>
  <done>IAudioController exposes the existence check, WindowsAudioController satisfies it unchanged, and FakeAudioController can drive both the "device present" and "device gone" paths for downstream tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings → ToggleService | Nullable audio-device IDs / app path loaded from `%APPDATA%\RigToggle\settings.json` cross into toggle logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | ToggleStepOutcome.Skipped vs NotAttempted | mitigate | Distinct enum member + doc comment keeps "deliberately unconfigured" from being misread as "blocked by failure" — this plan establishes the two-states-never-collapsed invariant that the runtime uses. Low severity (local single-user tool). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase (RESEARCH Package Legitimacy Audit: N/A). Nothing to verify. |
</threat_model>

<verification>
- `dotnet build src/RigToggle.Tests/RigToggle.Tests.csproj` compiles (proves the widened IAudioController is satisfied by FakeAudioController; WindowsAudioController picks up the member with no change).
- `grep -rn "== ToggleStepOutcome.Succeeded" src/RigToggle.Core/Models/ToggleResult.cs` returns zero matches (the strict predicate is gone).
</verification>

<success_criteria>
- `ToggleStepOutcome.Skipped` exists and is documented as distinct from `NotAttempted`.
- `ToggleResult.Success` returns true for an all-Succeeded/Skipped result.
- `ToggleResultFormatter.FormatChecklist` renders Skipped as `"{name}: Skipped (not configured)"`.
- `IAudioController.TryResolveDevice` exists; `WindowsAudioController.cs` is unmodified; `FakeAudioController` implements it with a `deviceExists` knob.
- Existing tests still compile and pass (all changes additive).
</success_criteria>

<output>
Create `.planning/phases/15-optional-app-audio-targets/15-01-SUMMARY.md` when done.
</output>
