---
phase: 12-theme-infrastructure-live-theme-following
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - src/RigToggle.Windows/DwmTitleBar.cs
  - src/RigToggle.Windows/NativeMethods.cs
  - src/RigToggle.Windows/WindowsThemeProvider.cs
  - src/RigToggle.App/ThemeApplier.cs
  - src/RigToggle.App/MainForm.Designer.cs
  - src/RigToggle.App/MonitorConfirmDialog.Designer.cs
  - src/RigToggle.App/SettingsForm.Designer.cs
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.App/MonitorConfirmDialog.cs
  - src/RigToggle.App/SettingsForm.cs
autonomous: true
requirements: [THEME-03, THEME-04, THEME-05]

must_haves:
  truths:
    - "In dark mode the title bar of MainForm, SettingsForm, and MonitorConfirmDialog renders dark, not white (THEME-03)"
    - "In dark mode every button on all three forms (btnToggle, btnSettings, btnBrowse, btnSaveSettings, btnDiscardChanges, btnContinue, btnCancel) renders with a dark fill and light text, not stock white/light, at rest AND on hover/pressed (THEME-05)"
    - "In dark mode the two SettingsForm audio-device ComboBoxes (cboAudioNormal, cboAudioRig) render dark, not stock white (THEME-04)"
    - "Flipping the Windows theme live re-applies the dark title bar, dark buttons, and dark combos on any open form without restart"
  artifacts:
    - path: "src/RigToggle.Windows/DwmTitleBar.cs"
      provides: "ApplyRoundedCornersAndMica now sets DWMWA_USE_IMMERSIVE_DARK_MODE from a darkMode parameter"
      contains: "DWMWA_USE_IMMERSIVE_DARK_MODE"
    - path: "src/RigToggle.App/ThemeApplier.cs"
      provides: "ThemeButton + ThemeComboBox non-throwing/idempotent palette helpers"
      contains: "ThemeButton"
    - path: "src/RigToggle.App/SettingsForm.cs"
      provides: "cboAudioNormal/cboAudioRig + all buttons themed on load and OnThemeChanged; DWM call passes IsDarkTheme"
      contains: "ThemeComboBox"
  key_links:
    - from: "src/RigToggle.App/MainForm.cs / MonitorConfirmDialog.cs / SettingsForm.cs"
      to: "DwmTitleBar.ApplyRoundedCornersAndMica(Handle, dark)"
      via: "every call site passes _themeProvider.CurrentTheme == AppTheme.Dark"
      pattern: "ApplyRoundedCornersAndMica\\(Handle, "
    - from: "each form load + OnThemeChanged"
      to: "ThemeApplier.ThemeButton(...)"
      via: "one call per button at load and on live flip"
      pattern: "ThemeApplier\\.ThemeButton"
    - from: "SettingsForm load + OnThemeChanged"
      to: "ThemeApplier.ThemeComboBox(cboAudioNormal/cboAudioRig, dark)"
      via: "themed alongside the existing grid pattern"
      pattern: "ThemeApplier\\.ThemeComboBox"
---

<objective>
Close the three rig-confirmed dark-mode gaps from `12-04-SUMMARY.md`, root-caused in `12-REVIEW.md` (CR-01/CR-02/CR-03), plus the two low-cost robustness warnings (WR-01/WR-02). The original phase bet that `Application.SetColorMode(SystemColorMode.System)` alone would recolor the non-client title bar, `FlatStyle.System` buttons, and the audio `ComboBox`es. The Windows 11 rig proved that bet wrong for all three surfaces. This plan replaces those unproven assumptions with the same explicit, per-control theming pattern already established and rig-confirmed working for `dgvMonitors` and `txtHotkey`.

Purpose: THEME-03 (dark title bar), THEME-05 (flat, dark-themed buttons), THEME-04 (audio ComboBoxes recolored) — all live-following on theme flip, matching every other themed control.
Output: `DwmTitleBar` gains an explicit immersive-dark-mode call; `ThemeApplier` gains `ThemeButton`/`ThemeComboBox`; all three forms wire button + combo theming into their load and `OnThemeChanged` passes; two dialogs gain a `Dispose(bool)` unsubscribe backstop; `WindowsThemeProvider` gains a lock around its cross-thread theme read/write.

Task ordering note (buildability): `RigToggle.Windows` compiles independently of the App-layer call sites, so Task 1 (the `DwmTitleBar` signature change + provider lock) is verified with a project-scoped `RigToggle.Windows` build. The 6 App-layer `DwmTitleBar` call sites are updated in Task 2 (not deferred), so the `RigToggle.App` project compiles cleanly at the end of Task 2. Task 3 adds only runtime theming calls against helpers that already exist, so it carries the full-solution build + test gate. No task's verify command is ever expected to fail.

Scope guard: Do NOT re-open anything the rig confirmed working — launch-theme detection, the live-flip infrastructure itself, the monitor grid, `txtHotkey` states, the GroupBox→Panel layout, Mica/rounded corners, `MonitorConfirmDialog`'s existing theming, or the accepted D-01/D-03 limitations (light MessageBox popups, stale tray separator glyph color). This is a targeted correction of three incorrect runtime assumptions, not a re-theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-theme-infrastructure-live-theme-following/12-04-SUMMARY.md
@.planning/phases/12-theme-infrastructure-live-theme-following/12-REVIEW.md
@.planning/phases/12-theme-infrastructure-live-theme-following/12-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase — no exploration required. -->

IThemeProvider.CurrentTheme → RigToggle.Core.Models.AppTheme (Light | Dark).
  dark = _themeProvider.CurrentTheme == AppTheme.Dark.
  SettingsForm already exposes `private bool IsDarkTheme => _themeProvider.CurrentTheme == AppTheme.Dark;`.
  MainForm and MonitorConfirmDialog have `_themeProvider` but no equivalent property yet.

DwmTitleBar (RigToggle.Windows) — CURRENT signature:
  public static void ApplyRoundedCornersAndMica(IntPtr handle)
  Sets DWMWA_WINDOW_CORNER_PREFERENCE (33) + DWMWA_SYSTEMBACKDROP_TYPE (38) only.
  NativeMethods already declares (internal const) DWMWA_USE_IMMERSIVE_DARK_MODE = 20 — currently UNUSED.
  DwmSetWindowAttribute returns an HRESULT int and never throws (D-07) — no try/catch needed at the P/Invoke.

All 6 current ApplyRoundedCornersAndMica call sites (all updated in Task 2 to pass the dark flag):
  MainForm.cs:81 (OnThemeChanged), MainForm.cs:103 (ApplyDwmChrome)
  MonitorConfirmDialog.cs:51 (ctor), MonitorConfirmDialog.cs:69 (OnThemeChanged)
  SettingsForm.cs:83 (ctor), SettingsForm.cs:131 (OnThemeChanged)

The 7 buttons (all currently FlatStyle.System — Designer files):
  MainForm.Designer.cs: btnToggle (84), btnSettings (94)
  MonitorConfirmDialog.Designer.cs: btnContinue (63), btnCancel (73)
  SettingsForm.Designer.cs: btnBrowse (301), btnSaveSettings (401), btnDiscardChanges (412)

The 2 audio combos (SettingsForm.Designer.cs): cboAudioNormal (DropDownList), cboAudioRig (DropDownList).
  Populated by PopulateAudioCombo(ComboBox, ...) in SettingsForm.cs (~line 519); PopulateAudioPickers calls it for both.

ThemeApplier (RigToggle.App) existing pattern to mirror EXACTLY (idempotent, try/catch, never throws):
  public static void ThemeMonitorGrid(DataGridView grid, bool dark) { try { ... } catch { /* cosmetic-only */ } }

Dark-mode literal palette (UI-SPEC Color section — use these exact ARGB values, same as the existing grid/hotkey helpers):
  Cell/control surface #2D2D30 = Color.FromArgb(45,45,48)
  Primary text #F0F0F0 = Color.FromArgb(240,240,240)
  Grid lines / borders #464646 = Color.FromArgb(70,70,70)
  Button hover (dark, derived tone lighter than the surface) = Color.FromArgb(62,62,66)
  Button pressed (dark, derived tone darker than the surface) = Color.FromArgb(28,28,30)
Light mode: use the corresponding SystemColors.* (Control/ControlText for button fill+text, ControlLight/ControlDark for button hover/pressed; Window/WindowText for combos).

#13897 constraint (12-RESEARCH.md Pitfall 6 / dotnet/winforms#13897, open): "FlatAppearance border/hover/pressed colors don't reliably apply once dark mode is active when FlatStyle.Flat is used, producing visually broken buttons." The mitigation applied in ThemeButton below (BorderSize=0 + explicit MouseOverBackColor/MouseDownBackColor) is 12-REVIEW.md CR-02 "Fix Option 2": set BorderSize=0 and explicit colors rather than relying on the buggy auto-apply pipeline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RigToggle.Windows infra — immersive dark-mode title bar + provider lock</name>
  <files>src/RigToggle.Windows/DwmTitleBar.cs, src/RigToggle.Windows/NativeMethods.cs, src/RigToggle.Windows/WindowsThemeProvider.cs</files>
  <read_first>
    - src/RigToggle.Windows/DwmTitleBar.cs (full — current 2-attribute body + the doc comment that must be corrected)
    - src/RigToggle.Windows/NativeMethods.cs (lines 124-138 — the DWMWA_USE_IMMERSIVE_DARK_MODE const + its "never call it manually" comment that is now falsified)
    - src/RigToggle.Windows/WindowsThemeProvider.cs (full — CurrentTheme auto-property + OnUserPreferenceChanged read-compare-write)
    - .planning/phases/12-theme-infrastructure-live-theme-following/12-REVIEW.md (CR-01 fix block; WR-02 fix block)
  </read_first>
  <action>
    CR-01 (THEME-03): Change `DwmTitleBar.ApplyRoundedCornersAndMica` to take a second parameter `bool darkMode`. As the FIRST attribute call in the body (before the corner + backdrop calls), pass `int useDark = darkMode ? 1 : 0;` to `NativeMethods.DwmSetWindowAttribute(handle, NativeMethods.DWMWA_USE_IMMERSIVE_DARK_MODE, ref useDark, sizeof(int));` — same silent best-effort HRESULT posture as the two existing attribute calls (no try/catch needed, D-07). Keep the corner + backdrop calls unchanged after it. Update the class-level doc comment to state that the immersive dark-mode attribute IS now set explicitly here (the earlier "owned by Application.SetColorMode" bet was falsified by the Windows 11 rig test in 12-04 — SetColorMode does not flip the non-client title bar for these forms, so setting it manually is the fix and there is no double-set because SetColorMode demonstrably never touches it). In NativeMethods.cs, correct the comment above `DWMWA_USE_IMMERSIVE_DARK_MODE = 20` (lines ~128-133): remove the "declared for reference only -- never call it manually (Pitfall 1: double-set causes a title-bar color flash)" claim and note it is now called by DwmTitleBar per the 12-04 rig finding; the constant stays `internal`.

    WR-02: In WindowsThemeProvider, guard the cross-thread theme read/write per its own documented "ThemeChanged may fire off the UI thread" contract. Add `private readonly object _themeLock = new();`. Convert `CurrentTheme` from an auto-property to a manually-backed property whose getter returns the backing field under `lock (_themeLock)`, and wrap the read-compare-write inside `OnUserPreferenceChanged` (the `resolved != CurrentTheme` compare through the `CurrentTheme = resolved;` assignment) in the same `lock (_themeLock)` so two rapid UserPreferenceChanged events cannot race the diff-and-assign. Raise `ThemeChanged?.Invoke(...)` OUTSIDE the lock (do not hold the lock across the event dispatch). Keep the initial ctor assignment and Log calls behaviorally unchanged.

    This task changes only `RigToggle.Windows`. The 6 App-layer call sites that call the old 1-arg signature are updated in Task 2; because `RigToggle.Windows` has no reference to `RigToggle.App`, this project compiles independently and the verify below is a project-scoped build that stays green.
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.Windows/RigToggle.Windows.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DWMWA_USE_IMMERSIVE_DARK_MODE" src/RigToggle.Windows/DwmTitleBar.cs` is >= 1 (the attribute is now actually passed to DwmSetWindowAttribute, not just referenced in prose).
    - `grep -n "ApplyRoundedCornersAndMica(IntPtr handle, bool" src/RigToggle.Windows/DwmTitleBar.cs` matches (signature gained the darkMode parameter).
    - NativeMethods.cs no longer contains the string "never call it manually" (`grep -c "never call it manually" src/RigToggle.Windows/NativeMethods.cs` is 0).
    - `grep -c "_themeLock" src/RigToggle.Windows/WindowsThemeProvider.cs` is >= 3 (field + getter lock + OnUserPreferenceChanged lock).
    - `grep -c "ThemeChanged?.Invoke" src/RigToggle.Windows/WindowsThemeProvider.cs` is 1 and it is NOT inside the lock block (dispatch stays outside).
    - `dotnet build src/RigToggle.Windows/RigToggle.Windows.csproj -c Release` succeeds with 0 errors (this project has no dependency on the App-layer call sites, so it builds independently of Task 2's call-site updates).
  </acceptance_criteria>
  <done>DwmTitleBar sets the immersive dark-mode attribute from an explicit darkMode flag; NativeMethods comment corrected; WindowsThemeProvider's theme read/write is lock-guarded; the RigToggle.Windows project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 App call-site fixes + ThemeApplier button/combo helpers + Designer flat buttons + Dispose backstops</name>
  <files>src/RigToggle.App/MainForm.cs, src/RigToggle.App/MonitorConfirmDialog.cs, src/RigToggle.App/SettingsForm.cs, src/RigToggle.App/ThemeApplier.cs, src/RigToggle.App/MainForm.Designer.cs, src/RigToggle.App/MonitorConfirmDialog.Designer.cs, src/RigToggle.App/SettingsForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (OnThemeChanged 70-88; ApplyDwmChrome 99-109; existing usings incl. RigToggle.Core.Models)
    - src/RigToggle.App/MonitorConfirmDialog.cs (ctor 26-52; OnThemeChanged 58-76; usings — RigToggle.Core.Models is NOT imported yet)
    - src/RigToggle.App/SettingsForm.cs (ctor 56-104; IsDarkTheme 109; OnThemeChanged 120-153)
    - src/RigToggle.Windows/DwmTitleBar.cs (new 2-arg signature from Task 1)
    - src/RigToggle.App/ThemeApplier.cs (full — the ThemeMonitorGrid / ApplyHotkey* idempotent try/catch pattern to mirror; the class-level doc comment that currently, incorrectly, claims Button+ComboBox are "already owned by SetColorMode")
    - src/RigToggle.App/MainForm.Designer.cs (lines 80-95 btnToggle/btnSettings FlatStyle; Dispose(bool) lines 14-35 — the reference backstop that already unsubscribes _themeProvider.ThemeChanged)
    - src/RigToggle.App/MonitorConfirmDialog.Designer.cs (btnContinue/btnCancel lines 56-74; Dispose(bool) lines 14-21 — MISSING the unsubscribe)
    - src/RigToggle.App/SettingsForm.Designer.cs (btnBrowse 292-302, btnSaveSettings 394-402, btnDiscardChanges 404-413; Dispose(bool) lines 14-21 — MISSING the unsubscribe; cboAudioNormal 211-217, cboAudioRig 236-242)
    - .planning/phases/12-theme-infrastructure-live-theme-following/12-REVIEW.md (CR-02 fix Option 2; CR-03 ThemeComboBox example; WR-01 fix block)
    - .planning/phases/12-theme-infrastructure-live-theme-following/12-RESEARCH.md (Pitfall 6 / dotnet/winforms#13897)
  </read_first>
  <action>
    CR-01 call sites (do this FIRST so RigToggle.App compiles against Task 1's new 2-arg signature): Update all 6 `DwmTitleBar.ApplyRoundedCornersAndMica(Handle)` calls to pass the dark flag as the second argument.
      - SettingsForm.cs (ctor line 83, OnThemeChanged line 131): pass `IsDarkTheme` (the property already exists).
      - MainForm.cs (OnThemeChanged 81, ApplyDwmChrome 103) and MonitorConfirmDialog.cs (ctor 51, OnThemeChanged 69): add a private helper `private bool IsDark => _themeProvider.CurrentTheme == AppTheme.Dark;` to each of those two forms (mirroring SettingsForm.IsDarkTheme) and pass `IsDark`. Add `using RigToggle.Core.Models;` to MonitorConfirmDialog.cs (MainForm.cs already imports it).
    After this sub-step, the `RigToggle.App` project builds cleanly (all call sites now match the Task-1 signature); the ThemeButton/ThemeComboBox helpers added below are defined but not yet called, which compiles fine (their runtime wiring is Task 3).

    CR-02/CR-03 helpers: In ThemeApplier add two public static methods mirroring the existing idempotent/never-throw pattern (wrap the whole body in try/catch that leaves the control unchanged on failure):
      - `ThemeButton(Button button, bool dark)`: set `button.FlatStyle = FlatStyle.Flat;`, then `button.FlatAppearance.BorderSize = 0;`, `button.BackColor = dark ? Color.FromArgb(45,45,48) : SystemColors.Control;`, `button.ForeColor = dark ? Color.FromArgb(240,240,240) : SystemColors.ControlText;`, `button.FlatAppearance.MouseOverBackColor = dark ? Color.FromArgb(62,62,66) : SystemColors.ControlLight;`, `button.FlatAppearance.MouseDownBackColor = dark ? Color.FromArgb(28,28,30) : SystemColors.ControlDark;`. This deliberately replaces the native FlatStyle.System pipeline (which the 12-04 rig proved does NOT recolor these buttons in dark mode, review CR-02) with explicit palette colors — the same approach already working for the grid and hotkey box, and it also satisfies THEME-05's "flat, not 3D bevel" requirement in both themes. This is 12-REVIEW.md CR-02 "Fix Option 2": BorderSize=0 (NOT 1) avoids dotnet/winforms#13897's unreliable FlatAppearance border-color application in dark mode, and the explicit MouseOverBackColor/MouseDownBackColor pin the hover/pressed surfaces (the exact states #13897 says don't reliably auto-apply) so they stay dark-themed rather than flashing a stock light color. Add a "why, not what" code comment above the method (matching the D-03/D-08 comment style used elsewhere in this phase) that references dotnet/winforms#13897 and explains WHY FlatStyle.Flat here needs explicit BorderSize=0 + FlatAppearance hover/pressed overrides rather than relying on the buggy auto-apply pipeline.
      - `ThemeComboBox(ComboBox combo, bool dark)`: set `combo.FlatStyle = FlatStyle.Flat;`, `combo.BackColor = dark ? Color.FromArgb(45,45,48) : SystemColors.Window;`, `combo.ForeColor = dark ? Color.FromArgb(240,240,240) : SystemColors.WindowText;`.
    Correct the ThemeApplier class-level doc comment: remove the assertion that Button/ComboBox fill+text are "already owned by SetColorMode" (falsified by the 12-04 rig for the DropDownList combos and FlatStyle.System buttons); state they now have explicit overrides here for the same reason the grid and hotkey box do.

    CR-02 Designer: In all three Designer files, change every button's `FlatStyle = System.Windows.Forms.FlatStyle.System;` assignment to `FlatStyle.Flat` (btnToggle, btnSettings, btnContinue, btnCancel, btnBrowse, btnSaveSettings, btnDiscardChanges — 7 total). Update each button's adjacent code comment that references "FlatStyle.System" so it no longer contradicts the code (the comment prose currently justifies System; note the switch to explicit-color Flat theming per the 12-04 rig finding and the #13897 workaround). Do NOT change any other button property (Location/Size/Text/DialogResult/Click wiring stay identical). The runtime ThemeButton call (Task 3) also re-asserts FlatStyle.Flat, so this Designer change is the declarative default and belt-and-suspenders.

    WR-01 Dispose backstop: In MonitorConfirmDialog.Designer.cs and SettingsForm.Designer.cs, add `_themeProvider.ThemeChanged -= OnThemeChanged;` inside the `if (disposing)` block of `Dispose(bool)` (both members are accessible — same partial class as the .cs file; this mirrors MainForm.Designer.cs's existing backstop). This is a deterministic backstop in addition to the existing FormClosed unsubscribe; `-=` on an already-removed handler is a safe no-op, so the double-unsubscribe on the normal close path is harmless.
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "ApplyRoundedCornersAndMica(Handle)" src/RigToggle.App/` is 0 (every call now passes the dark flag) and `grep -rc "ApplyRoundedCornersAndMica(Handle, " src/RigToggle.App/` is 6.
    - `grep -c "IsDark" src/RigToggle.App/MainForm.cs` >= 1 and `grep -c "IsDark" src/RigToggle.App/MonitorConfirmDialog.cs` >= 1 (both gained the helper); `grep -c "using RigToggle.Core.Models" src/RigToggle.App/MonitorConfirmDialog.cs` is 1.
    - `grep -c "public static void ThemeButton" src/RigToggle.App/ThemeApplier.cs` is 1 and `grep -c "public static void ThemeComboBox" src/RigToggle.App/ThemeApplier.cs` is 1.
    - ThemeButton uses `FlatAppearance.BorderSize = 0` (grep `grep -c "BorderSize = 0" src/RigToggle.App/ThemeApplier.cs` >= 1) and NOT BorderSize = 1 (`grep -c "BorderSize = 1" src/RigToggle.App/ThemeApplier.cs` is 0).
    - ThemeButton sets both hover and pressed states: `grep -c "MouseOverBackColor" src/RigToggle.App/ThemeApplier.cs` >= 1 and `grep -c "MouseDownBackColor" src/RigToggle.App/ThemeApplier.cs` >= 1.
    - ThemeApplier documents the workaround: `grep -c "13897" src/RigToggle.App/ThemeApplier.cs` >= 1 (the #13897 rationale comment is present).
    - ThemeButton dark branch uses `FromArgb(45,45,48)` and `FromArgb(240,240,240)`; ThemeComboBox dark branch uses `FromArgb(45,45,48)` and `FromArgb(240,240,240)` (grep confirms both ARGB literals present in ThemeApplier.cs).
    - Both new methods contain a `catch` block (idempotent/never-throw), matching ThemeMonitorGrid.
    - `grep -rc "FlatStyle = System.Windows.Forms.FlatStyle.System" src/RigToggle.App/` is 0 (no button assignment left on System) and `grep -rc "FlatStyle = System.Windows.Forms.FlatStyle.Flat" src/RigToggle.App/` is >= 7.
    - `grep -c "_themeProvider.ThemeChanged -= OnThemeChanged" src/RigToggle.App/MonitorConfirmDialog.Designer.cs` is 1 and the same in src/RigToggle.App/SettingsForm.Designer.cs is 1.
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release` succeeds with 0 errors — the call-site fixes above make the App project (and its RigToggle.Windows reference) compile cleanly; the still-uncalled ThemeButton/ThemeComboBox helpers do not affect the build.
  </acceptance_criteria>
  <done>All 6 DWM call sites pass the live dark flag (App project builds clean); ThemeApplier exposes idempotent ThemeButton (BorderSize=0 + hover/pressed overrides, #13897-documented) / ThemeComboBox; all 7 buttons are FlatStyle.Flat in the Designers; both transient dialogs unsubscribe in Dispose(bool).</done>
</task>

<task type="auto">
  <name>Task 3: Wire button + combo theming into all three forms (load + live flip)</name>
  <files>src/RigToggle.App/MainForm.cs, src/RigToggle.App/MonitorConfirmDialog.cs, src/RigToggle.App/SettingsForm.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (OnThemeChanged 70-88; ApplyDwmChrome 99-109; InitializeTrayState 146-155 — the always-runs-on-both-startup-paths hook; the IsDark helper added in Task 2)
    - src/RigToggle.App/MonitorConfirmDialog.cs (ctor 26-52; OnThemeChanged 58-76; the IsDark helper added in Task 2)
    - src/RigToggle.App/SettingsForm.cs (IsDarkTheme 109; OnThemeChanged 120-153; SettingsForm_Load 155-194; PopulateAudioCombo 519-559)
    - src/RigToggle.App/ThemeApplier.cs (ThemeButton/ThemeComboBox from Task 2)
  </read_first>
  <action>
    All 6 DWM title-bar call sites and the per-form `IsDark`/`IsDarkTheme` helpers already exist (added in Task 2). This task only ADDS the button + combo runtime theming calls in the same places DWM chrome is (re)applied; do not re-touch the DWM calls.

    CR-02 button wiring: Theme every button at load AND on every live flip.
      - MainForm: in `InitializeTrayState()` (runs unconditionally on both normal and --tray startup, before first paint), after `ApplyDwmChrome()`, call `ThemeApplier.ThemeButton(btnToggle, IsDark);` and `ThemeApplier.ThemeButton(btnSettings, IsDark);`. In `OnThemeChanged`, after the DWM call, add the same two ThemeButton calls before `Refresh()`.
      - MonitorConfirmDialog: in the ctor after the DWM call, add `ThemeApplier.ThemeButton(btnContinue, IsDark);` and `ThemeApplier.ThemeButton(btnCancel, IsDark);`. Add the same two in `OnThemeChanged` after the DWM call.
      - SettingsForm: in `SettingsForm_Load` (after the existing grid theming) add `ThemeApplier.ThemeButton` for btnBrowse, btnSaveSettings, btnDiscardChanges. Add the same three in `OnThemeChanged` after the grid theming.

    CR-03 combo wiring: In SettingsForm, theme both audio combos at load and on live flip. Add `ThemeApplier.ThemeComboBox(combo, IsDarkTheme);` at the END of `PopulateAudioCombo(...)` (so it is re-applied every time the combos are (re)populated, after the DataSource rebind), and add `ThemeApplier.ThemeComboBox(cboAudioNormal, IsDarkTheme);` + `ThemeApplier.ThemeComboBox(cboAudioRig, IsDarkTheme);` in `OnThemeChanged` after the grid theming.

    Keep all existing marshal-then-try/catch structure in each OnThemeChanged intact — these additions go INSIDE the existing try block so a theming failure still cannot crash the form (T-12-02). Do NOT change any control text, layout, or non-theming behavior.
  </action>
  <verify>
    <automated>dotnet build RigToggle.sln -c Release &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ThemeApplier.ThemeButton" src/RigToggle.App/MainForm.cs` >= 4 (2 buttons × load + OnThemeChanged), same >= 4 in MonitorConfirmDialog.cs, and >= 6 in SettingsForm.cs (3 buttons × 2 sites).
    - `grep -c "ThemeApplier.ThemeComboBox" src/RigToggle.App/SettingsForm.cs` >= 3 (PopulateAudioCombo once, covering both combos, + 2 in OnThemeChanged).
    - `dotnet build RigToggle.sln -c Release` succeeds (0 errors) and `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` passes.
    - No control Text / Location / Size / DialogResult / Click wiring changed (pure theming additions).
  </acceptance_criteria>
  <done>Every form themes its buttons (and, for Settings, its audio combos) at load and on each theme flip; full solution build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS event thread → form UI thread | `ThemeChanged` may arrive off the UI thread while a form is open |
| Long-lived provider → transient dialogs | App-lifetime `WindowsThemeProvider` outlives fresh-per-open `SettingsForm`/`MonitorConfirmDialog` |
| Managed → native (dwmapi.dll) | Immersive-dark-mode + Mica attributes on a possibly-unsupported OS (Windows 10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Denial of Service | ThemeApplier.ThemeButton/ThemeComboBox + each form's OnThemeChanged | mitigate | New helpers wrapped in try/catch (cosmetic-only, leave control unchanged on failure); all new calls sit inside the existing marshaled try/catch OnThemeChanged bodies — a theming failure cannot crash toggle/save/confirm flows. |
| T-12-03 | Tampering | DwmTitleBar immersive-dark-mode call | mitigate | Best-effort non-throwing HRESULT (D-07); applied only with a real Handle already in scope; silent no-op on Windows 10 / unsupported builds — same posture as the existing corner/backdrop calls. |
| T-12-05 | Denial of Service | SettingsForm / MonitorConfirmDialog ThemeChanged subscription lifecycle | mitigate | WR-01: add `Dispose(bool)` unsubscribe backstop to both transient dialogs (mirrors MainForm) so an abnormal dispose path that never fires FormClosed cannot leak a closed dialog onto the app-lifetime provider. |
| T-12-RACE | Tampering | WindowsThemeProvider.CurrentTheme cross-thread read/write | mitigate | WR-02: lock-guard the read-compare-write and the getter under `_themeLock`; event dispatch stays outside the lock to avoid holding it across subscriber callbacks. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (12-RESEARCH.md Package Legitimacy Audit — all APIs are BCL / already-referenced shared framework). |
</threat_model>

<verification>
- `dotnet build src/RigToggle.Windows/RigToggle.Windows.csproj -c Release` succeeds after Task 1 (Windows project builds independently).
- `dotnet build src/RigToggle.App/RigToggle.App.csproj -c Release` succeeds after Task 2 (call sites fixed; App project + its Windows reference compile clean).
- `dotnet build RigToggle.sln -c Release` succeeds after Task 3 (0 errors) and `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` passes.
- All per-task grep gates pass — especially: immersive-dark-mode attribute actually called in DwmTitleBar; zero `FlatStyle.System` button assignments remaining; ThemeButton uses BorderSize=0 + explicit MouseOverBackColor/MouseDownBackColor + a #13897 rationale comment; both new ThemeApplier helpers present with catch blocks; all 6 DWM call sites pass the dark flag; both dialogs unsubscribe in Dispose(bool).
- Final pixel-level confirmation of the dark title bar, dark buttons (rest AND hover/pressed), and dark audio combos (and their live-flip behavior) is a real-Windows-rig check — deferred to plan 12-06 (this project's precedent from Phases 8/9/11: WinForms rendered appearance cannot be proven by build/grep alone).
</verification>

<success_criteria>
- Title bars of MainForm, SettingsForm, and MonitorConfirmDialog render dark in dark mode (THEME-03).
- All 7 buttons render flat with dark fill + light text in dark mode, on all three forms, at load and after a live flip, with hover/pressed states also dark (not a light flash) per the #13897 workaround (THEME-05).
- cboAudioNormal / cboAudioRig render dark in dark mode, at load and after a live flip (THEME-04).
- No new packages; solution builds; tests pass; the two robustness warnings (leak backstop, provider lock) are closed.
</success_criteria>

<output>
Create `.planning/phases/12-theme-infrastructure-live-theme-following/12-05-SUMMARY.md` when done.
Then run plan 12-06 (rig re-verification) to confirm closure on real Windows 11 hardware.
</output>
</content>
</invoke>
