---
phase: 27-monitor-activation-logic-redesign
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - src/RigToggle.Windows/WindowsMonitorController.cs
  - src/RigToggle.Core/Abstractions/IMonitorController.cs
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs
  - .planning/debug/knowledge-base.md
  - .planning/debug/monitor-position-regre.md
autonomous: false
requirements: [REDESIGN-03, REDESIGN-04]
user_setup: []

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The class-level summary of WindowsMonitorController describes the Extend-only architecture as current, with the 20-round scoped-activation narrative preserved verbatim but clearly demoted to a historical section (REDESIGN-03)."
    - "IMonitorController.ActivateMonitors's monitorSwapDisableSet doc describes the post-redesign consumer (the correction loop's exclusion filter), not the deleted scoped-plan array-exclusion step."
    - "No comment anywhere in src/ describes a deleted symbol as if it were live code."
    - ".planning/debug/knowledge-base.md's monitor-enable-reactivates-others-again entry no longer recommends the scoped-ApplyPathInfos approach without a superseding note, so a future debug session does not re-derive the reversed guidance."
    - "The debug session file's disposition matches the rig outcome — moved to resolved/ with status resolved only if Plan 01's rig checks passed, otherwise left in place with the reason recorded."
    - "The final published build runs on the rig: app launches, one tile toggle and one Rig/Normal toggle behave correctly after the deletions."
  artifacts:
    - "src/RigToggle.Windows/WindowsMonitorController.cs — class summary rewritten with a Historical record (pre-Phase-27) section"
    - "src/RigToggle.Core/Abstractions/IMonitorController.cs — monitorSwapDisableSet param doc rewritten"
    - ".planning/debug/knowledge-base.md — new Phase 27 entry plus a superseded note on the prior scoped-activation entry"
  key_links:
    - ".planning/debug/knowledge-base.md -> gsd-debugger's Phase-0 recall — the reason a stale recommendation here is a real defect, not a cosmetic one"
    - "IMonitorController doc -> ToggleService and MainForm call sites that must keep passing monitorSwapDisableSet correctly"
---

<objective>
Bring the documentation of record in line with the code that now exists, and confirm the deleted
build still works on the rig.

This phase deleted ~700 lines whose rationale was carried almost entirely in doc comments and in
`.planning/debug/knowledge-base.md`. Two of those records are now actively misleading rather than
merely stale: the `WindowsMonitorController` class summary opens by describing scoped activation
as the current primary mechanism, and the knowledge-base entry for
`monitor-enable-reactivates-others-again` closes with a recurrence guard that tells a future
debugger to "prefer a scoped `ApplyPathInfos` plan when one specific target needs activating" —
precisely the guidance this phase reversed. Because that knowledge base exists specifically to
surface known-pattern hypotheses at the start of new investigations, leaving it uncorrected would
seed the next debug session with the wrong recommendation.

Purpose: make the written record match reality, so the next person (or the next debug round) is
not re-derived into the architecture this phase just removed.

Output: corrected doc comments, a corrected and extended knowledge base, a closed-out debug
session file, and a final rig confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-monitor-activation-logic-redesign/27-01-SUMMARY.md
@.planning/phases/27-monitor-activation-logic-redesign/27-02-SUMMARY.md

Source of the historical narrative being preserved and of §22.4's open doc-convention question:
@.planning/debug/monitor-position-regre.md
</context>

<!-- planner-discipline-allow: TryBuildScopedActivationPlan -->
<!-- planner-discipline-allow: PromoteToOriginIfNeeded -->
<!-- planner-discipline-allow: SelectSourceForActivation -->
<!-- planner-discipline-allow: ShouldRetryScopedActivation -->
<!-- planner-discipline-allow: ShouldRetryNestedCorrectionActivation -->
<!-- planner-discipline-allow: PollUntilTargetsReachable -->

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the architecture documentation and purge stale symbol references from src/</name>
  <files>src/RigToggle.Windows/WindowsMonitorController.cs, src/RigToggle.Core/Abstractions/IMonitorController.cs, src/RigToggle.App/MainForm.cs, src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs</files>
  <read_first>
    - `src/RigToggle.Windows/WindowsMonitorController.cs` lines 14-300 — the entire class-level `&lt;summary&gt;` block being restructured. Read it in full before editing; it is ~286 lines of accumulated narrative and the task is to demote it, not to delete it.
    - `src/RigToggle.Core/Abstractions/IMonitorController.cs` lines 28-60 — the `ActivateMonitors` summary and the `monitorSwapDisableSet` param doc.
    - `src/RigToggle.App/MainForm.cs` around line 1106 — the comment inside `TryReactivelyCorrectAgainstLastIntent` that names the deleted plan builder.
    - `src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs` around line 16 — the file-level comment citing the deleted retry predicates as precedent for pure-seam testing discipline.
    - `.planning/debug/monitor-position-regre.md` §22.1 (target architecture, for the new leading text), §22.3 (the accepted-tradeoff wording), and §22.4's paragraph flagging the append-vs-rewrite doc convention as an open question for this planning round.
    - `.planning/phases/27-monitor-activation-logic-redesign/27-02-SUMMARY.md` — the recorded cache disposition, so the new summary describes the cache accurately.
  </read_first>
  <action>
    Resolve debug §22.4's open doc-convention question as follows, and state in `27-03-SUMMARY.md`
    that this plan made that call: restructure the class-level `&lt;summary&gt;` rather than appending
    to it, but preserve the pre-redesign narrative verbatim by moving it, unedited, beneath a new
    heading line reading exactly `Historical record (pre-Phase-27) -- retained verbatim:` inside
    the same summary block. Nothing from the existing narrative is deleted; it is relocated below
    that marker. This satisfies the file's append-don't-delete convention while making the current
    architecture the first thing a reader encounters.

    Write the new leading portion of the summary — everything above that marker — to state, in
    prose: that `GetActiveMonitors`, `GetAllMonitors`, `CaptureState`, and `DeactivateMonitors`
    behave as before; that as of Phase 27, `ActivateMonitors` performs exactly one activation call,
    `PathInfo.ApplyTopology(DisplayConfigTopologyId.Extend, allowPersistence: false)`, with no
    per-target scoping and no alternative path; that the in-call settle-poll-then-correct loop
    (bounded by `MaxCorrectionRounds` and `RequiredConsecutiveCleanRounds`, using
    `PollUntilStableActiveDevicePaths`, `ComputeUnexpectedlyActivated` into `DeactivateMonitors`,
    and `ComputeUnexpectedlyDeactivated` into a nested `ActivateMonitorsCore` call) is the sole
    in-call safety net; that `MainForm`'s `ArmIntentGuard` / `TryReactivelyCorrectAgainstLastIntent`
    remains a deliberately separate, out-of-call, OS-event-driven watchdog covering drift that
    surfaces seconds after any call returns; that verify-and-throw discipline is unchanged (D-03
    re-query, D-04 never `Screen.AllScreens` as oracle, D-05 no automatic rollback), with
    `CollateralMonitorRestoreFailedException` still distinguishing a failed nested restore from a
    failed top-level request; that a brief visible flash of an unrequested monitor turning on and
    being corrected back off is an EXPECTED and accepted consequence of whole-topology Extend, not
    a defect; and that the underlying external nondeterminism affecting one specific display
    (`root_cause (8)` in the cited debug session) is explicitly NOT claimed to be understood or
    eliminated by this redesign. Cite `.planning/debug/monitor-position-regre.md` §21 and §22 as
    the source. Describe the removed design in prose without naming any deleted member, so the
    leading section stays free of dangling identifiers.

    Also update the live-mode cache field's remark if Plan 02 retained the field, so the leading
    architecture description and that remark agree about whether anything reads it.

    In `src/RigToggle.Core/Abstractions/IMonitorController.cs`, rewrite the
    `&lt;param name="monitorSwapDisableSet"&gt;` body. Keep the `&lt;summary&gt;` block's Pitfall-2 statement
    that `ActivateMonitors` must run BEFORE `DeactivateMonitors` on rig-mode entry, and update its
    "via CCD Extend topology" phrasing to state that Extend is now the only mechanism. The new
    param text must say: the parameter still carries the device paths that an immediately-following
    `DeactivateMonitors` call, within the same logical Monitor step, is about to remove, and callers
    with no such following call still pass an empty set; that as of Phase 27 its only consumer
    inside the implementation is the correction loop's `ComputeUnexpectedlyDeactivated` exclusion
    filter, which uses it to avoid flagging a deliberately-excluded monitor as an accidentally-lost
    survivor; and that because whole-topology Extend restores the persisted layout, the disable-set
    monitors are expected to still be active when `ActivateMonitors` returns — the following
    `DeactivateMonitors` call is what actually removes them, which is why the ordering contract is
    load-bearing. Keep the historical explanation of why this parameter exists, relocating it into
    a clearly-marked historical sentence rather than deleting it.

    In `src/RigToggle.App/MainForm.cs`, replace the comment fragment that attributes plan-building
    and `PathChangeException` fragility to the deleted helper with wording that attributes the same
    hazard to whole-topology Extend's own behavior, keeping the surrounding rationale for the
    single atomic swap-aware `ActivateMonitors(wentInactive, monitorSwapDisableSet: reactivated)`
    call intact.

    In `src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs`, replace the two deleted
    predicate names cited as precedent for pure-seam testing discipline with two surviving
    examples: `ComputeUnexpectedlyActivated` and `ComputeUnexpectedlyDeactivated`.
  </action>
  <verify>
    <automated>cd /home/bpivk/moza &amp;&amp; dotnet build RigToggle.sln -c Debug --nologo 2>&amp;1 | tail -5 &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo 2>&amp;1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Historical record (pre-Phase-27) -- retained verbatim:' src/RigToggle.Windows/WindowsMonitorController.cs` returns exactly `1`.
    - Region-scoped check on the leading summary: `awk '/Historical record \(pre-Phase-27\)/{exit} /^\/\/\//' src/RigToggle.Windows/WindowsMonitorController.cs | grep -cE 'TryBuildScopedActivationPlan|SelectSourceForActivation|ShouldRetryScopedActivation|ShouldRetryNestedCorrectionActivation|PollUntilTargetsReachable|PromoteToOriginIfNeeded'` returns `0`.
    - That same leading region contains the strings `ApplyTopology`, `MaxCorrectionRounds`, `ComputeUnexpectedlyActivated`, `ComputeUnexpectedlyDeactivated`, `ArmIntentGuard`, and `CollateralMonitorRestoreFailedException` at least once each.
    - The historical region below the marker is byte-identical to the corresponding text in the pre-Task-1 file: verify with `git diff` that no line of the old narrative was reworded, only relocated, and state that explicitly in `27-03-SUMMARY.md`.
    - `grep -cE 'TryBuildScopedActivationPlan|SelectSourceForActivation|ShouldRetryScopedActivation|ShouldRetryNestedCorrectionActivation|PollUntilTargetsReachable|PromoteToOriginIfNeeded' src/RigToggle.App/MainForm.cs src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs src/RigToggle.Core/Abstractions/IMonitorController.cs` returns `0` for all three files.
    - `src/RigToggle.Core/Abstractions/IMonitorController.cs` still declares `void ActivateMonitors(IReadOnlySet<string> monitorDevicePaths, IReadOnlySet<string> monitorSwapDisableSet);` unchanged — the signature is documentation-only work, asserted by `git diff` showing no change to that line.
    - `dotnet build RigToggle.sln -c Debug --nologo` reports `0 Error(s)`; `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo` reports `Failed: 0, Passed: 249`.
  </acceptance_criteria>
  <done>The class summary leads with the Extend-only architecture and carries the prior narrative verbatim below an explicit historical marker; the interface doc describes the surviving consumer of `monitorSwapDisableSet`; no source comment names a deleted member; build and tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct and extend the debug knowledge base, and close out the debug session file</name>
  <files>.planning/debug/knowledge-base.md, .planning/debug/monitor-position-regre.md</files>
  <read_first>
    - `.planning/debug/knowledge-base.md` in full — in particular the `monitor-enable-reactivates-others-again` entry, whose "Fix" and "Recurrence guard" fields recommend the scoped-`ApplyPathInfos` approach this phase removed, and the `monitor-enable-affects-other` entry, which already carries a "Superseded ... by" note showing the established format for this correction.
    - `.planning/debug/monitor-position-regre.md` — the frontmatter block at lines 1-6 and the "SESSION CLOSED" note at lines 8-39, plus §22.5 (Closure).
    - `.planning/phases/27-monitor-activation-logic-redesign/27-01-SUMMARY.md` — the rig-check outcomes that determine whether the session file may be marked resolved.
  </read_first>
  <action>
    Append a new entry to `.planning/debug/knowledge-base.md` titled
    `monitor-activation-extend-only-redesign`, following the exact field structure the existing
    entries use: Date, Error patterns, Root cause(s), Fix, Files changed, Why not caught,
    Recurrence guard. Populate it so a future Phase-0 recall surfaces the right facts: error
    patterns should include terms a future investigator would search on, covering monitor
    activation flakiness, whole-topology Extend, scoped activation removal, correction loop,
    accumulated-mechanism overbuild, and the Odyssey G5 / SAM7489 pairing. Root cause must state
    honestly that this was NOT a bug fix — no root cause was found or eliminated; the change is an
    architectural simplification adopted after 20+ incremental rounds failed to converge, and the
    underlying external nondeterminism affecting that one display remains unexplained. Fix must
    describe the Extend-only single-pass shape and name the mechanisms removed versus retained.
    Why not caught must record the two structural gaps this phase re-confirmed: the mutating CCD
    methods have no injectable seam and are unit-testable only through their pure helpers, and
    `RigToggle.Windows.Tests` cannot execute in the Linux build sandbox at all. Recurrence guard
    must name the surviving pure-seam tests and this entry itself, and must state that the
    accepted cosmetic flash is expected behavior so it is not re-filed as a new bug.

    Then amend the existing `monitor-enable-reactivates-others-again` entry. Do not edit or delete
    its historical content. Append a superseding note in the same style the
    `monitor-enable-affects-other` entry already uses, stating that its Recurrence guard's advice
    to prefer a scoped `ApplyPathInfos` plan for activating one specific target was reversed by
    Phase 27, that the scoped path was removed as the primary mechanism after it never became
    reliable for one specific display across 20+ rounds, that the visible flash that advice was
    designed to prevent is now an accepted tradeoff, and that a reader should consult the new
    `monitor-activation-extend-only-redesign` entry for the current architecture. Both of that
    entry's root causes remain true and are not being retracted — say so explicitly, since the
    diagnostic-pipeline root cause (live-reconfigurable debug logging) is untouched by this phase.

    Finally, dispose of `.planning/debug/monitor-position-regre.md` according to Plan 01's rig
    outcome. If Plan 01's rig checks A, C, and D all passed: set its frontmatter `status` to
    `resolved`, set `updated` to today's date, append a short closure paragraph under the existing
    "SESSION CLOSED" note recording that Phase 27 implemented the round-22 proposal and naming the
    rig-verification date and outcome, and move the file to `.planning/debug/resolved/` with
    `git mv` — which also reconciles the path with the one `ROADMAP.md` and `STATE.md` already
    cite. If any rig check failed or was not performed: leave the file where it is, leave its
    status as `superseded-by-redesign`, and append a paragraph recording exactly which checks were
    not satisfied and that closure is deferred. Do not fabricate a resolved status.
  </action>
  <verify>
    <automated>cd /home/bpivk/moza &amp;&amp; grep -c 'monitor-activation-extend-only-redesign' .planning/debug/knowledge-base.md &amp;&amp; grep -c 'Superseded' .planning/debug/knowledge-base.md &amp;&amp; ls .planning/debug/ .planning/debug/resolved/</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/debug/knowledge-base.md` contains a heading line beginning `## monitor-activation-extend-only-redesign`, and that entry contains all seven standard field labels used by the existing entries: `**Date:**`, `**Error patterns:**`, `**Root cause`, `**Fix:**`, `**Files changed:**`, `**Why not caught:**`, `**Recurrence guard:**`.
    - That new entry's Root cause field contains an explicit statement that no root cause was found or eliminated by this phase — verified by reading it, not by grep alone, and quoted in `27-03-SUMMARY.md`.
    - The `monitor-enable-reactivates-others-again` entry gained a superseding note that names Phase 27 and states its scoped-`ApplyPathInfos` recurrence-guard advice is reversed; `git diff .planning/debug/knowledge-base.md` shows only additions to that entry, no deletions or rewordings of its existing lines.
    - Session-file disposition matches Plan 01's recorded rig outcome: if checks A, C, D all passed, `.planning/debug/resolved/monitor-position-regre.md` exists, `.planning/debug/monitor-position-regre.md` does not, and the moved file's frontmatter reads `status: resolved`; otherwise the file is still at `.planning/debug/monitor-position-regre.md` with `status: superseded-by-redesign` and an appended paragraph naming the unsatisfied checks.
    - `27-03-SUMMARY.md` states which of those two branches was taken and why, citing the rig outcome from `27-01-SUMMARY.md`.
  </acceptance_criteria>
  <done>The knowledge base carries a new Phase 27 entry and a superseding note on the reversed guidance; the debug session file's location and status honestly reflect the rig outcome.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final rig confirmation that the simplified build still works (REDESIGN-04)</name>
  <what-built>
    Plan 02 deleted 9 methods, 2 static members, 23 tests, and 4 using directives; Plan 03 rewrote
    the architecture documentation. None of that should change runtime behavior relative to the
    build you already verified in Plan 01 Task 3 — this checkpoint exists to confirm that
    expectation on hardware, because the only test project that covers this code cannot execute in
    the build sandbox. This is deliberately a short smoke check, not a repeat of Plan 01's full
    matrix.
  </what-built>
  <how-to-verify>
    Rebuild from the current commit on the Windows rig:
    `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64`
    Then run three checks and report each:

    Check 1 — the app launches, the monitor tile dashboard renders, and every monitor's tile shows
    the correct enabled/disabled state.

    Check 2 — click one disabled monitor's tile to enable it, then click it again to disable it.
    Both directions complete with no error dialog and the tiles end up correct.

    Check 3 — toggle to Rig mode and back to Normal using the toggle switch. Both directions end
    with the correct active monitor set and no error dialog.

    If anything differs from what you observed in Plan 01 Task 3, say so — that would mean a
    deletion removed something still load-bearing, and is the signal to stop.
  </how-to-verify>
  <read_first>
    - `.planning/phases/27-monitor-activation-logic-redesign/27-01-SUMMARY.md` — the Plan 01 rig results this check is compared against
    - `README.md` — the publish command
  </read_first>
  <acceptance_criteria>
    - Checks 1, 2, and 3 each have a recorded PASS or FAIL in `27-03-SUMMARY.md`.
    - Any behavioral difference from Plan 01 Task 3's observed results is recorded explicitly, including "no difference observed" when that is the answer.
    - If any check fails, execution HALTS and the failing behavior is written up against the specific deletion suspected, rather than the phase being closed.
    - If the operator signs off without running a check, `27-03-SUMMARY.md` records which checks were not performed, verbatim, as open follow-up items — never as fabricated passes (matching the disclosure pattern used for Phases 25 and 26 in STATE.md).
  </acceptance_criteria>
  <resume-signal>Type "approved" plus your PASS/FAIL for Checks 1-3, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>

Artifacts this PLAN creates:

| Kind | Artifact | Change |
|---|---|---|
| doc marker | `Historical record (pre-Phase-27) -- retained verbatim:` in `WindowsMonitorController.cs` | added — separates current architecture from preserved narrative |
| doc text | leading class `<summary>` of `WindowsMonitorController` | rewritten to describe the Extend-only architecture |
| doc text | `IMonitorController.ActivateMonitors` `<summary>` and `<param name="monitorSwapDisableSet">` | rewritten to describe the surviving consumer and the load-bearing ordering contract |
| KB entry | `## monitor-activation-extend-only-redesign` in `.planning/debug/knowledge-base.md` | added |
| KB note | superseding note on `## monitor-enable-reactivates-others-again` | added |
| file move | `.planning/debug/monitor-position-regre.md` -> `.planning/debug/resolved/` | conditional on Plan 01's rig outcome |

Symbols/text this PLAN REMOVES:

Stale references to `TryBuildScopedActivationPlan` in `src/RigToggle.App/MainForm.cs`, and to
`ShouldRetryScopedActivation` / `ShouldRetryNestedCorrectionActivation` in
`src/RigToggle.Tests/MonitorEnableFailureMessageBuilderTests.cs` — the last two places in `src/`
that named deleted members. The reversed scoped-activation recommendation in the knowledge base is
superseded by an appended note rather than deleted, per that file's established convention.

Symbols this PLAN must NOT change:

`IMonitorController.ActivateMonitors`'s and `DeactivateMonitors`'s declarations (documentation-only
plan), every member on debug §22.2's KEPT list, and every line of the pre-Phase-27 narrative that
is relocated rather than reworded.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/debug/knowledge-base.md` -> future debug sessions | this file is consumed as authoritative prior-art at the start of new investigations |
| doc comments -> future implementers | the class summary is the primary written record of why this subsystem is shaped as it is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-10 | Tampering | knowledge base seeding a future session with reversed guidance | high | mitigate | The `monitor-enable-reactivates-others-again` entry's scoped-activation recurrence guard is explicitly superseded by an appended note naming Phase 27, and a new entry documents the current architecture. Task 2's acceptance criteria assert both by grep and by quoted reading. |
| T-27-11 | Repudiation | doc rewrite silently discarding rig-earned knowledge | high | mitigate | The historical narrative is relocated verbatim beneath an explicit marker, and the acceptance criteria require a `git diff` confirmation that no historical line was reworded — only moved. The `ccd-topology-restore-findings` precedent in the knowledge base is the project's established pattern for preserving rig-earned findings past a code deletion. |
| T-27-12 | Repudiation | marking the debug session resolved without evidence | high | mitigate | Task 2's action branches explicitly on Plan 01's recorded rig outcome and forbids fabricating a resolved status; Task 3's criteria require unperformed checks to be disclosed verbatim rather than recorded as passes. |
| T-27-13 | Information Disclosure | doc comments and knowledge base recording device paths and monitor model identifiers | low | accept | These are hardware identifiers on a single-user personal rig, already present throughout the repository's planning artifacts and committed history; they are not credentials. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs occur in this plan; no csproj or package reference is modified. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `dotnet build RigToggle.sln -c Debug --nologo` -> `0 Error(s)`.
- `dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -c Debug --nologo` -> `0 Warning(s)`, `0 Error(s)`.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo` -> `Passed: 249`.
- The historical marker exists exactly once and the region above it names no deleted member.
- No file under `src/` matches any deleted member name.
- The knowledge base contains the new entry and the superseding note.
- The debug session file's location and status match Plan 01's rig outcome.
- Task 3's Checks 1-3 answered by the operator.
</verification>

<success_criteria>
- A reader opening `WindowsMonitorController.cs` sees the current architecture first and the 20-round history clearly marked as history, with none of it lost.
- `IMonitorController`'s contract doc explains `monitorSwapDisableSet` in terms of code that exists.
- No comment in `src/` names a member this phase deleted.
- `.planning/debug/knowledge-base.md` will not re-recommend the removed architecture to a future debug session.
- The debug session file's status is evidence-backed, not assumed.
- The final published build is confirmed working on the rig, or the failure is documented against the specific deletion suspected.
</success_criteria>

<output>
Create `.planning/phases/27-monitor-activation-logic-redesign/27-03-SUMMARY.md` when done.
</output>