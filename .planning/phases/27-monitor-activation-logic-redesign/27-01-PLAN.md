---
phase: 27-monitor-activation-logic-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Windows/WindowsMonitorController.cs
autonomous: false
requirements: [REDESIGN-01, REDESIGN-04]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ActivateMonitorsCore performs exactly one activation call, and it is PathInfo.ApplyTopology(DisplayConfigTopologyId.Extend, allowPersistence: false) — no scoped-plan branch, no primary/fallback split, no outer retry loop (REDESIGN-01, debug §22.1)."
    - "Enabling one disabled monitor from the tile dashboard on the real rig leaves exactly that monitor active and the other disabled monitor off in the settled final state (REDESIGN-04)."
    - "A full Rig->Normal->Rig toggle on the real rig ends with the correct active monitor set in both directions, with ToggleService's ActivateMonitors-before-DeactivateMonitors ordering unchanged (debug §22.4 open question)."
    - "An activation that does not take effect still throws rather than reporting EXIT success — CollateralMonitorRestoreFailedException from the nested correction call, InvalidOperationException from the top-level call (D-05 discipline preserved)."
    - "RigToggle.Tests still passes 249/249 and the whole solution still builds with 0 warnings and 0 errors."
  artifacts:
    - "src/RigToggle.Windows/WindowsMonitorController.cs — ActivateMonitorsCore rewritten to the single-pass shape in debug §22.1"
  key_links:
    - "ToggleService.ToggleToRigMode/ToggleToNormalMode -> IMonitorController.ActivateMonitors(enableSet, monitorSwapDisableSet: disableSet) -> DeactivateMonitors(disableSet) — Pitfall 2 ordering, unchanged by this plan"
    - "ActivateMonitorsCore correction loop -> nested ActivateMonitorsCore(..., isNestedCorrectionCall: true) -> CollateralMonitorRestoreFailedException -> MonitorEnableFailureMessageBuilder.Build -> MainForm.OnTileAction MessageBox"
    - "MainForm.ArmIntentGuard / TryReactivelyCorrectAgainstLastIntent — the separate out-of-call OS-event watchdog, deliberately NOT merged with the in-call correction loop (debug §21.5)"
---

<objective>
Land the redesign's tracer slice: make whole-topology `ApplyTopology(Extend)` the sole activation
mechanism inside `ActivateMonitorsCore`, collapsing it to the single-pass shape specified in
`.planning/debug/monitor-position-regre.md` §22.1 — and prove it end-to-end on the real rig
BEFORE any of the now-orphaned scoped-activation helpers are deleted (Plan 02).

This is deliberately a behavior-change-only slice. Every helper method that becomes unreachable
stays physically in the file after this plan, so the commit that changes activation behavior is a
small, reviewable, single-`git revert`-able diff, and the rig gate in Task 3 answers the questions
Plan 02's decisions depend on.

Purpose: prove the Extend-only architecture works on real CCD hardware before committing to the
~700-line subtraction. If Extend-only fails on the rig, this is the commit to revert — not a
23-test, 9-method deletion.

Output: a rewritten `ActivateMonitorsCore` plus a recorded rig-verification result (including an
explicit answer on monitor-position preservation, which Plan 02 Task 1 consumes as evidence).

**Phase-local requirement IDs** (this phase was scoped ad-hoc post-v2.2; there is no
REQUIREMENTS.md entry — the authoritative scope source is the debug file's round 21/22 sections):
- **REDESIGN-01** — `ApplyTopology(Extend)` is the sole activation mechanism in `ActivateMonitorsCore`; no scoped-plan branch, no primary/fallback split, no outer retry loop (debug §22.1).
- **REDESIGN-02** — every mechanism dispositioned REMOVED in debug §22.2 is deleted from source along with its now-dead tests, and every mechanism dispositioned KEPT survives intact (Plan 02).
- **REDESIGN-03** — the documentation of record (class summary, `IMonitorController` doc comment, `.planning/debug/knowledge-base.md`) describes the post-redesign architecture and carries no stale references to deleted symbols (Plan 03).
- **REDESIGN-04** — the redesigned activation path is verified on real Windows rig hardware, since no check available in this sandbox can exercise live CCD APIs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md

Primary source of scope for this entire phase (read §21.1-21.6 and §22.1-22.5 before touching code):
@.planning/debug/monitor-position-regre.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the removal strategy for the scoped-activation code path (one-way door)</name>
  <decision>
    How should the scoped-`ApplyPathInfos` activation path be retired — deleted outright, kept
    behind a runtime flag, or deleted in two commits with a rig gate in between?
  </decision>
  <context>
    Once the scoped-activation branch stops being called and its helpers are deleted (Plan 02),
    restoring it means reverting the phase — the accumulated design knowledge lives only in git
    history and in `.planning/debug/monitor-position-regre.md`. That makes this a one-way door on
    the app's single most safety-critical code path (per CLAUDE.md, the monitor toggle IS this
    app's core value). The debug file's own §22.3 is explicit that this trades one basket of
    proven-real anomalies for a different basket of proven-real anomalies — `root_cause (2)`
    (Extend both failing to activate the requested target AND reactivating an unrelated,
    independently-disabled one) was confirmed on this exact rig hardware, and the cosmetic flash
    that scoped activation was invented to eliminate comes back as the common case.
  </context>
  <options>
    <option id="option-a">
      <name>Outright removal in a single commit (literal reading of debug §22.1/§22.2)</name>
      <pros>Achieves the full simplification the phase exists for; no dual-path configuration surface; smallest end state.</pros>
      <cons>The behavior change and the ~700-line subtraction land together, so a rig failure means reverting a diff that also deletes 23 tests; no intermediate commit exists where Extend-only is live and the old code is still recoverable in the working tree.</cons>
    </option>
    <option id="option-b">
      <name>Retain both paths behind an AppSettings toggle (e.g. UseScopedActivation, default false)</name>
      <pros>Instant rollback on the rig with no rebuild; both mechanisms stay available for A/B comparison against root_cause (8).</pros>
      <cons>Directly defeats the phase's stated goal — it keeps all 15 catalogued mechanisms alive AND adds a 16th (the flag, its persistence, and its Settings UI). The debug file's entire thesis (§21.2, §21.6) is that coexisting, independently-triggered mechanisms are the problem, not the individual mechanisms.</cons>
    </option>
    <option id="option-c">
      <name>Outright removal, staged across two commits with a rig gate between them (what this plan set implements)</name>
      <pros>Ends in exactly option-a's state, but git history contains one commit where Extend-only activation is live and every scoped helper is still present — a one-commit revert point. The rig gate (Task 3) also produces the position-preservation evidence Plan 02 Task 1 needs to decide the live-mode cache disposition honestly instead of by assumption.</pros>
      <cons>One extra commit boundary; the intermediate commit contains unreachable code, which is a transient state a reviewer must understand is deliberate.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/debug/monitor-position-regre.md` — §21.5 ("Is a genuinely simpler design available?"), §22.1 (target architecture), §22.3 (honest risk/migration note: root_cause (2)'s two confirmed Extend failure modes and the cosmetic-flash regression)
    - `CLAUDE.md` — "Core Value" and "Constraints" sections (why monitor toggling is safety-critical here)
    - `src/RigToggle.Windows/WindowsMonitorController.cs` lines 483-928 (the current `ActivateMonitors` / `ActivateMonitorsCore` pair this decision governs)
  </read_first>
  <acceptance_criteria>
    - The operator has selected exactly one of option-a / option-b / option-c and the choice is recorded verbatim in `27-01-SUMMARY.md`.
    - If option-b is selected, Task 2's action is superseded and execution HALTS for replanning — this plan set does not implement a dual-path design.
    - If option-a is selected, Plan 02's Task 2 and Task 3 are merged into this plan's Task 2 commit and Task 3's rig gate moves to after that merged commit; record that deviation in the summary.
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Collapse ActivateMonitorsCore to the single-pass Extend-only shape (tracer slice)</name>
  <files>src/RigToggle.Windows/WindowsMonitorController.cs</files>
  <reversibility rating="one-way">Removes the scoped-activation call path from the app's core safety-critical monitor toggle; reverting after Plan 02 means reverting the phase. Gated by Task 1's decision checkpoint.</reversibility>
  <read_first>
    - `src/RigToggle.Windows/WindowsMonitorController.cs` lines 483-928 in full — the current `ActivateMonitors` public wrapper and the whole `ActivateMonitorsCore` body being rewritten. Read before editing; do not work from this plan's description alone.
    - `src/RigToggle.Windows/WindowsMonitorController.cs` lines 1510-1720 — `QueryActiveDevicePaths`, `SettlePollDelay`, `MaxSettlePollAttempts`, `MaxCorrectionRounds`, `PollUntilStableActiveDevicePaths`, `ObservePostApplyStability`, `Log` (all KEPT; you must not alter them).
    - `.planning/debug/monitor-position-regre.md` §22.1 (the exact target shape, including which lines of the current body are "unchanged") and §22.3 (why the correction loop alone is considered sufficient coverage).
    - `src/RigToggle.Core/ToggleService.cs` lines 90-120 and 440-465 — the two call sites that pass a non-empty `monitorSwapDisableSet`, to confirm the ordering contract this task must not disturb.
  </read_first>
  <action>
    Rewrite the body of `ActivateMonitorsCore(IReadOnlySet&lt;string&gt; monitorDevicePaths, IReadOnlySet&lt;string&gt; monitorSwapDisableSet, bool isNestedCorrectionCall)` in place. Do NOT change its signature, do NOT change the public `ActivateMonitors` wrapper (it keeps delegating with `isNestedCorrectionCall: false`), and do NOT delete any helper method bodies in this task — orphaned helpers are removed in Plan 02.

    KEEP byte-for-byte, in this order, at the top of the method: the `isPartOfMonitorSwap` local derived from `monitorSwapDisableSet.Count > 0`; the unconditional ENTER `Log(...)` line; the `monitorDevicePaths.Count == 0` no-op return with its EXIT log; the `QueryActiveDevicePaths()` call into `currentlyActiveDevicePaths`; the Pitfall-3 skip (`monitorDevicePaths.All(currentlyActiveDevicePaths.Contains)`) with its EXIT log; the `PathInfo.GetAllPaths(virtualModeAware: false)` availability guard, its `missing` computation, and its `InvalidOperationException("Cannot enable monitor(s) — not detected: ...")` throw; and the `requested=/preExtendActive=` Log line.

    DELETE the `const int MaxScopedActivationRetryAttempts = 2;` local declaration and the entire enclosing `for (int attemptNumber = 1; attemptNumber &lt;= MaxScopedActivationRetryAttempts + 1; attemptNumber++)` loop construct, de-indenting its former body by one level. The `postCorrectionActiveDevicePaths` and `stillInactive` locals that were hoisted above that loop solely so they would survive it are now declared at their point of use inside the flattened body.

    Replace the activation section with a single unconditional call. Specifically: keep the `PathInfo.GetActivePaths(virtualModeAware: false)` read, renaming its local from `activePathsForScopedPlan` to `activePathsBeforeMutation`, and keep the `CacheLiveModes(activePathsBeforeMutation)` call and its accompanying Log line (its disposition is decided separately in Plan 02 Task 1 — leave it alone here). Then DELETE: the `bool usedScopedActivation` local and every read/write of it; the informational `if (isPartOfMonitorSwap) { Log(...) }` block; the entire `if (TryBuildScopedActivationPlan(devicePathsToActivate, ...)) { ... } else { ... }` branch including its try/catch around the scoped apply, its two plan-detail Log lines, and its `scopedFailureReason` out-parameter usage; the `devicePathsToActivate` local (its only consumers were inside that branch); and the `if (!usedScopedActivation)` guard. In their place emit one Log line naming the whole-topology call and then the unconditional call `PathInfo.ApplyTopology(DisplayConfigTopologyId.Extend, allowPersistence: false);`.

    KEEP the settle-poll-then-correct loop entirely unchanged, including its `const int RequiredConsecutiveCleanRounds = 2;`, `int consecutiveCleanRounds`, `for (int round = 1; round &lt;= MaxCorrectionRounds; round++)`, the `PollUntilStableActiveDevicePaths()` call, both `ComputeUnexpectedlyActivated(currentlyActiveDevicePaths, settledActiveDevicePaths, monitorDevicePaths)` and `ComputeUnexpectedlyDeactivated(currentlyActiveDevicePaths, settledActiveDevicePaths, monitorSwapDisableSet)` calls, the correction-round Log line, the `DeactivateMonitors(unexpectedlyActivated)` call, and the nested `ActivateMonitorsCore(unexpectedlyDeactivated, monitorSwapDisableSet: new HashSet&lt;string&gt;(), isNestedCorrectionCall: true)` call with its following Log line. Preserve the clean-round counting and `break` semantics exactly.

    KEEP the verify-and-throw computation unchanged: the `PathInfo.GetActivePaths(virtualModeAware: false)` re-query into `postCorrection`, `postCorrectionActiveDevicePaths`, `requestedStillInactive`, `survivorStillInactive`, and the combined `stillInactive`.

    Replace the post-verify control flow. Where the old code had `if (stillInactive.Length == 0) { break; }`, emit instead the success terminal: the `EXIT success -- all requested paths active. finalActive=[...]` Log line, the `ObservePostApplyStability("ActivateMonitors", postCorrectionActiveDevicePaths)` call, and a `return`. Then DELETE the `retryEligibleTopLevel` local and its `ShouldRetryScopedActivation(...)` call, the `retryEligibleNestedOnly` local and its `ShouldRetryNestedCorrectionActivation(...)` call, the whole `if (retryEligibleTopLevel || retryEligibleNestedOnly) { ... }` block including its retry Log line, its `Stopwatch` timing of the reachability poll, its `PollUntilTargetsReachable(...)` call and that call's Log line, and its `continue`.

    KEEP the terminal throw exactly as it is today, minus its retry bookkeeping: build `exitThrowMessage` with the same two-sentence text ending in "No further automatic recovery is attempted (D-05)."; when `isNestedCorrectionCall` is true, Log the EXIT-throwing line and `throw new CollateralMonitorRestoreFailedException(exitThrowMessage, stillInactive);`; otherwise Log the EXIT-throwing line and `throw new InvalidOperationException(exitThrowMessage);`. Remove the `attemptNumber > 1` conditional suffix from both of those Log lines, since no attempt counter exists any more. Delete the now-unreachable trailing success block that followed the old loop.

    Add one new remark block immediately above `ActivateMonitorsCore` stating that Phase 27 replaced the scoped-primary/Extend-fallback split with unconditional whole-topology Extend, that the in-call settle-poll-then-correct loop plus MainForm's separate out-of-call watchdog are the sole safety nets, that the previously-eliminated cosmetic flash is an accepted, expected tradeoff, and citing `.planning/debug/monitor-position-regre.md` §22.1/§22.3 as the source. Describe the removed design in prose only — do not name the deleted helper methods in this or any other retained comment, because Plan 02 and Plan 03 assert on their absence.
  </action>
  <verify>
    <automated>cd /home/bpivk/moza &amp;&amp; dotnet build RigToggle.sln -c Debug --nologo 2>&amp;1 | tail -5 &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo 2>&amp;1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build RigToggle.sln -c Debug --nologo` reports `0 Error(s)` and no more than the 6 pre-existing xUnit1031 warnings in `RigToggle.Tests` (baseline recorded 2026-08-29 in STATE.md); `dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -c Debug --nologo` reports `0 Warning(s)` and `0 Error(s)`.
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo` reports `Failed: 0, Passed: 249`.
    - `grep -c 'ApplyTopology(' src/RigToggle.Windows/WindowsMonitorController.cs` returns exactly `1`, and that single occurrence is inside `ActivateMonitorsCore`.
    - `grep -vE '^\s*(//|///|\*|/\*)' src/RigToggle.Windows/WindowsMonitorController.cs | grep -c 'usedScopedActivation'` returns `0`.
    - `grep -vE '^\s*(//|///|\*|/\*)' src/RigToggle.Windows/WindowsMonitorController.cs | grep -c 'MaxScopedActivationRetryAttempts'` returns `0`.
    - Each of `TryBuildScopedActivationPlan(`, `ShouldRetryScopedActivation(`, `ShouldRetryNestedCorrectionActivation(`, `PollUntilTargetsReachable(` appears exactly once in non-comment lines of `src/RigToggle.Windows/WindowsMonitorController.cs` — its own declaration only, with zero call sites remaining.
    - `grep -c 'isNestedCorrectionCall' src/RigToggle.Windows/WindowsMonitorController.cs` shows the parameter still present on `ActivateMonitorsCore`, still passed `true` by exactly one nested call site, and still consulted by exactly one terminal branch that throws `CollateralMonitorRestoreFailedException`.
    - `grep -c 'CacheLiveModes(' src/RigToggle.Windows/WindowsMonitorController.cs` returns `3` (one declaration, one call in `ActivateMonitorsCore`, one call in `DeactivateMonitors`) — unchanged from before this task.
    - Hand-trace recorded in `27-01-SUMMARY.md`: for a `ToggleService.ToggleToRigMode` call with a non-empty `disableSet`, state which monitors are active at each of these points — after `ApplyTopology(Extend)` returns, after correction round 1, at `ActivateMonitors` return, and after the immediately-following `DeactivateMonitors(disableSet)` returns — and state explicitly whether `ComputeUnexpectedlyDeactivated`'s `monitorSwapDisableSet` exclusion still prevents the swap's disable-set from being flagged as lost survivors.
  </acceptance_criteria>
  <done>`ActivateMonitorsCore` contains exactly one activation call (`ApplyTopology(Extend)`), no retry loop, and no scoped-plan branch; the correction loop, verify-and-throw, and both exception types are unchanged; solution builds clean and `RigToggle.Tests` passes 249/249.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rig-hardware verification of the Extend-only activation path (REDESIGN-04)</name>
  <what-built>
    `ActivateMonitorsCore` now performs one unconditional whole-topology `ApplyTopology(Extend)`
    call and relies solely on the in-call settle-poll-then-correct loop (plus MainForm's separate
    OS-event watchdog) to reach the requested state. The scoped-`ApplyPathInfos` activation path,
    its automatic retry loop, its retry-eligibility gates, and its poll-until-reachable wait are
    all no longer executed. No unit test in this repository can exercise this: the CCD calls are
    static natives with no injectable seam (documented at the top of
    `WindowsMonitorControllerTests.cs`), and `RigToggle.Windows.Tests` cannot even execute in this
    build sandbox (`Microsoft.WindowsDesktop.App` is not installable on Linux — confirmed this
    phase). Real hardware is the only oracle.
  </what-built>
  <how-to-verify>
    Build natively on the Windows rig from this phase's branch/commit:
    `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64`
    (the command documented in `README.md` and used by `.github/workflows/release.yml`). Launch the
    published exe, open Settings, tick "Enable debug logging" (it applies live, no restart needed),
    then run these checks and report each one's result:

    Check A — single-tile enable, no collateral activation. Start with at least two monitors
    OS-disabled. Click ONE disabled monitor's tile to enable it. Expected settled result: that
    monitor active, the other disabled monitor still off, tiles correct. Report PASS/FAIL. Also
    report separately whether you SAW the other monitor briefly flash on then off — a flash is an
    EXPECTED, accepted consequence of this redesign (debug §22.3), not a failure of Check A;
    estimate how long it lasted.

    Check B — position preservation (this is Symptom 1 of the whole debug session, and Plan 02's
    cache decision depends on your answer). Before disabling anything, open Windows Display
    Settings and note the exact arrangement/position of the monitor you are about to test. Disable
    it via its tile, then re-enable it via its tile. Report: did it come back at the SAME position
    it had before, or did its position change/reset? Answer this one explicitly even if everything
    else passes.

    Check C — full swap ordering (debug §22.4's flagged open question). Use the Rig/Normal toggle
    switch, not a tile. Toggle to Rig mode: confirm the configured enable-set monitors are on and
    the configured disable-set monitors are off. Toggle back to Normal: confirm the symmetric
    result. Report PASS/FAIL for each direction, and report whether any error dialog appeared.

    Check D — the flaky monitor specifically. Repeat Check A three times targeting the Samsung
    Odyssey G5 (device path SAM7489). Report how many of the three settled correctly, and for any
    that did not, what the app showed.

    Check E — failure surfacing. If any check produced a dialog, copy its exact text.

    Finally, attach or paste the `debug.log` covering the session (the startup banner line onward).
  </how-to-verify>
  <read_first>
    - `README.md` — the publish command and the run instructions
    - `.planning/debug/monitor-position-regre.md` §22.3 — so the expected cosmetic flash and the two known Extend failure modes are recognized as anticipated, not as new bugs
  </read_first>
  <acceptance_criteria>
    - Checks A, B, C, D, E each have a recorded PASS / FAIL / explicit answer in `27-01-SUMMARY.md`; no check is marked passed without an operator statement covering it.
    - Check B's answer is recorded as a plain yes/no on position preservation, quoted verbatim from the operator, because Plan 02 Task 1 selects between mutually exclusive options based on it.
    - If Check A, C, or D fails, execution HALTS here and does not proceed to Plan 02 — the Task 2 commit is the designated revert point per Task 1 option-c.
    - If the operator signs off without performing a check (the pattern recorded for Phases 25 and 26 in STATE.md), the summary records which checks were NOT actually performed, verbatim, as open follow-up items — never as fabricated passes.
  </acceptance_criteria>
  <resume-signal>Type "approved" plus your answers to Checks A-E, or describe the failures.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>

Symbols and files this PLAN creates or modifies:

| Kind | Symbol / file | Change |
|---|---|---|
| method body | `WindowsMonitorController.ActivateMonitorsCore` | rewritten to the single-pass shape (debug §22.1) |
| local | `activePathsBeforeMutation` (was `activePathsForScopedPlan`) | renamed |
| comment block | new remark above `ActivateMonitorsCore` | added, cites debug §22.1/§22.3 |

Symbols this PLAN makes unreachable but does NOT yet delete (Plan 02 deletes them):

`TryBuildScopedActivationPlan`, `PromoteToOriginIfNeeded`, `SelectSourceForActivation`,
`ResolveIsPathActiveBackingField`, `_isPathActiveBackingFieldCache`, `DescribeSource`,
`DescribeScopedPathEntry`, `ShouldRetryScopedActivation`, `ShouldRetryNestedCorrectionActivation`,
`PollUntilTargetsReachable`, `MaxReachabilityPollAttempts`.

Symbols this PLAN deletes outright:

`MaxScopedActivationRetryAttempts` (method-local const), the `attemptNumber` retry `for` loop,
the `usedScopedActivation` local, the `devicePathsToActivate` local, `scopedFailureReason`'s
usage, the `retryEligibleTopLevel` / `retryEligibleNestedOnly` locals, the informational
`isPartOfMonitorSwap` Log branch, and the reachability-poll `Stopwatch` block.

Symbols this PLAN must leave intact (debug §22.2 KEPT list):

`_lastKnownActiveModeByDevicePath`, `CacheLiveModes`, `PollUntilStableActiveDevicePaths`,
`MaxCorrectionRounds`, `RequiredConsecutiveCleanRounds`, `ComputeUnexpectedlyActivated`,
`ComputeUnexpectedlyDeactivated`, `ComputeUndetectedDevicePaths`, `DeactivateMonitors`,
`AnyRectanglesOverlap`, `MergeAllMonitors`, `ObservePostApplyStability`, `QueryActiveDevicePaths`,
`isNestedCorrectionCall` (now single-purpose), `CollateralMonitorRestoreFailedException`,
`MonitorEnableFailureMessageBuilder`, `ToggleService.LiveFilterMonitorSets`,
`ToggleOrchestrator.BeginExclusiveMonitorAccess`, `MainForm.ArmIntentGuard`,
`MainForm.TryReactivelyCorrectAgainstLastIntent`.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Windows CCD API | `SetDisplayConfig` mutates system-wide display topology for every process on the machine |
| app -> `%APPDATA%\RigToggle\settings.json` | user-editable JSON supplies the device-path sets this code acts on |
| app -> `debug.log` | diagnostic sink that records device paths and topology state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Denial of Service | `ActivateMonitorsCore` -> `ApplyTopology(Extend)` | high | mitigate | Removing the outer retry loop reduces, not increases, the number of CCD mutations per call. The Pitfall-3 early skip (every requested path already active -> return) is preserved verbatim, so a repeated toggle cannot spin Extend calls. `MaxCorrectionRounds` still bounds the correction loop and the nested correction call still only targets paths found genuinely inactive — recursion remains bounded exactly as before. |
| T-27-02 | Denial of Service | `DeactivateMonitors` zero-survivor guard | high | mitigate | Unchanged by this plan: the "at least one active display must remain" throw is in `DeactivateMonitors`, which this plan does not touch. Task 2's acceptance criteria assert `DeactivateMonitors` is unmodified. |
| T-27-03 | Tampering | `settings.json` monitor sets | medium | accept | Single-user local rig, file lives in the user's own roaming profile at the user's own integrity level; an attacker able to write it already has the user's token. `ToggleService.LiveFilterMonitorSets` (KEPT) already drops device paths not currently detected, so a stale or hand-edited path degrades to a skip, not a crash. |
| T-27-04 | Information Disclosure | `debug.log` device-path/topology logging | low | accept | Device paths and monitor model IDs are not secrets; logging is opt-in via a Settings checkbox and off by default. Unchanged by this plan. |
| T-27-05 | Repudiation | activation reported successful while state is wrong | high | mitigate | D-05 discipline is preserved exactly: the verify-and-throw re-queries `PathInfo.GetActivePaths()` and throws on any requested path or non-excluded survivor still inactive. Task 2's acceptance criteria assert both throw branches and both exception types survive. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs occur in this phase — no new NuGet, npm, pip, or cargo dependency is added or upgraded. `RigToggle.Windows.csproj` is not modified. Package Legitimacy Gate is therefore not applicable. |
</threat_model>

<verification>
- Whole solution builds: `dotnet build RigToggle.sln -c Debug --nologo` -> `0 Error(s)`.
- `RigToggle.Windows.Tests` builds clean (it cannot execute here): `dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -c Debug --nologo` -> `0 Warning(s)`, `0 Error(s)`.
- `RigToggle.Tests` still green: `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Debug --nologo` -> `Passed: 249`.
- Source assertions in Task 2's acceptance criteria all hold.
- Task 3's rig checks A-E answered by the operator, recorded verbatim.
</verification>

<success_criteria>
- `ActivateMonitorsCore` has exactly one activation call and it is `ApplyTopology(Extend)`.
- No retry loop, no retry-eligibility gate, no reachability poll runs during activation.
- The correction loop, the verify-and-throw, and both terminal exception types behave as before.
- The rig operator has answered Checks A-E, and Check B's position-preservation answer is recorded verbatim for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/27-monitor-activation-logic-redesign/27-01-SUMMARY.md` when done.
</output>
