---
phase: 18-cleanup-pass-exe-size-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Core/Abstractions/ISnapshotStore.cs
  - src/RigToggle.Core/Models/StateSnapshot.cs
  - src/RigToggle.Core/Persistence/JsonSnapshotStore.cs
  - src/RigToggle.App/Program.cs
  - src/RigToggle.Core/Abstractions/IModeStore.cs
  - src/RigToggle.Core/Persistence/JsonModeStore.cs
  - src/RigToggle.Core/Persistence/JsonToggleInProgressStore.cs
  - src/RigToggle.Core/Abstractions/IAudioController.cs
  - src/RigToggle.Windows/WindowsAudioController.cs
  - src/RigToggle.Tests/Doubles/InMemoryStores.cs
  - src/RigToggle.Tests/JsonStoreTests.cs
autonomous: true
requirements: [CLEANUP-01]

must_haves:
  truths:
    - "No type named ISnapshotStore, JsonSnapshotStore, StateSnapshot or InMemorySnapshotStore exists anywhere in src/"
    - "A first launch with no mode.json but an existing legacy state.json still seeds mode as Rig; with neither file present it still seeds Normal"
    - "IAudioController no longer declares a Restore member and WindowsAudioController no longer implements one"
    - "The full solution builds with 0 errors and RigToggle.Tests passes with 80 tests"
  artifacts:
    - path: "src/RigToggle.App/Program.cs"
      provides: "Legacy-mode bootstrap seeding via a bare File.Exists check against state.json, with no snapshot-store abstraction"
      contains: "legacyStateJsonPath"
    - path: "src/RigToggle.Windows/WindowsAudioController.cs"
      provides: "Forward-only audio default-device control: GetPlaybackDevices, CaptureState, SetDefault, ApplyAndVerify, TryResolveDevice"
    - path: ".planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md"
      provides: "Recorded grep/build/test output proving the snapshot subsystem and audio Restore are gone"
  key_links:
    - from: "src/RigToggle.App/Program.cs"
      to: "mode.json seeding decision"
      via: "File.Exists(legacyStateJsonPath) ternary feeding modeStore.Save"
      pattern: "File\\.Exists\\(legacyStateJsonPath\\)"
    - from: "src/RigToggle.Windows/WindowsAudioController.cs"
      to: "IPolicyConfig COM apply+verify"
      via: "SetDefault -> SetDefaultForAllRoles -> ApplyAndVerify (Restore no longer a caller)"
      pattern: "ApplyAndVerify"
---

<objective>
Delete the dead snapshot-persistence subsystem (`ISnapshotStore`, `JsonSnapshotStore`, `StateSnapshot`, `InMemorySnapshotStore`, their tests) and the dead audio-side `Restore` path, replacing the one remaining live consumer in `Program.cs` with an equivalent bare file-existence check.

Purpose: Phase 16 rewired Normal mode from "restore a pre-toggle snapshot" to "apply an explicitly configured target set", which orphaned the entire snapshot round-trip. CLEANUP-01 removes it so no future contributor can accidentally re-wire a code path the architecture no longer supports.
Output: three deleted Core files, a snapshot-free `Program.cs` bootstrap, a `Restore`-free audio contract and implementation, and a test suite with the 5 snapshot persistence tests removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-RESEARCH.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-PATTERNS.md

<environment_note>
This dev host is Linux. `dotnet` lives at `$HOME/.dotnet/dotnet` — prefix commands with `PATH="$HOME/.dotnet:$PATH"`.

- `dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` works and is the full-solution gate.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` runs — this is the only executable test project here.
- `src/RigToggle.Windows.Tests` **builds but cannot execute** on this host (`Microsoft.WindowsDesktop.App` runtime is absent; `dotnet test` on it aborts with "No frameworks were found"). This is a pre-existing environment limitation carried since Phase 16 — build it, do not attempt to run it, and record it as "built, not executed (dev-host limitation)".
- 4 pre-existing `xUnit1031` warnings in `ToggleOrchestratorTests.cs` are the known baseline; the solution currently reports `0 Warning(s) 0 Error(s)` on a warm incremental build and those warnings only surface on a cold rebuild of the test project. Do not treat either as a regression.
</environment_note>

<measured_baselines>
Measured against the working tree at planning time — use these as the exact before/after references:

- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` → `Failed: 0, Passed: 85, Total: 85`
- `src/RigToggle.Tests/JsonStoreTests.cs` contains exactly 5 `[Fact]` methods whose names start with `SnapshotStore_` (declared at lines 261, 274, 286, 305, 322). Removing all 5 gives a post-plan total of **80**.
- `grep -rn "\.Restore(" src --include="*.cs"` currently returns exactly ONE line: a doc-comment mention at `src/RigToggle.Windows.Tests/WindowsMonitorControllerTests.cs:12`. There are zero production call sites.
- `WindowsAudioController.cs` is 237 lines; `Restore` is declared at line 126 and the next member (`ApplyAndVerify`) at line 183.
</measured_baselines>

<scope_boundary>
This plan does NOT touch: `src/RigToggle.Windows/WindowsMonitorController.cs`, `src/RigToggle.Core/Abstractions/IMonitorController.cs`, `src/RigToggle.Windows/AssemblyInfo.cs`, `src/RigToggle.Windows.Tests/**` (plan 18-02 owns those), or `src/RigToggle.Tests/Doubles/FakeControllers.cs`, `src/RigToggle.Tests/Doubles/BlockingMonitorController.cs`, `src/RigToggle.Tests/ToggleServiceTests.cs` (plan 18-05 owns those).

Removing `IAudioController.Restore` while `FakeAudioController` still declares a public `Restore` method is **intentional and compiles fine** — an implementer may carry extra public methods beyond its interface. Do not "fix" `FakeAudioController` here; 18-05 removes it.

Do NOT delete `AudioState`, `AudioRoleState`, `MonitorState`, or `MonitorPathSnapshot`. All four remain live via `CaptureState()` on both controllers. Only the composite `StateSnapshot` record goes.
</scope_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the snapshot-persistence trio and rewire the Program.cs bootstrap read</name>
  <files>src/RigToggle.Core/Abstractions/ISnapshotStore.cs, src/RigToggle.Core/Models/StateSnapshot.cs, src/RigToggle.Core/Persistence/JsonSnapshotStore.cs, src/RigToggle.App/Program.cs, src/RigToggle.Core/Abstractions/IModeStore.cs, src/RigToggle.Core/Persistence/JsonModeStore.cs, src/RigToggle.Core/Persistence/JsonToggleInProgressStore.cs</files>
  <read_first>
    - src/RigToggle.Core/Persistence/JsonSnapshotStore.cs (confirm for yourself that `Exists()` is literally `File.Exists(_path)` and nothing more — this is what justifies inlining it)
    - src/RigToggle.App/Program.cs (lines 85-115 hold the store construction block and the one-time mode-seeding block; read the whole file so you keep the surrounding best-effort-swallow startup idiom intact)
    - src/RigToggle.Core/Abstractions/IModeStore.cs (its doc comment at line 7 references ISnapshotStore by name)
    - src/RigToggle.Core/Persistence/JsonModeStore.cs (doc comment lines 11 and 16 reference JsonSnapshotStore by name)
    - src/RigToggle.Core/Persistence/JsonToggleInProgressStore.cs (doc comment lines 12 and 15 reference JsonSnapshotStore by name)
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-PATTERNS.md (the `Program.cs` Pattern Assignment section gives the literal before/after)
  </read_first>
  <action>
    First run the confirm-dead-before-delete gate and capture its output for the summary: `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot" src --include="*.cs"`. Confirm the only non-comment production reference is the `JsonSnapshotStore` construction at `src/RigToggle.App/Program.cs:93`. If any other production call site appears, STOP and report it instead of deleting.

    Delete these three files outright:
    - `src/RigToggle.Core/Abstractions/ISnapshotStore.cs`
    - `src/RigToggle.Core/Models/StateSnapshot.cs`
    - `src/RigToggle.Core/Persistence/JsonSnapshotStore.cs`

    In `src/RigToggle.App/Program.cs`, replace the `var snapshotStore = new JsonSnapshotStore(Path.Combine(basePath, "state.json"));` line with a local string declaration named exactly `legacyStateJsonPath`, assigned `Path.Combine(basePath, "state.json")`. Then, inside the existing `if (!modeStore.Exists())` block, change the argument of `modeStore.Save(...)` from `snapshotStore.Exists() ? ToggleMode.Rig : ToggleMode.Normal` to `File.Exists(legacyStateJsonPath) ? ToggleMode.Rig : ToggleMode.Normal`. Preserve the existing 16-RESEARCH.md Pattern 1 comment block above that `if` verbatim — its "seed mode.json from legacy snapshot presence exactly once" warning is still exactly what the code does. Keep the `using RigToggle.Core.Persistence;` import: `JsonSettingsStore`, `JsonModeStore` and `JsonToggleInProgressStore` are all still constructed in this file. Do not reorder or otherwise disturb the `modeStore` / `markerStore` construction lines or the `StartupRecoveryChecker.Run(modeStore, markerStore)` call.

    Update the three surviving doc comments that name the now-deleted types, so the post-deletion grep returns zero and no comment points at a type that no longer exists:
    - `IModeStore.cs` line 7: it currently says this interface replaces "ISnapshotStore's role as the mode indicator (D-14)". Reword to describe the historical mechanism without naming the deleted type — e.g. refer to "the retired snapshot-file-presence mode indicator (D-14, removed in Phase 18)".
    - `JsonModeStore.cs` lines 11 and 16: both cite `JsonSnapshotStore` as the sibling sharing the atomic temp-file + `File.Move(..., overwrite: true)` idiom and as the single-exception-coverage contrast. Repoint line 11 at `JsonSettingsStore`/`JsonToggleInProgressStore` (both still present and still using the same idiom) and rewrite line 16's contrast so it no longer names the deleted class.
    - `JsonToggleInProgressStore.cs` lines 12 and 15: same treatment — repoint the idiom reference at `JsonModeStore`, and reword the `JsonSnapshotStore.Clear()` mention at line 15 to describe the behavior rather than name the deleted method.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.App/RigToggle.App.csproj -p:EnableWindowsTargeting=true 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/RigToggle.Core/Abstractions/ISnapshotStore.cs && test ! -e src/RigToggle.Core/Models/StateSnapshot.cs && test ! -e src/RigToggle.Core/Persistence/JsonSnapshotStore.cs` exits 0
    - `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot" src/RigToggle.Core src/RigToggle.App src/RigToggle.Windows --include="*.cs"` prints nothing (exit status 1)
    - `grep -c "legacyStateJsonPath" src/RigToggle.App/Program.cs` prints exactly `2` (the declaration and the `File.Exists` use)
    - `grep -n "File.Exists(legacyStateJsonPath) ? ToggleMode.Rig : ToggleMode.Normal" src/RigToggle.App/Program.cs` prints exactly one line
    - `grep -c "snapshotStore" src/RigToggle.App/Program.cs` prints `0`
    - `grep -n "using RigToggle.Core.Persistence;" src/RigToggle.App/Program.cs` still prints one line
    - `grep -n "StartupRecoveryChecker.Run(modeStore, markerStore)" src/RigToggle.App/Program.cs` still prints one line
    - `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.App/RigToggle.App.csproj -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>The three snapshot files are gone, `Program.cs` seeds mode from a bare `File.Exists(legacyStateJsonPath)` check with identical semantics, no surviving comment names a deleted type, and the App project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the dead audio Restore path from the contract and the Windows adapter</name>
  <files>src/RigToggle.Core/Abstractions/IAudioController.cs, src/RigToggle.Windows/WindowsAudioController.cs</files>
  <read_first>
    - src/RigToggle.Core/Abstractions/IAudioController.cs (26 lines — read all of it; the class doc comment at lines 5-10 and the `Restore` declaration at line 16 both change)
    - src/RigToggle.Windows/WindowsAudioController.cs (read the whole file; you need the exact boundary between the deleted `Restore` at line 126 and the surviving `ApplyAndVerify` at line 183, plus the class doc comment at lines 9-21 and `ApplyAndVerify`'s own leading comment)
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-PATTERNS.md (the `WindowsAudioController.cs` Pattern Assignment names exactly what stays)
  </read_first>
  <action>
    In `src/RigToggle.Core/Abstractions/IAudioController.cs`: delete the `void Restore(AudioState previousState);` member declaration (line 16). Rewrite the interface's XML doc summary (lines 5-10) — it currently reads "Audio playback endpoint enumeration and default-device switch/restore contract" and "Read methods (GetPlaybackDevices, CaptureState) are real starting Phase 2; SetDefault/Restore are no-op stubs until Phase 3". Both halves are stale. Replace with a summary describing the surviving forward-only contract: enumeration (`GetPlaybackDevices`), per-role capture (`CaptureState`), forward application (`SetDefault`), and existence resolution (`TryResolveDevice`), implemented by `RigToggle.Windows.WindowsAudioController`. Do not touch the `TryResolveDevice` declaration or its own XML doc block.

    In `src/RigToggle.Windows/WindowsAudioController.cs`: delete the entire `public void Restore(AudioState previousState)` method (declared at line 126, body ends immediately before `ApplyAndVerify` at line 183), together with the multi-line leading `//` comment block directly above it that begins "isolated in its own try/catch so one role's restore failure". The deletion takes the whole stale-ID-to-friendly-name fallback with it — the `TryResolveDevice(deviceId) is null` null-out, the `GetPlaybackDevices().FirstOrDefault(d => string.Equals(d.FriendlyName, snapshot.DeviceName, ...))` lookup, and the per-role `catch (InvalidOperationException)` swallow. All of that existed only to serve `Restore`.

    Keep, untouched: `GetPlaybackDevices`, `CaptureState`, `SetDefault`, `SetDefaultForAllRoles`, `ApplyAndVerify`, `TryResolveDevice`, the `Roles` static array, and the `ERole`/`Role` pairing. `TryResolveDevice` is a live Phase 15 / AUDIO-05 method with its own callers — deleting it would be a regression, not cleanup.

    Update three stale comments left behind by the deletion:
    - Class doc summary line 11: "SetDefault/Restore apply the requested device to all three Windows audio roles" → drop the `/Restore`.
    - Class doc summary lines 19-20: "so restore can be exact per role" → reword to describe why all three roles are captured independently (D-02) without implying a restore path exists.
    - The `Roles` field comment ("applied to all three roles for every SetDefault/Restore cycle") and `ApplyAndVerify`'s leading comment ("Shared by SetDefault and Restore (D-01/D-02/D-03/D-04)") → both should now name only `SetDefault`.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Restore" src/RigToggle.Core/Abstractions/IAudioController.cs` prints `0`
    - `grep -c "Restore" src/RigToggle.Windows/WindowsAudioController.cs` prints `0`
    - `grep -c "TryResolveDevice" src/RigToggle.Windows/WindowsAudioController.cs` prints `1` (only the surviving method declaration — the `Restore`-internal call site is gone)
    - `grep -n "public AudioDeviceInfo? TryResolveDevice" src/RigToggle.Core/Abstractions/IAudioController.cs src/RigToggle.Windows/WindowsAudioController.cs` prints one line per file
    - `grep -n "ApplyAndVerify\|SetDefaultForAllRoles\|public void SetDefault\|public AudioState CaptureState\|GetPlaybackDevices" src/RigToggle.Windows/WindowsAudioController.cs` shows all five members still present
    - `wc -l < src/RigToggle.Windows/WindowsAudioController.cs` prints a number below 190 (was 237; the deleted `Restore` block plus its comment is roughly 60 lines)
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>`IAudioController` declares four forward-only members and no `Restore`; `WindowsAudioController` contains no `Restore` method, no stale-ID friendly-name fallback, and no comment naming `Restore`; the full solution still builds.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the snapshot test double and the five snapshot persistence tests</name>
  <files>src/RigToggle.Tests/Doubles/InMemoryStores.cs, src/RigToggle.Tests/JsonStoreTests.cs</files>
  <read_first>
    - src/RigToggle.Tests/Doubles/InMemoryStores.cs (120 lines — `InMemorySnapshotStore` occupies lines 6-33 including its doc comment; lines 57 and 80 in the surviving `InMemoryModeStore` / `InMemoryToggleInProgressStore` doc comments cross-reference it by name)
    - src/RigToggle.Tests/JsonStoreTests.cs (365 lines — the file header comment at line 9 names `JsonSnapshotStore`; the five `SnapshotStore_` facts start at lines 261, 274, 286, 305, 322 and run to end of file)
  </read_first>
  <action>
    In `src/RigToggle.Tests/Doubles/InMemoryStores.cs`: delete the entire `InMemorySnapshotStore` class together with its XML doc comment (the block spanning the `/// <summary> Hand-written in-memory ISnapshotStore double ...` comment through the class's closing brace). Leave `InMemorySettingsStore`, `InMemoryModeStore`, `InMemoryToggleInProgressStore` and `ThrowingClearToggleInProgressStore` untouched. Two surviving doc comments cross-reference the deleted class by name — `InMemoryModeStore`'s (around line 57, "matching InMemorySnapshotStore's convention") and `InMemoryToggleInProgressStore`'s (around line 80, same phrasing). Reword both to describe the shared call-log convention directly ("records mutating operations to the shared call-log; read-only queries are intentionally not logged") instead of naming a class that no longer exists. If the `using RigToggle.Core.Abstractions;` or `using RigToggle.Core.Models;` imports become unused after the deletion, remove them; if the surviving doubles still need them, keep them.

    In `src/RigToggle.Tests/JsonStoreTests.cs`: locate the current line numbers with `grep -n "SnapshotStore_" src/RigToggle.Tests/JsonStoreTests.cs` before editing (line numbers shift as you delete). Delete all five `[Fact]` methods whose names begin with `SnapshotStore_` — `SnapshotStore_Exists_IsFalseBeforeSave_TrueAfterSave`, `SnapshotStore_Clear_DeletesFile_SoExistsReturnsFalseAgain`, `SnapshotStore_Load_ReturnsNullWhenAbsent_AndSavedSnapshotWhenPresent`, `SnapshotStore_Load_OnMalformedFile_ReturnsNullInsteadOfThrowing`, `SnapshotStore_MonitorState_RoundTripsAllPathFields` — each including its `[Fact]` attribute line and any leading comment that belongs solely to it. Delete nothing whose name begins with `SettingsStore_`: those exercise `JsonSettingsStore`, an unrelated live class. Update the file's header comment at line 9 ("Proves JsonSettingsStore/JsonSnapshotStore's create-if-missing load, round-trip ...") to name only `JsonSettingsStore`. Remove any `using` import that becomes unused (e.g. one pulling in `RigToggle.Core.Models` solely for `StateSnapshot`) — only if the surviving `SettingsStore_` tests do not also need it.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "InMemorySnapshotStore\|SnapshotStore_" src --include="*.cs"` prints nothing (exit status 1)
    - `grep -c "SettingsStore_" src/RigToggle.Tests/JsonStoreTests.cs` prints a number greater than 0 (the surviving settings-persistence coverage is intact)
    - `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot" src --include="*.cs"` prints nothing across the entire src tree (exit status 1)
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0` and `Total: 80` (exactly 5 fewer than the measured 85 baseline — a different number means over- or under-deletion)
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
  </acceptance_criteria>
  <done>The snapshot double and its five persistence tests are gone, no source file anywhere in `src/` mentions a snapshot type, and the core suite passes at exactly 80 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app (startup) | `%APPDATA%\RigToggle\state.json` / `mode.json` are user-writable files read during the one-time mode-seeding bootstrap |
| interface contract → implementers | Removing a member from `IAudioController` changes the compile-time contract every adapter and test double must satisfy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | `Program.cs` legacy-mode seeding | mitigate | The replacement reads only file *presence* via `File.Exists(legacyStateJsonPath)` and never deserializes attacker-controllable JSON — strictly less parsing surface than the `JsonSnapshotStore.Load()` path it replaces. Acceptance criteria assert the exact ternary so the semantics cannot silently invert. |
| T-18-01-02 | Denial of Service | Startup bootstrap | accept | A hostile `state.json` can at most cause the app to seed mode as Rig on a first-ever launch, which the existing `StartupRecoveryChecker` mode-corruption dialog already surfaces to the user. Local single-user desktop utility; no privilege boundary crossed. |
| T-18-01-03 | Tampering | Dead-code deletion of `WindowsAudioController.Restore` | mitigate | Full deletion (not just call-site removal) prevents any future accidental re-wiring of an unexercised COM-interop path that could rot undetected. Confirm-dead-before-delete grep gate is run and its output recorded before any file is removed. |
| T-18-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan — it is deletion-only. Package changes are confined to plan 18-03, which carries its own audit gate. |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` → `0 Error(s)`
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` → `Failed: 0, Total: 80`
- `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` → builds; record "not executed (dev-host limitation)"
- `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot\|InMemorySnapshotStore" src --include="*.cs"` → no output
- `grep -c "Restore" src/RigToggle.Core/Abstractions/IAudioController.cs src/RigToggle.Windows/WindowsAudioController.cs` → `0` for both
</verification>

<success_criteria>
The snapshot-persistence subsystem no longer exists in `src/`, the audio contract and adapter are forward-only, `Program.cs` preserves the exact legacy-mode-seeding semantics through a bare file-existence check, the full solution builds with zero errors, and the core test suite passes at exactly 80 tests (85 baseline minus the 5 removed snapshot persistence tests).
</success_criteria>

<output>
Create `.planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md` when done, including the verbatim before-deletion grep output, the final build output, and the final test totals.
</output>
