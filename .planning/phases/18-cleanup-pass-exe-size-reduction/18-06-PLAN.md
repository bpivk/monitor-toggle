---
phase: 18-cleanup-pass-exe-size-reduction
plan: 06
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05"]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "After every Phase 18 change is merged, the full solution builds and the core suite passes at exactly 81 tests"
    - "The merged-tree published exe is measurably smaller than the pre-phase baseline, measured in bytes"
    - "IL trimming is still explicitly disabled and no elevation manifest, AOT or ReadyToRun property was introduced anywhere"
    - "On real rig hardware, a cold autostart boot still brings the tray icon up promptly despite single-file decompression"
    - "On real rig hardware, a full Rig-to-Normal-and-back toggle round trip still switches monitors, audio and the companion app correctly"
    - "On real rig hardware, the Settings audio pickers and the manual monitor panel still behave correctly after the cleanup pass"
  artifacts:
    - path: ".planning/phases/18-cleanup-pass-exe-size-reduction/18-06-SUMMARY.md"
      provides: "Merged-tree regression output, the final exe byte count and percentage reduction, all regression-gate outputs, and the rig verdict for both PERF-02 checks"
  key_links:
    - from: "src/RigToggle.App/Properties/PublishProfiles/win-x64.pubxml"
      to: "cold autostart boot time on the rig"
      via: "EnableCompressionInSingleFile decompressing embedded assemblies at process start"
      pattern: "EnableCompressionInSingleFile"
    - from: "src/RigToggle.Windows/WindowsMonitorController.cs"
      to: "rig toggle round trip"
      via: "DeactivateMonitors/ActivateMonitors after the _originalPathsCache write site was removed"
      pattern: "DeactivateMonitors"
---

<objective>
Close Phase 18 with a merged-tree regression gate, a final exe-size measurement on the fully-integrated code, and a rig-hardware verification that the packaging change did not break the cold autostart path or the toggle round trip.

Purpose: everything provable on this Linux dev host has been proved by plans 18-01 through 18-05 in isolation. What remains is confirming the five parallel changes are correct *together* rather than only individually, and then the one thing that is structurally unprovable off-rig — that a compressed single-file exe still starts promptly on the real machine and that CCD display control, IPolicyConfig audio switching and companion-app control all still work after the dead-code pass.
Output: `18-06-SUMMARY.md` carrying the merged regression output, the final byte-count delta, and the rig verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-RESEARCH.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-02-SUMMARY.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-03-SUMMARY.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-04-SUMMARY.md
@.planning/phases/18-cleanup-pass-exe-size-reduction/18-05-SUMMARY.md

<phase_success_criteria>
From ROADMAP.md Phase 18 — every one must be addressed by Task 1 (automated) or Task 2 (rig):
1. `Restore()`/`RestoreViaReconstruction()` and related snapshot-restore models are removed, with rig-specific knowledge reviewed and preserved elsewhere first (CLEANUP-01) — Task 1 audits
2. The codebase shows measurably less duplication/cruft, with no user-facing behavior change (CLEANUP-02) — Task 1 audits statically, Task 2 confirms the no-behavior-change half on the rig
3. The self-contained exe is measurably smaller after the four MSBuild levers, without IL trimming (PERF-01) — Task 1 measures
4. A full toggle round trip and a cold autostart boot are verified working on real rig hardware after the exe-size changes, not just a build-output file-size diff (PERF-02) — Task 2
</phase_success_criteria>

<environment_note>
This dev host is Linux. `dotnet` lives at `$HOME/.dotnet/dotnet` — prefix commands with `PATH="$HOME/.dotnet:$PATH"`.

- `dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` is the full-solution gate.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` runs.
- `src/RigToggle.Windows.Tests` **builds but cannot execute** here (`Microsoft.WindowsDesktop.App` runtime absent). Build it, do not run it, and report it as "built, not executed (dev-host limitation)". This is a pre-existing constraint carried since Phase 16, not a Phase 18 regression.
</environment_note>

<measured_baselines>
Reference figures for the final assertions:

- **Test totals:** the pre-phase baseline was `Total: 85`. Plan 18-01 removes exactly 5 (the `SnapshotStore_*` persistence tests) and plan 18-04 adds exactly 1 (`FormatChecklist_SkippedStep_RendersLowercaseSkipped`). Plans 18-02 and 18-05 remove zero from this project. **The merged expected total is exactly 81.** Plan 18-02 removes 6 facts from `RigToggle.Windows.Tests` (14 to 8), which cannot be executed here and must be verified by `[Fact]` count instead.
- **Exe size:** the pre-phase baseline measured in research was **116,946,229 bytes**; with all four levers applied it measured **49,360,387 bytes** (57.8% smaller). The merged tree also deletes several hundred lines of dead code, so the final figure should land at or slightly below the research number.
- **`WindowsMonitorController.cs`:** 727 lines before Phase 18, expected under 400 after plan 18-02.
</measured_baselines>

<hard_constraints>
`PublishTrimmed` must still read literally `false`. Do not attempt to improve the size result further with `PublishTrimmed`, `PublishAot` or `PublishReadyToRun` — trimming strips the IPolicyConfig COM interop and WindowsDisplayAPI P/Invoke paths this app depends on, and it is a locked prohibition in both CLAUDE.md and REQUIREMENTS.md.

No elevation manifest may be introduced. The tool stays `asInvoker`.
</hard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merged-tree regression gate, final exe-size measurement, and phase audits</name>
  <files>.planning/phases/18-cleanup-pass-exe-size-reduction/18-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-01-SUMMARY.md through 18-05-SUMMARY.md (every deviation and every recorded number; in particular 18-03's baseline byte count, which you need to compute the final percentage against the same reference)
    - src/RigToggle.App/Properties/PublishProfiles/win-x64.pubxml (confirm the merged state has EnableCompressionInSingleFile true and PublishTrimmed false)
    - src/RigToggle.App/RigToggle.App.csproj (confirm both SatelliteResourceLanguages and InvariantGlobalization survived the merge into the single PropertyGroup)
    - src/RigToggle.Windows/RigToggle.Windows.csproj (confirm the NAudio.Wasapi reference survived the merge and no NAudio meta-package reference came back)
    - .planning/debug/knowledge-base.md (confirm plan 18-02's ccd-topology-restore-findings entry is present in the merged tree — CLEANUP-01's "preserve first" requirement is only satisfied if it actually landed)
  </read_first>
  <action>
    Run every gate below against the fully-merged tree and record verbatim output for each in the summary. Do not report a gate as "passed" without including its output.

    Regression gate:
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` must report `0 Error(s)`. Record the warning count; the pre-existing `xUnit1031` warnings in `ToggleOrchestratorTests.cs` are the known baseline. Investigate and report any rise above baseline rather than ignoring it.
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` must report `Failed: 0` with `Total: 81`. Any other total means one of the five plans over- or under-deleted; reconcile against each plan's recorded delta before proceeding.
    - `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` must build; record explicitly that it was not executed and why.

    CLEANUP-01 closing audit — run all four greps, all must return nothing:
    - `grep -rn "\.Restore(" src --include="*.cs"`
    - `grep -rn "ISnapshotStore\|JsonSnapshotStore\|StateSnapshot\|InMemorySnapshotStore" src --include="*.cs"`
    - `grep -rn "RestoreViaReconstruction\|CopyOutputTechnology\|AssignSource\|_originalPathsCache\|throwOnRestore" src --include="*.cs"`
    - `grep -rn "SnapshotStore_" src --include="*.cs"`

    Then confirm the knowledge-preservation precondition actually landed: `grep -c "ccd-topology-restore-findings" .planning/debug/knowledge-base.md` returns at least 1, and that entry contains the exact fragile identifiers `<OutputTechnology>k__BackingField` and `SDC_ALLOW_CHANGES`. CLEANUP-01's requirement is "removed *after* being reviewed for rig-specific knowledge worth preserving" — without this entry the requirement is not met even if the code is gone.

    CLEANUP-02 closing audit:
    - `grep -rn "No audio devices detected\|items.Count == 0" src/RigToggle.App/SettingsForm.cs` returns nothing.
    - `grep -c "Skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs` prints `0`.
    - `grep -c "\[Fact\]" src/RigToggle.Windows.Tests/WindowsMonitorControllerTests.cs` prints `8`.
    - `wc -l < src/RigToggle.Windows/WindowsMonitorController.cs` prints under 400.
    - Record the aggregate lines-of-code delta across `src/` for the whole phase via `git diff --stat` against the pre-phase commit. This is the quantitative "measurably less cruft" evidence ROADMAP success criterion 2 asks for — report the net deleted-line count, not an adjective.

    PERF-01 final measurement:
    - Delete `src/RigToggle.App/bin/publish/win-x64/` so no stale artifact is measured, then run `PATH="$HOME/.dotnet:$PATH" dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64 -p:EnableWindowsTargeting=true`.
    - Record `stat -c %s src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe` as an exact integer, and compute the absolute and percentage delta against the pre-phase baseline recorded in `18-03-SUMMARY.md`.
    - List the publish directory and confirm the output is still a genuine single exe with no loose managed `.dll` files beside it.
    - Preserve this exe and note its full path and byte count in the summary. Task 2's rig verification must run this exact artifact, so the human is testing the binary that was measured rather than a separate rebuild.

    Prohibited-lever regression gates:
    - `grep -rn "PublishTrimmed" src/` returns exactly one line, reading `<PublishTrimmed>false</PublishTrimmed>`.
    - `grep -rin "requestedExecutionLevel\|ApplicationManifest\|PublishAot\|PublishReadyToRun" src/ --include="*.csproj" --include="*.pubxml"` returns nothing.

    Finally, write the rig test script into the summary — the numbered steps from Task 2, with the exe path filled in — so the checkpoint hands the user concrete instructions rather than a vague request to try it.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true 2>&1 | tail -3 && stat -c %s src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe && grep -rn "PublishTrimmed" src/</automated>
  </verify>
  <acceptance_criteria>
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` reports `0 Error(s)`
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0` and `Total: 81`
    - `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` reports `0 Error(s)` and the summary records it as built-not-executed with the reason
    - All four CLEANUP-01 audit greps return nothing (exit status 1) and their commands plus empty results are recorded in the summary
    - `grep -c "ccd-topology-restore-findings" .planning/debug/knowledge-base.md` prints at least `1`
    - `grep -c "<OutputTechnology>k__BackingField" .planning/debug/knowledge-base.md` prints at least `1` and `grep -c "SDC_ALLOW_CHANGES" .planning/debug/knowledge-base.md` prints at least `1`
    - `grep -c "\[Fact\]" src/RigToggle.Windows.Tests/WindowsMonitorControllerTests.cs` prints `8`
    - `wc -l < src/RigToggle.Windows/WindowsMonitorController.cs` prints a number below `400`
    - `grep -rn "No audio devices detected\|items.Count == 0" src/RigToggle.App/SettingsForm.cs` returns nothing
    - `grep -c "Skipped (not configured)" src/RigToggle.Core/ToggleResultFormatter.cs` prints `0`
    - `stat -c %s src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe` prints an integer below `60000000`, and the summary records it as at least 45% below the pre-phase baseline integer from `18-03-SUMMARY.md`
    - `ls src/RigToggle.App/bin/publish/win-x64/` shows `RigToggle.App.exe` and contains no `*.dll` entries
    - `grep -rn "PublishTrimmed" src/` returns exactly one line and it reads `<PublishTrimmed>false</PublishTrimmed>`
    - `grep -rin "requestedExecutionLevel\|ApplicationManifest\|PublishAot\|PublishReadyToRun" src/ --include="*.csproj" --include="*.pubxml"` returns nothing
    - The summary records the `git diff --stat` net line delta across `src/` as an explicit number
  </acceptance_criteria>
  <done>Every automated phase gate has been run against the merged tree with its verbatim output recorded: the solution builds, the suite passes at 81, all four dead-code audits are empty, the preserved knowledge entry is confirmed present, the exe is under 60 MB and at least 45% smaller, and no prohibited publish lever or elevation element exists anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rig verification — cold autostart boot and full toggle round trip (PERF-02)</name>
  <files>.planning/phases/18-cleanup-pass-exe-size-reduction/18-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-06-SUMMARY.md (Task 1's output — you need the exact measured exe path and byte count to hand the user, so they test the binary that was measured)
    - .planning/phases/18-cleanup-pass-exe-size-reduction/18-RESEARCH.md Pitfall 1 and Open Question 2 (why the cold-boot check is qualitative for this codebase: there is no existing startup telemetry to compare against, so a stopwatch impression is the correct instrument and inventing a millisecond SLA would be fabrication)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-VERIFICATION.md (the documented-override convention this project uses when the user waives a scenario rather than testing it — accepted_by / accepted_at in frontmatter, never a false "tested and passed")
  </read_first>
  <action>
    Present the ten numbered rig verification steps below to the user verbatim, with the measured exe path and byte count from Task 1 substituted in, then wait for their results and record the outcome in `18-06-SUMMARY.md` under a `## Rig Verification` heading — one labeled entry per numbered step with an explicit PASS / FAIL / NOT TESTED verdict and the user's observed behavior. For step 3, record their timing impression as stated (for example "tray icon appeared in about a second, same as before") rather than converting it into a number the user did not give.

    Do not run any of this yourself and do not infer a result from code reading. None of it is observable on this Linux dev host: there is no Windows display subsystem, no audio endpoint, no Moza Companion process and no autostart registration here. Do not mark a step PASS on the basis of "the code looks correct" or "the automated gates passed".

    Treat step 3 with the threshold stated in the script: a sub-second cold-start delta is the documented, accepted tradeoff of `EnableCompressionInSingleFile` and is a PASS with the delta noted — do not record it as a failure. A multi-second delay, or the tray icon never appearing, is a genuine FAIL.

    If the user reports a FAIL, do not attempt a fix inside this plan — write it up as a gap with observed-versus-expected so the operator can run gap closure. If the user chooses to waive a step rather than test it (for instance declining a full reboot), record it as a documented override naming who accepted it and when, in the same style Phase 16 used for DISPLAY-13 — never as a false "tested and passed".
  </action>
  <what-built>
    Phase 18 changed nothing about what the app does — it deleted the dead snapshot-restore subsystem, closed four reviewed code-quality findings, and shrank the shipped exe from roughly 112 MB to roughly 47 MB using four MSBuild-level packaging options. Three of those options are inert at runtime. One is not: `EnableCompressionInSingleFile` compresses the embedded managed assemblies inside the single-file bundle, and Windows must decompress them into memory on every process start. Microsoft documents this as a real cold-start cost and recommends measuring it rather than assuming it is free.

    That cost, and the continued correctness of the CCD display API, the IPolicyConfig audio COM interop and the companion-app window control after a large dead-code deletion, are the four things that cannot be observed from a Linux build host. Everything else in this phase has already been proved automatically and is recorded in `18-06-SUMMARY.md`.

    The exe to test is the exact artifact Task 1 measured — its full path and byte count are recorded at the top of `18-06-SUMMARY.md`. Copy that file to the rig and test it; do not rebuild on the rig, or the measured binary and the tested binary stop being the same thing.
  </what-built>
  <how-to-verify>
    **A — Cold autostart boot (the PERF-02 compression check).** This is the one check the compression change could plausibly regress.

    1. Copy the published `RigToggle.App.exe` from `src/RigToggle.App/bin/publish/win-x64/` to wherever the rig's autostart entry points, replacing the previous exe. Keep a copy of the old exe so you can A/B it if step 3 feels slow.
    2. Reboot Windows, or sign out and back in, so the autostart path runs genuinely cold rather than from a warm file cache.
    3. Watch the system tray after login. Note roughly how long the RigToggle tray icon takes to appear, and whether it appears at all. A stopwatch is fine — this codebase has never had startup telemetry, so there is no numeric SLA to hit and none is being invented. The question is qualitative: **does autostart still feel effectively instant, or is there a new, noticeable delay?**
    4. If it feels slower, do the same reboot with the old exe for comparison and note the rough difference. A sub-second delta is the expected tradeoff and is a pass — record it, do not fail the phase for it. A delay of multiple seconds, or the tray icon not appearing at all, is a real failure and should be reported as such.

    **B — Full toggle round trip (the PERF-02 functional check).**

    5. Trigger a toggle to Rig mode (tray icon, hotkey, or the main window — whichever you normally use). Confirm: the configured monitors actually switch, the default audio output actually moves to the rig speakers, and the Moza Companion app launches or is brought up as configured.
    6. Confirm the result checklist dialog/toast appears and reads correctly. If any step shows "skipped", it should now render lowercase as `skipped (not configured)`, matching the existing `not attempted` wording. That capitalization change is intentional (15-REVIEW IN-02) and is the only user-visible text change in this phase.
    7. Toggle back to Normal mode. Confirm the Normal-mode monitor set and Normal-mode audio device both apply and the companion app minimizes as configured.
    8. Close the app completely and relaunch it. Confirm it reports the correct current mode. This exercises the rewritten `Program.cs` bootstrap, which now reads legacy `state.json` presence with a plain file check instead of through the deleted snapshot store.

    **C — Cleanup-pass spot checks (confirming "no user-facing behavior change").**

    9. Open Settings. Confirm both audio dropdowns still list your devices with `(None — don't switch audio)` at the top, that selecting the sentinel is accepted and Save is not blocked, and that a previously-saved selection still comes back selected when you reopen the dialog.
    10. Open the manual monitor panel. Confirm it still lists every monitor with the correct on/off status, that enabling or disabling one still works, and that trying to disable the last remaining enabled monitor is still rejected. This path shares `DeactivateMonitors` with the toggle, and that method had a dead cache-write line removed from inside it.

    Report per-item pass/fail. For item 3, include your rough timing impression. If anything fails, say which numbered step and what you observed rather than a general "it broke".
  </how-to-verify>
  <resume-signal>Type "approved" if all ten items pass (a sub-second cold-start delta counts as a pass — note it), or describe which numbered step failed and what you observed.</resume-signal>
  <verify>
    <human-check>The user has reported a PASS / FAIL / NOT TESTED verdict for each of the ten numbered steps, and those verdicts are written into `18-06-SUMMARY.md` under `## Rig Verification`. No automated command can substitute for this: the CCD display API, the IPolicyConfig audio endpoint, the companion process and the autostart path do not exist on this Linux dev host.</human-check>
  </verify>
  <acceptance_criteria>
    - `18-06-SUMMARY.md` contains a `## Rig Verification` heading with exactly ten labeled entries, one per numbered step, each carrying an explicit PASS, FAIL or NOT TESTED verdict
    - Step 3's entry records the user's own stated cold-boot timing impression, not an invented millisecond figure
    - The exe path and byte count presented to the user match the values Task 1 recorded from `stat -c %s src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe`
    - No step is marked PASS without the user's reported observation quoted or paraphrased alongside it
    - Any NOT TESTED step is recorded as a documented override with `accepted_by` and `accepted_at`, matching the Phase 16 DISPLAY-13 convention, and is never recorded as PASS
    - Any FAIL is written up as an observed-versus-expected gap, with no fix attempted inside this plan
  </acceptance_criteria>
  <done>All ten rig verification steps have a recorded verdict from the user, PERF-02's two required checks (cold autostart boot and full toggle round trip) are both covered, and any failure or waiver is documented in this project's established convention rather than glossed as a pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published artifact → rig machine | The measured exe is copied to and executed on the real Windows machine |
| app → CCD display driver / IPolicyConfig COM / companion process | The three native subsystems exercised only by the rig round trip |
| autostart registration → process launch | The `--tray` hidden-start path runs unattended at login |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Denial of Service | Cold autostart boot after `EnableCompressionInSingleFile` | mitigate | Verification item A is a blocking human check on the real cold-boot path with an explicit pass/fail threshold: a sub-second delta is an accepted documented tradeoff, a multi-second delay or a missing tray icon is a failure. The A/B comparison against the previous exe is specified so the judgement is relative rather than impressionistic. |
| T-18-06-02 | Tampering | Testing a different binary than the one measured | mitigate | Task 1 records the exact path and byte count of the measured artifact and Task 2 instructs the user to copy that file rather than rebuild on the rig, so the size claim and the functional verdict describe the same binary. |
| T-18-06-03 | Denial of Service | Regression in CCD display control after removing the `_originalPathsCache` write from inside `DeactivateMonitors` | mitigate | Verification items B5, B7 and C10 exercise all three callers of `DeactivateMonitors` (Rig toggle, Normal toggle, manual panel) on real hardware, including the zero-survivors rejection path that the shared safety guard from Phase 17 protects. |
| T-18-06-04 | Elevation of Privilege | Elevation manifest or trimming reintroduced during the phase | mitigate | Task 1 greps every `.csproj` and `.pubxml` in the merged tree for `requestedExecutionLevel`, `ApplicationManifest`, `PublishAot` and `PublishReadyToRun` and requires zero hits, plus requires `PublishTrimmed` to read exactly `false`. The app stays `asInvoker` and untrimmed. |
| T-18-06-05 | Repudiation | Claiming CLEANUP-01 complete without the knowledge-preservation precondition | mitigate | Task 1 asserts the `ccd-topology-restore-findings` knowledge-base entry exists in the merged tree and contains the two exact fragile identifiers, since CLEANUP-01's wording makes preservation a precondition of removal rather than an optional extra. |
| T-18-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan — it runs existing gates and a human verification. The one NuGet change in this phase was made and audited in plan 18-03; Task 1 re-confirms `NAudio.Wasapi` survived the merge and that the meta-package did not return. |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` → `0 Error(s)`
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` → `Failed: 0, Total: 81`
- `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` → builds; recorded as not executed (dev-host limitation)
- Four CLEANUP-01 audit greps → all empty
- `stat -c %s src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe` → under 60,000,000 and at least 45% below the pre-phase baseline
- `grep -rn "PublishTrimmed" src/` → exactly one line, `<PublishTrimmed>false</PublishTrimmed>`
- Human rig verdict recorded for all ten numbered items, with the cold-boot timing impression noted for item 3
</verification>

<success_criteria>
All four ROADMAP Phase 18 success criteria are evidenced: the snapshot-restore subsystem is provably absent with its rig knowledge preserved first (CLEANUP-01); the codebase carries a recorded net line reduction with the four reviewed findings closed and no user-facing behavior change confirmed on the rig (CLEANUP-02); the exe is measurably smaller by an exact recorded byte delta achieved without IL trimming (PERF-01); and a real cold autostart boot plus a full toggle round trip are confirmed working on rig hardware, not merely inferred from a file-size diff (PERF-02).
</success_criteria>

<output>
Create `.planning/phases/18-cleanup-pass-exe-size-reduction/18-06-SUMMARY.md` when done, containing: the measured exe path and byte count at the top (so the rig tester copies the right artifact), the merged build and test output, the four CLEANUP-01 audit greps with their empty results, the CLEANUP-02 audit results and the `git diff --stat` net line delta, both prohibited-lever gate outputs, and the per-item rig verdict for all ten verification steps including the cold-boot timing impression.
</output>
