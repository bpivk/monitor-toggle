---
phase: 11-configurable-tray-close-minimize-behavior-user-selectable-op
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/RigToggle.App/SettingsForm.Designer.cs
  - src/RigToggle.App/SettingsForm.cs
  - src/RigToggle.App/Program.cs
autonomous: true
requirements: [TRAY-01]

must_haves:
  truths:
    - "D-03/D-06/D-07: Settings shows two new checkboxes: 'Closing the window (X) minimizes to tray' and 'Minimizing the window also sends it to tray', grouped directly above 'Start with Windows'"
    - "D-03/D-06: Opening Settings loads each checkbox from its AppSettings field; Save writes each back with no failure/error path (plain round-trip)"
    - "D-08: Saving Settings applies the derived tray-icon visibility immediately (live), not only after restart"
  artifacts:
    - path: "src/RigToggle.App/SettingsForm.Designer.cs"
      provides: "chkCloseMinimizesToTray + chkMinimizeToTray controls and the relayout that makes room for them"
      contains: "chkCloseMinimizesToTray"
    - path: "src/RigToggle.App/SettingsForm.cs"
      provides: "Load/Save wiring for both checkboxes + injected Action that applies tray visibility live on Save"
      contains: "_applyTrayVisibility"
    - path: "src/RigToggle.App/Program.cs"
      provides: "SettingsFormFactory passes mainForm.ApplyTrayVisibility as the new Action arg"
      contains: "mainForm.ApplyTrayVisibility"
  key_links:
    - from: "src/RigToggle.App/SettingsForm.cs BtnSaveSettings_Click"
      to: "MainForm.ApplyTrayVisibility"
      via: "injected Action _applyTrayVisibility invoked right after _settingsStore.Save"
      pattern: "_applyTrayVisibility\\(\\)"
    - from: "src/RigToggle.App/Program.cs SettingsFormFactory"
      to: "mainForm.ApplyTrayVisibility"
      via: "constructor argument"
      pattern: "mainForm.ApplyTrayVisibility"
---

<objective>
Add the two Settings checkboxes that let the user configure the close/minimize preferences, wire them to their AppSettings fields on Load/Save, and make the tray icon appear/disappear live the moment Settings is saved.

Purpose: Deliver the D-03/D-06/D-07 Settings UI (the only user-facing control surface for this phase) and the D-08 live-update-on-Save requirement, using the plain-AppSettings-round-trip pattern (`chkEnableDebugLogging`), NOT the registry-backed `chkStartWithWindows` pattern.

Output: Two `CheckBox` controls placed per the UI-SPEC layout (with the existing controls relaid-out to make room), Load/Save wiring in `SettingsForm`, a new injected `Action` that fires `MainForm.ApplyTrayVisibility()` on Save, and the `Program.cs` composition-root wiring that supplies that Action — with no `ErrorProvider`/warning label for either checkbox (neither has a failure path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-CONTEXT.md
@.planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-PATTERNS.md
@.planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-UI-SPEC.md

<interfaces>
<!-- BEHAVIOR template = chkEnableDebugLogging (plain AppSettings round-trip, NO service, NO ErrorProvider). -->
Load  (SettingsForm.cs line 88):   chkEnableDebugLogging.Checked = _settings.EnableDebugLogging;
Save  (SettingsForm.cs line 721):  EnableDebugLogging = chkEnableDebugLogging.Checked,   // inside the settingsToSave initializer
Designer decl (lines 268-274):     Text/Location/Size/AutoSize=false/Name, plain CheckBox, not in a GroupBox
Designer field (line 427):         private System.Windows.Forms.CheckBox chkEnableDebugLogging;
Designer instantiate (line 54):    this.chkEnableDebugLogging = new System.Windows.Forms.CheckBox();
Controls.Add (line 382):           this.Controls.Add(this.chkEnableDebugLogging);

<!-- DO NOT copy chkStartWithWindows's behavior: it round-trips through _autostartConfigurator (registry) with try/catch + errAutostart/lblAutostartWarning. That failure-handling pattern is WRONG for these two checkboxes (D-03/D-06 explicitly no confirmation/error path). chkStartWithWindows is only the LAYOUT/section precedent. -->

<!-- Injected-delegate DI precedent (mirror this exactly for the new Action): -->
SettingsForm ctor param (line 50):   Func<bool> tryRegisterConfiguredHotkey
SettingsForm field (line 19):        private readonly Func<bool> _tryRegisterConfiguredHotkey;
Save persist point (line 733):       _settingsStore.Save(settingsToSave);

<!-- Program.cs composition root -->
Line 114:  SettingsForm SettingsFormFactory() => new SettingsForm(monitorController, audioController, settingsStore, autostartConfigurator, mainForm.TryRegisterConfiguredHotkey);
<!-- mainForm.ApplyTrayVisibility is the new public method from Plan 11-02 -->

<!-- UI-SPEC prescribed layout (absolute px, 12px left margin, 396px width, 8px gaps) -->
chkCloseMinimizesToTray  Location(12, 600) Size(396, 24)  Text "Closing the window (X) minimizes to tray"
chkMinimizeToTray        Location(12, 632) Size(396, 24)  Text "Minimizing the window also sends it to tray"
chkStartWithWindows      moves (12, 600) -> (12, 664)
lblAutostartWarning      moves (12, 624) -> (12, 688)
btnSaveSettings          moves (180, 656) -> (180, 720)
btnDiscardChanges        moves (298, 656) -> (298, 720)
SettingsForm.ClientSize  grows (420, 704) -> (420, 768)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the two checkbox controls and relayout SettingsForm.Designer.cs</name>
  <files>src/RigToggle.App/SettingsForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.Designer.cs (chkEnableDebugLogging block lines 268-274, chkStartWithWindows block lines 312-318, lblAutostartWarning lines 320-327, btnSaveSettings/btnDiscardChanges lines 329-346, ClientSize line 370, field decls lines 427/433, instantiation lines 54/60, Controls.Add lines 382-388)
    - .planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-UI-SPEC.md ("D-07 Resolution" table — the exact prescribed coordinates)
  </read_first>
  <action>
    Add two plain `CheckBox` controls named `chkCloseMinimizesToTray` and `chkMinimizeToTray`, declared and instantiated exactly like `chkEnableDebugLogging` (field: `private System.Windows.Forms.CheckBox chkCloseMinimizesToTray;` near the existing checkbox field decls; instantiation `this.chkCloseMinimizesToTray = new System.Windows.Forms.CheckBox();` near line 54; same for `chkMinimizeToTray`). Neither is wrapped in a GroupBox.

    Set their designer properties to the UI-SPEC coordinates: `chkCloseMinimizesToTray` — Text `"Closing the window (X) minimizes to tray"`, Location `(12, 600)`, Size `(396, 24)`, AutoSize `false`, Name `"chkCloseMinimizesToTray"`. `chkMinimizeToTray` — Text `"Minimizing the window also sends it to tray"`, Location `(12, 632)`, Size `(396, 24)`, AutoSize `false`, Name `"chkMinimizeToTray"`.

    Relayout the existing controls below them to make room (they are pushed down 64px total): change `chkStartWithWindows.Location` from `(12, 600)` to `(12, 664)`; `lblAutostartWarning.Location` from `(12, 624)` to `(12, 688)`; `btnSaveSettings.Location` from `(180, 656)` to `(180, 720)`; `btnDiscardChanges.Location` from `(298, 656)` to `(298, 720)`; and `SettingsForm.ClientSize` from `(420, 704)` to `(420, 768)`. Do not change any Size values on the moved controls, only their Location (and the form's ClientSize).

    Add both new checkboxes to the form via `this.Controls.Add(...)`, placed immediately before the existing `this.Controls.Add(this.chkStartWithWindows);` line so tab order reads close → minimize → start-with-windows (matching the top-to-bottom visual order). Do NOT add any `ErrorProvider`, warning label, or `errCloseMinimize`/`errMinimizeToTray` control — neither checkbox has a runtime failure path (UI-SPEC Spacing/Copywriting: omitting the ErrorProvider is deliberate; adding one would be dead code).
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.App/RigToggle.App.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chkCloseMinimizesToTray" src/RigToggle.App/SettingsForm.Designer.cs` >= 4 (field decl, instantiation, property block, Controls.Add) and same for `chkMinimizeToTray`
    - `grep -c 'Closing the window (X) minimizes to tray' src/RigToggle.App/SettingsForm.Designer.cs` returns 1 and `grep -c 'Minimizing the window also sends it to tray' src/RigToggle.App/SettingsForm.Designer.cs` returns 1
    - `grep -c "new System.Drawing.Point(12, 600)" src/RigToggle.App/SettingsForm.Designer.cs` corresponds to chkCloseMinimizesToTray (chkStartWithWindows no longer at 600) and `grep -c "new System.Drawing.Size(420, 768)" src/RigToggle.App/SettingsForm.Designer.cs` returns 1
    - `grep -c "new System.Drawing.Point(12, 664)" src/RigToggle.App/SettingsForm.Designer.cs` returns 1 (chkStartWithWindows moved)
    - No `errCloseMinimize`, `errMinimizeToTray`, or new warning label is added (`grep -c "errCloseMinimize\|errMinimizeToTray\|lblCloseWarning\|lblMinimizeWarning" src/RigToggle.App/SettingsForm.Designer.cs` returns 0)
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` succeeds
  </acceptance_criteria>
  <done>Two plain checkboxes exist at the UI-SPEC coordinates with the existing controls shifted down and ClientSize grown; no ErrorProvider/warning control was added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Load/Save wiring + live-apply Action in SettingsForm and Program.cs</name>
  <files>src/RigToggle.App/SettingsForm.cs, src/RigToggle.App/Program.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.cs (constructor lines 50-79 and the `_tryRegisterConfiguredHotkey` field line 19 — the injected-delegate DI shape to mirror; SettingsForm_Load line 88 `chkEnableDebugLogging.Checked = _settings.EnableDebugLogging;`; BtnSaveSettings_Click settingsToSave initializer lines 707-728 and the persist call `_settingsStore.Save(settingsToSave);` line 733)
    - src/RigToggle.App/Program.cs (SettingsFormFactory line 114; mainForm construction line 116)
    - src/RigToggle.App/MainForm.cs (the public `ApplyTrayVisibility()` added in Plan 11-02 — the target of the injected Action)
    - .planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-PATTERNS.md ("Save-flow integration point" — the exact DI shape and wiring site)
  </read_first>
  <action>
    In SettingsForm.cs, add a new constructor parameter `Action applyTrayVisibility` as the last parameter (after `Func<bool> tryRegisterConfiguredHotkey`), a matching `private readonly Action _applyTrayVisibility;` field, and assign it with the same `?? throw new ArgumentNullException(...)` null-guard the sibling delegates use. This mirrors the `_tryRegisterConfiguredHotkey` DI shape exactly so SettingsForm still takes no direct MainForm dependency.

    In `SettingsForm_Load`, after the existing `chkEnableDebugLogging.Checked = _settings.EnableDebugLogging;` line, add `chkCloseMinimizesToTray.Checked = _settings.CloseMinimizesToTray;` and `chkMinimizeToTray.Checked = _settings.MinimizeToTray;` (plain round-trip, no try/catch, no service call — the `chkEnableDebugLogging` behavior template, NOT the registry-backed `chkStartWithWindows` one).

    In `BtnSaveSettings_Click`, add `CloseMinimizesToTray = chkCloseMinimizesToTray.Checked,` and `MinimizeToTray = chkMinimizeToTray.Checked,` to the `settingsToSave` object initializer (alongside `EnableDebugLogging = chkEnableDebugLogging.Checked,`). Then, immediately after the existing `_settingsStore.Save(settingsToSave);` call (line 733) and before the autostart-registry block, invoke `_applyTrayVisibility();` so the tray icon's derived visibility updates live the moment settings persist — this must run regardless of whether the later hotkey-registration step keeps the dialog open (DialogResult.None) or closes it, because the settings are already saved at that point (D-08 live-update requirement).

    In Program.cs, update `SettingsFormFactory()` to pass `mainForm.ApplyTrayVisibility` as the new final argument to the `SettingsForm` constructor, mirroring how `mainForm.TryRegisterConfiguredHotkey` is already passed — the factory closure captures `mainForm` by reference (pre-declared `null!`), so the method group resolves correctly by the time Settings is opened.
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.App/RigToggle.App.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private readonly Action _applyTrayVisibility;" src/RigToggle.App/SettingsForm.cs` returns 1 and the constructor guards it with `ArgumentNullException`
    - `grep -c "chkCloseMinimizesToTray.Checked = _settings.CloseMinimizesToTray;" src/RigToggle.App/SettingsForm.cs` returns 1 and `grep -c "chkMinimizeToTray.Checked = _settings.MinimizeToTray;" src/RigToggle.App/SettingsForm.cs` returns 1
    - `grep -c "CloseMinimizesToTray = chkCloseMinimizesToTray.Checked," src/RigToggle.App/SettingsForm.cs` returns 1 and `grep -c "MinimizeToTray = chkMinimizeToTray.Checked," src/RigToggle.App/SettingsForm.cs` returns 1
    - `_applyTrayVisibility();` is invoked immediately after `_settingsStore.Save(settingsToSave);` (behavior assertion: it appears between the Save call and the autostart try-block, so it runs even if the later hotkey block sets DialogResult.None)
    - Neither checkbox has a try/catch, `errCloseMinimize`, `errMinimizeToTray`, or `_autostartConfigurator` call around it (`grep -c "chkCloseMinimizesToTray\|chkMinimizeToTray" src/RigToggle.App/SettingsForm.cs` lines are all plain assignments)
    - `grep -c "mainForm.ApplyTrayVisibility" src/RigToggle.App/Program.cs` returns 1
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` and `dotnet build` of the solution succeed
  </acceptance_criteria>
  <done>Both checkboxes round-trip through AppSettings on Load/Save with no error path; saving Settings invokes the injected Action that live-applies tray visibility; Program.cs supplies mainForm.ApplyTrayVisibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ SettingsForm controls | Local modal dialog editing the user's own settings.json; no network/IPC. The new injected Action calls back into MainForm (same process, same thread) only to recompute a local UI property. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | injected _applyTrayVisibility Action | mitigate | Constructor null-guards the Action (`ArgumentNullException`) exactly like the existing `_tryRegisterConfiguredHotkey`, so a mis-wired composition root fails fast at construction rather than NRE-ing mid-Save. SettingsForm holds no direct MainForm reference (delegate-only), preserving the established anti-coupling boundary. |
| T-11-07 | Repudiation | checkbox Save with no error path | accept | Both preferences are pure in-memory bool writes with no I/O failure mode (unlike autostart/hotkey), so no inline error surface is warranted — omitting ErrorProvider is a deliberate UI-SPEC decision, not a swallowed failure. |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs occur in this phase — no new dependency added to any project. |

Risk is low/none: a local Settings dialog editing two boolean preferences and calling one same-process helper; no external, network, auth, or privilege boundary is crossed.
</threat_model>

<verification>
- `dotnet build` of the full solution succeeds after both tasks.
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` still passes (no regression to existing suites).
- Live-on-Save tray-icon appearance/disappearance and checkbox behavior are confirmed in the 11-04 human-verify checkpoint (WinForms interaction, not unit-testable here).
</verification>

<success_criteria>
- Settings shows the two new checkboxes with the locked wording, grouped directly above "Start with Windows".
- Load populates each from its AppSettings field; Save writes each back with no error/confirmation path.
- Saving Settings applies the derived tray-icon visibility immediately via `mainForm.ApplyTrayVisibility`.
</success_criteria>

<output>
Create `.planning/phases/11-configurable-tray-close-minimize-behavior-user-selectable-op/11-03-SUMMARY.md` when done.
</output>
