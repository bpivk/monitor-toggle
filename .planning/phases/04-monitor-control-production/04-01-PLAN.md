---
phase: 04-monitor-control-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/MonitorDetachSpike/Program.cs
  - spike/PHASE4-RETEST.md
autonomous: false
requirements: [DISPLAY-01, DISPLAY-02]
must_haves:
  truths:
    - "Running the extended spike's --disable-primary mode on the rig removes the PRIMARY monitor's path without throwing PathChangeException (resolves the spike's original Finding 3)"
    - "After repositioning-aware removal, the disabled monitor is genuinely absent from PathInfo.GetActivePaths() and exactly one surviving path sits at (0,0)"
    - "The CCD-disabled monitor remains discoverable via PathInfo.GetAllPaths() (confirms Assumption A2's restore-discovery premise on this AMD/DisplayPort hardware)"
    - "Restoring the original in-memory topology returns the display layout to its prior state"
  artifacts:
    - path: "spike/MonitorDetachSpike/Program.cs"
      provides: "--disable-primary <index> mode implementing Pattern 1 repositioning-aware primary removal"
      contains: "disable-primary"
    - path: "spike/PHASE4-RETEST.md"
      provides: "Go/no-go capture template for the Phase 4 repositioning re-test (A1/A2)"
      contains: "GO"
  key_links:
    - from: "spike/MonitorDetachSpike/Program.cs"
      to: "WindowsDisplayAPI.DisplayConfig.PathInfo.ApplyPathInfos"
      via: "reconstructed survivor PathInfo[] with shifted positions"
      pattern: "new PathInfo\\("
---

<objective>
De-risk the single highest-priority technical assumption of this phase (RESEARCH Assumption A1) BEFORE any production controller code is written: that constructing repositioning-aware survivor `PathInfo` objects lets `ApplyPathInfos` remove the GDI-primary monitor's path where the Phase 1 spike's naive removal threw `PathChangeException` twice. This mirrors Phase 1's "de-risk the core mechanism first" philosophy.

Extend the existing throwaway spike tool with a `--disable-primary` mode that implements RESEARCH Pattern 1 (shift every survivor by a delta so exactly one lands at `(0,0)`, then apply the reduced+repositioned topology). The USER runs it on the rig and records GO/NO-GO. A GO unblocks the production `Disable`/`Restore` implementation (Plan 03) to use this exact mechanism; a NO-GO redirects Plan 03 to the raw P/Invoke fallback documented in STACK.md.

Purpose: Empirically confirm the repositioning fix on the real AMD Radeon/DisplayPort hardware, since no further static research can resolve it (RESEARCH Open Question 1).
Output: An extended spike tool + a captured re-test result (GO/NO-GO with evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitor-control-production/04-RESEARCH.md
@.planning/phases/04-monitor-control-production/04-CONTEXT.md
@spike/RESULTS-TEMPLATE.md

# The spike tool being extended — read in full; the new mode reuses RunList/RunDisable/VerifyOnce structure.
@spike/MonitorDetachSpike/Program.cs

<interfaces>
<!-- Key WindowsDisplayAPI members the harness uses. Already used in spike/Program.cs and WindowsMonitorController.cs — no exploration needed. -->
- PathInfo.GetActivePaths(virtualModeAware: false) -> PathInfo[]
- PathInfo.GetAllPaths(virtualModeAware: false) -> PathInfo[]   // includes inactive-but-connected paths after active ones
- PathInfo.ApplyPathInfos(PathInfo[] paths, bool allowChanges, bool saveToDatabase=false, bool forceModeEnumeration=false)
- new PathInfo(PathDisplaySource displaySource, Point position, Size resolution, DisplayConfigPixelFormat pixelFormat, PathTargetInfo[] pathTargetInfos)   // public constructor; Position has NO public setter
- PathInfo instance getters: .Position (System.Drawing.Point), .Resolution (Size), .PixelFormat, .DisplaySource, .TargetsInfo (PathTargetInfo[]), .IsGDIPrimary (bool)
- PathTargetInfo.DisplayTarget.DevicePath (string, stable per-monitor id), .FriendlyName
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repositioning-aware --disable-primary mode to the spike tool</name>
  <read_first>
    - spike/MonitorDetachSpike/Program.cs (the existing --list/--disable/--verify structure to extend)
    - .planning/phases/04-monitor-control-production/04-RESEARCH.md (Pattern 1, lines 167-217; Assumption A1/A2, lines 486-487; Open Question 1)
  </read_first>
  <files>spike/MonitorDetachSpike/Program.cs</files>
  <action>
    Add a new `--disable-primary <index>` command case alongside the existing `--list`/`--disable`/`--verify` switch, and a corresponding `RunDisablePrimary(string[] args)` static method. Implement RESEARCH Pattern 1 exactly:
    (1) Capture `originalActivePaths = PathInfo.GetActivePaths(virtualModeAware: false)` and validate `index` against its bounds using the same guard style as the existing `RunDisable`.
    (2) Select `targetPath = originalActivePaths[index]`; capture `targetDevicePath` from `targetPath.TargetsInfo.FirstOrDefault()?.DisplayTarget.DevicePath`.
    (3) Build `survivors = originalActivePaths.Where(p => p != targetPath).ToArray()`. If `survivors.Length == 0`, print an error and return 1 (cannot remove the only display).
    (4) If `targetPath.IsGDIPrimary`, compute `delta = new Point(-survivors[0].Position.X, -survivors[0].Position.Y)` and rebuild EVERY survivor via the public constructor `new PathInfo(p.DisplaySource, new Point(p.Position.X + delta.X, p.Position.Y + delta.Y), p.Resolution, p.PixelFormat, p.TargetsInfo)`. Apply the delta uniformly to all survivors, not just survivors[0] (Pitfall 1). If not primary, use `survivors` unchanged.
    (5) Call `PathInfo.ApplyPathInfos(rebuilt, allowChanges: true, saveToDatabase: false, forceModeEnumeration: false)` inside a try/catch that prints the full exception type + message on `PathChangeException` (so the rig operator sees whether A1 held).
    (6) Verify via a fresh `PathInfo.GetActivePaths()`: print whether `targetDevicePath` is still active and whether `GetActivePaths().Count(p => p.IsGDIPrimary) == 1`. Do NOT use Screen.AllScreens as the pass/fail oracle here (D-04) — you may print its count as informational only.
    (7) A2 probe: call `PathInfo.GetAllPaths(virtualModeAware: false)` and print whether `targetDevicePath` is still discoverable there (this confirms the disabled monitor can be re-found at restore time).
    (8) Prompt "Press Enter to RESTORE", then `PathInfo.ApplyPathInfos(originalActivePaths, allowChanges: true)` and print restored active-path count. Reuse the existing `RunDisable`'s Console prompt/ReadLine restore idiom.
    Update `PrintUsage()` to list the new command. Keep this a throwaway diagnostic — no production abstractions, no new files.
  </action>
  <verify>
    <automated>grep -q "disable-primary" spike/MonitorDetachSpike/Program.cs && grep -q "GetAllPaths" spike/MonitorDetachSpike/Program.cs && grep -q "new PathInfo(" spike/MonitorDetachSpike/Program.cs</automated>
  </verify>
  <acceptance_criteria>
    - `spike/MonitorDetachSpike/Program.cs` contains a `--disable-primary` switch case and a `RunDisablePrimary` method.
    - The method constructs survivor paths via `new PathInfo(...)` (Position has no setter) and shifts ALL survivors by a single computed delta.
    - `ApplyPathInfos` is wrapped in try/catch that surfaces `PathChangeException` type + message rather than crashing silently.
    - Verification reads `PathInfo.GetActivePaths()` (not Screen.AllScreens) for the pass/fail decision, and additionally probes `PathInfo.GetAllPaths()` for the removed target.
    - `PrintUsage()` documents the new command.
  </acceptance_criteria>
  <done>The spike tool exposes a repositioning-aware primary-removal mode with active-path verification and a GetAllPaths re-discovery probe, ready to run on the rig.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 4 re-test capture template</name>
  <read_first>
    - spike/RESULTS-TEMPLATE.md (the Phase 1 go/no-go capture format to mirror)
    - .planning/phases/04-monitor-control-production/04-RESEARCH.md (Assumption A1/A2, Open Questions 1 and 2)
  </read_first>
  <files>spike/PHASE4-RETEST.md</files>
  <action>
    Create `spike/PHASE4-RETEST.md` as a fill-in results template mirroring `spike/RESULTS-TEMPLATE.md`'s structure. Include: a one-line GO/NO-GO decision field; build-and-run instructions (`dotnet run --project spike/MonitorDetachSpike -- --list` then `-- --disable-primary <index-of-primary>`); a results table with rows for "PathChangeException thrown? (Y/N)", "Target absent from GetActivePaths? (Y/N)", "Exactly one primary at (0,0) after? (Y/N)", "Target still discoverable via GetAllPaths (A2)? (Y/N)", "Restore returned prior layout? (Y/N)"; and a "Decision" section stating that GO unblocks Plan 03's `WindowsMonitorController.Disable`/`Restore` using RESEARCH Pattern 1 as-is, while NO-GO redirects Plan 03 to the raw P/Invoke `SetDisplayConfig` fallback documented in `.planning/research/STACK.md`'s Alternatives table. Reference RESEARCH Open Question 1 and Assumption A1/A2 explicitly.
  </action>
  <verify>
    <automated>test -f spike/PHASE4-RETEST.md && grep -qi "GO" spike/PHASE4-RETEST.md && grep -q "disable-primary" spike/PHASE4-RETEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `spike/PHASE4-RETEST.md` exists with a GO/NO-GO decision field.
    - Contains build/run instructions referencing `--disable-primary`.
    - Contains a results table covering PathChangeException, GetActivePaths absence, single-primary-at-(0,0), GetAllPaths re-discovery (A2), and restore.
    - States the GO→Pattern-1 / NO-GO→P/Invoke-fallback branch for Plan 03.
  </acceptance_criteria>
  <done>A capture template exists for the rig operator to record the re-test outcome and the resulting Plan 03 direction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rig re-test of repositioning-aware primary removal (A1/A2)</name>
  <what-built>An extended spike tool (`--disable-primary`) implementing repositioning-aware primary-monitor removal (RESEARCH Pattern 1), plus a `spike/PHASE4-RETEST.md` capture template. This sandbox cannot build or run Windows/net10.0-windows code — the rig test is the only way to resolve Assumption A1/A2.</what-built>
  <how-to-verify>
    On the rig PC (Windows, .NET 10 SDK installed):
    1. `dotnet run --project spike/MonitorDetachSpike -- --list` — note the index of the PRIMARY display (IsGDIPrimary=True).
    2. `dotnet run --project spike/MonitorDetachSpike -- --disable-primary <that-index>`.
    3. Confirm: NO `PathChangeException` is thrown; output reports the target device path is absent from GetActivePaths; output reports exactly one primary at (0,0); output reports whether the target is still discoverable via GetAllPaths (A2).
    4. Visually confirm the primary monitor genuinely went dark/disconnected (not just powered off) and the remaining monitor became primary.
    5. Press Enter to restore; confirm the original layout returns.
    6. Record all answers in `spike/PHASE4-RETEST.md` and set the GO/NO-GO decision.
  </how-to-verify>
  <resume-signal>Type "GO" (repositioning works — Plan 03 uses Pattern 1 as-is) or "NO-GO: <what failed>" (Plan 03 must use the P/Invoke fallback). Paste the filled results table if possible.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → spike CLI | The rig operator passes a display index; the tool mutates live display topology. Single local interactive user, no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `ApplyPathInfos` primary removal | mitigate | Wrap the mutating call in try/catch and re-query `GetActivePaths()` as the authoritative oracle before declaring success; a restore path (Enter → re-apply original topology) is always offered so a bad topology is reversible. |
| T-04-01-02 | Denial of Service | invalid/out-of-range index removing the only display | mitigate | Bounds-check the index (existing `RunDisable` guard style) and refuse when `survivors.Length == 0`. |
| T-04-01-03 | Elevation of Privilege | spike execution level | accept | Tool is non-elevated by construction (asInvoker, no manifest) — Phase 1 confirmed the CCD mechanism needs no elevation; no change here. |

Note: This is a throwaway single-user local desktop diagnostic with no network-facing surface, no persisted secrets, and no multi-user concept — most ASVS categories are not applicable (RESEARCH Security Domain).
</threat_model>

<verification>
- Static: both grep gates in Task 1/2 pass.
- Hardware (rig, blocking): the human-verify checkpoint records a GO or NO-GO in `spike/PHASE4-RETEST.md`.
</verification>

<success_criteria>
- The spike tool has a working `--disable-primary` mode implementing Pattern 1.
- `spike/PHASE4-RETEST.md` records an explicit GO/NO-GO with evidence from the rig.
- Downstream Plan 03 knows whether to use Pattern 1 as-is (GO) or the P/Invoke fallback (NO-GO).
</success_criteria>

<output>
Create `.planning/phases/04-monitor-control-production/04-01-SUMMARY.md` when done. Record the GO/NO-GO outcome prominently so Plan 03 can read it.
</output>
