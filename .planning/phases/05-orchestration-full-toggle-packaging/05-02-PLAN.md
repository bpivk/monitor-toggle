---
phase: 05-orchestration-full-toggle-packaging
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/RigToggle.App/MainForm.cs
autonomous: false
requirements: [CORE-04, CORE-01, CORE-02]

must_haves:
  truths:
    - "When a toggle reports failure (result.Success == false), MainForm shows a per-step checklist MessageBox (Monitor / Audio / App with OK / FAILED (reason) / not attempted) (D-01)"
    - "A fully-successful toggle shows no dialog — only RefreshUi() runs, matching today's silent-success behavior (D-01)"
    - "RefreshUi() still runs after every toggle attempt (success or partial failure), so the mode indicator reflects any partial state change (D-10 — the mode label is the crash/partial-state recovery signal, no separate startup dialog is added)"
    - "Preflight guards (unconfigured settings redirect, monitor-confirm dialog cancel) and the generic catch block for InvalidOperationException remain unchanged"
    - "dotnet build and dotnet test pass on the Windows rig (human-verified)"
  artifacts:
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "ToggleResult-consuming checklist rendering in BtnToggle_Click"
      contains: "ToggleResult"
  key_links:
    - from: "src/RigToggle.App/MainForm.cs"
      to: "ToggleService.ToggleToRigMode / ToggleToNormalMode"
      via: "captured ToggleResult return value"
      pattern: "= _toggleService.ToggleTo(Rig|Normal)Mode\\(\\)"
    - from: "src/RigToggle.App/MainForm.cs"
      to: "result.Success"
      via: "branch to checklist MessageBox vs silent RefreshUi"
      pattern: "result\\.Success"
---

<objective>
Wire the CORE-04 structured result into the UI: MainForm.BtnToggle_Click now captures the ToggleResult returned by both toggle directions and, on partial failure, renders a per-step checklist MessageBox in the existing house tone; on full success it stays silent as today. Then verify the whole solution builds and all tests pass on the Windows rig (this Linux sandbox cannot build net10.0-windows).

Purpose: The structured result from Plan 01 is only observable to the user through this rendering. This closes the CORE-04 loop ("reports which steps succeeded/failed") and confirms CORE-01/CORE-02 (one-action toggle in both directions) still trigger correctly against the new return contract.

Output: Updated MainForm.cs and a human-verified green build + test run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-CONTEXT.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-PATTERNS.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-01-SUMMARY.md
@src/RigToggle.App/MainForm.cs

<interfaces>
<!-- The contract produced by Plan 01 that this plan consumes. -->
From src/RigToggle.Core/Models/ToggleResult.cs:
`public sealed record ToggleResult(IReadOnlyList<ToggleStepResult> Steps) { public bool Success => ...; }`
`public sealed record ToggleStepResult(string StepName, ToggleStepOutcome Outcome, string? Reason);`
`public enum ToggleStepOutcome { Succeeded, Failed, NotAttempted }`
ToggleService methods now: `ToggleResult ToggleToRigMode()`, `ToggleResult ToggleToNormalMode()`.

<!-- Existing MessageBox house conventions in this file (owner=this, title="Rig Toggle"). -->
Failure dialog today (MainForm.cs ~line 125): `MessageBox.Show(this, "...", "Rig Toggle", MessageBoxButtons.OK, MessageBoxIcon.Warning);`
Info dialog today (MainForm.cs ~line 79): same call with `MessageBoxIcon.Information`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the ToggleResult checklist in BtnToggle_Click</name>
  <files>src/RigToggle.App/MainForm.cs</files>
  <read_first>
    src/RigToggle.App/MainForm.cs,
    src/RigToggle.Core/Models/ToggleResult.cs,
    src/RigToggle.Core/Models/ToggleStepResult.cs,
    src/RigToggle.Core/Models/ToggleStepOutcome.cs,
    .planning/phases/05-orchestration-full-toggle-packaging/05-PATTERNS.md
  </read_first>
  <action>
    In BtnToggle_Click, change the two toggle call sites inside the try block to capture the return value: `var result = _toggleService.ToggleToNormalMode();` in the rig-mode branch and `var result = _toggleService.ToggleToRigMode();` in the normal-mode branch (after the unchanged preflight/confirm guards). Both the unconfigured-settings early-return and the monitor-confirm-dialog cancel path stay exactly as they are (they `return;` before reaching a toggle call, so no result exists in those paths).

    After the if/else, call `RefreshUi();` unconditionally (state may have partially changed even on failure — e.g. monitor disabled OK but audio failed), THEN, if `!result.Success`, build and show a checklist MessageBox. Because `result` is declared inside each branch, hoist a single `ToggleResult? result` declared before the if so both branches assign it and the post-block code can read it; guard the render with `if (result is not null && !result.Success)`.

    Add a small private helper (e.g. `static string FormatChecklist(ToggleResult result)`) that maps each ToggleStepResult to one line: Succeeded → `"{StepName}: OK"`; Failed → `"{StepName}: FAILED ({Reason})"`; NotAttempted → `"{StepName}: not attempted"`. Join lines with newlines. The MessageBox call must use the existing conventions: owner `this`, title `"Rig Toggle"`, `MessageBoxButtons.OK`, `MessageBoxIcon.Warning`, and a lead-in line consistent with the current failure dialog tone (e.g. "The toggle did not fully complete:" followed by the checklist). Do NOT show any dialog on success (keep today's silent-success behavior).

    Leave the outer `catch (Exception ex)` block that renders the generic InvalidOperationException message unchanged — it still handles preflight-guard throws and corrupted-snapshot throws (which are NOT part of ToggleResult per Plan 01). Update ONLY the stale inline comment in that catch that says "full per-step CORE-04 partial-failure reporting is out of scope until Phase 5" to reflect that per-step reporting now happens via the ToggleResult checklist above, and this catch remains the fallback for the exception-based preflight/corruption guards. Add `using RigToggle.Core.Models;` if the ToggleResult type is not otherwise resolvable.
  </action>
  <verify>
    <automated>grep -Eq '= _toggleService.ToggleToNormalMode\(\)' src/RigToggle.App/MainForm.cs && grep -Eq '= _toggleService.ToggleToRigMode\(\)' src/RigToggle.App/MainForm.cs && grep -q 'result.Success' src/RigToggle.App/MainForm.cs && grep -Eq 'FAILED|not attempted' src/RigToggle.App/MainForm.cs && ! grep -q 'out of scope until Phase 5' src/RigToggle.App/MainForm.cs && grep -c 'RefreshUi()' src/RigToggle.App/MainForm.cs</automated>
  </verify>
  <acceptance_criteria>
    - Both toggle calls capture their return into a `ToggleResult` variable; the render branch is guarded by `!result.Success`.
    - A checklist string is built mapping Succeeded/Failed/NotAttempted to "OK" / "FAILED ({Reason})" / "not attempted" lines.
    - The failure MessageBox uses owner `this`, title `"Rig Toggle"`, `MessageBoxButtons.OK`, `MessageBoxIcon.Warning`.
    - No dialog is shown when `result.Success` is true; `RefreshUi()` runs after every toggle attempt.
    - The unconfigured-settings redirect and monitor-confirm-cancel early returns are unchanged; the generic `catch (Exception ex)` block still exists and its stale "out of scope until Phase 5" comment is removed/updated.
  </acceptance_criteria>
  <done>MainForm renders the per-step checklist on partial failure and stays silent on success, consuming the Plan 01 ToggleResult contract while leaving the exception-based preflight/corruption handling intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify build + tests green on the Windows rig</name>
  <action>Human-verify checkpoint — no source edits. The user runs the build/test/round-trip steps below on the rig; execution pauses until they confirm or report failures.</action>
  <what-built>
    CORE-04 is now complete in source: ToggleService returns a per-step ToggleResult (Plan 01) and MainForm renders it as a checklist on partial failure (Task 1 above). No build/test could run in the Linux planning/execution sandbox (no .NET SDK, net10.0-windows target).
  </what-built>
  <how-to-verify>
    On the Windows rig, from the repo root:
    1. Run `dotnet build RigToggle.sln -c Release`. Expected: build succeeds with 0 errors (warnings OK).
    2. Run `dotnet test src/RigToggle.Tests` (or `dotnet test RigToggle.sln`). Expected: all tests pass, including the two new failure-path tests (monitor-disable-throws → NotAttempted rest; audio-restore-throws → still clears) and the unchanged preflight `Assert.Throws` test.
    3. Launch the app (`dotnet run --project src/RigToggle.App` or the built exe). With Settings fully configured, click the toggle to Rig Mode and back once — confirm a fully-successful toggle shows NO dialog and the mode label/button update correctly (CORE-01/CORE-02).
    4. (Optional but recommended) Force a partial failure if easy to induce (e.g. temporarily point the rig audio device at an unplugged endpoint) and confirm the checklist MessageBox lists per-step OK / FAILED (reason) / not attempted rather than a single generic error.
  </how-to-verify>
  <resume-signal>Type "approved" once build + tests are green and the toggle round-trip behaves as described, or paste the build/test errors or unexpected dialog behavior to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToggleService (Core) → MainForm (App) | ToggleResult and its verbatim Reason strings are rendered into a user-facing MessageBox. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Checklist MessageBox rendering `ToggleStepResult.Reason` verbatim (may contain local device names / file paths) | accept | Same posture as the existing catch-block dialog this replaces (D-13 / T-02-FAKEFAIL): single-user local tool, no second principal, real-error visibility is a diagnostic feature. No new data sink — the text was already surfaced pre-CORE-04. |
| T-05-04 | Denial of Service | Malformed/oversized Reason producing an unwieldy MessageBox | accept | Reason is an adapter exception `.Message` (bounded, developer-authored strings); at most three step lines are shown. Not an attacker-controlled input on a single-user desktop tool. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (no new NuGet packages — RESEARCH.md Package Legitimacy Audit N/A). Nothing to audit. |
</threat_model>

<verification>
- Static (this sandbox): grep gates confirm both toggle calls are captured, `result.Success` branches the render, checklist wording present, stale comment removed, RefreshUi retained.
- Human (Windows rig checkpoint): `dotnet build -c Release` + `dotnet test` green; successful round-trip shows no dialog; induced partial failure shows the per-step checklist.
</verification>

<success_criteria>
- CORE-04 user-visible reporting complete: partial failure → per-step checklist; success → silent.
- CORE-01/CORE-02 one-action toggle both directions confirmed still working against the new return contract.
- Build and full test suite green on the rig.
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-full-toggle-packaging/05-02-SUMMARY.md` when done.
</output>
