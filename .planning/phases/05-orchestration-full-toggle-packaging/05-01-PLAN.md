---
phase: 05-orchestration-full-toggle-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Core/Models/ToggleStepOutcome.cs
  - src/RigToggle.Core/Models/ToggleStepResult.cs
  - src/RigToggle.Core/Models/ToggleResult.cs
  - src/RigToggle.Core/ToggleService.cs
  - src/RigToggle.Tests/Doubles/FakeControllers.cs
  - src/RigToggle.Tests/ToggleServiceTests.cs
autonomous: true
requirements: [CORE-04, CORE-03]

must_haves:
  truths:
    - "ToggleToRigMode returns a ToggleResult whose Steps list the Monitor/Audio/App outcomes, stopping on the first failed step and marking later steps NotAttempted"
    - "ToggleToNormalMode returns a ToggleResult while preserving its existing isolate-and-continue control flow (monitor failure recorded, audio swallowed-and-recorded, minimize/clear skipped only when monitor restore failed)"
    - "The snapshot is still persisted before any mutation call (CORE-03 preserved)"
    - "Preflight guards (unconfigured settings, missing companion app path, corrupted snapshot) remain exception-based and are NOT represented as ToggleResult steps"
  artifacts:
    - path: "src/RigToggle.Core/Models/ToggleStepOutcome.cs"
      provides: "enum ToggleStepOutcome { Succeeded, Failed, NotAttempted }"
      contains: "enum ToggleStepOutcome"
    - path: "src/RigToggle.Core/Models/ToggleStepResult.cs"
      provides: "record (StepName, Outcome, Reason)"
      contains: "record ToggleStepResult"
    - path: "src/RigToggle.Core/Models/ToggleResult.cs"
      provides: "record wrapping IReadOnlyList<ToggleStepResult> with computed Success"
      contains: "record ToggleResult"
    - path: "src/RigToggle.Core/ToggleService.cs"
      provides: "both toggle methods returning ToggleResult"
      contains: "ToggleResult ToggleToRigMode"
    - path: "src/RigToggle.Tests/ToggleServiceTests.cs"
      provides: "tests asserting on ToggleResult contract + failure paths"
  key_links:
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "RigToggle.Core.Models.ToggleResult"
      via: "return value of ToggleToRigMode/ToggleToNormalMode"
      pattern: "ToggleResult ToggleTo(Rig|Normal)Mode"
    - from: "src/RigToggle.Tests/ToggleServiceTests.cs"
      to: "ToggleResult.Steps"
      via: "assertions on per-step Outcome"
      pattern: "ToggleStepOutcome\\.(Succeeded|Failed|NotAttempted)"
    - from: "src/RigToggle.Tests/Doubles/FakeControllers.cs"
      to: "FakeMonitorController.Disable"
      via: "throwOnDisable constructor flag"
      pattern: "throwOnDisable"
---

<objective>
Introduce the CORE-04 structured-result contract in RigToggle.Core: three small model types (ToggleStepOutcome / ToggleStepResult / ToggleResult), refactor ToggleService's two toggle methods from void-and-throw to returning a ToggleResult, and update the test project (a new throwOnDisable fake flag plus result-based assertions) to lock the new contract.

Purpose: CORE-04 requires the app to report which toggle steps succeeded/failed/were-not-attempted rather than throwing a single generic exception. This plan builds the reporting substrate in the business tier only — the MainForm checklist UI (Plan 02) and packaging (Plan 03) build on top of it.

Output: 3 new model files, a rewritten ToggleService, an extended FakeMonitorController, and updated + new unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-CONTEXT.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-RESEARCH.md
@.planning/phases/05-orchestration-full-toggle-packaging/05-PATTERNS.md

<interfaces>
<!-- House-style model shape the executor must mirror. Extracted from codebase. -->

From src/RigToggle.Core/Models/AudioRoleState.cs (nullable-field positional record precedent):
`public sealed record AudioRoleState(string? DeviceId, string? DeviceName);`

From src/RigToggle.Core/Models/StateSnapshot.cs (wrapping-record + decision-ID doc precedent):
`public sealed record StateSnapshot(MonitorState Monitor, AudioState Audio);`

From src/RigToggle.Core/Models/MonitorState.cs (IReadOnlyList used with NO explicit using — ImplicitUsings is enabled in RigToggle.Core.csproj, so ToggleResult needs no `using System.Linq;`/`using System.Collections.Generic;`):
`public sealed record MonitorState(IReadOnlyList<MonitorPathSnapshot> Paths, string TargetDevicePath);`

Adapter interface methods ToggleService already calls (signatures unchanged this phase):
- IMonitorController: `MonitorState CaptureState()`, `void Disable(string monitorDevicePath)`, `void Restore(MonitorState previousState)`
- IAudioController: `AudioState CaptureState()`, `void SetDefault(string deviceId)`, `void Restore(AudioState previousState)`
- IAppController: `void LaunchOrFocus(string companionAppPath)`, `void MinimizeIfRunning(string companionAppPath)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three structured-result model types</name>
  <files>
    src/RigToggle.Core/Models/ToggleStepOutcome.cs (new),
    src/RigToggle.Core/Models/ToggleStepResult.cs (new),
    src/RigToggle.Core/Models/ToggleResult.cs (new)
  </files>
  <read_first>
    src/RigToggle.Core/Models/AudioRoleState.cs,
    src/RigToggle.Core/Models/StateSnapshot.cs,
    src/RigToggle.Core/Models/MonitorState.cs,
    .planning/phases/05-orchestration-full-toggle-packaging/05-PATTERNS.md
  </read_first>
  <action>
    Create three files in src/RigToggle.Core/Models/, all with file-scoped `namespace RigToggle.Core.Models;` and each carrying a `<summary>` XML-doc in the established house style (explains WHY, references decision IDs by name).

    ToggleStepOutcome.cs: a public enum named `ToggleStepOutcome` with exactly three members in this order — `Succeeded`, `Failed`, `NotAttempted`. Doc must state that NotAttempted covers steps skipped because an earlier step in a stop-on-first-failure sequence (ToggleToRigMode, D-04) already failed.

    ToggleStepResult.cs: a `public sealed record ToggleStepResult(string StepName, ToggleStepOutcome Outcome, string? Reason);`. Doc must state Reason is null for Succeeded/NotAttempted and is populated with the underlying exception's Message for Failed, matching the existing "surface the real error" posture (D-13 / T-02-FAKEFAIL).

    ToggleResult.cs: a `public sealed record ToggleResult(IReadOnlyList<ToggleStepResult> Steps)` with a body containing a computed property `public bool Success => Steps.All(s => s.Outcome == ToggleStepOutcome.Succeeded);`. Doc must reference D-03 (single shared shape consumed identically by MainForm regardless of direction) AND state the result is scoped strictly to the three mutation steps (Monitor/Audio/App); preflight guards remain exception-based and are NOT represented here. Do NOT add explicit `using` directives — ImplicitUsings is enabled in RigToggle.Core.csproj (verified: MonitorState uses IReadOnlyList with no using).
  </action>
  <verify>
    <automated>grep -q 'enum ToggleStepOutcome' src/RigToggle.Core/Models/ToggleStepOutcome.cs && grep -q 'NotAttempted' src/RigToggle.Core/Models/ToggleStepOutcome.cs && grep -q 'record ToggleStepResult(string StepName, ToggleStepOutcome Outcome, string? Reason)' src/RigToggle.Core/Models/ToggleStepResult.cs && grep -q 'record ToggleResult(IReadOnlyList<ToggleStepResult> Steps)' src/RigToggle.Core/Models/ToggleResult.cs && grep -q 'Steps.All(s => s.Outcome == ToggleStepOutcome.Succeeded)' src/RigToggle.Core/Models/ToggleResult.cs</automated>
  </verify>
  <acceptance_criteria>
    - src/RigToggle.Core/Models/ToggleStepOutcome.cs declares `public enum ToggleStepOutcome` with members Succeeded, Failed, NotAttempted (in that order).
    - src/RigToggle.Core/Models/ToggleStepResult.cs declares `public sealed record ToggleStepResult(string StepName, ToggleStepOutcome Outcome, string? Reason)`.
    - src/RigToggle.Core/Models/ToggleResult.cs declares `public sealed record ToggleResult(IReadOnlyList<ToggleStepResult> Steps)` with a `public bool Success` computed property returning `Steps.All(s => s.Outcome == ToggleStepOutcome.Succeeded)`.
    - All three files use file-scoped `namespace RigToggle.Core.Models;` and carry a `<summary>` doc comment; none add a WindowsDisplayAPI/NAudio/WinForms reference.
    - No explicit `using System.Linq;` or `using System.Collections.Generic;` directive is added (ImplicitUsings covers them).
  </acceptance_criteria>
  <done>The three model types compile-shaped per house style and expose the exact identifiers Plan 02's MainForm and this plan's ToggleService/tests will consume.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ToggleService to return ToggleResult (stop-on-first-failure rig / isolate-and-continue normal)</name>
  <files>src/RigToggle.Core/ToggleService.cs</files>
  <read_first>
    src/RigToggle.Core/ToggleService.cs,
    src/RigToggle.Core/Models/ToggleResult.cs,
    src/RigToggle.Core/Models/ToggleStepResult.cs,
    src/RigToggle.Core/Models/ToggleStepOutcome.cs,
    .planning/phases/05-orchestration-full-toggle-packaging/05-PATTERNS.md
  </read_first>
  <action>
    Change both `public void ToggleToRigMode()` and `public void ToggleToNormalMode()` to `public ToggleResult ToggleToRigMode()` / `public ToggleResult ToggleToNormalMode()`. Use the literal step-name strings "Monitor", "Audio", "App" for the three mutation steps.

    ToggleToRigMode (stop-on-first-failure, D-04/D-06): KEEP the two preflight guards unchanged — the `IsFullyConfigured` check (throws InvalidOperationException) and the `File.Exists(settings.CompanionAppPath)` check (throws InvalidOperationException). KEEP the snapshot capture + `_snapshotStore.Save(...)` sequence unchanged and BEFORE any mutation (CORE-03) — the Save is NOT a ToggleResult step. Then run the three mutation calls in order — `_monitorController.Disable(settings.MonitorDevicePath!)` as "Monitor", `_audioController.SetDefault(settings.RigAudioDeviceId!)` as "Audio", `_appController.LaunchOrFocus(settings.CompanionAppPath!)` as "App" — each wrapped so that: on success append a ToggleStepResult(name, Succeeded, null); on the FIRST exception append ToggleStepResult(name, Failed, ex.Message), then append ToggleStepResult(NotAttempted, null) for every remaining not-yet-run step, then stop (do not run later steps, do not roll back, do not clear the snapshot). Return `new ToggleResult(steps)`. A small local helper or an explicit short-circuit flag is acceptable — do not throw on a mutation-step failure.

    ToggleToNormalMode (isolate-and-continue — PRESERVE EXISTING CONTROL FLOW EXACTLY, change only reporting): KEEP the corrupted-snapshot guard (`snapshot is null` while `wasInRigMode`) throwing InvalidOperationException — NOT a step. In the else branch, KEEP: monitor Restore in try/catch capturing `monitorFailure`; audio Restore in try/catch that swallows-and-Trace.WriteLines (gap-closure 03-04); the conditional that skips MinimizeIfRunning/Clear when monitor restore failed. Change the final `ExceptionDispatchInfo.Capture(monitorFailure).Throw()` re-throw into result-recording: record "Monitor" as Failed(monitorFailure.Message) or Succeeded; record "Audio" as Failed(ex.Message) [still after swallowing/tracing] or Succeeded; when monitorFailure is non-null, record "App" as NotAttempted and DO NOT clear the snapshot (matching existing skip); when monitor restore succeeded, run MinimizeIfRunning (guarded by the existing non-empty CompanionAppPath check) as "App" and then `_snapshotStore.Clear()`. Return `new ToggleResult(steps)`. Note: the empty-CompanionAppPath case where minimize is skipped should still record "App" (e.g. Succeeded/NotAttempted) so the checklist has three entries — pick and document the representation.

    Update the class-level `<summary>`: DELETE the now-stale final sentence "Partial-failure handling (CORE-04) is explicitly out of scope for this phase; the sequence below is linear and unconditional." Replace it with a description of the deliberate D-04/D-05 asymmetry — ToggleToRigMode is stop-on-first-failure (forward steps have real dependencies; no point switching audio if the monitor did not disable), ToggleToNormalMode is isolate-and-continue (existing since gap-closure 03-04) — and state explicitly that this asymmetry is intentional and must not be "fixed" into symmetry. Reference D-04/D-05 by name.
  </action>
  <verify>
    <automated>grep -q 'public ToggleResult ToggleToRigMode()' src/RigToggle.Core/ToggleService.cs && grep -q 'public ToggleResult ToggleToNormalMode()' src/RigToggle.Core/ToggleService.cs && ! grep -q 'public void ToggleTo' src/RigToggle.Core/ToggleService.cs && ! grep -q 'explicitly out of scope' src/RigToggle.Core/ToggleService.cs && grep -Eq 'D-04|D-05' src/RigToggle.Core/ToggleService.cs && grep -q 'ToggleStepOutcome.NotAttempted' src/RigToggle.Core/ToggleService.cs && awk '/public ToggleResult ToggleToRigMode/{r=1} r&&/_snapshotStore.Save/{s=NR} r&&/_monitorController.Disable/{d=NR} END{exit !(s>0 && d>0 && s<d)}' src/RigToggle.Core/ToggleService.cs</automated>
  </verify>
  <acceptance_criteria>
    - Both methods have return type `ToggleResult`; no `public void ToggleTo...` signature remains.
    - The stale "explicitly out of scope" sentence is gone; the class doc now references D-04 and D-05 and describes the stop-vs-isolate asymmetry as intentional.
    - `_snapshotStore.Save(...)` still appears before `_monitorController.Disable(...)` in ToggleToRigMode (awk source-order gate passes).
    - The two ToggleToRigMode preflight guards and the ToggleToNormalMode corrupted-snapshot guard still `throw new InvalidOperationException(...)` (not converted to steps).
    - ToggleToRigMode marks remaining steps `ToggleStepOutcome.NotAttempted` after a failed step and does not call later mutation methods once a step fails.
    - ToggleToNormalMode still swallows+Traces audio-restore failures and still skips MinimizeIfRunning/Clear when monitor restore threw; neither method re-throws on a mutation-step failure.
  </acceptance_criteria>
  <done>ToggleService returns per-step ToggleResult in both directions, preserves CORE-03 snapshot ordering and the existing normal-mode isolation semantics, and documents the deliberate asymmetry inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend FakeMonitorController and update tests for the ToggleResult contract</name>
  <files>
    src/RigToggle.Tests/Doubles/FakeControllers.cs,
    src/RigToggle.Tests/ToggleServiceTests.cs
  </files>
  <read_first>
    src/RigToggle.Tests/Doubles/FakeControllers.cs,
    src/RigToggle.Tests/ToggleServiceTests.cs,
    src/RigToggle.Core/ToggleService.cs,
    src/RigToggle.Core/Models/ToggleResult.cs
  </read_first>
  <behavior>
    - ToggleToRigMode on the happy path returns a ToggleResult with Success == true and three Succeeded steps (Monitor, Audio, App).
    - When the monitor Disable throws, ToggleToRigMode returns Success == false with Monitor=Failed (Reason contains the fake's message) and Audio + App = NotAttempted, and audio.SetDefault / app.LaunchOrFocus are NOT recorded in the call log.
    - ToggleToNormalMode on the happy path returns a ToggleResult with Success == true.
    - When audio Restore throws, ToggleToNormalMode still returns a ToggleResult (does not throw), Audio step is Failed, App/Clear still run, and IsInRigMode() flips to false (existing gap-closure 03-04 behavior, now asserted via the result).
    - The existing preflight-guard test (missing companion app path) still asserts `Assert.Throws<InvalidOperationException>` — unchanged, because preflight guards stay exception-based.
  </behavior>
  <action>
    In FakeControllers.cs, add a `throwOnDisable` capability to FakeMonitorController mirroring FakeAudioController's existing `throwOnRestore` pattern exactly: a constructor parameter `bool throwOnDisable = false` stored in a readonly field; in `Disable(...)`, append the existing `monitor.Disable:{path}` call-log entry FIRST, then if the flag is set throw `new InvalidOperationException("Fake monitor disable failure (simulated CCD/topology failure).")`. Do not change any other fake.

    In ToggleServiceTests.cs, update the CreateService helper to also accept and forward a `bool monitorThrowsOnDisable = false` into `new FakeMonitorController(callLog, throwOnDisable: monitorThrowsOnDisable)`. Update every existing test that calls `service.ToggleToRigMode()` / `service.ToggleToNormalMode()` in a happy-path assertion to capture the returned `ToggleResult` where it strengthens the assertion (the call-order and settings-passthrough tests can keep their call-log assertions and simply ignore the return value, but add a `ToggleResult` capture + `Assert.True(result.Success)` to `ToggleToRigMode_SetsIsInRigModeTrue` and to `ToggleToNormalMode_SetsIsInRigModeFalse_AndClearIsLastSnapshotInteraction`). Add two NEW tests: (a) `ToggleToRigMode_ReturnsFailedMonitorStep_AndNotAttemptedRest_WhenDisableThrows` using `monitorThrowsOnDisable: true` — asserts result.Success is false, the Monitor step Outcome is Failed with a non-null Reason containing "Fake monitor disable failure", the Audio and App steps are NotAttempted, and the call log does NOT contain `audio.SetDefault` or `app.LaunchOrFocus`; (b) `ToggleToNormalMode_ReturnsFailedAudioStep_ButStillClears_WhenAudioRestoreThrows` using `audioThrowsOnRestore: true` — asserts the returned ToggleResult's Audio step is Failed, IsInRigMode() is false, and the call log contains `app.MinimizeIfRunning` and `snapshot.Clear`. Leave `ToggleToRigMode_Throws_WhenCompanionAppPathDoesNotExist` asserting `Assert.Throws<InvalidOperationException>` unchanged. Use `RigToggle.Core.Models` types (add the using if not already present).
  </action>
  <verify>
    <automated>grep -q 'throwOnDisable' src/RigToggle.Tests/Doubles/FakeControllers.cs && grep -q 'Fake monitor disable failure' src/RigToggle.Tests/Doubles/FakeControllers.cs && grep -q 'monitorThrowsOnDisable' src/RigToggle.Tests/ToggleServiceTests.cs && grep -Eq 'ToggleStepOutcome\.(Failed|NotAttempted)' src/RigToggle.Tests/ToggleServiceTests.cs && grep -q 'Assert.Throws<InvalidOperationException>' src/RigToggle.Tests/ToggleServiceTests.cs</automated>
    <automated>MISSING — `dotnet test src/RigToggle.Tests` cannot run in this Linux sandbox (no .NET SDK / net10.0-windows). Real test-run verification is gated on the Plan 02 Windows human-verify checkpoint.</automated>
  </verify>
  <acceptance_criteria>
    - FakeMonitorController has a `throwOnDisable` constructor flag; when true, Disable() records its call-log entry then throws InvalidOperationException with message containing "Fake monitor disable failure".
    - CreateService forwards a `monitorThrowsOnDisable` parameter into FakeMonitorController.
    - A new test drives `monitorThrowsOnDisable: true` and asserts Monitor=Failed (Reason non-null), Audio+App=NotAttempted, and no `audio.SetDefault`/`app.LaunchOrFocus` in the call log.
    - A new test asserts the audio-restore-throws normal-mode path returns a ToggleResult (no throw) with Audio=Failed while still recording `app.MinimizeIfRunning` and `snapshot.Clear` and IsInRigMode()==false.
    - The preflight-missing-app-path test still uses `Assert.Throws<InvalidOperationException>`.
  </acceptance_criteria>
  <done>Tests compile-shaped against the ToggleResult contract, cover both new failure paths, and preserve the exception-based preflight-guard assertion; actual green-run deferred to the Windows checkpoint in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToggleService (Core) → MainForm (App) | ToggleResult crosses from the business tier to the UI tier; Reason strings carry adapter exception messages verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `ToggleStepResult.Reason` (verbatim exception `.Message`, e.g. local file paths/device names) | accept | Deliberate, pre-existing posture (D-13 / T-02-FAKEFAIL): this is a single-user local diagnostic tool with no second principal; surfacing the real error is more useful than hiding it. No new sink introduced — the same exception text was already shown in MainForm's catch block today. |
| T-05-02 | Tampering | Structured-result internal data (step names are compile-time constants, not user input) | accept | Step names ("Monitor"/"Audio"/"App") are hard-coded literals; no external input flows into ToggleResult construction, so there is no injection surface. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (RESEARCH.md "Package Legitimacy Audit: Not applicable this phase" — zero new NuGet packages; only hand-written C# types). Nothing to audit. |
</threat_model>

<verification>
- Static (this sandbox): grep gates in each task pass; both toggle methods return ToggleResult; no `void ToggleTo...` remains; the stale "out of scope" doc sentence is gone and D-04/D-05 asymmetry is documented; Save-before-Disable source order holds.
- Deferred to Windows (Plan 02 checkpoint): `dotnet build` of the solution and `dotnet test src/RigToggle.Tests` both succeed with the updated + new tests green.
</verification>

<success_criteria>
- CORE-04 reporting substrate exists: ToggleResult/ToggleStepResult/ToggleStepOutcome in RigToggle.Core.Models.
- ToggleToRigMode = stop-on-first-failure; ToggleToNormalMode = isolate-and-continue; both return ToggleResult; asymmetry documented inline (D-04/D-05).
- CORE-03 snapshot-before-mutation ordering preserved.
- Preflight guards remain exception-based.
- Tests cover happy path + monitor-disable-failure + audio-restore-failure via the new result contract.
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-full-toggle-packaging/05-01-SUMMARY.md` when done.
</output>
