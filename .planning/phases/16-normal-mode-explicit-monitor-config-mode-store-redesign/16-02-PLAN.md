---
phase: 16-normal-mode-explicit-monitor-config-mode-store-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Core/Models/AppSettings.cs
  - src/RigToggle.App/SettingsForm.Designer.cs
  - src/RigToggle.App/SettingsForm.cs
autonomous: true
requirements: [DISPLAY-09]

must_haves:
  truths:
    - "AppSettings carries two new nullable Normal-mode monitor fields (NormalMonitorsToDisable / NormalMonitorsToEnable) symmetric to the existing Rig-mode fields"
    - "Settings shows a second monitor grid labeled 'Normal Mode', stacked directly below the Rig grid, with 'Off (Normal)'/'On (Normal)' column headers and its own explanation label"
    - "Editing the Normal grid persists its selection into NormalMonitorsToDisable/NormalMonitorsToEnable on Save, preserving stale (unenumerated) entries the same way the Rig grid does"
    - "The Normal grid participates in the existing light/dark theming at both the Load and OnThemeChanged call sites"
    - "The three stale Rig-grid strings and one designer doc line that falsely claim Normal mode 'restores exactly as it was' are corrected to mode-agnostic wording"
    - "An all-empty Normal grid is a valid, saveable configuration (D-01: a monitor not listed is left untouched), unlike the Rig grid which requires at least one selection"
  artifacts:
    - path: "src/RigToggle.Core/Models/AppSettings.cs"
      provides: "NormalMonitorsToDisable / NormalMonitorsToEnable nullable list fields"
      contains: "NormalMonitorsToDisable"
    - path: "src/RigToggle.App/SettingsForm.Designer.cs"
      provides: "pnlMonitorNormal panel + dgvMonitorsNormal grid + labels, full downstream Y-reflow, corrected stale prose"
      contains: "dgvMonitorsNormal"
    - path: "src/RigToggle.App/SettingsForm.cs"
      provides: "PopulateMonitorGridNormal, Normal cell-changed handler, GetGridSelectionNormal, validation + save merge, theming"
      contains: "PopulateMonitorGridNormal"
  key_links:
    - from: "src/RigToggle.App/SettingsForm.cs"
      to: "src/RigToggle.Core/Models/AppSettings.cs"
      via: "Save merges Normal grid selection into NormalMonitorsToDisable/NormalMonitorsToEnable"
      pattern: "NormalMonitorsTo(Disable|Enable)"
    - from: "src/RigToggle.App/SettingsForm.cs"
      to: "src/RigToggle.App/SettingsForm.Designer.cs"
      via: "code-behind references dgvMonitorsNormal / colDisableNormal / colEnableNormal declared in the designer"
      pattern: "dgvMonitorsNormal"
    - from: "src/RigToggle.App/SettingsForm.cs"
      to: "src/RigToggle.App/ThemeApplier.cs"
      via: "ThemeApplier.ThemeMonitorGrid(dgvMonitorsNormal, IsDarkTheme) at Load + OnThemeChanged"
      pattern: "ThemeMonitorGrid\\(dgvMonitorsNormal"
---

<objective>
Deliver DISPLAY-09: give Normal mode its own explicitly configured monitor set. Add two nullable `NormalMonitorsToDisable`/`NormalMonitorsToEnable` fields to `AppSettings`, add a second "Normal Mode" monitor grid to `SettingsForm` stacked directly below the existing Rig grid (D-04/D-05), duplicate the grid population/validation/save logic for it, wire it into the existing theming, and correct the four now-false "always restored" strings (RESEARCH.md Pitfall 3) that this milestone makes obsolete.

Purpose: This is the user-facing configuration surface. It is intentionally independent of the mode-store rewrite (Plan 03) — it only touches `AppSettings` (which Plan 03 also reads) and the two `SettingsForm` files. Landing it in Wave 1 in parallel with Plan 01 keeps the critical-path logic rewrite (Plan 03) unblocked.

Output: Updated `AppSettings.cs`, `SettingsForm.Designer.cs`, and `SettingsForm.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-CONTEXT.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md

<interfaces>
<!-- Existing self-analogs to duplicate. The UI-SPEC coordinate + copy tables are LOCKED; use them verbatim. -->

AppSettings.cs (current Rig fields to mirror): public List<string>? MonitorsToDisable { get; set; } / MonitorsToEnable { get; set; }
  → add flat siblings: public List<string>? NormalMonitorsToDisable { get; set; } / NormalMonitorsToEnable { get; set; }
  DO NOT add any default-population/migration logic to JsonSettingsStore.Load() — correct diff to that file is EMPTY (Pitfall 5).

SettingsForm.Designer.cs Rig block to mirror: pnlMonitor(12,12 / 396,234), lblMonitorCaption(9,9),
  lblMonitorExplain(12,22 / 372,50), dgvMonitors(12,80 / 372,120), colMonitorName/colDisable/colEnable,
  lblMonitorWarning(12,206 / 372,20).

SettingsForm.cs self-analogs to duplicate (NOT share — per RESEARCH Recommended Structure):
  PopulateMonitorGrid, OnMonitorCellValueChanged (guarded by _updatingMonitorGridProgrammatically),
  GetGridSelection, ValidateSettingsForm, BtnSaveSettings_Click stale-preserving merge.
  Theming call sites: SettingsForm_Load (~line 182) and OnThemeChanged (~line 144).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Normal-mode fields to AppSettings and lay out the Normal grid + reflow in the designer</name>
  <files>src/RigToggle.Core/Models/AppSettings.cs, src/RigToggle.App/SettingsForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.Core/Models/AppSettings.cs (full file — the MonitorsToDisable/ToEnable fields to mirror, all-nullable convention)
    - src/RigToggle.App/SettingsForm.Designer.cs (the pnlMonitor/dgvMonitors block AND every control from pnlAudioDevices down, so their Y-coordinates can be re-flowed; also the ClientSize line)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (the exact pnlMonitorNormal coordinate table, the full +246px downstream reflow table, and the Copywriting Contract stale-prose fixes — LOCKED, use verbatim)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (SettingsForm.Designer.cs section)
  </read_first>
  <action>
    In AppSettings.cs add two flat sibling nullable properties directly after `MonitorsToEnable`: `public List<string>? NormalMonitorsToDisable { get; set; }` and `public List<string>? NormalMonitorsToEnable { get; set; }`. Match the existing nullable-list convention exactly. Add NO logic anywhere else in Core for these — they round-trip through System.Text.Json with zero code changes (Pitfall 5; JsonSettingsStore.cs must NOT be edited).

    In SettingsForm.Designer.cs add a new `pnlMonitorNormal` panel mirroring `pnlMonitor` exactly, at `Location(12,258)`, `Size(396,234)`, `BorderStyle.FixedSingle`, containing: `lblMonitorNormalCaption` (Text "Normal Mode", Location(9,9), AutoSize=true); `lblMonitorNormalExplain` (Location(12,22), Size(372,50), AutoSize=false, Text "Only controls what changes when switching TO Normal Mode. A monitor not listed here is left untouched."); `dgvMonitorsNormal` (Location(12,80), Size(372,120), with the IDENTICAL grid-config properties as dgvMonitors: AllowUserToAddRows=false, AllowUserToDeleteRows=false, AllowUserToResizeRows=false, AllowUserToResizeColumns=false, RowHeadersVisible=false, SelectionMode=CellSelect, MultiSelect=false, ColumnHeadersHeightSizeMode=DisableResizing, EditMode=EditOnEnter); columns `colMonitorNameNormal` (HeaderText "Monitor", ReadOnly=true, AutoSizeMode=Fill), `colDisableNormal` (HeaderText "Off (Normal)", Width=66, ToolTipText "Turns this monitor off when switching to Normal Mode."), `colEnableNormal` (HeaderText "On (Normal)", Width=66, ToolTipText "Turns this monitor on when switching to Normal Mode (for a monitor normally kept off, e.g. to save power)."); `lblMonitorNormalWarning` (Location(12,206), Size(372,20), Visible=false). Register all new fields, add them to `pnlMonitorNormal.Controls`, add `pnlMonitorNormal` to the form's Controls, and declare each as a private field. Do not set any `Font` property (inherit form default).

    Re-flow every control at or below `pnlAudioDevices` DOWN by exactly +246px on `Location.Y` only (X unchanged), using the UI-SPEC downstream table verbatim: pnlAudioDevices 258→504, pnlAppPath 402→648, chkEnableDebugLogging 484→730, lblHotkeyCaption 532→778, txtHotkey 529→775, lblHotkeyWarning 556→802, chkCloseMinimizesToTray 600→846, chkMinimizeToTray 632→878, chkStartWithWindows 664→910, lblAutostartWarning 688→934, btnSaveSettings 720→966, btnDiscardChanges 720→966. Change `SettingsForm.ClientSize` from `(420, 768)` to `(420, 1014)`.

    Correct the three stale Rig-grid strings in this same file (Pitfall 3, LOCKED in UI-SPEC): `lblMonitorExplain.Text` → "Only controls what changes when switching TO Rig Mode. A monitor not listed here is left untouched." (drop the "always restored exactly as it was before" clause); `colDisable.ToolTipText` → "Turns this monitor off when switching to Rig Mode." (drop "Restored automatically..."); `colEnable.ToolTipText` → "Turns this monitor on when switching to Rig Mode (for a monitor normally kept off, e.g. to save power)." (drop "Turned off again automatically...").
  </action>
  <verify>
    <automated>grep -q "NormalMonitorsToDisable" src/RigToggle.Core/Models/AppSettings.cs && grep -q "NormalMonitorsToEnable" src/RigToggle.Core/Models/AppSettings.cs && git -C /home/bpivk/moza diff --quiet src/RigToggle.Core/Persistence/JsonSettingsStore.cs && grep -q "dgvMonitorsNormal" src/RigToggle.App/SettingsForm.Designer.cs && grep -q "Off (Normal)" src/RigToggle.App/SettingsForm.Designer.cs && grep -q "ClientSize = new System.Drawing.Size(420, 1014)" src/RigToggle.App/SettingsForm.Designer.cs && ! grep -q "restored exactly as it was before" src/RigToggle.App/SettingsForm.Designer.cs && ! grep -q "Restored automatically when switching back" src/RigToggle.App/SettingsForm.Designer.cs</automated>
  </verify>
  <acceptance_criteria>
    - AppSettings.cs declares `NormalMonitorsToDisable` and `NormalMonitorsToEnable` as `List<string>?` properties
    - `git diff` shows JsonSettingsStore.cs unmodified (empty diff — Pitfall 5)
    - SettingsForm.Designer.cs declares pnlMonitorNormal at (12,258) size (396,234), dgvMonitorsNormal at (12,80) size (372,120), and columns with headers "Off (Normal)"/"On (Normal)"
    - Every downstream control is at its new +246 Y per the UI-SPEC table; `ClientSize` is `(420, 1014)`
    - No occurrence of "restored exactly as it was before" or "Restored automatically when switching back" remains in the file
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` compiles once Task 2's code-behind lands (designer alone may reference handler names — acceptable if declared; otherwise defer the build check to Task 2's verify)
  </acceptance_criteria>
  <done>The data model has the two Normal fields, the Settings dialog has a fully laid-out, correctly-positioned "Normal Mode" grid, downstream controls are re-flowed without overlap, and the stale Rig-grid prose is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Duplicate grid population, validation, save-merge, and theming for the Normal grid</name>
  <files>src/RigToggle.App/SettingsForm.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.cs (PopulateMonitorGrid, OnMonitorCellValueChanged + the _updatingMonitorGridProgrammatically field, GetGridSelection, ValidateSettingsForm, BtnSaveSettings_Click merge block, SettingsForm_Load, OnThemeChanged)
    - src/RigToggle.App/SettingsForm.Designer.cs (the dgvMonitorsNormal / colDisableNormal / colEnableNormal / lblMonitorNormalWarning names Task 1 created)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (SettingsForm.cs section — the exact self-analog code blocks and the D-01 validation divergence note)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (Interaction Contract theming requirement + Interaction States Reference table)
  </read_first>
  <action>
    Add `PopulateMonitorGridNormal()` mirroring `PopulateMonitorGrid()` but: reuse the already-populated `_allMonitors` list (call `PopulateMonitorGrid()` first, then this — do NOT call `GetAllMonitors()` a second time), read `_settings.NormalMonitorsToDisable`/`NormalMonitorsToEnable` into the disable/enable HashSets, and write rows/Tag to `dgvMonitorsNormal` with the `lblMonitorNormalWarning` empty-state handling. Wire it at the same place `PopulateMonitorGrid()` is called during load.

    Add a `private bool _updatingMonitorGridNormalProgrammatically;` field and an `OnMonitorNormalCellValueChanged` handler mirroring `OnMonitorCellValueChanged` exactly (row-header guard `e.RowIndex < 0`; reentrancy-guarded sibling-uncheck using the new flag against `colDisableNormal.Index`/`colEnableNormal.Index`; call `ValidateSettingsForm()` at the end). Subscribe it to `dgvMonitorsNormal.CellValueChanged` (and unsubscribe/resubscribe around programmatic population like the Rig grid does).

    Add `GetGridSelectionNormal()` mirroring `GetGridSelection()` — same `row.Tag is string devicePath` keying, reading `colDisableNormal.Index`/`colEnableNormal.Index`, returning `(HashSet<string> Disable, HashSet<string> Enable)`.

    Extend `ValidateSettingsForm()`: compute a `monitorNormalOk` boolean from `GetGridSelectionNormal()` and fold it into the final `btnSaveSettings.Enabled` expression. CRITICAL D-01 divergence: the Normal grid must NOT block Save when both sets are empty (an all-empty Normal config is valid — a monitor not listed is left untouched). Do NOT add a `WouldLeaveAtLeastOneMonitorActive` cross-check for the Normal grid (RESEARCH Anti-Pattern: the zero-monitor safety guard stays apply-time-only in WindowsMonitorController). Treat `lblMonitorNormalWarning` as non-blocking advisory only. Effectively `monitorNormalOk` is true whenever the grid is enabled (or trivially true), so it never regresses Save-ability.

    Extend `BtnSaveSettings_Click`'s stale-preserving merge: mirror the Rig merge for the Normal sets — compute `staleDisableNormal`/`staleEnableNormal` from `_settings.NormalMonitorsToDisable`/`NormalMonitorsToEnable` filtered to paths NOT in `_allMonitors`, union with `GetGridSelectionNormal()`'s output, and assign `NormalMonitorsToDisable = mergedDisableNormal.ToList()` / `NormalMonitorsToEnable = mergedEnableNormal.ToList()` into the same `settingsToSave` AppSettings object that already carries the Rig fields.

    Theming (mandatory, UI-SPEC Interaction Contract): add `ThemeApplier.ThemeMonitorGrid(dgvMonitorsNormal, IsDarkTheme);` immediately after each existing `ThemeApplier.ThemeMonitorGrid(dgvMonitors, IsDarkTheme);` call — in `SettingsForm_Load` and in `OnThemeChanged`. Omitting either leaves the grid unthemed under dark mode.
  </action>
  <verify>
    <automated>grep -q "PopulateMonitorGridNormal" src/RigToggle.App/SettingsForm.cs && grep -q "_updatingMonitorGridNormalProgrammatically" src/RigToggle.App/SettingsForm.cs && grep -q "GetGridSelectionNormal" src/RigToggle.App/SettingsForm.cs && grep -q "NormalMonitorsToDisable = " src/RigToggle.App/SettingsForm.cs && [ "$(grep -c "ThemeMonitorGrid(dgvMonitorsNormal" src/RigToggle.App/SettingsForm.cs)" -ge 2 ] && dotnet build src/RigToggle.App/RigToggle.App.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `PopulateMonitorGridNormal` exists, reuses `_allMonitors` (no second `GetAllMonitors()` call), and reads the Normal fields
    - A dedicated `_updatingMonitorGridNormalProgrammatically` flag and `OnMonitorNormalCellValueChanged` handler exist (not the shared Rig flag)
    - `GetGridSelectionNormal` returns selection keyed by `row.Tag` device path
    - `BtnSaveSettings_Click` writes `NormalMonitorsToDisable`/`NormalMonitorsToEnable` with stale-entry preservation
    - `ThemeApplier.ThemeMonitorGrid(dgvMonitorsNormal, ...)` appears at least twice (Load + OnThemeChanged)
    - Save is NOT blocked by an all-empty Normal grid (no `WouldLeaveAtLeastOneMonitorActive` applied to the Normal grid)
    - `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0
  </acceptance_criteria>
  <done>The Normal grid populates, enforces the same one-per-row check-behavior, persists its selection with stale-preservation, is themed in both light/dark, and never blocks Save on an empty (valid) configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (grid) → AppSettings persistence | Monitor device paths chosen in the Normal grid are merged into settings.json |
| persisted settings → SettingsForm | Existing NormalMonitorsToDisable/ToEnable (possibly stale/unenumerated device paths) loaded back into the grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering / Information Disclosure | Stale device paths in NormalMonitorsToDisable across a monitor reconfigure | mitigate | The save-merge preserves stale (unenumerated) entries rather than silently dropping them, matching the Rig grid's proven behavior — a monitor temporarily disconnected at Settings-time is not lost from config. |
| T-16-04 | Denial of Service (self) | Zero-monitor Normal config | accept | Per D-01 + RESEARCH Anti-Pattern, no Settings-time zero-monitor block for the Normal grid; the apply-time guard in WindowsMonitorController.DeactivateMonitors (generalized in Plan 03) is the single enforcement point, avoiding a duplicate check that could drift. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet build src/RigToggle.App/RigToggle.App.csproj` exits 0.
- `git diff --quiet src/RigToggle.Core/Persistence/JsonSettingsStore.cs` (the correct diff to that file is empty — Pitfall 5).
- `grep -rn "restored exactly as it was before\|Restored automatically when switching back\|Turned off again automatically when switching back" src/RigToggle.App/SettingsForm.Designer.cs` returns zero matches.
- Visual confirmation of the re-flowed dialog is deferred to Plan 05's rig checkpoint (overlap/clipping is not compiler-catchable — Pitfall 4).
</verification>

<success_criteria>
- `AppSettings` has the two Normal-mode fields; `JsonSettingsStore.cs` is untouched.
- Settings shows a themed "Normal Mode" grid below the Rig grid with correct headers/labels and no downstream overlap.
- Normal grid selection persists with stale-preservation; an empty Normal config is saveable.
- The four stale "always restored" strings are corrected to mode-agnostic wording.
</success_criteria>

<output>
Create `.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-02-SUMMARY.md` when done.
</output>
