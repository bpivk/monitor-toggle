---
phase: 16-normal-mode-explicit-monitor-config-mode-store-redesign
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - src/RigToggle.Core/ToggleService.cs
  - src/RigToggle.Core/ToggleOrchestrator.cs
  - src/RigToggle.Windows/WindowsMonitorController.cs
  - src/RigToggle.Tests/ToggleServiceTests.cs
  - src/RigToggle.Tests/ToggleOrchestratorTests.cs
autonomous: true
requirements: [DISPLAY-10, DISPLAY-11, DISPLAY-13]

must_haves:
  truths:
    - "Toggling to Normal mode applies settings.NormalMonitorsToDisable/ToEnable directly via ActivateMonitors(enable) then DeactivateMonitors(disable), never restoring a snapshot"
    - "Current mode is read from IModeStore, not snapshot-file presence; the mode flag is written only AFTER the Monitor step succeeds, in both toggle directions"
    - "A single shared ReconcileModeAfterMonitorFailure helper preserves the CR-01 recapture-and-compare safety net, called from BOTH ToggleToRigMode and ToggleToNormalMode failure paths"
    - "ToggleService no longer takes ISnapshotStore; it takes IModeStore instead"
    - "A toggle-in-progress marker is written at the start of every guarded toggle and cleared in the orchestrator's finally, surviving a real process kill"
    - "The zero-survivors safety-guard exception text is mode-agnostic (reachable now from both toggle directions)"
    - "IsModeKnown() (and a way to read the current mode) is exposed through ToggleOrchestrator for MainForm's guards"
    - "BOTH test files (ToggleServiceTests, ToggleOrchestratorTests) compile and pass against the new IModeStore-backed ToggleService ctor and the marker-aware ToggleOrchestrator ctor"
  artifacts:
    - path: "src/RigToggle.Core/ToggleService.cs"
      provides: "Rewritten ToggleToNormalMode Monitor step, IModeStore-backed mode read/write, shared reconcile helper, ISnapshotStore removed"
      contains: "NormalMonitorsToDisable"
    - path: "src/RigToggle.Core/ToggleOrchestrator.cs"
      provides: "Marker lifecycle around RunGuarded + IsModeKnown pass-through"
      contains: "ToggleInProgressMarker"
    - path: "src/RigToggle.Windows/WindowsMonitorController.cs"
      provides: "Mode-agnostic zero-survivors guard exception text"
      contains: "at least one active display must remain"
    - path: "src/RigToggle.Tests/ToggleServiceTests.cs"
      provides: "CreateService rewired to IModeStore/marker doubles; snapshot-specific assertions and null-guard test rewritten"
      contains: "InMemoryModeStore"
    - path: "src/RigToggle.Tests/ToggleOrchestratorTests.cs"
      provides: "CreateOrchestrator rewired to IModeStore + IToggleInProgressStore doubles and the new ctors; in-flight and Normal-toggle assertions updated to post-success mode-write timing and explicit monitor-set application"
      contains: "InMemoryToggleInProgressStore"
  key_links:
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "src/RigToggle.Core/Abstractions/IModeStore.cs"
      via: "constructor dependency; TryLoad on read, Save on confirmed success"
      pattern: "_modeStore\\.(TryLoad|Save)"
    - from: "src/RigToggle.Core/ToggleOrchestrator.cs"
      to: "src/RigToggle.Core/Abstractions/IToggleInProgressStore.cs"
      via: "Save(marker) in try, Clear() in finally"
      pattern: "_markerStore\\.(Save|Clear)"
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "src/RigToggle.Windows/WindowsMonitorController.cs"
      via: "ToggleToNormalMode now calls the same guarded DeactivateMonitors (second caller)"
      pattern: "DeactivateMonitors"
---

<objective>
Deliver the core logic rewrite: DISPLAY-10 (Normal mode applies its explicit monitor set instead of restoring a snapshot), DISPLAY-11 (mode tracked via `IModeStore`, not snapshot presence), and the orchestration half of DISPLAY-13 (the toggle-in-progress marker lifecycle). Rewrite `ToggleToNormalMode`'s Monitor step to mirror `ToggleToRigMode`, replace `IsInRigMode() == _snapshotStore.Exists()` with an `IModeStore`-backed read, extract the CR-01 safety net into a shared helper called from both directions, remove `ISnapshotStore` from `ToggleService`, add the marker lifecycle + `IsModeKnown` pass-through to `ToggleOrchestrator`, generalize the zero-survivors guard text, and rewire BOTH test files (`ToggleServiceTests` and `ToggleOrchestratorTests`) that construct the changed constructors.

Purpose: This is the phase's central, highest-risk change (RESEARCH: the whole phase's risk is concentrated in this rewrite's sequencing/timing). It depends on Plan 01's stores/types and Plan 02's `AppSettings` fields. The `ToggleService` constructor change forces `ToggleServiceTests` to be rewired in the same plan, and the additional `ToggleOrchestrator` constructor parameter plus the post-success mode-write timing force `ToggleOrchestratorTests` to be rewired here too — both live in the same test project and will not compile (or will assert against retired timing) otherwise, so both are included here rather than split off.

Output: Rewritten `ToggleService.cs`, extended `ToggleOrchestrator.cs`, one-line-generalized `WindowsMonitorController.cs`, and rewired `ToggleServiceTests.cs` + `ToggleOrchestratorTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-CONTEXT.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-01-SUMMARY.md
@.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + current shapes to rewrite. Use directly. -->

From Plan 01 (already landed): IModeStore { bool Exists(); ToggleMode? TryLoad(); void Save(ToggleMode mode); }
  IToggleInProgressStore { ToggleInProgressMarker? TryLoad(); void Save(marker); void Clear(); }
  ToggleMode { Normal, Rig }  ToggleInProgressMarker(ToggleMode TargetMode, DateTimeOffset StartedAtUtc)
  InMemoryModeStore(callLog) logs "mode.Save"; InMemoryToggleInProgressStore(callLog) logs "marker.Save"/"marker.Clear"

From Plan 02 (already landed): AppSettings.NormalMonitorsToDisable / NormalMonitorsToEnable (List<string>?)

Current ToggleService (to rewrite):
  ctor(ISettingsStore, ISnapshotStore, IMonitorController, IAudioController, IAppController)  ← drop ISnapshotStore, add IModeStore
  IsInRigMode() => _snapshotStore.Exists()  (line 456)
  ToggleToRigMode Monitor step (lines 87-137): ActivateMonitors(enableSet); DeactivateMonitors(disableSet); + CR-01 recapture-compare (110-134); _snapshotStore.Save pre-mutation at line 83
  ToggleToNormalMode (lines 299-450): ENTIRE control-flow spine is snapshot-null-branching — genuine rewrite
  MonitorStateUnchanged(before, after) helper (lines 228-237) — reuse verbatim
  Class doc comment (lines 1-24) documents retired D-14/D-02 asymmetry — must be replaced
  Static helpers TryExecuteStep / TryExecuteOptionalStep (lines 184-226) — reusable as-is

Current ToggleOrchestrator (to extend):
  ctor(ToggleService)  ← add IToggleInProgressStore (mandatory, null-guarded) as a second parameter
  RunGuarded(Func<ToggleResult> pipeline) with busy-flag CompareExchange + finally Volatile.Write (lines 56-77)
  ToggleToRigMode() => RunGuarded(_toggleService.ToggleToRigMode)  (line 46)
  ToggleToNormalMode() => RunGuarded(_toggleService.ToggleToNormalMode)  (line 48)
  IsInRigMode()/IsSettingsConfigured() no-guard pass-throughs (lines 50-54)

Current WindowsMonitorController guard (lines 295-309): throws InvalidOperationException(
  "Cannot disable all configured monitors — at least one active display must remain. Connect
   and enable another display before switching to Rig Mode.")  ← drop the Rig-specific trailer

Current ToggleOrchestratorTests (to rewire):
  CreateOrchestrator factory (lines 45-59): builds InMemorySnapshotStore, constructs
    ToggleService(settingsStore, snapshotStore, monitor, audioController, appController) and
    ToggleOrchestrator(toggleService) — BOTH ctor calls change.
  ToggleToNormalMode_Idle_DelegatesToToggleServiceAndReturnsItsResult (lines 73-85): asserts
    callLog contains "monitor.Restore" — that call no longer happens after the rewrite.
  IsInRigMode_And_IsSettingsConfigured_ArePassThroughs_CallableWhileToggleInFlight (lines 147-175):
    asserts IsInRigMode() is TRUE while DeactivateMonitors is blocked mid-mutation, on the retired
    pre-mutation-snapshot timing — invalidated by the post-success mode-write timing this plan introduces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ToggleService — IModeStore-backed mode, Normal Monitor step, shared CR-01 helper, drop ISnapshotStore</name>
  <files>src/RigToggle.Core/ToggleService.cs</files>
  <read_first>
    - src/RigToggle.Core/ToggleService.cs (full file — constructor, ToggleToRigMode Monitor step + CR-01 block, ToggleToNormalMode entire method, IsInRigMode, MonitorStateUnchanged, TryExecuteStep helpers, class doc comment)
    - src/RigToggle.Core/Abstractions/IModeStore.cs (Plan 01 contract)
    - src/RigToggle.Core/Models/AppSettings.cs (the NormalMonitorsToDisable/ToEnable fields from Plan 02)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md (Pattern 2 mode-write timing + ReconcileModeAfterMonitorFailure sketch, Pattern 4 ToggleToNormalMode rewrite, Pattern 5 dependency removal)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (ToggleService.cs section)
  </read_first>
  <action>
    Replace the `ISnapshotStore snapshotStore` constructor parameter and `_snapshotStore` field with `IModeStore modeStore` / `_modeStore`, keeping the identical `?? throw new ArgumentNullException(...)` null-guard idiom. Remove the `_snapshotStore.Save(new StateSnapshot(...))` pre-mutation call (line ~83) entirely — mode is now written after success, and snapshot data is no longer produced by ToggleService.

    Rewrite `IsInRigMode()` (line 456): expose the current mode via `_modeStore.TryLoad()`. Provide `public bool IsModeKnown() => _modeStore.TryLoad() is not null;` and keep `public bool IsInRigMode() => _modeStore.TryLoad() == ToggleMode.Rig;` as a thin convenience wrapper (minimizes MainForm call-site churn). Do NOT default a null (corrupted/missing) mode to Normal here — return the honest value; the startup dialog (Plan 04) and the MainForm guards handle unknown mode.

    Rewrite `ToggleToNormalMode()` to mirror `ToggleToRigMode`'s Monitor-step shape (Pattern 4): capture a pre-mutation `MonitorState` baseline; build `disableSet` from `settings.NormalMonitorsToDisable ?? new List<string>()` and `enableSet` from `settings.NormalMonitorsToEnable ?? new List<string>()`; run the Monitor step via `TryExecuteStep("Monitor", () => { _monitorController.ActivateMonitors(enableSet); _monitorController.DeactivateMonitors(disableSet); }, steps)` (Activate before Deactivate — 06-RESEARCH Pitfall 2 ordering; add the same ordering comment). Delete the entire snapshot-null-branch spine (the `if (snapshot is null) {...} else {...}` at lines ~308-412), the corrupted-snapshot throw (lines ~312-316), and the "never was in rig mode, no-op" branch (lines ~318-326) — that responsibility moves to Plan 04's startup mode-corruption check. Keep the Audio step exactly as-is (already Phase-15 symmetric, `SetDefault(settings.NormalAudioDeviceId)`) and the App minimize step exactly as-is (isolate-and-continue, CR-02). On Monitor success call `_modeStore.Save(ToggleMode.Normal)`; on Monitor failure call the shared reconcile helper (below), mark remaining steps NotAttempted, and return.

    Extract a `private void ReconcileModeAfterMonitorFailure(Models.MonitorState before)` helper (Pattern 2): `try { if (MonitorStateUnchanged(before, _monitorController.CaptureState())) return; /* partial mutation: leave mode flag at prior value — do NOT write a new mode */ } catch { /* recapture failed — leave mode as-is, same fail-safe posture as today's CR-01 catch */ }`. Call it from BOTH the rewritten `ToggleToNormalMode` failure path AND the existing `ToggleToRigMode` failure path (replacing that method's inline `try { if (MonitorStateUnchanged(...)) { _snapshotStore.Clear(); } } catch {}` block — since nothing is written until success now, there is no snapshot to clear; the helper simply refrains from writing a mode). Reuse `MonitorStateUnchanged` verbatim.

    In `ToggleToRigMode`, move the mode write to AFTER the Monitor step succeeds: `_modeStore.Save(ToggleMode.Rig)` on confirmed Monitor success (mirroring Normal). Ensure both directions write mode only post-success.

    Replace the stale class-level doc comment (lines 1-24) that describes the retired D-14 snapshot-presence mode derivation and the retired D-02 "enable-set always re-disabled, never snapshot-restored" asymmetry. New comment: state that mode is tracked via IModeStore, both directions apply explicit symmetric monitor sets (Rig: MonitorsToDisable/ToEnable; Normal: NormalMonitorsToDisable/ToEnable), and the mode flag is written only after a successful Monitor step. Remove all remaining references to ISnapshotStore/StateSnapshot from this file (do NOT delete WindowsMonitorController.Restore or the StateSnapshot model — those are Phase 18 scope; just stop referencing them here).
  </action>
  <verify>
    <automated>grep -q "IModeStore modeStore" src/RigToggle.Core/ToggleService.cs && grep -q "NormalMonitorsToDisable" src/RigToggle.Core/ToggleService.cs && grep -q "ReconcileModeAfterMonitorFailure" src/RigToggle.Core/ToggleService.cs && grep -q "_modeStore.Save(Models.ToggleMode.Normal)\|_modeStore.Save(ToggleMode.Normal)" src/RigToggle.Core/ToggleService.cs && grep -q "IsModeKnown" src/RigToggle.Core/ToggleService.cs && ! grep -q "_snapshotStore" src/RigToggle.Core/ToggleService.cs && dotnet build src/RigToggle.Core/RigToggle.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - ToggleService constructor takes `IModeStore` and NOT `ISnapshotStore`; `_snapshotStore` appears nowhere in the file
    - ToggleToNormalMode builds sets from `settings.NormalMonitorsToDisable`/`NormalMonitorsToEnable` and calls `ActivateMonitors` before `DeactivateMonitors`
    - The snapshot-null-branch spine, corrupted-snapshot throw, and never-was-in-rig no-op branch are removed from ToggleToNormalMode
    - `ReconcileModeAfterMonitorFailure(MonitorState)` exists and is called from BOTH toggle directions' failure paths
    - `_modeStore.Save(...)` is called only after a confirmed successful Monitor step, in both directions (no pre-mutation mode write)
    - `IsModeKnown()` exists; `IsInRigMode()` reads from `_modeStore`
    - The class doc comment no longer mentions snapshot-presence mode derivation or the D-02 asymmetry
    - `dotnet build src/RigToggle.Core/RigToggle.Core.csproj` exits 0
  </acceptance_criteria>
  <done>Mode is explicit and file-backed, Normal mode applies its own symmetric set, the CR-01 safety net is shared across both directions and mode writes only on success, and ISnapshotStore is gone from ToggleService.</done>
</task>

<task type="auto">
  <name>Task 2: Add marker lifecycle + IsModeKnown to ToggleOrchestrator and generalize the guard text</name>
  <files>src/RigToggle.Core/ToggleOrchestrator.cs, src/RigToggle.Windows/WindowsMonitorController.cs</files>
  <read_first>
    - src/RigToggle.Core/ToggleOrchestrator.cs (full file — RunGuarded busy-flag finally, the two toggle entry points, the pass-through block)
    - src/RigToggle.Core/Abstractions/IToggleInProgressStore.cs (Plan 01 contract)
    - src/RigToggle.Windows/WindowsMonitorController.cs (lines ~295-309, the zero-survivors guard exception)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-RESEARCH.md (ToggleOrchestrator marker sketch + the ToggleInProgressException naming caution)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-UI-SPEC.md (Copywriting Contract row for the WindowsMonitorController guard text)
  </read_first>
  <action>
    Add an `IToggleInProgressStore _markerStore` dependency to `ToggleOrchestrator` (constructor parameter with null-guard, mirroring the existing `_toggleService` injection). Change `RunGuarded` to `RunGuarded(ToggleMode targetMode, Func<ToggleResult> pipeline)`: as the FIRST statement inside the existing `try` (after the busy-flag CompareExchange), call `_markerStore.Save(new ToggleInProgressMarker(targetMode, DateTimeOffset.UtcNow));`; as the FIRST statement inside `finally` (before `Volatile.Write(ref _busy, 0)`), call `_markerStore.Clear();`. Add a comment noting the finally clears on any managed exception but deliberately NOT on a real process kill — that is exactly the DISPLAY-13 condition detected at next launch. Update the two entry points to pass the target mode: `ToggleToRigMode() => RunGuarded(ToggleMode.Rig, _toggleService.ToggleToRigMode)` and `ToggleToNormalMode() => RunGuarded(ToggleMode.Normal, _toggleService.ToggleToNormalMode)`. Add an `public bool IsModeKnown() => _toggleService.IsModeKnown();` pass-through in the same no-guard style as the existing `IsInRigMode()` pass-through. Add a doc comment distinguishing `IToggleInProgressStore`/`ToggleInProgressMarker` (disk crash marker) from the existing `ToggleInProgressException` (in-memory reentrancy guard) so the two are not conflated.

    In WindowsMonitorController.cs, change the zero-survivors guard exception message from "Cannot disable all configured monitors — at least one active display must remain. Connect and enable another display before switching to Rig Mode." to the mode-agnostic "Cannot disable all configured monitors — at least one active display must remain." (drop the Rig-specific trailer entirely — this guard is now reachable from ToggleToNormalMode too, the first second-caller). Leave the surrounding guard logic unchanged.
  </action>
  <verify>
    <automated>grep -q "RunGuarded(ToggleMode targetMode" src/RigToggle.Core/ToggleOrchestrator.cs && grep -q "_markerStore.Save(new ToggleInProgressMarker" src/RigToggle.Core/ToggleOrchestrator.cs && grep -q "_markerStore.Clear()" src/RigToggle.Core/ToggleOrchestrator.cs && grep -q "IsModeKnown" src/RigToggle.Core/ToggleOrchestrator.cs && grep -q "at least one active display must remain" src/RigToggle.Windows/WindowsMonitorController.cs && ! grep -q "before switching to Rig Mode" src/RigToggle.Windows/WindowsMonitorController.cs && dotnet build src/RigToggle.Core/RigToggle.Core.csproj && dotnet build src/RigToggle.Windows/RigToggle.Windows.csproj</automated>
  </verify>
  <acceptance_criteria>
    - ToggleOrchestrator takes an `IToggleInProgressStore` and `RunGuarded` has a `ToggleMode targetMode` parameter
    - The marker is Saved as the first action in the guarded try and Cleared as the first action in finally (before the busy reset)
    - Both toggle entry points pass `ToggleMode.Rig`/`ToggleMode.Normal`
    - `IsModeKnown()` pass-through exists in the no-guard style
    - WindowsMonitorController guard text is "Cannot disable all configured monitors — at least one active display must remain." with no "before switching to Rig Mode" trailer
    - `dotnet build` succeeds for both RigToggle.Core and RigToggle.Windows
  </acceptance_criteria>
  <done>Every guarded toggle records and clears a disk marker for crash detection, the orchestrator exposes mode-known state, and the shared safety-guard message is mode-agnostic for its new second caller.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire BOTH ToggleServiceTests AND ToggleOrchestratorTests to the new ctors/doubles and rewrite snapshot/timing-specific assertions</name>
  <files>src/RigToggle.Tests/ToggleServiceTests.cs, src/RigToggle.Tests/ToggleOrchestratorTests.cs</files>
  <read_first>
    - src/RigToggle.Tests/ToggleServiceTests.cs (full file — the CreateService factory ~lines 47-57, the tuple return type, all 23 test methods' destructuring, the standalone Constructor_Throws_WhenAnyDependencyIsNull test ~lines 376-400 that constructs ToggleService directly five times, and specifically the snapshot-assertion tests: ToggleToRigMode_SavesSnapshotBeforeAnyMutationCall, ToggleToNormalMode_SetsIsInRigModeFalse_AndClearIsLastSnapshotInteraction, ToggleToRigMode_KeepsSnapshot_WhenDisableThrowsAfterPartiallyMutating)
    - src/RigToggle.Tests/ToggleOrchestratorTests.cs (full file — the CreateOrchestrator factory ~lines 45-59 building InMemorySnapshotStore + the two ctor calls, ConfiguredSettings ~lines 28-38, ToggleToNormalMode_Idle_DelegatesToToggleServiceAndReturnsItsResult ~lines 73-85 asserting "monitor.Restore", and IsInRigMode_And_IsSettingsConfigured_ArePassThroughs_CallableWhileToggleInFlight ~lines 147-175)
    - src/RigToggle.Tests/Doubles/InMemoryStores.cs (the InMemoryModeStore/InMemoryToggleInProgressStore doubles from Plan 01 and their call-log strings)
    - src/RigToggle.Tests/Doubles/BlockingMonitorController.cs (confirms the orchestrator in-flight test blocks on DeactivateMonitors — i.e. the Monitor step has NOT completed while blocked, so mode is not yet written)
    - src/RigToggle.Core/ToggleService.cs (the just-rewritten constructor signature + mode-write timing + IsModeKnown/IsInRigMode)
    - src/RigToggle.Core/ToggleOrchestrator.cs (the just-added IToggleInProgressStore ctor parameter)
    - .planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-PATTERNS.md (ToggleServiceTests.cs section — the exact factory-rewiring and assertion-rewrite guidance)
  </read_first>
  <action>
    **ToggleServiceTests.cs:** Update the `CreateService(...)` factory: replace `var snapshotStore = new InMemorySnapshotStore(callLog);` with `var modeStore = new InMemoryModeStore(callLog);`, and construct the service with `modeStore` in place of `snapshotStore` per the new ctor order. Update the factory's return tuple to expose `InMemoryModeStore ModeStore` instead of `SnapshotStore` (rename the third element), and update every test method's destructuring accordingly (most use `var (service, callLog, _) = CreateService();` — those only need the type change to compile; the few that name the third element need the rename). Also swap the `snapshotStore` argument for `modeStore` in the standalone `Constructor_Throws_WhenAnyDependencyIsNull` test (~lines 376-400), which constructs `ToggleService` directly five times (not via the factory) to test the per-argument null guards — each of its five direct ctor calls must pass `modeStore` in the position the ctor now expects, so the null-guard-per-dependency coverage still maps to the new dependency set.

    Rewrite the assertions in the three snapshot-specific tests to the new mode semantics:
    - Any test asserting a pre-mutation snapshot Save must now assert the mode is written AFTER the Monitor step succeeds (mode.Save appears after the monitor-mutation call-log entries, not before). Rename such tests if their names reference "snapshot" (e.g. to reflect mode-write timing).
    - Any test asserting "Clear is the last snapshot interaction" on toggle-to-Normal must now assert `_modeStore` ends at `ToggleMode.Normal` after a successful toggle and that `IsInRigMode()` returns false.
    - The partial-mutation test (ToggleToRigMode_KeepsSnapshot_WhenDisableThrowsAfterPartiallyMutating) must now assert the shared reconcile helper leaves the mode flag UNCHANGED (no mode.Save recorded) when a partial mutation occurs — not that a snapshot is kept.
    Add a new test asserting the CR-01 reconcile path applies to ToggleToNormalMode too (a Normal-mode Monitor failure after partial mutation leaves the mode flag unchanged), since Normal can now hit the same guarded DeactivateMonitors. Keep all other tests behavior-equivalent — only their wiring/type changes.

    **ToggleOrchestratorTests.cs:** Rewire the `CreateOrchestrator(...)` factory (~lines 45-59) exactly as Task 3 rewires `CreateService`: replace `var snapshotStore = new InMemorySnapshotStore(callLog);` with `var modeStore = new InMemoryModeStore(callLog);`, add `var markerStore = new InMemoryToggleInProgressStore(callLog);`, and change the two constructor calls to `new ToggleService(settingsStore, modeStore, monitor, audioController, appController)` and `new ToggleOrchestrator(toggleService, markerStore)` matching the new ctor signatures from Tasks 1–2. (`InMemorySnapshotStore` must no longer appear in this file.)
    Rewrite the two assertion-level breaks introduced by the rewrite:
    - `ToggleToNormalMode_Idle_DelegatesToToggleServiceAndReturnsItsResult` (~lines 73-85): the `Assert.Contains(callLog, entry => entry.StartsWith("monitor.Restore"))` assertion is invalid — ToggleToNormalMode no longer calls `Restore`; it now applies the explicit set via `ActivateMonitors`/`DeactivateMonitors`. Add `NormalMonitorsToDisable`/`NormalMonitorsToEnable` to `ConfiguredSettings` (~lines 28-38) so the Normal Monitor step has a meaningful set to apply, then change the assertion to prove the delegated toggle ran the explicit-set Monitor step (assert a `monitor.ActivateMonitors`/`monitor.DeactivateMonitors` entry) and that `IsInRigMode()` returns false afterward. Keep the pre-step `orchestrator.ToggleToRigMode(); callLog.Clear();` setup.
    - `IsInRigMode_And_IsSettingsConfigured_ArePassThroughs_CallableWhileToggleInFlight` (~lines 147-175): the inline comment and `Assert.True(orchestrator.IsInRigMode())` assume the retired pre-mutation-snapshot timing. Because the blocking double blocks inside `DeactivateMonitors` (the Monitor step has NOT completed and mode is written only after Monitor success), while the toggle is in flight `IsInRigMode()` is now FALSE. Rewrite: replace the comment to state that mode is written only after the Monitor step succeeds, so mid-mutation the mode flag has NOT yet flipped; assert `Assert.False(orchestrator.IsInRigMode())` while blocked (proving the pass-through is still callable and returns the honest pre-success value) and keep `Assert.True(orchestrator.IsSettingsConfigured())`; then, after `releaseFirstCall.Set()` and awaiting `firstCallTask` to a successful result, assert `Assert.True(orchestrator.IsInRigMode())` to prove the mode flipped only on confirmed success. The pass-through remaining callable while a toggle is in flight (the actual D-04 property under test) is preserved.
  </action>
  <verify>
    <automated>grep -q "InMemoryModeStore" src/RigToggle.Tests/ToggleServiceTests.cs && ! grep -q "InMemorySnapshotStore" src/RigToggle.Tests/ToggleServiceTests.cs && grep -q "InMemoryModeStore" src/RigToggle.Tests/ToggleOrchestratorTests.cs && grep -q "InMemoryToggleInProgressStore" src/RigToggle.Tests/ToggleOrchestratorTests.cs && ! grep -q "InMemorySnapshotStore" src/RigToggle.Tests/ToggleOrchestratorTests.cs && ! grep -q "monitor.Restore" src/RigToggle.Tests/ToggleOrchestratorTests.cs && dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `CreateService` and `CreateOrchestrator` both wire `InMemoryModeStore` (and `CreateOrchestrator` also `InMemoryToggleInProgressStore`); `InMemorySnapshotStore` appears in neither test file
    - `Constructor_Throws_WhenAnyDependencyIsNull` passes `modeStore` in place of `snapshotStore` across all five direct ctor calls
    - The three previously snapshot-specific ToggleServiceTests assert mode-write-after-success / mode unchanged on partial mutation, not snapshot behavior
    - A test covers the reconcile path for ToggleToNormalMode (Normal-mode partial-mutation failure leaves mode unchanged)
    - ToggleOrchestratorTests no longer asserts `monitor.Restore`; the Normal-toggle test asserts the explicit-set Monitor step ran, and the in-flight test asserts `IsInRigMode()` is false mid-mutation and true only after confirmed success
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>Both test files compile and pass against the new IModeStore-backed ToggleService ctor and marker-aware ToggleOrchestrator ctor, with the CR-01 safety net verified in both toggle directions and the mode-write-only-after-success timing asserted at both the service and orchestrator level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted mode/marker → ToggleService/Orchestrator | mode.json / toggle-in-progress.json cross into the toggle-routing and crash-detection logic |
| configured monitor sets → CCD mutation | NormalMonitorsToDisable/ToEnable drive real ActivateMonitors/DeactivateMonitors calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering / Spoofing (state misrepresentation) | Mode flag vs. real display topology after a partial-mutation failure | mitigate | Shared `ReconcileModeAfterMonitorFailure` recaptures MonitorState and refrains from writing a new mode when the topology changed but doesn't match the target (CR-01 preserved), and writes the mode ONLY after a confirmed-successful Monitor step — the flag never claims a mode the display didn't actually reach. |
| T-16-06 | Denial of Service (self) | ToggleToNormalMode now reachable to the zero-survivors guard | mitigate | Both directions route through the single guarded `DeactivateMonitors`; the generalized guard rejects an all-disable Normal config at apply-time rather than blanking every display — one enforcement point, no duplicated/driftable check. |
| T-16-07 | Repudiation (crash left undetected) | toggle-in-progress marker lifecycle | mitigate | Marker written before the pipeline runs and cleared only in the managed `finally`; a real process kill leaves it behind for Plan 04's next-launch dialog — the app can no longer silently lose the fact that a toggle was interrupted. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase. Nothing to verify. |
</threat_model>

<verification>
- `dotnet build RigToggle.sln` exits 0 (Core + Windows + App all still compile — App still constructs the old ToggleService ctor until Plan 04, so this may fail App-tier; if so, scope the build to Core/Windows/Tests here and let Plan 04 restore the full-solution build).
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` exits 0.
- `grep -rn "_snapshotStore" src/RigToggle.Core/ToggleService.cs` returns zero matches.
- `grep -rn "before switching to Rig Mode" src/RigToggle.Windows/` returns zero matches.
- `grep -rn "InMemorySnapshotStore" src/RigToggle.Tests/ToggleServiceTests.cs src/RigToggle.Tests/ToggleOrchestratorTests.cs` returns zero matches.
</verification>

<success_criteria>
- Normal mode applies its explicit set via ActivateMonitors/DeactivateMonitors; no snapshot restore path remains in ToggleService.
- Mode is read/written through IModeStore, written only after a successful Monitor step, with the shared CR-01 reconcile helper protecting both directions.
- ISnapshotStore is removed from ToggleService's constructor.
- The orchestrator writes/clears a crash marker per toggle and exposes IsModeKnown.
- The zero-survivors guard text is mode-agnostic.
- Both ToggleServiceTests and ToggleOrchestratorTests compile and pass against the new wiring and post-success mode-write timing.
</success_criteria>

<output>
Create `.planning/phases/16-normal-mode-explicit-monitor-config-mode-store-redesign/16-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
