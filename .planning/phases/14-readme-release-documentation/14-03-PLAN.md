---
phase: 14-readme-release-documentation
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - README.md
  - docs/screenshots/.gitkeep
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03]
must_haves:
  truths:
    - "D-10: A repo visitor sees a feature overview covering toggle, tray residency/autostart, global hotkey, multi-monitor sets, live theme-following, and the redesigned icons"
    - "D-12: README includes a generic problem statement explaining why the tool exists, without naming specific games/apps"
    - "README documents both downloading the released .exe and building from source"
    - "README displays three live-endpoint badges: build status, license, latest release"
    - "D-06, D-07, D-08: README shows four screenshot placeholders using real markdown image syntax under docs/screenshots/"
    - "D-09: No animated GIF/demo clip is added — static screenshots only"
    - "D-11: README uses generic framing with no 'Moza'/'BeamNG' naming"
    - "D-13: README states Windows 10/11 x64 requirement and Windows-11-only visual-polish graceful degradation"
  artifacts:
    - path: "README.md"
      provides: "GitHub-ready README: badges, feature overview, problem statement, install/build, screenshots, system requirements"
      contains: "img.shields.io/github/v/release/bpivk/monitor-toggle"
    - path: "docs/screenshots/.gitkeep"
      provides: "Persists the docs/screenshots/ directory in git (target for user-supplied screenshots)"
      contains: ""
  key_links:
    - from: "README.md build-status badge"
      to: ".github/workflows/build.yml"
      via: "actions/workflows/build.yml/badge.svg (filename must match the workflow created in 14-01)"
      pattern: "actions/workflows/build.yml/badge.svg"
    - from: "README.md screenshot placeholders"
      to: "docs/screenshots/{main-normal,main-rig,settings,tray-menu}.png"
      via: "markdown image syntax pointing at not-yet-existing files"
      pattern: "docs/screenshots/main-normal.png"
---

<objective>
Rewrite the root README.md into a GitHub-ready document: three live badges, a generic feature overview + problem statement, download-and-build instructions, four screenshot placeholders, and a system-requirements note — then verify on the live public repo that badges render and the build workflow ran.

Purpose: Delivers DOCS-01 (screenshots/placeholders both modes), DOCS-02 (download released .exe + build from source), DOCS-03 (build/license/release badges), per CONTEXT.md D-06 through D-13. This plan is wave 2 because badge honesty depends on 14-01 (build.yml must exist and be named for the badge URL) and 14-02 (repo public + v1.0/v1.1 releases so license/release badges resolve — RESEARCH Pitfall 2).
Output: rewritten README.md, docs/screenshots/.gitkeep, and a human-confirmed honest-badge end state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-readme-release-documentation/14-CONTEXT.md
@.planning/phases/14-readme-release-documentation/14-RESEARCH.md
@.planning/phases/14-readme-release-documentation/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/screenshots/ placeholder directory</name>
  <files>docs/screenshots/.gitkeep</files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-CONTEXT.md (D-06/D-07/D-08 — four slots, real markdown image syntax, docs/screenshots/ location)
    - .planning/phases/14-readme-release-documentation/14-PATTERNS.md (docs/screenshots/ section, lines 187-197 — note git does not track empty dirs; use a .gitkeep placeholder)
  </read_first>
  <action>Create the directory `docs/screenshots/` and add an empty `docs/screenshots/.gitkeep` file so git persists the otherwise-empty directory (per 14-PATTERNS.md note that git will not track a truly empty dir). Do NOT create any .png files this phase — the four screenshots are user-supplied later (D-06/D-09: static screenshots only, no GIF). This directory is only the target the README image placeholders point at.</action>
  <verify>
    <automated>test -f docs/screenshots/.gitkeep && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/screenshots/.gitkeep` exists (directory is git-persistable)
    - No `.png` files were created under docs/screenshots/
  </acceptance_criteria>
  <done>docs/screenshots/ exists in the repo as the placeholder target for user screenshots.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md — badges, feature overview, install/build, screenshots, requirements</name>
  <files>README.md</files>
  <read_first>
    - README.md (repo root — current 29-line baseline; the two dotnet publish blocks + untrimmed/COM-interop rationale + output-path callout MUST be preserved verbatim)
    - .planning/phases/14-readme-release-documentation/14-PATTERNS.md (README section lines 21-76 — what to preserve verbatim vs. add; Shared Patterns lines 199-234 — badge URLs, publish command, action pins)
    - .planning/phases/14-readme-release-documentation/14-CONTEXT.md (D-06 through D-13 — the full content spec and the generic-framing constraint)
    - .planning/phases/13-tray-app-icon-redesign/13-CONTEXT.md (icon motif — so the feature overview/tray-menu placeholder reference the icons correctly)
    - .planning/phases/12-theme-infrastructure-live-theme-following/12-CONTEXT.md (theming/Mica/Win10-fallback — so the system-requirements note D-13 is accurate)
  </read_first>
  <action>Rewrite README.md in place with these elements (section ordering/headings are Claude's discretion per CONTEXT.md; keep the existing terse, no-marketing-fluff tone):

  1. BADGES (top of file, DOCS-03) — three live-endpoint badges, markdown image syntax, exactly: build status `https://github.com/bpivk/monitor-toggle/actions/workflows/build.yml/badge.svg` (filename must match the build.yml created in 14-01), license `https://img.shields.io/github/license/bpivk/monitor-toggle`, latest release `https://img.shields.io/github/v/release/bpivk/monitor-toggle`. Never hardcode a static "passing"/version badge (RESEARCH Anti-Pattern).

  2. PROBLEM STATEMENT (D-12) — generic: some games/apps misbehave when launched while Windows treats the target as a secondary display; this tool switches to a secondary display + launches a companion app and reliably restores prior state. Do NOT name "Moza" or "BeamNG.drive" (D-11 — deliberate divergence from PROJECT.md/CLAUDE.md; do not "correct" back to specific names).

  3. FEATURE OVERVIEW (D-10) — cover the full current surface: one-click toggle (disable primary monitor at OS level + switch default audio + launch/focus a companion app, restore exactly on toggle-back); tray residency + autostart; global hotkey trigger; multi-monitor sets; live light/dark theme-following; redesigned shape-distinct tray + exe icons. Generic framing throughout.

  4. SCREENSHOTS (DOCS-01, D-06/D-07/D-08) — four placeholders using REAL markdown image syntax (not bracketed TODO text) pointing at not-yet-existing files: `![MainForm — normal mode](docs/screenshots/main-normal.png)`, `![MainForm — rig mode](docs/screenshots/main-rig.png)`, `![Settings](docs/screenshots/settings.png)`, `![Tray menu](docs/screenshots/tray-menu.png)`. These render as broken-image icons until the user drops real files in — correct final syntax already in place.

  5. DOWNLOAD + BUILD (DOCS-02) — a "Download" subsection pointing users to the GitHub Releases page (link to `https://github.com/bpivk/monitor-toggle/releases/latest`) to get the released .exe (note: binaries are attached from v1.2 onward; v1.0/v1.1 are notes-only). Keep the existing "Build a standalone .exe" section: preserve BOTH `dotnet publish` code blocks verbatim (character-for-character from current README lines 13-15 and 20-22), the untrimmed/COM-interop/P-Invoke rationale paragraph, and the `src/RigToggle.App/bin/publish/win-x64/` output-path callout.

  6. SYSTEM REQUIREMENTS (D-13) — Windows 10/11 x64; note full visual polish (Mica backdrop / rounded corners) is Windows 11-only and gracefully degrades on Windows 10 (per Phase 12 THEME-06).

  Do not add a GIF/demo clip (D-09). Preserving the existing parenthetical requirement-ID citation convention (e.g. `(PACKAGING-01)`) is optional discretion.</action>
  <verify>
    <automated>grep -q "actions/workflows/build.yml/badge.svg" README.md && grep -q "img.shields.io/github/license/bpivk/monitor-toggle" README.md && grep -q "img.shields.io/github/v/release/bpivk/monitor-toggle" README.md && grep -q "docs/screenshots/main-normal.png" README.md && grep -q "docs/screenshots/main-rig.png" README.md && grep -q "docs/screenshots/settings.png" README.md && grep -q "docs/screenshots/tray-menu.png" README.md && grep -q "dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64" README.md && grep -q "releases" README.md && [ "$(grep -ci 'moza\|beamng' README.md)" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README contains all three badge URLs exactly as specified (build.yml badge.svg, shields.io license, shields.io v/release)
    - README contains all four `docs/screenshots/*.png` placeholders in markdown image syntax
    - README preserves the exact `dotnet publish ... -p:PublishProfile=win-x64` command and the untrimmed/COM-interop rationale
    - README links to the GitHub Releases page for the download flow
    - README contains a Windows 10/11 x64 system-requirements note mentioning Windows-11-only Mica/rounded-corners graceful degradation
    - `grep -ci 'moza\|beamng' README.md` returns `0` (generic framing per D-11)
  </acceptance_criteria>
  <done>Root README.md is GitHub-ready: honest badges, generic feature overview + problem statement, download+build instructions, four screenshot placeholders, and a system-requirements note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm honest-badge end state on the live public repo</name>
  <what-built>Repo flipped public (14-02), v1.0/v1.1 releases backfilled (14-02), build + release workflows added (14-01), and README rewritten with three live badges + four screenshot placeholders (this plan). Per RESEARCH Open Question #1, this phase's completion bar is "workflow files exist and are correct, and the build workflow triggers successfully on this phase's own commits" — the actual v1.2 tag push is deferred to milestone close, not done here.</what-built>
  <how-to-verify>
    After this phase's commits are pushed to GitHub `main`:
    1. Open https://github.com/bpivk/monitor-toggle in a normal (or logged-out/incognito) browser — confirm the repo is publicly viewable.
    2. Open the Actions tab — confirm the "Build" workflow ran on the pushed commit and finished green (this is the phase's completion bar for CI).
    3. On the repo home page, confirm all three README badges RENDER (not broken/red): Build = passing, License = MIT, Latest release = v1.1 (v1.1 is expected as latest; v1.2 is deferred to milestone close).
    4. Open the Releases tab — confirm v1.0 and v1.1 are listed with notes and no attached binary.
    5. Confirm the four screenshot placeholders appear as broken-image icons (expected — real screenshots are user-supplied later) and the README reads generically with no "Moza"/"BeamNG" naming.
  </how-to-verify>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Open Question #1 — completion bar; Pitfall 2 — badge ordering/render dependencies)
  </read_first>
  <acceptance_criteria>
    - Repo loads publicly for a logged-out visitor
    - "Build" workflow ran green on the pushed commit
    - All three badges render (build=passing, license=MIT, release=v1.1)
    - v1.0 and v1.1 show in Releases, notes-only
    - README is generic (no Moza/BeamNG) and shows four screenshot placeholders
  </acceptance_criteria>
  <resume-signal>Type "approved" once all five checks pass, or describe which badge/workflow/release did not render as expected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public README badge URLs → shields.io / GitHub badge endpoints | Badges are served by third-party/GitHub endpoints reading live repo API state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Spoofing / Tampering | Badge trust source | mitigate | All three badges use live GitHub/shields.io endpoints reading real repo state — never hardcoded static "passing"/version text (RESEARCH Anti-Pattern), so a badge cannot misrepresent build/license/release status |
| T-14-07 | Information Disclosure | Generic framing (D-11) | accept | README intentionally omits specific rig/game names on the public-facing doc; internal .planning/ docs still name them and are exposed by the public flip — accepted per D-03/D-11 (user's deliberate choice) |
</threat_model>

<verification>
- README contains all three badge URLs, four screenshot placeholders, preserved publish command, releases link, and requirements note
- `grep -ci 'moza\|beamng' README.md` == 0
- docs/screenshots/.gitkeep present
- Human checkpoint confirms live public repo renders badges + green build + backfilled releases
</verification>

<success_criteria>
- GitHub-ready README with honest build/license/release badges, generic feature overview, download+build docs, four screenshot placeholders, system-requirements note
- docs/screenshots/ persisted via .gitkeep
- Human-verified honest end state on the live public repo (badges render, build green, v1.0/v1.1 releases present)
</success_criteria>

<output>
Create `.planning/phases/14-readme-release-documentation/14-03-SUMMARY.md` when done
</output>
