---
phase: 14-readme-release-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .github/workflows/build.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [DOCS-02, DOCS-03]
must_haves:
  truths:
    - "D-01: A LICENSE file at repo root contains standard OSI MIT text (backs the license badge)"
    - "D-02: A build workflow runs dotnet build + dotnet test on windows-latest on push/PR to main"
    - "D-05: A release workflow, on a v* tag push, publishes the self-contained single-file exe and attaches it to the GitHub Release"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text GitHub's license-detector recognizes (license badge source)"
      contains: "MIT License"
    - path: ".github/workflows/build.yml"
      provides: "CI build+test pipeline; build-status badge source"
      contains: "runs-on: windows-latest"
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered publish + asset upload"
      contains: "softprops/action-gh-release@v3"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "src/RigToggle.App/RigToggle.App.csproj"
      via: "dotnet publish -p:PublishProfile=win-x64 (verbatim from README)"
      pattern: "dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64"
    - from: ".github/workflows/release.yml"
      to: "src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe"
      via: "softprops files: input matching pubxml PublishDir"
      pattern: "src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe"
---

<objective>
Create the three backing infrastructure files that make the README's badges honest (not decorative): an MIT LICENSE file, a CI build workflow, and a tag-triggered release workflow.

Purpose: DOCS-03 requires build-status and license badges that reflect real state; DOCS-02 requires a "download the released .exe" flow. Both depend on this infrastructure existing. Per CONTEXT.md D-01 (MIT LICENSE), D-02 (build workflow), D-05 (release workflow reusing the exact existing publish command).
Output: LICENSE, .github/workflows/build.yml, .github/workflows/release.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-readme-release-documentation/14-CONTEXT.md
@.planning/phases/14-readme-release-documentation/14-RESEARCH.md
@.planning/phases/14-readme-release-documentation/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file at repo root</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Code Examples — MIT LICENSE file, lines 248-272; Assumption A1 on holder name)
    - .planning/phases/14-readme-release-documentation/14-PATTERNS.md (LICENSE section, lines 152-183 — the verbatim OSI text to use)
  </read_first>
  <action>Create a file named exactly `LICENSE` (no extension) at the repo root containing the OSI-canonical MIT License text reproduced verbatim in 14-RESEARCH.md Code Examples / 14-PATTERNS.md LICENSE section. Use copyright line "Copyright (c) 2026 Blaz Pivk" (holder from git config user.name per Assumption A1; year 2026). Do NOT place it under docs/ — GitHub's license-detector and the shields.io license badge require the standard MIT text at repo root exactly as `LICENSE` (per RESEARCH Pitfall 2). Do not paraphrase, reflow, or abbreviate the license body.</action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Copyright (c) 2026 Blaz Pivk" LICENSE && grep -q "WITHOUT WARRANTY OF ANY KIND" LICENSE && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists at repo root (not docs/LICENSE)
    - File first line is `MIT License`
    - File contains `Copyright (c) 2026 Blaz Pivk`
    - File contains the standard warranty-disclaimer paragraph (`THE SOFTWARE IS PROVIDED "AS IS"` ... `WITHOUT WARRANTY OF ANY KIND`)
  </acceptance_criteria>
  <done>Repo root has a standard MIT LICENSE file GitHub can detect for the license badge.</done>
</task>

<task type="auto">
  <name>Task 2: Create build workflow (.github/workflows/build.yml)</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-PATTERNS.md (build.yml section, lines 79-106 — verified example + solution-level context confirming bare dotnet commands resolve from repo root)
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Pattern 1 example lines 145-164; Pitfall 1 confirming tests run headless on windows-latest)
  </read_first>
  <action>Create `.github/workflows/build.yml` matching the verified example in 14-PATTERNS.md build.yml section verbatim. Workflow `name: Build`; triggers on `push` to branch `main` and `pull_request` to branch `main`; single job `build` with `runs-on: windows-latest`; steps in order: `actions/checkout@v7`, `actions/setup-dotnet@v6` with `dotnet-version: '10.0.x'`, then `run: dotnet restore`, `run: dotnet build --no-restore -c Release`, `run: dotnet test --no-build -c Release`. Use bare dotnet commands with no project/sln path argument — repo root has RigToggle.sln so they resolve to the whole solution (both test projects run headless per RESEARCH Pitfall 1). MUST be `windows-latest`, never `ubuntu-latest` — this is a net10.0-windows WinForms/COM/P-Invoke project that will not compile on Linux. Pin actions to major-version tags `@v7`/`@v6` (not `@main`/`@latest`/SHA).</action>
  <verify>
    <automated>test -f .github/workflows/build.yml && grep -q "runs-on: windows-latest" .github/workflows/build.yml && grep -q "actions/checkout@v7" .github/workflows/build.yml && grep -q "actions/setup-dotnet@v6" .github/workflows/build.yml && grep -q "dotnet test" .github/workflows/build.yml && ! grep -q "ubuntu-latest" .github/workflows/build.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/build.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/build.yml` exists and is valid YAML (python yaml.safe_load succeeds)
    - Contains `runs-on: windows-latest`; contains NO `ubuntu-latest`
    - Uses `actions/checkout@v7` and `actions/setup-dotnet@v6` with `dotnet-version: '10.0.x'`
    - Runs `dotnet restore`, `dotnet build --no-restore -c Release`, and `dotnet test --no-build -c Release`
    - Triggers include `push` and `pull_request` on `main`
  </acceptance_criteria>
  <done>A build workflow exists that GitHub serves a badge for at actions/workflows/build.yml/badge.svg and that compiles+tests the solution on Windows.</done>
</task>

<task type="auto">
  <name>Task 3: Create release workflow (.github/workflows/release.yml)</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/14-readme-release-documentation/14-PATTERNS.md (release.yml section, lines 110-148 — verified example + "confirmed this pass directly from source" notes on publish command and output path)
    - .planning/phases/14-readme-release-documentation/14-RESEARCH.md (Code Examples full release workflow lines 287-317; Pitfall 4 publish path; Anti-Patterns on not shortening the publish command)
    - README.md (repo root — the single source of truth for the exact publish command to reuse)
    - src/RigToggle.App/RigToggle.App.csproj (confirms RuntimeIdentifier is set in the csproj PropertyGroup, so no redundant -r win-x64 flag is needed)
  </read_first>
  <action>Create `.github/workflows/release.yml` matching the verified example in 14-PATTERNS.md release.yml section verbatim. Workflow `name: Release`; trigger `on: push: tags: - 'v*'` only (tag push ONLY — never on every push, per RESEARCH Anti-Pattern); top-level `permissions: contents: write`; single job `release` with `runs-on: windows-latest`; steps: `actions/checkout@v7`, `actions/setup-dotnet@v6` with `dotnet-version: '10.0.x'`, a publish step running the command verbatim `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64` (do NOT add `-r win-x64` or `--self-contained` — the RID is already set in the csproj PropertyGroup; do not shorten, expand, or "improve" this command), then `softprops/action-gh-release@v3` with `files: src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe` (this exact path matches the pubxml PublishDir per RESEARCH Pitfall 4 — do NOT use the .NET SDK default publish path). Pin action tags `@v7`/`@v6`/`@v3`.</action>
  <verify>
    <automated>test -f .github/workflows/release.yml && grep -q "runs-on: windows-latest" .github/workflows/release.yml && grep -q "softprops/action-gh-release@v3" .github/workflows/release.yml && grep -q "contents: write" .github/workflows/release.yml && grep -q "src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe" .github/workflows/release.yml && grep -q "dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64" .github/workflows/release.yml && ! grep -q -- "-r win-x64" .github/workflows/release.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` exists and is valid YAML
    - Trigger is `on: push: tags: ['v*']` only (no branch push trigger)
    - Has top-level `permissions: contents: write`; `runs-on: windows-latest`
    - Publish step is the exact command `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -p:PublishProfile=win-x64` with no `-r win-x64` / `--self-contained` additions
    - `softprops/action-gh-release@v3` step has `files: src/RigToggle.App/bin/publish/win-x64/RigToggle.App.exe`
  </acceptance_criteria>
  <done>A tag-push release workflow exists that builds the self-contained exe via the project's canonical publish command and attaches it to the auto-created GitHub Release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → external Actions | Third-party Marketplace actions execute with repo token scope |
| Release job → GitHub Releases API | Workflow token writes release assets (`contents: write`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | build.yml / release.yml runner OS | mitigate | Pin `windows-latest`; reuse the csproj-canonical publish command verbatim so trimming/self-contained flags can't drift (CLAUDE.md hard constraint) |
| T-14-02 | Elevation of Privilege | release.yml token scope | mitigate | `permissions: contents: write` declared at workflow top level, scoped to the release job only; no `pull_request_target`/`workflow_run` triggers used |
| T-14-SC | Tampering | GitHub Actions supply chain (checkout, setup-dotnet, action-gh-release) | mitigate | All three audited OK in RESEARCH Package Legitimacy Audit (official org + long-established); pinned to major-version tags `@v7`/`@v6`/`@v3`. No npm/pip/cargo installs in this phase, so no `[ASSUMED]`/`[SUS]` package checkpoint applies. |
</threat_model>

<verification>
- LICENSE, build.yml, release.yml all present with content asserted above
- Both workflow YAMLs parse with python yaml.safe_load
- release.yml publish command is byte-identical to README's documented command; exe path matches pubxml PublishDir
- Neither workflow references ubuntu-latest
</verification>

<success_criteria>
- MIT LICENSE at repo root, GitHub-detectable
- build.yml (push/PR, windows-latest, build+test) and release.yml (v* tag, publish+attach exe) exist and are valid
- Release workflow reuses the exact existing publish command and correct output path
</success_criteria>

<output>
Create `.planning/phases/14-readme-release-documentation/14-01-SUMMARY.md` when done
</output>
