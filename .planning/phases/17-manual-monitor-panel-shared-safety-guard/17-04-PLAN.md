---
phase: 17-manual-monitor-panel-shared-safety-guard
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified: []
autonomous: false
requirements: [DISPLAY-12, PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05]

must_haves:
  truths:
    - "The full solution builds and the core test suite passes with no regressions after all Phase 17 changes"
    - "A static audit proves the zero-survivors rule is implemented in exactly one place in the codebase and that all three mutation paths (Rig toggle, Normal toggle, manual panel) reach it"
    - "On real rig hardware the panel lists every monitor with a correct status icon, and enabling/disabling one takes effect immediately"
    - "On real rig hardware the panel's list updates when a monitor is physically connected or disconnected while the panel is open"
    - "On real rig hardware the disable confirmation appears (or is correctly skipped per SkipMonitorConfirmation) and Identify places a correctly-sized numbered overlay on the correct physical screen"
    - "On real rig hardware, attempting to disable the last remaining enabled monitor is rejected with the same message from all three paths"
  artifacts:
    - path: ".planning/phases/17-manual-monitor-panel-shared-safety-guard/17-04-SUMMARY.md"
      provides: "Recorded build/test output, the DISPLAY-12 single-implementation audit result, and the rig verification verdict for all six phase success criteria"
  key_links:
    - from: "src/RigToggle.Core/ToggleService.cs"
      to: "IMonitorController.DeactivateMonitors"
      via: "both ToggleToRigMode and ToggleToNormalMode Monitor steps"
      pattern: "DeactivateMonitors"
    - from: "src/RigToggle.App/MonitorPanelForm.cs"
      to: "IMonitorController.DeactivateMonitors"
      via: "the third caller — same method, no local guard"
      pattern: "DeactivateMonitors"
---

<objective>
Close Phase 17 with a full-solution regression gate, a static DISPLAY-12 single-implementation audit, and a rig-hardware verification of all six phase success criteria.

Purpose: everything in Plans 01-03 that can be proven in this Linux dev environment has been. The remaining risks — CCD behavior, mixed-DPI overlay placement, real hotplug events, and true three-way guard equivalence — are only observable on the real rig. This plan separates what the machine can prove from what only the rig can.
Output: `17-04-SUMMARY.md` carrying the audit output and the rig verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-RESEARCH.md
@.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-UI-SPEC.md
@.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-01-SUMMARY.md
@.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-02-SUMMARY.md
@.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-03-SUMMARY.md

<phase_success_criteria>
From ROADMAP.md Phase 17 — every one must be addressed by Task 1 (static) or Task 2 (rig):
1. A new panel shows one row/tile per detected monitor with its on/off status shown via icon, not just text (PANEL-01)
2. User can enable/disable any individual monitor directly from the panel, independent of the Rig/Normal toggle action, and it takes effect immediately (PANEL-02)
3. The panel's monitor list and status update live if a monitor is connected or disconnected while the panel is open (PANEL-03)
4. Disabling a monitor from the panel is gated by the same SkipMonitorConfirmation setting as the Rig/Normal toggle (PANEL-04)
5. The panel includes an Identify action that briefly overlays a number on each physical screen (PANEL-05)
6. Attempting to disable the last remaining enabled monitor is rejected identically whether attempted via the Rig toggle, the Normal toggle, or the manual panel (DISPLAY-12)
</phase_success_criteria>

<environment_note>
This dev host is Linux. `dotnet build` works for the Windows-targeted projects only with `-p:EnableWindowsTargeting=true`. `RigToggle.Windows.Tests` builds but cannot execute here (`Microsoft.WindowsDesktop.App` runtime is not installed) — a documented, pre-existing environment limitation carried over from Phase 16, not a regression. Do not report it as a failure; report it as "built, not executed (dev-host limitation)".
</environment_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-solution regression gate and DISPLAY-12 single-implementation audit</name>
  <files>.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-04-SUMMARY.md</files>
  <read_first>
    - src/RigToggle.Windows/WindowsMonitorController.cs (the `DeactivateMonitors` zero-survivors guard — the one legitimate implementation site; note its exact message text and line region)
    - src/RigToggle.Core/ToggleService.cs (confirm both `ToggleToRigMode` and `ToggleToNormalMode` still reach `DeactivateMonitors` — these are callers 1 and 2)
    - src/RigToggle.App/MonitorPanelForm.cs (caller 3 — confirm it calls the same method and contains no local guard)
    - .planning/phases/17-manual-monitor-panel-shared-safety-guard/17-01-SUMMARY.md, 17-02-SUMMARY.md, 17-03-SUMMARY.md (deviations recorded by earlier plans that this gate must account for)
  </read_first>
  <action>
    Run the regression gate and the static DISPLAY-12 audit, and record verbatim command output in the summary.

    Regression gate:
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` — must report `0 Error(s)`. Record the warning count too; the 3 pre-existing `xUnit1031` warnings in `ToggleOrchestratorTests.cs` are the known baseline. If the warning count rose above baseline, investigate and report rather than ignoring it.
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` — must report `Failed: 0` with `Total: 84`.
    - `PATH="$HOME/.dotnet:$PATH" dotnet build src/RigToggle.Windows.Tests/RigToggle.Windows.Tests.csproj -p:EnableWindowsTargeting=true` — must build; record explicitly that it was not executed and why.

    DISPLAY-12 single-implementation audit — this is the phase's defining check, so run each part and record the actual output, not a summary judgement:
    - Exactly one implementation site: `grep -rn "at least one active display must remain" src --include=*.cs` returns exactly ONE line, in `src/RigToggle.Windows/WindowsMonitorController.cs`. Any second occurrence in a non-test `.cs` file is a phase failure.
    - Exactly three production call sites: `grep -rn "DeactivateMonitors(" src --include=*.cs | grep -v "/bin/" | grep -v "/obj/"` — the production hits must be the interface declaration (`IMonitorController.cs`), the implementation (`WindowsMonitorController.cs`), the two `ToggleService.cs` call sites, and the one `MonitorPanelForm.cs` call site. Test doubles and test files are expected additional hits; list them separately so the production set is unambiguous.
    - No panel-side re-derivation: in `src/RigToggle.App/MonitorPanelForm.cs`, with comment lines excluded, there must be zero matches for each of `Count\(.*IsActive`, `survivors`, and `at least one active display`. Use the form `grep -nE 'PATTERN' FILE | grep -vE '^[0-9]+:\s*(//|\*|/\*)' | wc -l` so a comment that legitimately mentions the rule does not fail the gate.
    - No toggle-pipeline coupling in the panel: the same comment-excluded form must return zero for `ToggleToRigMode|ToggleToNormalMode|ToggleResult|ToggleService|IModeStore` in `MonitorPanelForm.cs`.
    - No mode-config persistence from the panel: `grep -c 'MonitorsToDisable\|MonitorsToEnable\|NormalMonitorsToDisable\|NormalMonitorsToEnable' src/RigToggle.App/MonitorPanelForm.cs` outputs `0`.
    - No GDI-identity-space leakage: `grep -rn "Screen.AllScreens" src/RigToggle.App --include=*.cs` returns no hits in `MonitorPanelForm.cs` or `MonitorIdentifyOverlay.cs`.

    Also confirm no dependency drift: a `git diff --stat` over the phase's commits restricted to `'*.csproj'` shows no changes — Phase 17 adds no packages.

    Write `17-04-SUMMARY.md` with a `## Regression Gate` section (verbatim final lines of each command) and a `## DISPLAY-12 Audit` section (the actual grep output for each check, plus a PASS/FAIL verdict per check). Do not paraphrase command output.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true && PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` exits 0 and its output contains `0 Error(s)`.
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` output contains `Failed:     0` and `Total:    84`.
    - `grep -rc "at least one active display must remain" src --include=*.cs | grep -v ':0$'` lists exactly one file, and that file is `src/RigToggle.Windows/WindowsMonitorController.cs`.
    - `grep -rln "DeactivateMonitors(" src --include=*.cs | grep -v '/bin/' | grep -v '/obj/' | grep -c 'MonitorPanelForm.cs'` outputs `1` — the panel is a real third caller.
    - For `src/RigToggle.App/MonitorPanelForm.cs`, the comment-excluded match count is `0` for each of these patterns: `Count\(.*IsActive`, `survivors`, `at least one active display`, `ToggleToRigMode|ToggleToNormalMode|ToggleResult|ToggleService|IModeStore`.
    - `grep -c 'MonitorsToDisable\|MonitorsToEnable\|NormalMonitorsToDisable\|NormalMonitorsToEnable' src/RigToggle.App/MonitorPanelForm.cs` outputs `0`.
    - `grep -rn "Screen.AllScreens" src/RigToggle.App/MonitorPanelForm.cs src/RigToggle.App/MonitorIdentifyOverlay.cs | wc -l` outputs `0`.
    - `17-04-SUMMARY.md` exists and contains a `## Regression Gate` section and a `## DISPLAY-12 Audit` section, with actual command output pasted rather than summarized.
  </acceptance_criteria>
  <done>Solution builds clean, core suite is green at 84, and the static audit proves the zero-survivors rule has exactly one implementation reached by all three mutation paths, with no duplicate guard, no toggle-pipeline coupling, no mode-config writes, and no GDI screen-enumeration leakage in the new files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rig-hardware verification of all six Phase 17 success criteria</name>
  <files>.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/17-manual-monitor-panel-shared-safety-guard/17-RESEARCH.md (Pitfall 3 mixed-DPI overlay placement — the specific failure mode to look for in scenario 5)
    - .planning/phases/17-manual-monitor-panel-shared-safety-guard/17-UI-SPEC.md (the exact copy and colors to compare the running UI against)
  </read_first>
  <action>
    Present the six numbered rig scenarios below to the user verbatim, wait for their results, then record the outcome in `17-04-SUMMARY.md` under a `## Rig Verification` heading — one labeled entry per scenario with an explicit PASS / FAIL / NOT TESTED verdict, the user's observed behavior, and (for scenarios 5 and 6) the specific evidence values called out in the acceptance criteria.

    Do not run any of this yourself and do not infer a result from code reading: none of it is observable on this Linux dev host. Do not mark a scenario PASS on the basis of "the code looks correct."

    If the user reports a FAIL, do not attempt a fix inside this plan — write it up as a gap (observed vs. expected) so the operator can run gap closure. If the user chooses to waive a scenario instead of testing it, record it as a documented override naming who accepted it and when, in the same style Phase 16 used for DISPLAY-13 — never as a false "tested and passed".
  </action>
  <what-built>
    Phase 17 delivered: `MonitorPanelForm` (a non-modal `Rig Toggle — Monitors` window with one row per monitor, a green/red status dot, a per-row Disable/Enable button, and an Identify button), `MonitorIdentifyOverlay` (a full-screen numbered overlay per active monitor, auto-closing after 2.5 s), a `Monitors…` button on the main window plus a `Monitors` tray entry, and `ToggleOrchestrator.BeginExclusiveMonitorAccess()` serializing panel actions against Rig/Normal toggles.

    Everything provable without display hardware has been proved: the solution builds, 84 core tests pass, and the static audit confirms the zero-survivors guard has exactly one implementation reached by all three paths. The remaining risk is entirely CCD/display-hardware behavior.
  </what-built>
  <how-to-verify>
    Build and run on the rig: `dotnet publish src/RigToggle.App/RigToggle.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`, then launch the produced exe (or run from Visual Studio). Work through all six scenarios and record PASS/FAIL plus a note for each.

    **1. PANEL-01 — rows and status icon.** Open the panel from the main window's `Monitors…` button. Confirm: one row per monitor you expect (including any currently OS-disabled monitor); each row shows a colored dot (green = on, red = off), not only text; the name column shows the friendly name with ` (Primary)` on the primary and ` (currently OS-disabled)` on any disabled one; the window title reads exactly `Rig Toggle — Monitors`. Then close the panel, right-click the tray icon, and confirm a `Monitors` entry sits between `Settings` and the separator and opens the same window. With the main window minimized to tray, confirm the tray entry still opens the panel. Click `Monitors` twice in a row and confirm you get one focused window, not two.

    **2. PANEL-02 — immediate per-monitor effect.** With the panel open, click `Disable` on a non-primary monitor. Confirm the display actually goes dark/detached (not merely powered off — Windows Display Settings should no longer list it as an active display), the row's dot turns red, the row's button flips to `Enable`, and NO audio device change and NO Moza Companion launch/minimize occurred. Click `Enable` on that same row and confirm the display comes back. Confirm the main window's `Mode:` label did NOT change as a result of either action.

    **3. PANEL-03 — live hotplug update.** With the panel open and untouched, physically unplug a monitor (or disable it from Windows' own Display Settings). Confirm the panel's rows update on their own within a second or two, with no click in the panel. Plug it back in and confirm the panel updates again.

    **4. PANEL-04 — confirmation gate.** In Settings, ensure "don't ask again" has NOT been set (if it has, the confirmation is expected to be skipped — note which state you tested). With confirmation enabled, click `Disable` on a monitor and confirm a dialog titled `Confirm Monitor Changes?` appears naming that one monitor, with `Continue` / `Cancel` and a `Don't ask again` checkbox. Cancel it and confirm nothing changed. Then click `Enable` on an OS-disabled monitor and confirm NO dialog appears (Enable is intentionally never gated). Finally, tick `Don't ask again`, continue, and confirm a subsequent Disable proceeds without a dialog.

    **5. PANEL-05 — Identify overlay (highest-risk item).** Click `Identify`. Confirm: one numbered overlay appears per ACTIVE monitor (an OS-disabled monitor correctly gets none); each overlay covers its whole screen with no offset, no clipping, and no spill onto a neighboring screen; the numbers match the panel's row order top-to-bottom; all overlays disappear on their own after roughly 2.5 seconds; and clicking Identify did not steal focus in a way that broke the panel. **This is the mixed-DPI risk (17-RESEARCH.md Pitfall 3)** — if your monitors use different scaling factors, look specifically for an overlay that is offset or the wrong size, and report the per-monitor scaling percentages alongside the result.

    **6. DISPLAY-12 — identical rejection from all three paths.** Arrange for exactly one monitor to be active. Then attempt to disable it three ways and confirm all three produce the SAME message text, `Cannot disable all configured monitors — at least one active display must remain.`:
      (a) From the panel, click `Disable` on that last remaining monitor.
      (b) Configure Rig mode's monitor set so the disable-set covers every active monitor, then press `Switch to Rig Mode`.
      (c) Configure Normal mode's monitor set the same way and toggle to Normal.
    Note whether the panel shows it as a `Rig Toggle` MessageBox with a warning icon (expected). Confirm the wording is character-identical across all three — the toggle-flavored phrase "configured monitors" is deliberately kept in the panel context.

    **Bonus (concurrency, optional but valuable):** with the panel open, click `Disable` to bring up the confirmation dialog, and while that dialog is open press the global toggle hotkey. Expected: the toggle is rejected with `A toggle is already in progress. Wait for it to finish, then try again.` (as a tray balloon), and the panel's confirmation dialog is unaffected. Then dismiss the dialog and confirm the hotkey works normally again.
  </how-to-verify>
  <verify>
    <human-check>All six scenarios reported PASS, or any FAIL/partial recorded with enough detail (message text, monitor scaling factors, screenshots where visual) to plan a gap-closure fix.</human-check>
    <automated>test -f .planning/phases/17-manual-monitor-panel-shared-safety-guard/17-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-04-SUMMARY.md` contains a `## Rig Verification` section with one labeled entry per scenario 1-6, each carrying an explicit PASS / FAIL / NOT TESTED verdict — never an implied or blank result.
    - Scenario 5's entry records the per-monitor scaling percentages used during the test, so a future mixed-DPI regression is comparable against a known baseline.
    - Scenario 6's entry quotes the exact message text observed on each of the three paths, so "identical" is evidenced rather than asserted.
    - Any FAIL is written up with both the observed and the expected behavior, and surfaced to the operator as a gap rather than silently accepted.
    - Any waived scenario is recorded as a documented override naming who accepted it and when, matching Phase 16's DISPLAY-13 precedent.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six scenarios pass, or describe which scenario failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification claims → phase completion | An unverified or paraphrased result would let a broken CCD/DPI behavior ship as "passed" |
| Static audit → DISPLAY-12 guarantee | The audit is the only automated proof that the guard was not silently duplicated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Repudiation | Verification evidence recorded as a summary judgement rather than actual output, making a later regression untraceable | mitigate | Task 1 requires verbatim command output in the summary; Task 2 requires per-scenario verdicts, quoted message text, and recorded scaling factors. |
| T-17-18 | Denial of Service | A mixed-DPI overlay-placement defect ships and covers the wrong screen or fails to cover a screen fully | mitigate | Scenario 5 is a dedicated rig checkpoint with an explicit instruction to record scaling percentages and look for offset/clipping (17-RESEARCH.md Pitfall 3). |
| T-17-19 | Tampering | A duplicated zero-survivors check drifts from the adapter's topology-aware guard, producing inconsistent rejection across the three paths | mitigate | Task 1's single-implementation grep audit plus scenario 6's three-way message-equality check. |
| T-17-20 | Elevation of Privilege | A packaging change introducing an elevation manifest or IL trimming that breaks P/Invoke or COM interop | accept | This plan performs no packaging change; `RigToggle.App.csproj` remains untouched (no `PublishTrimmed`, no elevation manifest — see CLAUDE.md constraints). Exe-size work is Phase 18's scope. |
| T-17-SC | Tampering | Supply chain (package installs) | accept | No package references added anywhere in Phase 17; the `.csproj` no-drift check in Task 1 pins this. |
</threat_model>

<verification>
- Automated gate: solution build 0 errors, `RigToggle.Tests` 84/84, `RigToggle.Windows.Tests` builds (execution not possible on this dev host).
- Static gate: exactly one zero-survivors implementation, the expected set of production `DeactivateMonitors` call sites, and zero duplicate-guard / toggle-coupling / mode-write / screen-enumeration hits in the new App-tier files.
- Human gate: six rig scenarios covering PANEL-01..05 and DISPLAY-12, plus an optional concurrency spot-check.
</verification>

<success_criteria>
- All six ROADMAP Phase 17 success criteria have an explicit, evidenced verdict.
- DISPLAY-12's "one shared codepath, not three separate checks" is proven both statically (single implementation, three callers) and behaviorally (identical message from all three paths on the rig).
- No package, project-file, or packaging change was introduced by this phase.
</success_criteria>

<output>
Create `.planning/phases/17-manual-monitor-panel-shared-safety-guard/17-04-SUMMARY.md` when done, containing `## Regression Gate`, `## DISPLAY-12 Audit`, and `## Rig Verification` sections.
</output>
