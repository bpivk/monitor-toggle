---
phase: 25-single-instance-guard
plan: 04
type: execute
wave: 4
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - src/RigToggle.Core/SingleInstanceGuard.cs
  - src/RigToggle.Tests/SingleInstanceGuardTests.cs
  - src/RigToggle.App/UpdateApplyEntryPoint.cs
autonomous: false
gap_closure: true
requirements: [INSTANCE-01, INSTANCE-02, UPDATE-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A duplicate Rig Toggle launch whose primary died between `SingleInstanceGuard.Acquire()` and `MarkReady()` completes `WaitForInstanceReady` and returns `true` instead of terminating with an unhandled `AbandonedMutexException` — the CR-01 crash path is closed, and closed by the guard itself rather than by a process-wide exception handler."
    - "The abandoned wait releases the readiness mutex it was handed ownership of, rather than returning early and leaving it owned by a thread about to exit — so one abandoned primary cannot cascade the identical exception into the next waiter, and the next waiter after that."
    - "The regression test was observed RED against the unfixed code, with `AbandonedMutexException` named in its failure output, BEFORE the catch was added. The red output is recorded verbatim in the SUMMARY. A test that would have passed either way is not a regression net, and the whole point of this plan is the regression net."
    - "The abandoned-mutex regression test generates its own instance id locally, so the deliberately-unreleased guard it creates cannot leak into any other fact in `SingleInstanceGuardTests` no matter what order xUnit runs them in."
    - "Readiness-mutex construction failure inside `Acquire()` degrades to a logged, null readiness handle instead of propagating out of `Acquire()` and killing startup with zero diagnostic trail (WR-02). The never-crash-on-startup contract (T-25-06, Security Domain V5) now holds at BOTH of the guard's kernel-object call sites, not just the one it originally covered."
    - "The class doc comment no longer claims the guard is categorically free of abandoned-mutex risk. It names the one call site that genuinely carries that risk and states how the case is handled — the doc and the code agree again."
    - "The `Global\\`/`Local\\` cross-process namespace-divergence risk (WR-01) is recorded in the source as a known, accepted limitation, naming its trigger and naming the stronger fix as deliberately deferred. The doc no longer reads as though the fallback is unconditionally safe."
    - "`dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` reports `Failed: 0` with exactly one more passing fact than the 120 this suite reported before this plan, and zero `PackageReference` entries are added, removed, or version-changed in any project (D-05, D-06)."
    - statement: "WR-01's stronger structural fix — probing the opposite namespace before a process concludes it is primary, so a scope mismatch degrades to 'assume duplicate and broadcast' rather than 'assume primary' — is carried forward, not discharged here. It changes single-instance detection semantics and carries more regression risk than the failure it prevents, which has never been observed on this rig."
      verification: backstop
  artifacts:
    - "src/RigToggle.Core/SingleInstanceGuard.cs — `WaitForInstanceReady` gains a narrowly-typed `catch (AbandonedMutexException)` that treats the abandoned-but-acquired wait as a successful wait; `Acquire()` gains a guarded readiness-mutex construction that degrades to null on failure; the class doc comment's abandoned-mutex and namespace-fallback paragraphs are corrected."
    - "src/RigToggle.Tests/SingleInstanceGuardTests.cs — new fact `WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing`, the CI regression net for CR-01."
    - "src/RigToggle.App/UpdateApplyEntryPoint.cs — doc comment reworded to match `Program.cs`'s precise ordering claim (IN-01)."
    - ".planning/phases/25-single-instance-guard/25-04-SUMMARY.md — records the RED test output verbatim, the GREEN output, and the operator's Windows rig results from Task 3."
  key_links:
    - "`AbandonedMutexException` is a plain `System.Threading` type available on every platform .NET 10 runs on. `RigToggle.Core` targets bare `net10.0` (not `net10.0-windows`) precisely so its suite runs on non-Windows machines, and both the fix and its regression test respect that: they live in `RigToggle.Core` and the cross-platform `RigToggle.Tests`, never in the Windows-only projects. This was empirically confirmed during planning — a thread-abandoned named mutex throws `AbandonedMutexException` on this repository's Linux build environment, so the test genuinely goes red here and is not a Windows-only proof."
    - "`acquired = true` in the catch is not cosmetic and not merely 'the friendlier answer'. When `WaitOne` throws `AbandonedMutexException`, the .NET contract is that the wait SUCCEEDED and ownership WAS transferred to the calling thread. Setting `acquired = true` is what routes control into the existing `ReleaseIgnoringOwnershipError(readyMutex)` branch, which clears the abandoned state. Returning `false` instead would skip that release, leave this thread owning the mutex, and re-abandon it the moment the thread exits — turning one dead primary into an unbounded chain of abandoned-mutex exceptions for every subsequent launch."
    - "`Program.cs:247` needs no change and gets none. It already broadcasts the activation signal regardless of what `WaitForInstanceReady` returns, so the return value only affects a diagnostic log line. The gap was never 'the wrong value came back' — it was 'no value came back at all, because the process died'. Fixing it inside the guard keeps the single duplicate-launch branch (D-02) exactly as single as it was."
    - "`MarkReady()` already no-ops on a null `_readyMutex` and `Dispose()` already null-guards it, and the loser side already has a bounded open-retry that fails fast to `false` when the readiness handle never appears. That is why WR-02's degradation target is a null readiness handle: every downstream path it produces already exists, is already reachable via the non-primary guard, and is already covered by `MarkReady_OnNonPrimaryGuard_DoesNotThrow` and `WaitForInstanceReady_NoInstancePublished_ReturnsFalseQuickly`. WR-02 adds a failure mode, not a code path."
  prohibitions:
    - statement: "MUST NOT simulate abandonment by calling `guard.Dispose()` without first calling `MarkReady()`. `Dispose()` releases the readiness mutex cleanly through `ReleaseIgnoringOwnershipError(_readyMutex)` under the `_readyMutexReleased` interlock, so the subsequent wait returns `true` even with the CR-01 bug fully present — a permanently green test that proves nothing and would have shipped this phase as 'covered'. Abandonment requires the OWNING THREAD to terminate while still holding the mutex. This is an open, deliberate deviation from the literal wording of 25-VERIFICATION.md's second `missing` item and 25-REVIEW.md's closing suggestion, both of which describe the Dispose-based construction; the substance they ask for (a CI regression net for the abandoned path) is fully delivered, by the only construction that actually produces abandonment."
    - statement: "MUST NOT use the class-level `TestInstanceId` field for the new fact. The abandoned guard is deliberately never released, so sharing the class-wide id would leave the mutex held for the remainder of the test-assembly lifetime and silently flip `IsPrimaryInstance` to false in every other fact in the class that ran after it. xUnit does not guarantee method order within a class, so this would surface as order-dependent intermittent red — exactly the cross-suite flakiness class 25-03 Task 3 already had to dig out of this file once."
    - statement: "MUST NOT fix CR-01 by registering an `AppDomain.UnhandledException` or `Application.ThreadException` handler. The review names the absence of one only as evidence that nothing else would have caught the exception — not as the prescribed fix. A process-wide handler would mask this entire bug class rather than close it, would change startup architecture well outside this gap's scope, and would leave the readiness mutex un-released and therefore still abandoned for the next waiter."
    - statement: "MUST NOT widen the main mutex's existing `catch (UnauthorizedAccessException)` in `Acquire()`. That catch is deliberately single-typed because the call it guards decides primary-vs-duplicate and is correctness-critical, not best-effort (25-CONTEXT.md, T-25-06). WR-02's broader catch applies ONLY to the readiness-mutex construction, which is a Pitfall-8 optimisation layered over an already-tolerant broadcast path."
    - statement: "MUST NOT add, remove, or version-change any `PackageReference` in any project. The milestone's zero-new-NuGet decision stands; `AbandonedMutexException` is BCL."
    - statement: "MUST NOT weaken, skip, `Trait`-exclude, or delete any existing fact to get a green run (D-06). If an existing fact turns red, that is a real regression from this plan's changes and must be fixed at the source."
  decisions_implemented: [D-01, D-02, D-05, D-06]
---

<objective>
Close 25-VERIFICATION.md's one blocking gap and 25-REVIEW.md's one Critical finding — they are the same defect. `SingleInstanceGuard.WaitForInstanceReady`'s `readyMutex.WaitOne(remaining)` is unguarded against `AbandonedMutexException`, so a duplicate launch crashes with an unhandled exception whenever the primary dies between `Acquire()` and `MarkReady()`. Add the catch, and add the CI regression net that proves it — proven by observing the test RED first.

Then take the two cheap, same-class hardening items from the same review (WR-02's unguarded readiness-mutex construction, and the doc comments that currently assert safety properties the code does not have), and hand the phase's remaining evidence gap to the operator at a blocking checkpoint.

Purpose: Truth 4 of 25-VERIFICATION.md — "the guard mechanism never crashes a process due to its own kernel-object handling" — is the phase's own stated design posture (Security Domain V5, T-25-06) and is currently FAILED. Every other artifact in this phase is verified. This is the single thing standing between Phase 25 and `passed`.

Output: One narrowly-typed catch, one guarded construction, one new xUnit fact, three corrected doc comments, and the operator's Windows-hardware result.

**Decision coverage.** All six phase decisions were implemented by 25-01/25-02/25-03. This plan actively honours four and must not break the other two while editing a doc comment that describes them:

- **D-01:** silent restore, no toast or notification. Honoured negatively and literally — the crash this plan removes is the loudest possible violation of it, an unhandled-exception dialog on a path whose entire specified user-visible output is "the existing window comes to front." No toast, dialog, `MessageBox`, or balloon is added anywhere in this plan.
- **D-02:** exactly one duplicate-launch branch, no reason-based sub-cases. The fix lands entirely inside `RigToggle.Core`; `Program.cs`'s single `if (!guard.IsPrimaryInstance)` branch is not touched, not duplicated, and gains no sub-case. Task 1's acceptance criteria re-assert the branch count mechanically.
- **D-03:** the bypass is a dedicated CLI flag checked before the guard is touched. Untouched here in behaviour — Task 2 Part D corrects only the doc comment that describes its position (IN-01), and the branch ordering it documents is unchanged.
- **D-04:** `--apply-update` is the final flag name Phase 26 consumes, with a placeholder body in Phase 25. `UpdateApplyEntryPoint.Run(string[])`'s body stays side-effect-free and the parse contract is unchanged; only its doc comment is reworded. This is the phase's one-way cross-phase contract and this plan deliberately does not touch it.
- **D-05:** automated xUnit tests, not a manual script. The regression net is an automated xUnit fact. Note this plan satisfies D-05's LITERAL wording that 25-03 had to deviate from: the new fact genuinely lives in `RigToggle.Tests`, because the abandoned-mutex path is pure `RigToggle.Core` kernel-object semantics with no window, no exe, and no Windows dependency.
- **D-06:** tests run in the normal CI build, never skip-tagged. The new fact carries no `Skip` argument and no CI-exclusion trait, and `.github/workflows/build.yml` needs no edit — its existing solution-wide `dotnet test` already discovers it. Task 3 routes the phase's remaining rig-only evidence to the operator rather than weakening any test to make it automatable.

**Gap-closure scope discipline.** 25-VERIFICATION.md also records two `behavior_unverified` items (ROADMAP SC1 and SC3) and two `human_verification` items. Those are NOT code gaps — the tests exist, are wired, and carry no skip attribute; they simply cannot execute in the Linux sandbox that verified this phase. No task here attempts to "fix" them in code. Task 3 routes them to the operator, which is the only thing that can actually discharge them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/25-single-instance-guard/25-VERIFICATION.md
@.planning/phases/25-single-instance-guard/25-REVIEW.md
@.planning/phases/25-single-instance-guard/25-CONTEXT.md

@src/RigToggle.Core/SingleInstanceGuard.cs
@src/RigToggle.Tests/SingleInstanceGuardTests.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-01 — the abandoned readiness mutex, red first then green</name>

  <files>src/RigToggle.Tests/SingleInstanceGuardTests.cs, src/RigToggle.Core/SingleInstanceGuard.cs</files>

  <read_first>
    - `src/RigToggle.Core/SingleInstanceGuard.cs` — the file being fixed, in full (358 lines). You specifically need: `Acquire()` (162-197) to see that BOTH mutexes are created with `initiallyOwned: true` on the calling thread; `MarkReady()` (207-220) and `Dispose()` (316-336) to see that both release the readiness mutex under the `_readyMutexReleased` interlock — this is why the Dispose-based simulation is banned in this plan's prohibitions; `WaitForInstanceReady` (243-286) which is the call site being fixed; and `ReleaseIgnoringOwnershipError` (346-357) which already swallows the wrong-thread `ApplicationException`.
    - `src/RigToggle.Tests/SingleInstanceGuardTests.cs` — the file being extended, in full (200 lines). Copy its established discipline wholesale: the `Record.Exception` assertion idiom used by three existing facts; the named-timeout-constant convention; the doc comment on every fact naming the pitfall or decision it proves; and above all the thread-affinity rule spelled out at length on `WaitForInstanceReady_ReadinessPublishedWhileWaitInProgress_ReturnsTrue` (lines 165-199) — a named `Mutex` has thread affinity on the acquiring side, which is exactly the mechanism this new fact weaponises.
    - `.planning/phases/25-single-instance-guard/25-REVIEW.md` — the CR-01 section (lines 58-131). It contains the prescribed patch verbatim. Follow it, with the one documented departure noted in this plan's prohibitions.
    - `src/RigToggle.App/Program.cs` lines 226-253 — the only production call site. Read it to confirm for yourself that it already broadcasts regardless of the return value, so no change is needed there and none is to be made.
  </read_first>

  <precondition>Plans 25-01, 25-02 and 25-03 are committed, so `SingleInstanceGuard.cs` is at the 358-line post-25-03 state described above. `dotnet` is on `PATH` and `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` currently reports `Passed: 120, Failed: 0` — capture that baseline number before you change anything, because a later acceptance criterion is stated relative to it.</precondition>

  <reversibility rating="reversible">One narrowly-typed catch clause plus one new test fact. Reverting restores the crash and removes the net; it changes no public signature, no name, and no cross-phase contract. Phase 26 consumes `StartupArgs`/`UpdateApplyEntryPoint`, neither of which this task touches.</reversibility>

  <behavior>
    The new fact, `WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing`:
    - Given a guard acquired on a dedicated thread that then exits WITHOUT calling `MarkReady()` and WITHOUT disposing — so the OS marks both of that guard's mutexes abandoned
    - When `WaitForInstanceReady` is called from the test thread against that same instance id
    - Then it returns `true`, and throws nothing — in particular it does not throw `AbandonedMutexException`
    - Before the production fix: this fact FAILS, and `AbandonedMutexException` appears in the failure output
    - After the production fix: this fact PASSES, and every one of the 10 pre-existing facts in the class still passes regardless of the order xUnit runs them in
  </behavior>

  <action>
    Work in strict red-then-green order. The red observation is a deliverable of this task, not a formality — it is the only thing that distinguishes a real regression net from a test that would have passed with the bug present.

    **Step 1 — write ONLY the test. Do not touch `SingleInstanceGuard.cs` yet.**

    Add one fact to `src/RigToggle.Tests/SingleInstanceGuardTests.cs`, named `WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing`, appended after the existing final fact. Give it a doc comment that states: the scenario is 25-REVIEW.md CR-01 and 25-VERIFICATION.md's blocking gap; the production trigger is the primary process dying anywhere between `Acquire()` and `MarkReady()` — a window that in `Program.cs` spans `StartupRecoveryChecker.Run`, `MainForm` construction, `InitializeTrayState()` and `RegisterHotkeyAtStartup()`; and that in-process THREAD termination while owning the mutex is the deterministic equivalent of that PROCESS termination, because the OS marks a mutex abandoned on owning-thread exit and the waiter observes an identical `AbandonedMutexException` either way.

    Body, exactly this shape:
    - Declare a test-local instance id: `string abandonedInstanceId = Guid.NewGuid().ToString();`. Comment it as deliberately NOT the class-level shared field, because this fact's guard is never released and a shared id would poison every sibling fact in the class under xUnit's unordered execution.
    - Declare `SingleInstanceGuard? abandonedGuard = null;` and a `ManualResetEventSlim` named `acquired`.
    - Create `var owner = new Thread(...)` whose body is exactly two statements: assign `abandonedGuard = SingleInstanceGuard.Acquire(abandonedInstanceId);` then `acquired.Set();`. The thread must return immediately after that — no `MarkReady()`, no `Dispose()`, no join-blocking work. Its exit while owning both mutexes IS the abandonment.
    - `owner.Start();` then assert `acquired.Wait(...)` returned true against a named timeout constant, with a failure message saying the owner thread never acquired the guard so the rest of the fact would be meaningless. Then `owner.Join();`. Synchronise deterministically on `Join()` — never on a fixed-duration sleep — matching this file's existing no-timing-guess discipline.
    - Wrap everything from here in `try { ... } finally { abandonedGuard?.Dispose(); }`. The `finally` serves two purposes and both are load-bearing: it releases the leaked handles at the end, and it keeps `abandonedGuard` reachable for the whole wait, so the finalizer cannot close the last handle to the named object mid-test and turn a genuine abandoned-wait into a spurious "handle never opened" false negative.
    - Inside the `try`: `Assert.NotNull(abandonedGuard);` then assert `abandonedGuard!.IsPrimaryInstance` is true with a message saying a non-primary guard publishes no readiness mutex, so there would be nothing to abandon.
    - Then capture the call through this file's existing `Record.Exception` idiom so the red failure is legible: declare `bool result = false;`, call `var exception = Record.Exception(() =` `> result = SingleInstanceGuard.WaitForInstanceReady(...));` passing a named timeout constant and `abandonedInstanceId`.
    - Assert in three steps, in this order, so a failing run names the actual defect rather than a bare boolean: first `Assert.False(exception is AbandonedMutexException, ...)` with a message stating this is CR-01 and that the abandoned wait must be caught inside `WaitForInstanceReady` and treated as a successful acquisition; then `Assert.Null(exception);` to catch any other escape; then `Assert.True(result, ...)` with a message explaining that an abandoned wait genuinely acquired ownership, so reporting false would skip the release and re-abandon the mutex for the next waiter.

    Add `using System.Threading;` to the file's using block if it is not already there.

    **Step 2 — observe RED.** Run `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release --filter "FullyQualifiedName~WaitForInstanceReady_PrimaryAbandonedReadinessMutex"`. It MUST fail AND the failure output MUST name `AbandonedMutexException`. Copy that output verbatim — it goes in the SUMMARY. If it passes, or fails for any other reason (handle never opened, timeout, null guard), the fact is not exercising the CR-01 path: stop, fix the test, and re-observe. Do not proceed to Step 3 on an unexplained result. Commit the red state: `test(25-04): add failing abandoned-readiness-mutex regression test for CR-01`.

    **Step 3 — apply the production fix**, in `src/RigToggle.Core/SingleInstanceGuard.cs`, inside `WaitForInstanceReady`'s `using (readyMutex)` block. Replace the single statement `bool acquired = readyMutex.WaitOne(remaining);` with a `bool acquired;` declaration followed by a `try` whose body is the same `readyMutex.WaitOne(remaining)` assignment, and a single `catch (AbandonedMutexException)` whose body sets `acquired = true;` and calls `TryLog` with a distinct message naming the abandoned case, so the operator's `debug.log` distinguishes it from an ordinary successful wait. Catch nothing else — the surrounding call is correctness-critical, not best-effort.

    Comment the catch with the two facts that make `true` correct rather than merely convenient, citing `CR-01`: (a) the .NET contract is that a wait ending in `AbandonedMutexException` SUCCEEDED and ownership WAS transferred to this thread, and the readiness mutex carries no shared state — only a held-or-released signal — so there is no inconsistent state a normal successful wait would not also have; and (b) `true` is what routes control into the existing `ReleaseIgnoringOwnershipError(readyMutex)` branch below, which clears the abandoned state. Returning `false` would skip that release, leave this thread owning the mutex, and re-abandon it on thread exit, converting one dead primary into an unbounded chain of the same exception for every launch that follows.

    Leave the `if (acquired)` release branch, the trailing `TryLog`, and the `return acquired;` structurally as they are. Change nothing in `Program.cs` — it already broadcasts regardless of the return value, and D-02 requires its duplicate-launch branch to stay exactly one branch.

    **Step 4 — observe GREEN.** Re-run the filtered command: the fact passes. Then run the whole project unfiltered: `Failed: 0`, with exactly one more passing fact than the baseline you captured in the precondition. Commit: `fix(25-04): catch AbandonedMutexException in WaitForInstanceReady (CR-01)`.
  </action>

  <acceptance_criteria>
    - The RED run from Step 2 was actually executed against unfixed production code, failed, and named `AbandonedMutexException` in its output. That output is pasted verbatim into `25-04-SUMMARY.md`. A SUMMARY without it means this criterion is unmet regardless of the final green run.
    - **Every count-equality gate below is comment-filtered** with `grep -v '^[[:space:]]*//'` before counting. This is deliberate and load-bearing: this task's action instructs you to write several of these exact tokens INTO doc comments, so an unfiltered count would rise above its target and fail a correct implementation. Filtering makes each gate count real code only. Citation gates (`CR-01`, `WR-01`, `WR-02`, `IN-01`) are intentionally NOT filtered — they exist to match comments.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cE 'catch \(AbandonedMutexException'` outputs `1` — exactly one, and it is in `WaitForInstanceReady`.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cF 'catch (UnauthorizedAccessException)'` outputs `1` — the main mutex's narrow catch is unchanged and was not widened.
    - `grep -cF 'CR-01' src/RigToggle.Core/SingleInstanceGuard.cs` outputs at least `1` — the catch cites the finding it closes. Unfiltered by design.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Tests/SingleInstanceGuardTests.cs | grep -c '\[Fact\]'` outputs `11` — the 10 pre-existing facts plus exactly one new one.
    - `grep -c 'WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing' src/RigToggle.Tests/SingleInstanceGuardTests.cs` outputs at least `1` — the `[Fact]` count above already bounds it from the other side.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Tests/SingleInstanceGuardTests.cs | grep -cF 'Guid.NewGuid().ToString()'` outputs `2` — the pre-existing class-level id plus the new fact's own locally-generated id. A `1` here means the new fact reused the shared field, which this plan prohibits.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Tests/SingleInstanceGuardTests.cs | grep -cF 'new Thread('` outputs `1` — abandonment is produced by owning-thread exit, not by a `Dispose()` call.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Tests/SingleInstanceGuardTests.cs | grep -cF 'Thread.Sleep('` outputs `0` — synchronisation is deterministic, on `Join()` and on a real handle wait, matching this file's existing discipline.
    - `grep -cF 'AppDomain' src/RigToggle.App/Program.cs` outputs `0` and `grep -cF 'ThreadException' src/RigToggle.App/Program.cs` outputs `0` — CR-01 was not "fixed" by masking it behind a process-wide handler. `Program.cs` is not in this plan's `files_modified`, so both are `0` today and must stay `0`.
    - `grep -c 'IsPrimaryInstance' src/RigToggle.App/Program.cs` outputs `1` — D-02's single duplicate-launch branch is intact and `Program.cs` was not edited.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Tests/SingleInstanceGuardTests.cs | grep -cE 'Skip[[:space:]]*=|Trait\('` outputs `0` — the new fact runs in normal CI unconditionally (D-06).
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` reports `Failed: 0` and `Passed: 121`. If the pre-existing count has drifted from 120, the binding requirement is `Failed: 0` plus exactly one more passing fact than the baseline captured in the precondition.
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` run a second time reports the identical result — the new fact leaks no held mutex into a subsequent run.
    - `git diff --name-only` for this task lists exactly `src/RigToggle.Core/SingleInstanceGuard.cs` and `src/RigToggle.Tests/SingleInstanceGuardTests.cs` and nothing else.
  </acceptance_criteria>

  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; G="src/RigToggle.Core/SingleInstanceGuard.cs"; T="src/RigToggle.Tests/SingleInstanceGuardTests.cs"; NC() { grep -v '^[[:space:]]*//' "$1"; }; [ "$(NC $G | grep -cE 'catch \(AbandonedMutexException')" = "1" ] &amp;&amp; [ "$(NC $G | grep -cF 'catch (UnauthorizedAccessException)')" = "1" ] &amp;&amp; [ "$(grep -cF 'CR-01' $G)" -ge "1" ] &amp;&amp; [ "$(NC $T | grep -c '\[Fact\]')" = "11" ] &amp;&amp; [ "$(grep -c 'WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing' $T)" -ge "1" ] &amp;&amp; [ "$(NC $T | grep -cF 'Guid.NewGuid().ToString()')" = "2" ] &amp;&amp; [ "$(NC $T | grep -cF 'new Thread(')" = "1" ] &amp;&amp; [ "$(NC $T | grep -cF 'Thread.Sleep(')" = "0" ] &amp;&amp; [ "$(grep -cF 'AppDomain' src/RigToggle.App/Program.cs)" = "0" ] &amp;&amp; [ "$(grep -cF 'ThreadException' src/RigToggle.App/Program.cs)" = "0" ] &amp;&amp; [ "$(grep -c 'IsPrimaryInstance' src/RigToggle.App/Program.cs)" = "1" ] &amp;&amp; [ "$(NC $T | grep -cE 'Skip[[:space:]]*=|Trait\(')" = "0" ] &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release &amp;&amp; echo GATE-25-04-1-PASS</automated>
  </verify>

  <done>`WaitForInstanceReady` catches `AbandonedMutexException`, treats it as a successful acquisition so the existing release branch runs and clears the abandoned state, and logs the case distinctly. A new xUnit fact reproduces the abandonment by letting the owning thread exit while holding the readiness mutex, was observed RED naming `AbandonedMutexException` before the fix and GREEN after, uses its own locally-generated instance id so it cannot poison its siblings, and runs unskipped in CI. `Program.cs` is untouched, the main mutex's narrow catch is untouched, no process-wide exception handler was added, and the cross-platform suite reports `Failed: 0` twice in a row. Red and green are separate commits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-02's unguarded construction, and the doc comments that assert safety the code does not have</name>

  <files>src/RigToggle.Core/SingleInstanceGuard.cs, src/RigToggle.App/UpdateApplyEntryPoint.cs</files>

  <read_first>
    - `src/RigToggle.Core/SingleInstanceGuard.cs` — the post-Task-1 state. You need `Acquire()` (162-197) for the construction being guarded, and the class doc comment (6-60) for the two paragraphs being corrected: the abandoned-mutex paragraph at 26-31 and the `Global\`/`Local\` fallback paragraph at 33-49.
    - `.planning/phases/25-single-instance-guard/25-REVIEW.md` — the WR-01 section (135-167), the WR-02 section (169-211) which contains the prescribed patch verbatim, and the IN-01 section (215-235) which contains the prescribed rewording.
    - `src/RigToggle.App/UpdateApplyEntryPoint.cs` — all 39 lines. Only the doc comment changes; `Run(string[])` must stay side-effect-free, which 25-VERIFICATION.md confirms with a standing negative grep.
    - `src/RigToggle.App/Program.cs` lines 100-125 — the `--apply-update` branch and the comment immediately above it. That comment is the more accurate phrasing IN-01 asks you to align to; read it so the reword matches reality rather than being invented.
    - `.planning/phases/25-single-instance-guard/25-VERIFICATION.md` — the Anti-Patterns Found table (108-114), which is the record these three corrections discharge.
  </read_first>

  <precondition>Task 1 is committed. This task edits the same file and must not be interleaved with the red-green cycle, or the RED observation stops being an observation of unfixed code plus nothing else.</precondition>

  <reversibility rating="reversible">One defensive try/catch that degrades to an already-supported null state, plus three comment rewrites. No signature, name, or cross-phase contract changes. WR-01's structural fix is explicitly NOT taken here — see this plan's backstop truth.</reversibility>

  <action>
    **Part A — WR-02, `src/RigToggle.Core/SingleInstanceGuard.cs`, in `Acquire()`.** Replace the unguarded conditional-expression construction of `readyMutex` with an explicit `Mutex? readyMutex = null;` followed by `if (createdNew) { try { readyMutex = new Mutex(initiallyOwned: true, readyEventName, out _); } catch (Exception ex) { TryLog(...); } }`. The `TryLog` message must name the readiness event name and the exception, and state that readiness will never be signalled by this instance.

    Comment the block, citing `WR-02` and `T-25-06`, with three points. First, why the catch is deliberately broad here while the main mutex's catch two blocks above is deliberately narrow: the main mutex decides primary-vs-duplicate and is correctness-critical, so swallowing an unexpected type there would trade a loud failure for a silent INSTANCE-01 violation; the readiness mutex is a Pitfall-8 latency optimisation layered over an already-tolerant broadcast path, so no failure to create it may ever be allowed to take down startup (Security Domain V5's never-crash-on-startup posture, which is the same contract CR-01 violated at the other end of this class). Second, that the degraded state is already fully supported and already reachable: `MarkReady()` no-ops on a null readiness mutex, `Dispose()` null-guards it, the loser's `WaitForInstanceReady` already fails fast to `false` when the handle never appears, and `Program.cs` broadcasts regardless — so this adds a failure mode, not a code path, and the two existing facts `MarkReady_OnNonPrimaryGuard_DoesNotThrow` and `WaitForInstanceReady_NoInstancePublished_ReturnsFalseQuickly` already cover the resulting behaviour. Third, that without this guard the exception escaped `Acquire()` before its `TryLog` line ever ran, so the app crashed on startup with zero diagnostic trail AND leaked the already-owned main mutex handle.

    Do not add a test for this path. There is no seam to inject a construction failure through, and manufacturing one would mean widening the public API — a larger and riskier change than the two-line guard it would cover. Say so explicitly in the SUMMARY rather than leaving the absence unexplained.

    **Part B — the abandoned-mutex paragraph, class doc comment (26-31).** It currently asserts that this class's constructor path never surfaces `AbandonedMutexException`. That is true of the main mutex and materially misleading about the class, and CR-01 is the proof. Rewrite it to keep the true half and add the missing half: Windows still releases ownership on process termination so no launch can be permanently wedged; the main mutex is acquired via `new Mutex(...)` and therefore cannot surface the exception; but the SEPARATE readiness mutex genuinely is waited on via `WaitOne` in `WaitForInstanceReady`, which DOES surface it when the primary dies between `Acquire()` and `MarkReady()`; and that case is caught there and treated as a successful acquisition, because the wait did acquire ownership and the release that follows is what clears the abandoned state for the next waiter. Cite `CR-01` and name the regression fact `WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing` so a future editor can find the proof.

    **Part C — WR-01, the namespace-fallback paragraph, class doc comment (33-49).** The paragraph currently discusses the `Global\`-to-`Local\` fallback purely as single-process resilience, which reads as though the fallback is unconditionally safe. Append a known-accepted-limitation note citing `WR-01`: the scope is resolved independently per-process, so two processes whose tokens disagree about `Global\` access — one launched elevated and one not, or two login/RDP sessions — can each resolve a different namespace, each observe `createdNew == true`, and each believe it is primary, silently defeating INSTANCE-01 and leaving two processes writing `mode.json`, `settings.json` and the monitor/audio state with no coordination. Record it as accepted at ASVS L1 for this single-user personal rig, where every launch is the same non-elevated user in one session. Name the deferred stronger fix — have the primary-concluding path probe the opposite namespace with `Mutex.TryOpenExisting` before concluding it is primary, so a scope mismatch degrades to "cannot tell, assume duplicate and broadcast" — and name why it is deferred: it changes single-instance detection semantics, carries more regression risk than the failure it prevents, and that failure has never been observed on this rig. Do NOT implement it in this task.

    **Part D — IN-01, `src/RigToggle.App/UpdateApplyEntryPoint.cs` doc comment (8-9).** It claims the bypass is reached "before any other bootstrap step," which is not accurate: `Application.SetColorMode(...)` and `ApplicationConfiguration.Initialize()` — including the latter's documented `SystemEvents` background-thread spin-up — both run first. Reword to match `Program.cs`'s own more precise phrasing: the first branch after those two position-sensitive calls, and strictly before the single-instance guard and every other bootstrap step. Name both `SetColorMode` and `ApplicationConfiguration.Initialize` explicitly so the two files' comments can no longer drift apart, and cite `IN-01`. Change only the comment — `Run(string[])`'s body stays side-effect-free, returning the bypass exit code and nothing else (UPDATE-07, D-03/D-04's cross-phase contract, which Phase 26 consumes unchanged).
  </action>

  <acceptance_criteria>
    - **Count-equality gates on `.cs` files are comment-filtered** with `grep -v '^[[:space:]]*//'` before counting; citation gates (`CR-01`, `WR-01`, `WR-02`, `IN-01`) are not, because they exist to match comments. This matters most for `TryOpenExisting` below: Part C's action tells you to NAME `Mutex.TryOpenExisting` in a doc comment as the deferred fix, which would push an unfiltered count to `2` and fail a correct implementation for doing exactly what it was told.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cE 'catch \(Exception ex\)'` outputs `1` — exactly one broad catch, and it is the readiness-mutex construction.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cF 'catch (UnauthorizedAccessException)'` outputs `1` — still narrow, still not widened.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cF 'Mutex? readyMutex = null;'` outputs `2` — the pre-existing declaration in `WaitForInstanceReady`'s open-retry loop (line 248 today), PLUS the new one in `Acquire()`. This gate reads `1` on the unmodified file, so a `1` after this task means Part A was not applied.
    - `grep -cF 'WR-02' src/RigToggle.Core/SingleInstanceGuard.cs` outputs at least `1`.
    - `grep -cF 'WR-01' src/RigToggle.Core/SingleInstanceGuard.cs` outputs at least `1`.
    - `grep -cF 'CR-01' src/RigToggle.Core/SingleInstanceGuard.cs` outputs at least `2` — Task 1's catch citation plus Part B's doc citation.
    - `grep -cF 'WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing' src/RigToggle.Core/SingleInstanceGuard.cs` outputs at least `1` — the doc comment names its own proof.
    - `grep -v '^[[:space:]]*//' src/RigToggle.Core/SingleInstanceGuard.cs | grep -cF 'TryOpenExisting'` outputs `2` — the two pre-existing CODE uses in `WaitForInstanceReady`'s open-retry loop (the `Global\` attempt and its `Local\` fallback, lines 256 and 258 today), unchanged. A `3` here means WR-01's deferred structural fix was actually implemented, which this plan defers by design. Naming the API in Part C's doc comment is expected and is filtered out by this gate.
    - `grep -cF 'IN-01' src/RigToggle.App/UpdateApplyEntryPoint.cs` outputs at least `1`, and `grep -cF 'ApplicationConfiguration.Initialize' src/RigToggle.App/UpdateApplyEntryPoint.cs` outputs at least `1` — the reword names the calls that actually precede it.
    - `grep -c 'PackageReference' src/RigToggle.Core/RigToggle.Core.csproj` outputs `1` — unchanged; the one occurrence is the file's pre-existing prohibition comment, not an element.
    - `dotnet build src/RigToggle.Core/RigToggle.Core.csproj -c Release` and `dotnet build src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` each exit 0 with `0 Warning(s)`.
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` still reports `Failed: 0` and the same passing count as at the end of Task 1 — no fact was added, removed, weakened or skipped here.
    - `git diff --name-only` for this task lists exactly `src/RigToggle.Core/SingleInstanceGuard.cs` and `src/RigToggle.App/UpdateApplyEntryPoint.cs`.
  </acceptance_criteria>

  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; G="src/RigToggle.Core/SingleInstanceGuard.cs"; U="src/RigToggle.App/UpdateApplyEntryPoint.cs"; NC() { grep -v '^[[:space:]]*//' "$1"; }; [ "$(NC $G | grep -cE 'catch \(Exception ex\)')" = "1" ] &amp;&amp; [ "$(NC $G | grep -cF 'catch (UnauthorizedAccessException)')" = "1" ] &amp;&amp; [ "$(NC $G | grep -cF 'Mutex? readyMutex = null;')" = "2" ] &amp;&amp; [ "$(grep -cF 'WR-02' $G)" -ge "1" ] &amp;&amp; [ "$(grep -cF 'WR-01' $G)" -ge "1" ] &amp;&amp; [ "$(grep -cF 'CR-01' $G)" -ge "2" ] &amp;&amp; [ "$(grep -cF 'WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing' $G)" -ge "1" ] &amp;&amp; [ "$(NC $G | grep -cF 'TryOpenExisting')" = "2" ] &amp;&amp; [ "$(grep -cF 'IN-01' $U)" -ge "1" ] &amp;&amp; [ "$(grep -cF 'ApplicationConfiguration.Initialize' $U)" -ge "1" ] &amp;&amp; [ "$(grep -c 'PackageReference' src/RigToggle.Core/RigToggle.Core.csproj)" = "1" ] &amp;&amp; dotnet build src/RigToggle.Tests/RigToggle.Tests.csproj -c Release &amp;&amp; dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release &amp;&amp; echo GATE-25-04-2-PASS</automated>
  </verify>

  <done>Readiness-mutex construction can no longer escape `Acquire()`: it degrades to a logged null handle whose every downstream path already existed and is already covered. The class doc comment states what is actually true about abandoned mutexes in this class and names the fact that proves it, and records the `Global\`/`Local\` cross-process divergence as a known accepted limitation with its trigger and its deferred fix named rather than reading as unconditionally safe. `UpdateApplyEntryPoint`'s ordering claim matches `Program.cs`. The main mutex's narrow catch is unchanged, WR-01's structural fix is deliberately not implemented, no package changed, build is warning-free and the suite is still green. Work committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator verification on the Windows rig — the phase's remaining evidence gap</name>

  <files>(none — operator action on real hardware)</files>

  <read_first>
    - `.planning/phases/25-single-instance-guard/25-VERIFICATION.md` — the `human_verification` block (53-59) and the "Human Verification Required" section (137-149). These two items, plus the CR-01 fix Tasks 1 and 2 just landed, are exactly what 25-VERIFICATION.md's closing line lists as "what would move this to passed."
    - `.planning/phases/25-single-instance-guard/25-03-SUMMARY.md` — the "Open follow-up, NOT closed out by this SUMMARY" paragraph. The three-consecutive-run check was requested by 25-03's own Task 3 and was never completed; the operator's investigation pivoted into fixing two real production bugs instead.
  </read_first>

  <precondition>Tasks 1 and 2 are committed, and the operator is at a real Windows machine with the .NET 10 SDK installed and a working copy at this commit. `RigToggle.Windows.Tests` requires the `Microsoft.WindowsDesktop.App` runtime and cannot execute anywhere else — this checkpoint cannot be discharged from the Linux build environment, and must not be marked complete by inspection.</precondition>

  <what-built>
    `WaitForInstanceReady` now catches `AbandonedMutexException` and treats the abandoned-but-acquired wait as a successful wait, so a duplicate launch whose primary died mid-startup broadcasts and exits cleanly instead of crashing. A new cross-platform xUnit fact reproduces the abandonment by owning-thread exit and was observed failing before the fix and passing after. `Acquire()`'s readiness-mutex construction can no longer escape as an unhandled exception. Three doc comments were corrected to stop asserting safety properties the code did not have.

    What is still unproven is not code — it is evidence. The seven `SingleInstanceProcessTests` facts (rapid relaunch, tight race, hidden-window restore, and the three `--apply-update` bypass facts) exist, are wired, and carry no skip attribute, but have never been run to a confirmed stable green on real hardware. 25-03-SUMMARY.md records the bypass facts' status as "unknown."
  </what-built>

  <how-to-verify>
    **Check 1 — three consecutive clean runs (the flakiness check 25-03 Task 3 asked for and never got).**
    From the repository root on the Windows rig:
    1. `dotnet build RigToggle.sln -c Release`
    2. `dotnet test RigToggle.sln -c Release --no-build` — run this three times in a row, without rebuilding between runs.
    Expected: identical results all three times, `Failed: 0` every time, and all seven `SingleInstanceProcessTests` facts green every time — including the three `ApplyUpdateBypass_*` facts (UPDATE-07), whose pass/fail status has never actually been reported. Report the summary line from each of the three runs. If any fact is intermittently red, report which one and how often — that is a real finding and must surface now, not silently in CI later (D-06).

    **Check 2 — the CR-01 scenario on real hardware, now that the fix has landed.**
    1. Launch Rig Toggle normally and immediately kill it from Task Manager during startup, before its window finishes appearing — this is the `Acquire()`-to-`MarkReady()` window.
    2. While that is happening, launch a second copy so it is blocked inside `WaitForInstanceReady`. Repeat a handful of times; the window is short, so the first attempt may miss it.
    Expected: the second process exits quietly every time. It must never surface an unhandled-exception dialog, a .NET crash window, or a Windows "has stopped working" prompt.
    3. Open `%LOCALAPPDATA%\RigToggle\debug.log` and look for the `SingleInstanceGuard` line naming the abandoned case. If you managed to hit the window, that line is direct confirmation the new catch fired on real hardware rather than only in the test. If you never see it, say so — that means the race was not reproduced, not that the fix failed, and Check 1 plus the automated fact remain the standing evidence.

    Report back the three run summary lines from Check 1, whether any bypass fact was flaky, whether any crash dialog appeared in Check 2, and whether the abandoned-case log line was observed.
  </how-to-verify>

  <acceptance_criteria>
    - Three consecutive `dotnet test RigToggle.sln -c Release --no-build` runs reported by the operator, with the summary line from each, all showing `Failed: 0`.
    - The three `ApplyUpdateBypass_*` facts explicitly confirmed green (UPDATE-07's outstanding evidence — their status was "unknown," not "passing").
    - No unhandled-exception or crash dialog observed during any Check 2 attempt.
    - The operator's verbatim report, including any flakiness observed and whether the abandoned-case log line appeared, is recorded in `25-04-SUMMARY.md`. If flakiness appears, it is recorded as a finding rather than retried until green.
  </acceptance_criteria>

  <resume-signal>Type "approved" with the three run summary lines and the Check 2 result, or describe what failed.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>

Every symbol this plan creates or modifies:

| Symbol | File | Change |
|--------|------|--------|
| `SingleInstanceGuard.WaitForInstanceReady(TimeSpan, string?)` | `src/RigToggle.Core/SingleInstanceGuard.cs` | MODIFIED — body gains `try` / `catch (AbandonedMutexException)` around `readyMutex.WaitOne(remaining)`, setting `acquired = true` and logging the abandoned case distinctly. Signature, return type and every caller unchanged. |
| `SingleInstanceGuard.Acquire(string?)` | `src/RigToggle.Core/SingleInstanceGuard.cs` | MODIFIED — readiness-mutex construction moves from a conditional expression to a guarded `if (createdNew) { try { ... } catch (Exception ex) { TryLog(...); } }` that degrades to a null readiness handle. Signature, return type and every caller unchanged. |
| `SingleInstanceGuard` class doc comment | `src/RigToggle.Core/SingleInstanceGuard.cs` | MODIFIED — abandoned-mutex paragraph corrected (CR-01); namespace-fallback paragraph gains the WR-01 accepted-limitation note. |
| `SingleInstanceGuardTests.WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing()` | `src/RigToggle.Tests/SingleInstanceGuardTests.cs` | NEW — `[Fact]`, the CR-01 CI regression net. Eleventh fact in the class. |
| `UpdateApplyEntryPoint` class doc comment | `src/RigToggle.App/UpdateApplyEntryPoint.cs` | MODIFIED — ordering claim reworded to match `Program.cs` (IN-01). `Run(string[])` body unchanged and still side-effect-free. |

**Deliberately NOT produced** (so the absence is a decision on the record, not an oversight):
- No change to `src/RigToggle.App/Program.cs`. It already broadcasts regardless of `WaitForInstanceReady`'s return value, and D-02 requires its duplicate-launch branch to remain exactly one branch.
- No `AppDomain.UnhandledException` / `Application.ThreadException` handler anywhere in `RigToggle.App`. See this plan's prohibitions.
- No test for WR-02's construction-failure path. No seam exists to inject the failure through, and creating one would widen the public API further than the guard it would cover.
- No implementation of WR-01's cross-namespace probe. Deferred with rationale in the source and recorded as this plan's backstop truth.
- No new, removed, or version-changed `PackageReference` in any project.

</artifacts_this_phase_produces>

<multi_source_coverage_audit>

| Source | Item | Covered by | Status |
|--------|------|-----------|--------|
| GOAL | ROADMAP Phase 25 goal — never two processes side by side; duplicate surfaces the existing instance; deliberate bypass exists | 25-01, 25-02, 25-03; the crash on the readiness-wait path closed here | COVERED |
| GOAL | SC1 — exactly one process survives scripted rapid relaunch | 25-03 (tests exist and are wired); evidence discharged by Task 3 Check 1 | COVERED |
| GOAL | SC2 — existing instance brought to focus and made visible | 25-01/25-03; already `VERIFIED` on real hardware in 25-VERIFICATION.md | COVERED |
| GOAL | SC3 — explicit `--apply-update` bypass, verified by scripted relaunch | 25-02/25-03 (structurally verified); the three bypass facts' evidence discharged by Task 3 Check 1 | COVERED |
| REQ | INSTANCE-01 — launching while running does not start a second instance | 25-01, 25-03; its readiness-wait path de-crashed by Task 1 | COVERED |
| REQ | INSTANCE-02 — already-running instance brought to focus | 25-01, 25-03; `SATISFIED` in 25-VERIFICATION.md; the crash on the same duplicate-launch path removed by Task 1 | COVERED |
| REQ | UPDATE-07 — update-apply relaunch always succeeds despite the guard | 25-02, 25-03; unaffected by CR-01 (the bypass never reaches `WaitForInstanceReady`); evidence discharged by Task 3 Check 1 | COVERED |
| RESEARCH | PITFALLS.md Pitfall 8 — mutex win alone does not guarantee the loser can signal the winner | 25-01's readiness handshake; Task 1 closes its crash-on-abandonment edge; Task 2's WR-02 guard keeps its optimisation from becoming a startup crash | COVERED |
| RESEARCH | PITFALLS.md Pitfall 7 — do not reuse `ToggleOrchestrator._busy` | 25-01; untouched here | COVERED |
| RESEARCH | STACK.md §2 — named `Global\RigToggle-{GUID}` mutex, `RegisterWindowMessage`/`PostMessage` signal, zero new NuGet | 25-01; re-asserted here by the unchanged-`PackageReference` criterion | COVERED |
| RESEARCH | PITFALLS.md Pitfall 4 — relaunch races the guard | Explicitly owned by Phase 26 per 25-CONTEXT.md | OUT OF SCOPE (this phase) |
| CONTEXT | D-01 silent restore, no notification | 25-01; honoured here by removing the crash dialog and adding no UI | COVERED |
| CONTEXT | D-02 one universal duplicate-launch branch | 25-01; re-asserted mechanically by Task 1's `IsPrimaryInstance` count criterion | COVERED |
| CONTEXT | D-03 bypass is a dedicated CLI flag, first branch | 25-02; only its doc comment corrected here (IN-01), contract untouched | COVERED |
| CONTEXT | D-04 flag is the final `--apply-update` name Phase 26 consumes | 25-02; untouched | COVERED |
| CONTEXT | D-05 automated xUnit tests, not a manual script | 25-03; Task 1's new fact satisfies D-05's literal `RigToggle.Tests` placement that 25-03 had to deviate from | COVERED |
| CONTEXT | D-06 tests run in normal CI, not skip-tagged | 25-03; Task 1's no-skip/no-trait criterion | COVERED |
| GAP | 25-VERIFICATION.md gap 1 `missing[0]` — wrap `WaitOne` in `try/catch (AbandonedMutexException)`, treat abandoned-but-acquired as successful | Task 1 Step 3 | COVERED |
| GAP | 25-VERIFICATION.md gap 1 `missing[1]` — add a `SingleInstanceGuardTests` case asserting `WaitForInstanceReady` returns true rather than throwing | Task 1 Steps 1-2, by owning-thread exit rather than the `Dispose()` construction its literal wording describes — see this plan's first prohibition for why the literal construction would have produced a permanently-green test | COVERED (with documented deviation) |
| GAP | 25-REVIEW.md WR-01 | Task 2 Part C — documented as an accepted limitation; structural fix deferred with rationale, recorded as this plan's backstop truth | PARTIALLY COVERED, deliberately |
| GAP | 25-REVIEW.md WR-02 | Task 2 Part A | COVERED |
| GAP | 25-REVIEW.md IN-01 | Task 2 Part D | COVERED |
| GAP | 25-VERIFICATION.md `behavior_unverified` SC1/SC3 and both `human_verification` items | Task 3 — operator action, not code | COVERED |

No unplanned items. `deferred: []` in 25-VERIFICATION.md, and 25-CONTEXT.md records no deferred ideas.

</multi_source_coverage_audit>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this plan. The boundaries 25-01 established are unchanged and restated for continuity:

| Boundary | Description |
|----------|-------------|
| any local process (same desktop, same integrity level) → the named `Global\RigToggle-*` mutex and readiness handle | Unchanged. This plan changes only how this process REACTS to a state the OS puts that handle into (abandoned), and how it reacts to failing to create it. It changes nothing about who can address the name. |
| any local process (same desktop) → the registered broadcast window message received by `MainForm.WndProc` | Unchanged and untouched. `MainForm.cs` is not in this plan's `files_modified`. |
| the losing process → the winning process | Unchanged. Still one-way and payload-free. |
| (absent) network, credentials, secrets, package installs | This plan makes no network call, reads no credential, and runs no package-manager install. |

Assessed at ASVS level 1, block-on threshold `high`. No threat below is rated `high`; nothing blocks. Note that 25-REVIEW.md rates CR-01 `Critical` on a DEFECT-severity axis (crash-causing, blocker for merge); the register below rates the same issue on the STRIDE impact-times-likelihood axis for a single-user, local, non-networked desktop utility, where a self-inflicted crash of a duplicate launch is genuinely `medium`. The two ratings do not conflict — they measure different things, and the issue is being mitigated here either way.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-20 | Denial of Service | `SingleInstanceGuard.WaitForInstanceReady`'s readiness wait | medium | mitigate | This is CR-01. A primary that dies between `Acquire()` and `MarkReady()` makes the OS mark the readiness mutex abandoned; the concurrently-waiting duplicate's `WaitOne` throws, nothing catches it, and the duplicate dies with an unhandled exception. The user's outcome is the worst available: the primary is already dead, so they get no window AND a crash dialog. Mitigated in Task 1 by one narrowly-typed `catch (AbandonedMutexException)` inside the guard, treating the abandoned-but-acquired wait as the successful wait it actually is, which routes control into the existing release branch. Deliberately NOT mitigated by a process-wide unhandled-exception handler — that would mask the class rather than close it and would leave the mutex abandoned for the next waiter, converting a single crash into a chain. Regression-netted by a new CI fact proven RED before the fix. |
| T-25-21 | Denial of Service | readiness-mutex construction inside `SingleInstanceGuard.Acquire()` | medium | mitigate | This is WR-02. A construction failure propagated out of the static `Acquire()` before its own `TryLog` line ran — so the app died on EVERY launch, including autostart, with zero diagnostic trail and a leaked already-owned main mutex handle. Same never-crash-on-startup contract as T-25-06, same class of defect as T-25-20, opposite end of the same file. Mitigated in Task 2 Part A by degrading to a logged null readiness handle, a state `MarkReady()`, `Dispose()`, the loser's fail-fast wait and `Program.cs`'s unconditional broadcast all already support and already cover. The broad catch is scoped strictly to this construction; the main mutex's correctness-critical narrow catch is explicitly not widened, and a grep criterion enforces that. |
| T-25-22 | Tampering | `Global\` to `Local\` namespace divergence between two processes | medium | accept | This is WR-01. Two processes whose tokens disagree about `Global\` access each resolve a different namespace, each see `createdNew == true`, and each act as primary — two live instances writing `mode.json`, `settings.json` and the monitor/audio state with no coordination, silently defeating INSTANCE-01. Accepted at ASVS L1: the trigger requires deliberately mismatched security contexts (one elevated launch and one not, or two login/RDP sessions) on a single-user personal rig where every launch is the same non-elevated user in one session, and it has never been observed. Not accepted SILENTLY — Task 2 Part C records it in the source next to the fallback, with its trigger and the deferred stronger fix (probe the opposite namespace before concluding primary) both named, so the doc no longer reads as though the fallback is unconditionally safe. Carried as this plan's backstop truth. |
| T-25-23 | Repudiation | the CR-01 regression test itself | medium | mitigate | The realistic failure mode of THIS plan is not attack, it is self-deception: a regression test that passes with the bug still present, after which the phase is declared closed on evidence that was never evidence. The specific trap is real and is written down in the source artifacts — 25-VERIFICATION.md and 25-REVIEW.md both suggest simulating abandonment via `guard.Dispose()`, which releases the readiness mutex cleanly and therefore goes green either way. Mitigated by four controls: the abandonment mechanism is owning-thread exit, which genuinely abandons; the RED observation is a mandatory numbered step with its output pasted verbatim into the SUMMARY; the `Dispose()`-based construction is an explicit prohibition; and a grep criterion requires `new Thread(` to be present in the test file. |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable to this plan: zero package-manager install steps in any task, and zero `PackageReference` entries added, removed, or version-changed — asserted mechanically by Task 2's criterion that `RigToggle.Core.csproj` still contains exactly its one pre-existing prohibition-comment occurrence. `AbandonedMutexException` is BCL (`System.Threading`), consistent with the milestone's recorded zero-new-NuGet decision. No RESEARCH-side Package Legitimacy Audit table is required because no install task exists. Row retained per protocol. |
</threat_model>

<verification>

**Automated (must all pass before Task 3 is raised):**
1. `dotnet build src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` exits 0 with `0 Warning(s)`.
2. `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -c Release` reports `Failed: 0` and 121 passing facts (120 pre-existing plus the new one).
3. That same command run twice back to back gives the identical result — the new fact's deliberately-unreleased guard leaks into nothing.
4. `grep -cE 'catch \(AbandonedMutexException' src/RigToggle.Core/SingleInstanceGuard.cs` outputs `1`.
5. `grep -c 'IsPrimaryInstance' src/RigToggle.App/Program.cs` outputs `1` — D-02's single branch intact, `Program.cs` untouched.
6. `git diff --name-only` across the whole plan lists exactly three source files: `src/RigToggle.Core/SingleInstanceGuard.cs`, `src/RigToggle.Tests/SingleInstanceGuardTests.cs`, `src/RigToggle.App/UpdateApplyEntryPoint.cs`.

**Evidence that is NOT automatable here and is Task 3's job:**
- The seven `SingleInstanceProcessTests` facts run to a stable green three times consecutively on real Windows hardware. `RigToggle.Windows.Tests` requires the `Microsoft.WindowsDesktop.App` runtime, which the Linux build environment does not have — this is a pre-existing property of the repository, not something this plan introduces, and it must not be worked around by weakening or relocating those tests.
- The three `ApplyUpdateBypass_*` facts confirmed green specifically. 25-03-SUMMARY.md records their status as "unknown," which is the outstanding evidence for UPDATE-07.
- No crash dialog on a real-hardware reproduction of the CR-01 race.

**Explicitly out of scope, recorded so its absence is deliberate:**
- WR-01's cross-namespace probe. Documented, deferred, carried as a backstop truth.

</verification>

<success_criteria>

This plan is complete when:

1. `WaitForInstanceReady` catches `AbandonedMutexException`, treats it as a successful acquisition, releases the mutex it was handed, and logs the case distinctly — and the fix lives inside the guard, not behind a process-wide exception handler.
2. `WaitForInstanceReady_PrimaryAbandonedReadinessMutex_ReturnsTrueWithoutThrowing` exists, produces abandonment by owning-thread exit, was observed RED naming `AbandonedMutexException` before the fix with that output recorded verbatim in the SUMMARY, passes after, uses its own locally-generated instance id, and runs unskipped in CI.
3. `Acquire()`'s readiness-mutex construction degrades to a logged null handle instead of escaping the method.
4. The class doc comment states what is actually true about abandoned mutexes in this class and names the fact that proves it; the namespace-fallback paragraph records WR-01 as a known accepted limitation with its trigger and deferred fix named; `UpdateApplyEntryPoint`'s ordering claim matches `Program.cs`.
5. `Program.cs` is unmodified, the main mutex's narrow catch is unmodified, WR-01's structural fix is not implemented, and no `PackageReference` changed.
6. The cross-platform suite reports `Failed: 0` with exactly one more passing fact than before, twice in a row.
7. The operator has reported three consecutive `dotnet test RigToggle.sln -c Release --no-build` runs from the Windows rig with `Failed: 0` each time, the three `ApplyUpdateBypass_*` facts explicitly confirmed green, and no crash dialog during the Check 2 reproduction — with that report recorded verbatim in the SUMMARY.

</success_criteria>

<output>
Create `.planning/phases/25-single-instance-guard/25-04-SUMMARY.md` when done.

It MUST contain, beyond the standard template:
- The verbatim RED test output from Task 1 Step 2, showing the failure and `AbandonedMutexException`. Without it, must-have truth 3 is unmet no matter how green the final run is.
- The verbatim GREEN output from Task 1 Step 4.
- An explicit note that no test was added for WR-02's construction-failure path, and why (no injection seam; creating one would widen the public API further than the guard it would cover).
- An explicit note that WR-01's structural fix was deferred, with the rationale, so the next verifier reads it as a decision rather than an omission.
- The operator's verbatim Task 3 report: three run summary lines, the `ApplyUpdateBypass_*` result, any flakiness observed, whether a crash dialog appeared, and whether the abandoned-case log line was seen in `debug.log`.
</output>
