---
phase: 13-tray-app-icon-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.IconGen/RigToggle.IconGen.csproj
  - src/RigToggle.IconGen/Program.cs
  - src/RigToggle.IconGen/IconGeometry.cs
  - src/RigToggle.IconGen/IconWriter.cs
  - RigToggle.sln
  - src/RigToggle.App/Resources/normal.ico
  - src/RigToggle.App/Resources/rig.ico
  - src/RigToggle.App/Resources/app.ico
autonomous: true
requirements: [ICON-01, ICON-02, ICON-03, ICON-04]

must_haves:
  truths:
    - "A monitor silhouette (normal.ico) and a tri-spoke steering-wheel silhouette (rig.ico) are distinguishable by shape alone at 16px, procedurally drawn via GDI+ (D-01 motif, D-02 flat Fluent style, D-03 procedural drawing)"
    - "Both tray icons are white-filled with a black outline tracing every boundary, legible on light and dark taskbars (D-04 monochrome-only differentiation, D-05 self-contained-contrast outline mechanism)"
    - "normal.ico and rig.ico each contain 4 frames (16/20/24/32px); app.ico contains 6 frames (16/20/24/32/48/256px)"
    - "app.ico reuses normal.ico's monitor geometry at a larger size with a #2D2D30 body and #005A9E screen-glass accent (D-06 exe icon reuses normal-mode motif, D-07 exe icon full-color treatment)"
    - "Every generated .ico round-trips: new Icon(path, new Size(s,s)) loads without throwing and reports the requested size for every embedded frame"
  artifacts:
    - path: "src/RigToggle.IconGen/RigToggle.IconGen.csproj"
      provides: "Dev-time-only console generator project; NOT referenced by RigToggle.App"
      contains: "UseWindowsForms"
    - path: "src/RigToggle.IconGen/IconGeometry.cs"
      provides: "DrawNormalIcon/DrawRigIcon/DrawAppIcon GDI+ drawing per UI-SPEC fractions"
      min_lines: 60
    - path: "src/RigToggle.IconGen/IconWriter.cs"
      provides: "Hand-rolled multi-frame ICONDIR/ICONDIRENTRY + BMP-in-ICO writer"
      min_lines: 40
    - path: "src/RigToggle.App/Resources/normal.ico"
      provides: "4-frame monochrome monitor tray icon"
    - path: "src/RigToggle.App/Resources/rig.ico"
      provides: "4-frame monochrome steering-wheel tray icon"
    - path: "src/RigToggle.App/Resources/app.ico"
      provides: "6-frame color exe/taskbar icon (monitor motif)"
  key_links:
    - from: "src/RigToggle.IconGen/Program.cs"
      to: "src/RigToggle.App/Resources/*.ico"
      via: "atomic file write (.tmp + File.Move overwrite) via relative path"
      pattern: "File\\.Move"
    - from: "src/RigToggle.IconGen/Program.cs"
      to: "System.Drawing.Icon round-trip self-verification"
      via: "new Icon(path, new Size(s,s)) assertion loop"
      pattern: "new (System\\.Drawing\\.)?Icon\\("
---

<objective>
Create the dev-time-only `RigToggle.IconGen` console project that procedurally draws the three icons per `13-UI-SPEC.md`'s locked geometry and packs them into standards-compliant multi-frame `.ico` files, then run it to (re)generate the three checked-in assets under `src/RigToggle.App/Resources/`.

Purpose: Deliver the artwork that satisfies ICON-01 (monitor-vs-wheel shape contrast), ICON-02 (white-fill/black-outline self-contained contrast), the multi-frame half of ICON-03 (16/20/24/32px tray frames, plus 48/256px for the exe icon), and the artwork half of ICON-04 (app.ico reuses the monitor motif in color). The runtime DPI-selection fix and `<ApplicationIcon>` wiring that complete ICON-03/ICON-04 are Plan 13-02.
Output: A new `src/RigToggle.IconGen/` project (registered in `RigToggle.sln`, NOT referenced by `RigToggle.App`) plus regenerated `normal.ico`/`rig.ico` and a new `app.ico`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tray-app-icon-redesign/13-CONTEXT.md
@.planning/phases/13-tray-app-icon-redesign/13-RESEARCH.md
@.planning/phases/13-tray-app-icon-redesign/13-UI-SPEC.md
@.planning/phases/13-tray-app-icon-redesign/13-PATTERNS.md

<interfaces>
<!-- The concrete code source for the two no-in-repo-analog files lives in RESEARCH.md.
     Copy these directly; they are complete, cross-referenced implementations. -->
- IconWriter (WriteIco + EncodeBmpInIco): 13-RESEARCH.md Pattern 3, lines 237-329.
  Handles Pitfall 3 (256px width/height byte = 0) and Pitfall 4 (bottom-up BGRA rows,
  32-bit-padded 1bpp AND mask, doubled biHeight).
- IconGeometry (redraw-per-size GDI+): 13-RESEARCH.md Pattern 1 (lines 175-201) +
  Pattern 2 (lines 205-221), driven by 13-UI-SPEC.md Icon Geometry Contract (lines 65-107).
- Existing csproj shape to mirror: src/RigToggle.App/RigToggle.App.csproj (WinExe form).
- Atomic-write shape to mirror: src/RigToggle.Core/Persistence/JsonSettingsStore.cs:84-86.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the RigToggle.IconGen console project and the ICO writer</name>
  <files>src/RigToggle.IconGen/RigToggle.IconGen.csproj, src/RigToggle.IconGen/Program.cs, src/RigToggle.IconGen/IconWriter.cs, RigToggle.sln</files>
  <read_first>
    - src/RigToggle.App/RigToggle.App.csproj (csproj shape to mirror; note it has ProjectReferences and EmbeddedResource blocks that IconGen must NOT copy)
    - src/RigToggle.App/Program.cs (entry-point/[STAThread] shape only, lines ~34-36; do NOT copy its DI/Application.Run wiring)
    - src/RigToggle.Core/Persistence/JsonSettingsStore.cs (atomic-write pattern, lines 76-87)
    - 13-RESEARCH.md Pattern 3 (lines 237-329) — the WriteIco/EncodeBmpInIco source of truth
    - 13-PATTERNS.md sections on RigToggle.IconGen.csproj, Program.cs, IconWriter.cs
  </read_first>
  <action>
    Create `src/RigToggle.IconGen/RigToggle.IconGen.csproj` as `<Project Sdk="Microsoft.NET.Sdk">` with `OutputType=Exe` (console, NOT WinExe), `TargetFramework=net10.0-windows`, `UseWindowsForms=true` (this is how System.Drawing/GDI+ types are obtained with zero new PackageReferences — the same mechanism RigToggle.App already uses), `ImplicitUsings=enable`, `Nullable=enable`. Do NOT add `RuntimeIdentifier`, any `<ProjectReference>`, or any `<EmbeddedResource>` — this project references nothing and embeds nothing; it writes files to disk. Register it in the solution with `dotnet sln add src/RigToggle.IconGen/RigToggle.IconGen.csproj` (let the CLI generate the GUID and config rows rather than hand-editing RigToggle.sln).
    Create `IconWriter.cs` with the hand-rolled ICO writer from 13-RESEARCH.md Pattern 3: `WriteIco(Stream, IReadOnlyList<Bitmap>)` (6-byte ICONDIR header + 16-byte ICONDIRENTRY per frame with offsets computed up front + concatenated frame bytes) and `EncodeBmpInIco(Bitmap)` (40-byte BITMAPINFOHEADER with biHeight doubled, bottom-up BGRA rows written in reverse via LockBits/Marshal.Copy on Format32bppArgb, and an all-zero 1bpp AND mask padded to `((w+31)/32)*4` bytes per row). Preserve Pitfall 3's explicit `(byte)(bmp.Width >= 256 ? 0 : bmp.Width)` (and same for height) — do NOT simplify it to a plain cast. Preserve Pitfall 4's reverse row loop and mask padding exactly.
    Create `Program.cs` with a `[STAThread] static void Main` stub (no message loop, no `Application.Run`) — full generation wiring lands in Task 3; for now it may be an empty body or a `return` so the project compiles.
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.IconGen/RigToggle.IconGen.csproj -c Debug; dotnet sln list | grep -i RigToggle.IconGen; grep -c "RigToggle.IconGen" src/RigToggle.App/RigToggle.App.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/RigToggle.IconGen/RigToggle.IconGen.csproj` exits 0
    - RigToggle.IconGen.csproj contains `<OutputType>Exe</OutputType>` and `<UseWindowsForms>true</UseWindowsForms>`
    - RigToggle.IconGen.csproj contains NO `<ProjectReference` and NO `<PackageReference` element
    - `dotnet sln list` output includes `RigToggle.IconGen`
    - `grep -c "RigToggle.IconGen" src/RigToggle.App/RigToggle.App.csproj` prints `0` (App never references the generator)
    - IconWriter.cs contains `EncodeBmpInIco` and the literal `>= 256 ? 0` guard for both width and height bytes
    - IconWriter.cs writes AND-mask rows padded via `((w + 31) / 32) * 4` and iterates pixel rows in reverse (`y = h - 1; y >= 0; y--`)
  </acceptance_criteria>
  <done>The generator project compiles, is in the solution, is not referenced by RigToggle.App, and contains a complete multi-frame ICO writer.</done>
</task>

<task type="auto">
  <name>Task 2: Draw the three icon silhouettes per the UI-SPEC geometry contract</name>
  <files>src/RigToggle.IconGen/IconGeometry.cs</files>
  <read_first>
    - 13-UI-SPEC.md Icon Geometry Contract (lines 65-107) and Color table (lines 46-62) — the LOCKED fractions and hex values
    - 13-UI-SPEC.md Icon Canvas & Geometry Grid governing rules (lines 34-43) — design in fractions, redraw per size, ≥2px minimum feature, transparent background
    - 13-RESEARCH.md Pattern 1 (lines 175-201) and Pattern 2 (lines 205-221) — GDI+ drawing + contiguous-silhouette outline
    - 13-RESEARCH.md Pitfall 2 (lines 381-386) — anti-aliasing thin-feature softness at 16px
  </read_first>
  <action>
    Create `IconGeometry.cs` with three methods returning a fresh `Bitmap` per call: `DrawNormalIcon(int size)`, `DrawRigIcon(int size)`, `DrawAppIcon(int size)`. Each creates a `new Bitmap(size, size, PixelFormat.Format32bppArgb)`, sets `g.SmoothingMode = SmoothingMode.AntiAlias`, `g.PixelOffsetMode = PixelOffsetMode.HighQuality`, `g.Clear(Color.Transparent)`, and draws using coordinates computed as UI-SPEC fractions × `size` (redraw per size — never scale one master bitmap, per governing rule 2).
    normal.ico geometry (fill `#FFFFFF`, outline `#000000`): Screen = rounded rect at (0.125W,0.125H) size 0.75W×0.5H radius 0.06W; Neck = sharp rect at (0.4375W,0.625H) size 0.125W×0.125H; Base = rounded bar at (0.28W,0.75H) size 0.44W×0.125H radius 0.03W. Add all three sub-shapes to ONE `GraphicsPath` so a single `DrawPath(outlinePen, path)` traces the contiguous silhouette (screen ∪ neck ∪ base) with no internal seams (Pattern 2).
    rig.ico geometry (fill `#FFFFFF`, outline `#000000`): center (0.5W,0.5H); outer rim radius 0.40W, inner cutout radius 0.26W (ring thickness 0.14W), hub disc radius 0.12W, 3 spokes width 0.14W at 180°/60°/300° clockwise from 12 o'clock, each radiating from hub edge (0.12W) to inner rim edge (0.26W). Build rim as filled outer ellipse minus inner cutout ellipse (FillMode.Alternate two concentric AddEllipse), add hub + 3 spokes to the same path so the whole silhouette (rim ∪ hub ∪ spokes minus inner cutout) fills and outlines as one unit.
    app.ico geometry: reuse normal.ico's exact screen/neck/base fractions, but fill bezel/neck/base with `#2D2D30` and add a screen-glass inset rect filled `#005A9E`, inset 0.06W from the screen's inner edge on all sides. NO black outline on app.ico (flat color fill only, per UI-SPEC Color section).
    Use an outline pen width of `Math.Max(1f, size / 16f)` as the starting value on the two tray icons (tunable at the rig checkpoint per Pitfall 2 / Open Question 2 — do not over-pin).
  </action>
  <verify>
    <automated>dotnet build src/RigToggle.IconGen/RigToggle.IconGen.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/RigToggle.IconGen/RigToggle.IconGen.csproj` exits 0
    - IconGeometry.cs defines `DrawNormalIcon`, `DrawRigIcon`, and `DrawAppIcon`, each accepting an `int size`
    - Every Bitmap is created with `PixelFormat.Format32bppArgb` (grep confirms no `Format8bppIndexed`/paletted usage)
    - Source sets `SmoothingMode.AntiAlias` and `PixelOffsetMode.HighQuality` on every Graphics
    - normal.ico and app.ico share the same screen/neck/base fraction constants (ICON-04 motif reuse — the monitor shape is not re-derived differently)
    - app.ico drawing uses fills `#2D2D30` (45,45,48) and `#005A9E` (0,90,158) and adds NO outline pen; tray icons use `#FFFFFF` fill + `#000000` outline only (no other colors)
    - rig.ico draws exactly 3 spokes at 180/60/300 degrees
  </acceptance_criteria>
  <done>All three draw methods render their UI-SPEC-locked silhouettes at an arbitrary size and compile.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Program.cs to generate all three .ico files with atomic write + round-trip self-verification</name>
  <files>src/RigToggle.IconGen/Program.cs, src/RigToggle.App/Resources/normal.ico, src/RigToggle.App/Resources/rig.ico, src/RigToggle.App/Resources/app.ico</files>
  <read_first>
    - src/RigToggle.Core/Persistence/JsonSettingsStore.cs (atomic .tmp + File.Move(overwrite:true) shape, lines 76-87)
    - 13-RESEARCH.md Assumptions Log A4 (line 422) — mandatory round-trip load verification before the human-verify step
    - 13-UI-SPEC.md Multi-Resolution .ico File Contract (lines 111-121) — required frame sizes per file
    - 13-PATTERNS.md IconWriter.cs section (atomic-write requirement, round-trip verification requirement)
  </read_first>
  <action>
    Implement `Main`: for each icon, build the frame list by calling the corresponding `IconGeometry` draw method once per required size, pack via `IconWriter.WriteIco` into a `MemoryStream`, then write atomically to disk. Required sizes: `normal.ico` and `rig.ico` = {16,20,24,32}; `app.ico` = {16,20,24,32,48,256}. Resolve output paths relative to the project (write to `../RigToggle.App/Resources/normal.ico`, `rig.ico`, `app.ico`) — mirror JsonSettingsStore's atomic pattern adapted to binary: write bytes to `path + ".tmp"` then `File.Move(tempPath, path, overwrite: true)` so an interrupted run never leaves a half-written `.ico` checked into git.
    After writing each file, perform round-trip self-verification (Assumption A4): for every size embedded in that file, construct `new System.Drawing.Icon(path, new Size(s, s))` and assert the returned icon's `Width`/`Height` equals `s`; if any assertion fails, print a diagnostic and set a non-zero exit code (`Environment.Exit(1)`). This is the automated gate that catches malformed ICO byte layout (Pitfall 3/4) before any human verification. Print a one-line success summary per file on success.
    Run the generator once (`dotnet run --project src/RigToggle.IconGen`) so the three regenerated/new `.ico` assets are produced and committed as part of this plan.
  </action>
  <verify>
    <automated>dotnet run --project src/RigToggle.IconGen/RigToggle.IconGen.csproj; echo "exit=$?"; dotnet build RigToggle.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project src/RigToggle.IconGen` exits 0 (round-trip self-verification passed for all frames of all three files)
    - `src/RigToggle.App/Resources/normal.ico`, `rig.ico`, and `app.ico` all exist and are non-empty after the run
    - Program.cs writes via a temp path then `File.Move(..., overwrite: true)` (no direct overwrite of the final path)
    - Program.cs constructs `new System.Drawing.Icon(path, new Size(...))` per embedded size and asserts the reported size matches (grep confirms the sized Icon constructor + a size assertion are present)
    - Program.cs generates app.ico with the 48 and 256 sizes present (grep confirms 48 and 256 in the app.ico size set)
    - `dotnet build RigToggle.sln` exits 0 (no project broken by the new project)
  </acceptance_criteria>
  <done>The generator runs clean, produces three valid multi-frame .ico files that round-trip-load at every embedded size, and the solution still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time generator → checked-in binary assets | The hand-rolled ICO writer produces bytes that the shipped app loads at startup. No external/untrusted input crosses here — all inputs are the app's own procedurally-computed bitmaps and hardcoded constants. |
| new dev project → self-contained publish output | The generator must stay OUT of `RigToggle.App`'s publish; a stray reference would ship dev tooling in the .exe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial of Service (self-inflicted) | IconWriter byte layout (bottom-up rows, padded AND mask, doubled biHeight, 256px byte=0) | mitigate | Task 3's round-trip self-verification loads every frame via `new Icon(path, new Size(s,s))` and asserts size, exiting non-zero on any failure — a malformed .ico fails the build gate before it can crash the app at startup (preserves the existing `LoadTrayIconsIfNeeded` InvalidOperationException path as backstop) |
| T-13-02 | Elevation / scope creep | RigToggle.IconGen accidentally swept into the self-contained publish | mitigate | No `<ProjectReference>` from RigToggle.App to IconGen; no `<RuntimeIdentifier>` on IconGen; Task 1 asserts `grep -c RigToggle.IconGen src/RigToggle.App/RigToggle.App.csproj` == 0 |
| T-13-03 | Tampering / supply chain | New third-party package pulled into the build | mitigate | System.Drawing/GDI+ obtained via `UseWindowsForms=true` only; Task 1 asserts IconGen.csproj contains no `<PackageReference>` |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase adds ZERO package-manager installs (confirmed 13-RESEARCH.md Package Legitimacy Audit); no `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint applies |
</threat_model>

<verification>
- `dotnet build RigToggle.sln -c Debug` exits 0.
- `dotnet run --project src/RigToggle.IconGen` exits 0 with round-trip self-verification passing for all frames.
- The three `.ico` files exist under `src/RigToggle.App/Resources/`.
- RigToggle.App.csproj contains no reference to RigToggle.IconGen.
- No `<PackageReference>` introduced anywhere.
</verification>

<success_criteria>
- A dev-time-only `RigToggle.IconGen` project exists, builds, is in the solution, and is not referenced by RigToggle.App.
- `normal.ico` (4 frames), `rig.ico` (4 frames), and `app.ico` (6 frames) are regenerated/created with the UI-SPEC-locked geometry and colors.
- Every generated frame round-trip-loads at its requested size.
- Monitor-vs-wheel shape contrast (ICON-01), white-fill/black-outline contrast (ICON-02), multi-frame sizing (ICON-03 asset half), and the app.ico monitor motif in color (ICON-04 asset half) are all delivered as artwork.
</success_criteria>

<output>
Create `.planning/phases/13-tray-app-icon-redesign/13-01-SUMMARY.md` when done.
</output>
