---
phase: 20-custom-toggle-switch-control
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/RigToggle.App/MainForm.Designer.cs
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.App/MonitorConfirmDialog.Designer.cs
autonomous: true
requirements: [THEME-08]

must_haves:
  truths:
    - "MainForm hosts the ToggleSwitch in btnToggle's exact former slot — same position in the Controls.Add sequence, same reading order (tile row → Identify → toggle row → Settings gear), same gap formulas above and below (D-09, MAIN-01/02 preserved)"
    - "btnToggle and lblMode no longer exist anywhere in the solution's executable code — field, Designer block, Controls.Add, every handler, and both layout constants are gone in the same change that wires the switch in (UI-SPEC Deletion order)"
    - "RefreshUi() pushes mode into the switch instead of two control texts: Rig → On, Normal → Off, unknown → Indeterminate, and the tray menu item's text is computed directly from the same branch rather than read off a deleted button (D-06, D-07, D-08)"
    - "Every gate in the toggle click path — unknown-mode refusal, WR-01 unconfigured-Settings refusal, DISPLAY-07 confirmation dialog, CORE-04 partial-failure checklist, ToggleInProgressException branch — survives the port to the switch's ActionRequested handler byte-identically"
    - "The switch is themed from both MainForm theming call sites via ApplyDashboardTheming(), so it can never freeze at the startup theme while its neighbours keep flipping (Pitfall 1)"
    - "LayoutDashboard() collapses the vertical space lblMode vacated and sizes the switch row from a new ToggleRowHeightPx constant, with every value still passing through Scaled()"
  artifacts:
    - path: "src/RigToggle.App/MainForm.Designer.cs"
      provides: "toggleSwitch field, instantiation, ActionRequested wiring, and Controls.Add in btnToggle's former slot; lblMode/btnToggle fully removed"
      contains: "toggleSwitch"
    - path: "src/RigToggle.App/MainForm.cs"
      provides: "ToggleSwitch_ActionRequested (verbatim port of BtnToggle_Click), switch-driven RefreshUi, ToggleRowHeightPx layout, ThemeToggleSwitch inside ApplyDashboardTheming"
      contains: "ToggleSwitch_ActionRequested"
  key_links:
    - from: "src/RigToggle.App/MainForm.Designer.cs"
      to: "src/RigToggle.App/MainForm.cs"
      via: "the switch's ActionRequested event is wired to the ported click handler"
      pattern: "toggleSwitch\\.ActionRequested \\+="
    - from: "src/RigToggle.App/MainForm.cs"
      to: "src/RigToggle.App/ThemeApplier.cs"
      via: "ApplyDashboardTheming — the single helper both theming call sites invoke"
      pattern: "ThemeApplier\\.ThemeToggleSwitch"
    - from: "src/RigToggle.App/MainForm.cs"
      to: "RigToggle.App.Controls.ToggleSwitchState"
      via: "RefreshUi maps the three mode states onto the switch"
      pattern: "SetState\\(ToggleSwitchState\\."
---

<objective>
Swap the `ToggleSwitch` in for `btnToggle` on `MainForm` and delete `lblMode` in the same change: Designer field/instantiation/wiring, the ported click handler, the mode→state mapping in `RefreshUi()`, the collapsed layout, and the theming hookup.

Purpose: this is the risky half of the phase. `btnToggle` is the control that actually performs mode changes, and its click handler carries four separate user-protection gates (DISPLAY-11 unknown-mode refusal, WR-01 unconfigured-Settings refusal, DISPLAY-07 confirmation dialog, CORE-04 partial-failure reporting). All four must survive a verbatim port. Simultaneously `lblMode` — today's "Mode: Unknown" signal — is deleted, so the switch's Indeterminate state becomes the sole carrier of DISPLAY-11's "never guess Rig or Normal" rule.
Output: `MainForm.Designer.cs` and `MainForm.cs` fully migrated, plus one stale cross-reference comment fixed in `MonitorConfirmDialog.Designer.cs`. Solution builds, all 81 tests pass.
</objective>

<hard_constraints>
Load-bearing rules. Violating any one fails the phase even if the window looks right.

1. `BtnToggle_Click`'s body is ported VERBATIM into `ToggleSwitch_ActionRequested` — same control flow, same order, same four gates, same three locked message strings, same `ToggleInProgressException` and generic `catch` branches. Only the method name changes. Do not "simplify", reorder, merge, or reword anything inside it.
2. `btnToggle` and `lblMode` are removed TOGETHER with the switch wiring in place, in this one plan — never left half-migrated (20-UI-SPEC.md "Deletion order").
3. `ThemeApplier.ThemeToggleSwitch` must be reachable from BOTH `MainForm.OnThemeChanged` and `MainForm.InitializeTrayState()`. Achieve this by calling it from inside `ApplyDashboardTheming()` (which both already invoke) — do NOT add two separate direct calls that can drift. This is the bug class this codebase shipped twice in Phase 12 (Pitfall 1).
4. `LayoutDashboard()` keeps its "every value passes through `Scaled()`" discipline. No bare `new Size(<digit>` / `new Point(<digit>` literal may appear in that method (Pitfall 9).
5. The switch occupies `btnToggle`'s exact index in the `Controls.Add` sequence (after `btnIdentify`, before `btnSettings`), preserving the D-09 reading/tab order the phase inherits from Phase 19.
6. Do not touch `MonitorConfirmDialog`'s behavior, `ToggleOrchestrator`, `ToggleService`, `IMonitorController`, or any settings/mode-store code. This phase is presentation-layer only.
7. `MainForm` acquires no new `SystemEvents` subscription and no new field beyond the Designer's `toggleSwitch` (ARCHITECTURE.md Anti-Pattern 2).
</hard_constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-custom-toggle-switch-control/20-CONTEXT.md
@.planning/phases/20-custom-toggle-switch-control/20-UI-SPEC.md
@.planning/phases/20-custom-toggle-switch-control/20-PATTERNS.md
@.planning/phases/20-custom-toggle-switch-control/20-01-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
Contracts and exact edit sites the executor needs (extracted from the codebase — no exploration required):

- From Plan 01: `public enum ToggleSwitchState { Off, On, Indeterminate }` and `public sealed class ToggleSwitch : UserControl` in namespace `RigToggle.App.Controls`, with `public void SetState(ToggleSwitchState state)` and `public event EventHandler? ActionRequested`. `MainForm.cs` already has `using RigToggle.App.Controls;` at line 3.
- `ThemeApplier.ThemeToggleSwitch(ToggleSwitch toggleSwitch, bool dark)` (Plan 01, Task 2).
- COMPLETE list of every `btnToggle` / `lblMode` / `TogglePx` / `ModeLabelHeightPx` site in the solution (verified by grep, 2026-08-10):
  - `MainForm.Designer.cs`: 61, 62 (instantiation), 78-85 (`lblMode` block), 88-105 (`btnToggle` block), 201 (comment referencing `btnToggle.Text`), 283 (comment referencing `lblMode`), 286, 290 (`Controls.Add`), 298, 299 (field declarations)
  - `MainForm.cs`: 74 (`ModeLabelHeightPx`), 82 (`TogglePx`), 160 (`OnThemeChanged` theming), 253 (`InitializeTrayState` theming), 363-364, 371-372, 383 (`RefreshUi`), 898-906 (`LayoutDashboard` lblMode block + `stripTop`), 919-920, 926 (`LayoutDashboard` btnToggle block), 1012 + 1033-1058 (`BtnToggle_Paint`/`_Enter`/`_Leave` and the `DrawButtonFocusRing` doc comment referencing btnToggle)
  - `MonitorConfirmDialog.Designer.cs`: 76 (comment cross-referencing `MainForm.Designer.cs`'s btnToggle comment, which this plan deletes)
- `MainForm.LayoutDashboard()` current relevant lines (864-928): `lblMode.Location`/`Size`, `int stripTop = margin + Scaled(ModeLabelHeightPx) + Scaled(GapSmPx);`, `btnToggle.Size = new Size(contentWidth, Scaled(TogglePx));`, `btnToggle.Location = new Point(margin, btnIdentify.Bottom + Scaled(GapSmPx));`, `btnSettings.Location = new Point(margin + contentWidth - btnSettings.Width, btnToggle.Bottom + Scaled(GapLgPx));`, `ClientSize = new Size(contentWidth + 2 * margin, btnSettings.Bottom + margin);`
- `MainForm.ApplyDashboardTheming()` (lines 944-959) is invoked from `OnThemeChanged` (line 165) and `InitializeTrayState()` (line 265) — both already exist and must not change shape.
- The three locked message strings that must survive the port byte-identically:
  - `Rig Toggle's current mode is unknown — fix or delete the mode file, then restart the app, before toggling.`
  - `Please choose at least one monitor to disable or enable in Settings before switching to Rig Mode.`
  - `The toggle did not fully complete:` (the CORE-04 checklist prefix)
- Tray menu copy (unchanged, only its source moves): `Switch to Rig Mode` / `Switch to Normal Mode` / `Toggle`.
- `DrawButtonFocusRing` stays — `btnIdentify` and `btnSettings` still use it. Only `BtnToggle_Paint`'s call site is deleted.
- Build command: `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true`
- Test command: `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` — baseline `Failed: 0, Passed: 81, Total: 81`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainForm.Designer.cs — replace the lblMode/btnToggle declarations with the toggleSwitch control</name>
  <files>src/RigToggle.App/MainForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.Designer.cs (the file being modified — the `lblMode` block at 78-85, the `btnToggle` block at 88-105 including its FlatStyle/#13897 rationale comment, the `trayToggleMenuItem` comment at 200-201, the `Controls.Add` sequence and its D-09 comment at 282-291, and the field declarations at 298-299)
    - src/RigToggle.App/Controls/ToggleSwitch.cs (Plan 01 — the exact type name, namespace, and `ActionRequested` event signature being wired here)
    - .planning/phases/20-custom-toggle-switch-control/20-PATTERNS.md (the "MainForm.Designer.cs (modified — config)" section, which gives the exact replacement block shape)
  </read_first>
  <action>
    THIS TASK LEAVES THE SOLUTION IN A DELIBERATELY NON-COMPILING STATE. After it, `MainForm.cs` still references `lblMode`, `btnToggle`, `BtnToggle_Click`, `BtnToggle_Paint`, `BtnToggle_Enter`, and `BtnToggle_Leave`, and `ToggleSwitch_ActionRequested` does not exist yet — Task 2 resolves all of it. Do NOT patch `MainForm.cs` from here, and do NOT re-add a shim to make the build green early.

    Edit `src/RigToggle.App/MainForm.Designer.cs`:

    1. In `InitializeComponent()`'s instantiation block, delete `this.lblMode = new System.Windows.Forms.Label();` and `this.btnToggle = new System.Windows.Forms.Button();`, and add `this.toggleSwitch = new RigToggle.App.Controls.ToggleSwitch();` at the position `btnToggle`'s line occupied (so the declaration order still reads tile-row-then-actions).

    2. Delete the entire `// lblMode` comment-and-configuration block (Text, TextAlign, Location, Size, AutoSize, Name).

    3. Delete the entire `// btnToggle` comment-and-configuration block, including its long 12-05/THEME-05/#13897 `FlatStyle.Flat` rationale comment and all four event wirings (`Click`, `Paint`, `Enter`, `Leave`). Before deleting, note that this comment is cross-referenced from `MonitorConfirmDialog.Designer.cs` — Task 2 redirects that reference; do not delete anything in that file from here.

    4. In its place add a `// toggleSwitch` block, following the same declare-then-configure-then-wire ordering every other control in this file uses:
       - `this.toggleSwitch.Name = "toggleSwitch";`
       - `this.toggleSwitch.Size = new System.Drawing.Size(288, 32);`
       - `this.toggleSwitch.Location = new System.Drawing.Point(16, 148);`
       - `this.toggleSwitch.ActionRequested += new System.EventHandler(this.ToggleSwitch_ActionRequested);`
       Add a comment block above it stating: THEME-08/D-01 — this replaces the 288x40 `btnToggle` with a compact Settings-style toggle row (label left, pill switch right); the control is owner-drawn and code-only (`Controls/ToggleSwitch.cs`), so unlike `btnToggle` it needs no `FlatStyle`/`FlatAppearance` workaround — the dotnet/winforms#13897 rationale that used to live here now applies only to the remaining stock `Button`s and is documented on `ThemeApplier.ThemeButton`. Also state that the Location/Size above are PLACEHOLDER defaults which `LayoutDashboard()` (MainForm.cs) overwrites on every population and hotplug, using the same wording the `btnIdentify`/`tileStrip` blocks already carry — do not "fix" these Designer coordinates.

    5. Update the `trayToggleMenuItem` comment (currently "Text left at a safe default; RefreshUi() overrides it every call with btnToggle.Text so the tray menu and the GUI button never drift (D-04)"): `btnToggle` no longer exists, so state instead that `RefreshUi()` computes this text directly from the same `isInRigMode` branch that sets the switch's state, so the tray menu and the switch can never drift. The literal default text `"Switch to Rig Mode"` stays exactly as-is.

    6. In the `Controls.Add` sequence, delete `this.Controls.Add(this.lblMode);` and replace `this.Controls.Add(this.btnToggle);` with `this.Controls.Add(this.toggleSwitch);` — same index, so reading and tab order stay tile row → Identify → toggle → Settings. Update the D-09 comment above the sequence: drop the sentence about `lblMode` staying above the tile row (it is deleted this phase, D-06) and state that the toggle row is now MainForm's sole mode readout.

    7. Delete the field declarations `private System.Windows.Forms.Label lblMode;` and `private System.Windows.Forms.Button btnToggle;`, and add `private RigToggle.App.Controls.ToggleSwitch toggleSwitch;` at the position `btnToggle`'s declaration occupied.

    Do NOT change: `Dispose(bool)`, the `components` container, `tileStrip`, `lblNoMonitors`, `btnIdentify`, `btnSettings`, any tray member, `AutoScaleMode`, `ClientSize`, or the form-level property block.
  </action>
  <verify>
    <automated>test "$(grep -c 'btnToggle' src/RigToggle.App/MainForm.Designer.cs)" = "0" && test "$(grep -c 'lblMode' src/RigToggle.App/MainForm.Designer.cs)" = "0" && test "$(grep -c 'private RigToggle.App.Controls.ToggleSwitch toggleSwitch;' src/RigToggle.App/MainForm.Designer.cs)" = "1" && test "$(grep -c 'this.Controls.Add(this.toggleSwitch);' src/RigToggle.App/MainForm.Designer.cs)" = "1" && test "$(grep -c 'this.toggleSwitch.ActionRequested += new System.EventHandler(this.ToggleSwitch_ActionRequested);' src/RigToggle.App/MainForm.Designer.cs)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'btnToggle' src/RigToggle.App/MainForm.Designer.cs` outputs `0` and `grep -c 'lblMode' src/RigToggle.App/MainForm.Designer.cs` outputs `0` — including in comments.
    - `grep -c 'this.toggleSwitch = new RigToggle.App.Controls.ToggleSwitch();' src/RigToggle.App/MainForm.Designer.cs` outputs `1`.
    - `grep -c 'private RigToggle.App.Controls.ToggleSwitch toggleSwitch;' src/RigToggle.App/MainForm.Designer.cs` outputs `1`.
    - `grep -c 'this.toggleSwitch.ActionRequested += new System.EventHandler(this.ToggleSwitch_ActionRequested);' src/RigToggle.App/MainForm.Designer.cs` outputs `1`.
    - Tab/reading order preserved: `grep -n 'this.Controls.Add' src/RigToggle.App/MainForm.Designer.cs` lists exactly five entries in this order — `tileStrip`, `lblNoMonitors`, `btnIdentify`, `toggleSwitch`, `btnSettings`.
    - The switch is not given a stock-Button workaround it does not need: `grep -c 'toggleSwitch.FlatStyle\|toggleSwitch.FlatAppearance' src/RigToggle.App/MainForm.Designer.cs` outputs `0`.
    - The placeholder-coordinate warning is present: `awk '/\/\/ toggleSwitch/,/ActionRequested/' src/RigToggle.App/MainForm.Designer.cs | grep -ci 'PLACEHOLDER'` is at least `1`.
    - Nothing else was disturbed: `grep -c 'this.tileStrip' src/RigToggle.App/MainForm.Designer.cs` is unchanged and non-zero, `grep -c 'btnIdentify' src/RigToggle.App/MainForm.Designer.cs` is non-zero, `grep -c 'btnSettings' src/RigToggle.App/MainForm.Designer.cs` is non-zero, and `grep -c 'DisplaySettingsChanged' src/RigToggle.App/MainForm.Designer.cs` outputs `1`.
    - `git diff --stat src/RigToggle.App/` shows exactly one changed file (`MainForm.Designer.cs`).
    - The build is EXPECTED to fail after this task with errors confined to `MainForm.cs` (unresolved `lblMode`, `btnToggle`, and `ToggleSwitch_ActionRequested`). Record the error list in the summary; do not fix any of it here.
  </acceptance_criteria>
  <done>`MainForm.Designer.cs` declares, configures, wires, and adds `toggleSwitch` in `btnToggle`'s exact former slot; `lblMode` and `btnToggle` are gone from that file entirely, including their comments; the two stale comments (tray-menu source, D-09 reading order) now describe the new arrangement.</done>
</task>

<task type="auto">
  <name>Task 2: MainForm.cs — port the click handler, map mode onto the switch, collapse the layout, and theme it from both call sites</name>
  <files>src/RigToggle.App/MainForm.cs, src/RigToggle.App/MonitorConfirmDialog.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/MainForm.cs (the file being modified — constants at 70-84, `OnThemeChanged` at 148-172, `InitializeTrayState` at 240-266, `RefreshUi` at 354-384, `BtnToggle_Click` at 386-530 (the body being ported verbatim), `LayoutDashboard` at 864-937, `ApplyDashboardTheming` at 944-959, `DrawButtonFocusRing` at 1010-1030, `BtnToggle_Paint`/`_Enter`/`_Leave` at 1032-1058)
    - src/RigToggle.App/MainForm.Designer.cs (as edited in Task 1 — the `toggleSwitch` field name and the handler name it wires to)
    - src/RigToggle.App/Controls/ToggleSwitch.cs (Plan 01 — `ToggleSwitchState` member names and `SetState`'s signature)
    - src/RigToggle.App/MonitorConfirmDialog.Designer.cs lines 68-80 (the stale cross-reference to the btnToggle comment Task 1 deleted)
    - .planning/phases/20-custom-toggle-switch-control/20-UI-SPEC.md (Layout Integration section — the resolved "collapse, don't preserve" decision for the space lblMode vacates; Copywriting Contract — the tray-menu integration note)
  </read_first>
  <action>
    Edit `src/RigToggle.App/MainForm.cs`. After this task the solution must build clean and all 81 tests must pass.

    1. Constants block (lines 70-84): delete `private const int ModeLabelHeightPx = 20;` and `private const int TogglePx = 40;`. Add `private const int ToggleRowHeightPx = 32;` in `TogglePx`'s position, with a comment: 20-UI-SPEC.md Geometry Contract — the toggle row is 288x32 (was a 288x40 button); it is shorter because it is now a Settings-style row rather than a big primary-action button (D-01), and the switch's own track/thumb geometry is derived inside the control from this row height.

    2. `OnThemeChanged` (line 160): delete `ThemeApplier.ThemeButton(btnToggle, IsDark);`. Add nothing here — the switch is themed via `ApplyDashboardTheming()` two lines below (step 6). Leave `ThemeApplier.ThemeButton(btnSettings, IsDark);`, the `SetColorMode` call, `DwmTitleBar.ApplyRoundedCornersAndMica`, and `Refresh()` exactly as they are.

    3. `InitializeTrayState()` (line 253): delete `ThemeApplier.ThemeButton(btnToggle, IsDark);` for the same reason. Leave the `btnSettings` line and update the surrounding comment ("theme both buttons at load time too") so it names what is actually themed there now.

    4. `RefreshUi()` (lines 354-384) — the mode→UI mapping, per D-06/D-07/D-08:
       - Unknown branch: delete `lblMode.Text = "Mode: Unknown";` and `btnToggle.Text = "Toggle";`. Add `toggleSwitch.SetState(ToggleSwitchState.Indeterminate);`. Keep `trayToggleMenuItem.Text = "Toggle";` and `notifyIcon.Text = "Rig Toggle — Mode Unknown";` and the early `return` unchanged. Extend the existing DISPLAY-11/T-16-09 comment: `lblMode` is gone (D-06), so the switch's Indeterminate position — mid-track, filled gray, distinct from both real states — is now the sole carrier of the "never default to either Rig or Normal wording" rule.
       - Known branch: delete `lblMode.Text = ...;` and `btnToggle.Text = ...;`. Add `toggleSwitch.SetState(isInRigMode ? ToggleSwitchState.On : ToggleSwitchState.Off);` with a comment naming D-08 (Rig = on/right/filled, Normal = the app's baseline everywhere else and therefore the neutral off/left position).
       - Replace `trayToggleMenuItem.Text = btnToggle.Text;` with `trayToggleMenuItem.Text = isInRigMode ? "Switch to Normal Mode" : "Switch to Rig Mode";`. The copy is unchanged — only its source moves from "read another control's Text" to "computed alongside the switch's SetState call". Keep the D-04 one-source-of-truth intent in the comment, restated for the new source.
       - Leave the `notifyIcon.Icon` / `notifyIcon.Text` lines untouched.

    5. Rename `BtnToggle_Click` to `ToggleSwitch_ActionRequested` — same `(object? sender, EventArgs e)` signature — and port its body VERBATIM (hard constraint 1). Every one of these must be byte-identical after the rename: the `IsModeKnown()` refusal and its locked message, the `IsInRigMode()` branch calling `ToggleToNormalMode()`, the `IsSettingsConfigured()` WR-01 refusal and its locked message, the `SkipMonitorConfirmation` gate with its `GetAllMonitors()` name resolution and traced fallback, `MonitorConfirmDialog.ShowDialog(this)` and the `DontAskAgain` save, `ToggleToRigMode()`, the trailing `RefreshUi()`, the CORE-04 `!result.Success` checklist dialog, the `ToggleInProgressException` branch, and the generic `catch (Exception ex)` branch. Do not add a state-push to the switch inside this handler — `RefreshUi()` already does it, and duplicating it would create a second place mode can be rendered.

    6. `ApplyDashboardTheming()` (lines 944-959): add `ThemeApplier.ThemeToggleSwitch(toggleSwitch, IsDark);` alongside the existing tile loop and button calls. Extend the method's doc comment to name the switch as one of the controls the two-call-site rule now covers, and state explicitly that routing it through this single helper — rather than adding two direct calls in `OnThemeChanged` and `InitializeTrayState()` — is what makes the two call sites structurally unable to drift (Pitfall 1, hard constraint 3).

    7. `LayoutDashboard()` (lines 864-937):
       - Delete the `lblMode.Location` / `lblMode.Size` lines and the "lblMode is deliberately KEPT above the tile row" comment block above them.
       - Change `int stripTop = margin + Scaled(ModeLabelHeightPx) + Scaled(GapSmPx);` to `int stripTop = margin;`, with a comment recording 20-UI-SPEC.md's resolved discretion: the ~28px `lblMode` vacated is COLLAPSED, not preserved as breathing room — once the switch row is the sole mode readout (D-06), an empty gap there would read as an unexplained layout hole, so the tile strip now starts flush with the top margin and the window stays exactly as tall as its content needs.
       - Replace `btnToggle.Size` / `btnToggle.Location` with `toggleSwitch.Size = new Size(contentWidth, Scaled(ToggleRowHeightPx));` and `toggleSwitch.Location = new Point(margin, btnIdentify.Bottom + Scaled(GapSmPx));` — identical gap formula, so MAIN-01's "directly below the tile row" placement is unchanged.
       - Replace `btnToggle.Bottom` with `toggleSwitch.Bottom` in the `btnSettings.Location` line. Leave the `GapLgPx` gap and the MAIN-02 comment above it untouched — that larger gap is the de-emphasis cue and must survive.
       - Leave `ClientSize = new Size(contentWidth + 2 * margin, btnSettings.Bottom + margin);` and the try/catch unchanged.

    8. Delete `BtnToggle_Paint`, `BtnToggle_Enter`, and `BtnToggle_Leave` (lines 1032-1058) entirely, including `BtnToggle_Paint`'s doc comment. Keep `DrawButtonFocusRing` — `btnIdentify` and `btnSettings` still call it — but edit its doc comment, which currently says "unlike btnToggle (still native FlatAppearance rendering, unaffected)": `btnToggle` no longer exists, so restate the sentence in terms of the two buttons that still use the helper, and note that the toggle switch draws its own pill-shaped focus ring inside `ToggleSwitch.OnPaint` because a rectangle is the wrong shape for a pill.

    9. `src/RigToggle.App/MonitorConfirmDialog.Designer.cs` line 76: the comment "see MainForm.Designer.cs's btnToggle comment for the full rig-finding + #13897 rationale" now points at deleted text. Redirect it to `ThemeApplier.ThemeButton`'s doc comment, which carries the same rig finding and dotnet/winforms#13897 rationale and is not going anywhere. Change nothing else in that file — `btnContinue.FlatStyle` and every other line stay as-is.

    Do NOT: add a `toggleSwitch` null guard (it is Designer-instantiated like every other control), cache `IsDark` in a field, add a `SystemEvents` subscription, change `ToggleOrchestrator`/`ToggleService`/`IMonitorController`, change `MonitorConfirmDialog`'s behavior, or alter any of the three locked message strings.
  </action>
  <verify>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true && PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true</automated>
  </verify>
  <acceptance_criteria>
    - `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` exits 0 and reports `0 Error(s)`.
    - `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0` with `Total: 81`.
    - Full retirement — `grep -rn 'btnToggle\|lblMode\|TogglePx\|ModeLabelHeightPx' src/ --include=*.cs | wc -l` outputs `0` (zero hits solution-wide, comments included).
    - `grep -c 'private const int ToggleRowHeightPx = 32;' src/RigToggle.App/MainForm.cs` outputs `1`.
    - The ported handler exists and the old one does not: `grep -c 'private void ToggleSwitch_ActionRequested(object? sender, EventArgs e)' src/RigToggle.App/MainForm.cs` outputs `1`; `grep -c 'BtnToggle_' src/RigToggle.App/MainForm.cs` outputs `0`.
    - All four gates survived the port — inside `awk '/private void ToggleSwitch_ActionRequested/,/^        }$/' src/RigToggle.App/MainForm.cs`, each of these greps outputs `1`: `IsModeKnown()`, `current mode is unknown — fix or delete the mode file`, `IsSettingsConfigured()`, `Please choose at least one monitor to disable or enable in Settings`, `SkipMonitorConfirmation`, `new MonitorConfirmDialog(`, `DontAskAgain`, `ToggleToRigMode()`, `ToggleToNormalMode()`, `The toggle did not fully complete:`, `catch (ToggleInProgressException ex)`.
    - The handler still calls `RefreshUi()` exactly once and pushes no state itself: the same awk range greps `1` for `RefreshUi();` and `0` for `SetState(`.
    - Mode mapping is correct and complete: `grep -c 'toggleSwitch.SetState(ToggleSwitchState.Indeterminate);' src/RigToggle.App/MainForm.cs` outputs `1`; `grep -c 'toggleSwitch.SetState(isInRigMode ? ToggleSwitchState.On : ToggleSwitchState.Off);' src/RigToggle.App/MainForm.cs` outputs `1`; and `grep -c 'toggleSwitch.SetState' src/RigToggle.App/MainForm.cs` outputs `2` (no third render path exists).
    - Tray menu text is computed, not read off a control: `grep -c 'trayToggleMenuItem.Text = isInRigMode ? "Switch to Normal Mode" : "Switch to Rig Mode";' src/RigToggle.App/MainForm.cs` outputs `1`, and `grep -c 'trayToggleMenuItem.Text = "Toggle";' src/RigToggle.App/MainForm.cs` outputs `1`.
    - Two-call-site theming holds (Pitfall 1): `awk '/private void ApplyDashboardTheming/,/^        }$/' src/RigToggle.App/MainForm.cs | grep -c 'ThemeApplier.ThemeToggleSwitch'` outputs `1`; `awk '/private void OnThemeChanged/,/^        }$/' src/RigToggle.App/MainForm.cs | grep -c 'ApplyDashboardTheming'` outputs `1`; `awk '/public void InitializeTrayState/,/^        }$/' src/RigToggle.App/MainForm.cs | grep -c 'ApplyDashboardTheming'` outputs `1`; and `grep -c 'ThemeApplier.ThemeToggleSwitch' src/RigToggle.App/MainForm.cs` outputs `1` (exactly one call site, reached from both).
    - Layout collapsed and DPI discipline intact: `awk '/private void LayoutDashboard/,/^        }$/' src/RigToggle.App/MainForm.cs | grep -c 'int stripTop = margin;'` outputs `1`; the same awk range greps `1` for `toggleSwitch.Size = new Size(contentWidth, Scaled(ToggleRowHeightPx));`, `1` for `btnIdentify.Bottom + Scaled(GapSmPx)`, and `1` for `toggleSwitch.Bottom + Scaled(GapLgPx)`; and `awk '/private void LayoutDashboard/,/^        }$/' src/RigToggle.App/MainForm.cs | grep -vE '^\s*(//|\*|/\*)' | grep -cE 'new (Size|Point)\([0-9]'` outputs `0`.
    - `DrawButtonFocusRing` survived for the two buttons that still need it: `grep -c 'private static void DrawButtonFocusRing' src/RigToggle.App/MainForm.cs` outputs `1` and `grep -c 'DrawButtonFocusRing(e.Graphics' src/RigToggle.App/MainForm.cs` outputs `2` (Identify and Settings only).
    - No new subscription or cached theme state: `grep -c 'SystemEvents.' src/RigToggle.App/MainForm.cs` outputs `1` (the single pre-existing `DisplaySettingsChanged` subscription), and `grep -cE 'private bool _isDark|_isDark =' src/RigToggle.App/MainForm.cs` outputs `0`.
    - The stale cross-reference is fixed: `grep -c 'btnToggle' src/RigToggle.App/MonitorConfirmDialog.Designer.cs` outputs `0` and `grep -c 'ThemeApplier.ThemeButton' src/RigToggle.App/MonitorConfirmDialog.Designer.cs` is at least `1`.
    - Domain layer untouched: `git status --porcelain src/RigToggle.Core src/RigToggle.Windows` outputs nothing.
  </acceptance_criteria>
  <done>The solution builds clean with `btnToggle` and `lblMode` fully gone; the switch sits in their slot, receives mode through `RefreshUi()`'s three branches, raises the verbatim-ported gated click handler, is themed from both call sites through the single `ApplyDashboardTheming()` helper, and the layout collapses the vacated space while keeping every dimension inside `Scaled()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (click / Space / Enter) → mode mutation | `ToggleSwitch.ActionRequested` now reaches the handler that calls `ToggleOrchestrator.ToggleToRigMode()` / `ToggleToNormalMode()`, which disable and enable real displays and switch the default audio endpoint. This is the phase's one privileged path. |
| Persisted mode file → rendered switch position | `IsModeKnown()`/`IsInRigMode()` read the `IModeStore` flag; a missing or corrupted file must render as Indeterminate, never as a confident Rig or Normal. |
| `ISettingsStore` → confirmation dialog + `SkipMonitorConfirmation` write | Unchanged from today, but the write now happens inside a renamed handler; a dropped branch here would silently bypass DISPLAY-07's informed consent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | A user-protection gate lost while porting `BtnToggle_Click` (unknown-mode refusal, WR-01 refusal, DISPLAY-07 confirmation, CORE-04 reporting) | mitigate | Hard constraint 1 requires a verbatim port; Task 2's acceptance criteria grep the ported handler body for all eleven gate markers and the three locked message strings, and the rig checkpoint in Plan 03 exercises each gate live |
| T-20-06 | Spoofing | Unknown mode rendering as a confident Rig or Normal now that `lblMode` (today's "Mode: Unknown" signal) is deleted | mitigate | `RefreshUi()`'s unknown branch pushes `ToggleSwitchState.Indeterminate` (grep-asserted), the switch renders it mid-track and filled-gray (Plan 01), and the click gate still refuses to toggle from unknown with the locked message |
| T-20-07 | Repudiation | Tray menu text drifting from the switch's rendered state after losing `btnToggle.Text` as the shared source | mitigate | Both are computed from the same `isInRigMode` local in the same `RefreshUi()` branch; grep-asserted single computation, no cross-control read |
| T-20-08 | Denial of Service | An exception in `LayoutDashboard()` or the theming path crashing the toggle flow | mitigate | Both remain inside their existing try/catch with `Trace.WriteLine` / cosmetic-only swallow; this plan adds no new unguarded path |
| T-20-09 | Elevation of Privilege | A second mutation call site appearing during the swap | accept | Structurally impossible here: `ToggleSwitch` holds no controller reference (Plan 01, grep-asserted) and this plan renames one handler rather than adding one; Plan 03's audit re-confirms `DeactivateMonitors`/`ActivateMonitors` call-site counts are unchanged |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs zero external packages; the `PackageReference` count-zero criterion in Plan 01 enforces it |
</threat_model>

<verification>
- `PATH="$HOME/.dotnet:$PATH" dotnet build RigToggle.sln -p:EnableWindowsTargeting=true` exits 0 with `0 Error(s)`.
- `PATH="$HOME/.dotnet:$PATH" dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj -p:EnableWindowsTargeting=true` reports `Failed: 0, Total: 81`.
- `grep -rn 'btnToggle\|lblMode\|TogglePx\|ModeLabelHeightPx' src/ --include=*.cs | wc -l` outputs `0`.
- `git status --porcelain src/` lists exactly three modified files: `MainForm.cs`, `MainForm.Designer.cs`, `MonitorConfirmDialog.Designer.cs`.
- Visual/interactive correctness (row proportions, three-state legibility, live theme flip, `--tray` hidden start, 125%/150% scaling, keyboard-only toggle, gate dialogs) is NOT provable in this Linux build environment and is deferred to Plan 03.
</verification>

<success_criteria>
- The Rig/Normal action on `MainForm` is the custom `ToggleSwitch`, not a `Button` (success criterion 1).
- `btnToggle` and `lblMode` have zero remaining references anywhere in `src/`.
- All three mode states map onto the switch, with unknown mapping to Indeterminate.
- The ported handler retains all four user-protection gates and all three locked message strings verbatim.
- `ThemeApplier.ThemeToggleSwitch` is reached from both `OnThemeChanged` and `InitializeTrayState()` through the single `ApplyDashboardTheming()` helper.
- `LayoutDashboard()` collapses the vacated space, sizes the row from `ToggleRowHeightPx`, and contains no bare pixel literal.
- Build clean, 81 tests passing, no change to `RigToggle.Core` or `RigToggle.Windows`.
</success_criteria>

<output>
Create `.planning/phases/20-custom-toggle-switch-control/20-02-SUMMARY.md` when done, recording the build/test output, the verbatim-port evidence for the four gates, the final `LayoutDashboard()` vertical arithmetic, and confirmation that the two-call-site theming rule is satisfied through a single call site.
</output>
