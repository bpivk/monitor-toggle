---
phase: 06-multi-monitor-data-model-controller-generalization
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/RigToggle.App/SettingsForm.Designer.cs
  - src/RigToggle.App/SettingsForm.cs
autonomous: true
requirements: [DISPLAY-04, DISPLAY-05, DISPLAY-06]
must_haves:
  truths:
    - "Settings shows one grid row per monitor from GetAllMonitors(), with Disable and Enable checkbox columns"
    - "Checking Disable on a row live-unchecks Enable on that same row (and vice versa) on a single click (D-04)"
    - "Save is blocked with a clear message when the config would leave no monitor active (DISPLAY-06/D-05) or when neither set has any selection (D-07)"
    - "A saved-but-currently-undetected monitor is preserved (not dropped) and surfaced via a non-blocking warning"
  artifacts:
    - path: "src/RigToggle.App/SettingsForm.Designer.cs"
      provides: "dgvMonitors DataGridView + two checkbox columns, layout shifted per UI-SPEC"
      contains: "dgvMonitors"
    - path: "src/RigToggle.App/SettingsForm.cs"
      provides: "grid population, D-04 mutual exclusivity, D-05/D-06 validation, merged-set save"
      contains: "WouldLeaveAtLeastOneMonitorActive"
  key_links:
    - from: "SettingsForm.PopulateMonitorGrid"
      to: "IMonitorController.GetAllMonitors"
      via: "one grid row per enumerated (active + OS-disabled) monitor"
      pattern: "GetAllMonitors"
    - from: "SettingsForm.ValidateSettingsForm"
      to: "WouldLeaveAtLeastOneMonitorActive predicate"
      via: "DISPLAY-06 save gate"
      pattern: "WouldLeaveAtLeastOneMonitorActive"
---

<objective>
Replace `SettingsForm`'s single monitor `ComboBox` with the `DataGridView` grid contract from `06-UI-SPEC.md` (D-03): one row per monitor from `GetAllMonitors()`, two checkbox columns (Disable / Enable) with live single-click mutual exclusivity (D-04), the DISPLAY-06 / D-07 save-time validation, and merged-set save that preserves saved-but-disconnected monitors. This is the primary UI surface for DISPLAY-04 (choose N monitors to disable) and DISPLAY-05 (choose a normally-disabled monitor to enable).

Purpose: Give the user a single grid that ties both monitor sets to the same enumerated monitor list, with the exact validation and stale-handling behavior the UI-SPEC locks.
Output: Updated `SettingsForm.Designer.cs` (layout) and `SettingsForm.cs` (population/events/validation/save).

ENVIRONMENT: `RigToggle.App` is `net10.0-windows`; it does NOT build on a non-Windows box, and after Plan 02 it won't fully compile until Plan 05 also lands (MainForm). Do NOT rely on `dotnet build src/RigToggle.App` as this plan's verify — use the source assertions in each `<acceptance_criteria>`. Full App build + visual behavior are validated at the Plan 06 rig checkpoint. Follow the system-default WinForms styling constraint: NO custom colors/fonts/icons; represent "OS-disabled" as a plain-text row suffix only (06-UI-SPEC.md Color/Typography sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-CONTEXT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md

<interfaces>
IMonitorController (from Plan 02): GetAllMonitors() -> IReadOnlyList<MonitorInfo>, plus GetActiveMonitors().
MonitorInfo (from Plan 01): (string DevicePath, string FriendlyName, bool IsPrimary, bool IsActive).
AppSettings (from Plan 01): List<string>? MonitorsToDisable, List<string>? MonitorsToEnable (+ legacy + audio + CompanionAppPath + SkipMonitorConfirmation + EnableDebugLogging).

Existing SettingsForm members to reuse/replace (src/RigToggle.App/SettingsForm.cs):
  PopulateMonitorPicker (59-117) — REPLACE with grid population.
  ShowStaleWarning (203-209) — generalize to a non-blocking list warning for the grid case.
  ValidateSettingsForm (211-219) — replace monitorOk gate; keep audioNormalOk/audioRigOk/appPathOk.
  BtnSaveSettings_Click (295-339) — replace monitor persistence (single Id) with merged-set persistence + SetEquals skip-reset.
  Constructor event wiring (40-44) — add dgvMonitors handlers.

Existing Designer (src/RigToggle.App/SettingsForm.Designer.cs):
  grpMonitor (73-78, size 396x76), cboMonitor (81-86, to be removed), lblMonitorWarning (89-95),
  grpAudioDevices (100), grpAppPath (164), chkEnableDebugLogging (209), btnSaveSettings (218), btnDiscardChanges (228), ClientSize (253 = 420x424).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grid layout in SettingsForm.Designer.cs (replace cboMonitor with dgvMonitors)</name>
  <files>src/RigToggle.App/SettingsForm.Designer.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.Designer.cs (full — grpMonitor/cboMonitor/lblMonitorWarning declarations + Controls.Add order + ClientSize)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md ("Grid Spec" DataGridView configuration + "Exact layout coordinates" table)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md (SettingsForm Designer.cs pattern note)
  </read_first>
  <action>
    Remove the `cboMonitor` `ComboBox` field, its instantiation, its `//cboMonitor` config block, and its `grpMonitor.Controls.Add(this.cboMonitor)`. Add a `DataGridView dgvMonitors` field with three columns: `DataGridViewTextBoxColumn colMonitorName` (`ReadOnly=true`, `AutoSizeMode=Fill`), `DataGridViewCheckBoxColumn colDisable` (`Width=60`), `DataGridViewCheckBoxColumn colEnable` (`Width=60`). Configure `dgvMonitors` EXACTLY per 06-UI-SPEC.md Grid Spec: `AllowUserToAddRows=false`, `AllowUserToDeleteRows=false`, `AllowUserToResizeRows=false`, `AllowUserToResizeColumns=false`, `RowHeadersVisible=false`, `SelectionMode=CellSelect`, `MultiSelect=false`, `ColumnHeadersHeightSizeMode=DisableResizing`, `EditMode=EditOnEnter`; column header texts "Monitor"/"Disable"/"Enable"; do NOT set any custom `Font`/`ColumnHeadersDefaultCellStyle.Font`/colors (system default only). Apply the exact coordinates from the UI-SPEC "Exact layout coordinates" table: `grpMonitor` (12,12) size (396,176); `dgvMonitors` at (12,22) size (372,120); `lblMonitorWarning` at (12,148) size (372,20); `grpAudioDevices` -> (12,200); `grpAppPath` -> (12,344); `chkEnableDebugLogging` -> (12,426); `btnSaveSettings` -> (180,476); `btnDiscardChanges` -> (298,476); `SettingsForm.ClientSize` -> (420,524). Add the columns to `dgvMonitors.Columns` and `dgvMonitors` to `grpMonitor.Controls`. Keep `errMonitor` and `lblMonitorWarning` (both reused). Preserve all other unchanged form properties (`FixedDialog`, `AutoScaleMode.Font`, `CenterParent`, etc.).
  </action>
  <verify>
    <automated>grep -q "dgvMonitors" src/RigToggle.App/SettingsForm.Designer.cs && grep -q "colDisable" src/RigToggle.App/SettingsForm.Designer.cs && grep -q "colEnable" src/RigToggle.App/SettingsForm.Designer.cs && ! grep -q "this.cboMonitor" src/RigToggle.App/SettingsForm.Designer.cs && grep -q "new System.Drawing.Size(420, 524)" src/RigToggle.App/SettingsForm.Designer.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dgvMonitors` (DataGridView), `colMonitorName`, `colDisable`, `colEnable` are all declared; `cboMonitor` is fully removed (`grep -c "cboMonitor" src/RigToggle.App/SettingsForm.Designer.cs` returns 0).
    - `dgvMonitors` sets `RowHeadersVisible=false`, `AllowUserToAddRows=false`, `EditMode = ... EditOnEnter` and no custom Font/color assignments on the grid.
    - `SettingsForm.ClientSize` is `(420, 524)`; `grpAudioDevices` y=200; `grpAppPath` y=344; `btnSaveSettings`/`btnDiscardChanges` y=476 (controls shifted +100 per UI-SPEC).
    - `lblMonitorWarning` remains present at (12,148).
  </acceptance_criteria>
  <done>The Designer declares the three-column grid at the UI-SPEC coordinates with all controls below shifted +100 and the ComboBox removed.</done>
</task>

<task type="auto">
  <name>Task 2: Grid population + D-04 mutual exclusivity + non-blocking stale handling</name>
  <files>src/RigToggle.App/SettingsForm.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.cs (PopulateMonitorPicker 59-117, PopulateAudioCombo 137-177, ShowStaleWarning 203-209, constructor 26-44)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md (Grid Spec: row-label format, D-04 mechanism, "Currently OS-disabled" indication, "Stale saved-monitor handling", Empty state, Copywriting rows)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md (Pitfall 5 DataGridView commit-on-click, Pitfall 1 GetAllMonitors)
  </read_first>
  <action>
    Add a private field to hold this-open's enumerated monitors (e.g. `IReadOnlyList<MonitorInfo> _allMonitors`) and a reentrancy-guard bool for the programmatic checkbox write. Replace `PopulateMonitorPicker` with `PopulateMonitorGrid`: call `_monitorController.GetAllMonitors()` inside a `try/catch(Exception) { degrade to empty }` (reuse the existing degrade idiom). Empty/failed enumeration → `dgvMonitors.Rows.Clear()`, `dgvMonitors.Enabled=false`, `lblMonitorWarning.Text="No displays detected."`, `Visible=true` (do NOT use `errMonitor.SetError` for this informational empty-state — matches the old ComboBox empty behavior). Otherwise add one row per monitor: column 0 text = `FriendlyName` + suffix `" (Primary)"` if `IsPrimary` else `" (currently OS-disabled)"` if `!IsActive` else none (exactly one suffix or none, per Copywriting). Pre-check `colDisable`/`colEnable` cells from `_settings.MonitorsToDisable`/`MonitorsToEnable` membership by `DevicePath` (store the `DevicePath` in each row's `Tag` or a hidden value — key by stable DevicePath, NEVER row index, per REQUIREMENTS Out of Scope). Compute stale device paths = saved set entries whose `DevicePath` is not enumerated by `GetAllMonitors()`; if any, show a NON-BLOCKING `lblMonitorWarning` using the UI-SPEC "Stale saved-monitor warning" copy (full comma-separated list, no truncation) and do NOT call `errMonitor.SetError` / do NOT disable Save (document this deliberate divergence from the old blocking ComboBox behavior inline with a why-comment).
    Wire D-04 (Pitfall 5): in the constructor add `dgvMonitors.CurrentCellDirtyStateChanged += (s,e) => { if (dgvMonitors.IsCurrentCellDirty && dgvMonitors.CurrentCell is DataGridViewCheckBoxCell) dgvMonitors.CommitEdit(DataGridViewDataErrorContexts.Commit); };` and `dgvMonitors.CellValueChanged += OnMonitorCellValueChanged`. In `OnMonitorCellValueChanged`: guard against reentrancy with the bool flag; if the changed cell is in `colDisable` and its new value is `true`, set the same row's `colEnable` value to `false` (and symmetric for `colEnable`->`colDisable`); wrap the programmatic sibling write in the reentrancy flag so it doesn't recurse; then call `ValidateSettingsForm()`. Remove the old `cboMonitor.SelectedIndexChanged` wiring from the constructor; keep the audio combo wiring.
  </action>
  <verify>
    <automated>grep -q "GetAllMonitors" src/RigToggle.App/SettingsForm.cs && grep -q "CurrentCellDirtyStateChanged" src/RigToggle.App/SettingsForm.cs && grep -q "CommitEdit(DataGridViewDataErrorContexts.Commit)" src/RigToggle.App/SettingsForm.cs && grep -q "currently OS-disabled" src/RigToggle.App/SettingsForm.cs && ! grep -q "cboMonitor" src/RigToggle.App/SettingsForm.cs</automated>
  </verify>
  <acceptance_criteria>
    - Grid is populated from `GetAllMonitors()` (not `GetActiveMonitors()`): `grep -c "GetAllMonitors" src/RigToggle.App/SettingsForm.cs` >= 1 and no `cboMonitor` references remain.
    - D-04 wiring present: both `CurrentCellDirtyStateChanged` (with `CommitEdit(DataGridViewDataErrorContexts.Commit)`) and `CellValueChanged` handlers exist, and the sibling-uncheck write is reentrancy-guarded (a bool flag set/cleared around the programmatic `Value` assignment).
    - Row label suffix logic includes both `" (Primary)"` and `" (currently OS-disabled)"` strings.
    - Stale handling does NOT call `errMonitor.SetError` in the stale case and does NOT disable Save (a why-comment documents the divergence); rows are keyed by `DevicePath`, not index.
    - Empty-state sets `lblMonitorWarning.Text = "No displays detected."` and `dgvMonitors.Enabled = false`.
  </acceptance_criteria>
  <done>The grid renders one row per enumerated monitor with correct suffixes, enforces D-04 mutual exclusivity on a single click, and surfaces stale saved monitors non-blockingly.</done>
</task>

<task type="auto">
  <name>Task 3: DISPLAY-06/D-07 validation + merged-set save with SetEquals skip-reset</name>
  <files>src/RigToggle.App/SettingsForm.cs</files>
  <read_first>
    - src/RigToggle.App/SettingsForm.cs (ValidateSettingsForm 211-219, BtnSaveSettings_Click 295-339, IsValidLaunchTarget)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-UI-SPEC.md (Validation contract priority order + Copywriting error strings + "Don't ask again" reset condition)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md (D-05 predicate code example, Pitfall 4 SetEquals)
    - src/RigToggle.Core/ToggleService.cs (MonitorStateUnchanged lines 164-165 — the in-repo "don't trust default collection equality" precedent)
  </read_first>
  <action>
    Add a helper to read the current grid selection into two `HashSet<string>` of DevicePaths (`disableSelected`, `enableSelected`) from checked `colDisable`/`colEnable` cells (keyed by each row's stored `DevicePath`). Add `private bool WouldLeaveAtLeastOneMonitorActive(IReadOnlyList<MonitorInfo> allMonitors, HashSet<string> disable, HashSet<string> enable)` exactly per 06-RESEARCH.md: `bool anySurvivingActive = allMonitors.Any(m => m.IsActive && !disable.Contains(m.DevicePath)); return anySurvivingActive || enable.Count > 0;`.
    Rewrite `ValidateSettingsForm`'s monitor gate to the UI-SPEC priority order (only one `lblMonitorWarning`/`errMonitor` message visible at a time): (1) DISPLAY-06 gate — if `!WouldLeaveAtLeastOneMonitorActive(...)` show the exact "This configuration would leave no monitor active. At least one monitor must stay enabled after switching to Rig Mode." message and disable Save; else (2) non-empty gate — if `disableSelected.Count == 0 && enableSelected.Count == 0` show "Select at least one monitor to disable or enable." and disable Save; else (3) if stale saved paths exist show the non-blocking stale warning (does NOT disable Save); else (4) clear `lblMonitorWarning`/`errMonitor`. Combine with the UNCHANGED `audioNormalOk && audioRigOk && appPathOk` gates for `btnSaveSettings.Enabled`.
    Rewrite the monitor portion of `BtnSaveSettings_Click`: compute the persisted sets as `(previously-saved entries NOT currently enumerated) ∪ (currently-checked rows' DevicePaths)` for BOTH `MonitorsToDisable` and `MonitorsToEnable` (stale/disconnected entries pass through untouched — do NOT drop them). Replace the single-string `monitorChanged` skip-reset with a set comparison: `bool monitorsChanged = !new HashSet<string>(_settings.MonitorsToDisable ?? new()).SetEquals(mergedDisable) || !new HashSet<string>(_settings.MonitorsToEnable ?? new()).SetEquals(mergedEnable);` (order-independent, mirrors `ToggleService.MonitorStateUnchanged`). Build the saved `AppSettings` with `MonitorsToDisable = mergedDisable.ToList()`, `MonitorsToEnable = mergedEnable.ToList()`, keep the legacy `MonitorDevicePath`/`MonitorFriendlyName` fields untouched (do not repopulate them — migration source only) or leave them as previously loaded, set `SkipMonitorConfirmation = monitorsChanged ? false : _settings.SkipMonitorConfirmation`, and keep audio/app/debug fields exactly as today. Remove the old `cboMonitor.SelectedItem` reads and the single-monitor `GetActiveMonitors().FirstOrDefault` friendly-name re-resolution.
  </action>
  <verify>
    <automated>grep -q "WouldLeaveAtLeastOneMonitorActive" src/RigToggle.App/SettingsForm.cs && grep -q "SetEquals" src/RigToggle.App/SettingsForm.cs && grep -q "would leave no monitor active" src/RigToggle.App/SettingsForm.cs && grep -q "Select at least one monitor to disable or enable" src/RigToggle.App/SettingsForm.cs</automated>
  </verify>
  <acceptance_criteria>
    - `WouldLeaveAtLeastOneMonitorActive` implements `anySurvivingActive || enable.Count > 0` exactly.
    - Both exact error strings are present: "This configuration would leave no monitor active. At least one monitor must stay enabled after switching to Rig Mode." and "Select at least one monitor to disable or enable."
    - Skip-reset uses `HashSet<string>.SetEquals` against both plural sets (not `!=`/reference comparison): `grep -c "SetEquals" src/RigToggle.App/SettingsForm.cs` >= 2.
    - Save persists merged sets = `(saved-but-not-enumerated) ∪ (checked rows)` — a comment or code path shows stale entries are unioned in, not dropped.
    - No `cboMonitor` references remain anywhere in `SettingsForm.cs`.
  </acceptance_criteria>
  <done>Save is gated by the DISPLAY-06 predicate then the D-07 non-empty gate, persists both merged sets preserving disconnected monitors, and resets the confirmation-skip flag via order-independent set equality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user grid interaction → persisted settings.json | User checkbox selections become the persisted disable/enable sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service (self) | User saves a config that disables every active monitor | mitigate | `WouldLeaveAtLeastOneMonitorActive` (DISPLAY-06/D-05) blocks Save with a clear message; D-04 also prevents the both-checked contradictory row state at the source. |
| T-06-07 | Tampering | Saved-but-disconnected device paths silently discarded, losing a user's rig config when a monitor is merely powered off | mitigate | Merged-set save unions stale entries through untouched (never drops); non-blocking warning informs the user. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; `DataGridView` is built into the WinForms SDK (no registry/component dependency). |
</threat_model>

<verification>
- Source assertions in each task's `<acceptance_criteria>` (portable).
- On the Windows rig (after Plan 05 also lands): full `RigToggle.App` builds; grid behavior + validation verified in the Plan 06 rig checkpoint.
</verification>

<success_criteria>
- DISPLAY-04/05: user selects N monitors to disable and/or enable via the grid, including OS-disabled ones.
- DISPLAY-06: a would-disable-everything config is blocked with the exact locked message.
- D-04/D-07: single-click mutual exclusivity; enable-only configs are savable; stale monitors preserved.
</success_criteria>

<output>
Create `.planning/phases/06-multi-monitor-data-model-controller-generalization/06-04-SUMMARY.md` when done.
</output>
