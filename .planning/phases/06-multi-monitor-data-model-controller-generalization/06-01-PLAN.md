---
phase: 06-multi-monitor-data-model-controller-generalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RigToggle.Core/Models/AppSettings.cs
  - src/RigToggle.Core/Models/MonitorInfo.cs
  - src/RigToggle.Core/Persistence/JsonSettingsStore.cs
  - src/RigToggle.Tests/JsonStoreTests.cs
autonomous: true
requirements: [DISPLAY-04, DISPLAY-05, DISPLAY-08]
must_haves:
  truths:
    - "AppSettings carries two independently-persisted device-path sets (disable + enable)"
    - "A genuine v1.0-era settings.json (only legacy singular fields) loads with its monitor already in the disable-set"
    - "A corrupted/malformed settings.json still degrades to fresh AppSettings, never throws — migration does not add a new failure mode"
  artifacts:
    - path: "src/RigToggle.Core/Models/AppSettings.cs"
      provides: "MonitorsToDisable / MonitorsToEnable plural fields alongside preserved legacy fields"
      contains: "MonitorsToDisable"
    - path: "src/RigToggle.Core/Models/MonitorInfo.cs"
      provides: "IsActive flag for grid rendering + DISPLAY-06 predicate"
      contains: "IsActive"
    - path: "src/RigToggle.Core/Persistence/JsonSettingsStore.cs"
      provides: "silent v1.0->v1.1 migration inside the existing degrade-gracefully try block"
      contains: "MonitorsToDisable"
    - path: "src/RigToggle.Tests/JsonStoreTests.cs"
      provides: "acceptance test loading a genuine v1.0-shape JSON literal"
      contains: "MonitorsToDisable"
  key_links:
    - from: "src/RigToggle.Core/Persistence/JsonSettingsStore.cs"
      to: "AppSettings.MonitorsToDisable"
      via: "migration mapping of legacy MonitorDevicePath into the disable-set"
      pattern: "MonitorsToDisable\\s*=\\s*new"
---

<objective>
Add the multi-monitor data-model foundation for Phase 6: plural `MonitorsToDisable` / `MonitorsToEnable` sets on `AppSettings`, an `IsActive` flag on `MonitorInfo`, and the silent v1.0->v1.1 settings migration (DISPLAY-08). This plan touches only `RigToggle.Core` + `RigToggle.Tests` (both `net10.0`, cross-platform) so it is fully build-and-test verifiable via `dotnet test`, and it does not touch `IMonitorController` (that is Plan 02) so nothing in the Windows/App projects breaks.

Purpose: Establish the persisted shape every later Phase 6 plan (and every later v1.1 trigger phase) reads and writes, and guarantee a v1.0 user keeps working with zero re-configuration.
Output: Updated `AppSettings`, `MonitorInfo`, `JsonSettingsStore`, plus a migration acceptance test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-CONTEXT.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md
@.planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md

<environment_note>
This planning/executor box may be Linux without the .NET SDK. `RigToggle.Core` and `RigToggle.Tests` target `net10.0` (cross-platform) and MUST build/test wherever `dotnet` is available. If `dotnet` is absent in your environment, satisfy each task via the source assertions in `<acceptance_criteria>` and defer the `dotnet test` run to the Windows rig.
</environment_note>

<interfaces>
Current AppSettings (src/RigToggle.Core/Models/AppSettings.cs) — a MUTABLE POCO with { get; set; }, NOT a record:
  string? MonitorDevicePath      (legacy singular — KEEP as migration source, do not delete)
  string? MonitorFriendlyName    (display-cache only — KEEP)
  string? NormalAudioDeviceId / NormalAudioDeviceName / RigAudioDeviceId / RigAudioDeviceName
  string? CompanionAppPath
  bool SkipMonitorConfirmation
  bool EnableDebugLogging

Current MonitorInfo (positional record):
  record MonitorInfo(string DevicePath, string FriendlyName, bool IsPrimary)
  Existing 3-arg call sites: WindowsMonitorController.GetActiveMonitors (Plan 03 project),
  FakeMonitorController.GetActiveMonitors (`new("\\\\?\\DISPLAY#FAKE", "Fake Monitor", true)`).

Current JsonSettingsStore.Load() (src/RigToggle.Core/Persistence/JsonSettingsStore.cs lines 27-51):
  try { json = File.ReadAllText; return Deserialize<AppSettings> ?? new(); }
  catch (JsonException) { return new(); } catch (IOException) { return new(); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plural monitor-set fields to AppSettings and IsActive to MonitorInfo</name>
  <files>src/RigToggle.Core/Models/AppSettings.cs, src/RigToggle.Core/Models/MonitorInfo.cs</files>
  <read_first>
    - src/RigToggle.Core/Models/AppSettings.cs (current singular fields + doc comment)
    - src/RigToggle.Core/Models/MonitorInfo.cs (current 3-arg positional record)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md (AppSettings section: two-related-items precedent; MonitorInfo section: add IsActive)
  </read_first>
  <action>
    In `AppSettings.cs`: add two mutable properties `public List<string>? MonitorsToDisable { get; set; }` and `public List<string>? MonitorsToEnable { get; set; }` (nullable `List<string>` per Claude's-Discretion D-08 note — a legacy file with no plural fields deserializes them to null, which is the exact signal the migration checks). KEEP `MonitorDevicePath` and `MonitorFriendlyName` unchanged (D-08 migration source — do NOT delete or rename them). Update the class XML-doc to note that `MonitorsToDisable`/`MonitorsToEnable` are the live v1.1 sets and `MonitorDevicePath` is retained only as the legacy migration source. Match the existing `{ get; set; }` mutable-POCO convention; do NOT convert to a record.
    In `MonitorInfo.cs`: add a fourth positional parameter `bool IsActive = false` (DEFAULTED, so the existing 3-arg construction sites in Plan 03's `WindowsMonitorController` and the test `FakeMonitorController` keep compiling until their own plans update them). New signature: `record MonitorInfo(string DevicePath, string FriendlyName, bool IsPrimary, bool IsActive = false)`. Update the doc comment to reference both `GetActiveMonitors()` and the new `GetAllMonitors()` (added in Plan 02/03) and note `IsActive` drives the grid's "(currently OS-disabled)" suffix and the DISPLAY-06 predicate.
  </action>
  <verify>
    <automated>grep -q "MonitorsToDisable" src/RigToggle.Core/Models/AppSettings.cs && grep -q "MonitorsToEnable" src/RigToggle.Core/Models/AppSettings.cs && grep -q "MonitorDevicePath" src/RigToggle.Core/Models/AppSettings.cs && grep -q "bool IsActive" src/RigToggle.Core/Models/MonitorInfo.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MonitorsToDisable\|MonitorsToEnable" src/RigToggle.Core/Models/AppSettings.cs` returns 2 (both plural fields declared).
    - `AppSettings.cs` still contains `MonitorDevicePath` and `MonitorFriendlyName` (legacy fields NOT deleted).
    - `MonitorInfo` record signature ends with `bool IsActive = false)` — defaulted so existing 3-arg callers still compile.
    - `AppSettings` remains a `sealed class` with `{ get; set; }` properties (not converted to a record).
  </acceptance_criteria>
  <done>Both plural `List<string>?` fields exist on `AppSettings` alongside the preserved legacy fields; `MonitorInfo` has a defaulted `IsActive` flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Silent v1.0->v1.1 migration in JsonSettingsStore.Load() + acceptance test</name>
  <files>src/RigToggle.Core/Persistence/JsonSettingsStore.cs, src/RigToggle.Tests/JsonStoreTests.cs</files>
  <read_first>
    - src/RigToggle.Core/Persistence/JsonSettingsStore.cs (Load() try/catch degrade-gracefully block, lines 27-51)
    - src/RigToggle.Tests/JsonStoreTests.cs (existing round-trip + malformed-file test patterns, lines 46-86 and 132-147)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-RESEARCH.md (Code Examples: "Migration (D-08)" + Pitfall 6)
    - .planning/phases/06-multi-monitor-data-model-controller-generalization/06-PATTERNS.md (JsonSettingsStore section)
  </read_first>
  <behavior>
    - Test: a JSON literal containing ONLY legacy singular fields (`MonitorDevicePath`, `MonitorFriendlyName`, audio ids, `CompanionAppPath`) and NO plural fields at all, written directly to the temp path, then `Load()`-ed, yields `MonitorsToDisable` == exactly `[ that legacy MonitorDevicePath ]` and `MonitorsToEnable` null-or-empty.
    - Test: a v1.1-shape file that already has a non-empty `MonitorsToDisable` is NOT overwritten by the legacy `MonitorDevicePath` (migration only fires when the plural set is null/empty).
    - Existing malformed-JSON degrade-to-fresh behavior is unchanged (migration must live inside the existing try block).
  </behavior>
  <action>
    In `JsonSettingsStore.Load()`, INSIDE the existing `try` block, after the `JsonSerializer.Deserialize<AppSettings>(...)` call succeeds and BEFORE returning: assign the deserialized value to a local `loaded`, then apply the D-08 migration — if `!string.IsNullOrEmpty(loaded.MonitorDevicePath)` AND (`loaded.MonitorsToDisable is null || loaded.MonitorsToDisable.Count == 0`), set `loaded.MonitorsToDisable = new List<string> { loaded.MonitorDevicePath };`. Return `loaded`. Do NOT add a separate post-Load migration pass and do NOT add new catch clauses — the migration must inherit the existing `JsonException`/`IOException` degrade-to-`new AppSettings()` handling for free (Pitfall 6). No dialog, toast, or banner (D-08: fully silent).
    In `JsonStoreTests.cs`, add a test (model it on `SettingsStore_Save_ThenLoad_RoundTripsAllFields` but WRITE a raw v1.0-shape JSON literal via `File.WriteAllText` directly to the temp path — do NOT go through `Save()`, which would emit the current shape): `SettingsStore_Load_MigratesLegacyMonitorDevicePath_IntoDisableSet`. Assert `loaded.MonitorsToDisable` has Count 1 and its single element equals the legacy device path, and that `MonitorsToEnable` is null or empty. Add a second test `SettingsStore_Load_DoesNotRemigrate_WhenDisableSetAlreadyPopulated` writing a JSON literal with BOTH a legacy `MonitorDevicePath` and a non-empty `MonitorsToDisable` array, asserting the plural set is preserved unchanged. Reuse the per-test temp-dir + `IDisposable` cleanup already in the test class.
  </action>
  <verify>
    <automated>dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj --filter "FullyQualifiedName~JsonStoreTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` passes (all JsonStoreTests green) on a host with the .NET SDK.
    - The migration assignment appears inside the `try` block of `Load()`: `grep -n "MonitorsToDisable = new" src/RigToggle.Core/Persistence/JsonSettingsStore.cs` returns a line, and that line is between the `try {` and its `catch (JsonException)` (no new catch clause added — `grep -c "catch (" src/RigToggle.Core/Persistence/JsonSettingsStore.cs` is unchanged at 2).
    - Both new test method names exist: `grep -c "SettingsStore_Load_MigratesLegacyMonitorDevicePath_IntoDisableSet\|SettingsStore_Load_DoesNotRemigrate_WhenDisableSetAlreadyPopulated" src/RigToggle.Tests/JsonStoreTests.cs` returns 2.
    - The migration test's fixture JSON literal contains NO `MonitorsToDisable`/`MonitorsToEnable` keys (proves genuine v1.0 shape).
  </acceptance_criteria>
  <done>A genuine v1.0-shape settings.json migrates its `MonitorDevicePath` into `MonitorsToDisable` on Load, migration is idempotent against already-migrated files, malformed files still degrade gracefully, and both behaviors are pinned by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local settings.json → JsonSettingsStore.Load | Hand-editable local file (single-user desktop tool); the only untrusted-ish input this plan touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Hand-edited / malformed `MonitorsToDisable`/`MonitorsToEnable` array or corrupted legacy file in `JsonSettingsStore.Load()` | mitigate | Migration runs INSIDE the existing `try` block so `JsonException`/`IOException` still degrade to a fresh `AppSettings()` (Pitfall 6); no new failure mode is introduced. `System.Text.Json` already bounds array parsing. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (06-RESEARCH.md Package Legitimacy Audit: not applicable); no install task exists. |
</threat_model>

<verification>
- `dotnet test src/RigToggle.Tests/RigToggle.Tests.csproj` passes (on a host with the SDK).
- `AppSettings` exposes both plural sets and retains both legacy fields.
- Migration is idempotent and lives inside the degrade-gracefully try block.
</verification>

<success_criteria>
- DISPLAY-08: a v1.0-era settings.json migrates silently, verified by a passing acceptance test.
- DISPLAY-04/05 data foundation: both `MonitorsToDisable` and `MonitorsToEnable` persist on `AppSettings`.
- No `IMonitorController` change (nothing in Windows/App projects is disturbed by this plan).
</success_criteria>

<output>
Create `.planning/phases/06-multi-monitor-data-model-controller-generalization/06-01-SUMMARY.md` when done.
</output>
