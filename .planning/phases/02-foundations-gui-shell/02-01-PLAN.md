---
phase: 02-foundations-gui-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RigToggle.sln
  - src/RigToggle.App/RigToggle.App.csproj
  - src/RigToggle.Core/RigToggle.Core.csproj
  - src/RigToggle.Windows/RigToggle.Windows.csproj
  - src/RigToggle.Tests/RigToggle.Tests.csproj
  - src/RigToggle.Core/Models/AppSettings.cs
  - src/RigToggle.Core/Models/MonitorInfo.cs
  - src/RigToggle.Core/Models/MonitorState.cs
  - src/RigToggle.Core/Models/AudioDeviceInfo.cs
  - src/RigToggle.Core/Models/AudioState.cs
  - src/RigToggle.Core/Models/StateSnapshot.cs
  - src/RigToggle.Core/Abstractions/IMonitorController.cs
  - src/RigToggle.Core/Abstractions/IAudioController.cs
  - src/RigToggle.Core/Abstractions/IAppController.cs
  - src/RigToggle.Core/Abstractions/ISettingsStore.cs
  - src/RigToggle.Core/Abstractions/ISnapshotStore.cs
autonomous: false
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04]

must_haves:
  truths:
    - "The four-project solution (App, Core, Windows, Tests) exists with correct target frameworks and project references"
    - "RigToggle.Core has zero references to WindowsDisplayAPI or NAudio (structural enforcement of D-08)"
    - "Every contract (5 interfaces) and persisted/enumeration model type the rest of the phase consumes is defined and compiles"
    - "NuGet package installs (WindowsDisplayAPI, NAudio) were human-verified before install per the legitimacy gate"
  artifacts:
    - path: "RigToggle.sln"
      provides: "Solution referencing all four projects"
      contains: "RigToggle.Core"
    - path: "src/RigToggle.Core/RigToggle.Core.csproj"
      provides: "Windows-API-free Core class library (net10.0)"
      contains: "net10.0"
    - path: "src/RigToggle.Windows/RigToggle.Windows.csproj"
      provides: "net10.0-windows library with WindowsDisplayAPI + NAudio package refs"
      contains: "WindowsDisplayAPI"
    - path: "src/RigToggle.Core/Abstractions/IMonitorController.cs"
      provides: "Monitor controller contract (GetActiveMonitors/CaptureState/Disable/Restore)"
      contains: "interface IMonitorController"
    - path: "src/RigToggle.Core/Models/AppSettings.cs"
      provides: "Persisted settings POCO with all seven nullable string fields"
      contains: "class AppSettings"
  key_links:
    - from: "src/RigToggle.App/RigToggle.App.csproj"
      to: "RigToggle.Core + RigToggle.Windows"
      via: "ProjectReference"
      pattern: "ProjectReference.*RigToggle\\.(Core|Windows)"
    - from: "src/RigToggle.Windows/RigToggle.Windows.csproj"
      to: "RigToggle.Core"
      via: "ProjectReference"
      pattern: "ProjectReference.*RigToggle\\.Core"
---

<objective>
Scaffold the four-project .NET 10 solution and define every Core contract (interfaces) and data model the rest of Phase 2 builds against. This is the foundation plan: all other Phase 2 plans depend on the interfaces and models created here.

Purpose: Establish the layered architecture (GUI / Orchestration / Adapters / Persistence per ARCHITECTURE.md) with structural enforcement that RigToggle.Core stays Windows-API-free (D-08), and hand every downstream executor the exact contracts they implement against — no codebase exploration needed.

Output: A compiling solution skeleton (empty adapter/service bodies come in later plans) plus all interfaces and models.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Execution boundary (carried from Phase 1):** This Linux sandbox has no .NET 10 SDK. The executor produces source; `dotnet` build/verification runs on the USER's Windows rig. Treat every `<automated>` command below as "the user runs this on the rig and reports results," matching Phase 1's human-verification loop.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundations-gui-shell/02-CONTEXT.md
@.planning/phases/02-foundations-gui-shell/02-RESEARCH.md
@.planning/phases/02-foundations-gui-shell/02-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (02..05) implement against these exact shapes. -->
<!-- Concrete field/method shapes are specified in 02-RESEARCH.md (cited per task) — do not invent divergent signatures. -->

Models (records/POCOs, RigToggle.Core/Models):
- MonitorInfo(string DevicePath, string FriendlyName, bool IsPrimary)      // enumeration result
- MonitorState(string MonitorDevicePath)                                   // Phase-2 minimal capture; Phase 4 enriches
- AudioDeviceInfo(string Id, string FriendlyName)                          // enumeration result
- AudioState(string? DefaultDeviceId)                                      // capture result
- StateSnapshot(MonitorState Monitor, AudioState Audio)                    // 02-RESEARCH.md line 320
- AppSettings: mutable class, 7 nullable string props — 02-RESEARCH.md lines 454-465

Interfaces (RigToggle.Core/Abstractions):
- IMonitorController: IReadOnlyList<MonitorInfo> GetActiveMonitors(); MonitorState CaptureState(string monitorDevicePath); void Disable(string monitorDevicePath); void Restore(MonitorState previousState);
- IAudioController: IReadOnlyList<AudioDeviceInfo> GetPlaybackDevices(); AudioState CaptureState(); void SetDefault(string deviceId); void Restore(AudioState previousState);
- IAppController: bool IsRunning(string companionAppPath); void LaunchOrFocus(string companionAppPath); void MinimizeIfRunning(string companionAppPath);
- ISettingsStore: AppSettings Load(); void Save(AppSettings settings);
- ISnapshotStore: bool Exists(); void Save(StateSnapshot snapshot); StateSnapshot? Load(); void Clear();
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — WindowsDisplayAPI + NAudio (+ xUnit test SDK)</name>
  <read_first>
    - .planning/phases/02-foundations-gui-shell/02-RESEARCH.md (Package Legitimacy Audit, lines 103-115 — both packages are [ASSUMED]: slopcheck does not cover the NuGet ecosystem; existence, version currency, and full source shape were independently confirmed this research session; WindowsDisplayAPI already ran non-elevated against this rig's hardware in Phase 1)
  </read_first>
  <what-built>
    Nothing yet — this gate runs BEFORE any `dotnet add package`. This is the supply-chain (T-02-SC) mitigation required by the package legitimacy gate because the NuGet ecosystem is unsupported by slopcheck, so both third-party packages are formally [ASSUMED].
  </what-built>
  <how-to-verify>
    1. Open https://www.nuget.org/packages/WindowsDisplayAPI — confirm latest stable is `1.3.0.13`, source repo `github.com/falahati/WindowsDisplayAPI`, not a recently-created typosquat.
    2. Open https://www.nuget.org/packages/NAudio — confirm latest stable is `2.3.0` (a `3.0.0-preview.*` line exists but is pre-release; do NOT use it), source repo `github.com/naudio/NAudio`, long download history.
    3. Note the test project will also pull the standard Microsoft xUnit template packages (`xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`) — Microsoft-authored/de-facto-standard; confirm you are comfortable with those.
    This can reasonably be a fast rubber-stamp: both packages are unchanged from STACK.md's project-research-time selection, WindowsDisplayAPI already executed successfully on this exact rig in Phase 1, and this session re-confirmed both are the current non-yanked latest-stable versions.
  </how-to-verify>
  <acceptance_criteria>
    - Human has confirmed both NuGet packages resolve to the expected version + source repo and approved installation, OR raised a concern that blocks Task 2.
  </acceptance_criteria>
  <action>Gate-only task: verify NuGet package legitimacy per the how-to-verify steps, then approve or block. No files are produced; this precedes the `dotnet add package` calls in Task 2.</action>
  <resume-signal>Type "approved" to proceed with the `dotnet add package` steps in Task 2, or describe the concern.</resume-signal>
  <verify><human-check>Human confirms both packages resolve to the expected version and source repo, and approves installation.</human-check></verify>
  <done>Package installs approved (or a blocking concern raised) before Task 2 runs `dotnet add package`.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold solution, four projects, references, and packages</name>
  <files>RigToggle.sln, src/RigToggle.App/RigToggle.App.csproj, src/RigToggle.Core/RigToggle.Core.csproj, src/RigToggle.Windows/RigToggle.Windows.csproj, src/RigToggle.Tests/RigToggle.Tests.csproj</files>
  <read_first>
    - .planning/phases/02-foundations-gui-shell/02-RESEARCH.md (Installation, lines 87-101 — the exact `dotnet new`/`dotnet add` command sequence and the TFM/reference rules; note line 101: RigToggle.Core.csproj MUST NOT reference WindowsDisplayAPI or NAudio)
    - .planning/phases/02-foundations-gui-shell/02-PATTERNS.md (RigToggle.Windows.csproj section, lines 87-117 — csproj shape, "no elevation manifest" convention, UseWindowsForms rationale)
    - spike/MonitorDetachSpike/MonitorDetachSpike.csproj (the only existing net10.0-windows + WindowsDisplayAPI csproj to mirror; note its no-manifest comment)
  </read_first>
  <action>
    Create the solution under repo root as `RigToggle.sln` and all four projects under `src/`. Use `dotnet new`: `classlib -n RigToggle.Core -f net10.0`; `classlib -n RigToggle.Windows -f net10.0-windows`; `winforms -n RigToggle.App -f net10.0-windows`; `xunit -n RigToggle.Tests -f net10.0` — each into its own `src/RigToggle.<Name>/` directory. Add all four to the solution with `dotnet sln add`. Wire references: RigToggle.App references RigToggle.Core AND RigToggle.Windows; RigToggle.Windows references RigToggle.Core; RigToggle.Tests references RigToggle.Core (tests target Core only — hand-written doubles, no Windows deps). Add packages ONLY to RigToggle.Windows: `WindowsDisplayAPI` version `1.3.0.13` and `NAudio` version `2.3.0`. Set `RigToggle.Windows.csproj` to `<UseWindowsForms>true</UseWindowsForms>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>` (per its Screen-oracle + COM-lib needs). Keep RigToggle.Core as a plain `net10.0` classlib with NO WinForms and NO WindowsDisplayAPI/NAudio references (D-08 structural enforcement). Do NOT add any `.manifest` file or `<ApplicationManifest>`/`requestedExecutionLevel` to any project — leave the implicit `asInvoker` default untouched (02-RESEARCH.md Pitfall 6, lines 429-434). Leave the winforms template's generated `Program.cs`/`Form1.*` in RigToggle.App as-is for now (Plan 05 replaces them) — they let the App project compile during Wave 2.
  </action>
  <acceptance_criteria>
    - `RigToggle.sln` lists exactly four projects.
    - `RigToggle.Core.csproj` targets `net10.0` and contains NO `PackageReference` to WindowsDisplayAPI or NAudio and no `UseWindowsForms`.
    - `RigToggle.Windows.csproj` targets `net10.0-windows`, contains `<PackageReference Include="WindowsDisplayAPI" Version="1.3.0.13" />` and `<PackageReference Include="NAudio" Version="2.3.0" />`, and a `ProjectReference` to RigToggle.Core.
    - `RigToggle.App.csproj` targets `net10.0-windows` and has `ProjectReference`s to both RigToggle.Core and RigToggle.Windows.
    - `grep -rl "requestedExecutionLevel\|ApplicationManifest" src/` returns no results (no elevation manifest anywhere).
    - `grep -c "WindowsDisplayAPI\|NAudio" src/RigToggle.Core/RigToggle.Core.csproj` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build RigToggle.sln  # run on Windows rig; must restore packages and build all four (empty) projects with 0 errors</automated>
  </verify>
  <done>Solution and four projects build clean on the rig; package refs isolated to RigToggle.Windows; Core is Windows-API-free.</done>
</task>

<task type="auto">
  <name>Task 3: Define Core contracts — models and interfaces</name>
  <files>src/RigToggle.Core/Models/AppSettings.cs, src/RigToggle.Core/Models/MonitorInfo.cs, src/RigToggle.Core/Models/MonitorState.cs, src/RigToggle.Core/Models/AudioDeviceInfo.cs, src/RigToggle.Core/Models/AudioState.cs, src/RigToggle.Core/Models/StateSnapshot.cs, src/RigToggle.Core/Abstractions/IMonitorController.cs, src/RigToggle.Core/Abstractions/IAudioController.cs, src/RigToggle.Core/Abstractions/IAppController.cs, src/RigToggle.Core/Abstractions/ISettingsStore.cs, src/RigToggle.Core/Abstractions/ISnapshotStore.cs</files>
  <read_first>
    - .planning/phases/02-foundations-gui-shell/02-RESEARCH.md (AppSettings shape lines 454-465; StateSnapshot line 320; System Architecture Diagram lines 146-159 for per-adapter method lists; Pattern 4 lines 322-339 for ISnapshotStore methods Exists/Save/Load/Clear)
    - .planning/phases/02-foundations-gui-shell/02-PATTERNS.md (IMonitorController section lines 121-123 — derive interface signatures from the WindowsMonitorController shape RESEARCH committed to)
  </read_first>
  <action>
    In `RigToggle.Core/Models/`, define: `AppSettings` as a mutable class with the seven nullable string auto-properties exactly per 02-RESEARCH.md lines 454-465 (MonitorDevicePath, MonitorFriendlyName, NormalAudioDeviceId, NormalAudioDeviceName, RigAudioDeviceId, RigAudioDeviceName, CompanionAppPath); `MonitorInfo(string DevicePath, string FriendlyName, bool IsPrimary)` as a `sealed record`; `MonitorState(string MonitorDevicePath)` as a `sealed record` (Phase-2-minimal — comment that Phase 4 may enrich it with full DISPLAYCONFIG arrays); `AudioDeviceInfo(string Id, string FriendlyName)` as a `sealed record`; `AudioState(string? DefaultDeviceId)` as a `sealed record`; `StateSnapshot(MonitorState Monitor, AudioState Audio)` as a `sealed record`. In `RigToggle.Core/Abstractions/`, define the five interfaces exactly per the `<interfaces>` block in this plan's context: `IMonitorController`, `IAudioController`, `IAppController`, `ISettingsStore`, `ISnapshotStore`. All types under namespace `RigToggle.Core.Models` / `RigToggle.Core.Abstractions`. No implementations, no bodies — contracts only. Ensure the record types serialize cleanly with System.Text.Json (single primary constructor — supported; add no custom converters).
  </action>
  <acceptance_criteria>
    - `grep -c "interface I" src/RigToggle.Core/Abstractions/*.cs` totals 5 (one interface per file).
    - `IMonitorController.cs` declares `GetActiveMonitors`, `CaptureState`, `Disable`, and `Restore`.
    - `ISnapshotStore.cs` declares `Exists`, `Save`, `Load`, and `Clear`.
    - `AppSettings.cs` contains all seven property names listed in the action, each typed `string?`.
    - `StateSnapshot.cs` declares a record with `MonitorState Monitor` and `AudioState Audio` members.
    - All model/interface files compile (no method bodies present in interfaces).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/RigToggle.Core/RigToggle.Core.csproj  # run on rig; contracts compile, 0 errors, no Windows API references</automated>
  </verify>
  <done>All 5 interfaces and 6 model types exist, compile, and match the signatures downstream plans depend on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → NuGet registry | Third-party package code (WindowsDisplayAPI, NAudio) is pulled and compiled into the app — untrusted supply-chain input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | NuGet installs (WindowsDisplayAPI, NAudio) | mitigate | Blocking human-verify gate (Task 1) before `dotnet add package`; both pinned to exact vetted versions (1.3.0.13 / 2.3.0); slopcheck unsupported for NuGet so packages are formally [ASSUMED] but independently source-verified this session and WindowsDisplayAPI already ran on this rig in Phase 1 |
| T-02-ELEV | Elevation of Privilege | App/Windows csproj manifest | accept/mitigate | No elevation manifest added (asInvoker default preserved); accepting non-elevated posture is correct — later elevation would break cross-process window focus via UIPI (02-RESEARCH.md Pitfall 6) |
</threat_model>

<verification>
- Solution builds clean on the Windows rig (`dotnet build RigToggle.sln`, 0 errors).
- RigToggle.Core is provably Windows-API-free (grep gate in Task 2 acceptance).
- All contracts compile and are ready for Plans 02-05 to implement against.
- No `.manifest`/`requestedExecutionLevel` anywhere under `src/`.
</verification>

<success_criteria>
- Four-project solution exists with correct TFMs and references.
- Packages isolated to RigToggle.Windows and installed only after the human legitimacy gate.
- All 5 interfaces + 6 models defined, compiling, matching the published contract shapes.
</success_criteria>

<output>
Create `.planning/phases/02-foundations-gui-shell/02-01-SUMMARY.md` when done.
</output>
