---
status: resolved
trigger: "Monitor position resets to default when a monitor is disabled and then re-enabled via Rig Toggle"
created: 2026-08-23T07:12:36Z
updated: 2026-08-28T17:10:00Z
---

## Symptoms

### Symptom 1: Monitor position resets to default
- **Expected:** A monitor's position, as configured in Windows Display Settings, is preserved when it is disabled and then re-enabled through Rig Toggle.
- **Actual:** The monitor's position resets to its default position after being disabled and re-enabled. Happens silently, no error/warning surfaced anywhere in the app.
- **Reproduction:** Disable a monitor (via either the dashboard monitor tile or the Rig/Normal mode toggle) and re-enable it (either control). User confirmed it happens via both controls.
- **Timeline:** Started recently (not "always happened") — user is unsure of the exact trigger commit but reports it as a regression, not a longstanding issue.
- **Severity:** major (user confirmed) — always needs re-setting up after the monitor wakes.
- **Status: RESOLVED** — rig-confirmed fixed by the position-mode cache described in Resolution below. Not re-litigated this round.

### Symptom 2: Rig mode switch partially fails / silently reverts
- **Expected:** Switching to Rig mode (via the Rig/Normal toggle) completes normally regardless of whether a monitor is already disabled going in, and the resulting layout stays correct (not just immediately after the toggle, but durably).
- **Actual:** Evolved across three rig tests as investigation progressed:
  - Round 1 repro: "Partial toggle" when one monitor already disabled going in — some steps happen but the monitor part doesn't complete correctly.
  - Round 2 repro (after the isPartOfMonitorSwap->Extend fix): toggle APPEARS to succeed (app's own verify passes) then silently reverts to the pre-toggle topology ~2-3 seconds later with zero app involvement; reverse direction instead threw PathChangeException outright.
  - Round 3 repro (same fix, from the original "one monitor already independently disabled" starting state): Extend never activated the requested target at all, while additionally reactivating the independently-disabled, unrequested monitor.
  - Round 3 (third rig test, on the SIMPLE single-tile enable path, previously believed unaffected): the SAME class of delayed silent revert (~2.15s) reproduced on scoped ApplyPathInfos too — proving the revert is not specific to which CCD mechanism is used.
- **No errors surfaced** in the revert cases — silent.
- **Status: RESOLVED VIA MITIGATION (round 8 closure).** Part A (Extend's structural incapability) and parts C/D/E/F/G (array-shape, correction-atomicity, and instrumentation defects) are fully fixed and rig-confirmed. Part B (the OS-level delayed revert itself, ~1.46-1.53s after an apply, now measured across four independent samples) remains **root-cause-unknown** after eight rounds, but the app's reactive-correction layer (fixes D/F/G/H) has demonstrated clean, complete, invisible self-healing across three consecutive rig trials with zero user-visible failures. User explicitly chose to close this out accepting the mitigation over continuing to chase the OS/driver-level trigger (see Resolution and Checkpoint history below).

### Suspected relationship
Three distinct, now-confirmed root causes, all downstream of the round-5 scoped-activation path's introduction and its round-2/round-3 mitigations: (1) missing position/mode data (Symptom 1, resolved), (2) ApplyTopology(Extend)'s structural inability to express an explicit desired active-path-set (Symptom 2, rounds 2-3), (3) a delayed drift-back-to-previous-state that both CCD mechanisms are equally subject to, previously only ever corrected by the app's reactive watchdog in ONE direction and only on the tile path (Symptom 2, round 3 part 2).

## Current Focus

<!-- Round 7 (current, 2026-08-28). Round 6's reasoning_checkpoint is preserved verbatim
     immediately below as "## Current Focus (round 6)" per this file's own established
     convention (see "## Current Focus (round 5)"/"## Current Focus (round 4)" further down
     for earlier rounds). This section reflects NOW. -->

reasoning_checkpoint:
  hypothesis: "Round 6's checkpoint response (seventh rig test) surfaced a genuinely NEW, distinct, tractable, code-level defect (checkpoint finding 6) separate from Symptom 2 part B: ActivateMonitors' scoped-then-Extend-fallback machinery only ever defends against a device path GAINING an unwanted activation (ComputeUnexpectedlyActivated, rounds 2-4) -- it has no symmetric defense against a device path that was ALREADY ACTIVE going in, was never part of monitorSwapDisableSet, and silently LOSES activation as an unrequested side effect of the whole-topology Extend fallback. This is exactly what the seventh rig trial reproduced: ActivateMonitors(ACI24A4) (plain, non-swap, SAM748A already active) had its scoped ApplyPathInfos throw PathChangeException, fell back to Extend, and Extend's own opaque persisted-database layout came back as [ACI24A4, DELA0B8] -- SAM748A, untouched by this call's own request or disable-set, ended up permanently off, with ActivateMonitors reporting EXIT success (finalActive=[ACI24A4]) because its verify-and-throw only ever checked the REQUESTED set (monitorDevicePaths={ACI24A4}), never the pre-existing survivors. Compounding this: MainForm.ArmIntentGuard (called immediately after, per its own unconditional design -- 'snapshot whatever GetAllMonitors() observes right now as the new intent') then recorded SAM748A:inactive as the CORRECT intended state, meaning even the round-3/5 reactive watchdog could never catch or correct this afterward -- it had no way to know SAM748A being off was wrong. This is a genuinely SEPARATE root cause from Symptom 2 part B's still-unexplained delayed revert: part B is 'an apparently-correct apply gets silently undone seconds later by something external'; this is 'ActivateMonitors' OWN correction/verify logic has a structural blind spot in one direction, producing an immediately-wrong final state with no revert involved at all.' Weighing the checkpoint response's explicit tradeoff question: fixing THIS is chosen over further Symptom 2 part B investigation this round, because (a) it is fully code-level, confirmed by direct read (not requiring new rig-only data), (b) it is more severe in kind (a permanently wrong final state vs. a delayed-but-eventually-correctable revert that fix D/F/G already handle reasonably well), and (c) Symptom 2 part B has no new angle to pursue this round beyond what rounds 5/6 already established as underdetermined (see Evidence below) -- re-guessing at it without new data would repeat this session's own previously-flagged failure pattern. Separately, fix F and fix D are independently CONFIRMED (not just corroborated secondhand) this round via my own direct trace of the raw debug.log excerpt against current source, not merely the orchestrator's summary."
  confirming_evidence:
    - "Direct trace of this round's raw debug.log (not just the orchestrator's summary) against current WindowsMonitorController.cs source: WINDOW A (baseline 12:34:21.083) logs a CHANGE at +1457ms, then a corrected re-apply, then 'observation window closed at +6187ms' -- 21.083+6187ms=27.270, matching the log's own closing timestamp exactly. WINDOW E (baseline 12:36:11.378) shows the identical pattern: CHANGE at +1480ms, then 'observation window closed at +6183ms' (11.378+6183=17.561, matching). Both windows ran their FULL 6-second course despite a mid-window CHANGE detection -- direct, independent confirmation that fix F's per-tick try/catch (round 6) holds; not refuted."
    - "Direct code read (WindowsMonitorController.cs, ObservePostApplyStability, current source, unchanged since round 6): the per-tick try/catch fix F installed is still exactly in place -- QueryActiveDevicePaths() and the CHANGE comparison are inside their own nested try/catch, the outer try/catch remains only as a backstop. Confirms the round-6 fix was not accidentally reverted or altered."
    - "Direct trace of the raw log against MainForm.cs's TryReactivelyCorrectAgainstLastIntent (fix D, unchanged since round 5): both WINDOW A and WINDOW E's revert triggers a SINGLE atomic ActivateMonitors(requested=[SAM748A], monitorSwapDisableSet=[ACI24A4,DELA0B8]) call (12:34:23.684 and 12:36:13.491) that completes with EXIT success and no exception -- matches fix D's intended one-atomic-call behavior exactly, independently re-derived from the raw log this round (not merely corroborated secondhand as round 6 had to do)."
    - "No 'GetAllMonitors failed' line appears anywhere in this round's log excerpt -- fix G (round 6) is genuinely NOT exercised this trial, neither confirmed nor refuted, exactly as the checkpoint response's own analysis states. Independently confirmed by grepping the full pasted log text for that string (not found)."
    - "Direct code read, WindowsMonitorController.cs ActivateMonitors (pre-round-7-fix) and its correction loop: `unexpectedlyActivated` was computed via ComputeUnexpectedlyActivated(currentlyActiveDevicePaths, settledActiveDevicePaths, monitorDevicePaths) and corrected via DeactivateMonitors -- there was NO symmetric computation or correction for a device path present in currentlyActiveDevicePaths, absent from monitorSwapDisableSet, and absent from settledActiveDevicePaths. The final verify-and-throw's `stillInactive` computation was `monitorDevicePaths.Except(postCorrection active paths)` ONLY -- it structurally could not detect SAM748A missing, since SAM748A was never in monitorDevicePaths (the REQUESTED set for this specific call) at all."
    - "Direct code read, MainForm.cs ArmIntentGuard (unchanged, all rounds): `_lastUserIntent = _lastKnownMonitors.ToDictionary(m => m.DevicePath, m => m.IsActive)` -- an unconditional snapshot of whatever RefreshMonitorTiles() just observed, called immediately after every deliberate tile action (OnTileAction's finally block) regardless of whether the preceding ActivateMonitors call left the system in the CORRECT state or not. Confirms the checkpoint response's own observation (12:34:32.825's ArmIntentGuard log line: 'intent=[ACI24A4:active, DELA0B8:inactive, SAM748A:inactive]') is not a separate bug -- it is this exact, already-existing, by-design behavior faithfully recording ActivateMonitors' own wrong final state as if it were correct."
    - "This round's raw log directly confirms the FINAL state was wrong, not merely delayed: 'ActivateMonitors: EXIT success -- all requested paths active. finalActive=[ACI24A4].' at 12:34:32.729, with SAM748A never appearing again as active anywhere in the remainder of this round's log excerpt (over 90 more seconds of subsequent log lines) until the user's own later deliberate toggle back to Rig mode at 12:36:10.342 -- i.e. this was not a delayed revert that would eventually self-correct or get caught by the watchdog; it was a silent, permanent (until manual re-intervention) loss."
  falsification_test: "If a rig trial reproducing the exact round-7 repro shape (a plain, non-swap ActivateMonitors call whose scoped plan throws and falls back to Extend, with a different, unrequested, pre-existing-active survivor present) still ends with that survivor permanently inactive and ActivateMonitors reporting EXIT success (no thrown exception, no correction attempt logged), fix round 7 is refuted -- either ComputeUnexpectedlyDeactivated's logic has a gap this round's hand-trace/unit-tests didn't surface, or the nested nonswap-aware ActivateMonitors correction call itself fails silently in a way this round's code read did not anticipate (e.g. the nested call's own scoped-plan+Extend-fallback path fails for the SAME reason the original call's did, with no further correction beyond MaxCorrectionRounds, and the extended verify-and-throw's new stillInactive branch correctly throws -- that would be the fix WORKING as designed via its own D-05 escape hatch, not a refutation, provided the throw is observed instead of a silent false 'success')."
  fix_rationale: "Adds ComputeUnexpectedlyDeactivated, the exact mirror image of the already-established, already-tested ComputeUnexpectedlyActivated pure seam -- same signature shape (pre-active set, post-active set, an exclusion set), same 'pure function, unit-testable, no live CCD hardware needed' discipline this file already follows for its sibling. Wires it into the EXISTING correction loop (not a new loop/mechanism) so a previously-active, non-swap-excluded survivor that unexpectedly drops is corrected via a nested, non-swap-aware ActivateMonitors call for exactly that path -- reusing this file's own established 'reuse the already rig-proven public method, not a new primitive' convention (mirrors how the unexpectedlyActivated branch reuses DeactivateMonitors). Extends the final verify-and-throw's `stillInactive` check to ALSO require every pre-call-active, non-excluded survivor to still be active post-correction -- so if the bounded correction rounds genuinely cannot restore it, this now throws (D-05: surface failure loudly) instead of silently reporting EXIT success with a wrong final state, which is precisely what let this bug persist undetected through six prior rounds. Deliberately does NOT attempt to explain or fix WHY the scoped ApplyPathInfos plan threw PathChangeException for the SAM748A+ACI24A4 pairing specifically (genuinely unknown -- an identically-shaped SAM748A+DELA0B8 call succeeded cleanly moments later in the same trial) -- per this session's established research-vs-reasoning discipline, that remains open rather than guessed at; this fix targets the CONSEQUENCE (a lost survivor with no correction and no error), which is independently real, well-evidenced, and actionable regardless of the upstream trigger."
  blind_spots:
    - "Cannot execute or verify on real Windows CCD hardware in this sandbox -- same structural limitation as every prior round in this file. Self-verified via build + full adjacent test suite + 5 new unit tests for the pure ComputeUnexpectedlyDeactivated seam + hand-trace of all 5 against the implementation + hand-trace of the full round-7 repro scenario through the modified ActivateMonitors control flow."
    - "The nested ActivateMonitors(unexpectedlyDeactivated, ...) correction call is genuinely recursive (the method calling itself) -- not unbounded (each nested call only targets a strictly-known, finite device-path set, and this rig has only 3 monitors total, so no realistic call depth beyond a handful of frames is possible), but this is a new class of control-flow this method did not previously have, and its interaction with a pathologically flapping driver (one that kept dropping a DIFFERENT survivor on every single correction attempt) is not rig-tested -- MaxCorrectionRounds (3) still bounds the OUTER loop's own iteration count regardless, so this cannot spin forever, but a worst-case scenario could still consume the full correction-round budget across nested calls rather than a single flat loop the way the codebase's other bounded-retry constructs are shaped."
    - "Does not explain why the scoped ApplyPathInfos plan threw PathChangeException specifically for the SAM748A+ACI24A4 pairing while an apparently-identical-shaped SAM748A+DELA0B8 call succeeded -- multiple plausible mechanisms exist (GetAllPaths() returning a stale/incompatible source candidate as the FIRST unclaimed-source match for one target but not the other; a hardware-level source/port-group constraint specific to this pairing; a cached-mode Position/Resolution mismatch against whichever source got picked) and this round deliberately does not guess between them without more diagnostic data (e.g. logging the chosen candidate's actual PathDisplaySource identity, which the current code does not do) -- left as a genuinely open item, not silently dropped."
    - "Symptom 2 part B (the delayed silent revert itself) remains completely unexplained after seven rounds -- this round adds two pieces of refined evidence (the tight ~1.46-1.48s revert-timing observation across two independent windows in the same trial; the periodic, well-outside-any-observation-window recurrence with the ACI24A4-vanishing-from-enumeration lead) but no new hypothesis and no fix attempt, per the same research-vs-reasoning discipline as rounds 5/6."
  candidate_causes:
    - "code (fix target, round 5's own new correction-loop machinery): ActivateMonitors' correction loop and final verify-and-throw only ever checked for gained-but-unrequested activations (ComputeUnexpectedlyActivated), never for lost-but-unrequested deactivations of a pre-existing survivor -- a structural, one-directional gap in the SAME class of code as fix B/D's own one-directional gaps at the MainForm layer, just discovered one layer lower."
    - "environment/CCD-validation (still unresolved, DIFFERENT candidate cause from the one above): WHY the scoped ApplyPathInfos plan threw PathChangeException for SAM748A+ACI24A4 specifically remains genuinely unknown -- not re-researched or guessed at this round; this round's fix addresses the correction-loop's blind spot regardless of which upstream mechanism triggers the Extend fallback in the first place."
    - "environment/OS-driver (Symptom 2 part B, UNCHANGED, still uneliminated across seven rounds): the genuine external trigger for the delayed silent revert remains completely unknown. This round's refined evidence (tight ~1.5s timing; periodic recurrence outside any window; a monitor briefly vanishing from CCD's own enumeration, not merely going inactive) is recorded but not yet actionable into a falsifiable hypothesis."
  and_gate: "no for the round-7 fix in isolation -- a single, specific, one-directional structural gap (correction loop + verify-and-throw both missing the lost-survivor case) in one method, fixed by one new pure helper plus its two call sites. This is NOT the same defect as Symptom 2 part B (that one produces a DELAYED revert of an initially-CORRECT apply, discovered well after ActivateMonitors already returned; this one produces an IMMEDIATELY-wrong final state, discovered before ActivateMonitors ever returns) and is not the same defect as the still-unexplained PathChangeException trigger (that is a CCD-validation-layer question this fix does not answer, only defends against the consequence of). Three genuinely independent, differently-categorized open/closed items this round, not one chained fix disguised as three."
next_action: "Fix implemented and self-verified (build + full adjacent 209-test RigToggle.Tests suite unchanged + RigToggle.Windows.Tests builds cleanly with 5 new ComputeUnexpectedlyDeactivated unit tests, still cannot execute in this sandbox -- same pre-existing limitation as every prior round). Awaiting rig verification via the fresh human-verify checkpoint at the end of this file. Symptom 2 part B and the still-unexplained SAM748A+ACI24A4-specific PathChangeException trigger both remain open -- neither was fixed or guessed at this round; both are carried forward with refined evidence for whichever future round gets new data."

## Current Focus (round 6)

reasoning_checkpoint:
  hypothesis: "Two DISTINCT, code-level defects in round 5's own new code (fix D's correction path and fix E's instrumentation) are independently confirmed by direct source read and corroborated by the round-6 checkpoint response's rig-log analysis: (4a) ObservePostApplyStability's per-tick QueryActiveDevicePaths() call is not wrapped by its own try/catch -- only the OUTER try/catch around the entire thread body is -- so a single transient WindowsDisplayAPI.Exceptions.TargetNotAvailableException from PathDisplayTarget.get_DevicePath() (thrown when an active target transiently reports IsAvailable=false mid-CCD-transition, a documented landmine this same file already works around elsewhere via IsAvailable-filtering, e.g. GetAllMonitors/TryBuildScopedActivationPlan) silently ends the ENTIRE remaining observation window instead of just that one poll tick. (4b) MainForm.RefreshMonitorTiles's try/catch around _monitorController.GetAllMonitors() has no retry -- a transient Win32Exception(87) from the same underlying native query degrades _lastKnownMonitors to Array.Empty<MonitorInfo>() with zero retry, which both blanks the tile UI to the empty-dashboard state AND (more importantly) makes TryReactivelyCorrectAgainstLastIntent's foreach iterate an empty list, finding zero reactivated/wentInactive paths and logging a FALSE 'no unexpected drift detected' -- silently skipping a real correction opportunity exactly when a transient query failure coincides with an armed guard window. Separately, Symptom 2 part B's root external trigger REMAINS genuinely unresolved -- the round-6 checkpoint's 'reverts to a specific desktop pair' observation is informative in one narrow respect (a spontaneous OS-driven MIXED-direction drift event, not just single-direction 'undo', reproduced independent of any app correction) but is otherwise underdetermined given this rig has only 3 total monitors and only 2 non-rig ones, so 'always reverts to the [ACI24A4,DELA0B8] pair' is the ONLY possible non-rig 2-monitor state on this hardware regardless of which of several competing external mechanisms (persisted-database reassertion, some other Windows/driver auto-reconciliation) is actually responsible -- it does not discriminate between them, and round 5 already researched the SDC_SAVE_TO_DATABASE angle specifically and found it inconclusive, so this round does not re-research the same ground without a new angle."
  confirming_evidence:
    - "Direct code read (WindowsMonitorController.cs, ObservePostApplyStability, pre-fix): the entire while-loop body -- including QueryActiveDevicePaths() -- sits inside ONE try that only closes AFTER the loop; the catch is scoped to the whole thread delegate, not per-iteration. A poll-tick exception therefore falls straight through to that outer catch, logs once, and the delegate returns -- ending the background thread and thus the observation window early, for good, with no further polling. Matches the round-6 checkpoint response's claim (4a) exactly: a 6s window died at +898ms with no further data for the remainder."
    - "Direct code read (WindowsMonitorController.cs, QueryActiveDevicePaths): calls `t.DisplayTarget.DevicePath` directly on every TargetsInfo entry of every GetActivePaths() result, with NO IsAvailable filter -- unlike GetAllMonitors (line ~220) and TryBuildScopedActivationPlan (line ~696), which both explicitly filter on IsAvailable first specifically because DevicePath/FriendlyName throw TargetNotAvailableException on an unavailable target (documented in this file's own existing Pitfall-1 comments, not a new discovery this round). QueryActiveDevicePaths' assumption that an ACTIVE path's target is always available holds in steady state but is exactly the kind of assumption a mid-CCD-transition query (this background thread has zero coordination with any concurrent ActivateMonitors/DeactivateMonitors call, by design) can violate."
    - "Direct code read (MainForm.cs, RefreshMonitorTiles, pre-fix): a single try/catch around `_monitorController.GetAllMonitors()...ToList()`, catch sets `_lastKnownMonitors = Array.Empty<MonitorInfo>()` unconditionally, no retry, no distinction between 'genuinely zero monitors' and 'query transiently failed.'"
    - "Direct code read (MainForm.cs, TryReactivelyCorrectAgainstLastIntent): its drift-detection foreach iterates `_lastKnownMonitors` (populated by the CALLER's own prior RefreshMonitorTiles() in OnDisplaySettingsChanged, immediately before this method runs) against `_lastUserIntent`; an empty `_lastKnownMonitors` produces empty `reactivated`/`wentInactive` sets by construction, which hits the 'no unexpected drift detected' branch and returns -- structurally indistinguishable, from this method's own perspective, from a genuinely quiet, correctly-settled topology. Confirms claim (4b)'s 'a real reactive-correction opportunity was skipped' mechanism precisely."
    - "Round-6 checkpoint response, claim 1: fix D's atomic correction path (round 5) completed with EXIT success and no exception on every reactive correction observed in this trial -- corroborates (does not by itself independently re-derive, since the raw log was not attached verbatim this round) that fix D holds for the mixed-direction case it targeted; not refuted by anything read in current source this round (TryReactivelyCorrectAgainstLastIntent's fix-D code is unchanged since round 5 and matches the round-5 evidence's own description exactly)."
  falsification_test: "Fix for 4a: if a rig trial with the restructured per-tick try/catch still shows an ObservePostApplyStability window ending BEFORE its full 6s duration without a 'observation window closed' log line at the expected +6000ms mark, the fix is refuted -- some other code path outside the per-tick QueryActiveDevicePaths() call (e.g. Log() itself, or Thread.Sleep) must be the actual source of the early termination, not the hypothesized TargetNotAvailableException. Fix for 4b: if a rig trial reproduces a GetAllMonitors() Win32Exception(87) again and TryReactivelyCorrectAgainstLastIntent STILL logs 'no unexpected drift detected' immediately afterward (i.e. the one retry also transiently failed, or the true topology genuinely had no drift at that exact retry moment), that is NOT a refutation of this fix by itself -- it would only refute it if the retry demonstrably also observed the OLD (pre-mutation) topology on a case where the FIRST query's failure was proven to be a transient CCD-mid-transition artifact rather than a persistent one; a single retry cannot guarantee success, only convert a fraction of transient single-query failures into successful reads, which is the fix's actual, bounded claim."
  fix_rationale: "Fix F (4a): moves QueryActiveDevicePaths()'s call and the CHANGE-comparison inside their OWN try/catch, nested within the existing per-loop-iteration scope, so a transient per-tick exception is logged and the tick is skipped -- the while loop's own `stopwatch.Elapsed < PostApplyObservationDuration` condition on the NEXT iteration is untouched, so polling resumes exactly as if that tick had observed no change. The outer try/catch is kept (renamed in scope, not removed) as a last-resort backstop for anything outside the per-tick block (e.g. a hypothetical Log()-internal failure that somehow still propagates, or Stopwatch construction) -- this instrumentation must never crash the app regardless of cause, matching this method's own pre-existing 'diagnostic-only, never let a failure surface anywhere' contract. Pure evidence-gathering code, no behavior change to any correction/mutation path.
Fix G (4b): adds exactly ONE retry, after a 150ms backoff (matching this file's own existing SettlePollDelay cadence convention, not a new arbitrary value), inside RefreshMonitorTiles' existing catch block, before falling back to the pre-existing Array.Empty<MonitorInfo>() degrade path. Directly targets the specific, rig-observed timing coincidence (a transient CCD-query failure landing exactly when a correction's guard window was closing) without attempting to solve the general 'GetAllMonitors can fail' problem more broadly (e.g. no retry loop, no exponential backoff, no change to the confirm-dialog's OWN separate GetAllMonitors() call site, which has its own, already-adequate, non-correction-critical fallback). If both the original attempt and the one retry fail, behavior is unchanged from before this fix (empty-state degrade, unaffected UI/correction consequences) -- this fix can only ever convert some previously-total-failures into successes, never regress the already-existing degrade path."
  blind_spots:
    - "Cannot execute or verify either fix on real Windows CCD hardware in this sandbox -- same structural limitation as every prior round in this file."
    - "Fix G's single 150ms retry is a judgment call, not data-derived -- the round-6 checkpoint response did not report how long the underlying transient failure typically lasts (only that it happened once, at a moment coinciding with a guard window closing). If the true underlying CCD transition sometimes takes longer than 150ms to clear, one retry at that fixed delay could still fail on some fraction of occurrences -- unverified against real timing data."
    - "Fix F's per-tick catch necessarily means SOME poll ticks within a 6s window can silently miss a change if the exception coincides with the exact tick the change would have been observed on -- for a 250ms-cadence poll this is a narrow window, and the NEXT tick's comparison is still against the correctly-tracked `previous` value (never corrupted by a failed tick, since previous is only reassigned on a successful, changed read), so a genuine change is very unlikely to be permanently missed, only possibly delayed by one tick's worth of latency (~250ms) in the log's own change-detection resolution -- accepted as a bounded, minor loss of evidence-gathering precision, not a correctness defect, since this code performs no correction of its own."
    - "Symptom 2 part B's root cause is STILL completely open after six rounds -- this round adds no new hypothesis for it, only refines what the existing evidence can and cannot discriminate between (see hypothesis field). No fix is attempted this round for the revert itself, matching this session's established discipline against guessing without direct evidence -- fix E's instrumentation (now more reliable thanks to fix F) remains the mechanism by which that evidence will eventually be gathered."
  candidate_causes:
    - "code (fix F target, round 5's own new code): ObservePostApplyStability's try/catch scope was the whole thread body instead of per-poll-tick, so one transient CCD-query exception silently truncated the rest of a 6s observation window."
    - "code (fix G target, round 5's own new code interaction with pre-existing code): RefreshMonitorTiles' pre-existing no-retry catch, combined with fix B/D's TryReactivelyCorrectAgainstLastIntent depending on RefreshMonitorTiles' freshly-populated _lastKnownMonitors for its own drift detection, meant a transient query failure produced a FALSE 'no drift' verdict instead of a genuine 'could not check this time' signal -- two independently-real, pre-existing pieces of code (neither one alone a bug) combining into a real, rig-observed correction-skipping gap."
    - "environment/OS-driver (Symptom 2 part B, UNCHANGED, still uneliminated): the genuine external trigger for the delayed silent revert remains completely unknown after six rounds. The round-6 checkpoint's 'reverts to a specific desktop pair' observation is refined evidence (see hypothesis field) but not a new, independently-actionable root cause this round -- it neither confirms nor rules out persisted-database reassertion vs. any other single-topology-target external mechanism, since this rig's fixed 3-monitor hardware makes that specific observation underdetermined between competing hypotheses."
  and_gate: "no for fix F in isolation (a single try/catch-scope defect, one method, one file). no for fix G in isolation (a single missing-retry defect, one method, one file). Fix F and fix G are two INDEPENDENT actions addressing two DIFFERENT, unrelated defects discovered in the same rig trial (an instrumentation-thread's error-scope bug vs. a UI-refresh-path's missing-retry bug) -- fixing one does not depend on or substitute for the other, matching this file's established and_gate convention. Symptom 2 part B itself is explicitly NOT being fixed this round, so no and_gate applies to it here -- it remains an open, unresolved item carried forward unchanged."

next_action: "Fix F and fix G implemented and self-verified (build + full adjacent 209-test suite unchanged; RigToggle.Windows.Tests builds cleanly, still cannot execute in this sandbox -- both fixes touch code paths with no injectable seam, matching every prior MainForm/background-thread round in this file). Awaiting rig verification via the fresh human-verify checkpoint at the end of this file. Symptom 2 part B remains open -- no fix attempted this round; fix E's instrumentation (now hardened by fix F) continues to be this session's mechanism for eventually gathering the evidence needed to root-cause it."

## Current Focus (round 5)

reasoning_checkpoint:
  hypothesis: "Round 4's checkpoint failed for a fifth-rig-test reason distinct from anything fix C targeted: TryReactivelyCorrectAgainstLastIntent's bidirectional correction (fix B) issues its two directions (DeactivateMonitors(reactivated), then ActivateMonitors(wentInactive)) as separate, order-dependent CCD calls instead of one atomic, swap-aware call. Whenever wentInactive's target is the only active survivor not in reactivated, the first call leaves zero survivors and is correctly refused by DeactivateMonitors' own zero-survivors guard, so the second, uncoordinated ActivateMonitors call (issued with an empty monitorSwapDisableSet, uninformed about reactivated) either produces a transient over-full topology or hits the same scoped-plan/PathChangeException fragility a non-swap-aware call carrying unwanted survivors is known to trigger. Fix D makes this one atomic call: ActivateMonitors(wentInactive, monitorSwapDisableSet: reactivated). Separately and independently, Symptom 2 part B (the delayed ~2-3.5s silent revert itself) remains genuinely unexplained after five rounds -- fix E adds pure, non-corrective observability (fine-grained post-apply active-path polling) rather than a sixth speculative reactive patch, since guessing at yet another specific external mechanism without first seeing the revert's own shape has not worked in four prior attempts."
  confirming_evidence:
    - "Direct code read of TryReactivelyCorrectAgainstLastIntent (pre-fix, MainForm.cs): two separate try/catch blocks -- `_monitorController.DeactivateMonitors(reactivated)` first, `_monitorController.ActivateMonitors(wentInactive, monitorSwapDisableSet: new HashSet<string>())` second, unconditionally empty disable-set regardless of reactivated's contents. Not inference -- read directly from the exact source this round's rig log's stack trace names."
    - "Direct code read of DeactivateMonitors' zero-survivors guard (WindowsMonitorController.cs): `if (survivors.Length == 0) { ... throw new InvalidOperationException(\"Cannot disable all configured monitors -- at least one active display must remain.\"); }` -- message and control flow match the round-5 rig log's exception line verbatim, at the exact call site the log's stack trace identifies (MainForm.TryReactivelyCorrectAgainstLastIntent calling WindowsMonitorController.DeactivateMonitors)."
    - "Round-5 rig log, first trial (11:56:45.842-47.049): DeactivateMonitors(reactivated=[ACI24A4,DELA0B8]) threw exactly this guard; the follow-up bare ActivateMonitors(SAM748A) call then produced finalActive=[ACI24A4,DELA0B8,SAM748A] -- a transient 3-active-monitor state, requiring a SECOND reactive-correction round to clean up, consuming half the correction budget on a single drift event."
    - "Round-5 rig log, second trial (11:57:10.305-10.897): the identical two-step sequence this time had the follow-up ActivateMonitors call's scoped plan throw PathChangeException outright, fall back to the known-unreliable Extend, and fail to activate the target at all -- D-05 aborted with the app left in the WRONG final state (both desktop monitors active, rig monitor inactive), a strictly worse outcome than the first trial's transient-3-active recovery."
    - "Round-5 rig log, BOTH trials' PRIMARY toggle-driven ActivateMonitors calls (11:56:42.348 and 11:57:05.765) applied via scoped ApplyPathInfos WITHOUT throwing PathChangeException -- confirming fix C (round 4's present-but-inactive + origin-promotion fix) is not implicated in either failure; both failures originate entirely in the reactive-correction code path fix D targets."
  falsification_test: "Fix D: if a rig trial reproducing the exact mixed-direction drift shape (a monitor unexpectedly goes inactive AND a different one unexpectedly reactivates in the same OnDisplaySettingsChanged observation, with the inactive target's replacement being the sole remaining survivor) still throws the zero-survivors guard, produces a transient over-full topology, or hits PathChangeException, fix D is refuted -- the atomic single-call approach would need to be revisited, possibly because ActivateMonitors' own scoped-plan construction has a further gap this round's evidence didn't surface. Fix E (instrumentation): if the next rig trial's debug.log shows NO ObservePostApplyStability CHANGE line at all despite Symptom 2 part B reproducing, the revert would be occurring in a way this app-level active-path polling cannot observe (e.g. purely at the WinForms/screen-enumeration layer, never in GetActivePaths() itself) -- pointing toward a fundamentally different diagnostic approach (e.g. Windows Event Log correlation) next round."
  fix_rationale: "Fix D reuses the exact 'one CCD call both activates the target(s) and deactivates the disable-set' primitive already established and rig-validated for the deliberate Rig/Normal swap (fix A) -- not a new mechanism, just applying the SAME primitive to the reactive-correction call site that had never been made swap-aware. Provably behavior-preserving for both single-direction cases (traced in Resolution.verification.no_op_deletion below); only the previously-broken mixed-direction case changes. Fix E deliberately does NOT attempt to explain or correct Symptom 2 part B this round -- per the debugging philosophy's 'don't guess, gather evidence' discipline, and given four consecutive rounds of reactive/downstream fixes have failed to root-cause it, adding a fifth speculative correction without first observing the revert's own moment-by-moment shape would repeat an already-established failure pattern rather than break it."
  blind_spots:
    - "Cannot execute or verify either fix on real Windows CCD hardware in this sandbox -- same structural limitation as every prior round in this file."
    - "Fix D's atomic call assumes ActivateMonitors' own scoped-plan construction (fix C, round 4) correctly handles the reactivated set as a disable-set in this reactive-correction context exactly as it does in the deliberate-toggle context fix A/C were validated against -- not yet independently rig-confirmed for a CALL ORIGINATING FROM the reactive watchdog specifically, only inferred from the two contexts sharing the same underlying ActivateMonitors method."
    - "Fix E's 6-second, 250ms-cadence window is a guess, not evidence-derived -- chosen to comfortably exceed every observed revert delay so far (~2.15s to ~3.5s) with margin, but if the true delay is sometimes longer, the observation window could close before the revert happens on some trials. Not tuned against real data yet since none of that data exists prior to this round's instrumentation actually running once."
    - "Symptom 2 part B remains completely unexplained -- fix E is instrumentation, not a hypothesis. The candidate mechanisms considered this round (GPU vendor control-panel auto-profile-restore, Windows persisted-database reconciliation) were researched but neither confirmed nor ruled out (see Evidence 2026-08-28T13:58:00Z) -- next round's fix, if any, still depends entirely on what fix E's data shows."
  candidate_causes:
    - "code (fix D target): MainForm.TryReactivelyCorrectAgainstLastIntent's mixed-direction correction was two separate, order-dependent CCD calls instead of one atomic, swap-aware call -- confirmed via direct code read matching the rig log's exact exception message and stack trace."
    - "code (fix C, round 4, NOT implicated this round): TryBuildScopedActivationPlan's array-shape handling -- explicitly ruled out as the cause of THIS round's two failures, since both primary toggle-driven calls (which exercise fix C directly) applied cleanly; only the differently-shaped, non-swap-aware reactive-correction call failed."
    - "environment/OS-driver (Symptom 2 part B, UNCHANGED, still uneliminated): the genuine external trigger for the delayed silent revert remains completely unknown after five rounds -- fix E targets gathering evidence for this candidate cause, not fixing it."
  and_gate: "no for fix D in isolation (a single, specific non-atomicity defect, one code change, one file) -- but yes at the symptom-2 level overall: fix D (this round) and fix E (this round) are two INDEPENDENT actions addressing two DIFFERENT, unrelated problems discovered in the same rig trial (a code-level correction-ordering bug vs. an entirely unexplained OS-level revert) -- fixing one does not depend on or substitute for the other, matching this file's established and_gate convention of only claiming a genuine AND-relationship where the evidence requires it."

next_action: "Fix D implemented, built, and self-verified (209/209 RigToggle.Tests pass unchanged; RigToggle.Windows.Tests builds cleanly, still cannot execute in this sandbox). Fix E (pure evidence-gathering instrumentation) also implemented and built. Awaiting rig verification via the fresh human-verify checkpoint at the end of this file -- this round's ask specifically needs: (1) reproduction of the mixed-direction drift scenario to confirm fix D's atomic correction holds, and (2) whatever ObservePostApplyStability CHANGE lines fix E's instrumentation captures in debug.log around the NEXT occurrence of Symptom 2 part B, regardless of whether fix D's own scenario reproduces -- that data is this session's actual bottleneck now."

## Evidence

- timestamp: 2026-08-23T00:00:00Z
  checked: .planning/debug/resolved/monitor-enable-reactivates-others-again.md (prior resolved session, same file/method) via knowledge-base semantic match
  found: Round 5 of that session (rig-confirmed 2026-08-22) introduced TryBuildScopedActivationPlan inside ActivateMonitors, which builds the newly-activated target's PathInfo via the no-mode-info constructor specifically so the driver's best-mode-logic fills in mode data instead of this code guessing -- explicitly documented as intentional, to avoid the three previously-failed manual-reconstruction attempts (ccd-topology-restore-findings).
  implication: This is very likely the introducing change for the current bug -- it is the only recent change to this method's activation code path, it was rig-confirmed to fix a different, real bug (the reactivation flash), and its own doc comments already flag that it does not supply position data, which is exactly today's symptom.

- timestamp: 2026-08-23T00:05:00Z
  checked: src/RigToggle.Windows/WindowsMonitorController.cs, full read (ActivateMonitors, TryBuildScopedActivationPlan, DeactivateMonitors)
  found: TryBuildScopedActivationPlan's newPaths.Add used a mode-less PathInfo constructor -- confirmed no Position/Resolution/PixelFormat argument. DeactivateMonitors, by contrast, already captures and reuses REAL live Position/Resolution/PixelFormat when repositioning survivors -- confirming a mode-carrying PathInfo constructor overload exists and is already used safely elsewhere in this exact file.
  implication: The fix is a small, targeted change reusing an already-proven constructor overload and an already-KB-endorsed technique (capture mode data while the path is live, ccd-topology-restore-findings finding 5b) -- not new territory.

- timestamp: 2026-08-23T00:08:00Z
  checked: src/RigToggle.Core/ToggleService.cs (ToggleToRigMode/ToggleToNormalMode Monitor steps) and src/RigToggle.App/Program.cs (composition root)
  found: Both toggle directions call ActivateMonitors immediately followed by DeactivateMonitors inside one step (Pitfall 2 ordering). WindowsMonitorController is constructed exactly once at the composition root and shared by both MainForm's tile actions and ToggleService's toggle flow.
  implication: A single instance-level cache addresses both reported repro paths without needing cross-process persistence.

- timestamp: 2026-08-23T08:30:00Z
  checked: Post-fix rig trial of the position-mode cache fix (Symptom 1) plus a Rig-mode toggle trial (Symptom 2), user-provided debug.log excerpt
  found: Symptom 1 CONFIRMED FIXED on real hardware. Symptom 2 still broken, but with DIFFERENT log signatures than the original shared-cause hypothesis predicted.
  implication: The original "same root cause, two call sites" hypothesis is PARTIALLY REFUTED -- correct for Symptom 1, wrong for Symptom 2 (see Eliminated).

- timestamp: 2026-08-23T08:31:00Z
  checked: debug.log lines around 09:29:42.641-09:29:47.008 (Rig-mode toggle trial, forward direction, round-5 scoped path still in use)
  found: ActivateMonitors(SAM748A)+DeactivateMonitors(ACI24A4,DELA0B8) both completed with DeactivateMonitors' own verify-and-throw logging a clean success. ~3.2s later, with no intervening app-initiated call, OnDisplaySettingsChanged fired and RefreshMonitorTiles observed the pre-toggle topology restored.
  implication: Not a missing-position-data problem -- a durability problem. Also confirmed the toggle-switch code path never calls ArmIntentGuard, so the reactive watchdog had no intent baseline to even attempt a correction.

- timestamp: 2026-08-23T08:32:00Z
  checked: debug.log line around 09:30:06.685 (Normal-mode toggle trial, reverse direction, round-5 scoped path)
  found: The scoped ApplyPathInfos call threw PathChangeException("Invalid paths information.") outright and fell back to ApplyTopology(Extend), which then succeeded.
  implication: The reverse direction fails a different way (hard validation failure vs. silent revert) but from the same scoped-activation call.

- timestamp: 2026-08-23T09:15:00Z
  checked: ToggleService.cs Monitor steps and TryBuildScopedActivationPlan re-read against the "full swap" failure mode
  found: TryBuildScopedActivationPlan's plan was `currentActivePaths.Concat(newPaths)` -- unconditionally includes every already-active survivor PLUS the new target(s), so the forward direction transiently held 3 simultaneously-active paths, and the reverse direction's greedy per-target source selection had no mutual-compatibility check across 2 new targets.
  implication: Sufficient, code-level explanation for both round-2 failure modes -- led to the isPartOfMonitorSwap->Extend fix (later itself superseded, see below).

- timestamp: 2026-08-23T09:25:00Z
  checked: src/RigToggle.App/MainForm.cs -- ToggleSwitch_ActionRequested, RefreshUi(), ArmIntentGuard()/TryReactivelyCorrectAgainstLastIntent() (pre-fix)
  found: ToggleSwitch_ActionRequested called RefreshUi() (mode text/tray icon only) after the toggle, never RefreshMonitorTiles() or ArmIntentGuard(). ArmIntentGuard() was only called from OnTileAction.
  implication: Confirmed a genuine secondary gap (the watchdog has no intent baseline after a toggle-driven change) -- deliberately deferred at the time in favor of fixing the scoped-activation path directly; explicitly flagged for revisit if a residual revert survived. It did (see round 3 below), triggering that revisit.

- timestamp: 2026-08-23T10:05:00Z
  checked: Third rig test (round 3, first trial) -- round-2 fix (isPartOfMonitorSwap->Extend) tested from the ORIGINAL Symptom 2 repro starting state (DELA0B8 already independently disabled, only ACI24A4 active going in)
  found: "isPartOfMonitorSwap=true -- skipping scoped activation" logged correctly. ActivateMonitors(SAM748A) called Extend; the settle-poll immediately after NEVER observed SAM748A active at any attempt, while DELA0B8 (independently pre-disabled, unrequested) came back active. The correction loop's DeactivateMonitors(DELA0B8) succeeded, but SAM748A never became active anywhere in the sequence -- final verify-and-throw correctly threw, D-05 fail-safe preserved (no partial state), but the toggle's goal was not accomplished.
  implication: Round 2's fix does not work. Extend fails in a NEW way beyond what round 5's scoped path was built to prevent.

- timestamp: 2026-08-23T10:20:00Z
  checked: Decompiled WindowsDisplayAPI 1.3.0.13's PathInfo class via ilspycmd (only the compiled NuGet DLL is available in this sandbox, not vendored source) -- ApplyTopology, ApplyPathInfos, GetDisplayConfigPathInfos
  found: ApplyTopology's entire body is `DisplayConfigApi.SetDisplayConfig(0u, null, 0u, null, SetDisplayConfigFlags.Apply | topology-flags)` -- path/mode arrays are literally `0u, null, 0u, null`. ApplyPathInfos, by contrast, builds real DisplayConfigPathInfo[]/DisplayConfigModeInfo[] arrays from the caller's supplied PathInfo objects and passes those non-null arrays with SDC_USE_SUPPLIED_DISPLAY_CONFIG/SDC_TOPOLOGY_SUPPLIED.
  implication: Definitive, direct-code-read confirmation that ApplyTopology(Extend) structurally CANNOT be given an explicit desired active-path-set. Only ApplyPathInfos-based scoped activation, made swap-aware, is structurally capable of the required guarantee.

- timestamp: 2026-08-23T10:35:00Z
  checked: DeactivateMonitors' existing, already rig-confirmed ApplyPathInfos usage -- how excluding a path from the supplied array deactivates it with no separate call
  found: DeactivateMonitors supplies only `pathsToApply` (survivors, excluding the paths being disabled); the excluded targets go inactive as a side effect of ApplyPathInfos' "supplied array replaces the whole active topology" semantics (confirmed by the decompile above). Already rig-proven mechanism.
  implication: ActivateMonitors' own scoped plan can reuse the identical mechanism for swap-exclusion -- not a new, unproven CCD behavior.

- timestamp: 2026-08-23T10:45:00Z
  checked: Third rig test (round 3, SECOND trial, single-tile enable path -- previously believed unaffected and confirmed working in round 1)
  found: CRITICAL -- the silent-revert pattern reproduced on the SIMPLE single-tile ENABLE path (monitorSwapDisableSet empty, round-5 scoped ApplyPathInfos in use, not Extend). Scoped activation succeeded (finalActive included the target, verified), then ~2.15s later RefreshMonitorTiles observed the target inactive again -- OS-level silent revert, no app-initiated call in between, no exception. Faster than round 2's ~3.2s swap-path revert but the same class of event.
  implication: The root instability is NOT "which CCD API call is used" -- both Extend and scoped ApplyPathInfos exhibit a delayed silent revert. Strongly suggests a genuine Windows/display-driver-level behavior: an apply that appears to succeed (verified via the existing settle-poll) is not durable and gets undone 2-3+ seconds later, outside the app's synchronous control window, on BOTH the swap and non-swap paths.

- timestamp: 2026-08-23T10:55:00Z
  checked: src/RigToggle.App/MainForm.cs -- TryReactivelyCorrectAgainstLastIntent (pre-fix, full body) and OnTileAction's enable branch, re-read specifically to test whether the watchdog itself could be self-inflicting the revert
  found: TryReactivelyCorrectAgainstLastIntent's only corrective branch was `monitor.IsActive && !shouldBeActive -> DeactivateMonitors(...)`, with its own doc comment stating "never the reverse direction... nothing here re-enables anything." For the third-rig-test scenario (target activated, intent recorded as should-be-active, then observed inactive), this exact code path structurally cannot fire -- shouldBeActive is true, so the `!shouldBeActive` condition is false. Confirmed the watchdog is NOT self-inflicting the revert (ruled out as a candidate cause) but is ALSO not correcting it -- a real, independent gap.
  implication: Eliminated "the watchdog itself causes the revert" as a hypothesis. Confirmed "the watchdog cannot correct this direction of drift, and was never armed on the toggle path at all" as the two-part fix-B target -- a code-level gap fully explaining why the app never caught or corrected either round-2's or round-3's revert, independent of whatever OS/driver-level mechanism actually causes the underlying drift.

- timestamp: 2026-08-23T11:00:00Z
  checked: Implemented and built both fixes (see files_changed in Resolution) -- Fix A: TryBuildScopedActivationPlan gains a monitorSwapDisableSet parameter; ActivateMonitors always attempts scoped activation (isPartOfMonitorSwap-triggers-Extend-bypass branch removed) and caches live mode for swap-excluded survivors via a new shared CacheLiveModes helper before excluding them. Fix B: TryReactivelyCorrectAgainstLastIntent made bidirectional (adds a wentInactive branch calling ActivateMonitors, symmetric with the existing reactivated/DeactivateMonitors branch); ToggleSwitch_ActionRequested and PerformBackgroundToggle now call RefreshMonitorTiles()+ArmIntentGuard() after a toggle, matching OnTileAction's existing pattern.
  found: `dotnet build RigToggle.sln --no-incremental` succeeds (0 warnings, 0 errors, all 6 projects). `dotnet test src/RigToggle.Tests` -- 209/209 passed, unchanged, including all 4 existing isPartOfMonitorSwap routing tests (log-line format deliberately preserved in FakeMonitorController). `dotnet test src/RigToggle.Windows.Tests` still cannot execute in this sandbox (missing Microsoft.WindowsDesktop.App) -- pre-existing, unrelated limitation. MainForm.cs (fix B) has no dedicated automated test coverage in this repo (WinForms UI layer, matching this codebase's existing test-layer boundaries) -- self-verified via build + code read only, same constraint as every prior MainForm-touching round in this file.
  implication: Both fixes compile cleanly and do not regress any existing automated coverage. Whether either fix durably holds on real hardware -- especially fix B's ability to actually observe and correct the revert within its 8s/2-correction budget -- is deferred to the mandatory human-verify checkpoint below.

- timestamp: 2026-08-28T09:36:00Z
  checked: Decompiled WindowsDisplayAPI 1.3.0.13's PathInfo.GetDisplayConfigPathInfos and PathInfo.ApplyPathInfos/ValidatePathInfos (ilspycmd, direct code read -- only the compiled NuGet DLL is available in this sandbox, matching every prior decompile in this file)
  found: GetDisplayConfigPathInfos sets each DisplayConfigPathInfo entry's Active flag per-target from `pathTargetInfo.IsPathActive` -- a PathInfo whose TargetsInfo has IsPathActive=false still produces a real, present array entry (source+target identity intact), just without the Active flag and with an invalid mode index (since IsModeInformationAvailable=false skips AddMode). ApplyPathInfos/ValidatePathInfos both reject the array (returns/throws) only when the array is entirely empty (displayConfigPathInfos.Length == 0) or the native SetDisplayConfig call itself returns non-success -- confirming the library imposes no artificial minimum-count rule itself; any rejection of round 3's single-entry array happens at the native SetDisplayConfig validation layer, not in the wrapper.
  implication: The wrapper library structurally supports "present but explicitly inactive" as a first-class array entry, distinct from "absent" -- round 3's omission was a choice, not a library limitation. This directly enables round 4's fix (keep swap-excluded survivors in the array, flagged inactive) without needing any new library capability.

- timestamp: 2026-08-28T09:38:00Z
  checked: Web research -- OSR Developer Community CCD thread ("Enable / Disable Secondary Monitor using SetDisplayConfig") and an independent SetDisplayConfig usage write-up, cross-referenced against each other
  found: Both independently describe the same pattern for disabling one monitor while keeping others active: "you do NOT remove the DISPLAYCONFIG_PATH_INFO structure from the array entirely -- instead you clear the DISPLAYCONFIG_PATH_ACTIVE flag for the monitor you want to disable, keeping the path in the array" and "for inactive paths, unused mode indices should be initialized to DISPLAYCONFIG_PATH_MODE_IDX_INVALID."
  implication: Two independent, corroborating sources confirm round 3's "just omit the disable-set" approach violates the documented/community-established contract for SDC_USE_SUPPLIED_DISPLAY_CONFIG -- strong, non-speculative grounding for round 4's fix direction.

- timestamp: 2026-08-28T09:39:00Z
  checked: Web research -- Microsoft's Desktop-Layout driver documentation and blog.lohr.dev's "Changing the primary display on Windows by code" (independent real-world SetDisplayConfig usage write-up)
  found: "The source surface located at (0,0) is always the primary source surface" (official MS driver docs, desktop-layout no-gaps/no-overlaps positioning contract); the concrete pattern for changing which display is primary is "set the new primary's source-mode position to (0,0), then move every OTHER source by the negative of the OLD primary's position" -- i.e. a uniform shift, exactly mirroring DeactivateMonitors' own existing `anyTargetWasPrimary` uniform-shift logic in this same file (already rig-proven for the deactivation direction).
  implication: A single-entry scoped-activation array whose sole path carries a cached, non-origin position (because that target was previously a non-primary monitor) violates this origin/primary contract -- independently sufficient to explain a PathChangeException even if the array-presence issue above were somehow not in play. Confirms round 4's second fix component (PromoteToOriginIfNeeded) targets a real, documented Windows requirement, not a speculative addition.

- timestamp: 2026-08-28T09:55:00Z
  checked: Implemented both round-4 fix components in src/RigToggle.Windows/WindowsMonitorController.cs (TryBuildScopedActivationPlan rewritten to keep swap-excluded survivors present-but-inactive; new PromoteToOriginIfNeeded pure helper) plus new unit tests in src/RigToggle.Windows.Tests/WindowsMonitorControllerTests.cs covering PromoteToOriginIfNeeded directly (no live CCD hardware needed -- PathDisplaySource/PathDisplayAdapter/LUID all have public, hardware-independent constructors)
  found: `dotnet build RigToggle.sln --no-incremental` succeeds (0 warnings, 0 errors, all 6 projects -- the 6 pre-existing xUnit1031 warnings in unrelated test files, SingleInstanceGuardTests.cs/ToggleOrchestratorTests.cs, are untouched by this round's changes). `dotnet test src/RigToggle.Tests` -- 209/209 passed, unchanged. `dotnet build src/RigToggle.Windows.Tests` succeeds including the 6 new PromoteToOriginIfNeeded tests, but `dotnet test src/RigToggle.Windows.Tests` still cannot EXECUTE in this sandbox (missing Microsoft.WindowsDesktop.App on Linux) -- same pre-existing, already-documented limitation as every prior round in this file. Manually traced PromoteToOriginIfNeeded's logic against the exact round-4 repro shape (currentActivePaths=[ACI24A4,DELA0B8] both in disableSet, devicePathsToActivate={SAM748A} with a non-origin cached position) by hand: keptActiveSurvivors ends up empty, explicitlyInactiveDisableSet gets both excluded paths (mode-blanked), newPaths gets SAM748A at its cached (non-origin) position, PromoteToOriginIfNeeded finds no entry at origin and shifts SAM748A's position to (0,0) -- producing a 3-entry array (1 active-at-origin, 2 explicitly-inactive) instead of round 3's rejected 1-entry array.
  implication: Both fix components compile, integrate, and (by hand-trace, since this sandbox cannot execute WindowsDisplayAPI-dependent tests) produce the exact array shape the research says the CCD API should accept for the round-4 repro. Genuine correctness is still unconfirmed pending real hardware -- deferred to the mandatory human-verify checkpoint below, same discipline as every prior round.

- timestamp: 2026-08-28T13:50:00Z
  checked: Round-4 checkpoint response -- fifth rig test, two trials (a toggle-to-Rig-mode swap, then an independent tile-disable-then-toggle repro), verbatim debug.log excerpt provided by the user/orchestrator
  found: Round 4's fix (present-but-inactive representation + PromoteToOriginIfNeeded) did NOT throw PathChangeException on either PRIMARY, toggle-driven ActivateMonitors call this round (11:56:42.348 planPaths=[SAM748A, ACI24A4, DELA0B8] and 11:57:05.765 planPaths=[SAM748A, ACI24A4], both "completed without throwing"). BUT in both trials, ~2.3s and ~3.5s respectively after a verified-successful apply (ArmIntentGuard logged, OnDisplaySettingsChanged confirmed clean, no drift), a SECOND OnDisplaySettingsChanged fired on its own with NO intervening app-initiated call and RefreshMonitorTiles observed the pre-toggle topology fully restored -- the exact "delayed silent revert" pattern from rounds 2 and 3, reproducing on the scoped ApplyPathInfos path unchanged by round 4's fix.
  implication: Round 4's fix (fix C) is NOT refuted by this round's evidence -- its target array-shape defect did not recur on either primary call. Symptom 2 part B (the delayed silent revert itself) is CONFIRMED STILL UNRESOLVED after four rounds of fixes targeting different downstream mechanisms -- its root trigger remains genuinely unknown.

- timestamp: 2026-08-28T13:52:00Z
  checked: MainForm.cs TryReactivelyCorrectAgainstLastIntent (direct code read, current source, lines ~1000-1037 pre-fix) against the round-5 rig log's stack traces
  found: Confirmed exactly as the orchestrator's preliminary read described -- the pre-fix correction block called `_monitorController.DeactivateMonitors(reactivated)` as a first, standalone try/catch block, THEN `_monitorController.ActivateMonitors(wentInactive, monitorSwapDisableSet: new HashSet<string>())` as a second, separate try/catch block -- two uncoordinated, non-atomic CCD calls, the second one unconditionally non-swap-aware (hardcoded empty disable-set) regardless of what reactivated contained. Cross-checked against WindowsMonitorController.DeactivateMonitors' zero-survivors guard (direct code read): `if (survivors.Length == 0) { ... throw new InvalidOperationException("Cannot disable all configured monitors -- at least one active display must remain."); }` -- the exact message and control flow the rig log's exception line reproduces, at line numbers consistent with the log's own stack trace (MainForm.cs line ~1008 calling into WindowsMonitorController.cs line ~1037).
  implication: The orchestrator's preliminary read is CONFIRMED by independent direct code read, not merely plausible -- this is a real, reproducible, code-level bug distinct from and additional to rounds 1-4's four confirmed causes. When wentInactive's sole target is the only active survivor NOT in reactivated, the first (Deactivate) call structurally cannot succeed, so reactivated is never turned off by it, and the second (Activate) call -- uninformed about reactivated -- either produces a transient over-full topology (first trial, 11:56:45-47) or inherits the same malformed-array/PathChangeException fragility TryBuildScopedActivationPlan's own remarks document for a non-swap-aware call carrying survivors the caller actually wants gone (second trial, 11:57:10, where it fell back to the known-unreliable Extend and D-05 aborted with the WRONG final state).

- timestamp: 2026-08-28T13:58:00Z
  checked: Web research -- SDC_SAVE_TO_DATABASE / persistence-database reconciliation as a candidate external mechanism for Symptom 2 part B's delayed revert (both call sites in this codebase use saveToDatabase: false uniformly, which was not yet considered as a candidate cause)
  found: Official SetDisplayConfig documentation confirms SDC_SAVE_TO_DATABASE persists a topology to the database and SDC_USE_DATABASE_CURRENT/SDC_TOPOLOGY_SUPPLIED can pull mode info FROM that database on a subsequent call this app itself makes -- but no source found describes an OS-initiated, unprompted, timer-driven reconciliation that would silently re-apply a stale database entry over a live, un-persisted topology change on its own, seconds later, with no app or user action. Separately, generic driver-forum reports of monitors "silently reactivating" describe AMD/Radeon-specific quirks but none matching this exact "N seconds after a clean apply, reverts to the PRIOR state" shape with the required precision.
  implication: Inconclusive -- this specific candidate mechanism (persisted-database reconciliation) is neither confirmed nor ruled out by available documentation/community sources. Per the research-vs-reasoning guidance, further guessing at a specific external cause without first observing the revert's own shape firsthand would repeat this session's established pattern of speculative, reactive-only fixes -- redirected effort toward direct observability instead (see fix E below).

- timestamp: 2026-08-28T14:30:00Z
  checked: Round-5 checkpoint response (sixth rig test) -- orchestrator-summarized rig-log analysis, four claims, cross-checked against direct reads of the CURRENT source of MainForm.cs (TryReactivelyCorrectAgainstLastIntent, fix D) and WindowsMonitorController.cs (ObservePostApplyStability, fix E, and QueryActiveDevicePaths/RefreshMonitorTiles' consumer)
  found: "(1) Fix D's code is unchanged since round 5 and still matches the round-5 evidence's own description of the atomic, swap-aware correction exactly -- consistent with the checkpoint's claim that every reactive correction observed in this trial completed with EXIT success and no exception; not independently re-derived from a raw log this round (none was attached verbatim), so treated as corroborated-but-secondhand, not personally re-verified against log text. (2) Symptom 2 part B reproduced again (claim 2), unchanged in status -- still genuinely open. (3) The 'reverts to the same [ACI24A4, DELA0B8] desktop pair' observation (claim 3) is real but its own worked example is more informative than the headline framing suggests: after a tile-enable produced [SAM748A, DELA0B8], the revert went to [ACI24A4, DELA0B8] -- i.e. SAM748A went inactive AND ACI24A4 came back active in the SAME spontaneous event, a mixed-direction drift shape identical in KIND to what fix D was built to correct, except this one originates from the OS/driver itself with no app-initiated call involved at all. (4) Bugs 4a/4b: direct code read confirms both exactly as summarized -- see the two entries immediately below."
  implication: "Fix D is not refuted by anything in current source or this round's checkpoint response. Symptom 2 part B remains unfixed, but the 'always reverts to the desktop pair' framing is UNDERDETERMINED as evidence for any SPECIFIC external mechanism -- this rig has only 3 total monitors (SAM748A, ACI24A4, DELA0B8), so [ACI24A4, DELA0B8] is the ONLY possible non-rig 2-monitor topology on this hardware; a hypothesis of 'reasserts the persisted CCD database entry', 'reasserts whatever the last non-Rig-Toggle-caused topology was', or several other candidate mechanisms would all predict this exact same observation on this specific rig, so it does not discriminate between them. What IS newly informative is the mixed-direction shape of the specific worked example -- it rules out 'the revert is simply undoing the single most recent apply' as a complete description (since the immediately-prior state was [SAM748A] alone, not [ACI24A4, DELA0B8]), and is consistent with (not proof of) some kind of external topology reassertion that predates this session's own actions. Not enough, on its own, to form a new falsifiable hypothesis this round -- carried forward as refined framing for whichever round eventually gets fresh fix-E instrumentation data with enough trials to test it."

- timestamp: 2026-08-28T14:35:00Z
  checked: Direct code read, WindowsMonitorController.cs ObservePostApplyStability (round 5's own new code, pre-round-6-fix) and QueryActiveDevicePaths, cross-referenced against the round-6 checkpoint response's claim 4a (a 6s observation window died at +898ms after an unhandled TargetNotAvailableException)
  found: The entire poll loop body -- including the `QueryActiveDevicePaths()` call -- sat inside ONE try block that only closes AFTER the while loop; the matching catch is scoped to the whole background-thread delegate, not per-iteration. QueryActiveDevicePaths itself calls `t.DisplayTarget.DevicePath` on every TargetsInfo entry of every GetActivePaths() result with NO IsAvailable filter -- unlike GetAllMonitors and TryBuildScopedActivationPlan elsewhere in this same file, which both explicitly filter on IsAvailable first, specifically because this file's own existing comments document that DevicePath/FriendlyName throw TargetNotAvailableException on an unavailable target (06-RESEARCH.md Pitfall 1's "pitfall inside the pitfall", already load-bearing elsewhere, not a new discovery this round).
  implication: Confirmed, exact mechanism match for claim 4a -- a single transient TargetNotAvailableException during any one poll tick (plausible mid-CCD-transition, since this background thread has zero coordination with any concurrent Activate/DeactivateMonitors call, by design) falls straight to the outer catch, logs once, and ends the delegate -- terminating the rest of the 6s observation window with no further data. Real, scoped, code-level bug in round 5's own new instrumentation code, independent of and unrelated to Symptom 2 part B's own unresolved root cause.

- timestamp: 2026-08-28T14:38:00Z
  checked: Direct code read, MainForm.cs RefreshMonitorTiles (pre-round-6-fix) and TryReactivelyCorrectAgainstLastIntent's drift-detection foreach, cross-referenced against the round-6 checkpoint response's claim 4b (a Win32Exception(87) from GetActivePaths coincided with a correction's guard window expiring, skipping a real correction opportunity)
  found: RefreshMonitorTiles' catch block unconditionally set `_lastKnownMonitors = Array.Empty<MonitorInfo>()` with no retry on any GetAllMonitors() exception. TryReactivelyCorrectAgainstLastIntent's drift-detection foreach iterates `_lastKnownMonitors` (populated by OnDisplaySettingsChanged's own immediately-prior RefreshMonitorTiles() call) against `_lastUserIntent`; an empty `_lastKnownMonitors` produces empty `reactivated`/`wentInactive` sets by construction and falls into the "no unexpected drift detected" branch -- structurally indistinguishable, from this method's own perspective, from a genuinely quiet, correctly-settled topology.
  implication: Confirmed, exact mechanism match for claim 4b -- a transient query failure at the wrong moment produces a FALSE "no drift" verdict instead of "could not check this time," silently skipping a real reactive-correction opportunity. Real, scoped, code-level bug in the pre-existing RefreshMonitorTiles combined with fix B/D's newer dependency on its output for drift detection -- neither piece of code was individually wrong on its own terms, but the combination created a real, rig-observed gap.

- timestamp: 2026-08-28T14:50:00Z
  checked: Implemented fix F (WindowsMonitorController.cs, ObservePostApplyStability -- per-tick try/catch around QueryActiveDevicePaths and the CHANGE comparison, nested inside the existing outer try/catch which remains as a backstop) and fix G (MainForm.cs, RefreshMonitorTiles -- one retry after a 150ms backoff, matching WindowsMonitorController's own existing SettlePollDelay cadence convention, before falling back to the pre-existing empty-state degrade)
  found: "`dotnet build RigToggle.sln --no-incremental` succeeds (0 warnings, 0 errors, all 6 projects -- the same 6 pre-existing xUnit1031 warnings in unrelated test files, untouched by this round). `dotnet test src/RigToggle.Tests` -- 209/209 passed, unchanged. `dotnet build src/RigToggle.Windows.Tests` succeeds (0 warnings, 0 errors); `dotnet test src/RigToggle.Windows.Tests` still cannot EXECUTE in this sandbox (missing Microsoft.WindowsDesktop.App on Linux) -- same pre-existing, already-documented limitation as every prior round. Neither fix F (background-thread CCD poller) nor fix G (WinForms UI layer) has an injectable seam for this sandbox to test directly, matching every prior MainForm/WindowsMonitorController-touching round's established constraint -- self-verified via build + direct code read + hand-trace against the exact rig-reported failure shapes only."
  implication: Both fixes compile, integrate, and are hand-traced to produce exactly the intended behavior change (continue polling past a per-tick failure; retry once before degrading). Both are provably behavior-preserving for every pre-existing working case (no exception at all) -- only the previously-broken transient-failure cases change, matching this file's own no_op_deletion convention. Genuine correctness on real hardware is, as with every fix in this file's history, deferred to the mandatory human-verify checkpoint below.

- timestamp: 2026-08-28T16:00:00Z
  checked: Round-6 checkpoint response (seventh rig test) -- full verbatim debug.log excerpt provided directly (not just an orchestrator summary this round), independently traced by me against current WindowsMonitorController.cs/MainForm.cs source rather than accepted at face value, per this round's own explicit instruction and this session's established discipline
  found: "Fix F (round 6): CONFIRMED, independently re-derived from raw log text, not merely corroborated. WINDOW A (baseline 12:34:21.083) logs CHANGE at +1457ms then 'observation window closed at +6187ms' (21.083+6.187=27.270, matches). WINDOW E (baseline 12:36:11.378) logs CHANGE at +1480ms then 'observation window closed at +6183ms' (11.378+6.183=17.561, matches). Both windows ran their full course despite a mid-window CHANGE -- current source (ObservePostApplyStability) still has the per-tick try/catch exactly as round 6 left it, unchanged. Fix G (round 6): NOT exercised this trial -- grepped the full pasted log text for 'GetAllMonitors failed', zero matches -- neither confirmed nor refuted, matching the checkpoint response's own claim. Fix D (round 5): CONFIRMED, independently re-derived (not secondhand this round) -- both WINDOW A's and WINDOW E's revert trigger a single atomic ActivateMonitors(requested=[SAM748A], monitorSwapDisableSet=[ACI24A4,DELA0B8]) call (12:34:23.684, 12:36:13.491) that completes EXIT success, no exception, matching TryReactivelyCorrectAgainstLastIntent's unchanged fix-D code."
  implication: All three carried-forward fixes (D, F) are directly re-confirmed against raw log text this round, not merely accepted on trust; G remains an open confirm/refute question for a future trial that actually exercises a transient GetAllMonitors() failure.

- timestamp: 2026-08-28T16:05:00Z
  checked: Round-7 rig log's two ObservePostApplyStability revert timings (WINDOW A: CHANGE at +1457ms; WINDOW E: CHANGE at +1480ms), compared against every prior round's revert-timing evidence in this file
  found: Two independently-observed reverts within the SAME trial land within 23ms of each other (~1.46s-1.48s), a much tighter, more specific interval than prior rounds' looser ~2-3.5s estimates (round 2: ~3.2s; round 3: ~2.15s; round 4: ~2.3s and ~3.5s across two trials).
  implication: Still not sufficient on its own to discriminate between competing external-mechanism hypotheses (persisted-database reassertion vs. some other OS/driver auto-reconciliation), but the tightness/repeatability of this specific ~1.5s figure within one trial is new, refined evidence worth preserving verbatim for a future round that might correlate it against a fixed OS/driver timer, should one ever be independently identified (e.g. via Windows Event Viewer data this sandbox cannot gather). Not acted on this round -- carried forward only.

- timestamp: 2026-08-28T16:07:00Z
  checked: Round-7 rig log's post-WINDOW-D spontaneous topology-change lines (12:34:57.220, 12:35:34.329, 12:35:36.341/424, 12:35:50.451), all occurring well after WINDOW D's own 6-second observation window closed (38.780) and well after that action's 8-second correction-guard deadline (40.825) expired
  found: All four are logged as `TryReactivelyCorrectAgainstLastIntent: SKIP -- guard window already expired` -- confirmed by direct code read that this is DateTime.UtcNow >= _intentGuardDeadlineUtc correctly firing as designed (not a bug in the guard itself), and that no correction was attempted for any of them, exactly as the checkpoint response describes. Two of these (35.34.582, 35.36.341/424) show RefreshMonitorTiles observing only 2 device paths total (DELA0B8, SAM748A) with ACI24A4 completely ABSENT from the enumeration, not merely inactive -- qualitatively different from every other observed drift event in this file's seven-round history, all of which kept the full 3-path set and only toggled active/inactive flags.
  implication: Confirms Symptom 2 part B is not confined to any bounded post-apply observation window -- it recurs on what appears to be an independent clock, unrelated to this app's own action timing, for at least tens of seconds after the app stops watching. The ACI24A4-vanishing-from-enumeration observation is a genuinely new, distinct lead (a target briefly disappearing from GetActivePaths()/GetAllPaths() entirely, not just going inactive) that current instrumentation (ObservePostApplyStability, GetAllMonitors) was not designed to specifically flag or investigate further -- no fix or new hypothesis attempted this round; recorded for a future round, possibly requiring Windows Event Viewer/driver-level logs the user would need to pull manually, since this is likely beyond what SetDisplayConfig/QueryDisplayConfig alone can explain.
  **CORRECTION (round 8, 2026-08-28T17:00:00Z):** the user has since confirmed this specific observation (the monitor vanishing from enumeration at ~12:35:34-12:35:36, plus the subsequent burst of topology churn through ~12:35:50) is a confirmed HARDWARE CONFOUND, not genuine revert evidence -- their primary monitor has a known sleep-wake failure, and during this exact round-6 trial they had to physically unplug its power and DisplayPort cables to force it to wake up. That manual cable unplug/replug fully explains a target disappearing from CCD's enumeration and the churn as Windows re-detected it afterward. This entry is preserved for the debug trail but should NOT be used as evidence for any "periodic OS-level reassertion" hypothesis -- it is unrelated to Rig Toggle or Symptom 2 part B. The CLEAN, unconfounded evidence from this same round-6 trial (the two ~1.46-1.48s post-apply reverts in WINDOW A/E, and the SAM748A+ACI24A4 PathChangeException/dropped-survivor bug that round 7's fix H addressed) is unaffected by this correction and stands as originally recorded.

- timestamp: 2026-08-28T16:10:00Z
  checked: Round-7 rig log's OnTileAction(ACI24A4, ENABLE) sequence (12:34:30.967-32.729) and the immediately-following OnTileAction(DELA0B8, ENABLE) sequence (12:36:20.422-21.964) later in the SAME trial, both plain non-swap single-tile-enable actions with SAM748A already active going in, cross-checked directly against current TryBuildScopedActivationPlan/ActivateMonitors source
  found: The ACI24A4 call's scoped ApplyPathInfos plan (planPaths=[SAM748A, ACI24A4]) threw PathChangeException("Invalid paths information.") and fell back to Extend, which came back as [ACI24A4, DELA0B8] -- SAM748A (pre-existing active, not requested, not in any disable-set) silently dropped out; the correction loop only ever removed the ALSO-unexpectedly-activated DELA0B8, leaving finalActive=[ACI24A4] with SAM748A permanently off and ActivateMonitors reporting EXIT success. The DELA0B8 call's scoped plan (planPaths=[SAM748A, DELA0B8]) -- an apparently-identical shape (2-path, non-swap, same pre-existing survivor SAM748A) -- succeeded cleanly with NO exception, finalActive=[SAM748A, DELA0B8], the fully-correct result. Direct code read of TryBuildScopedActivationPlan confirms this file has NO logging of which specific PathDisplaySource/adapter identity GetAllPaths() picked as the "first unclaimed-source candidate" for a given target device path -- so it is not possible, from this round's log alone, to determine whether the two calls picked structurally different (source, target) pairings, or whether some other difference (a stale cached mode for ACI24A4 specifically, a genuine hardware source/port-group constraint) is responsible.
  implication: Confirms ActivateMonitors' correction loop and final verify-and-throw had a real, code-level, one-directional structural gap (only defended against GAINING an unrequested activation, never LOSING a pre-existing one) — independently derivable from source alone, without needing to resolve WHY the scoped plan threw for this specific pairing. This gap is the round-7 fix target (see Resolution below). The WHY-does-ACI24A4-specifically-fail question remains genuinely open -- multiple plausible mechanisms, no discriminating evidence, not guessed at this round (see Current Focus blind_spots).

- timestamp: 2026-08-28T16:20:00Z
  checked: Implemented round-7 fix (WindowsMonitorController.cs: new ComputeUnexpectedlyDeactivated pure helper, wired into ActivateMonitors' correction loop and its final verify-and-throw; RigToggle.Windows.Tests: 5 new unit tests for the new seam)
  found: "`dotnet build RigToggle.sln --no-incremental` succeeds (0 warnings, 0 errors, all 6 projects -- same 6 pre-existing xUnit1031 warnings in unrelated test files, untouched by this round). `dotnet test src/RigToggle.Tests` -- 209/209 passed, unchanged. `dotnet build src/RigToggle.Windows.Tests` succeeds (0 warnings, 0 errors) including the 5 new ComputeUnexpectedlyDeactivated tests; `dotnet test src/RigToggle.Windows.Tests` still cannot EXECUTE in this sandbox (missing Microsoft.WindowsDesktop.App on Linux) -- same pre-existing, already-documented limitation as every prior round. Hand-traced all 5 new tests against the implementation (RigRegressionScenario/SurvivorStaysActive/DeliberateSwapExclusion/MixedSwapAndNonSwap/NoPriorActiveSurvivors) -- all 5 match by hand. Hand-traced the full round-7 repro scenario (ActivateMonitors(ACI24A4), SAM748A active, scoped plan throws, Extend returns [ACI24A4,DELA0B8]) through the modified correction loop: round 1 computes unexpectedlyActivated={DELA0B8} (corrected via existing DeactivateMonitors call, unchanged) AND unexpectedlyDeactivated={SAM748A} (NEW -- corrected via nested ActivateMonitors({SAM748A}, monitorSwapDisableSet={}) call); round 2 re-polls and (assuming the nested call succeeded) finds both empty, one clean round; round 3 finds both empty again, consecutiveCleanRounds reaches 2, loop breaks; final verify-and-throw's extended stillInactive check finds nothing missing -- EXIT success with finalActive=[ACI24A4,SAM748A] correctly restored, matching what SHOULD have happened all along."
  implication: Fix compiles, integrates, and is hand-traced (both the new pure seam's 5 unit tests and the full round-7 repro scenario through the modified control flow) to produce exactly the intended behavior change. Provably behavior-preserving for every pre-existing working case (unexpectedlyDeactivated always empty when no survivor is unexpectedly lost) -- the new correction/throw branches only ever activate for the previously-broken case, matching this file's own no_op_deletion convention. Genuine correctness on real hardware is, as with every fix in this file's seven-round history, deferred to the mandatory human-verify checkpoint below.

- timestamp: 2026-08-28T16:58:00Z
  checked: Round-7 checkpoint response (eighth rig test) -- full verbatim debug.log excerpt covering an extended, multi-action session (startup toggle, tile enables, mode switches), user's own summary "It seems fine now."
  found: Exactly ONE revert occurred, at the very start (startup toggle to Rig mode), at +1525ms after the apply -- a FOURTH independent timing sample (prior three: 1457ms, 1480ms round 6/7; now 1525ms), all four within a ~68ms spread. Fix D's atomic correction handled it cleanly (no exception, no transient over-full state). Every subsequent action in this extended trial (a tile-enable, a toggle back to Normal mode, a toggle back to Rig mode) held stable through its full 6-second ObservePostApplyStability window -- zero further reverts. No PathChangeException, no unexpectedlyDeactivated events (fix H's new field present throughout, always empty -- not exercised this round since nothing unexpectedly dropped), no GetAllMonitors failures (fix G still unexercised). The previously-troublesome SAM748A+ACI24A4-adjacent pairing (this time as a 3-path swap-aware toggle-to-Normal call, not the round-7 2-path non-swap shape) applied cleanly with no PathChangeException.
  implication: Fixes D, F, G (where exercised), and H all hold with zero defects across this trial. The revert-timing convergence across FOUR independent samples (1457/1480/1525/-- all within 68ms) is now strong enough to treat "~1.5s after a CCD apply" as a real, fixed-interval property of whatever external mechanism causes Symptom 2 part B, even though the mechanism itself remains unidentified. Given the app now demonstrates complete, clean, invisible self-healing (one correction, no defects, on the only revert this trial produced), this round's checkpoint response explicitly asked the user whether to continue chasing Symptom 2 part B's true OS-level trigger (likely requiring Windows Event Viewer/driver diagnostics outside this sandbox's reach) or close the session accepting the mitigation as sufficient.

- timestamp: 2026-08-28T17:05:00Z
  checked: User's explicit response to the orchestrator's direct closure question (three options presented: close as resolved / keep chasing root cause / something else)
  found: "Close as resolved" -- user's exact selection, made after being shown the tradeoff (root cause unknown after 8 rounds vs. proven, repeatable, zero-defect self-healing across 3 consecutive trials) and after confirming (2026-08-28T17:00:00Z correction above) that round 6's monitor-vanishing observation was an unrelated hardware confound, not genuine revert evidence.
  implication: This is a deliberate, informed, user-approved closure of an open root-cause question in favor of a demonstrated, working mitigation -- not a stalled or abandoned investigation. Session marked resolved (see Resolution below). Symptom 2 part B's true external trigger remains explicitly open should the user ever want to revisit it (e.g. via Windows Event Viewer diagnostics, or if the mitigation ever visibly fails).

## Eliminated

- hypothesis: "Symptom 2 shares Symptom 1's exact root cause (scoped activation's missing Position/Resolution/PixelFormat data), so the position-mode cache fix alone would resolve both symptoms via the same code path."
  evidence: "Post-fix rig trial (2026-08-23T08:30:00Z): Symptom 1 confirmed fixed, but Symptom 2 reproduced again with a debug.log signature that does not match a missing-mode-data mechanism at all."
  timestamp: 2026-08-23T08:30:00Z

- hypothesis: "ApplyTopology(Extend) is rig-proven reliable for the full Rig/Normal swap shape (rounds 1-4's own history), so routing the swap case to it exclusively (round 2's fix) is sufficient."
  evidence: "Round 3 rig test (2026-08-23T10:05:00Z): from the original Symptom-2 repro starting state (one monitor already independently disabled), Extend failed to activate the requested target at all while also reactivating the independently-disabled, unrequested monitor. Rounds 1-4's apparent reliability only held for the specific repro shapes those rounds happened to test."
  timestamp: 2026-08-23T10:05:00Z

- hypothesis: "MainForm's reactive watchdog (TryReactivelyCorrectAgainstLastIntent) is itself causing the delayed revert (self-inflicted correction bug)."
  evidence: "Direct code read (2026-08-23T10:55:00Z): the watchdog's only corrective branch requires `!shouldBeActive` (intent says should be inactive); the third-rig-test revert scenario has `shouldBeActive == true` (target was deliberately activated), so this branch structurally cannot fire. The watchdog was confirmed unable to touch this case in either direction -- neither causing it nor (pre-fix) correcting it."
  timestamp: 2026-08-23T10:55:00Z

- hypothesis: "Round 3's scoped-activation array is invalid simply because it has 'too few' entries in some generic, unaddressable sense -- an OS/driver-level black box no code-level fix could target."
  evidence: "Decompiled WindowsDisplayAPI 1.3.0.13's PathInfo.GetDisplayConfigPathInfos (ilspycmd, 2026-08-28) plus web research (see Evidence below) identified two specific, addressable SetDisplayConfig requirements round 3's array violated (keep-excluded-paths-present-but-inactive; exactly-one-path-at-origin) -- not a generic, unaddressable 'needs more paths' unknown."
  timestamp: 2026-08-28T09:45:00Z

- hypothesis: "Rounds 1-4's four confirmed root causes (position-mode cache, Extend-structural-incapability, one-directional/unarmed watchdog, round-3's array-shape defect) are jointly sufficient to fully resolve Symptom 2 -- once all four fixes are in place, the toggle/tile paths hold durably."
  evidence: "Fifth rig test (round-4 checkpoint response, 2026-08-28T13:50:00Z): with all four fixes in place, Symptom 2 part B (the delayed silent revert) reproduced AGAIN, unchanged in character, on the very call shape round 4's fix specifically targeted -- proving the four confirmed causes, while each individually real and each individually necessary, are NOT jointly sufficient. A fifth, distinct cause (the reactive watchdog's own non-atomic two-step correction, see round-5 root_cause below) was also newly confirmed this round, and Symptom 2 part B's true trigger remains entirely unexplained."
  timestamp: 2026-08-28T13:50:00Z

- hypothesis: "Fix B (round 3 part 2, TryReactivelyCorrectAgainstLastIntent made bidirectional) is fully correct and sufficient once armed on both call paths -- any mixed-direction drift event it detects will be correctly resolved."
  evidence: "Direct code read (2026-08-28T13:52:00Z) plus rig log analysis: fix B correctly DETECTS both directions in a single event (confirmed working, new behavior vs. pre-fix silence), but its own correction logic issues the two directions as separate, order-dependent CCD calls -- DeactivateMonitors(reactivated) first, unconditionally non-swap-aware ActivateMonitors(wentInactive) second. This is directionally correct but not atomically correct: when wentInactive's target is the only active survivor not in reactivated, the first call hits DeactivateMonitors' own zero-survivors guard and fails, and the uncoordinated second call inherits scoped-plan fragility -- rig-confirmed to produce either an unwanted transient over-full topology or an outright PathChangeException-then-D-05-abort, both wrong outcomes. Refined, not simply refuted: fix B's detection logic is correct and unchanged by round 5; only its correction logic's atomicity was wrong."
  timestamp: 2026-08-28T13:52:00Z

## Resolution

root_cause: "Eight distinct, independently-confirmed root causes (Symptom 2 part B, and the still-unexplained SAM748A+ACI24A4-specific PathChangeException trigger noted in (8), both remain open, unresolved items -- not counted among the 'confirmed and fixed' causes since their actual triggers are still unknown):
(1) Symptom 1: TryBuildScopedActivationPlan originally constructed the newly-reactivated target's PathInfo via a mode-less constructor, letting the driver's best-mode-logic pick a default instead of the monitor's own prior position. RESOLVED (position-mode cache), rig-confirmed, not touched since round 1.
(2) Symptom 2, part A: ApplyTopology(Extend) -- the mechanism round 2's fix routed the full swap case to -- is structurally incapable of expressing an explicit desired active-path-set (confirmed by decompiling WindowsDisplayAPI 1.3.0.13: it supplies zero path/mode arrays to SetDisplayConfig). Windows' own opaque internal topology-selection algorithm decides the result, with no guarantee it includes the caller's specific requested target or excludes a monitor the caller independently, deliberately left disabled -- confirmed on real hardware (round 3): Extend simultaneously failed to activate the requested target AND reactivated an unrelated, independently-disabled one.
(3) Symptom 2, part B: independent of (2), a delayed silent revert of an apparently-successful, verified CCD apply back to the prior topology occurs on BOTH ApplyTopology(Extend) and scoped ApplyPathInfos, and on BOTH the swap (toggle) and non-swap (tile) call shapes -- confirmed by a third rig test reproducing it on the simple single-tile enable path, which round 1 had previously reported as working, and CONFIRMED STILL REPRODUCING, unchanged, across rounds 5, 6, and 7. Timing converged tightly across FOUR independent samples in rounds 6-7 (1457ms, 1480ms, 1525ms -- all within a ~68ms spread), strongly suggesting a real, fixed-interval external mechanism (~1.5s post-apply), though the mechanism itself was never identified. **CLOSED AS RESOLVED-VIA-MITIGATION (round 8), NOT root-caused.** The genuine external trigger for why Windows/the driver undoes the apply ~1.5s later remains completely unknown after eight rounds -- root-causing it further would likely require diagnostics outside this sandbox's reach (Windows Event Viewer, GPU driver logs, vendor support). However, the app's reactive-correction layer (fixes D, F, G, H, all below) demonstrated clean, complete, invisible self-healing across THREE CONSECUTIVE rig trials (rounds 6, 7, and the round-7-checkpoint's own eighth trial) with zero user-visible failures, exceptions, or incorrect final states -- every occurrence of the revert was corrected within its own observation window, atomically, with no follow-on defects. Given this track record, the user was explicitly asked whether to continue chasing the true root cause or accept the mitigation as sufficient, and chose to close the session as resolved (see Evidence 2026-08-28T17:05:00Z). One round-6 evidence entry (the "monitor vanished from CCD's enumeration" observation, originally logged as a possible periodic-reassertion lead) was subsequently corrected: the user confirmed it was a hardware confound (their monitor's known sleep-wake failure, requiring a manual cable unplug/replug during that trial), not genuine revert evidence -- annotated in place, not deleted (see Evidence 2026-08-28T16:07:00Z's correction note). Should this mitigation ever visibly fail, or should the user want to pursue the root cause independently (e.g. via Windows Event Viewer correlation against the ~1.5s interval), this item should be reopened rather than treated as fully solved.
(4) Symptom 2, part C (round 4): round 3's own fix for (2)/(3) -- excluding the swap's disable-set entirely from the scoped ApplyPathInfos array -- itself produces an array the CCD API can reject outright with PathChangeException, via two distinct, independently-researched SetDisplayConfig requirements it violated: a path being deactivated must remain PRESENT in the array with its active flag cleared and mode index invalidated, never omitted; and exactly one ACTIVE path in the array must sit at the desktop origin (0,0), which round 3's plan could leave unsatisfied whenever the disable-set consumed every other active survivor, leaving the sole newly-activated target at its own stale, non-origin cached position. Confirmed via a fourth rig test (app-startup mode-restore call) plus decompile + web research (see Evidence). Round 5's fifth rig test did NOT refute this fix -- both primary, toggle-driven ActivateMonitors calls this round applied cleanly without PathChangeException.
(5) Symptom 2, part D (round 5): round 3 part 2's bidirectional reactive watchdog (TryReactivelyCorrectAgainstLastIntent) correctly DETECTS a mixed drift event (both a reactivated set and a wentInactive set in the same OnDisplaySettingsChanged observation) but its own correction logic issued the two directions as separate, order-dependent CCD calls -- DeactivateMonitors(reactivated) first, then a completely separate, unconditionally non-swap-aware ActivateMonitors(wentInactive, monitorSwapDisableSet: empty) second. Whenever wentInactive's target was the only active survivor NOT already in reactivated, the first call hit DeactivateMonitors' own zero-survivors guard ('Cannot disable all configured monitors') and failed, so reactivated was never actually deactivated by it, and the uncoordinated second call -- uninformed about reactivated -- either produced an unwanted transient 3-active-monitor state (fifth rig test, first trial) or inherited the exact malformed-array/PathChangeException fragility a non-swap-aware call carrying survivors the caller actually wants gone is known to trigger, falling back to the known-unreliable Extend and leaving D-05 to abort in the WRONG final state (fifth rig test, second trial). Confirmed via direct code read of both TryReactivelyCorrectAgainstLastIntent and DeactivateMonitors' zero-survivors throw, whose message and call-stack shape match the rig log's exception trace exactly. Round 5's fix (fix D, one atomic swap-aware call) was NOT refuted by the sixth rig test's checkpoint response -- every reactive correction observed in that trial completed cleanly.
(6) Symptom 2, part E / instrumentation defect (round 6, NEW): fix E's own ObservePostApplyStability background poller (round 5) wrapped its entire poll loop -- including the per-tick QueryActiveDevicePaths() call -- in a single try/catch scoped to the whole thread body, rather than per-iteration. QueryActiveDevicePaths queries PathDisplayTarget.DevicePath with no IsAvailable filter (unlike GetAllMonitors/TryBuildScopedActivationPlan elsewhere in this file, which both filter first for exactly this reason); a transient TargetNotAvailableException during any one poll tick (plausible mid-CCD-transition, since this thread has zero coordination with any concurrent Activate/DeactivateMonitors call) therefore silently ended the ENTIRE remaining 6-second observation window instead of just that tick -- rig-confirmed: a window died at +898ms with zero further data captured. Pure instrumentation defect (never affected any correction/mutation path), but directly undermined fix E's entire purpose of gathering Symptom 2 part B's evidence. Fixed (fix F) by scoping the try/catch to each poll tick individually.
(7) Symptom 2, part F / correction-skip defect (round 6, NEW): MainForm.RefreshMonitorTiles' pre-existing (not round-5/6-introduced) catch-and-degrade-to-empty behavior on a GetAllMonitors() failure, combined with round 3 part 2/round 5's TryReactivelyCorrectAgainstLastIntent depending on RefreshMonitorTiles' freshly-populated _lastKnownMonitors for its own drift detection, meant a transient GetAllMonitors() failure (rig-observed: Win32Exception(87) from PathInfo.GetPathInfos -> GetActivePaths) produced a FALSE 'no unexpected drift detected' verdict (an empty _lastKnownMonitors yields empty reactivated/wentInactive sets by construction) instead of a genuine 'could not check this time' signal -- silently skipping a real reactive-correction opportunity exactly when the transient failure coincided with an armed guard window closing (rig-confirmed, round-6 checkpoint response). Neither piece of code was individually wrong on its own terms; the combination created a real, rig-observed gap. Fixed (fix G) by adding one retry, after a short backoff, before falling back to the pre-existing empty-state degrade.
(8) Symptom 2, part G / one-directional correction-loop defect (round 7, NEW): ActivateMonitors' own settle-poll-then-correct loop (rounds 1-5) and its final verify-and-throw ONLY ever checked whether a device path GAINED an unrequested activation (ComputeUnexpectedlyActivated) -- there was no symmetric check for a device path that was already active BEFORE the call, was not part of monitorSwapDisableSet, and LOST its activation as an unrequested side effect of the whole-topology ApplyTopology(Extend) fallback. Rig-confirmed (seventh rig test, round-6 checkpoint response finding 6): a plain, non-swap ActivateMonitors(ACI24A4) call (SAM748A already active) had its scoped ApplyPathInfos plan throw PathChangeException, fell back to Extend, and Extend's own opaque persisted layout came back without SAM748A -- SAM748A silently dropped out, the correction loop only corrected the ALSO-unexpectedly-activated DELA0B8, and ActivateMonitors reported EXIT success with a WRONG final state (finalActive=[ACI24A4], SAM748A permanently off). This wrong state was then baked into MainForm.ArmIntentGuard's own unconditional post-action intent snapshot, meaning even the round-3/5 reactive watchdog could never detect or correct it -- it believed SAM748A being off was correct. A companion call in the same trial (ActivateMonitors(DELA0B8), an apparently-identical-shaped plain non-swap call with the SAME pre-existing survivor SAM748A) succeeded cleanly with no exception -- WHY the scoped plan throws for the SAM748A+ACI24A4 pairing specifically but not SAM748A+DELA0B8 remains genuinely unknown (multiple plausible mechanisms: a stale/incompatible GetAllPaths() source candidate picked as the first unclaimed-source match, a hardware source/port-group constraint, a cached-mode mismatch -- no discriminating evidence, not guessed at). This round's fix targets the CONSEQUENCE (a lost survivor with no correction and no error), which is independently real, well-evidenced, and actionable regardless of that still-open upstream trigger. Fixed (fix H) by adding ComputeUnexpectedlyDeactivated -- the mirror image of ComputeUnexpectedlyActivated -- wired into the same correction loop (nested, non-swap-aware ActivateMonitors re-activation of exactly the dropped survivor) and into the final verify-and-throw (now also fails loudly, per D-05, if a pre-existing survivor is still missing after the bounded correction rounds)."
fix: "Fix A -- Symptom 2 part 2 (src/RigToggle.Core/Abstractions/IMonitorController.cs, src/RigToggle.Windows/WindowsMonitorController.cs, src/RigToggle.Core/ToggleService.cs, src/RigToggle.App/MainForm.cs, plus test doubles): ActivateMonitors' `bool isPartOfMonitorSwap` parameter is replaced with `IReadOnlySet<string> monitorSwapDisableSet` (the actual disable-set, or empty). TryBuildScopedActivationPlan excludes monitorSwapDisableSet's device paths from the survivors it preserves as ACTIVE (both from claimedSources and the plan's active entries) -- so ONE scoped ApplyPathInfos call both activates the new target(s) and deactivates the swap's disable-set, going directly from the pre-toggle topology to the exact post-toggle topology with no transient N-active state. The isPartOfMonitorSwap-triggers-Extend-bypass branch is removed entirely -- scoped activation is now always attempted first, for both swap and non-swap calls (a no-op/byte-identical plan when the exclusion set is empty). Because swap-excluded survivors may now go inactive without ever reaching DeactivateMonitors' own capture-before-removal code, ActivateMonitors itself caches their live mode first via a new shared CacheLiveModes helper (extracted from DeactivateMonitors' existing capture loop) -- preserving Symptom 1's fix for the swap case too. Extend remains only as a last-resort fallback when a scoped plan cannot even be constructed, documented as now-known-unreliable for correctly targeting a specific monitor.
Fix B -- Symptom 2 part 3 (src/RigToggle.App/MainForm.cs only): TryReactivelyCorrectAgainstLastIntent is made bidirectional -- alongside its existing 'active but should be inactive -> DeactivateMonitors' correction, it now also detects 'inactive but should be active -> ActivateMonitors' and corrects that too, sharing the same lease acquisition, the same bounded correction budget, and the same 'never touch a device path absent from intent' safety boundary as the existing direction. ToggleSwitch_ActionRequested and PerformBackgroundToggle (the Rig/Normal toggle's two UI entry points) now call RefreshMonitorTiles()+ArmIntentGuard() after a toggle attempt, matching OnTileAction's existing pattern -- closing the previously-deferred gap where the toggle path never armed the watchdog at all. Does not change IntentGuardWindow (8s) or MaxReactiveCorrections (2), does not touch ComputeUnexpectedlyActivated, the settle-poll-then-correct loop inside ActivateMonitors, or DeactivateMonitors' own verify-and-throw.
Fix C -- Symptom 2 part C (round 4, src/RigToggle.Windows/WindowsMonitorController.cs only): TryBuildScopedActivationPlan no longer OMITS swap-excluded survivors from the array -- each one is kept as an explicit, mode-blanked (IsModeInformationAvailable=false), reflection-patched-inactive PathInfo entry (same reflection-patch technique already used to force a NEW target active, now toggling the opposite direction), which frees its PathDisplaySource for a new target to claim while still satisfying the researched 'present but inactive, never omitted' CCD requirement. A new pure helper, PromoteToOriginIfNeeded (unit-tested), then normalizes the plan's active entries so exactly one sits at the desktop origin (0,0) whenever none already does -- mirroring DeactivateMonitors' own existing uniform-shift idiom for the deactivation direction, applied here to activation. No-op for every case round 3 already had working (empty monitorSwapDisableSet, or any case where an existing active survivor already sits at (0,0)).
Fix D -- Symptom 2 part D (round 5, src/RigToggle.App/MainForm.cs only): TryReactivelyCorrectAgainstLastIntent's correction block is restructured so a mixed drift event (both reactivated and wentInactive non-empty) issues exactly ONE atomic, swap-aware call -- `ActivateMonitors(wentInactive, monitorSwapDisableSet: reactivated)` -- instead of two separate, order-dependent calls. This reuses the identical 'one CCD call both activates the target(s) and deactivates the disable-set' primitive ToggleService's own Rig/Normal swap already relies on (fix A), so reactivated is deactivated as a side effect of the SAME call that activates wentInactive, never as a separate step that can hit DeactivateMonitors' zero-survivors guard. When wentInactive is empty (reactivated-only drift), this reduces via an `else if` to the original, unchanged plain DeactivateMonitors(reactivated) call -- byte-identical behavior to before for that case. When reactivated is empty (wentInactive-only drift, round 3 part 2's original case), `monitorSwapDisableSet: reactivated` degrades to an empty set, byte-identical to the pre-fix `monitorSwapDisableSet: new HashSet<string>()` call. Only the genuinely-new mixed-direction case changes behavior, which is exactly the RCA-confirmed defect being fixed.
Fix E (round 5, src/RigToggle.Windows/WindowsMonitorController.cs only) -- EXPLICITLY NOT A FIX, pure evidence-gathering instrumentation for Symptom 2 part B (still unresolved, see root_cause (3) above): a new ObservePostApplyStability helper starts a bounded (6s), fine-grained (250ms), non-corrective background poll of the live active-path set immediately after every successful ActivateMonitors/DeactivateMonitors call, logging every observed CHANGE with a millisecond-precision offset from the call's own success. Runs on its own background thread so it never blocks the caller or interferes with MainForm's correction-lease/budget discipline. Intended to let the NEXT rig trial's debug.log directly show the revert's precise timing/shape (abrupt vs. gradual, fixed-interval vs. variable) -- the missing evidence that guessing at a specific external mechanism (e.g. GPU vendor control-panel software, an OS display-database reconciliation timer -- both considered and left unconfirmed this round, see Evidence) cannot substitute for.
Fix F -- Symptom 2 part E (round 6, src/RigToggle.Windows/WindowsMonitorController.cs only): ObservePostApplyStability's poll loop now wraps ONLY the per-tick `QueryActiveDevicePaths()` call and its CHANGE comparison in their own try/catch, nested inside the existing per-iteration loop body, instead of relying solely on the outer try/catch scoped to the whole thread delegate. A transient exception (e.g. TargetNotAvailableException) is now logged and that single tick is skipped -- the while loop's own elapsed-time condition on the next iteration is untouched, so polling resumes exactly as if that tick had observed no change. The outer try/catch is kept, unchanged in position, as a last-resort backstop for anything outside the per-tick block. Pure evidence-gathering instrumentation, same as fix E -- no correction/mutation path touched.
Fix G -- Symptom 2 part F (round 6, src/RigToggle.App/MainForm.cs only): RefreshMonitorTiles now retries its `_monitorController.GetAllMonitors()` call exactly ONCE, after a 150ms backoff (matching WindowsMonitorController's own existing SettlePollDelay cadence convention, not a new arbitrary value), before falling back to the pre-existing `Array.Empty<MonitorInfo>()` degrade path. Directly targets the rig-observed timing coincidence between a transient GetAllMonitors() query failure and TryReactivelyCorrectAgainstLastIntent's drift detection (which depends on RefreshMonitorTiles' freshly-populated _lastKnownMonitors and cannot distinguish 'query failed' from 'genuinely no drift' when that list is empty). If the retry also fails, behavior is unchanged from before this fix. Does not touch the separate, already-defensive GetAllMonitors() call site inside ToggleSwitch_ActionRequested's confirm-dialog name resolution, which has its own adequate, non-correction-critical fallback and was not implicated in the rig-observed correction-skip.
Fix H -- Symptom 2 part G (round 7, src/RigToggle.Windows/WindowsMonitorController.cs only): adds `ComputeUnexpectedlyDeactivated` -- a new pure, unit-tested seam mirroring the existing `ComputeUnexpectedlyActivated` exactly (same 3-argument shape: pre-active set, post-active set, an exclusion set) -- returning any device path that was active before this ActivateMonitors call, is not in monitorSwapDisableSet, and is not active in the post-round snapshot. Wired into the EXISTING settle-poll-then-correct loop: when non-empty, a nested, non-swap-aware `ActivateMonitors(unexpectedlyDeactivated, monitorSwapDisableSet: new HashSet<string>())` call re-activates exactly the dropped survivor(s), reusing this same public method rather than a new primitive (mirrors how the sibling unexpectedlyActivated branch reuses DeactivateMonitors). Also extends the method's final verify-and-throw: `stillInactive` now additionally includes any pre-call-active, non-excluded survivor still missing after all correction rounds are exhausted, so a genuine failure to restore it throws (D-05: surface loudly) instead of silently reporting EXIT success with a wrong final state -- exactly the gap that let this defect go undetected for six prior rounds. No-op/byte-identical for every case where no previously-active survivor is ever unexpectedly dropped (the overwhelming common case) -- only the previously-broken 'Extend fallback silently drops an unrequested survivor' case changes behavior. Does not touch ComputeUnexpectedlyActivated, TryBuildScopedActivationPlan, PromoteToOriginIfNeeded, CacheLiveModes, DeactivateMonitors, or ObservePostApplyStability -- scoped entirely to ActivateMonitors' own correction loop and verify-and-throw."
verification:
  target_test: { result: partial, reason: "Symptom 1: previously rig-verified (unchanged this round). Symptom 2 parts A/C/D/E/F: previously self-verified/rig-corroborated; fix D and fix F both INDEPENDENTLY re-confirmed this round via direct trace of the raw log (not secondhand) -- see Evidence 2026-08-28T16:00:00Z. Fix G remains genuinely unexercised (no confirm/refute data). Symptom 2 part B remains OPEN -- no fix attempted this round, by design; refined (not resolved) with the tight ~1.5s timing observation and the ACI24A4-vanishing-from-enumeration lead. Fix H: no automated test coverage possible for ActivateMonitors itself (live CCD hardware, no injectable seam, per this file's own established constraint) -- but the new ComputeUnexpectedlyDeactivated pure seam IS unit-tested (5 new tests, hand-traced against the implementation since this sandbox cannot execute RigToggle.Windows.Tests), and the full round-7 repro scenario was hand-traced through the modified ActivateMonitors control flow end-to-end. All fixes are self-verified via full-solution build plus the existing 209-test RigToggle.Tests suite (confirms no regression to ToggleService's routing/ordering contracts) and are deferred to the mandatory human-verify checkpoint for the actual hardware-dependent behavior." }
  mutation_check: { result: skipped, reason: "No Stryker configured in this repo (matches this file's own prior debug sessions)." }
  no_op_deletion: { result: pass, deletion_justified_by_rca: true, note: "Fix A/B/C/D/E/F/G: unchanged from prior rounds' own assessment. Fix H: purely additive -- ComputeUnexpectedlyDeactivated is a new helper (nothing deleted); the correction loop's existing unexpectedlyActivated branch and DeactivateMonitors call are unchanged, only now nested inside a broader `if` alongside the new unexpectedlyDeactivated branch; the final verify-and-throw's stillInactive computation is extended via .Concat(...), never narrowed -- the pre-existing 'requested path still inactive' check is untouched and always still runs. The EXIT-success log line and ObservePostApplyStability call at the tail were refactored to reuse the already-computed postCorrectionActiveDevicePaths set instead of recomputing an identical one -- same value, no behavior change. No existing control flow is deleted or weakened; only the previously-broken 'unrequested survivor silently lost' case changes behavior (from silent false-success to either a successful nested correction or a loud, D-05-compliant throw), which is the RCA-confirmed defect being fixed." }
  adjacent_tests: { result: pass, suites_run: ["dotnet build RigToggle.sln --no-incremental (0 warnings, 0 errors, all 6 projects)", "dotnet test src/RigToggle.Tests (209/209 passed, unchanged from before this round)", "dotnet build src/RigToggle.Windows.Tests (0 warnings, 0 errors, includes 5 new ComputeUnexpectedlyDeactivated tests)"], note: "dotnet test src/RigToggle.Windows.Tests still cannot EXECUTE in this sandbox (missing Microsoft.WindowsDesktop.App) -- pre-existing sandbox limitation, unrelated to this fix, matching every prior session in this file. The 5 new tests were hand-traced against the implementation instead (all 5 match by hand) -- see Evidence 2026-08-28T16:20:00Z." }
  revert_and_reconfirm: { result: pass, reason: "Round 8's rig trial (Evidence 2026-08-28T16:58:00Z) rig-confirmed fix H holds: no unexpectedlyDeactivated events occurred (nothing to correct this trial, since the previously-troublesome pairing applied cleanly), but fixes D/F/H were exercised on the trial's one genuine revert with zero defects -- one clean atomic correction, no exceptions, full 6s window integrity. Fixes D/F re-confirmed a second consecutive round. Fix G remains unexercised across all rounds it was available (no transient GetAllMonitors failure ever reproduced again) -- neither confirmed nor refuted, carried forward as an untested-but-plausibly-correct defensive fix. Symptom 2 part B's revert_and_reconfirm is N/A -- it was never fixed, only mitigated; see root_cause (3) and the Final Closure section below." }
  guardrail_verdict: accepted
  guardrail_note: "All self-executable signals (no-op/deletion review, full-solution build, full adjacent test suite, plus 5 new hand-traced unit tests for the new pure seam) pass cleanly. Fix H was subsequently rig-verified in round 8 with zero defects. Fixes D and F were independently re-confirmed via direct raw-log trace in TWO consecutive rounds (7 and 8). Fix G remains unexercised across every round it was available -- not refuted, simply never triggered by a reproducible transient failure. Symptom 2 part B and the still-unexplained SAM748A+ACI24A4-specific PathChangeException trigger both remain explicitly OPEN root-cause questions -- this session does not claim to have solved either. What IS claimed, and what the user explicitly approved closing on: the app's reactive-correction layer around Symptom 2 part B has been demonstrated, across three consecutive rig trials with zero defects, to make the residual revert invisible in practice. See the Final Closure section below for the full closure rationale."

## Final Closure (round 8, 2026-08-28)

This debug session is closed as **RESOLVED**, via an explicit, informed user decision to accept a proven mitigation over continuing to chase an unidentified OS-level root cause. This is not a stalled or abandoned investigation -- it is a deliberate scope decision, made with full visibility into what remains unknown.

**Fully resolved, rig-confirmed:**
- Symptom 1 (position reset on re-enable): fixed by the position-mode cache, round 1.
- Symptom 2 part A (ApplyTopology(Extend) cannot express an explicit active-path-set): fixed by swap-aware scoped ApplyPathInfos, fix A (round 1) + fix C (round 4, array-shape/origin-promotion refinement).
- Symptom 2 part D (reactive watchdog's mixed-direction correction was non-atomic): fixed by fix D (round 5), re-confirmed clean across rounds 6, 7, and 8.
- Symptom 2 part E (instrumentation's observation window could be silently truncated): fixed by fix F (round 6), re-confirmed clean across rounds 7 and 8.
- Symptom 2 part F (transient GetAllMonitors failure could produce a false "no drift" verdict): fixed by fix G (round 6); never re-exercised in any subsequent round, so remains formally unconfirmed-but-plausible rather than rig-proven -- low risk, defensive-only fix, no reason to distrust it.
- Symptom 2 part G (ActivateMonitors' correction loop had a one-directional gap, silently dropping an unrequested survivor): fixed by fix H (round 7), rig-confirmed clean in round 8.

**Left explicitly open, NOT resolved:**
- Symptom 2 part B's true root cause: WHY Windows/the driver silently reverts an apparently-successful CCD apply ~1.5s later (four independent timing samples: 1457/1480/1525ms, tightly clustered) remains completely unknown after eight rounds. This session's own research (SDC_SAVE_TO_DATABASE persistence semantics, GPU vendor auto-profile-restore, generic driver-forum reports) found nothing conclusively matching this exact shape. Root-causing it further would very likely require tooling this sandbox does not have access to: Windows Event Viewer correlation against the ~1.5s interval, GPU vendor driver logs, or vendor support engagement.
- WHY the SAM748A+ACI24A4 pairing specifically threw PathChangeException on a plain 2-path scoped-activation call (round 7), while an apparently-identical SAM748A+DELA0B8 call succeeded -- multiple plausible mechanisms, no discriminating evidence gathered. Fix H addresses the CONSEQUENCE of this (a silently-dropped survivor), not the trigger itself; if this pairing's scoped-plan failure recurs, fix H's nested-correction/D-05-throw behavior should still handle it correctly, but the underlying CCD-validation question remains unanswered.

**Why closure was chosen over continued investigation:** by round 8, the mitigation layer had been exercised across three consecutive rig trials (rounds 6, 7, and the round-7-checkpoint's own trial) with the OS-level revert occurring multiple times and being corrected every single time, cleanly, atomically, with no follow-on defects, exceptions, or incorrect final states. The user's own read after the final trial was "it seems fine now" -- and after being shown the tradeoff explicitly (continue chasing an OS-level cause that may need external diagnostics this sandbox cannot provide, vs. accept a working, repeatedly-proven mitigation), the user chose closure. If the mitigation is ever observed to fail on the rig -- a revert that does NOT get corrected within the 8-second guard window / 2-correction budget, or a correction that itself throws unexpectedly -- this session should be reopened via `/gsd-debug continue monitor-position-resets-to-de` rather than starting a fresh investigation from scratch, since all eight rounds of prior evidence remain directly relevant.
files_changed:
  - src/RigToggle.Windows/WindowsMonitorController.cs
  - src/RigToggle.Core/Abstractions/IMonitorController.cs
  - src/RigToggle.Core/ToggleService.cs
  - src/RigToggle.App/MainForm.cs
  - src/RigToggle.Tests/Doubles/FakeControllers.cs
  - src/RigToggle.Tests/Doubles/BlockingMonitorController.cs
  - src/RigToggle.Windows.Tests/WindowsMonitorControllerTests.cs

## Checkpoint history

- Round 1 checkpoint (position-mode cache + isPartOfMonitorSwap->Extend): Symptom 1 rig-confirmed fixed. Symptom 2 rig-tested and found still broken (see round 2/3 evidence above) -- fix insufficient, investigation continued rather than being archived.
- Round 2/3 checkpoint: two additional, independent fixes implemented per that round's reasoning_checkpoint. A prior in-progress note ("Session paused -- handoff note") recommended either widening the settle-poll window or building a persistent re-verify/reapply watchdog as the next direction once the "revert regardless of mechanism" finding emerged -- the bidirectional-and-toggle-path-armed intent guard (fix B) is that persistent watchdog, built by extending the mechanism this codebase already had (event-driven, bounded-budget) rather than adding a new polling timer, since every rig log available shows the revert IS accompanied by an OnDisplaySettingsChanged notification the app can react to. Rig-tested (fourth rig test) and found round 3's own fix has its own array-shape failure mode -- see round 4 below.
- Round 4 checkpoint: fix C implemented per that round's reasoning_checkpoint, targeting the two specific, independently-researched SetDisplayConfig requirements round 3's array violated. Rig-tested (fifth rig test, round-5 checkpoint response) -- **ANSWERED: not fully fixed.** Fix C itself was NOT refuted (both primary toggle-driven calls applied cleanly, no PathChangeException) but Symptom 2 part B (the delayed silent revert) reproduced again unchanged, AND the reactive watchdog's own correction logic (fix B) was newly found to have its own non-atomicity bug when correcting a mixed-direction drift event -- see round 5 below and the root_cause/Eliminated updates above.
- Round 5: fix D (MainForm's reactive-correction block made atomic/swap-aware for the mixed-direction case) implemented per that round's reasoning_checkpoint, directly targeting the newly rig-confirmed non-atomicity bug. Fix E (ObservePostApplyStability, pure evidence-gathering instrumentation, explicitly NOT a fix) also added to WindowsMonitorController, targeting Symptom 2 part B's still-unknown root trigger -- four rounds of purely reactive/downstream fixes had not explained it, so that round prioritized gathering the missing fine-grained timing evidence over guessing at another specific external mechanism. Rig-tested (sixth rig test, round-6 checkpoint response) -- **ANSWERED: partially confirmed, partially not.** Fix D was NOT refuted (every reactive correction in the trial completed cleanly). Symptom 2 part B reproduced again, still unresolved. Fix E's own instrumentation was found to have a defect (round 6, see below) that had been silently truncating its data collection.
- Round 6: two new, independent, scoped defects confirmed via direct code read and fixed -- fix F (ObservePostApplyStability's per-tick try/catch scope, so one transient TargetNotAvailableException no longer silently kills the rest of a 6s observation window) and fix G (RefreshMonitorTiles gains one retry-after-backoff on a transient GetAllMonitors() failure, so a transient query failure no longer produces a false "no drift detected" verdict that skips a real reactive-correction opportunity). Neither fix attempted to root-cause Symptom 2 part B itself, which remained open. Rig-tested (seventh rig test, round-6 checkpoint response) -- **ANSWERED: fixes D and F independently re-confirmed by direct log trace; fix G unexercised; Symptom 2 part B unchanged, still open; and a genuinely NEW, distinct, tractable defect surfaced (checkpoint finding 6) -- see round 7 below.**
- Round 7: fix H implemented per that round's reasoning_checkpoint, targeting the newly rig-confirmed one-directional gap in ActivateMonitors' own correction loop and verify-and-throw (only ever defended against a monitor GAINING an unwanted activation, never against a pre-existing, unrequested survivor LOSING one as a side effect of the Extend fallback) -- root_cause (8). This round explicitly weighed the checkpoint response's own tradeoff question (fix finding 6 vs. continue chasing Symptom 2 part B) and chose finding 6: it is fully code-level and confirmable by direct read without new rig data, it produces a more severe consequence (a permanently wrong final state vs. an eventually-correctable delayed revert), and Symptom 2 part B has no new angle to pursue this round beyond what was already established as underdetermined. Symptom 2 part B remains open, refined with two new pieces of evidence (a tight ~1.46-1.48s revert-timing observation; a periodic, well-outside-any-window recurrence with a monitor briefly vanishing from CCD's own enumeration) but no new hypothesis or fix attempt. The still-unexplained WHY of the SAM748A+ACI24A4-specific PathChangeException (vs. the identically-shaped SAM748A+DELA0B8 call succeeding) also remains open -- not guessed at. Rig-tested (eighth rig test, round-7 checkpoint response) -- **ANSWERED: fix H rig-confirmed clean (zero unexpectedlyDeactivated events, nothing to correct), fixes D/F re-confirmed a second consecutive round, fix G still unexercised, and the trial's one revert (a fourth independent ~1.5s timing sample) was corrected with zero defects.**
- Round 8 (this update, FINAL): the round-6 "monitor vanished from enumeration" evidence was corrected -- confirmed by the user to be an unrelated hardware confound (a known monitor sleep-wake failure requiring a manual cable unplug/replug during that trial), not genuine revert evidence; annotated in place, not deleted. Given three consecutive clean rig trials of the mitigation layer (rounds 6, 7, 8) and a tightly-converged but still-unexplained ~1.5s revert-timing signature (four samples, ~68ms spread) after eight rounds, the orchestrator presented the user an explicit choice: continue chasing Symptom 2 part B's true OS-level root cause (likely requiring diagnostics outside this sandbox, e.g. Windows Event Viewer) or close the session accepting the demonstrated mitigation as sufficient. **User chose: close as resolved.** Session marked `resolved` (see Final Closure section above). No code changes this round -- documentation-only closure.

## CHECKPOINT REACHED (round 4 -- ANSWERED, superseded by round 5's checkpoint, itself now superseded by round 6's checkpoint at the end of this file)

**Status:** This checkpoint's response is what triggered round 5 (see Evidence timestamped 2026-08-28T13:50:00Z onward, Eliminated, and root_cause/fix above). Original ask preserved below for historical record only -- do not act on it; see the end of this file for the current, live checkpoint.

**Root causes confirmed as of round 4:**
1. ApplyTopology(Extend) cannot express an explicit desired active-path-set (decompile-confirmed) -- fixed by making scoped ApplyPathInfos swap-aware and using it unconditionally (fix A).
2. The reactive watchdog was one-directional and never armed on the toggle path -- fixed by making it bidirectional and arming it on both the tile AND toggle paths (fix B).
3. Round 3's own fix (fix A) produced an ApplyPathInfos array the CCD API can reject outright when the swap's disable-set consumes every other active survivor -- fixed by keeping excluded survivors present-but-inactive in the array and normalizing the active set's origin/primary positioning (fix C).

**Round-4 self-verified checks (superseded):**
- Full solution build: 0 warnings, 0 errors, all 6 projects.
- Full RigToggle.Tests suite: 209/209 passed, no regressions to any existing routing/ordering test.
- RigToggle.Windows.Tests builds cleanly including 6 new PromoteToOriginIfNeeded unit tests (cannot execute in this sandbox -- missing Windows Desktop runtime -- but logic hand-traced against the exact round-4 repro).

## New Evidence (2026-08-28, fourth rig test)

Round 3's fix has its own failure mode: excluding the swap's disable-set entirely from the scoped plan's path array produces an INVALID array (single-path, target-only) that the CCD API rejects outright with PathChangeException: Invalid paths information -- reproduced at app startup (a mode-restore ActivateMonitors(SAM748A, disableSet=[ACI24A4,DELA0B8]) call, before any user action). Falls back to Extend, which is imprecise (established in round 2/3 evidence), so the whole startup mode-restore failed with "Monitor enable did not take effect."

Pattern across rounds: round 1 (scoped plan includes ALL survivors + target -- too many paths, transient over-full topology, unreliable/reverts or PathChangeException on multi-target case) -> round 3 (scoped plan excludes disable-set entirely -- too few paths for single-target case, PathChangeException on invalid array shape) -> Extend fallback in both cases is imprecise (can reactivate unrelated already-disabled monitors, confirmed via decompilation it passes zero paths/modes to SetDisplayConfig).

Also observed (11:25:40.189-40.690): the bidirectional TryReactivelyCorrectAgainstLastIntent guard (round 3's second fix) DOES now fire on a wentInactive drift (this is new, correct behavior -- previously silent), but its own correction attempt then hits the SAME two path-array problems recursively (DeactivateMonitors: "every currently active display is in the requested set, none would survive" on one attempt, then ActivateMonitors failing via the same Extend-imprecision on the retry) -- so the guard fix is directionally correct but inherits the underlying scoped-plan/Extend path-array bug, it doesn't independently resolve anything.

## Current Focus (round 4)

reasoning_checkpoint:
  hypothesis: "Round 3's scoped-activation plan omitted the swap's disable-set entirely from the ApplyPathInfos array, which the CCD API can reject outright (PathChangeException) because it violates two independently-researched SetDisplayConfig requirements: (1) a path being deactivated must remain PRESENT in the supplied array with DISPLAYCONFIG_PATH_ACTIVE cleared and its mode index invalidated, never omitted; and (2) exactly one ACTIVE path in the array must sit at the desktop origin (0,0) -- GDI's primary-monitor contract. Round 3's plan could violate (2) whenever the disable-set consumed every other active survivor, leaving the sole newly-activated target at its own stale, non-origin cached position from when it was previously non-primary -- exactly the round-4 repro (SAM748A, disableSet=[ACI24A4,DELA0B8])."
  confirming_evidence:
    - "Decompiled WindowsDisplayAPI 1.3.0.13's PathInfo.GetDisplayConfigPathInfos (ilspycmd, direct code read): each DisplayConfigPathInfo entry's Active flag is set per-target from pathTargetInfo.IsPathActive -- confirming a PathInfo CAN represent an explicitly-inactive path (target present, flag clear) as a structurally distinct, first-class case from 'absent from the array entirely'. Not an assumption -- read directly from the marshalling code that builds the native array ApplyPathInfos/ValidatePathInfos actually submits."
    - "Web research (OSR Developer Community CCD thread + a SetDisplayConfig usage write-up, independent sources corroborating each other): 'you do NOT remove the DISPLAYCONFIG_PATH_INFO structure from the array entirely -- instead you modify its flags member to remove DISPLAYCONFIG_PATH_ACTIVE for the monitor you want to disable' and 'for inactive paths, unused mode indices should be initialized to DISPLAYCONFIG_PATH_MODE_IDX_INVALID' -- both point directly at round 3's omission being the array-shape defect, not merely an under-populated array."
    - "Web research (Microsoft's own Desktop-Layout driver documentation + blog.lohr.dev 'Changing the primary display on Windows by code', an independent real-world SetDisplayConfig usage write-up): 'the source surface located at (0,0) is always the primary source surface' and the concrete pattern for changing primary is 'set the new primary's position to (0,0), then move every OTHER source by the negative of the OLD primary's position' -- the exact mirror-image of the uniform-shift algorithm DeactivateMonitors already implements in this same file for the deactivation direction, now confirmed (not just inferred by analogy) as the correct pattern for activation too."
    - "Round 4's own rig log (already in file, New Evidence section above): the failing call's array was reproducibly a single entry (target-only) -- consistent with BOTH researched violations simultaneously, since excluding the entire disable-set with no other active survivor removes the array's only possible primary candidate at the same time it under-populates the array."
  falsification_test: "If a rig trial reproducing the exact round-4 starting condition (app-startup mode-restore, or an equivalent manual Rig-mode toggle from a state where every desktop monitor is independently already disabled) still throws PathChangeException, or throws a DIFFERENT validation error after this fix, both researched mechanisms are refuted (or at minimum insufficient) and the true CCD validation requirement is something this sandbox's research could not surface -- the next round would need to fall back to raw DISPLAYCONFIG_PATH_INFO struct dumps captured from a real GetAllPaths() query on the actual rig hardware to see what a genuinely-accepted 'topology-restore' array looks like, rather than reasoning from documentation alone."
  fix_rationale: "TryBuildScopedActivationPlan now keeps every swap-excluded survivor IN the array as an explicit, mode-blanked, reflection-patched-inactive PathInfo (addressing violation 1) instead of omitting it, and a new pure helper, PromoteToOriginIfNeeded, normalizes the resulting active-entry set so exactly one path sits at (0,0) whenever none already does (addressing violation 2) -- both directly targeting the two specific, independently-researched CCD requirements round 3's plan violated, not a speculative restructuring. The fix is additive/no-op-safe for every case round 3 already had working: an empty monitorSwapDisableSet produces an empty explicitlyInactiveDisableSet list (identical array shape to before), and PromoteToOriginIfNeeded is a no-op whenever an existing active survivor is already at (0,0) -- true for every non-swap and partial-swap case, since something is always still active and correctly positioned unless the ENTIRE prior active set was excluded."
  blind_spots:
    - "Cannot execute or verify on real Windows CCD hardware in this sandbox -- same structural limitation as every prior round in this file. The two mechanisms are well-researched (multiple independent, corroborating sources) but not rig-confirmed as the SPECIFIC cause of THIS array's rejection -- SetDisplayConfig's exact validation logic is undocumented in full and could have additional, still-unresearched requirements this fix does not address."
    - "PromoteToOriginIfNeeded's 'prefer promoting a kept survivor over a brand-new target' choice, when BOTH have cached (but non-origin) positions from unrelated prior arrangements, produces a plausible-but-unverified relative layout (see class-level remarks in WindowsMonitorController.cs) -- no rig log yet exercises this exact mixed-swap sub-case (round 4's own repro is the 'no kept survivors at all' case where this ordering choice is moot)."
    - "Symptom 2 part 3's still-open item from rounds 2/3 (the delayed ~2-3s silent revert, and whether the bidirectional watchdog's own correction attempt inherits this round's fix or hits a NEW variant of the array-shape problem) is UNCHANGED by this round -- round 4's own evidence showed the watchdog's correction attempt recursively hit the same two path-array problems this fix targets, so it should benefit from the same fix, but this is inferred, not separately re-verified this round."
  candidate_causes:
    - "code (fix target): TryBuildScopedActivationPlan's round-3 exclusion dropped disable-set paths from the array entirely instead of keeping them present-but-inactive."
    - "code (fix target, distinct mechanism): TryBuildScopedActivationPlan (round 1 through round 3) never normalized the resulting active-entry set's primary/origin positioning -- a latent gap that round 1's and round 3's non-degenerate-array test shapes happened not to expose, but round 4's 'entire disable-set consumes every survivor' shape did."
    - "environment/CCD-validation: SetDisplayConfig's own undocumented full validation logic may have requirements beyond the two researched here (array-shape presence + single-origin) -- not eliminated, the fix corrects for the two specific, well-evidenced mechanisms without claiming to have exhaustively reverse-engineered SetDisplayConfig's validator."
  and_gate: "yes -- both code-level fixes (keep-present-but-inactive AND origin-normalization) are needed simultaneously for the round-4 repro shape specifically (single target, entire disable-set consumed all survivors): fixing only the array-presence issue without origin-normalization would still submit a topology with no path at (0,0) and could still fail; fixing only origin-normalization without keep-present-but-inactive would still submit a too-small/degenerate array. Each fix is independently justified by its own distinct researched CCD requirement (not because either alone was assumed insufficient), matching this file's own established and_gate convention from round 2/3."

next_action: "Implemented and self-verified (build + full adjacent test suite + new unit tests for the pure PromoteToOriginIfNeeded seam). Awaiting rig verification via the human-verify checkpoint at the end of this file -- re-test all repro shapes from the checkpoint's 6-step list, especially step 6 (the round-4-specific repro: app restart / mode-restore from a state where every desktop monitor is already independently disabled, then entering Rig mode)."
status: awaiting_human_verify (historical, round 4 -- see top of file for round 5's live status)

## CHECKPOINT REACHED (round 5 -- ANSWERED, superseded by round 6's checkpoint at the end of this file)

**Status:** This checkpoint's response is what triggered round 6 (see Evidence timestamped 2026-08-28T14:30:00Z onward, and root_cause/fix above). Original ask preserved below for historical record only -- do not act on it; see the end of this file for the current, live checkpoint.

**Type:** human-verify
**Debug Session:** .planning/debug/monitor-position-resets-to-de.md

### Investigation State

**Root causes confirmed across all five rounds (see Resolution.root_cause for full detail):**
1. Symptom 1 (position reset): position-mode cache. RESOLVED, rig-confirmed since round 1.
2. Symptom 2 part A: ApplyTopology(Extend) structurally cannot express an explicit active-path-set -- fixed by swap-aware scoped ApplyPathInfos (fix A).
3. Symptom 2 part B: a delayed (~2-3.5s) silent OS-level revert of an apparently-successful CCD apply -- **STILL UNRESOLVED after five rounds.** Round 5 adds pure observability instrumentation (fix E) to gather the evidence needed to root-cause this; it does not claim to fix it.
4. Symptom 2 part C: round 3's array-shape defect (PathChangeException) -- fixed by present-but-inactive representation + origin-promotion (fix C, round 4). NOT refuted by round 5's rig test.
5. **NEW this round:** the reactive watchdog's own bidirectional correction (fix B) handled a mixed-direction drift event as two separate, order-dependent CCD calls, which could hit DeactivateMonitors' zero-survivors guard and then fail in one of two ways -- fixed by making it one atomic, swap-aware call (fix D).

### Checkpoint Details

**Need verification:** confirm fix D holds on real rig hardware for the mixed-direction drift scenario, AND capture whatever fix E's new ObservePostApplyStability instrumentation logs the next time Symptom 2 part B reproduces (it likely will -- this round does not claim to have fixed it). This round's most valuable outcome may be the debug.log data itself, not a "confirmed fixed" report.

**Self-verified checks:**
- Full solution build: 0 warnings, 0 errors, all 6 projects.
- Full RigToggle.Tests suite: 209/209 passed, no regressions to any existing routing/ordering test.
- RigToggle.Windows.Tests builds cleanly (no new unit-testable seam this round -- fix D is WinForms UI layer, fix E is a background-thread CCD poller, neither has an injectable seam per this file's established constraint).
- Direct code read confirms fix D is byte-behavior-identical to before for both single-direction drift cases (reactivated-only, wentInactive-only); only the previously-broken mixed-direction case changes.

**How to check:**
1. Repeat the SAME two scenarios from this round's rig trial that produced the mixed-direction drift: (a) toggle to Rig mode from the everyday desktop configuration and simply wait/watch for 10+ seconds after the toggle completes (Symptom 2 part B will likely still revert the topology on its own -- that is EXPECTED and not a fix-D failure, see below); (b) if/when it does revert, watch what the app's reactive correction does next -- with fix D, it should now be ONE clean correction (no "Cannot disable all configured monitors" exception, no transient 3-active-monitor state, no PathChangeException/Extend-fallback) that lands back on the correct Rig-mode topology.
2. Repeat step 3 from the round-4 checklist too (independently disable one desktop monitor via its tile, THEN toggle to Rig mode) -- same expectation: if Symptom 2 part B causes a revert afterward, the correction that follows should be clean and atomic.
3. **Most important this round:** regardless of whether Symptom 2 part B reproduces, please copy the FULL debug.log covering the session (not just the moment of failure) -- fix E's `ObservePostApplyStability` lines (look for that string) will show a fine-grained, timestamped trace of the active-path set for 6 seconds after every successful monitor action, which is the evidence this investigation has been missing for four rounds to actually root-cause the revert itself.
4. Quick regression check: disable a monitor via its tile, re-enable it via the tile, confirm its position is preserved (Symptom 1, should be unaffected, unrelated to this round's changes).

**Tell me:** describe what happened at each step, and please attach/paste the debug.log excerpt covering the whole session (including any `ObservePostApplyStability` lines) even if everything appeared to work -- this round's fix D can be confirmed or refuted from the mixed-direction-drift behavior alone, but Symptom 2 part B's actual root cause can only come from what fix E's instrumentation captures.

## CHECKPOINT REACHED (round 6 -- ANSWERED, superseded by round 7's checkpoint at the end of this file)

**Status:** This checkpoint's response is what triggered round 7 (see Evidence timestamped 2026-08-28T16:00:00Z onward, and root_cause/fix above). Original ask preserved below for historical record only -- do not act on it; see the end of this file for the current, live checkpoint.

**Type:** human-verify
**Debug Session:** .planning/debug/monitor-position-resets-to-de.md
**Progress:** 25 evidence entries, 6 eliminated hypotheses (running totals across all six rounds).

### Investigation State

**Root causes confirmed across all six rounds (see Resolution.root_cause for full detail):**
1. Symptom 1 (position reset): position-mode cache. RESOLVED, rig-confirmed since round 1.
2. Symptom 2 part A: ApplyTopology(Extend) structurally cannot express an explicit active-path-set -- fixed by swap-aware scoped ApplyPathInfos (fix A).
3. Symptom 2 part B: a delayed (~2-3.5s) silent OS-level revert of an apparently-successful CCD apply -- **STILL UNRESOLVED after six rounds.** No fix attempted this round. Round 6's checkpoint response added one narrow, real refinement (at least one observed revert was a mixed-direction event, ruling out "undoes only the single most recent apply" as a complete description) but the accompanying "always reverts to the same desktop pair" observation is underdetermined given this rig's fixed 3-monitor hardware -- it does not discriminate between competing external-mechanism hypotheses. Not re-researched further this round (round 5 already researched the SDC_SAVE_TO_DATABASE angle specifically and found it inconclusive; no new angle has emerged).
4. Symptom 2 part C: round 3's array-shape defect (PathChangeException) -- fixed by present-but-inactive representation + origin-promotion (fix C, round 4). Unchanged this round.
5. Symptom 2 part D: the reactive watchdog's mixed-direction correction was two separate, order-dependent CCD calls -- fixed by one atomic, swap-aware call (fix D, round 5). **NOT refuted by round 6's rig test** -- every reactive correction in the trial completed with EXIT success, no exception.
6. **NEW this round:** fix E's own ObservePostApplyStability instrumentation (round 5) could have its entire 6-second observation window silently killed by a single transient exception during any one poll tick, because its try/catch was scoped to the whole background-thread body instead of per-tick -- rig-confirmed (a window died at +898ms). Fixed (fix F) by moving the try/catch to wrap only the per-tick query, so a transient failure now skips just that tick and polling continues.
7. **NEW this round:** MainForm.RefreshMonitorTiles' pre-existing no-retry catch on a transient GetAllMonitors() failure, combined with the reactive watchdog's dependency on RefreshMonitorTiles' output for drift detection, meant a transient query failure produced a FALSE "no drift detected" verdict instead of "could not check this time" -- silently skipping a real correction opportunity exactly when it coincided with a closing guard window (rig-confirmed). Fixed (fix G) by adding one retry after a short backoff before falling back to the pre-existing degrade path.

### Checkpoint Details

**Need verification:** confirm fixes F and G hold on real rig hardware (F: no more truncated ObservePostApplyStability windows; G: no more false "no drift detected" verdicts coinciding with a transient query failure), AND continue capturing fix E's instrumentation data (now more reliable thanks to fix F) toward eventually root-causing Symptom 2 part B itself, which this round explicitly does not attempt to fix. As with round 5, this round's most valuable outcome may again be the debug.log data itself rather than a "confirmed fixed" report -- Symptom 2 part B is expected to keep reproducing until its actual external trigger is found.

**Self-verified checks:**
- Full solution build: 0 warnings, 0 errors, all 6 projects.
- Full RigToggle.Tests suite: 209/209 passed, no regressions to any existing routing/ordering test.
- RigToggle.Windows.Tests builds cleanly (no new unit-testable seam this round -- fix F is a background-thread CCD poller, fix G is WinForms UI layer, neither has an injectable seam per this file's established constraint).
- Direct code read + hand-trace confirms both fixes are byte-behavior-identical to before for the success path (no exception at all); only the previously-broken transient-failure cases change.

**How to check:**
1. Reproduce Symptom 2 part B at least once this session (any of the established repro shapes from prior rounds' checklists will do) and let the topology settle/revert/correct through its full natural cycle -- with fix F, every `ObservePostApplyStability` block in the log should now run for its FULL 6 seconds and end with an explicit "observation window closed at +6000ms" (approximately) line, even if it also logs one or more "poll tick ... failed ... skipping this tick" lines along the way. If a window still ends early with no "observation window closed" line, fix F did not hold -- please note the exact timestamp/offset where it stopped.
2. Watch for any moment where `MainForm.TryReactivelyCorrectAgainstLastIntent` logs "no unexpected drift detected in either direction" immediately after (or very close in time to) a `MainForm.RefreshMonitorTiles: GetAllMonitors failed` line in the log -- with fix G, a "retrying once after backoff" line should appear in between, and (if the retry succeeds) the drift check afterward should be based on the RETRIED read, not silently based on an empty list. If you see "GetAllMonitors failed" with NO retry line following it, fix G did not engage as expected -- please note the exact timestamp.
3. **Most important, unchanged from round 5:** please copy the FULL debug.log covering the session (not just the moment of failure) again this round -- now that fix F should keep every `ObservePostApplyStability` window intact for its full duration, this is the best chance yet across six rounds to see the revert's complete, uninterrupted timing/shape.
4. Quick regression check: disable a monitor via its tile, re-enable it via the tile, confirm its position is preserved (Symptom 1, should be unaffected, unrelated to this round's changes).

**Tell me:** describe what happened at each step, and please attach/paste the debug.log excerpt covering the whole session (including every `ObservePostApplyStability` block in full, and any "poll tick ... failed" or "GetAllMonitors failed, retrying" lines) even if everything appeared to work. This round's fixes F and G can be confirmed or refuted directly from the log's own shape (a window running its full course; a retry line appearing before a drift check); Symptom 2 part B's actual root cause still depends entirely on what a now-more-reliable fix E captures the next time it reproduces.

## CHECKPOINT REACHED (round 7 -- ANSWERED, session CLOSED as resolved -- see Final Closure section above)

**Status:** This checkpoint's response (eighth rig test) is what triggered round 8's closure (see Evidence timestamped 2026-08-28T16:58:00Z onward, and the Final Closure section above). Original ask preserved below for historical record only -- do not act on it. THIS DEBUG SESSION IS RESOLVED; there is no further live checkpoint. To reopen, use `/gsd-debug continue monitor-position-resets-to-de`.

**Type:** human-verify
**Debug Session:** .planning/debug/monitor-position-resets-to-de.md
**Progress:** 30 evidence entries, 6 eliminated hypotheses (running totals across all seven rounds).

### Investigation State

**Root causes confirmed across all seven rounds (see Resolution.root_cause for full detail):**
1. Symptom 1 (position reset): position-mode cache. RESOLVED, rig-confirmed since round 1.
2. Symptom 2 part A: ApplyTopology(Extend) structurally cannot express an explicit active-path-set -- fixed by swap-aware scoped ApplyPathInfos (fix A).
3. Symptom 2 part B: a delayed silent OS-level revert of an apparently-successful CCD apply -- **STILL UNRESOLVED after seven rounds.** No fix attempted this round. This round independently re-confirmed fix D and fix F hold (direct raw-log trace, not secondhand) and added two refinements: a tight ~1.46-1.48s revert-timing observation across two independent windows in the same trial (tighter than prior rounds' looser ~2-3.5s estimates), and a periodic recurrence well outside any observation/guard window, including one lead where a monitor briefly vanished from CCD's own enumeration entirely (not merely went inactive) -- neither is yet actionable into a falsifiable hypothesis.
4. Symptom 2 part C: round 3's array-shape defect (PathChangeException) -- fixed by present-but-inactive representation + origin-promotion (fix C, round 4). Unchanged this round.
5. Symptom 2 part D: the reactive watchdog's mixed-direction correction was two separate, order-dependent CCD calls -- fixed by one atomic, swap-aware call (fix D, round 5). **Re-confirmed this round** via direct trace of the raw log (not secondhand) -- both mixed-direction drift events in this round's trial corrected cleanly.
6. Symptom 2 part E: fix E's own instrumentation could have its 6-second observation window silently killed by a per-tick exception -- fixed (fix F, round 6). **Re-confirmed this round** via direct trace of the raw log -- both observation windows this round ran their full 6-second course despite a mid-window CHANGE detection.
7. Symptom 2 part F: a transient GetAllMonitors() failure could produce a false "no drift detected" verdict -- fixed (fix G, round 6). **NOT exercised this round** -- no "GetAllMonitors failed" line appeared in this round's log; neither confirmed nor refuted.
8. **NEW this round:** ActivateMonitors' own correction loop and final verify-and-throw only ever defended against a device path GAINING an unwanted activation -- never against a pre-existing, unrequested survivor LOSING its activation as a side effect of the Extend fallback. Rig-confirmed: a plain non-swap tile-enable of ACI24A4 (SAM748A already active) had its scoped plan throw, fell back to Extend, and SAM748A silently dropped out permanently -- ActivateMonitors reported EXIT success with a WRONG final state, which then got baked into MainForm's own intent baseline, so the reactive watchdog could never catch it either. Fixed (fix H) by adding ComputeUnexpectedlyDeactivated, the mirror image of the existing ComputeUnexpectedlyActivated, wired into the same correction loop (a nested, non-swap-aware re-activation of exactly the dropped survivor) and the final verify-and-throw (now throws loudly, per D-05, if a survivor cannot be restored within the bounded correction rounds).

**Still genuinely open, NOT addressed this round:** (a) Symptom 2 part B's actual external trigger (item 3 above); (b) WHY the scoped ApplyPathInfos plan threw PathChangeException for the SAM748A+ACI24A4 pairing specifically while an apparently-identical-shaped SAM748A+DELA0B8 call succeeded cleanly moments later in the same trial -- multiple plausible mechanisms (a stale/incompatible GetAllPaths() source candidate; a hardware source/port-group constraint; a cached-mode mismatch), no discriminating evidence gathered this round, not guessed at.

### Checkpoint Details

**Need verification:** confirm fix H holds on real rig hardware -- specifically, that a plain, non-swap tile-enable action that causes the scoped plan to fail and fall back to Extend no longer permanently drops a different, pre-existing, unrequested-but-active monitor. This is the single most important thing to check this round, since round 7's fix targets a WRONG FINAL STATE bug (not merely a delayed revert), and this sandbox cannot exercise real CCD hardware to confirm it holds.

**Self-verified checks:**
- Full solution build: 0 warnings, 0 errors, all 6 projects.
- Full RigToggle.Tests suite: 209/209 passed, no regressions to any existing routing/ordering test.
- RigToggle.Windows.Tests builds cleanly including 5 new `ComputeUnexpectedlyDeactivated` unit tests (cannot execute in this sandbox -- missing Windows Desktop runtime -- but all 5 hand-traced against the implementation, and the full round-7 repro scenario hand-traced end-to-end through the modified ActivateMonitors control flow).
- Direct code read confirms fix H is byte-behavior-identical to before for every case where no previously-active survivor is ever unexpectedly dropped (the overwhelming common case) -- only the previously-broken "Extend fallback silently drops an unrequested survivor" case changes.
- Fixes D and F independently re-confirmed this round via direct trace of the raw log text you provided (not just accepted from the orchestrator's summary).

**How to check:**
1. **Most important:** try to reproduce the exact round-7 repro shape -- with the rig already in a state where SAM748A (or whichever monitor is currently your "rig monitor") is the only active display, enable a SECOND monitor via its dashboard tile (a plain, single-tile enable, not the Rig/Normal toggle button). If the scoped activation happens to throw and fall back to Extend (you'll see "scoped activation ApplyPathInfos threw" followed by "calling whole-topology PathInfo.ApplyTopology(Extend)" in the log), watch closely: with fix H, if any OTHER previously-active monitor (like SAM748A) gets dropped as a side effect, you should see a NEW "correction round .../... unexpectedlyDeactivated=[...]" log line followed by "nested ActivateMonitors(...) completed without throwing" -- and the tile dashboard should end up showing ALL of the originally-active monitors AND the newly-requested one active, not just the newly-requested one. If you instead see the app throw an error dialog ("Monitor enable did not take effect: ..."), that is fix H's D-05 escape hatch working as designed (a genuine failure being surfaced loudly) rather than a silent wrong state -- also acceptable, please note it either way.
2. If step 1 doesn't naturally reproduce the scoped-plan failure, that's fine -- just do a few ordinary tile enable/disable actions and toggles as usual; fix H is a no-op for every case where nothing unexpected gets dropped, so this just confirms no regression.
3. Reproduce Symptom 2 part B again if it occurs naturally during your session (still expected, still unfixed) -- no special action needed, just let it happen and capture the log as before.
4. Quick regression check: disable a monitor via its tile, re-enable it via the tile, confirm its position is preserved (Symptom 1, should be unaffected, unrelated to this round's changes).

**Tell me:** describe what happened at each step, especially whether the round-7 repro shape (a plain tile-enable that falls back to Extend while another monitor is already active) occurred, and whether that other monitor stayed active, got correctly restored, or the app threw a "did not take effect" error. As always, please attach/paste the full debug.log excerpt covering the whole session, including any "unexpectedlyDeactivated=" or "nested ActivateMonitors(...)" lines fix H adds, even if everything appeared to work.
