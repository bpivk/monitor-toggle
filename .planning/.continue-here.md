---
context: default
phase: 26-auto-update
task: null
total_tasks: null
status: in_progress
last_updated: 2026-08-28T09:28:47.339Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: monitor-swap-ccd-path-array — Two prior fix rounds for the Rig/Normal toggle bug each "worked" in self-verification (build+tests green) but failed on the rig with a NEW CCD API failure mode (round 1: too many paths in the scoped plan → unreliable/silent-revert or PathChangeException; round 3: too few paths (target-only) → PathChangeException outright). Do NOT trust build+test-green as sufficient confidence for this specific bug class — it requires rig verification against all 3 repro shapes (app-startup mode-restore, manual Rig<->Normal toggle, single-tile enable) held for 10+ seconds each, not just immediate post-apply state. — Structural mitigation: the checkpoint already returned by the debugger explicitly asks for 10+ second observation; honor that, do not shortcut to "confirmed" on an immediate-only check.

**Do not proceed until the box above is checked (i.e., understood).**

<current_state>
Phase 26 (Auto-Update) is functionally done: all 5 plans executed, merged, code-reviewed (2 Critical + 3 Warning findings all fixed), verified at code level (206/209+ tests pass), and pushed to origin/master (first real CI run for the whole v2.2 milestone — passed). Phase status is `human_needed` — 5 items in `.planning/phases/26-auto-update/26-UAT.md` need real-Windows-hardware confirmation before the phase can be marked complete.

Mid-session, the user reported two bugs: (1) monitor position resets to default on disable/re-enable, (2) Rig/Normal toggle doesn't complete properly. Routed through `/gsd-debug`, session file `.planning/debug/monitor-position-resets-to-de.md`. Symptom 1 is FIXED and operator-confirmed. Symptom 2 has been through 4 rig-tested rounds; root cause is narrowed to CCD `SetDisplayConfig` path-array semantics (not OS/driver flakiness — confirmed via decompilation and log analysis) but not yet resolved. Round 4's hypothesis (explicit-inactive path representation, not omission) is written up in the debug file but not yet implemented/tested.

The operator does NOT use git — they copy files manually from this /moza folder to a Windows machine, so stale/deleted files can linger on their side (already hit once this session with a leftover MonitorPanelForm.cs).
</current_state>

<completed_work>

- Phase 26 discuss → plan → execute → code-review(+fix) → verify chain: fully run, all artifacts committed.
- Pushed 130 commits (Phases 24-26) to origin/master; CI (windows-latest) ran and passed for the first time against this milestone.
- REQUIREMENTS.md UPDATE-05/UPDATE-06 marked complete.
- Todo captured: monitor position reset bug (led to the debug session below).
- Debug session `monitor-position-resets-to-de`: Symptom 1 root-caused (missing Position/Resolution/PixelFormat when `WindowsMonitorController.ActivateMonitors` reactivates a monitor via the scoped CCD path) and FIXED — operator confirmed on rig.
- Debug session Symptom 2: 4 rounds of hypothesis→fix→rig-test, each ruling out one mechanism:
  - Round 1: scoped-activation plan (all survivors + target) → unreliable, silent OS-level revert or PathChangeException on multi-target swaps.
  - Round 2: routed swaps to `ApplyTopology(Extend)` → imprecise, reactivates unrelated independently-disabled monitors (confirmed via decompiling WindowsDisplayAPI: Extend passes zero paths/modes to SetDisplayConfig, so Windows' own opaque algorithm picks the whole topology).
  - Round 3: scoped plan excluding disable-set entirely + made the reactive-correction guard (`TryReactivelyCorrectAgainstLastIntent`) bidirectional and armed on the toggle path too (previously only caught unwanted *reactivation*, never unwanted *deactivation*, and was never armed on the Rig/Normal toggle path at all — a real, now-fixed app-level gap) → still fails: a single-path (target-only) array is rejected by the CCD API as `PathChangeException: Invalid paths information`.
</completed_work>

<remaining_work>

- **Phase 26 UAT**: rig-test the 5 items in `26-UAT.md`, then run `/gsd-verify-work 26` to close out the phase.
- **Debug session round 5**: implement round 4's hypothesis — one explicit `ApplyPathInfos` call per swap, with the target path active (real/cached mode), the swap's disable-set paths marked EXPLICITLY INACTIVE within the same array (not omitted), and other already-active survivors included active with cached mode. Needs WindowsDisplayAPI `PathInfo`/`DisplayConfigPathInfo` struct-level research on how to represent "present but explicitly inactive" — may require dropping to raw `DISPLAYCONFIG_PATH_INFO` flag manipulation if the wrapper library doesn't expose this. Rig-test against all 3 repro shapes (startup mode-restore, manual toggle, single-tile enable), each held 10+ seconds.
</remaining_work>

<decisions_made>

- Pushed the 130-commit backlog to origin/master rather than leaving it local — CI had never executed against Phases 24-26 otherwise.
- REQUIREMENTS.md checkboxes track implementation+unit-test completeness, not full rig verification — kept consistent with UPDATE-01..04's existing precedent.
- Debug-session fix code stays uncommitted until the operator confirms it works on the rig (established project convention — see `.planning/debug/resolved/monitor-enable-reactivates-others-again.md`).
</decisions_made>

<blockers>
- Symptom 2 (Rig/Normal toggle): open, CCD path-array semantics not yet solved. No workaround — resume via `/gsd-debug continue monitor-position-resets-to-de`.
</blockers>

## Required Reading (in order)
1. `.planning/debug/monitor-position-resets-to-de.md` — full evidence trail for both symptoms; read the "Current Focus (round 4)" section specifically before touching monitor-control code again.
2. `.planning/phases/26-auto-update/26-UAT.md` — Phase 26's pending human-verify items.
3. `.planning/phases/26-auto-update/26-VERIFICATION.md` — why Phase 26 is `human_needed`, not `passed`.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Treating "build succeeds + unit tests pass" as sufficient confidence for a CCD/display-topology fix → Structural mitigation: this class of bug only manifests on real Windows hardware, often with a multi-second delay after an apparently-successful apply. Always ask the operator to hold/observe 10+ seconds post-apply, and test all 3 repro shapes (startup restore, manual toggle, single tile), not just one.
- [ANTI-PATTERN]: Assuming an operator's stale/missing local file means the repo is broken → Structural mitigation: the operator syncs by manual copy, not git; ask about their sync method before assuming a codebase-level regression when a build error references a file this repo doesn't have.

## Infrastructure State
- git: `master` pushed to `origin/master` at commit `3f4af6f`+ (debug-session doc commits may have landed after; check `git log -1`). Working tree has uncommitted monitor-control fix code (round 3, doesn't fully work) — expected, do not commit until round 5 lands and is rig-confirmed.
- CI: windows-latest Build workflow passing as of the last push.

<context>
This was a long session: full Phase 26 GSD pipeline (discuss/plan/execute/review/verify), then an extended live-debugging thread with the operator pasting real `debug.log` excerpts after each fix attempt. The debugging has been genuinely productive — real root causes found and partially fixed, not guesswork — but the underlying CCD API (`SetDisplayConfig`/`ApplyPathInfos`) has turned out to have non-obvious path-array validity rules that took several rig-tested iterations to characterize. Round 4's direction (explicit-inactive path state) is a reasoned next step, not yet tried.
</context>

<next_action>
Start with: `/gsd-debug continue monitor-position-resets-to-de` to resume Symptom 2's investigation at round 5, using round 4's hypothesis as the starting point. Independently, whenever the operator has rig time, walk through `26-UAT.md`.
</next_action>
