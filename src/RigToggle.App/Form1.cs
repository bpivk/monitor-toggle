namespace RigToggle.App
{
    // Template placeholder from the WinForms scaffold (dotnet new winforms).
    // Plan 05 replaces this with MainForm; kept as-is here solely so the
    // App project compiles during Wave 2 (02-01-PLAN.md Task 2 action).
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }
    }
}
